% AUTO-GENERATED by replication/scripts/gen_appendix_C.py
% Source: sp.function_schema('callaway_santanna') at v1.30.1.
\begin{lstlisting}[language=,basicstyle=\ttfamily\scriptsize,
                   frame=single,framesep=4pt,framerule=0pt,
                   backgroundcolor=\color{codebg!60!white},
                   breaklines=true,
                   caption={Live JSON schema emitted by
                   \code{sp.function\_schema("callaway\_santanna")}
                   at \statspai{} 1.30.1 (6,919 characters,
                   reproduced verbatim).}]
{
  "name": "callaway_santanna",
  "description": "Callaway-Sant'Anna (2021) staggered DID with group-time ATTs. Robust to heterogeneous treatment effects and staggered adoption. Validation: certified evidence with scoped limitations. Known limitations: clustervars is not yet supported with bstrap=False; the analytic standard errors do not account for within-cluster dependence, so the multiplier bootstrap is required. Do NOT use when: all treated units adopt in the same period (a plain 2x2 or block design) \u2014 CS reduces to the same estimand at much higher cost; use sp.did(method='2x2'); there is neither a never-treated group nor a not-yet-treated cohort \u2014 no valid comparison group exists for ATT(g,t); only one pre-treatment period per cohort \u2014 no pre-trend evidence and no event-study leads are estimable. Cost: Runtime scales with (number of cohorts x number of periods): one doubly-robust 2x2 estimate plus an influence function per (g,t) cell. Memory is O(n) \u2014 the hazard is wall-clock on many-cohort panels, not RAM.",
  "parameters": {
    "type": "object",
    "properties": {
      "data": {
        "description": "pandas DataFrame containing the variables used by the estimator.",
        "type": "string"
      },
      "notyet_cutoff": {
        "description": "Which date a control must still be untreated at when control_group='notyettreated': 'period' (G > max(t, base) + anticipation; R did), 'asinr' (G > t; csdid asinr) or 'cohort' (G > max(t, g); csdid default).",
        "type": "string",
        "enum": [
          "period",
          "asinr",
          "cohort"
        ],
        "default": "period"
      },
      "weights": {
        "description": "Unit-level sampling/population weights omega (R did::att_gt(weightsname=), Stata csdid [aweight=]). NOT a precision knob: omega enters the definition of the target parameter. Unweighted answers 'the average effect across treated units'; weighted answers 'across the population those units represent'. The two are different estimands and can differ in sign, so comparing them is not a robustness check. Must be constant within unit.",
        "type": "string"
      },
      "pscore_trim": {
        "description": "Drop control units whose estimated propensity score reaches this cutoff, matching DRDID's trim.level. Pass 1.0 to disable.",
        "type": "number",
        "default": 0.995
      },
      "pretest": {
        "description": "Report the joint pre-trend Wald test ('joint') or skip it ('none').",
        "type": "string",
        "enum": [
          "joint",
          "none"
        ],
        "default": "joint"
      },
      "pretest_periods": {
        "description": "Restrict the pre-trend test to the k event times closest to treatment.",
        "type": "integer"
      },
      "se_method": {
        "description": "Shared DiD spelling for the inference procedure: 'analytic', 'multiplier' (aka 'wboot'), or 'auto'. Synonym layer over bstrap=; passing both raises.",
        "type": "string",
        "enum": [
          "analytic",
          "multiplier",
          "auto"
        ]
      },
      "y": {
        "description": "Outcome variable",
        "type": "string"
      },
      "g": {
        "description": "First-treatment-period column (0 = never-treated)",
        "type": "string"
      },
      "t": {
        "description": "Time period column",
        "type": "string"
      },
      "i": {
        "description": "Unit identifier",
        "type": "string"
      },
      "x": {
        "description": "Covariates for conditional parallel trends. Either column names ['lpop'] or an R-style one-sided formula '~ lpop + I(lpop**2)' (R did/csdid xformla; a left-hand side is accepted and ignored). Inside I() the expression is Python, so powers are I(x**2); I(x^2) is rejected rather than read as bitwise XOR.",
        "type": "array",
        "items": {
          "type": "string"
        }
      },
      "estimator": {
        "description": "2x2 estimator",
        "type": "string",
        "enum": [
          "dr",
          "ipw",
          "reg"
        ],
        "default": "dr"
      },
      "control_group": {
        "description": "Control group",
        "type": "string",
        "enum": [
          "nevertreated",
          "notyettreated"
        ],
        "default": "nevertreated"
      },
      "base_period": {
        "description": "Base period",
        "type": "string",
        "enum": [
          "universal",
          "varying"
        ],
        "default": "universal"
      },
      "anticipation": {
        "description": "Number of anticipation periods",
        "type": "integer",
        "default": 0
      },
      "alpha": {
        "description": "Significance level for confidence intervals and tests.",
        "type": "number",
        "default": 0.05
      },
      "panel": {
        "description": "False means repeated cross-sections",
        "type": "boolean",
        "default": true
      },
      "allow_unbalanced_panel": {
        "description": "How to handle units missing some periods when panel=True. False differences within unit anyway, so a unit missing the base or comparison period drops out of that cell. True switches to the repeated-cross-section estimators, keeping every observed row and folding the influence functions back to the unit level (R did::att_gt(allow_unbalanced_panel=TRUE)); inert when the panel is in fact balanced. The two give different estimates on the same data \u2014 an estimator choice, not a tuning knob.",
        "type": "boolean",
        "default": false
      },
      "clustervars": {
        "description": "Cluster variable(s) for the multiplier bootstrap; unit id implied, at most one extra time-invariant variable (R did::att_gt clustervars / Stata csdid cluster())",
        "type": "array",
        "items": {
          "type": "string"
        }
      },
      "bstrap": {
        "description": "Multiplier-bootstrap SEs per ATT(g,t) instead of analytic (R did / Stata csdid wboot)",
        "type": "boolean",
        "default": false
      },
      "biters": {
        "description": "Bootstrap replications",
        "type": "integer",
        "default": 1000
      },
      "cband": {
        "description": "Uniform (sup-t) confidence bands across all ATT(g,t); requires bstrap=True",
        "type": "boolean",
        "default": false
      },
      "boot_weight_type": {
        "description": "Multiplier weight distribution (rademacher matches R did's implementation)",
        "type": "string",
        "enum": [
          "rademacher",
          "mammen"
        ],
        "default": "rademacher"
      },
      "random_state": {
        "description": "Random seed or RandomState for reproducible stochastic steps.",
        "type": "integer"
      }
    },
    "required": [
      "data",
      "y",
      "g",
      "t",
      "i"
    ]
  }
}
\end{lstlisting}
