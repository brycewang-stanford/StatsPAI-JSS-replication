# R package versions backing the reference-parity fixtures

The frozen reference values in `tests/reference_parity/_fixtures/*_R.json` were
generated by the `_generate_*.R` scripts against the **specific R package
versions recorded below**. A parity test compares StatsPAI to these frozen
numbers, so if an upstream R package changes its algorithm, the frozen fixture
can quietly become stale while the test still passes against the old number.
This manifest makes that risk visible: when regenerating fixtures, record the
versions you used here, and if they differ from the row below, re-verify that
the new R output still matches the old before overwriting the fixture.

- **R**: `4.5.2`
- **Generated/verified**: 2026-06-30
- **Platform**: macOS arm64

| package | version |
| --- | --- |
| `AER` | 1.2.16 |
| `bcf` | 2.0.2 |
| `data.table` | 1.18.2.1 |
| `did` | 2.3.0 |
| `did2s` | 1.2.1 |
| `didimputation` | 0.5.1 |
| `DoubleML` | 1.0.2 |
| `etwfe` | 0.6.2 |
| `fixest` | 0.14.0 |
| `glmnet` | 4.1.10 |
| `grf` | 2.6.1 |
| `hdm` | 0.3.2 |
| `jsonlite` | 2.0.0 |
| `lmtest` | 0.9.40 |
| `MASS` | 7.3.65 |
| `mlr3` | 1.6.0 |
| `mlr3learners` | 0.14.0 |
| `plm` | 2.6.7 |
| `pretrends` | 0.1.0 (GitHub: jonathandroth/pretrends -- not on CRAN) |
| `pscl` | 1.5.9 |
| `quantreg` | 6.1 |
| `sandwich` | 3.1.1 |
| `survival` | 3.8.3 |
| `synthdid` | 0.0.9 |

## Known divergence: `did` 2.3.0 clustered multiplier bootstrap

`did` 2.3.0 aggregates the influence function to cluster **means** over
`n_clusters` when `clustervars=` is passed with `bstrap=TRUE`
(`rowsum(inf.func, cluster)/cluster_n`, then `se <- bSigma/sqrt(n_clusters)`).
That equals the cluster-robust variance only when every cluster is the same
size; otherwise each cluster enters with weight `1/|c|` and the small clusters
dominate. Upstream `did` (GitHub master, post-2.3.0) switched to cluster
**sums** with `se <- bSigma * sqrt(n_clusters)/n`, and the `csdid` Python port
tracks the corrected form.

**StatsPAI deliberately follows the corrected upstream form, not 2.3.0.** So
`_generate_cs_gaps_R.R` records only the *unclustered* bootstrap — identical in
both — and the clustered path is covered by property-based tests in
`test_cs_gaps_parity.py` instead. Do not "fix" a clustered parity gap against
2.3.0 by changing StatsPAI; check which convention the installed `did` uses
first. When `did` releases the master behaviour, a clustered reference can be
pinned here directly.

## Regenerating a fixture

```bash
Rscript tests/reference_parity/_fixtures/_generate_<name>.R
```

After regenerating:

1. Confirm the produced numbers match the committed `*_R.json` within the
   documented tolerance (see `REFERENCES.md`). If they do **not**, an upstream R
   package changed — investigate before overwriting, and note the version bump
   here.
2. Update the version row above if you used a newer R / package set.
3. A fixture change that moves a StatsPAI parity number is a numerical change —
   treat it under the correctness-fix process in
   [`docs/guides/reproducibility.md`](../../docs/guides/reproducibility.md).
