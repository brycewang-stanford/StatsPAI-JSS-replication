[
  {
    "description": "OLS regression with robust/clustered standard errors. The workhorse of econometric analysis. Validation: certified parity evidence.",
    "name": "regress",
    "parameters": {
      "properties": {
        "cluster": {
          "description": "Column name for cluster-robust SEs",
          "type": "string"
        },
        "data": {
          "description": "pandas DataFrame with variables",
          "type": "string"
        },
        "formula": {
          "description": "R-style formula, e.g. 'y ~ x1 + x2'",
          "type": "string"
        },
        "robust": {
          "default": "nonrobust",
          "description": "Standard error type",
          "enum": [
            "nonrobust",
            "hc0",
            "hc1",
            "hc2",
            "hc3",
            "hac"
          ],
          "type": "string"
        },
        "vcov": {
          "description": "pyfixest/fixest-style variance specification, accepted so one spelling works across sp.regress, sp.ivreg and sp.feols: 'iid' / 'hetero' / 'HC0'-'HC3', or a cluster dict such as {'CRV1': 'firm'} (CRV2 / CRV3 select the matching small-sample correction). Mutually exclusive with robust=/cluster=.",
          "type": "string"
        }
      },
      "required": [
        "formula",
        "data"
      ],
      "type": "object"
    }
  },
  {
    "description": "Unified IV estimation and the entry point for the whole IV family: k-class (2SLS, LIML, Fuller, GMM, JIVE) plus the many-weak-instrument jackknife variants (jive1, ujive, ijive, rjive, jive_mw), rigorous/post-Lasso instrument selection (lasso, rlasso, post_lasso), marginal treatment effects (mte) and their MST sharp bounds (ivmte_bounds), plausibly-exogenous sensitivity (plausibly_exog_ltz / plausibly_exog_uci), nonparametric and ML variants (npiv, kernel, ivdml, deepiv), quantile IV (ivqreg), Bayesian IV (bayes), continuous-instrument LATE (continuous_late), many-weak-IV Anderson-Rubin (many_weak_ar) and shift-share (shift_share). Includes first-stage F, Sargan/Hansen J, Kleibergen-Paap rk, Sanderson-Windmeijer per-endog F and Hausman diagnostics. Validation: certified parity evidence.",
    "name": "iv",
    "parameters": {
      "properties": {
        "augmented_diagnostics": {
          "default": true,
          "description": "When the method returns EconometricResults, attach the Kleibergen-Paap rk statistic, Sanderson-Windmeijer per-endogenous F and Olea-Pflueger effective F to result.diagnostics.",
          "type": "boolean"
        },
        "cluster": {
          "description": "Column name for cluster-robust SEs",
          "type": "string"
        },
        "data": {
          "description": "pandas DataFrame",
          "type": "string"
        },
        "formula": {
          "description": "IV formula: 'y ~ (endog ~ instruments) + exog'",
          "type": "string"
        },
        "fuller_alpha": {
          "default": 1.0,
          "description": "Fuller constant (method='fuller' only)",
          "type": "number"
        },
        "method": {
          "default": "2sls",
          "description": "Estimation method. The k-class methods (2sls, liml, fuller, gmm, jive) take `formula` + `data`; the rest take their own explicit arguments -- see the target function's docstring. Aliases are case-insensitive and -/_ interchangeable.",
          "enum": [
            "2sls",
            "liml",
            "fuller",
            "gmm",
            "jive",
            "jive1",
            "ujive",
            "ijive",
            "rjive",
            "jive_mw",
            "many_weak_ar",
            "lasso",
            "rlasso",
            "post_lasso",
            "mte",
            "ivmte_bounds",
            "plausibly_exog_ltz",
            "plausibly_exog_uci",
            "npiv",
            "kernel",
            "ivdml",
            "deepiv",
            "ivqreg",
            "bayes",
            "continuous_late",
            "shift_share"
          ],
          "type": "string"
        },
        "robust": {
          "default": "nonrobust",
          "description": "Standard error type",
          "enum": [
            "nonrobust",
            "hc0",
            "hc1",
            "hc2",
            "hc3"
          ],
          "type": "string"
        }
      },
      "required": [
        "formula",
        "data"
      ],
      "type": "object"
    }
  },
  {
    "description": "Two-stage least squares (2SLS) IV regression. Alias for sp.iv(method='2sls'). Validation: certified parity evidence.",
    "name": "ivreg",
    "parameters": {
      "properties": {
        "data": {
          "description": "pandas DataFrame",
          "type": "string"
        },
        "formula": {
          "description": "IV formula: 'y ~ (endog ~ instruments) + exog'",
          "type": "string"
        },
        "robust": {
          "default": "nonrobust",
          "description": "Standard error type",
          "type": "string"
        },
        "vcov": {
          "description": "pyfixest/fixest-style variance specification, accepted so one spelling works across sp.regress, sp.ivreg and sp.feols: 'iid' / 'hetero' / 'HC0'-'HC3', or a cluster dict such as {'CRV1': 'firm'} (CRV2 / CRV3 select the matching small-sample correction). Mutually exclusive with robust=/cluster=.",
          "type": "string"
        }
      },
      "required": [
        "formula",
        "data"
      ],
      "type": "object"
    }
  },
  {
    "description": "Quantile regression at specified quantile(s). Validation: certified parity evidence.",
    "name": "qreg",
    "parameters": {
      "properties": {
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "formula": {
          "description": "'y ~ x1 + x2' (alternative to y/x)",
          "type": "string"
        },
        "quantile": {
          "default": 0.5,
          "description": "Quantile (0-1)",
          "type": "number"
        },
        "x": {
          "description": "Regressor columns (alternative to formula)",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Outcome column (alternative to formula)",
          "type": "string"
        }
      },
      "required": [
        "data"
      ],
      "type": "object"
    }
  },
  {
    "description": "Heckman two-step selection model correcting for sample selection bias. Validation: certified parity evidence.",
    "name": "heckman",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals",
          "type": "number"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "select": {
          "description": "Binary selection indicator (1 = observed, 0 = not)",
          "type": "string"
        },
        "x": {
          "description": "Regressors in the outcome equation",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Outcome variable (observed only when select=1)",
          "type": "string"
        },
        "z": {
          "description": "Selection-equation variables (include exclusion restrictions in z but not x)",
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "required": [
        "data",
        "y",
        "x",
        "select",
        "z"
      ],
      "type": "object"
    }
  },
  {
    "description": "Tobit model for censored dependent variables. Validation: certified parity evidence.",
    "name": "tobit",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals",
          "type": "number"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "ll": {
          "default": 0.0,
          "description": "Lower censoring limit (set -inf for none)",
          "type": "number"
        },
        "ul": {
          "description": "Upper censoring limit (default: none)",
          "type": "number"
        },
        "x": {
          "description": "Regressors",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Censored outcome variable",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "x"
      ],
      "type": "object"
    }
  },
  {
    "description": "Negative-binomial count regression for overdispersed non-negative outcomes. Formula fixed effects such as 'y ~ x | id' are implemented with explicit dummies for moderate panels. Validation: certified parity evidence.",
    "name": "nbreg",
    "parameters": {
      "properties": {
        "cluster": {
          "description": "Column name for cluster-robust SEs",
          "type": "string"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "dispersion": {
          "default": "mean",
          "description": "Negative-binomial parameterisation",
          "enum": [
            "mean",
            "constant"
          ],
          "type": "string"
        },
        "exposure": {
          "description": "Positive exposure column",
          "type": "string"
        },
        "formula": {
          "description": "Formula such as 'count ~ x1 + x2 | id'",
          "type": "string"
        },
        "irr": {
          "default": false,
          "description": "Report incidence-rate ratios",
          "type": "boolean"
        },
        "offset": {
          "description": "Column containing a log offset",
          "type": "string"
        },
        "robust": {
          "default": "nonrobust",
          "description": "Standard error type",
          "enum": [
            "nonrobust",
            "robust",
            "hc0",
            "hc1"
          ],
          "type": "string"
        }
      },
      "required": [
        "formula",
        "data"
      ],
      "type": "object"
    }
  },
  {
    "description": "Panel negative-binomial regression (Stata xtnbreg): model='fe' is the Hausman-Hall-Griliches conditional fixed-effects NB, model='re' the beta-dispersion random-effects NB; 'ufe' (dummy-variable NB-2) and 'normal_re' (menbreg) are the other panel NB estimators. Validation: certified parity evidence.",
    "name": "xtnbreg",
    "parameters": {
      "properties": {
        "cluster": {
          "description": "Cluster variable; defaults to entity for FE",
          "type": "string"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "entity": {
          "description": "Panel/unit identifier",
          "type": "string"
        },
        "exposure": {
          "description": "Exposure term or exposure column.",
          "type": "string"
        },
        "formula": {
          "description": "Formula such as 'count ~ x1 + x2'",
          "type": "string"
        },
        "irr": {
          "default": false,
          "description": "irr parameter (bool).",
          "type": "boolean"
        },
        "model": {
          "default": "fe",
          "description": "Panel model",
          "enum": [
            "fe",
            "re",
            "pooled",
            "ufe",
            "normal_re"
          ],
          "type": "string"
        },
        "offset": {
          "description": "Offset term or offset column.",
          "type": "string"
        },
        "time": {
          "description": "Optional time column",
          "type": "string"
        },
        "time_effects": {
          "default": false,
          "description": "Include time dummies for model='fe'",
          "type": "boolean"
        }
      },
      "required": [
        "formula",
        "data",
        "entity"
      ],
      "type": "object"
    }
  },
  {
    "description": "Difference-in-Differences. Supports 2x2, DDD, staggered (Callaway-Sant'Anna, Sun-Abraham), and Synthetic DID. Validation: validated evidence tier (known-truth, reference, external-parity, or Monte Carlo artifact). Do NOT use when: treatment is continuous / dose-valued rather than binary -- use sp.continuous_did or sp.dose_response; exactly one treated unit -- DiD inference is not credible with one treated cluster; use sp.synth or sp.sdid; no pre-treatment period exists -- parallel trends is untestable and the event study cannot be identified.",
    "name": "did",
    "parameters": {
      "properties": {
        "allow_unbalanced_panel": {
          "default": false,
          "description": "Staggered (method='cs') only. False lets units missing a period drop out of the affected ATT(g,t) cells; True keeps every observed row by switching to the repeated-cross-section estimators, with influence functions folded back to the unit level (R did::att_gt(allow_unbalanced_panel=TRUE)). Inert on a balanced panel. The two are different estimators.",
          "type": "boolean"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "id": {
          "description": "Unit identifier (for staggered DID / SDID)",
          "type": "string"
        },
        "method": {
          "default": "auto",
          "description": "Estimator. Parallel-trends: 'auto', '2x2', 'ddd', 'cs', 'sa', 'sdid'. Design-based (random adoption timing, NOT parallel trends): 'staggered_rollout', 'staggered_cs', 'staggered_sa' -- these skip the pre-trend test, honest-DiD and Bacon decomposition, which do not apply to them",
          "enum": [
            "auto",
            "2x2",
            "ddd",
            "callaway_santanna",
            "cs",
            "sun_abraham",
            "sa",
            "sdid",
            "staggered_rollout",
            "staggered_cs",
            "staggered_sa"
          ],
          "type": "string"
        },
        "subgroup": {
          "description": "Affected-subgroup column for DDD",
          "type": "string"
        },
        "time": {
          "description": "Time period column",
          "type": "string"
        },
        "treat": {
          "description": "Treatment indicator or first-treatment-period column",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "treat",
        "time"
      ],
      "type": "object"
    }
  },
  {
    "description": "Triple Differences (DDD). Extends 2x2 DID with a within-unit subgroup comparison to eliminate additional confounders. method='3wfe' (default) is the triple-interaction regression; covariates there are additive and do not identify the covariate-adjusted ATT, so method='dr'/'reg'/'ipw' with id= routes to the conditional estimators of sp.ddd_heterogeneous. Validation: certified parity evidence.",
    "name": "ddd",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "cluster": {
          "description": "Cluster variable for standard errors",
          "type": "string"
        },
        "covariates": {
          "description": "Covariates. Under method='3wfe' they enter additively and do NOT identify the covariate-adjusted ATT (the caveat is recorded in model_info['diagnostics']); for conditional-parallel-trends DDD use method='dr' with id=.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "id": {
          "description": "Unit identifier; required by every method other than '3wfe', which pairs each unit's two periods",
          "type": "string"
        },
        "method": {
          "default": "3wfe",
          "description": "'3wfe' is the triple-interaction regression; 'dr' / 'reg' / 'ipw' hand the design to sp.ddd_heterogeneous, whose covariate adjustment identifies the DDD ATT under conditional parallel trends",
          "enum": [
            "3wfe",
            "dr",
            "reg",
            "ipw"
          ],
          "type": "string"
        },
        "robust": {
          "default": true,
          "description": "HC1 heteroskedasticity-robust standard errors.",
          "type": "boolean"
        },
        "subgroup": {
          "description": "Binary affected-subgroup indicator (1=affected, 0=unaffected)",
          "type": "string"
        },
        "time": {
          "description": "Binary time period indicator",
          "type": "string"
        },
        "treat": {
          "description": "Binary treatment group indicator",
          "type": "string"
        },
        "weights": {
          "description": "Analytical weights column, as Stata's [aweight=...].",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "treat",
        "time",
        "subgroup"
      ],
      "type": "object"
    }
  },
  {
    "description": "Callaway-Sant'Anna (2021) staggered DID with group-time ATTs. Robust to heterogeneous treatment effects and staggered adoption. Validation: certified evidence with scoped limitations. Known limitations: clustervars is not yet supported with bstrap=False; the analytic standard errors do not account for within-cluster dependence, so the multiplier bootstrap is required. Do NOT use when: all treated units adopt in the same period (a plain 2x2 or block design) -- CS reduces to the same estimand at much higher cost; use sp.did(method='2x2'); there is neither a never-treated group nor a not-yet-treated cohort -- no valid comparison group exists for ATT(g,t); only one pre-treatment period per cohort -- no pre-trend evidence and no event-study leads are estimable. Cost: Runtime scales with (number of cohorts x number of periods): one doubly-robust 2x2 estimate plus an influence function per (g,t) cell. Memory is O(n) -- the hazard is wall-clock on many-cohort panels, not RAM.",
    "name": "callaway_santanna",
    "parameters": {
      "properties": {
        "allow_unbalanced_panel": {
          "default": false,
          "description": "How to handle units missing some periods when panel=True. False differences within unit anyway, so a unit missing the base or comparison period drops out of that cell. True switches to the repeated-cross-section estimators, keeping every observed row and folding the influence functions back to the unit level (R did::att_gt(allow_unbalanced_panel=TRUE)); inert when the panel is in fact balanced. The two give different estimates on the same data -- an estimator choice, not a tuning knob.",
          "type": "boolean"
        },
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "anticipation": {
          "default": 0,
          "description": "Number of anticipation periods",
          "type": "integer"
        },
        "base_period": {
          "default": "universal",
          "description": "Base period",
          "enum": [
            "universal",
            "varying"
          ],
          "type": "string"
        },
        "biters": {
          "default": 1000,
          "description": "Bootstrap replications",
          "type": "integer"
        },
        "boot_weight_type": {
          "default": "rademacher",
          "description": "Multiplier weight distribution (rademacher matches R did's implementation)",
          "enum": [
            "rademacher",
            "mammen"
          ],
          "type": "string"
        },
        "bstrap": {
          "default": false,
          "description": "Multiplier-bootstrap SEs per ATT(g,t) instead of analytic (R did / Stata csdid wboot)",
          "type": "boolean"
        },
        "cband": {
          "default": false,
          "description": "Uniform (sup-t) confidence bands across all ATT(g,t); requires bstrap=True",
          "type": "boolean"
        },
        "clustervars": {
          "description": "Cluster variable(s) for the multiplier bootstrap; unit id implied, at most one extra time-invariant variable (R did::att_gt clustervars / Stata csdid cluster())",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "control_group": {
          "default": "nevertreated",
          "description": "Control group",
          "enum": [
            "nevertreated",
            "notyettreated"
          ],
          "type": "string"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "estimator": {
          "default": "dr",
          "description": "2x2 estimator",
          "enum": [
            "dr",
            "ipw",
            "reg"
          ],
          "type": "string"
        },
        "g": {
          "description": "First-treatment-period column (0 = never-treated)",
          "type": "string"
        },
        "i": {
          "description": "Unit identifier",
          "type": "string"
        },
        "notyet_cutoff": {
          "default": "period",
          "description": "Which date a control must still be untreated at when control_group='notyettreated': 'period' (G > max(t, base) + anticipation; R did), 'asinr' (G > t; csdid asinr) or 'cohort' (G > max(t, g); csdid default).",
          "enum": [
            "period",
            "asinr",
            "cohort"
          ],
          "type": "string"
        },
        "panel": {
          "default": true,
          "description": "False means repeated cross-sections",
          "type": "boolean"
        },
        "pretest": {
          "default": "joint",
          "description": "Report the joint pre-trend Wald test ('joint') or skip it ('none').",
          "enum": [
            "joint",
            "none"
          ],
          "type": "string"
        },
        "pretest_periods": {
          "description": "Restrict the pre-trend test to the k event times closest to treatment.",
          "type": "integer"
        },
        "pscore_trim": {
          "default": 0.995,
          "description": "Drop control units whose estimated propensity score reaches this cutoff, matching DRDID's trim.level. Pass 1.0 to disable.",
          "type": "number"
        },
        "random_state": {
          "description": "Random seed or RandomState for reproducible stochastic steps.",
          "type": "integer"
        },
        "se_method": {
          "description": "Shared DiD spelling for the inference procedure: 'analytic', 'multiplier' (aka 'wboot'), or 'auto'. Synonym layer over bstrap=; passing both raises.",
          "enum": [
            "analytic",
            "multiplier",
            "auto"
          ],
          "type": "string"
        },
        "t": {
          "description": "Time period column",
          "type": "string"
        },
        "weights": {
          "description": "Unit-level sampling/population weights omega (R did::att_gt(weightsname=), Stata csdid [aweight=]). NOT a precision knob: omega enters the definition of the target parameter. Unweighted answers 'the average effect across treated units'; weighted answers 'across the population those units represent'. The two are different estimands and can differ in sign, so comparing them is not a robustness check. Must be constant within unit.",
          "type": "string"
        },
        "x": {
          "description": "Covariates for conditional parallel trends. Either column names ['lpop'] or an R-style one-sided formula '~ lpop + I(lpop**2)' (R did/csdid xformla; a left-hand side is accepted and ignored). Inside I() the expression is Python, so powers are I(x**2); I(x^2) is rejected rather than read as bitwise XOR.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Outcome variable",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "g",
        "t",
        "i"
      ],
      "type": "object"
    }
  },
  {
    "description": "RD estimation: sharp, fuzzy, kink, and donut-hole designs with robust inference. Validation: certified evidence with scoped limitations. Known limitations: observation-level weights are not yet supported -- passing a weight column raises NotImplementedError; R-parity certification applies to bwselect='cct' or manually matched h/b bandwidths; the dependency-light default bwselect='mserd' uses StatsPAI's calibrated selector and can differ from rdrobust::rdrobust defaults.",
    "name": "rdrobust",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level",
          "type": "number"
        },
        "b": {
          "description": "Manual bias-correction bandwidth",
          "type": "number"
        },
        "bootstrap": {
          "description": "Optional robust-bias-corrected bootstrap mode",
          "enum": [
            "rbc"
          ],
          "type": "string"
        },
        "bwselect": {
          "default": "mserd",
          "description": "Bandwidth selector; use cct for canonical R rdrobust parity when the statspai[rd-cct] extra is installed",
          "enum": [
            "mserd",
            "msetwo",
            "cerrd",
            "certwo",
            "msecomb1",
            "msecomb2",
            "cercomb1",
            "cercomb2",
            "cct"
          ],
          "type": "string"
        },
        "c": {
          "default": 0.0,
          "description": "Cutoff value",
          "type": "number"
        },
        "cluster": {
          "description": "Cluster variable for SEs",
          "type": "string"
        },
        "covs": {
          "description": "Covariate names for adjusted RD",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "deriv": {
          "default": 0,
          "description": "Derivative order (0=RD, 1=RKD)",
          "type": "integer"
        },
        "donut": {
          "default": 0.0,
          "description": "Donut-hole radius",
          "type": "number"
        },
        "fuzzy": {
          "description": "Treatment variable for fuzzy RD",
          "type": "string"
        },
        "h": {
          "description": "Manual estimation bandwidth",
          "type": "number"
        },
        "kernel": {
          "default": "triangular",
          "description": "Kernel type",
          "enum": [
            "triangular",
            "epanechnikov",
            "uniform"
          ],
          "type": "string"
        },
        "manipulation_test": {
          "default": true,
          "description": "Run a McCrary density test for sorting at the cutoff; stores model_info['mccrary']['pvalue'] and warns + flags result.violations() when p<0.05. Disable for placebo-cutoff loops.",
          "type": "boolean"
        },
        "n_boot": {
          "default": 999,
          "description": "Number of bootstrap draws when bootstrap='rbc'",
          "type": "integer"
        },
        "p": {
          "description": "Polynomial order for point estimation. None -> 1 (local linear) when deriv=0, deriv + 1 otherwise (R rdrobust rule).",
          "type": "integer"
        },
        "q": {
          "description": "Polynomial order for bias correction",
          "type": "integer"
        },
        "random_state": {
          "description": "Bootstrap RNG seed",
          "type": "integer"
        },
        "rho": {
          "description": "Ratio h/b for bias correction",
          "type": "number"
        },
        "warn_mass_points": {
          "default": true,
          "description": "Warn when the running variable has few distinct values",
          "type": "boolean"
        },
        "warn_weak_first_stage": {
          "default": true,
          "description": "Warn on weak first-stage discontinuity in fuzzy RD",
          "type": "boolean"
        },
        "weights": {
          "description": "Reserved; currently raises NotImplementedError",
          "type": "string"
        },
        "x": {
          "description": "Running variable",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable",
          "type": "string"
        }
      },
      "required": [
        "y",
        "x",
        "data"
      ],
      "type": "object"
    }
  },
  {
    "description": "Unified synthetic control estimator. method= selects variant: 'classic', 'demeaned', 'detrended', 'unconstrained', 'elastic_net', 'augmented', 'sdid', 'gsynth', 'staggered'. inference= selects: 'placebo', 'conformal', 'bootstrap', 'jackknife'. Validation: certified evidence with scoped limitations. Known limitations: Classical SCM certification is specification-specific: ADH/Synth parity requires passing the same special_predictors recipe; the default outcome-only V=I path is a documented Kaul-style convention; Default native classical SCM can differ from Synth on Basque-style panels by a documented local-optimum convention (the outer V optimisation has multiple near-equivalent minima); use backend='synth' or canonical special_predictors when exact R parity is required. Do NOT use when: many units are treated at once -- classic SCM is built for one (or few) treated units; use sp.gsynth, sp.sdid or sp.callaway_santanna; the pre-treatment window is short (fewer than ~10 periods) -- the donor weights overfit noise and pre-period fit stops being evidence; the treated unit's pre-period outcome lies outside the convex hull of the donors -- no non-negative weighting can match it; check the pre-period RMSPE and consider sp.augsynth. Cost: One constrained optimisation over the donor simplex per fit. placebo=True / inference re-runs the whole fit once per donor, so runtime is roughly (donors + 1) x a single fit -- the usual reason a synth call feels slow.",
    "name": "synth",
    "parameters": {
      "properties": {
        "backend": {
          "default": "native",
          "description": "Optional reference backend for exact R parity: synth for classic SCM",
          "type": "string"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "inference": {
          "description": "Inference method: placebo/conformal/bootstrap/jackknife",
          "type": "string"
        },
        "method": {
          "default": "classic",
          "description": "SCM variant: classic/demeaned/detrended/unconstrained/elastic_net/augmented/sdid/gsynth/staggered",
          "type": "string"
        },
        "outcome": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        },
        "time": {
          "description": "Time period column.",
          "type": "string"
        },
        "treated_unit": {
          "description": "Treated unit (not needed for staggered)",
          "type": "string"
        },
        "treatment": {
          "description": "Binary treatment column (staggered only)",
          "type": "string"
        },
        "treatment_time": {
          "description": "First treatment period",
          "type": "integer"
        },
        "unit": {
          "description": "Unit identifier column.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "outcome",
        "unit",
        "time"
      ],
      "type": "object"
    }
  },
  {
    "description": "Double/Debiased Machine Learning supports PLR, binary-D IRM, PLIV, and binary-D/binary-Z IIVM. Python-only external OOFPredictions supports unweighted, unnormalised IRM ATE scoring with exact alignment; caller_declared records are declarations rather than proof against leakage. Validation: certified evidence with scoped limitations. Known limitations: external_predictions, store_oof, and observation_ids are Python-only and absent from JSON/MCP inputs; external records accept no dict, JSON string, or file path; store_oof=True retains individual-level records for get_oof() and get_residuals() in memory only; ordinary serialization omits them; Retained IRM needs at least 10 rows per treatment arm in each training fold or raises DataInsufficient instead of fallback; internal explicit fold_indices require n_rep=1, while external repeated IRM records are allowed when the explicit partition is equivalent for every repeat.",
    "name": "dml",
    "parameters": {
      "properties": {
        "covariates": {
          "description": "List of control variable names",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "instrument": {
          "description": "Instrument (required for pliv/iivm)",
          "type": "string"
        },
        "model": {
          "default": "plr",
          "description": "DML model family",
          "enum": [
            "plr",
            "irm",
            "pliv",
            "iivm"
          ],
          "type": "string"
        },
        "n_folds": {
          "default": 5,
          "description": "Cross-fitting folds",
          "type": "integer"
        },
        "n_rep": {
          "default": 1,
          "description": "Repeated cross-fitting splits (median aggregation)",
          "type": "integer"
        },
        "normalize_ipw": {
          "default": false,
          "description": "Self-normalize the inverse-propensity weights for the IPW models (irm/iivm); matches DoubleML's normalize_ipw. Rejected for plr/pliv.",
          "type": "boolean"
        },
        "score": {
          "description": "Orthogonal score variant (DoubleML-compatible). PLR: 'partialling out' (default) or 'IV-type'. IRM: 'ATE' (default) or 'ATTE'. None selects the model default; defaults reproduce historical output exactly.",
          "enum": [
            "partialling out",
            "IV-type",
            "ATE",
            "ATTE"
          ],
          "type": "string"
        },
        "treat": {
          "description": "Treatment variable",
          "type": "string"
        },
        "trimming_threshold": {
          "default": 0.01,
          "description": "Symmetric propensity clip [t, 1-t] for irm/iivm (DoubleML trimming_rule='truncate'). Default 0.01 = historical clip.",
          "type": "number"
        },
        "y": {
          "description": "Outcome",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "treat",
        "covariates"
      ],
      "type": "object"
    }
  },
  {
    "description": "Treatment-Agnostic Representation Network for neural CATE/ATE estimation. Stores CATE, potential-outcome predictions, training diagnostics, and export-ready unit effects.",
    "name": "tarnet",
    "parameters": {
      "properties": {
        "batch_size": {
          "default": 256,
          "description": "Mini-batch size",
          "type": "integer"
        },
        "covariates": {
          "description": "Numeric covariate columns",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "dropout": {
          "default": 0.1,
          "description": "Representation dropout rate",
          "type": "number"
        },
        "early_stopping": {
          "default": false,
          "description": "Stop on validation-loss patience when validation_fraction > 0",
          "type": "boolean"
        },
        "epochs": {
          "default": 300,
          "description": "Training epochs",
          "type": "integer"
        },
        "head_layers": {
          "default": [
            100
          ],
          "description": "Outcome-head hidden-layer sizes",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "learning_rate": {
          "default": 0.001,
          "description": "Adam learning rate",
          "type": "number"
        },
        "random_state": {
          "default": 42,
          "description": "Random seed",
          "type": "integer"
        },
        "repr_layers": {
          "default": [
            200,
            200
          ],
          "description": "Shared representation hidden-layer sizes",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "treat": {
          "description": "Binary treatment column",
          "type": "string"
        },
        "validation_fraction": {
          "default": 0.0,
          "description": "Holdout fraction for validation-loss diagnostics",
          "type": "number"
        },
        "y": {
          "description": "Outcome",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "treat",
        "covariates"
      ],
      "type": "object"
    }
  },
  {
    "description": "Counterfactual Regression Network: TARNet plus an MMD/IPM representation-balance penalty for neural CATE/ATE estimation.",
    "name": "cfrnet",
    "parameters": {
      "properties": {
        "batch_size": {
          "default": 256,
          "description": "Mini-batch size",
          "type": "integer"
        },
        "covariates": {
          "description": "Numeric covariate columns",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "dropout": {
          "default": 0.1,
          "description": "Representation dropout rate",
          "type": "number"
        },
        "early_stopping": {
          "default": false,
          "description": "Stop on validation-loss patience when validation_fraction > 0",
          "type": "boolean"
        },
        "epochs": {
          "default": 300,
          "description": "Training epochs",
          "type": "integer"
        },
        "head_layers": {
          "default": [
            100
          ],
          "description": "Outcome-head hidden-layer sizes",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "ipm_weight": {
          "default": 1.0,
          "description": "Weight on the MMD representation-balance penalty",
          "type": "number"
        },
        "learning_rate": {
          "default": 0.001,
          "description": "Adam learning rate",
          "type": "number"
        },
        "random_state": {
          "default": 42,
          "description": "Random seed",
          "type": "integer"
        },
        "repr_layers": {
          "default": [
            200,
            200
          ],
          "description": "Shared representation hidden-layer sizes",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "treat": {
          "description": "Binary treatment column",
          "type": "string"
        },
        "validation_fraction": {
          "default": 0.0,
          "description": "Holdout fraction for validation-loss diagnostics",
          "type": "number"
        },
        "y": {
          "description": "Outcome",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "treat",
        "covariates"
      ],
      "type": "object"
    }
  },
  {
    "description": "DragonNet: neural potential-outcome heads plus a propensity head and targeted regularisation; reports AIPW ATE, CATE, propensity overlap diagnostics, and export-ready unit effects.",
    "name": "dragonnet",
    "parameters": {
      "properties": {
        "batch_size": {
          "default": 256,
          "description": "Mini-batch size",
          "type": "integer"
        },
        "covariates": {
          "description": "Numeric covariate columns",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "dropout": {
          "default": 0.1,
          "description": "Representation dropout rate",
          "type": "number"
        },
        "early_stopping": {
          "default": false,
          "description": "Stop on validation-loss patience when validation_fraction > 0",
          "type": "boolean"
        },
        "epochs": {
          "default": 300,
          "description": "Training epochs",
          "type": "integer"
        },
        "head_layers": {
          "default": [
            100
          ],
          "description": "Outcome-head hidden-layer sizes",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "learning_rate": {
          "default": 0.001,
          "description": "Adam learning rate",
          "type": "number"
        },
        "propensity_weight": {
          "default": 1.0,
          "description": "Weight on the propensity cross-entropy loss",
          "type": "number"
        },
        "random_state": {
          "default": 42,
          "description": "Random seed",
          "type": "integer"
        },
        "repr_layers": {
          "default": [
            200,
            200
          ],
          "description": "Shared representation hidden-layer sizes",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "targeted_reg_weight": {
          "default": 1.0,
          "description": "Weight on targeted regularisation",
          "type": "number"
        },
        "treat": {
          "description": "Binary treatment column",
          "type": "string"
        },
        "validation_fraction": {
          "default": 0.0,
          "description": "Holdout fraction for validation-loss diagnostics",
          "type": "number"
        },
        "y": {
          "description": "Outcome",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "treat",
        "covariates"
      ],
      "type": "object"
    }
  },
  {
    "description": "Honest generalized random forest for conditional average treatment effects (CATE). Default engine reimplements the GRF algorithm: splits on the causal gradient pseudo-outcome, out-of-bag nuisances and predictions, little-bag variances, cluster-aware sampling and inference. fe='twoway' gives a causal forest with fixed effects for panels (unit and period effects removed within every node and leaf). Validation: certified evidence with scoped limitations. Known limitations: The AIPW ATE/ATT are validated against grf on clean-overlap designs only; under severe propensity-overlap loss the AIPW influence function inflates the standard error (conservative, over-covering inference), so inspect the sp.audit overlap diagnostic before interpreting the ATE on that kind of sample; The forest is compared with grf only statistically (tier T3: RMSE, pointwise variances, coverage), not bit-for-bit; given a fitted forest, the inference operators match grf / sandwich to 1e-14; Doubly-robust averages (average_treatment_effect, best_linear_projection, rate) are not implemented for fe= forests, which have no propensity score; use sp.did_forest for group-time averages; For fe= forests the calibration slope tests for heterogeneity only; it comes from a globally within-transformed regression and does not de-attenuate CATE predictions.",
    "name": "causal_forest",
    "parameters": {
      "properties": {
        "W_hat": {
          "description": "Precomputed out-of-sample E[T | X, W] (skips the treatment nuisance forest).",
          "type": "string"
        },
        "Y_hat": {
          "description": "Precomputed out-of-sample E[Y | X, W] (skips the outcome nuisance forest).",
          "type": "string"
        },
        "clusters": {
          "description": "Cluster ids or column name; sampling, honesty, cross-fitting and standard errors respect clusters. Always set for repeated observations of a unit.",
          "type": "string"
        },
        "d": {
          "description": "Treatment column.",
          "type": "string"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "fe": {
          "description": "Panel fixed effects removed within nodes and leaves.",
          "enum": [
            "unit",
            "twoway"
          ],
          "type": "string"
        },
        "formula": {
          "description": "'y ~ treatment | x1 + x2 [| w1 + w2]' (pipe separates effect modifiers and controls). Alternatively use y/d/x/w column names or Y/T/X/W arrays.",
          "type": "string"
        },
        "id": {
          "description": "Unit id column (fe=); unit= is an alias.",
          "type": "string"
        },
        "max_samples": {
          "default": 0.5,
          "description": "Share of clusters per tree.",
          "type": "number"
        },
        "min_samples_leaf": {
          "default": 5,
          "description": "GRF min.node.size.",
          "type": "integer"
        },
        "n_estimators": {
          "default": 2000,
          "description": "Number of trees (``n_trees`` is accepted as an alias).",
          "type": "integer"
        },
        "random_state": {
          "description": "Random seed or RandomState for reproducible stochastic steps.",
          "type": "integer"
        },
        "split_rule": {
          "default": "grf",
          "description": "'cffe' (fe= only) uses the tau-heterogeneity criterion of Kattenberg, Scheer and Thiel instead of the GRF gradient; 'legacy' reproduces the pre-1.29 estimator (deprecated).",
          "enum": [
            "grf",
            "cffe",
            "legacy"
          ],
          "type": "string"
        },
        "time": {
          "description": "Period column (fe='twoway').",
          "type": "string"
        },
        "w": {
          "description": "Control columns (nuisances only).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "x": {
          "description": "Effect-modifier columns.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Outcome column.",
          "type": "string"
        }
      },
      "required": [],
      "type": "object"
    }
  },
  {
    "description": "Average treatment effects by group after a causal forest, with valid standard errors: means of unbiased scores (imputation scores on treated cells for fe= forests, AIPW scores for pooled forests) over groups given by labels, by quantiles of the out-of-bag CATE (GATES), or by membership in dyadic data (every country over all its pairs). Pair-clustered or dyadic-robust variance, equality Wald test. Validation: validated evidence tier (known-truth, reference, external-parity, or Monte Carlo artifact).",
    "name": "forest_group_effects",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "by": {
          "description": "None, 'cate_quantile', or one label per training row.",
          "type": "string"
        },
        "cluster": {
          "description": "None (forest clusters), 'dyadic' (needs members) or cluster ids.",
          "type": "string"
        },
        "covariates": {
          "default": "none",
          "description": "fe= forests: covariates in the untreated outcome model ('none' = did_imputation, 'auto' = time-varying x/w, or names). controls= is accepted as an alias.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "forest": {
          "description": "Fitted GRF-engine forest.",
          "type": "string"
        },
        "members": {
          "description": "(n, 2) members of each dyadic row; with by=None one group per member.",
          "type": "string"
        },
        "min_rows": {
          "default": 1,
          "description": "min_rows parameter (int).",
          "type": "integer"
        },
        "n_groups": {
          "default": 4,
          "description": "Quantile groups for by='cate_quantile'.",
          "type": "integer"
        },
        "scale": {
          "default": "level",
          "description": "'percent' adds 100*(exp(x)-1) columns for log outcomes.",
          "enum": [
            "level",
            "percent"
          ],
          "type": "string"
        },
        "variance": {
          "default": "forest",
          "description": "fe= forests: centre treated residuals on the OOB forest ('forest') or on cohort x event-time means only ('bjs').",
          "enum": [
            "forest",
            "bjs"
          ],
          "type": "string"
        }
      },
      "required": [
        "forest"
      ],
      "type": "object"
    }
  },
  {
    "description": "Treatment rule for a causal forest with fixed effects, and what it was worth. sp.policy_tree needs a doubly-robust score and so a propensity, which a within-unit design has none of; the imputation scores that give the ATT are used instead, which fixes the population to the treated cells: of the cells that were treated, which should have been. Fits on one half of the units and prices on the other, because a rule maximised on the scores it is then priced against is priced on its own noise -- where the true gain was exactly 0, the same-sample version averaged +0.048 and called it significant 13% of the time, against +0.005 and 3.5% split. Value, treat-all value and the gain between them share one exact, cluster- or dyad-robust covariance. Aggregates n_splits draws by VEIN. Validation: validated evidence tier (known-truth, reference, external-parity, or Monte Carlo artifact).",
    "name": "forest_policy_tree",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "cluster": {
          "description": "Cluster identifier column for clustered standard errors.",
          "type": "string"
        },
        "cost": {
          "default": 0.0,
          "description": "Cost of treating one cell, in the outcome's units. The rule treats where the effect exceeds it; with cost=0 and positive effects, treating everyone is optimal.",
          "type": "number"
        },
        "covariates": {
          "default": "none",
          "description": "Covariates in the untreated outcome model; controls= is accepted as an alias.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "depth": {
          "default": 2,
          "description": "Tree depth; <= 2 is searched exactly, deeper is greedy.",
          "type": "integer"
        },
        "forest": {
          "description": "Fitted GRF-engine forest with fe='twoway'.",
          "type": "string"
        },
        "members": {
          "description": "(n, 2) members of each dyadic row; splits by member.",
          "type": "string"
        },
        "min_leaf_size": {
          "description": "min_leaf_size parameter (int).",
          "type": "integer"
        },
        "n_splits": {
          "default": 21,
          "description": "Splits aggregated by Chernozhukov et al. (2025) VEIN. A rule cannot be averaged, so the tree reported is the one from the median-gain split and split_stability says how far the rule itself moved. n_splits=1 warns.",
          "type": "integer"
        },
        "random_state": {
          "default": 0,
          "description": "Seeds the sequence of splits.",
          "type": "integer"
        },
        "train_frac": {
          "default": 0.5,
          "description": "Share of units (members) that fit the rule.",
          "type": "number"
        },
        "variance": {
          "default": "bjs",
          "description": "Centring of treated residuals before the cohort x event-time blocks.",
          "enum": [
            "bjs",
            "forest"
          ],
          "type": "string"
        },
        "x": {
          "description": "Policy covariates; default the forest's effect modifiers.",
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "required": [
        "forest"
      ],
      "type": "object"
    }
  },
  {
    "description": "Instrumental forest (grf::instrumental_forest): heterogeneous local average treatment effects tau(x) = Cov[Y,Z|x]/Cov[W,Z|x] from an honest generalized random forest that splits on the IV gradient and uses the instrument in its split constraints. Reports out-of-bag CATEs with little-bag variances and the doubly-robust average conditional LATE (compliance-weighted scores). Given grf's forest, the local solve, scores, average and BLP match grf to 1e-13 (T2); the forest itself is T3. Validation: validated evidence tier (known-truth, reference, external-parity, or Monte Carlo artifact).",
    "name": "iv_forest",
    "parameters": {
      "properties": {
        "W_hat": {
          "description": "Precomputed E[W|X]; default out-of-bag regression forest.",
          "type": "string"
        },
        "Y_hat": {
          "description": "Precomputed E[Y|X]; default out-of-bag regression forest.",
          "type": "string"
        },
        "Z_hat": {
          "description": "Precomputed E[Z|X]; default out-of-bag regression forest.",
          "type": "string"
        },
        "alpha": {
          "default": 0.05,
          "description": "Significance level of the reported interval.",
          "type": "number"
        },
        "ci_group_size": {
          "default": 2,
          "description": "Trees per little bag; 1 disables variance estimates.",
          "type": "integer"
        },
        "clusters": {
          "description": "Cluster ids: trees sample whole clusters and every standard error is cluster-robust.",
          "type": "string"
        },
        "compliance_score": {
          "description": "``Delta(X_i)`` for the average-effect scores; by default estimated by an auxiliary 500-tree causal forest of ``W`` on ``Z``.",
          "type": "string"
        },
        "covariates": {
          "description": "Effect modifiers ``X``; the nuisances are also regressed on them.",
          "type": "string"
        },
        "data": {
          "description": "Input data. When omitted, ``y``, ``treat``, ``instrument`` and ``covariates`` are arrays.",
          "type": "string"
        },
        "equalize_cluster_weights": {
          "default": false,
          "description": "Give every cluster the same weight (incompatible with ``weights``).",
          "type": "boolean"
        },
        "honest": {
          "default": true,
          "description": "grf honesty: grow on one half of the drawn sample, estimate leaves on the other.",
          "type": "boolean"
        },
        "honesty_fraction": {
          "default": 0.5,
          "description": "Share of the drawn sample used to grow the tree.",
          "type": "number"
        },
        "honesty_prune_leaves": {
          "default": true,
          "description": "Prune leaves left empty by the estimation half.",
          "type": "boolean"
        },
        "imbalance_penalty": {
          "default": 0.0,
          "description": "grf imbalance.penalty on unbalanced splits.",
          "type": "number"
        },
        "instrument": {
          "description": "Instrument (binary or continuous).",
          "type": "string"
        },
        "max_depth": {
          "description": "Depth cap (no grf analogue).",
          "type": "string"
        },
        "max_samples": {
          "default": 0.5,
          "description": "Fraction of clusters drawn per tree (grf ``sample.fraction``).",
          "type": "number"
        },
        "min_samples_leaf": {
          "default": 5,
          "description": "grf ``min.node.size``.",
          "type": "integer"
        },
        "mtry": {
          "description": "Candidate variables per split (grf default ``min(ceil(sqrt(p) + 20), p)``).",
          "type": "string"
        },
        "n_bootstrap": {
          "description": "Ignored. Standard errors now come from the doubly-robust scores.",
          "type": "string"
        },
        "n_estimators": {
          "default": 2000,
          "description": "Trees (grf ``num.trees``).",
          "type": "integer"
        },
        "n_jobs": {
          "default": 1,
          "description": "Threads used to grow trees (-1 = all cores).",
          "type": "integer"
        },
        "random_state": {
          "default": 42,
          "description": "Seed; different seeds give independent forests.",
          "type": "integer"
        },
        "reduced_form_weight": {
          "default": 0.0,
          "description": "Mix the IV split criterion with the causal-forest criterion that treats ``W`` as exogenous (grf ``reduced.form.weight``).",
          "type": "number"
        },
        "split_alpha": {
          "default": 0.05,
          "description": "grf ``alpha``: minimum share of the parent's instrument variation each child of a stabilised split keeps.",
          "type": "number"
        },
        "stabilize_splits": {
          "default": true,
          "description": "Use the instrument in the split constraints.",
          "type": "boolean"
        },
        "treat": {
          "description": "Treatment (binary or continuous).",
          "type": "string"
        },
        "weights": {
          "description": "Sample weights (grf ``sample.weights``).",
          "type": "string"
        },
        "y": {
          "description": "Outcome (column name or array).",
          "type": "string"
        }
      },
      "required": [],
      "type": "object"
    }
  },
  {
    "description": "Multi-arm causal forest (grf::multi_arm_causal_forest): CATEs of K-1 arms against a reference, estimated jointly by one forest whose trees split on the gradient of all contrasts (multi-arm R-learner with GRF weights). Propensities from a probability forest, doubly-robust per-arm ATEs, BLPs per contrast. Operators match grf to 1e-13 (T2); forest statistics match grf within Monte Carlo error (T3). Validation: validated evidence tier (known-truth, reference, external-parity, or Monte Carlo artifact).",
    "name": "multi_arm_forest",
    "parameters": {
      "properties": {
        "W_hat": {
          "description": "``(n, K)`` arm propensities in the order of ``.arms``; default out-of-bag probability forest.",
          "type": "string"
        },
        "Y_hat": {
          "description": "``E[Y | X]``; default out-of-bag regression forest.",
          "type": "string"
        },
        "alpha": {
          "default": 0.05,
          "description": "Significance level for the reported intervals.",
          "type": "number"
        },
        "ci_group_size": {
          "default": 2,
          "description": "Trees per little bag; 1 disables variance estimates.",
          "type": "integer"
        },
        "clusters": {
          "description": "Cluster ids: trees sample whole clusters; SEs are cluster-robust.",
          "type": "string"
        },
        "covariates": {
          "description": "Covariates ``X``.",
          "type": "string"
        },
        "data": {
          "description": "Input data; when omitted, the other inputs are arrays.",
          "type": "string"
        },
        "equalize_cluster_weights": {
          "default": false,
          "description": "Give every cluster equal weight (incompatible with weights).",
          "type": "boolean"
        },
        "honest": {
          "default": true,
          "description": "grf honesty: grow on one half of the drawn sample, estimate leaves on the other.",
          "type": "boolean"
        },
        "honesty_fraction": {
          "default": 0.5,
          "description": "Share of the drawn sample used to grow the tree.",
          "type": "number"
        },
        "honesty_prune_leaves": {
          "default": true,
          "description": "Prune leaves left empty by the estimation half.",
          "type": "boolean"
        },
        "imbalance_penalty": {
          "default": 0.0,
          "description": "grf imbalance.penalty on unbalanced splits.",
          "type": "number"
        },
        "max_depth": {
          "description": "Optional depth cap (no grf analogue).",
          "type": "string"
        },
        "max_samples": {
          "default": 0.5,
          "description": "Fraction of clusters drawn per tree (grf sample.fraction); at most 0.5 when ci_group_size > 1.",
          "type": "number"
        },
        "min_samples_leaf": {
          "default": 5,
          "description": "grf min.node.size: nodes with at most this many growing samples are not split.",
          "type": "integer"
        },
        "mtry": {
          "description": "Candidate variables per split (grf default min(ceil(sqrt(p) + 20), p)).",
          "type": "string"
        },
        "n_estimators": {
          "default": 2000,
          "description": "Trees (grf num.trees).",
          "type": "integer"
        },
        "n_jobs": {
          "default": 1,
          "description": "Threads used to grow trees (-1 = all cores).",
          "type": "integer"
        },
        "propensity_bounds": {
          "description": "Clip the estimated propensities to these bounds in the average- effect scores (not done by default, as in grf).",
          "type": "string"
        },
        "random_state": {
          "default": 42,
          "description": "Seed; different seeds give independent forests.",
          "type": "integer"
        },
        "reference": {
          "description": "Reference arm; defaults to the smallest label (``0`` for integer arms, matching the previous API).",
          "type": "string"
        },
        "split_alpha": {
          "default": 0.05,
          "description": "grf alpha: minimum share of the parent each child keeps.",
          "type": "number"
        },
        "stabilize_splits": {
          "default": true,
          "description": "Apply the treatment/instrument split constraints (grf stabilize.splits).",
          "type": "boolean"
        },
        "treat": {
          "description": "Arm labels (any values; at least two arms).",
          "type": "string"
        },
        "weights": {
          "description": "Sample weights (grf sample.weights).",
          "type": "string"
        },
        "y": {
          "description": "Outcome.",
          "type": "string"
        }
      },
      "required": [],
      "type": "object"
    }
  },
  {
    "description": "Linear-model forest (grf::lm_forest): covariate-varying coefficients h_k(x) in Y = c(x) + sum_k h_k(x) W_k, by forest-weighted local least squares of the centred outcome(s) on the centred regressors. Use for varying-coefficient models and continuous multi-dimensional treatments; for mutually exclusive arms use sp.multi_arm_forest. Validation: validated evidence tier (known-truth, reference, external-parity, or Monte Carlo artifact).",
    "name": "lm_forest",
    "parameters": {
      "properties": {
        "W_hat": {
          "description": "Precomputed E[W|X], (n, K); default out-of-bag (multi-task) regression forest.",
          "type": "string"
        },
        "Y_hat": {
          "description": "Precomputed E[Y|X], (n, q); default out-of-bag (multi-task) regression forest.",
          "type": "string"
        },
        "ci_group_size": {
          "default": 2,
          "description": "Trees per little bag; 1 disables variance estimates.",
          "type": "integer"
        },
        "clusters": {
          "description": "Cluster ids: trees sample whole clusters; SEs are cluster-robust.",
          "type": "string"
        },
        "covariates": {
          "description": "The covariates ``X`` along which the coefficients vary.",
          "type": "string"
        },
        "data": {
          "description": "Input data; when omitted, the other inputs are arrays.",
          "type": "string"
        },
        "equalize_cluster_weights": {
          "default": false,
          "description": "Give every cluster equal weight (incompatible with weights).",
          "type": "boolean"
        },
        "honest": {
          "default": true,
          "description": "grf honesty: grow on one half of the drawn sample, estimate leaves on the other.",
          "type": "boolean"
        },
        "honesty_fraction": {
          "default": 0.5,
          "description": "Share of the drawn sample used to grow the tree.",
          "type": "number"
        },
        "honesty_prune_leaves": {
          "default": true,
          "description": "Prune leaves left empty by the estimation half.",
          "type": "boolean"
        },
        "imbalance_penalty": {
          "default": 0.0,
          "description": "grf imbalance.penalty on unbalanced splits.",
          "type": "number"
        },
        "max_depth": {
          "description": "Optional depth cap (no grf analogue).",
          "type": "string"
        },
        "max_samples": {
          "default": 0.5,
          "description": "Fraction of clusters drawn per tree (grf sample.fraction); at most 0.5 when ci_group_size > 1.",
          "type": "number"
        },
        "min_samples_leaf": {
          "default": 5,
          "description": "grf min.node.size: nodes with at most this many growing samples are not split.",
          "type": "integer"
        },
        "mtry": {
          "description": "Candidate variables per split (grf default min(ceil(sqrt(p) + 20), p)).",
          "type": "string"
        },
        "n_estimators": {
          "default": 2000,
          "description": "Trees (grf num.trees).",
          "type": "integer"
        },
        "n_jobs": {
          "default": 1,
          "description": "Threads used to grow trees (-1 = all cores).",
          "type": "integer"
        },
        "random_state": {
          "default": 42,
          "description": "Seed; different seeds give independent forests.",
          "type": "integer"
        },
        "regressors": {
          "description": "The regressors ``W_1..W_K`` whose coefficients vary with ``x``.",
          "type": "string"
        },
        "split_alpha": {
          "default": 0.05,
          "description": "grf alpha: minimum share of the parent each child keeps.",
          "type": "number"
        },
        "stabilize_splits": {
          "default": false,
          "description": "Apply the causal-forest split constraints to every regressor.",
          "type": "boolean"
        },
        "weights": {
          "description": "Sample weights (grf sample.weights).",
          "type": "string"
        },
        "y": {
          "description": "Outcome(s); several outcomes are modelled jointly.",
          "type": "string"
        }
      },
      "required": [],
      "type": "object"
    }
  },
  {
    "description": "Causal survival forest (Cui et al. 2023; grf::causal_survival_forest): heterogeneous effects on right-censored outcomes -- RMST or survival-probability difference at a horizon -- under unconfoundedness and conditionally independent censoring. Nuisance survival and censoring curves come from out-of-bag survival forests on [X, W]; the censoring-robust scores follow grf's discretisation exactly (T2 on the score map), with doubly-robust averages and BLPs. Validation: validated evidence tier (known-truth, reference, external-parity, or Monte Carlo artifact).",
    "name": "causal_survival_forest",
    "parameters": {
      "properties": {
        "W_hat": {
          "description": "Precomputed propensities; default out-of-bag regression forest.",
          "type": "string"
        },
        "alpha": {
          "default": 0.05,
          "description": "Significance level of the reported interval.",
          "type": "number"
        },
        "ci_group_size": {
          "default": 2,
          "description": "Trees per little bag; 1 disables variance estimates.",
          "type": "integer"
        },
        "clusters": {
          "description": "Cluster ids: trees sample whole clusters; SEs are cluster-robust.",
          "type": "string"
        },
        "covariates": {
          "description": "Covariates ``X`` (confounders and effect modifiers).",
          "type": "string"
        },
        "data": {
          "description": "Input data; when omitted, the other inputs are arrays.",
          "type": "string"
        },
        "equalize_cluster_weights": {
          "default": false,
          "description": "Give every cluster equal weight (incompatible with weights).",
          "type": "boolean"
        },
        "event": {
          "description": "1 = event observed, 0 = censored.",
          "type": "string"
        },
        "failure_times": {
          "description": "Grid for the survival nuisance forests (default: observed times).",
          "type": "string"
        },
        "honest": {
          "default": true,
          "description": "grf honesty: grow on one half of the drawn sample, estimate leaves on the other.",
          "type": "boolean"
        },
        "honesty_fraction": {
          "default": 0.5,
          "description": "Share of the drawn sample used to grow the tree.",
          "type": "number"
        },
        "honesty_prune_leaves": {
          "default": true,
          "description": "Prune leaves left empty by the estimation half.",
          "type": "boolean"
        },
        "horizon": {
          "description": "``h`` of the estimand. Defaults to the 80th percentile of observed event times (recorded in ``detail``); grf requires it -- choose it from the study design.",
          "type": "string"
        },
        "imbalance_penalty": {
          "default": 0.0,
          "description": "grf imbalance.penalty on unbalanced splits.",
          "type": "number"
        },
        "max_depth": {
          "description": "Optional depth cap (no grf analogue).",
          "type": "string"
        },
        "max_samples": {
          "default": 0.5,
          "description": "Fraction of clusters drawn per tree (grf sample.fraction); at most 0.5 when ci_group_size > 1.",
          "type": "number"
        },
        "min_samples_leaf": {
          "default": 5,
          "description": "grf min.node.size: nodes with at most this many growing samples are not split.",
          "type": "integer"
        },
        "mtry": {
          "description": "Candidate variables per split (grf default min(ceil(sqrt(p) + 20), p)).",
          "type": "string"
        },
        "n_estimators": {
          "default": 2000,
          "description": "Trees (grf num.trees).",
          "type": "integer"
        },
        "n_jobs": {
          "default": 1,
          "description": "Threads used to grow trees (-1 = all cores).",
          "type": "integer"
        },
        "propensity_bounds": {
          "description": "Clip ``e`` in the average-effect score denominators (off by default, as in grf).",
          "type": "string"
        },
        "random_state": {
          "default": 42,
          "description": "Seed; different seeds give independent forests.",
          "type": "integer"
        },
        "split_alpha": {
          "default": 0.05,
          "description": "grf ``alpha``; each child must also hold ``split_alpha`` of the parent's size in failures.",
          "type": "number"
        },
        "stabilize_splits": {
          "default": true,
          "description": "Apply the treatment/instrument split constraints (grf stabilize.splits).",
          "type": "boolean"
        },
        "target": {
          "default": "RMST",
          "description": "Estimand: 'RMST' or 'survival_probability'.",
          "type": "string"
        },
        "time": {
          "description": "Observed time ``min(T, C)``, non-negative.",
          "type": "string"
        },
        "treat": {
          "description": "Binary treatment.",
          "type": "string"
        },
        "weights": {
          "description": "Sample weights (grf sample.weights).",
          "type": "string"
        }
      },
      "required": [],
      "type": "object"
    }
  },
  {
    "description": "Split-frequency variable importance of any GRF-engine forest (grf::variable_importance): depth-weighted shares of splits per covariate, summing to one. Describes how the forest uses covariates; it is not a test of heterogeneity -- use sp.calibration_test or sp.best_linear_projection for that. Matches grf to 1e-15 given the same split counts. Validation: validated evidence tier (known-truth, reference, external-parity, or Monte Carlo artifact).",
    "name": "variable_importance",
    "parameters": {
      "properties": {
        "decay_exponent": {
          "default": 2.0,
          "description": "How quickly deeper splits lose weight.",
          "type": "number"
        },
        "forest": {
          "description": "Any of ``sp.causal_forest`` (GRF engine), ``sp.iv_forest``, ``sp.multi_arm_forest``, ``sp.lm_forest``, ``sp.regression_forest``, ``sp.multi_regression_forest``, ``sp.probability_forest``, ``sp.quantile_forest``, ``sp.survival_forest``, ``sp",
          "type": "string"
        },
        "max_depth": {
          "default": 4,
          "description": "Deepest level counted.",
          "type": "integer"
        }
      },
      "required": [
        "forest"
      ],
      "type": "object"
    }
  },
  {
    "description": "Best linear projection of a forest's conditional effect on covariates (grf::best_linear_projection): OLS of the doubly-robust scores on (1, A) with HC0-HC3 (cluster-robust when the forest has clusters) standard errors -- valid inference on a low-dimensional summary of tau(x) (Semenova and Chernozhukov 2021). Works for causal, instrumental, multi-arm and causal-survival forests; matches grf to 1e-15 given the scores. Validation: validated evidence tier (known-truth, reference, external-parity, or Monte Carlo artifact).",
    "name": "best_linear_projection",
    "parameters": {
      "properties": {
        "A": {
          "description": "Projection covariates, one row per training observation (after missing-value removal).",
          "type": "string"
        },
        "alpha": {
          "default": 0.05,
          "description": "Significance level for the intervals.",
          "type": "number"
        },
        "forest": {
          "description": "``sp.causal_forest`` (GRF engine), ``sp.iv_forest``, ``sp.multi_arm_forest`` (one projection per contrast, stacked), ``sp.causal_survival_forest``.",
          "type": "string"
        },
        "vce": {
          "default": "HC3",
          "description": "Covariance type HC0-HC3 (sandwich::vcovCL conventions; grf's vcov.type). vcov_type= is accepted as an alias.",
          "type": "string"
        }
      },
      "required": [
        "forest"
      ],
      "type": "object"
    }
  },
  {
    "description": "Doubly-robust scores behind a forest's average effect (grf::get_scores): AIPW for causal and multi-arm forests, compliance-weighted for instrumental forests, censoring-adjusted for causal survival forests. Their mean is the average effect; use them for custom averages, subgroup contrasts or policy values. Validation: validated evidence tier (known-truth, reference, external-parity, or Monte Carlo artifact).",
    "name": "get_scores",
    "parameters": {
      "properties": {
        "forest": {
          "description": "``sp.causal_forest`` (GRF engine, binary or continuous treatment), ``sp.iv_forest`` (average conditional LATE), ``sp.multi_arm_forest`` (one column per contrast), ``sp.causal_survival_forest``.",
          "type": "string"
        }
      },
      "required": [
        "forest"
      ],
      "type": "object"
    }
  },
  {
    "description": "Rank-weighted average treatment effect (AUTOC / QINI) of a causal forest's targeting rule, fitted and evaluated on disjoint units. sp.rate ranks the rows it also scores, which for a fe= forest is not a valid test -- every imputation score carries -gamma_hat_t from the periods the forest saw, so a nominal 5% test rejected 17.5% of the time under no heterogeneity at all (AUTOC averaged -0.025, not 0). Splitting units (or dyadic members) brings that to 7.5% and +0.0008, at 99.5% power. Validation: validated evidence tier (known-truth, reference, external-parity, or Monte Carlo artifact).",
    "name": "rate_split",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "cluster": {
          "description": "None (forest clusters), 'dyadic' or cluster ids.",
          "type": "string"
        },
        "covariates": {
          "default": "none",
          "description": "fe= forests: covariates in the untreated outcome model.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "forest": {
          "description": "Fitted GRF-engine forest; supplies the data and the hyper-parameters, and is left untouched.",
          "type": "string"
        },
        "members": {
          "description": "(n, 2) members of each dyadic row; splits by member and drops rows straddling the halves.",
          "type": "string"
        },
        "n_splits": {
          "default": 21,
          "description": "Splits aggregated by Chernozhukov et al. (2025) VEIN: median estimate, median of conditional intervals built at 1 - alpha/2, p doubled. n_splits=1 is one draw and warns.",
          "type": "integer"
        },
        "q_grid": {
          "default": 100,
          "description": "Points on the TOC curve.",
          "type": "integer"
        },
        "random_state": {
          "default": 0,
          "description": "Seeds the sequence of splits.",
          "type": "integer"
        },
        "se_method": {
          "default": "auto",
          "description": "'auto' picks 'imputation' for fe= forests, 'influence' otherwise.",
          "enum": [
            "auto",
            "imputation",
            "influence",
            "half_sample"
          ],
          "type": "string"
        },
        "target": {
          "default": "AUTOC",
          "description": "RATE weighting.",
          "enum": [
            "AUTOC",
            "QINI"
          ],
          "type": "string"
        },
        "train_frac": {
          "default": 0.5,
          "description": "Share of units (members) that fit the rule.",
          "type": "number"
        },
        "variance": {
          "default": "bjs",
          "description": "fe= forests: 'bjs' is conservative for the population RATE, 'forest' exact for the realised sample's.",
          "enum": [
            "bjs",
            "forest"
          ],
          "type": "string"
        }
      },
      "required": [
        "forest"
      ],
      "type": "object"
    }
  },
  {
    "description": "Support diagnostics for counterfactual CATE predictions: for each new row, the forest CATE with its little-bag interval, effect modifiers outside the range of the rows that inform the effect (switching units for fe= forests), and the k-nearest-neighbour distance relative to the reference sample.",
    "name": "forest_support",
    "parameters": {
      "properties": {
        "X_new": {
          "description": "Effect modifiers to predict.",
          "type": "string"
        },
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "forest": {
          "description": "Fitted GRF-engine forest.",
          "type": "string"
        },
        "k": {
          "default": 10,
          "description": "k parameter (int).",
          "type": "integer"
        },
        "quantile": {
          "default": 0.95,
          "description": "quantile parameter (float).",
          "type": "number"
        }
      },
      "required": [
        "forest",
        "X_new"
      ],
      "type": "object"
    }
  },
  {
    "description": "Pre-trend test by predicted-effect group for causal forests with fixed effects: units sorted by their out-of-bag CATE, untreated cells regressed on unit and period effects and group x lead indicators, cluster-robust Wald tests that the leads are zero and equal across groups.",
    "name": "cate_pretrend_test",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "covariates": {
          "default": "none",
          "description": "Covariate matrix, DataFrame, or column names.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "forest": {
          "description": "Fitted fe= forest.",
          "type": "string"
        },
        "groups": {
          "description": "Unit-constant labels per row.",
          "type": "string"
        },
        "leads": {
          "description": "Default: up to 4.",
          "type": "integer"
        },
        "n_groups": {
          "default": 2,
          "description": "Number of groups.",
          "type": "integer"
        },
        "time_effects": {
          "default": "common",
          "description": "time_effects parameter (str).",
          "enum": [
            "common",
            "by_group"
          ],
          "type": "string"
        }
      },
      "required": [
        "forest"
      ],
      "type": "object"
    }
  },
  {
    "description": "Difference-in-differences causal forests for staggered adoption: one honest causal forest per clean Callaway-Sant'Anna group-time comparison on the long-differenced outcome, doubly-robust ATT(g,t), influence-function event study and overall ATT, unit-level CATEs and per-cell heterogeneity tests. Validation: validated evidence tier (known-truth, reference, external-parity, or Monte Carlo artifact).",
    "name": "did_forest",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Level for confidence intervals.",
          "type": "number"
        },
        "anticipation": {
          "default": 0,
          "description": "anticipation parameter (int).",
          "type": "integer"
        },
        "clusters": {
          "description": "Cluster column constant within unit.",
          "type": "string"
        },
        "cohort": {
          "description": "First treatment period (0/NaN = never treated).",
          "type": "string"
        },
        "control_group": {
          "default": "notyettreated",
          "description": "control_group parameter (str).",
          "enum": [
            "notyettreated",
            "nevertreated"
          ],
          "type": "string"
        },
        "data": {
          "description": "Long panel.",
          "type": "string"
        },
        "event_window": {
          "description": "event_window parameter (tuple).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "forest_kwargs": {
          "description": "Extra sp.CausalForest arguments (min_samples_leaf, mtry, honesty_fraction, ...).",
          "type": "object"
        },
        "id": {
          "description": "Unit id column; unit= is an alias.",
          "type": "string"
        },
        "min_group_size": {
          "default": 20,
          "description": "min_group_size parameter (int).",
          "type": "integer"
        },
        "n_estimators": {
          "default": 1000,
          "description": "Trees per group-time forest.",
          "type": "integer"
        },
        "n_jobs": {
          "default": 1,
          "description": "Threads per forest (-1 = all cores).",
          "type": "integer"
        },
        "propensity_clip": {
          "default": 0.001,
          "description": "Upper clip for the cohort-membership probability in the control weights e/(1-e).",
          "type": "number"
        },
        "random_state": {
          "default": 0,
          "description": "Seed; cell k uses random_state + k.",
          "type": "integer"
        },
        "time": {
          "description": "Numeric period.",
          "type": "string"
        },
        "x": {
          "description": "Time-invariant effect modifiers.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "id",
        "time",
        "cohort",
        "x"
      ],
      "type": "object"
    }
  },
  {
    "description": "Meta-learner framework for CATE: S-, T-, X-, R-, DR-Learner. Validation: certified parity evidence.",
    "name": "metalearner",
    "parameters": {
      "properties": {
        "covariates": {
          "description": "Covariate matrix, DataFrame, or column names.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "fold_indices": {
          "description": "Explicit cross-fitting partition: one integer label 0..n_folds-1 per row of data. Replaces every internal split (R/DR nuisances and the AIPW cross-fit behind estimate/se). Default None keeps KFold(n_folds, shuffle=True, random_state=42); passing that split's labels reproduces the default exactly.",
          "type": "string"
        },
        "learner": {
          "default": "dr",
          "description": "Learner type",
          "enum": [
            "s",
            "t",
            "x",
            "r",
            "dr"
          ],
          "type": "string"
        },
        "treat": {
          "description": "Binary treatment column (0/1)",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "treat",
        "covariates"
      ],
      "type": "object"
    }
  },
  {
    "description": "Propensity score and covariate matching for treatment effect estimation. Validation: certified evidence with scoped limitations. Known limitations: greedy nearest-neighbour matching without replacement is order-dependent: the m_order convention can differ across packages and materially moves the estimate (>5x spread on MatchIt::lalonde with Mahalanobis distance). m_order='data' and 'closest' reproduce MatchIt exactly; m_order='farthest' is StatsPAI's own dynamic rule and is not MatchIt-equivalent; bias_correction=True follows a different convention from Matching::Match's BiasAdjust: StatsPAI regresses on the full covariate vector with unweighted OLS over all controls, the reference regresses on the matching variables weighted by match counts, so bias-corrected estimates can differ from it by about 0.1%. The uncorrected estimate and its Abadie-Imbens standard error are exact. Cost: Builds the dense n_treated x n_control distance matrix via scipy cdist before selecting neighbours: memory is O(n_treated x n_control). Comfortable into the thousands per arm; use a caliper or coarser blocking beyond that.",
    "name": "match",
    "parameters": {
      "properties": {
        "bootstrap_reps": {
          "default": 200,
          "description": "Replications for se_method='bootstrap'",
          "type": "integer"
        },
        "bootstrap_seed": {
          "description": "Seed for the bootstrap resampler",
          "type": "integer"
        },
        "caliper_scale": {
          "default": "raw",
          "description": "Units of `caliper`: 'raw' on the distance scale (Stata psmatch2) or 'sd' in standard deviations of the propensity score (MatchIt std.caliper=TRUE).",
          "enum": [
            "raw",
            "sd"
          ],
          "type": "string"
        },
        "covariates": {
          "description": "Covariate matrix, DataFrame, or column names.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "llr_stata_compat": {
          "default": false,
          "description": "method='llr' only: reproduce Stata psmatch2's SUBSTITUTE for LLR (lpoly-smoothed outcome + nearest-neighbour matching) rather than genuine local linear regression.",
          "type": "boolean"
        },
        "m_order": {
          "default": "smallest_min_dist",
          "description": "Order treated units are processed in for greedy matching without replacement. The result depends on it materially (>5x spread on lalonde with Mahalanobis distance). 'data' and 'closest' reproduce the MatchIt rules of the same name.",
          "enum": [
            "smallest_min_dist",
            "data",
            "closest",
            "farthest",
            "smallest",
            "largest"
          ],
          "type": "string"
        },
        "mahalanobis_cov": {
          "default": "pooled",
          "description": "Covariance defining the Mahalanobis metric: 'pooled' within-group (Rubin 1980, MatchIt) or 'total' full-sample (pre-1.21 behaviour).",
          "enum": [
            "pooled",
            "total"
          ],
          "type": "string"
        },
        "method": {
          "default": "nearest",
          "description": "Matching method. 'caliper' is not a method -- pass caliper= to any nearest-neighbour variant.",
          "enum": [
            "nearest",
            "psm",
            "mahalanobis",
            "kernel",
            "radius",
            "llr",
            "stratify",
            "cem",
            "optimal",
            "genmatch",
            "cardinality",
            "ebalance",
            "cbps",
            "sbw",
            "overlap"
          ],
          "type": "string"
        },
        "outcome": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        },
        "se_method": {
          "default": "auto",
          "description": "Standard error. 'auto' resolves to 'abadie_imbens' for nearest-neighbour, 'psmatch2' for kernel / radius, 'bootstrap' for llr. 'abadie_imbens' is the sample-ATT conditional variance (Stata psmatch2 ai()) and the only option measured to be correctly sized (0.95-1.04x the sampling SD, coverage 0.905-0.956 over 36 designs x 1000 reps; see benchmarks/matching_se_coverage.py). 'abadie_imbens_pop' is the population-ATT variance Matching::Match reports; 'psmatch2' the analytic Stata SE (1.50-1.69x, too wide); 'ai' the simple matched-pair SE (0.56-0.91x, never reaches nominal coverage); 'bootstrap' resamples within arm and re-estimates the propensity score each draw. 'abadie_imbens_2016' is the Abadie-Imbens (2016) estimated-propensity-score variance Stata teffects psmatch reports (population ATT; the score-estimation term can be negative).",
          "enum": [
            "auto",
            "ai",
            "psmatch2",
            "abadie_imbens",
            "abadie_imbens_pop",
            "abadie_imbens_2016",
            "bootstrap"
          ],
          "type": "string"
        },
        "tie_tolerance": {
          "default": 0.0,
          "description": "With ties='all', how close squared distances (scaled by the variance of the distance measure) must be to count as tied. 1e-5 reproduces Matching::Match's distance.tolerance default.",
          "type": "number"
        },
        "ties": {
          "default": "first",
          "description": "How equidistant controls are handled under matching with replacement. 'first' keeps the lowest-index one; 'all' pools them and splits the weight (the Matching::Match convention, which removes the row-order dependence).",
          "enum": [
            "first",
            "all"
          ],
          "type": "string"
        },
        "treatment": {
          "description": "Treatment indicator, treatment variable, or treatment array.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "treatment",
        "outcome",
        "covariates"
      ],
      "type": "object"
    }
  },
  {
    "description": "Stata psmatch2-faithful supported propensity-score matching paths (nearest-neighbour, kernel, radius, local linear regression, Mahalanobis): returns matched-sample variables (_pscore _treated _support _weight _y; plus _n1 through _nn _pdif for nearest-neighbour), the psmatch2 analytic ATT standard error, plus post-matching balance (.pstest() reproduces Stata pstest exactly), common-support plotting, and weighted PSM-DID. Validation: certified parity evidence.",
    "name": "psmatch2",
    "parameters": {
      "properties": {
        "ai": {
          "default": 0,
          "description": "Abadie-Imbens (2006) robust SE with J within-arm matches (Stata ai(J))",
          "type": "integer"
        },
        "bootstrap_reps": {
          "default": 200,
          "description": "Bootstrap replications when se='bootstrap'",
          "type": "integer"
        },
        "bootstrap_seed": {
          "description": "Seed for the bootstrap resampler (pass for reproducibility)",
          "type": "integer"
        },
        "bwidth": {
          "default": 0.06,
          "description": "Kernel bandwidth (method='kernel')",
          "type": "number"
        },
        "caliper": {
          "description": "Max PS distance / radius bandwidth",
          "type": "number"
        },
        "common_support": {
          "default": "none",
          "description": "Common-support trimming",
          "enum": [
            "none",
            "minmax"
          ],
          "type": "string"
        },
        "covariates": {
          "description": "Covariate matrix, DataFrame, or column names.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "kernel": {
          "default": "epan",
          "description": "Kernel type (method='kernel' or 'llr'). NOTE: Stata's psmatch2 does not run LLR with kerneltype(epan) -- it substitutes lpoly-smoothed nearest-neighbour matching. Use 'tricube' to reproduce psmatch2's own LLR routine.",
          "enum": [
            "epan",
            "normal",
            "biweight",
            "uniform",
            "tricube"
          ],
          "type": "string"
        },
        "llr_stata_compat": {
          "default": false,
          "description": "method='llr' only: reproduce Stata psmatch2's SUBSTITUTE for LLR (lpoly-smoothed outcome + nearest-neighbour matching) instead of genuine local linear regression. Set only to reconcile a published psmatch2 number.",
          "type": "boolean"
        },
        "method": {
          "default": "neighbor",
          "description": "Matching algorithm",
          "enum": [
            "neighbor",
            "kernel",
            "radius",
            "llr",
            "mahalanobis"
          ],
          "type": "string"
        },
        "neighbor": {
          "default": 1,
          "description": "Number of nearest neighbours k",
          "type": "integer"
        },
        "outcome": {
          "description": "Outcome variable (Stata outcome(); optional)",
          "type": "string"
        },
        "se": {
          "default": "psmatch2",
          "description": "Standard-error estimator. 'bootstrap' re-estimates the propensity score each replication and is the only valid choice for method='llr' (Stata reports seatt = . there).",
          "enum": [
            "psmatch2",
            "ai",
            "abadie_imbens",
            "bootstrap"
          ],
          "type": "string"
        },
        "treat": {
          "description": "Binary treatment column (0/1)",
          "type": "string"
        }
      },
      "required": [
        "data",
        "treat",
        "covariates"
      ],
      "type": "object"
    }
  },
  {
    "description": "Targeted Maximum Likelihood Estimation for ATE/ATT with double-robustness. Validation: certified parity evidence.",
    "name": "tmle",
    "parameters": {
      "properties": {
        "covariates": {
          "description": "Covariate matrix, DataFrame, or column names.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "estimand": {
          "default": "ATE",
          "description": "Target estimand",
          "type": "string"
        },
        "fold_indices": {
          "description": "Opt-in CV-TMLE: one integer label 0..K-1 (K >= 2) per row of data. Q and g are then Super Learners fitted outside each fold and predicted inside it; the fluctuation is fitted on the pooled out-of-fold predictions and the SE is the EIF at the targeted fits. The default (None) fits both nuisances on the full sample (not cross-fitted). Not combinable with Q / g1W.",
          "type": "string"
        },
        "q_bound": {
          "default": 1e-05,
          "description": "The initial outcome predictions, on the [0, 1] scale the logistic fluctuation works on (continuous outcomes are min-max rescaled), are truncated to ``[q_bound, 1 - q_bound]`` before targeting. ``tmle::tmle`` truncates at ``1 - alpha = 5e-4`` (its default ``alpha = 0.9995``); pass ``q_bound=5e-4`` to reproduce it.",
          "type": "number"
        },
        "treat": {
          "description": "Binary treatment column (0/1)",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "treat",
        "covariates"
      ],
      "type": "object"
    }
  },
  {
    "description": "Unified panel regression: FE, RE, between, FD, pooled OLS, two-way FE, Mundlak/Chamberlain CRE, Arellano-Bond, Blundell-Bond system GMM. Results include built-in diagnostics: .hausman_test(), .bp_lm_test(), .f_test_effects(), .pesaran_cd_test(), .compare(method). Validation: certified parity evidence.",
    "name": "panel",
    "parameters": {
      "properties": {
        "cluster": {
          "description": "Cluster variable: entity, time, or twoway",
          "type": "string"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "entity": {
          "description": "Unit identifier column",
          "type": "string"
        },
        "formula": {
          "description": "Regression formula: 'y ~ x1 + x2'",
          "type": "string"
        },
        "gmm_lags": {
          "default": "(2, 5)",
          "description": "GMM instrument lag range",
          "type": "string"
        },
        "lags": {
          "default": 1,
          "description": "AR lags for dynamic panel (ab/system)",
          "type": "integer"
        },
        "method": {
          "default": "fe",
          "description": "Estimation method",
          "enum": [
            "fe",
            "re",
            "be",
            "fd",
            "pooled",
            "twoway",
            "mundlak",
            "cre",
            "chamberlain",
            "ab",
            "system"
          ],
          "type": "string"
        },
        "robust": {
          "default": "nonrobust",
          "description": "Standard errors: nonrobust, robust, kernel, driscoll-kraay",
          "type": "string"
        },
        "time": {
          "description": "Time column",
          "type": "string"
        },
        "twostep": {
          "default": false,
          "description": "Two-step GMM",
          "type": "boolean"
        }
      },
      "required": [
        "data",
        "formula",
        "entity",
        "time"
      ],
      "type": "object"
    }
  },
  {
    "description": "Estimate the same model with multiple panel methods and return a side-by-side comparison table.",
    "name": "panel_compare",
    "parameters": {
      "properties": {
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "entity": {
          "description": "Panel entity identifier column.",
          "type": "string"
        },
        "formula": {
          "description": "Model formula using patsy/R-style syntax.",
          "type": "string"
        },
        "methods": {
          "description": "List of methods to compare, default: pooled/fe/re/twoway/mundlak",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "time": {
          "description": "Time period column.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "formula",
        "entity",
        "time"
      ],
      "type": "object"
    }
  },
  {
    "description": "Arellano-Bond / Blundell-Bond GMM for dynamic panels (standalone). Validation: certified parity evidence.",
    "name": "xtabond",
    "parameters": {
      "properties": {
        "ah_instrument": {
          "default": "levels",
          "description": "Anderson-Hsiao instrument for method='ah'",
          "enum": [
            "levels",
            "differences"
          ],
          "type": "string"
        },
        "cluster": {
          "description": "Cluster SEs on a coarser unit than the panel id (must be constant within unit)",
          "type": "string"
        },
        "collapse": {
          "default": false,
          "description": "Collapse instruments (Roodman 2009) to curb proliferation",
          "type": "boolean"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "endogenous": {
          "description": "Endogenous regressors (own lags 2+ as instruments)",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "h": {
          "default": 3,
          "description": "xtabond2 h(): one-step error covariance; 3 is xtabond2's default, 2 zeroes the system cross quadrants (Stata xtdpdsys), 1 the identity.",
          "type": "integer"
        },
        "id": {
          "default": "id",
          "description": "Unit identifier",
          "type": "string"
        },
        "iv_equation": {
          "description": "System GMM: equation(s) the exogenous regressors instrument; None means 'both' (xtabond2), 'diff' is Stata xtdpdsys.",
          "enum": [
            "both",
            "diff",
            "level"
          ],
          "type": "string"
        },
        "lags": {
          "default": 1,
          "description": "lags parameter (int).",
          "type": "integer"
        },
        "method": {
          "default": "difference",
          "description": "difference (Arellano-Bond), system (Blundell-Bond) or ah (Anderson-Hsiao IV)",
          "enum": [
            "difference",
            "system",
            "ah"
          ],
          "type": "string"
        },
        "orthogonal": {
          "default": false,
          "description": "Forward orthogonal deviations instead of first differences",
          "type": "boolean"
        },
        "predetermined": {
          "description": "Predetermined regressors (own lags 1+ as instruments)",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "steps": {
          "description": "Number of GMM steps, or 'iterated' / 'cue'",
          "type": "string"
        },
        "time": {
          "default": "time",
          "description": "Time column",
          "type": "string"
        },
        "time_dummies": {
          "default": false,
          "description": "Add period dummies as regressors and instruments",
          "type": "boolean"
        },
        "twostep": {
          "default": false,
          "description": "twostep parameter (bool).",
          "type": "boolean"
        },
        "x": {
          "description": "Exogenous regressors",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Dependent variable",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y"
      ],
      "type": "object"
    }
  },
  {
    "description": "Blundell-Bond system GMM for dynamic panels (alias for xtabond with method='system'). Validation: certified parity evidence.",
    "name": "xtdpdsys",
    "parameters": {
      "properties": {
        "collapse": {
          "default": false,
          "description": "Collapse instruments (Roodman 2009) to curb proliferation",
          "type": "boolean"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "h": {
          "default": 2,
          "description": "xtabond2 h(): one-step error covariance; 2 is Stata xtdpdsys, 3 xtabond2's default, 1 the identity.",
          "type": "integer"
        },
        "id": {
          "default": "id",
          "description": "Unit identifier",
          "type": "string"
        },
        "iv_equation": {
          "default": "diff",
          "description": "Equation(s) the exogenous regressors instrument; 'diff' is Stata xtdpdsys, 'both' is xtabond2's iv() default.",
          "enum": [
            "diff",
            "level",
            "both"
          ],
          "type": "string"
        },
        "lags": {
          "default": 1,
          "description": "lags parameter (int).",
          "type": "integer"
        },
        "time": {
          "default": "time",
          "description": "Time column",
          "type": "string"
        },
        "twostep": {
          "default": false,
          "description": "twostep parameter (bool).",
          "type": "boolean"
        },
        "x": {
          "description": "Exogenous regressors",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Dependent variable",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y"
      ],
      "type": "object"
    }
  },
  {
    "description": "Causal impact of an intervention on a time series: pre-period OLS on the covariate series with an AR(1) latent state (Kalman filter), post-period counterfactual forecasts and frequentist prediction intervals for pointwise, average and cumulative effects. Not the Bayesian structural time-series model of R CausalImpact. Validation: validated evidence tier (known-truth, reference, external-parity, or Monte Carlo artifact).",
    "name": "causal_impact",
    "parameters": {
      "properties": {
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "intervention_time": {
          "description": "Date/index of intervention",
          "type": "string"
        },
        "time": {
          "description": "Time / date column",
          "type": "string"
        },
        "y": {
          "description": "Outcome time-series column",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "time",
        "intervention_time"
      ],
      "type": "object"
    }
  },
  {
    "description": "Declare a complex survey design for design-corrected estimation. Validation: certified parity evidence.",
    "name": "svydesign",
    "parameters": {
      "properties": {
        "cluster": {
          "description": "PSU cluster variable",
          "type": "string"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "fpc": {
          "description": "Finite population correction column",
          "type": "string"
        },
        "lonely_psu": {
          "description": "How a stratum with a single sampled PSU enters the variance (R options(survey.lonely.psu=)): \"remove\"/\"certainty\" contribute zero, \"adjust\" centres at the grand mean of PSU totals, \"average\" rescales by #strata / #strata-with->1-PSU, \"fail\" raises (R default). None behaves as \"remove\" and warns when a lonely stratum is present.",
          "enum": [
            "remove",
            "certainty",
            "adjust",
            "average",
            "fail"
          ],
          "type": "string"
        },
        "strata": {
          "description": "Stratification variable",
          "type": "string"
        },
        "weights": {
          "description": "Sampling weights column",
          "type": "string"
        }
      },
      "required": [
        "data",
        "weights"
      ],
      "type": "object"
    }
  },
  {
    "description": "Export regression results to publication-quality tables (Excel, LaTeX, Word).",
    "name": "outreg2",
    "parameters": {
      "properties": {
        "decimal_places": {
          "description": "Decimals for coefficients and SEs. Unset uses adaptive precision (an estimate and its own SE share a decimal place); pass an int for fixed Stata-style rendering",
          "type": "integer"
        },
        "filename": {
          "description": "Output file path (.xlsx, .tex, .docx)",
          "type": "string"
        },
        "results": {
          "description": "One or more EconometricResults objects",
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "required": [
        "results",
        "filename"
      ],
      "type": "object"
    }
  },
  {
    "description": "Summary table comparing multiple models side by side.",
    "name": "modelsummary",
    "parameters": {
      "properties": {
        "digits": {
          "description": "Decimal places for coefficients; SEs follow unless se_fmt overrides them. Shorthand for the paired coefficient/SE precision controls.",
          "type": "integer"
        },
        "fmt": {
          "description": "Numeric precision, shared vocabulary with sp.regtable: 'auto' (default) pairs an estimate with its own SE, an int N or '%.Nf' fixes decimals, 'r3'/'s3' are R fixest's round / significant-digit spellings",
          "type": "integer"
        },
        "results": {
          "description": "List of EconometricResults",
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "required": [
        "results"
      ],
      "type": "object"
    }
  },
  {
    "description": "Publication-quality multi-model regression table with auto-extracted diagnostic rows (FE/Cluster indicators, IV first-stage F, DiD pre-trend p, RD bandwidth/kernel/poly), journal presets (AER/QJE/Econometrica/JF/AEJA/etc.), multi-SE side-by-side display, eform odds-ratio / IRR / HR transformation with delta-method SE, column spanners (\\multicolumn / colspan / cmidrule), unified coef_map (rename + order + drop), depvar_mean / depvar_sd auto rows, and N-mismatch consistency warnings. Returns a RegtableResult exporting to text/LaTeX/HTML/Markdown/Quarto/Word/Excel plus an agent-native to_dict()/to_json() payload (metadata + rendered cell grid + numeric truth per model); save(filename) and filename= infer the format from the extension, including .json.",
    "name": "regtable",
    "parameters": {
      "properties": {
        "apply_coef": {
          "description": "Arbitrary coefficient transform (generalises eform); mutually exclusive with eform",
          "type": "string"
        },
        "apply_coef_deriv": {
          "description": "Derivative of apply_coef for delta-method SE rescaling",
          "type": "string"
        },
        "coef_map": {
          "description": "Single-shot rename + reorder + drop (mutually exclusive with coef_labels/keep/drop/order)",
          "type": "object"
        },
        "column_spanners": {
          "description": "Multi-row header: list of (label, span) tuples whose spans partition the model columns (e.g. [('OLS', 2), ('IV', 2)])",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "consistency_check": {
          "default": true,
          "description": "Warn when sample sizes differ across columns",
          "type": "boolean"
        },
        "diagnostics": {
          "default": "auto",
          "description": "Auto-extract FE/Cluster/IV/DiD/RD rows",
          "type": "boolean"
        },
        "digits": {
          "description": "Int alias for fmt: digits=3 is fmt='%.3f'. Passing both fmt and digits raises",
          "type": "integer"
        },
        "eform": {
          "default": false,
          "description": "Report exp(b) (OR/IRR/HR) with delta-method SE; pass per-model list to mix transformed/untransformed columns",
          "type": "boolean"
        },
        "escape": {
          "default": true,
          "description": "Auto-escape user-supplied label strings; pass False to preserve raw LaTeX/HTML markup verbatim",
          "type": "boolean"
        },
        "estimate": {
          "description": "Top-line cell template -- placeholders {estimate} {stars} {std_error} {t_value} {p_value} {conf_low} {conf_high}",
          "type": "string"
        },
        "filename": {
          "description": "File path; format inferred from extension",
          "type": "string"
        },
        "fixef_sizes": {
          "default": false,
          "description": "Auto-emit '# Firm: N' rows from model_info['n_fe_levels']",
          "type": "boolean"
        },
        "fmt": {
          "description": "Coefficient/SE precision. Unset resolves to the journal template's precision, else 'auto', which picks decimals per row so an estimate and its own SE always agree (journal style); an int N or printf '%.Nf' fixes it",
          "type": "integer"
        },
        "multi_se": {
          "description": "Stack alternative SE specs under primary SE",
          "type": "object"
        },
        "notation": {
          "default": "stars",
          "description": "Significance marker family",
          "enum": [
            "stars",
            "symbols"
          ],
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "output": {
          "default": "text",
          "description": "Render format",
          "enum": [
            "text",
            "latex",
            "html",
            "markdown",
            "word",
            "excel"
          ],
          "type": "string"
        },
        "repro": {
          "description": "Append reproducibility footer (version+seed+data hash)",
          "type": "boolean"
        },
        "results": {
          "description": "Model result objects (positional)",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "rules": {
          "default": "auto",
          "description": "Horizontal-rule characters of the plain-text table. 'auto' uses box-drawing rules where stdout can encode them and folds to ASCII where it cannot (a Windows cp1252 console raises UnicodeEncodeError on the box-drawing glyphs); 'unicode' and 'ascii' pin the choice for reproducible output",
          "enum": [
            "auto",
            "unicode",
            "ascii"
          ],
          "type": "string"
        },
        "se_fmt": {
          "description": "Override precision of the SE row alone; defaults to fmt so a coefficient and its SE stay paired",
          "type": "integer"
        },
        "se_type": {
          "default": "se",
          "description": "Bottom-row content",
          "enum": [
            "se",
            "t",
            "p",
            "ci"
          ],
          "type": "string"
        },
        "statistic": {
          "description": "Bottom-line cell template (same placeholders as estimate)",
          "type": "string"
        },
        "stats_fmt": {
          "default": "%.3f",
          "description": "Precision for summary-statistic rows (R2, adj. R2, F); independent of fmt because they are on their own scale",
          "type": "integer"
        },
        "template": {
          "description": "Journal preset",
          "enum": [
            "aer",
            "qje",
            "econometrica",
            "restat",
            "jf",
            "aeja",
            "jpe",
            "restud"
          ],
          "type": "string"
        },
        "tests": {
          "description": "Hypothesis-test rows: {label: [(stat,p) | p | None per model]} (stars honour notation)",
          "type": "object"
        },
        "transpose": {
          "default": false,
          "description": "Pivot rows<->columns (single-panel; rejects multi_se)",
          "type": "boolean"
        },
        "vcov": {
          "description": "Recompute SE/t/p/CI at print time (OLS-only)",
          "enum": [
            "HC0",
            "HC1",
            "HC2",
            "HC3",
            "robust"
          ],
          "type": "string"
        }
      },
      "required": [
        "results"
      ],
      "type": "object"
    }
  },
  {
    "description": "Stata-style esttab clone -- tabulate one or more model results (or models stored via sp.eststo) into text/LaTeX/HTML/Markdown/CSV.",
    "name": "esttab",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "CI level when ci=True",
          "type": "number"
        },
        "ci": {
          "default": false,
          "description": "Show confidence intervals instead of SE",
          "type": "boolean"
        },
        "digits": {
          "description": "Decimal places for coefficients; SEs follow unless se_fmt overrides them. Shorthand for the paired coefficient/SE precision controls.",
          "type": "integer"
        },
        "fmt": {
          "description": "Numeric precision, shared vocabulary with sp.regtable: 'auto' (default) pairs an estimate with its own SE, an int N or '%.Nf' fixes decimals, 'r3'/'s3' are R fixest's round / significant-digit spellings",
          "type": "integer"
        },
        "results": {
          "description": "Models; falls back to global eststo store",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "se": {
          "default": true,
          "description": "Show standard errors",
          "type": "boolean"
        }
      },
      "required": [],
      "type": "object"
    }
  },
  {
    "description": "Multi-panel paper-facing table bundle (Main / Heterogeneity / Robustness / Placebo) with one-shot export to LaTeX/Markdown/Word/Excel.",
    "name": "paper_tables",
    "parameters": {
      "properties": {
        "heterogeneity": {
          "description": "Subsample / interaction results",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "main": {
          "description": "Main-spec results",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "placebo": {
          "description": "Placebo-outcome results",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "robustness": {
          "description": "Alt-estimator results",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "template": {
          "default": "aer",
          "description": "Journal preset",
          "enum": [
            "aer",
            "qje",
            "econometrica",
            "restat",
            "jf",
            "aeja",
            "jpe",
            "restud"
          ],
          "type": "string"
        }
      },
      "required": [
        "main"
      ],
      "type": "object"
    }
  },
  {
    "description": "Inline coefficient citation -- formats one term as e.g. '0.234*** (0.041)' for embedding directly in manuscript prose, Jupyter Markdown cells, or Quarto inline expressions. Mirrors regtable's formatting conventions (stars, SE/CI brackets) for cross-table consistency.",
    "name": "cite",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "CI level when second_row='ci'",
          "type": "number"
        },
        "fmt": {
          "default": "%.3f",
          "description": "printf-style format string",
          "type": "string"
        },
        "output": {
          "default": "text",
          "description": "Markup",
          "enum": [
            "text",
            "latex",
            "markdown",
            "html"
          ],
          "type": "string"
        },
        "result": {
          "description": "EconometricResults or CausalResult",
          "type": "string"
        },
        "second_row": {
          "default": "se",
          "description": "What to put in parens",
          "enum": [
            "se",
            "t",
            "p",
            "ci",
            "none"
          ],
          "type": "string"
        },
        "term": {
          "description": "Coefficient name (default: estimand or first param)",
          "type": "string"
        }
      },
      "required": [
        "result"
      ],
      "type": "object"
    }
  },
  {
    "description": "Balance / mean-comparison table -- Mean (SD) per group, difference, and t-test/ranksum/chi2 p-value. Renders to text/LaTeX/HTML/Markdown/Excel/Word.",
    "name": "mean_comparison",
    "parameters": {
      "properties": {
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "digits": {
          "description": "Int alias for fmt: digits=3 is fmt='%.3f'. Passing both fmt and digits raises",
          "type": "integer"
        },
        "fmt": {
          "description": "Numeric precision, shared vocabulary with sp.regtable: 'auto' (default) pairs an estimate with its own SE, an int N or '%.Nf' fixes decimals, 'r3'/'s3' are R fixest's round / significant-digit spellings",
          "type": "integer"
        },
        "group": {
          "description": "Binary grouping variable",
          "type": "string"
        },
        "test": {
          "default": "ttest",
          "description": "Statistical test",
          "enum": [
            "ttest",
            "ranksum",
            "chi2"
          ],
          "type": "string"
        },
        "variables": {
          "description": "Columns to compare",
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "required": [
        "data",
        "variables",
        "group"
      ],
      "type": "object"
    }
  },
  {
    "description": "Session-level multi-table container (Stata 15 collect / R gt::gtsave style). Gather regressions, summary stats, balance tables, and free-form text in one Collection, then export the whole bundle to a single .docx / .xlsx / .tex / .md / .html file.",
    "name": "collect",
    "parameters": {
      "properties": {
        "template": {
          "default": "aer",
          "description": "Journal style template",
          "enum": [
            "aer",
            "qje",
            "econometrica",
            "restat"
          ],
          "type": "string"
        },
        "title": {
          "description": "Document title shown at the top",
          "type": "string"
        }
      },
      "required": [],
      "type": "object"
    }
  },
  {
    "description": "Sensitivity analysis for omitted variable bias (Cinelli & Hazlett 2020). Validation: certified parity evidence.",
    "name": "sensemakr",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "benchmark": {
          "description": "Covariates to benchmark confounding strength against",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "controls": {
          "description": "Observed control variables",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "treat": {
          "description": "Treatment column of interest",
          "type": "string"
        },
        "y": {
          "description": "Outcome column",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "treat",
        "controls"
      ],
      "type": "object"
    }
  },
  {
    "description": "Specification curve analysis -- run many model specifications and visualise robustness.",
    "name": "spec_curve",
    "parameters": {
      "properties": {
        "controls": {
          "description": "Candidate control sets to sweep",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "x": {
          "description": "Treatment / focal regressor",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "x"
      ],
      "type": "object"
    }
  },
  {
    "description": "Inverse Probability Weighting for ATE/ATT/ATC with propensity score trimming. Validation: certified parity evidence.",
    "name": "ipw",
    "parameters": {
      "properties": {
        "covariates": {
          "description": "Covariate matrix, DataFrame, or column names.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "estimand": {
          "default": "ATE",
          "description": "Target estimand",
          "enum": [
            "ATE",
            "ATT",
            "ATC"
          ],
          "type": "string"
        },
        "treat": {
          "description": "Treatment indicator or first-treatment-period column.",
          "type": "string"
        },
        "trim": {
          "default": 0.0,
          "description": "Propensity score trimming threshold",
          "type": "number"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "treat",
        "covariates"
      ],
      "type": "object"
    }
  },
  {
    "description": "Declare a causal DAG and perform identification analysis: backdoor/frontdoor adjustment sets, d-separation, path enumeration, bad controls detection, variable role classification, do-operator. Validation: validated evidence tier (known-truth, reference, external-parity, or Monte Carlo artifact).",
    "name": "dag",
    "parameters": {
      "properties": {
        "spec": {
          "description": "Edge spec: \"Z -> X; Z -> Y; X -> Y\"",
          "type": "string"
        }
      },
      "required": [
        "spec"
      ],
      "type": "object"
    }
  },
  {
    "description": "Load a classic textbook DAG: confounding, collider, mediation, discrimination, movie_star, police, frontdoor, bad_control_earnings, m_bias.",
    "name": "dag_example",
    "parameters": {
      "properties": {
        "name": {
          "description": "Example name, e.g. 'discrimination'",
          "type": "string"
        }
      },
      "required": [
        "name"
      ],
      "type": "object"
    }
  },
  {
    "description": "Augmented Synthetic Control with ridge bias correction (Ben-Michael et al. 2021). Validation: certified parity evidence. Do NOT use when: pre-treatment fit from plain sp.synth is already good -- the ridge augmentation mainly buys bias correction for poor fit and adds a tuning parameter to justify; many treated units -- use sp.gsynth or sp.sdid.",
    "name": "augsynth",
    "parameters": {
      "properties": {
        "backend": {
          "default": "native",
          "description": "Computation backend: native or augsynth/R bridge backend",
          "type": "string"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "outcome": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        },
        "time": {
          "description": "Time period column.",
          "type": "string"
        },
        "treated_unit": {
          "description": "treated_unit parameter (str).",
          "type": "string"
        },
        "treatment_time": {
          "description": "treatment_time parameter (int).",
          "type": "integer"
        },
        "unit": {
          "description": "Unit identifier column.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "outcome",
        "unit",
        "time",
        "treated_unit",
        "treatment_time"
      ],
      "type": "object"
    }
  },
  {
    "description": "Spatial Autoregressive (Lag) Model: Y = rhoWY + Xbeta + epsilon via ML. Validation: certified parity evidence.",
    "name": "sar",
    "parameters": {
      "properties": {
        "W": {
          "description": "(n,n) spatial weights matrix",
          "type": "string"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "formula": {
          "description": "'y ~ x1 + x2'",
          "type": "string"
        }
      },
      "required": [
        "W",
        "data",
        "formula"
      ],
      "type": "object"
    }
  },
  {
    "description": "Spatial Error Model: Y = Xbeta + u, u = lambdaWu + epsilon via ML. Validation: certified parity evidence.",
    "name": "sem",
    "parameters": {
      "properties": {
        "W": {
          "description": "(n,n) spatial weights matrix",
          "type": "string"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "formula": {
          "description": "Model formula using patsy/R-style syntax.",
          "type": "string"
        }
      },
      "required": [
        "W",
        "data",
        "formula"
      ],
      "type": "object"
    }
  },
  {
    "description": "Spatial Durbin Model: Y = rhoWY + Xbeta + WXtheta + epsilon with direct/indirect effects. Validation: certified parity evidence.",
    "name": "sdm",
    "parameters": {
      "properties": {
        "W": {
          "description": "(n,n) spatial weights matrix",
          "type": "string"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "formula": {
          "description": "Model formula using patsy/R-style syntax.",
          "type": "string"
        }
      },
      "required": [
        "W",
        "data",
        "formula"
      ],
      "type": "object"
    }
  },
  {
    "description": "General bootstrap inference: nonparametric, cluster, block. Percentile/BCa/normal CIs. Validation: validated evidence tier (known-truth, reference, external-parity, or Monte Carlo artifact).",
    "name": "bootstrap",
    "parameters": {
      "properties": {
        "ci_method": {
          "default": "percentile",
          "description": "CI method",
          "enum": [
            "percentile",
            "bca",
            "normal"
          ],
          "type": "string"
        },
        "cluster": {
          "description": "Cluster variable for cluster bootstrap",
          "type": "string"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "n_boot": {
          "default": 1000,
          "description": "Number of bootstrap replications.",
          "type": "integer"
        },
        "statistic": {
          "description": "Function f(df) -> float",
          "type": "string"
        }
      },
      "required": [
        "data",
        "statistic"
      ],
      "type": "object"
    }
  },
  {
    "description": "Method-aware diagnostic battery: auto-selects tests by model type (OLS/DID/RDD/IV/SCM).",
    "name": "diagnose_result",
    "parameters": {
      "properties": {
        "result": {
          "description": "Fitted result from any StatsPAI estimator",
          "type": "string"
        }
      },
      "required": [
        "result"
      ],
      "type": "object"
    }
  },
  {
    "description": "Parametric g-formula (standardization) estimator. ATE/ATT for binary D, or dose-response curve for continuous D. Consistent under correctly-specified outcome model; not doubly robust. Validation: certified parity evidence.",
    "name": "g_computation",
    "parameters": {
      "properties": {
        "covariates": {
          "description": "Baseline covariates",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "estimand": {
          "default": "ATE",
          "description": "Target estimand",
          "enum": [
            "ATE",
            "ATT",
            "dose_response"
          ],
          "type": "string"
        },
        "n_boot": {
          "default": 500,
          "description": "Bootstrap replications for SE",
          "type": "integer"
        },
        "treat": {
          "description": "Treatment variable",
          "type": "string"
        },
        "treat_values": {
          "description": "Dose grid (required for dose_response)",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Outcome",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "treat",
        "covariates"
      ],
      "type": "object"
    }
  },
  {
    "description": "Pearl's front-door adjustment: identifies ATE with unmeasured confounding when a mediator fully transmits the effect of D on Y. Supports binary or continuous mediator; integrate_by controls Pearl (marginal) vs Fulcher et al. (conditional) aggregation. Validation: validated evidence tier (known-truth, reference, external-parity, or Monte Carlo artifact).",
    "name": "front_door",
    "parameters": {
      "properties": {
        "covariates": {
          "description": "Pre-treatment covariates",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "integrate_by": {
          "default": "marginal",
          "description": "MC integration formulation (continuous M only)",
          "enum": [
            "marginal",
            "conditional"
          ],
          "type": "string"
        },
        "mediator": {
          "description": "Fully-transmitting mediator",
          "type": "string"
        },
        "mediator_type": {
          "default": "auto",
          "description": "Mediator model",
          "enum": [
            "auto",
            "binary",
            "continuous"
          ],
          "type": "string"
        },
        "treat": {
          "description": "Binary treatment (0/1)",
          "type": "string"
        },
        "y": {
          "description": "Outcome",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "treat",
        "mediator"
      ],
      "type": "object"
    }
  },
  {
    "description": "Marginal Structural Models for time-varying treatments with time-varying confounders. Uses stabilized IPTW and cluster-robust inference. Handles binary or continuous treatment; exposure summary can be current, cumulative, or ever. Validation: certified parity evidence.",
    "name": "msm",
    "parameters": {
      "properties": {
        "baseline": {
          "description": "Baseline covariates",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "data": {
          "description": "Long-format panel (unit x time)",
          "type": "string"
        },
        "density_sd": {
          "default": "unbiased",
          "description": "Residual-SD convention for continuous-treatment density weights; see :func:`stabilized_weights`.",
          "enum": [
            "unbiased",
            "ml"
          ],
          "type": "string"
        },
        "exposure": {
          "default": "cumulative",
          "description": "Exposure summary",
          "enum": [
            "cumulative",
            "current",
            "ever"
          ],
          "type": "string"
        },
        "family": {
          "default": "gaussian",
          "description": "Outcome family",
          "enum": [
            "gaussian",
            "binomial"
          ],
          "type": "string"
        },
        "id": {
          "description": "Unit identifier",
          "type": "string"
        },
        "time": {
          "description": "Period identifier",
          "type": "string"
        },
        "time_varying": {
          "description": "Time-varying confounders (pre-treatment)",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "treat": {
          "description": "Time-varying treatment",
          "type": "string"
        },
        "trim": {
          "default": 0.01,
          "description": "Weight truncation quantile",
          "type": "number"
        },
        "y": {
          "description": "Outcome",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "treat",
        "id",
        "time",
        "time_varying"
      ],
      "type": "object"
    }
  },
  {
    "description": "Interventional (in)direct effects (VanderWeele, Vansteelandt, Robins 2014). Identifies mediation effects in the presence of treatment-induced mediator-outcome confounders where natural (in)direct effects are not identified. Validation: certified parity evidence.",
    "name": "mediate_interventional",
    "parameters": {
      "properties": {
        "covariates": {
          "description": "Baseline covariates",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "mediator": {
          "description": "Mediator variable",
          "type": "string"
        },
        "treat": {
          "description": "Binary treatment",
          "type": "string"
        },
        "tv_confounders": {
          "description": "Treatment-induced M-Y confounders",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Outcome",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "treat",
        "mediator"
      ],
      "type": "object"
    }
  },
  {
    "description": "Proximal Causal Inference via linear 2SLS on the outcome bridge. Identifies ATE with unmeasured confounding using two proxy variables: a treatment-side Z (instrument for W) and an outcome-side W (endogenous bridge regressor). Validation: validated evidence tier (known-truth, reference, external-parity, or Monte Carlo artifact).",
    "name": "proximal",
    "parameters": {
      "properties": {
        "bridge": {
          "default": "linear",
          "description": "Bridge function family",
          "enum": [
            "linear"
          ],
          "type": "string"
        },
        "covariates": {
          "description": "Baseline covariates",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "n_boot": {
          "default": 0,
          "description": "Bootstrap SE replications",
          "type": "integer"
        },
        "proxy_w": {
          "description": "Outcome-side proxies (endogenous)",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "proxy_z": {
          "description": "Treatment-side proxies (instruments for W)",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "treat": {
          "description": "Treatment",
          "type": "string"
        },
        "y": {
          "description": "Outcome",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "treat",
        "proxy_z",
        "proxy_w"
      ],
      "type": "object"
    }
  },
  {
    "description": "Principal Stratification (Frangakis & Rubin 2002). 'monotonicity' method identifies the complier PCE (= LATE) and reports Zhang-Rubin sharp bounds on the always-survivor SACE. 'principal_score' uses Ding-Lu covariate weighting to point-identify stratum-specific effects under principal ignorability. Validation: certified evidence with scoped limitations. Known limitations: Always-survivor SACE under encouragement design (Mealli & Pacini 2013, partial identification) is not yet implemented; only AIR / Wald LATE point estimates (tau_Y on outcome, tau_S on the post-treatment stratum) are reported when an instrument is supplied.",
    "name": "principal_strat",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "CI level (e.g. 0.05 for 95% CIs)",
          "type": "number"
        },
        "covariates": {
          "description": "Baseline covariates (required for principal_score)",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "instrument": {
          "description": "Binary instrument column. When supplied, switches to the AIR / Wald LATE estimator: under random Z, monotonicity, and exclusion, reports two LATEs among Z-compliers -- tau_Y for the effect of the treatment on the outcome, and tau_S for the effect on the post-treatment stratum variable. method= is ignored on this path.",
          "type": "string"
        },
        "method": {
          "default": "monotonicity",
          "description": "Identification strategy",
          "enum": [
            "monotonicity",
            "principal_score"
          ],
          "type": "string"
        },
        "n_boot": {
          "default": 500,
          "description": "Bootstrap replications",
          "type": "integer"
        },
        "seed": {
          "description": "Random seed for reproducible bootstrap draws",
          "type": "integer"
        },
        "strata": {
          "description": "Binary post-treatment variable",
          "type": "string"
        },
        "treat": {
          "description": "Binary treatment",
          "type": "string"
        },
        "trimming": {
          "default": "quantile",
          "description": "Trimming rule for the Zhang-Rubin SACE bounds (monotonicity method); same meaning as in :func:`sp.lee_bounds`, with which the bounds coincide.",
          "enum": [
            "quantile",
            "exact"
          ],
          "type": "string"
        },
        "y": {
          "description": "Outcome",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "treat",
        "strata"
      ],
      "type": "object"
    }
  },
  {
    "description": "Mediation analysis (Imai-Keele-Tingley 2010). Decomposes the total effect into natural direct effect (NDE) and natural indirect effect (NIE) via an interventional or sequential-ignorability identification strategy. Validation: certified parity evidence.",
    "name": "mediate",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "covariates": {
          "description": "Pre-treatment confounders",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "mediator": {
          "description": "Mediator variable",
          "type": "string"
        },
        "n_boot": {
          "default": 1000,
          "description": "Bootstrap reps for NDE/NIE CIs",
          "type": "integer"
        },
        "treat": {
          "description": "Binary treatment",
          "type": "string"
        },
        "y": {
          "description": "Outcome",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "treat",
        "mediator"
      ],
      "type": "object"
    }
  },
  {
    "description": "Bartik / shift-share IV estimator (Adao-Kolesar-Morales 2019; Borusyak-Hull-Jaravel 2022). Uses pre-period industry / group shares x exogenous shocks as an instrument for local outcome exposure. Validation: certified parity evidence.",
    "name": "bartik",
    "parameters": {
      "properties": {
        "covariates": {
          "description": "Covariate matrix, DataFrame, or column names.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "endog": {
          "description": "Endogenous local exposure being instrumented (e.g. employment growth)",
          "type": "string"
        },
        "shares": {
          "description": "Pre-period share column (e.g. industry share)",
          "type": "string"
        },
        "shocks": {
          "description": "Shock column (e.g. industry-level change)",
          "type": "string"
        },
        "y": {
          "description": "Outcome (e.g. local wage growth)",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "endog",
        "shares",
        "shocks"
      ],
      "type": "object"
    }
  },
  {
    "description": "Bayesian sharp Regression Discontinuity -- full posterior over the RD jump via local polynomial with prior regularisation on bandwidth and bias-correction slopes. Reports HDI, rhat, ESS, divergences. Validation: validated evidence tier (known-truth, reference, external-parity, or Monte Carlo artifact).",
    "name": "bayes_rd",
    "parameters": {
      "properties": {
        "chains": {
          "default": 4,
          "description": "chains parameter (int).",
          "type": "integer"
        },
        "cutoff": {
          "default": 0.0,
          "description": "Cutoff value",
          "type": "number"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "draws": {
          "default": 2000,
          "description": "draws parameter (int).",
          "type": "integer"
        },
        "poly": {
          "default": 1,
          "description": "Polynomial order",
          "type": "integer"
        },
        "running": {
          "description": "Running variable",
          "type": "string"
        },
        "tune": {
          "default": 1000,
          "description": "tune parameter (int).",
          "type": "integer"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "running"
      ],
      "type": "object"
    }
  },
  {
    "description": "Bayesian fuzzy RD: joint model of first-stage jump in treatment probability and outcome jump, yielding posterior over the LATE at the cutoff (Wald ratio).",
    "name": "bayes_fuzzy_rd",
    "parameters": {
      "properties": {
        "chains": {
          "default": 4,
          "description": "chains parameter (int).",
          "type": "integer"
        },
        "cutoff": {
          "default": 0.0,
          "description": "Cutoff",
          "type": "number"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "draws": {
          "default": 2000,
          "description": "draws parameter (int).",
          "type": "integer"
        },
        "poly": {
          "default": 1,
          "description": "Polynomial order",
          "type": "integer"
        },
        "running": {
          "description": "Running variable",
          "type": "string"
        },
        "treat": {
          "description": "Take-up / treatment received",
          "type": "string"
        },
        "tune": {
          "default": 1000,
          "description": "tune parameter (int).",
          "type": "integer"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "treat",
        "running"
      ],
      "type": "object"
    }
  },
  {
    "description": "Bayesian Marginal Treatment Effect (Heckman-Vytlacil 2005). Full posterior over the MTE curve under essential heterogeneity, with bivariate-normal latent errors. Derives ATE / ATT / LATE / PRTE as posterior linear functionals.",
    "name": "bayes_mte",
    "parameters": {
      "properties": {
        "chains": {
          "default": 4,
          "description": "chains parameter (int).",
          "type": "integer"
        },
        "covariates": {
          "description": "Covariate matrix, DataFrame, or column names.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "draws": {
          "default": 2000,
          "description": "draws parameter (int).",
          "type": "integer"
        },
        "instrument": {
          "description": "Instrument(s)",
          "type": "string"
        },
        "treat": {
          "description": "Binary treatment",
          "type": "string"
        },
        "tune": {
          "default": 1000,
          "description": "tune parameter (int).",
          "type": "integer"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "treat",
        "instrument"
      ],
      "type": "object"
    }
  },
  {
    "description": "Create a 7-component target trial protocol (Hernan-Robins / JAMA 2022 framework). Formalizes eligibility, treatment strategies, time zero, follow-up, outcome, causal contrast, and analysis plan before any estimation. Validation: validated evidence tier (known-truth, reference, external-parity, or Monte Carlo artifact).",
    "name": "target_trial_protocol",
    "parameters": {
      "properties": {
        "analysis_plan": {
          "description": "analysis_plan parameter (str).",
          "type": "string"
        },
        "assignment": {
          "description": "'randomization' or 'observational emulation'",
          "type": "string"
        },
        "baseline_covariates": {
          "description": "baseline_covariates parameter (list).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "causal_contrast": {
          "default": "ITT",
          "description": "causal_contrast parameter (str).",
          "enum": [
            "ITT",
            "per-protocol",
            "as-treated",
            "observational-analogue"
          ],
          "type": "string"
        },
        "eligibility": {
          "description": "eligibility parameter (str | list | callable).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "followup_end": {
          "description": "followup_end parameter (str).",
          "type": "string"
        },
        "outcome": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        },
        "time_varying_covariates": {
          "description": "time_varying_covariates parameter (list).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "time_zero": {
          "description": "time_zero parameter (str).",
          "type": "string"
        },
        "treatment_strategies": {
          "description": "treatment_strategies parameter (list).",
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "required": [
        "eligibility",
        "treatment_strategies",
        "assignment",
        "time_zero",
        "followup_end",
        "outcome"
      ],
      "type": "object"
    }
  },
  {
    "description": "Clone-Censor-Weight (CCW) for sustained-treatment target trials. Clones each subject per strategy, artificially censors on deviation, and re-weights via IPCW. Validation: validated evidence tier (known-truth, reference, external-parity, or Monte Carlo artifact).",
    "name": "clone_censor_weight",
    "parameters": {
      "properties": {
        "censor_covariates": {
          "description": "censor_covariates parameter (list).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "id_col": {
          "description": "Column name for id.",
          "type": "string"
        },
        "stabilize": {
          "default": true,
          "description": "stabilize parameter (bool).",
          "type": "boolean"
        },
        "strategies": {
          "description": "strategies parameter (dict[str, callable]).",
          "type": "object"
        },
        "time_col": {
          "description": "Column name for time.",
          "type": "string"
        },
        "treatment_col": {
          "description": "Treatment variable column name.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "id_col",
        "time_col",
        "treatment_col",
        "strategies"
      ],
      "type": "object"
    }
  },
  {
    "description": "Inverse Probability of Censoring Weights -- corrects for informative censoring under conditional independent censoring given covariates. Validation: certified parity evidence.",
    "name": "ipcw",
    "parameters": {
      "properties": {
        "censor_covariates": {
          "description": "censor_covariates parameter (list).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "event": {
          "description": "event parameter (str).",
          "type": "string"
        },
        "method": {
          "default": "pooled_logistic",
          "description": "Estimator or algorithm variant to use.",
          "enum": [
            "pooled_logistic",
            "cox_ph"
          ],
          "type": "string"
        },
        "stabilize": {
          "default": true,
          "description": "stabilize parameter (bool).",
          "type": "boolean"
        },
        "time": {
          "description": "Time period column.",
          "type": "string"
        },
        "treatment_covariates": {
          "description": "treatment_covariates parameter (list).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "truncate": {
          "default": [
            0.01,
            0.99
          ],
          "description": "truncate parameter (tuple).",
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "required": [
        "data",
        "time",
        "event",
        "censor_covariates"
      ],
      "type": "object"
    }
  },
  {
    "description": "Shpitser-Pearl ID algorithm: decide if P(Y | do(X)) is non-parametrically identifiable on a semi-Markovian DAG, return the do-free estimand or a witness hedge.",
    "name": "identify",
    "parameters": {
      "properties": {
        "dag": {
          "description": "dag parameter (DAG).",
          "type": "string"
        },
        "outcome": {
          "description": "Outcome variable column name or outcome array.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "treatment": {
          "description": "Treatment indicator, treatment variable, or treatment array.",
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "required": [
        "dag",
        "treatment",
        "outcome"
      ],
      "type": "object"
    }
  },
  {
    "description": "Build a Single-World Intervention Graph (SWIG) by node-splitting intervened variables. Bridges Pearl's SCM and Hernan-Robins potential-outcome languages.",
    "name": "swig",
    "parameters": {
      "properties": {
        "dag": {
          "description": "dag parameter (DAG).",
          "type": "string"
        },
        "intervention": {
          "description": "intervention parameter (dict | list).",
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "required": [
        "dag",
        "intervention"
      ],
      "type": "object"
    }
  },
  {
    "description": "Invariant Causal Prediction: infer direct parents of Y by testing invariance of P(Y | X_S) across environments.",
    "name": "icp",
    "parameters": {
      "properties": {
        "X": {
          "description": "Feature matrix or covariate DataFrame.",
          "type": "string"
        },
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "environment": {
          "description": "environment parameter (ndarray).",
          "type": "string"
        },
        "max_subset_size": {
          "description": "max_subset_size parameter (int).",
          "type": "integer"
        },
        "method": {
          "default": "linear",
          "description": "Estimator or algorithm variant to use.",
          "enum": [
            "linear",
            "nonlinear"
          ],
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "X",
        "y",
        "environment"
      ],
      "type": "object"
    }
  },
  {
    "description": "Density-ratio (inverse odds of sampling) weighting to transport an effect estimated in the source population to a named target population. Validation: certified parity evidence.",
    "name": "transport_weights_fn",
    "parameters": {
      "properties": {
        "features": {
          "description": "Feature matrix or feature column names.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "outcome": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        },
        "source": {
          "description": "source parameter (DataFrame).",
          "type": "string"
        },
        "target": {
          "description": "target parameter (DataFrame).",
          "type": "string"
        },
        "treatment": {
          "description": "Treatment indicator, treatment variable, or treatment array.",
          "type": "string"
        },
        "truncate": {
          "default": [
            0.01,
            0.99
          ],
          "description": "truncate parameter (tuple).",
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "required": [
        "source",
        "target",
        "features",
        "treatment",
        "outcome"
      ],
      "type": "object"
    }
  },
  {
    "description": "Pearl-Bareinboim transportability: enumerate s-admissible adjustment sets on a selection diagram; returns the transport formula or NOT identifiable. Validation: validated evidence tier (known-truth, reference, external-parity, or Monte Carlo artifact).",
    "name": "identify_transport",
    "parameters": {
      "properties": {
        "dag": {
          "description": "dag parameter (DAG).",
          "type": "string"
        },
        "outcome": {
          "description": "Outcome variable column name or outcome array.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "selection_nodes": {
          "description": "selection_nodes parameter (set).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "treatment": {
          "description": "Treatment indicator, treatment variable, or treatment array.",
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "required": [
        "dag",
        "treatment",
        "outcome",
        "selection_nodes"
      ],
      "type": "object"
    }
  },
  {
    "description": "Container returned by sp.ope.* estimators (IPS, SNIPS, DR, Switch-DR, DM). Reports value, SE, CI, importance-ratio diagnostics.",
    "name": "OPEResult",
    "parameters": {
      "properties": {
        "ci": {
          "description": "Confidence interval (lo, hi)",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "diagnostics": {
          "description": "Importance-ratio diagnostics",
          "type": "object"
        },
        "method": {
          "description": "OPE estimator name (ips, snips, dr, switch_dr, dm)",
          "type": "string"
        },
        "se": {
          "description": "Standard error of the value estimate",
          "type": "number"
        },
        "value": {
          "description": "Estimated policy value",
          "type": "number"
        }
      },
      "required": [
        "method",
        "value",
        "se",
        "ci",
        "diagnostics"
      ],
      "type": "object"
    }
  },
  {
    "description": "Causal Effect Variational Auto-Encoder: infer a latent confounder Z from noisy proxies X, then estimate ITE via counterfactual decoding. Uses PyTorch when available, else a numpy linear-variational fallback.",
    "name": "cevae",
    "parameters": {
      "properties": {
        "X": {
          "description": "Feature matrix or covariate DataFrame.",
          "type": "string"
        },
        "hidden": {
          "default": 32,
          "description": "hidden parameter (int).",
          "type": "integer"
        },
        "lr": {
          "default": 0.01,
          "description": "lr parameter (float).",
          "type": "number"
        },
        "n_epochs": {
          "default": 200,
          "description": "Number of epochs.",
          "type": "integer"
        },
        "outcome": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        },
        "seed": {
          "default": 0,
          "description": "Random seed for reproducible stochastic steps.",
          "type": "integer"
        },
        "treatment": {
          "description": "Treatment indicator, treatment variable, or treatment array.",
          "type": "string"
        },
        "z_dim": {
          "default": 4,
          "description": "z_dim parameter (int).",
          "type": "integer"
        }
      },
      "required": [
        "X",
        "treatment",
        "outcome"
      ],
      "type": "object"
    }
  },
  {
    "description": "Parametric g-formula via Iterative Conditional Expectation (ICE) -- sequential regression of the outcome on treatment and time-varying confounders, with recursive plug-in of the target strategy. Consistent under correctly-specified nuisance models; handles time-varying confounding that vanilla adjustment cannot. Validation: certified parity evidence.",
    "name": "gformula_ice_fn",
    "parameters": {
      "properties": {
        "bootstrap": {
          "default": 0,
          "description": "bootstrap parameter (int).",
          "type": "integer"
        },
        "confounder_cols": {
          "description": "confounder_cols parameter (list | list[list]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "id_col": {
          "description": "Column name for id.",
          "type": "string"
        },
        "outcome_col": {
          "description": "Outcome variable column name.",
          "type": "string"
        },
        "time_col": {
          "description": "Column name for time.",
          "type": "string"
        },
        "treatment_cols": {
          "description": "treatment_cols parameter (list).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "treatment_strategy": {
          "description": "treatment_strategy parameter (list | callable).",
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "required": [
        "data",
        "id_col",
        "time_col",
        "treatment_cols",
        "confounder_cols",
        "outcome_col",
        "treatment_strategy"
      ],
      "type": "object"
    }
  },
  {
    "description": "Odds ratio from a 2x2 table with Woolf (asymptotic) or Fisher-exact CI. Haldane-Anscombe correction for zero cells. Validation: certified parity evidence.",
    "name": "odds_ratio",
    "parameters": {
      "properties": {
        "a": {
          "description": "a (exposed, outcome+) count or 2x2 array",
          "type": "number"
        },
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "b": {
          "description": "b (exposed, outcome-) count",
          "type": "number"
        },
        "c": {
          "description": "c (unexposed, outcome+) count",
          "type": "number"
        },
        "d": {
          "description": "d (unexposed, outcome-) count",
          "type": "number"
        },
        "method": {
          "default": "woolf",
          "description": "CI method",
          "enum": [
            "woolf",
            "exact"
          ],
          "type": "string"
        }
      },
      "required": [
        "a"
      ],
      "type": "object"
    }
  },
  {
    "description": "Relative risk (risk ratio) from a 2x2 table with Katz log-RR CI. Haldane correction for zero cells. Validation: certified parity evidence.",
    "name": "relative_risk",
    "parameters": {
      "properties": {
        "a": {
          "description": "a parameter (float | 2x2 array).",
          "type": "number"
        },
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "b": {
          "description": "b parameter (float).",
          "type": "number"
        },
        "c": {
          "description": "c parameter (float).",
          "type": "number"
        },
        "d": {
          "description": "d parameter (float).",
          "type": "number"
        }
      },
      "required": [
        "a"
      ],
      "type": "object"
    }
  },
  {
    "description": "Risk difference (absolute risk reduction) with Wald or Newcombe hybrid-score CI. Validation: certified parity evidence.",
    "name": "risk_difference",
    "parameters": {
      "properties": {
        "a": {
          "description": "a parameter (float | 2x2 array).",
          "type": "number"
        },
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "b": {
          "description": "b parameter (float).",
          "type": "number"
        },
        "c": {
          "description": "c parameter (float).",
          "type": "number"
        },
        "d": {
          "description": "d parameter (float).",
          "type": "number"
        },
        "method": {
          "default": "wald",
          "description": "Estimator or algorithm variant to use.",
          "enum": [
            "wald",
            "newcombe"
          ],
          "type": "string"
        }
      },
      "required": [
        "a"
      ],
      "type": "object"
    }
  },
  {
    "description": "Attributable fractions in the exposed (AF) and in the population (Levin PAF) with delta-method CI. Validation: certified parity evidence.",
    "name": "attributable_risk",
    "parameters": {
      "properties": {
        "a": {
          "description": "a parameter (float | 2x2 array).",
          "type": "number"
        },
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "b": {
          "description": "b parameter (float).",
          "type": "number"
        },
        "c": {
          "description": "c parameter (float).",
          "type": "number"
        },
        "d": {
          "description": "d parameter (float).",
          "type": "number"
        }
      },
      "required": [
        "a"
      ],
      "type": "object"
    }
  },
  {
    "description": "Person-time incidence rate ratio with exact Poisson CI (Clopper-Pearson on conditional binomial). Validation: certified parity evidence.",
    "name": "incidence_rate_ratio",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "events_exposed": {
          "description": "events_exposed parameter (float).",
          "type": "number"
        },
        "events_unexposed": {
          "description": "events_unexposed parameter (float).",
          "type": "number"
        },
        "method": {
          "default": "exact",
          "description": "Estimator or algorithm variant to use.",
          "enum": [
            "exact",
            "wald"
          ],
          "type": "string"
        },
        "pt_exposed": {
          "description": "Person-time at risk (exposed)",
          "type": "number"
        },
        "pt_unexposed": {
          "description": "pt_unexposed parameter (float).",
          "type": "number"
        }
      },
      "required": [
        "events_exposed",
        "pt_exposed",
        "events_unexposed",
        "pt_unexposed"
      ],
      "type": "object"
    }
  },
  {
    "description": "Mantel-Haenszel pooled OR or RR across K strata, with Robins-Breslow-Greenland variance and Cochran's Q homogeneity check. Validation: certified parity evidence.",
    "name": "mantel_haenszel",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "measure": {
          "default": "OR",
          "description": "measure parameter (str).",
          "enum": [
            "OR",
            "RR"
          ],
          "type": "string"
        },
        "tables": {
          "description": "Stack of K per-stratum 2x2 tables",
          "type": "string"
        }
      },
      "required": [
        "tables"
      ],
      "type": "object"
    }
  },
  {
    "description": "Breslow-Day test for homogeneity of the odds ratio across strata, with Tarone correction. Validation: certified parity evidence.",
    "name": "breslow_day_test",
    "parameters": {
      "properties": {
        "tables": {
          "description": "tables parameter (array (K, 2, 2)).",
          "type": "string"
        },
        "tarone_correction": {
          "default": true,
          "description": "tarone_correction parameter (bool).",
          "type": "boolean"
        }
      },
      "required": [
        "tables"
      ],
      "type": "object"
    }
  },
  {
    "description": "Direct age/covariate standardization of a rate using external standard-population weights. Validation: certified parity evidence.",
    "name": "direct_standardize",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "ci_method": {
          "default": "gamma",
          "description": "``\"lognormal\"``: ``exp(log r +/- z se / r)``. ``\"gamma\"``: the Fay-Feuer gamma interval, as R ``epitools::ageadjust.direct``.",
          "enum": [
            "lognormal",
            "gamma",
            "normal"
          ],
          "type": "string"
        },
        "events": {
          "description": "events parameter (list | ndarray).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "population": {
          "description": "population parameter (list | ndarray).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "standard_weights": {
          "description": "standard_weights parameter (list | ndarray).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "variance": {
          "default": "poisson",
          "description": "``\"poisson\"``: ``sum w_k^2 events_k / population_k^2`` (epitools). ``\"binomial\"``: ``sum w_k^2 r_k (1 - r_k) / population_k`` (Stata ``dstdize``).",
          "enum": [
            "poisson",
            "binomial"
          ],
          "type": "string"
        }
      },
      "required": [
        "events",
        "population",
        "standard_weights"
      ],
      "type": "object"
    }
  },
  {
    "description": "Indirect standardization -> SMR (standardized morbidity / mortality ratio) with Garwood exact Poisson CI. Validation: certified parity evidence.",
    "name": "indirect_standardize",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "ci_method": {
          "default": "exact",
          "description": "\"exact\": exact Poisson (Garwood) interval for the observed count divided by the expected count, as Stata istdize. \"lognormal\": SMR exp(+/- z / sqrt(O)), as R epitools::ageadjust.indirect.",
          "enum": [
            "exact",
            "lognormal"
          ],
          "type": "string"
        },
        "events_reference": {
          "description": "events_reference parameter (list | ndarray).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "observed": {
          "description": "observed parameter (float).",
          "type": "number"
        },
        "population_reference": {
          "description": "population_reference parameter (list | ndarray).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "population_study": {
          "description": "population_study parameter (list | ndarray).",
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "required": [
        "observed",
        "events_reference",
        "population_reference",
        "population_study"
      ],
      "type": "object"
    }
  },
  {
    "description": "Structured 9-viewpoint Bradford-Hill causal-assessment rubric with prerequisite check (temporality required) and narrative verdict. Validation: validated evidence tier (known-truth, reference, external-parity, or Monte Carlo artifact).",
    "name": "bradford_hill",
    "parameters": {
      "properties": {
        "analogy": {
          "description": "analogy parameter (float).",
          "type": "number"
        },
        "biological_gradient": {
          "description": "biological_gradient parameter (float).",
          "type": "number"
        },
        "coherence": {
          "description": "coherence parameter (float).",
          "type": "number"
        },
        "consistency": {
          "description": "consistency parameter (float).",
          "type": "number"
        },
        "evidence": {
          "description": "Optional dict mapping viewpoint -> [0,1] score",
          "type": "object"
        },
        "experiment": {
          "description": "experiment parameter (float).",
          "type": "number"
        },
        "notes": {
          "description": "notes parameter (dict).",
          "type": "object"
        },
        "plausibility": {
          "description": "plausibility parameter (float).",
          "type": "number"
        },
        "specificity": {
          "description": "specificity parameter (float).",
          "type": "number"
        },
        "strength": {
          "description": "strength parameter (float).",
          "type": "number"
        },
        "temporality": {
          "description": "temporality parameter (float).",
          "type": "number"
        }
      },
      "required": [],
      "type": "object"
    }
  },
  {
    "description": "Cochran's Q (IVW) or Ruecker's Q' (Egger) heterogeneity statistic with I^2, used to detect horizontal pleiotropy. Validation: certified parity evidence.",
    "name": "mr_heterogeneity",
    "parameters": {
      "properties": {
        "beta_exposure": {
          "description": "beta_exposure parameter (ndarray).",
          "type": "string"
        },
        "beta_outcome": {
          "description": "beta_outcome parameter (ndarray).",
          "type": "string"
        },
        "method": {
          "default": "ivw",
          "description": "Estimator or algorithm variant to use.",
          "enum": [
            "ivw",
            "egger"
          ],
          "type": "string"
        },
        "se_outcome": {
          "description": "se_outcome parameter (ndarray).",
          "type": "string"
        }
      },
      "required": [
        "beta_exposure",
        "beta_outcome",
        "se_outcome"
      ],
      "type": "object"
    }
  },
  {
    "description": "Formal MR-Egger intercept test for directional (unbalanced) horizontal pleiotropy. Validation: certified parity evidence.",
    "name": "mr_pleiotropy_egger",
    "parameters": {
      "properties": {
        "beta_exposure": {
          "description": "beta_exposure parameter (ndarray).",
          "type": "string"
        },
        "beta_outcome": {
          "description": "beta_outcome parameter (ndarray).",
          "type": "string"
        },
        "se_outcome": {
          "description": "se_outcome parameter (ndarray).",
          "type": "string"
        }
      },
      "required": [
        "beta_exposure",
        "beta_outcome",
        "se_outcome"
      ],
      "type": "object"
    }
  },
  {
    "description": "Drop-one IVW sensitivity -- per-SNP table of estimates when each SNP is removed in turn. Validation: certified parity evidence.",
    "name": "mr_leave_one_out",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "beta_exposure": {
          "description": "beta_exposure parameter (ndarray).",
          "type": "string"
        },
        "beta_outcome": {
          "description": "beta_outcome parameter (ndarray).",
          "type": "string"
        },
        "model": {
          "default": "default",
          "description": "Model variant or parameterisation to fit.",
          "enum": [
            "default",
            "fixed",
            "random"
          ],
          "type": "string"
        },
        "se_outcome": {
          "description": "se_outcome parameter (ndarray).",
          "type": "string"
        },
        "snp_ids": {
          "description": "snp_ids parameter (list).",
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "required": [
        "beta_exposure",
        "beta_outcome",
        "se_outcome"
      ],
      "type": "object"
    }
  },
  {
    "description": "Steiger directionality test -- verifies that the SNPs explain more variance in the exposure than the outcome, supporting the assumed causal direction. Validation: certified parity evidence.",
    "name": "mr_steiger",
    "parameters": {
      "properties": {
        "alternative": {
          "default": "two-sided",
          "description": "alternative parameter (str).",
          "enum": [
            "two-sided",
            "greater"
          ],
          "type": "string"
        },
        "beta_exposure": {
          "description": "beta_exposure parameter (ndarray).",
          "type": "string"
        },
        "beta_outcome": {
          "description": "beta_outcome parameter (ndarray).",
          "type": "string"
        },
        "eaf": {
          "description": "Effect-allele frequencies",
          "type": "string"
        },
        "n_exposure": {
          "description": "Number of exposure.",
          "type": "integer"
        },
        "n_outcome": {
          "description": "Number of outcome.",
          "type": "integer"
        },
        "se_exposure": {
          "description": "se_exposure parameter (ndarray).",
          "type": "string"
        },
        "se_outcome": {
          "description": "se_outcome parameter (ndarray).",
          "type": "string"
        }
      },
      "required": [
        "beta_exposure",
        "se_exposure",
        "n_exposure",
        "beta_outcome",
        "se_outcome",
        "n_outcome"
      ],
      "type": "object"
    }
  },
  {
    "description": "MR-PRESSO global test + per-SNP outlier detection + outlier-corrected IVW estimate + distortion test. Validation: certified parity evidence.",
    "name": "mr_presso",
    "parameters": {
      "properties": {
        "beta_exposure": {
          "description": "beta_exposure parameter (ndarray).",
          "type": "string"
        },
        "beta_outcome": {
          "description": "beta_outcome parameter (ndarray).",
          "type": "string"
        },
        "n_boot": {
          "default": 1000,
          "description": "Number of bootstrap replications.",
          "type": "integer"
        },
        "se_exposure": {
          "description": "se_exposure parameter (ndarray).",
          "type": "string"
        },
        "se_outcome": {
          "description": "se_outcome parameter (ndarray).",
          "type": "string"
        },
        "seed": {
          "description": "Random seed for reproducible stochastic steps.",
          "type": "integer"
        },
        "sig_threshold": {
          "default": 0.05,
          "description": "sig_threshold parameter (float).",
          "type": "number"
        }
      },
      "required": [
        "beta_exposure",
        "beta_outcome",
        "se_exposure",
        "se_outcome"
      ],
      "type": "object"
    }
  },
  {
    "description": "Radial IVW MR (Bowden 2018) with per-SNP Bonferroni-thresholded outlier flagging. Validation: certified parity evidence.",
    "name": "mr_radial",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "beta_exposure": {
          "description": "beta_exposure parameter (ndarray).",
          "type": "string"
        },
        "beta_outcome": {
          "description": "beta_outcome parameter (ndarray).",
          "type": "string"
        },
        "bonferroni": {
          "default": true,
          "description": "bonferroni parameter (bool).",
          "type": "boolean"
        },
        "se_outcome": {
          "description": "se_outcome parameter (ndarray).",
          "type": "string"
        },
        "snp_ids": {
          "description": "snp_ids parameter (list).",
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "required": [
        "beta_exposure",
        "beta_outcome",
        "se_outcome"
      ],
      "type": "object"
    }
  },
  {
    "description": "Unified longitudinal causal-effect estimator. Auto-routes to IPW (no time-varying confounders) / MSM (dynamic regime with time-varying confounders) / parametric g-formula ICE (static regime). Accepts a string DSL or callable for the treatment regime. Validation: validated evidence tier (known-truth, reference, external-parity, or Monte Carlo artifact).",
    "name": "longitudinal_analyze",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "baseline": {
          "description": "baseline parameter (list).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "id": {
          "description": "Unit, subject, or panel identifier column.",
          "type": "string"
        },
        "method": {
          "default": "auto",
          "description": "Estimator or algorithm variant to use.",
          "enum": [
            "auto",
            "msm",
            "g-formula",
            "ipw"
          ],
          "type": "string"
        },
        "outcome": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        },
        "regime": {
          "default": "always_treat",
          "description": "regime parameter (str | Regime | list | callable).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "time": {
          "description": "Time period column.",
          "type": "string"
        },
        "time_varying": {
          "description": "time_varying parameter (list).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "treatment": {
          "description": "Treatment indicator, treatment variable, or treatment array.",
          "type": "string"
        },
        "trim": {
          "default": 0.01,
          "description": "trim parameter (float).",
          "type": "number"
        }
      },
      "required": [
        "data",
        "id",
        "time",
        "treatment",
        "outcome"
      ],
      "type": "object"
    }
  },
  {
    "description": "Plug-in estimator of E[Y(regime_a)] - E[Y(regime_b)] with delta-method SE. Validation: validated evidence tier (known-truth, reference, external-parity, or Monte Carlo artifact).",
    "name": "longitudinal_contrast",
    "parameters": {
      "properties": {
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "id": {
          "description": "Unit, subject, or panel identifier column.",
          "type": "string"
        },
        "outcome": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        },
        "regime_a": {
          "description": "regime_a parameter (str | Regime).",
          "type": "string"
        },
        "regime_b": {
          "description": "regime_b parameter (str | Regime).",
          "type": "string"
        },
        "time": {
          "description": "Time period column.",
          "type": "string"
        },
        "treatment": {
          "description": "Treatment indicator, treatment variable, or treatment array.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "id",
        "time",
        "treatment",
        "outcome",
        "regime_a",
        "regime_b"
      ],
      "type": "object"
    }
  },
  {
    "description": "Build a dynamic or static treatment regime from a string DSL, list, callable, or scalar. Supports 'if <cond> then <a> else <b>', 'always_treat', 'never_treat', and arbitrary safe expressions. Parsed via a whitelisted AST walker -- no dynamic code execution. Validation: validated evidence tier (known-truth, reference, external-parity, or Monte Carlo artifact).",
    "name": "regime",
    "parameters": {
      "properties": {
        "K": {
          "default": 1,
          "description": "K parameter (int).",
          "type": "integer"
        },
        "name": {
          "description": "name parameter (str).",
          "type": "string"
        },
        "rule": {
          "description": "rule parameter (str | list | callable | scalar).",
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "required": [
        "rule"
      ],
      "type": "object"
    }
  },
  {
    "description": "Render a target-trial emulation result as a structured Methods + Results block (Markdown / LaTeX / plain text), tracking the JAMA 2022 7-component spec. Validation: validated evidence tier (known-truth, reference, external-parity, or Monte Carlo artifact).",
    "name": "target_trial_report",
    "parameters": {
      "properties": {
        "fmt": {
          "default": "markdown",
          "description": "fmt parameter (str).",
          "enum": [
            "markdown",
            "latex",
            "text"
          ],
          "type": "string"
        },
        "result": {
          "description": "result parameter (TargetTrialResult).",
          "type": "string"
        },
        "title": {
          "description": "title parameter (str).",
          "type": "string"
        }
      },
      "required": [
        "result"
      ],
      "type": "object"
    }
  },
  {
    "description": "Inspect a declared DAG and recommend a StatsPAI estimator for (exposure, outcome) with a plain-English identification story. Priority: backdoor adjustment -> IV -> frontdoor -> not-identifiable. Also available as DAG.recommend_estimator().",
    "name": "dag_recommend_estimator",
    "parameters": {
      "properties": {
        "candidate_instruments": {
          "description": "candidate_instruments parameter (list[str]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "dag": {
          "description": "dag parameter (DAG).",
          "type": "string"
        },
        "exposure": {
          "description": "Exposure term or exposure column.",
          "type": "string"
        },
        "outcome": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "dag",
        "exposure",
        "outcome"
      ],
      "type": "object"
    }
  },
  {
    "description": "Declare a causal question up front (estimand-first). .identify() picks an estimator and lists identifying assumptions; .estimate() runs the analysis; .report() produces a Markdown Methods + Results paragraph. Auto-routes to IV / RD / DiD / longitudinal / selection-on-observables based on supplied fields.",
    "name": "causal_question",
    "parameters": {
      "properties": {
        "covariates": {
          "description": "Covariate matrix, DataFrame, or column names.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "cutoff": {
          "description": "cutoff parameter (float).",
          "type": "number"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "design": {
          "default": "auto",
          "description": "design parameter (str).",
          "enum": [
            "auto",
            "rct",
            "selection_on_observables",
            "iv",
            "natural_experiment",
            "policy_shock",
            "regression_discontinuity",
            "synthetic_control",
            "did",
            "event_study",
            "longitudinal_observational"
          ],
          "type": "string"
        },
        "estimand": {
          "default": "ATE",
          "description": "estimand parameter (str).",
          "enum": [
            "ATE",
            "ATT",
            "ATU",
            "LATE",
            "CATE",
            "ITT"
          ],
          "type": "string"
        },
        "id": {
          "description": "Unit, subject, or panel identifier column.",
          "type": "string"
        },
        "instruments": {
          "description": "instruments parameter (list[str]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "outcome": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        },
        "population": {
          "description": "population parameter (str).",
          "type": "string"
        },
        "running_variable": {
          "description": "running_variable parameter (str).",
          "type": "string"
        },
        "time": {
          "description": "Time period column.",
          "type": "string"
        },
        "time_structure": {
          "default": "cross_section",
          "description": "time_structure parameter (str).",
          "enum": [
            "cross_section",
            "panel",
            "repeated_cross_section",
            "longitudinal",
            "time_series",
            "pre_post"
          ],
          "type": "string"
        },
        "treatment": {
          "description": "Treatment indicator, treatment variable, or treatment array.",
          "type": "string"
        }
      },
      "required": [
        "treatment",
        "outcome"
      ],
      "type": "object"
    }
  },
  {
    "description": "Weighted or simple mode-based MR estimator (Hartwig 2017). Consistent under the ZEMPA (zero-mode pleiotropy) assumption -- more permissive than the median's 50% rule. Validation: certified parity evidence.",
    "name": "mr_mode",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "beta_exposure": {
          "description": "beta_exposure parameter (ndarray).",
          "type": "string"
        },
        "beta_outcome": {
          "description": "beta_outcome parameter (ndarray).",
          "type": "string"
        },
        "method": {
          "default": "weighted",
          "description": "Estimator or algorithm variant to use.",
          "enum": [
            "weighted",
            "simple"
          ],
          "type": "string"
        },
        "n_boot": {
          "default": 1000,
          "description": "Number of bootstrap replications.",
          "type": "integer"
        },
        "phi": {
          "default": 1.0,
          "description": "phi parameter (float).",
          "type": "number"
        },
        "refine": {
          "default": false,
          "description": "refine parameter (bool).",
          "type": "boolean"
        },
        "se_exposure": {
          "description": "se_exposure parameter (ndarray).",
          "type": "string"
        },
        "se_outcome": {
          "description": "se_outcome parameter (ndarray).",
          "type": "string"
        },
        "seed": {
          "description": "Random seed for reproducible stochastic steps.",
          "type": "integer"
        }
      },
      "required": [
        "beta_exposure",
        "beta_outcome",
        "se_exposure",
        "se_outcome"
      ],
      "type": "object"
    }
  },
  {
    "description": "Per-SNP F-statistic summary for instrument strength. Flags weak-instrument risk when any F < 10 (Staiger-Stock). Validation: certified parity evidence.",
    "name": "mr_f_statistic",
    "parameters": {
      "properties": {
        "beta_exposure": {
          "description": "beta_exposure parameter (ndarray).",
          "type": "string"
        },
        "n_samples": {
          "description": "Number of samples.",
          "type": "integer"
        },
        "se_exposure": {
          "description": "se_exposure parameter (ndarray).",
          "type": "string"
        }
      },
      "required": [
        "beta_exposure",
        "se_exposure"
      ],
      "type": "object"
    }
  },
  {
    "description": "Sensitivity, specificity, PPV, NPV, LR+ / LR- with Wilson score CIs. Accepts either raw binary labels or pre-computed confusion counts. Validation: certified parity evidence.",
    "name": "sensitivity_specificity",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "ci_method": {
          "default": "wilson",
          "description": "Interval for sensitivity and specificity: Wilson score (R ``epiR::epi.tests(method=\"wilson\")``) or Clopper-Pearson exact (``epi.tests`` default, Stata ``diagt``).",
          "enum": [
            "wilson",
            "exact"
          ],
          "type": "string"
        },
        "fn": {
          "description": "fn parameter (int).",
          "type": "integer"
        },
        "fp": {
          "description": "fp parameter (int).",
          "type": "integer"
        },
        "tn": {
          "description": "tn parameter (int).",
          "type": "integer"
        },
        "tp": {
          "description": "tp parameter (int).",
          "type": "integer"
        },
        "y_pred": {
          "description": "y_pred parameter (array).",
          "type": "string"
        },
        "y_true": {
          "description": "y_true parameter (array).",
          "type": "string"
        }
      },
      "required": [],
      "type": "object"
    }
  },
  {
    "description": "ROC curve with AUC (trapezoidal) and Hanley-McNeil (1982) standard error. Validation: certified parity evidence.",
    "name": "roc_curve",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "scores": {
          "description": "scores parameter (array).",
          "type": "string"
        },
        "se_method": {
          "default": "delong",
          "description": "``\"hanley\"``: the Hanley-McNeil (1982) variance with the exponential approximations ``Q1 = A/(2-A)``, ``Q2 = 2A^2/(1+A)``. ``\"hanley-empirical\"``: the same variance with ``Q1`` and ``Q2`` estimated from the data (ties weighted 1/3), which is what Stata ``roctab, hanley`` reports.",
          "enum": [
            "hanley",
            "hanley-empirical",
            "delong"
          ],
          "type": "string"
        },
        "y_true": {
          "description": "y_true parameter (array).",
          "type": "string"
        }
      },
      "required": [
        "y_true",
        "scores"
      ],
      "type": "object"
    }
  },
  {
    "description": "Cohen's kappa for inter-rater agreement on nominal or ordinal scales. Supports linear / quadratic weighting. Validation: certified parity evidence.",
    "name": "cohen_kappa",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "rater_a": {
          "description": "rater_a parameter (array).",
          "type": "string"
        },
        "rater_b": {
          "description": "rater_b parameter (array).",
          "type": "string"
        },
        "weights": {
          "default": "unweighted",
          "description": "Observation weights.",
          "enum": [
            "unweighted",
            "linear",
            "quadratic"
          ],
          "type": "string"
        }
      },
      "required": [
        "rater_a",
        "rater_b"
      ],
      "type": "object"
    }
  },
  {
    "description": "Write a pre-analysis plan (CausalQuestion) to YAML / JSON for OSF, AEA RCT Registry, or a repo-local PAP. Includes a metadata block with timestamp and statspai version.",
    "name": "preregister",
    "parameters": {
      "properties": {
        "filename": {
          "description": "filename parameter (str | Path).",
          "type": "string"
        },
        "fmt": {
          "default": "auto",
          "description": "fmt parameter (str).",
          "enum": [
            "auto",
            "yaml",
            "json"
          ],
          "type": "string"
        },
        "note": {
          "description": "note parameter (str).",
          "type": "string"
        },
        "question": {
          "description": "question parameter (CausalQuestion | dict).",
          "type": "object"
        },
        "registry_url": {
          "description": "registry_url parameter (str).",
          "type": "string"
        }
      },
      "required": [
        "question",
        "filename"
      ],
      "type": "object"
    }
  },
  {
    "description": "Load a pre-registration file back into a CausalQuestion.",
    "name": "load_preregister",
    "parameters": {
      "properties": {
        "filename": {
          "description": "filename parameter (str | Path).",
          "type": "string"
        }
      },
      "required": [
        "filename"
      ],
      "type": "object"
    }
  },
  {
    "description": "Estimate ONE model with several INDEPENDENT engines (StatsPAI, pyfixest, linearmodels, DoubleML, R's fixest, Stata) and report whether they agree (AGREE / PARTIAL / DISAGREE / INSUFFICIENT). Operationalises the cross-package reproducibility check: trust a number only when >=2 independent implementations reproduce it.",
    "name": "cross_validate",
    "parameters": {
      "properties": {
        "covariates": {
          "description": "Covariate matrix, DataFrame, or column names.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "data_or_result": {
          "description": "Dataset (with `estimand` + spec) or a fitted StatsPAI result to re-run elsewhere.",
          "type": "string"
        },
        "endog": {
          "description": "IV endogenous",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "engines": {
          "default": "auto",
          "description": "'auto' (all installed, applicable) or a list e.g. ['statspai','R::fixest','pyfixest','Stata']",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "estimand": {
          "description": "ols / feols / iv / poisson / dml / did (Callaway-Sant'Anna; and aliases)",
          "enum": [
            "ols",
            "feols",
            "iv",
            "poisson",
            "dml",
            "did"
          ],
          "type": "string"
        },
        "fixed_effects": {
          "description": "fixed_effects parameter (list[str]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "formula": {
          "description": "fixest-style: 'y ~ x | fe | endog ~ z'",
          "type": "string"
        },
        "g": {
          "description": "DiD cohort / first-treatment period (0 = never)",
          "type": "string"
        },
        "i": {
          "description": "DiD unit-id column",
          "type": "string"
        },
        "instruments": {
          "description": "instruments parameter (list[str]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "t": {
          "description": "DiD time column",
          "type": "string"
        },
        "tol": {
          "description": "Override tolerance, e.g. {'coef_rtol':1e-4}",
          "type": "object"
        },
        "treatment": {
          "description": "Focal regressor (default reconciled term)",
          "type": "string"
        },
        "vcov": {
          "description": "Variance estimator",
          "type": "string"
        },
        "y": {
          "description": "Outcome column",
          "type": "string"
        }
      },
      "required": [
        "data_or_result"
      ],
      "type": "object"
    }
  },
  {
    "description": "Normalise a World Bank Indicators payload (already fetched via a data MCP / REST) into a tidy long or wide panel ready for sp.feols / sp.cross_validate. No network call.",
    "name": "from_worldbank",
    "parameters": {
      "properties": {
        "payload": {
          "description": "World Bank Indicators API v2 / Data360 response, already fetched.",
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "required": [
        "payload"
      ],
      "type": "object"
    }
  },
  {
    "description": "Normalise FRED series observations (one or many series) into a tidy time series with parsed dates and NaN for '.' missings. No network call.",
    "name": "from_fred",
    "parameters": {
      "properties": {
        "payload": {
          "description": "FRED series observations response, already fetched.",
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "required": [
        "payload"
      ],
      "type": "object"
    }
  },
  {
    "description": "Expand an SDMX-JSON payload (OECD / Eurostat / IMF) into a long frame with one column per dimension plus a value column. No network call.",
    "name": "from_sdmx",
    "parameters": {
      "properties": {
        "payload": {
          "description": "SDMX-JSON (OECD / Eurostat / IMF) response, already fetched.",
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "required": [
        "payload"
      ],
      "type": "object"
    }
  },
  {
    "description": "Run every applicable sensitivity analysis in one shot: E-value, Oster delta (when R^2 inputs given), Rosenbaum Gamma (when matched_pairs outcomes exposed), Sensemakr (when raw data supplied via data/y/treat/controls), and a breakdown-frontier bias estimate. Also available as result.sensitivity(). Validation: certified parity evidence.",
    "name": "unified_sensitivity",
    "parameters": {
      "properties": {
        "beta_uncontrolled": {
          "description": "beta_uncontrolled parameter (float).",
          "type": "number"
        },
        "controls": {
          "description": "Control-variable column names.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "include_oster": {
          "default": true,
          "description": "Whether to include oster.",
          "type": "boolean"
        },
        "include_rosenbaum": {
          "default": true,
          "description": "Whether to include rosenbaum.",
          "type": "boolean"
        },
        "include_sensemakr": {
          "default": true,
          "description": "Whether to include sensemakr.",
          "type": "boolean"
        },
        "r2_controlled": {
          "description": "r2_controlled parameter (float).",
          "type": "number"
        },
        "r2_treated": {
          "description": "r2_treated parameter (float).",
          "type": "number"
        },
        "result": {
          "description": "result parameter (CausalResult | EconometricResults).",
          "type": "string"
        },
        "rho_max": {
          "description": "Oster R_max; None -> min(1, 1.3 R^2_long), the sp.oster_delta default.",
          "type": "number"
        },
        "treat": {
          "description": "Treatment indicator or first-treatment-period column.",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "result"
      ],
      "type": "object"
    }
  },
  {
    "description": "Athey-Chetty-Imbens-Kang surrogate-index estimator for the long-term ATE: combines an experimental sample (treatment + short-term surrogate) with an observational sample (surrogate + long-term outcome) to extrapolate the effect on the long-term outcome. Validation: validated evidence tier (known-truth, reference, external-parity, or Monte Carlo artifact).",
    "name": "surrogate_index",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "covariates": {
          "description": "Covariate matrix, DataFrame, or column names.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "experimental": {
          "description": "experimental parameter (DataFrame).",
          "type": "string"
        },
        "long_term_outcome": {
          "description": "long_term_outcome parameter (str).",
          "type": "string"
        },
        "model": {
          "default": "ols",
          "description": "Model variant or parameterisation to fit.",
          "type": "string"
        },
        "n_boot": {
          "default": 0,
          "description": "Bootstrap replicates (0 = analytic delta-method SE)",
          "type": "integer"
        },
        "observational": {
          "description": "observational parameter (DataFrame).",
          "type": "string"
        },
        "surrogates": {
          "description": "surrogates parameter (list).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "treatment": {
          "description": "Treatment indicator, treatment variable, or treatment array.",
          "type": "string"
        }
      },
      "required": [
        "experimental",
        "observational",
        "treatment",
        "surrogates",
        "long_term_outcome"
      ],
      "type": "object"
    }
  },
  {
    "description": "Long-term ATE under multi-wave short-term surrogates; extends the classical surrogate index to sustained treatments via iterated conditional expectations (Ghassami et al. 2024). Validation: validated evidence tier (known-truth, reference, external-parity, or Monte Carlo artifact).",
    "name": "long_term_from_short",
    "parameters": {
      "properties": {
        "covariates": {
          "description": "Covariate matrix, DataFrame, or column names.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "experimental": {
          "description": "experimental parameter (DataFrame).",
          "type": "string"
        },
        "long_term_outcome": {
          "description": "long_term_outcome parameter (str).",
          "type": "string"
        },
        "n_boot": {
          "default": 200,
          "description": "Number of bootstrap replications.",
          "type": "integer"
        },
        "observational": {
          "description": "observational parameter (DataFrame).",
          "type": "string"
        },
        "surrogates_waves": {
          "description": "List of wave column lists",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "treatment": {
          "description": "Treatment indicator, treatment variable, or treatment array.",
          "type": "string"
        }
      },
      "required": [
        "experimental",
        "observational",
        "treatment",
        "surrogates_waves",
        "long_term_outcome"
      ],
      "type": "object"
    }
  },
  {
    "description": "Inverse-variance pooling of an RCT and RWD estimate with optional transport shift (Dahabreh et al. 2020; arXiv:2511.19735 2025). Validation: certified parity evidence.",
    "name": "synthesise_evidence",
    "parameters": {
      "properties": {
        "rct_estimate": {
          "description": "rct_estimate parameter (float).",
          "type": "number"
        },
        "rct_se": {
          "description": "rct_se parameter (float).",
          "type": "number"
        },
        "rwd_estimate": {
          "description": "rwd_estimate parameter (float).",
          "type": "number"
        },
        "rwd_se": {
          "description": "rwd_se parameter (float).",
          "type": "number"
        },
        "transport_shift": {
          "default": 0.0,
          "description": "transport_shift parameter (float).",
          "type": "number"
        },
        "transport_shift_se": {
          "default": 0.0,
          "description": "transport_shift_se parameter (float).",
          "type": "number"
        },
        "weight_mode": {
          "default": "inverse_variance",
          "description": "weight_mode parameter (str).",
          "enum": [
            "inverse_variance",
            "rct_heavy"
          ],
          "type": "string"
        }
      },
      "required": [
        "rct_estimate",
        "rct_se",
        "rwd_estimate",
        "rwd_se"
      ],
      "type": "object"
    }
  },
  {
    "description": "DerSimonian-Laird tau2 / Q / I2 heterogeneity statistics for multi-study evidence synthesis. Validation: certified parity evidence.",
    "name": "heterogeneity_of_effect",
    "parameters": {
      "properties": {
        "estimates": {
          "description": "estimates parameter (list).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "ses": {
          "description": "ses parameter (list).",
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "required": [
        "estimates",
        "ses"
      ],
      "type": "object"
    }
  },
  {
    "description": "Report-card: does the RWD estimate fall inside the RCT's 95% CI?",
    "name": "rwd_rct_concordance",
    "parameters": {
      "properties": {
        "rct_estimate": {
          "description": "rct_estimate parameter (float).",
          "type": "number"
        },
        "rct_se": {
          "description": "rct_se parameter (float).",
          "type": "number"
        },
        "rwd_estimate": {
          "description": "rwd_estimate parameter (float).",
          "type": "number"
        }
      },
      "required": [
        "rct_estimate",
        "rct_se",
        "rwd_estimate"
      ],
      "type": "object"
    }
  },
  {
    "description": "Level-1 (knowledge) and Level-2 (deductive reasoning) evaluation of an LLM's causal-reasoning ability.",
    "name": "llm_causal_assess",
    "parameters": {
      "properties": {
        "level1_items": {
          "description": "level1_items parameter (DataFrame).",
          "type": "string"
        },
        "level2_items": {
          "description": "level2_items parameter (DataFrame).",
          "type": "string"
        },
        "llm_client": {
          "description": "llm_client parameter (callable).",
          "type": "string"
        },
        "llm_identifier": {
          "default": "llm",
          "description": "llm_identifier parameter (str).",
          "type": "string"
        }
      },
      "required": [
        "llm_client"
      ],
      "type": "object"
    }
  },
  {
    "description": "Pairwise causal-direction discovery benchmark for an LLM.",
    "name": "pairwise_causal_benchmark",
    "parameters": {
      "properties": {
        "ground_truth": {
          "description": "ground_truth parameter (DataFrame).",
          "type": "string"
        },
        "llm_client": {
          "description": "llm_client parameter (callable).",
          "type": "string"
        }
      },
      "required": [
        "ground_truth",
        "llm_client"
      ],
      "type": "object"
    }
  },
  {
    "description": "Bareinboim-Forney-Pearl contextual causal bandit: pick the optimal arm by Monte-Carlo estimation of E[Y(a) | context].",
    "name": "causal_bandit",
    "parameters": {
      "properties": {
        "arms": {
          "description": "arms parameter (list).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "context": {
          "description": "context parameter (dict).",
          "type": "object"
        },
        "n_samples": {
          "default": 500,
          "description": "Number of samples.",
          "type": "integer"
        },
        "reward_fn": {
          "description": "reward_fn parameter (callable).",
          "type": "string"
        }
      },
      "required": [
        "arms",
        "reward_fn"
      ],
      "type": "object"
    }
  },
  {
    "description": "Counterfactual policy evaluation under a linear-Gaussian SCM via noise inversion (Oberst-Sontag 2019, Buesing et al. 2019).",
    "name": "counterfactual_policy_optimization",
    "parameters": {
      "properties": {
        "action": {
          "description": "action parameter (str).",
          "type": "string"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "reward": {
          "description": "reward parameter (str).",
          "type": "string"
        },
        "state": {
          "description": "state parameter (str).",
          "type": "string"
        },
        "target_policy": {
          "description": "target_policy parameter (callable).",
          "type": "string"
        }
      },
      "required": [
        "data",
        "state",
        "action",
        "reward",
        "target_policy"
      ],
      "type": "object"
    }
  },
  {
    "description": "Fit a linear SVAR for a Markov decision process and roll out counterfactual trajectories under alternative policies.",
    "name": "structural_mdp",
    "parameters": {
      "properties": {
        "action_cols": {
          "description": "action_cols parameter (list).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "next_state_cols": {
          "description": "next_state_cols parameter (list).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "reward": {
          "description": "reward parameter (str).",
          "type": "string"
        },
        "state_cols": {
          "description": "state_cols parameter (list).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "time": {
          "description": "Time period column.",
          "type": "string"
        },
        "trajectory": {
          "description": "trajectory parameter (str).",
          "type": "string"
        }
      },
      "required": [
        "data",
        "state_cols",
        "action_cols",
        "reward"
      ],
      "type": "object"
    }
  },
  {
    "description": "Neural-net propensity score estimator (arXiv:2404.04794, 2024).",
    "name": "dl_propensity_score",
    "parameters": {
      "properties": {
        "covariates": {
          "description": "Covariate matrix, DataFrame, or column names.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "hidden_sizes": {
          "description": "hidden_sizes parameter (list).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "treatment": {
          "description": "Treatment indicator, treatment variable, or treatment array.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "treatment",
        "covariates"
      ],
      "type": "object"
    }
  },
  {
    "description": "Split-conformal prediction bands for continuous-treatment dose-response curves (Schroder, Frauen, Schweisthal, He, Melnychuk, Feuerriegel 2024, arXiv:2407.03094).",
    "name": "conformal_continuous",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.1,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "covariates": {
          "description": "Covariate matrix, DataFrame, or column names.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "test_data": {
          "description": "test_data parameter (DataFrame).",
          "type": "string"
        },
        "treatment": {
          "description": "Treatment indicator, treatment variable, or treatment array.",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "treatment",
        "covariates",
        "test_data"
      ],
      "type": "object"
    }
  },
  {
    "description": "Cluster-exchangeable split-conformal prediction under network interference (2509.21660 systematic review).",
    "name": "conformal_interference",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.1,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "cluster": {
          "description": "Cluster identifier column for clustered standard errors.",
          "type": "string"
        },
        "covariates": {
          "description": "Covariate matrix, DataFrame, or column names.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "test_clusters": {
          "description": "test_clusters parameter (list).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "treatment": {
          "description": "Treatment indicator, treatment variable, or treatment array.",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "treatment",
        "cluster",
        "covariates",
        "test_clusters"
      ],
      "type": "object"
    }
  },
  {
    "description": "Sharp bounds on off-policy value under unobserved confounding via the marginal-sensitivity Gamma-model (Kallus, Mao, Uehara 2025). Validation: validated evidence tier (known-truth, reference, external-parity, or Monte Carlo artifact).",
    "name": "sharp_ope_unobserved",
    "parameters": {
      "properties": {
        "actions": {
          "description": "actions parameter (str).",
          "type": "string"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "gamma": {
          "default": 1.5,
          "description": "gamma parameter (float).",
          "type": "number"
        },
        "logging_prob": {
          "description": "logging_prob parameter (str).",
          "type": "string"
        },
        "rewards": {
          "description": "rewards parameter (str).",
          "type": "string"
        },
        "target_prob": {
          "description": "target_prob parameter (str).",
          "type": "string"
        }
      },
      "required": [
        "data",
        "actions",
        "rewards",
        "logging_prob",
        "target_prob"
      ],
      "type": "object"
    }
  },
  {
    "description": "Forest of doubly-robust policy trees: ensembles depth-limited trees over AIPW-scored actions to reduce variance and give honest policy-value SE (2025). Validation: validated evidence tier (known-truth, reference, external-parity, or Monte Carlo artifact).",
    "name": "causal_policy_forest",
    "parameters": {
      "properties": {
        "actions": {
          "description": "actions parameter (str).",
          "type": "string"
        },
        "covariates": {
          "description": "Covariate matrix, DataFrame, or column names.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "depth": {
          "default": 3,
          "description": "depth parameter (int).",
          "type": "integer"
        },
        "n_trees": {
          "default": 20,
          "description": "Number of trees.",
          "type": "integer"
        },
        "rewards": {
          "description": "rewards parameter (str).",
          "type": "string"
        }
      },
      "required": [
        "data",
        "actions",
        "rewards",
        "covariates"
      ],
      "type": "object"
    }
  },
  {
    "description": "Orthogonal learning of direct + spillover effects under network interference via cross-fitted double-residualisation (Wu & Yuan 2025, arXiv:2509.18484).",
    "name": "network_hte",
    "parameters": {
      "properties": {
        "covariates": {
          "description": "Covariate matrix, DataFrame, or column names.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "n_folds": {
          "default": 5,
          "description": "Number of cross-fitting or cross-validation folds.",
          "type": "integer"
        },
        "neighbor_exposure": {
          "description": "neighbor_exposure parameter (str).",
          "type": "string"
        },
        "treatment": {
          "description": "Treatment indicator, treatment variable, or treatment array.",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "treatment",
        "neighbor_exposure",
        "covariates"
      ],
      "type": "object"
    }
  },
  {
    "description": "Decompose network spillover into inward (incoming edges to unit i) and outward (from i to neighbours) components.",
    "name": "inward_outward_spillover",
    "parameters": {
      "properties": {
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "inward_exposure": {
          "description": "inward_exposure parameter (str).",
          "type": "string"
        },
        "outward_exposure": {
          "description": "outward_exposure parameter (str).",
          "type": "string"
        },
        "treatment": {
          "description": "Treatment indicator, treatment variable, or treatment array.",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "treatment",
        "inward_exposure",
        "outward_exposure"
      ],
      "type": "object"
    }
  },
  {
    "description": "Bayesian Double Machine Learning (DiTraglia & Liu 2025): Normal-Normal conjugate update on a DML point estimate, with optional full PyMC MCMC over the orthogonal moment equation.",
    "name": "bayes_dml",
    "parameters": {
      "properties": {
        "covariates": {
          "description": "Covariate matrix, DataFrame, or column names.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "mode": {
          "default": "conjugate",
          "description": "mode parameter (str).",
          "enum": [
            "conjugate",
            "full"
          ],
          "type": "string"
        },
        "model": {
          "default": "plr",
          "description": "Model variant or parameterisation to fit.",
          "enum": [
            "plr",
            "irm",
            "pliv"
          ],
          "type": "string"
        },
        "prior_mean": {
          "default": 0.0,
          "description": "prior_mean parameter (float).",
          "type": "number"
        },
        "prior_sd": {
          "default": 10.0,
          "description": "prior_sd parameter (float).",
          "type": "number"
        },
        "treatment": {
          "description": "Treatment indicator, treatment variable, or treatment array.",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "treatment",
        "covariates"
      ],
      "type": "object"
    }
  },
  {
    "description": "Bayesian Difference-in-Differences with staggered adoption -- hierarchical ATT(g,t) posterior with optional cohort / unit random effects. Full MCMC with PyMC; reports R-hat, ESS, divergences, and 94% HDI.",
    "name": "bayes_did",
    "parameters": {
      "properties": {
        "chains": {
          "default": 4,
          "description": "Number of parallel chains",
          "type": "integer"
        },
        "cohort": {
          "description": "First-treatment-period column for staggered adoption",
          "type": "string"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "draws": {
          "default": 2000,
          "description": "Post-warmup draws per chain",
          "type": "integer"
        },
        "post": {
          "description": "Post-treatment indicator (1 = post-period)",
          "type": "string"
        },
        "target_accept": {
          "default": 0.9,
          "description": "HMC target acceptance rate",
          "type": "number"
        },
        "time": {
          "description": "Time period column",
          "type": "string"
        },
        "treat": {
          "description": "Treatment-group indicator (1 = ever-treated)",
          "type": "string"
        },
        "tune": {
          "default": 1000,
          "description": "Warmup draws per chain",
          "type": "integer"
        },
        "unit": {
          "description": "Unit identifier (enables unit random effects)",
          "type": "string"
        },
        "y": {
          "description": "Outcome",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "treat",
        "post"
      ],
      "type": "object"
    }
  },
  {
    "description": "Bayesian instrumental variables with full posterior over structural parameters. Handles weak instruments via shrinkage priors and reports posterior mass near zero on the first-stage coefficient. Validation: validated evidence tier (known-truth, reference, external-parity, or Monte Carlo artifact).",
    "name": "bayes_iv",
    "parameters": {
      "properties": {
        "chains": {
          "default": 4,
          "description": "chains parameter (int).",
          "type": "integer"
        },
        "covariates": {
          "description": "Exogenous controls",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "draws": {
          "default": 2000,
          "description": "draws parameter (int).",
          "type": "integer"
        },
        "instrument": {
          "description": "Instrument(s) -- str or list",
          "type": "string"
        },
        "treat": {
          "description": "Endogenous treatment",
          "type": "string"
        },
        "tune": {
          "default": 1000,
          "description": "tune parameter (int).",
          "type": "integer"
        },
        "y": {
          "description": "Outcome",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "treat",
        "instrument"
      ],
      "type": "object"
    }
  },
  {
    "description": "Multivariable Mendelian randomization (Sanderson-Windmeijer 2019): direct causal effects of multiple correlated exposures via weighted least-squares on SNP-summary data, with conditional F-statistics for instrument strength. Validation: certified parity evidence.",
    "name": "mr_multivariable",
    "parameters": {
      "properties": {
        "exposures": {
          "description": "exposures parameter (list).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "outcome": {
          "default": "beta_y",
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        },
        "outcome_se": {
          "default": "se_y",
          "description": "outcome_se parameter (str).",
          "type": "string"
        },
        "snp_associations": {
          "description": "snp_associations parameter (DataFrame).",
          "type": "string"
        }
      },
      "required": [
        "snp_associations"
      ],
      "type": "object"
    }
  },
  {
    "description": "Two-step (network) MR: decompose the total causal effect of an exposure on an outcome into direct + indirect (mediated) components. Validation: certified parity evidence.",
    "name": "mr_mediation",
    "parameters": {
      "properties": {
        "beta_exposure": {
          "default": "beta_x",
          "description": "beta_exposure parameter (str).",
          "type": "string"
        },
        "beta_mediator": {
          "default": "beta_m",
          "description": "beta_mediator parameter (str).",
          "type": "string"
        },
        "beta_outcome": {
          "default": "beta_y",
          "description": "beta_outcome parameter (str).",
          "type": "string"
        },
        "snp_associations": {
          "description": "snp_associations parameter (DataFrame).",
          "type": "string"
        }
      },
      "required": [
        "snp_associations"
      ],
      "type": "object"
    }
  },
  {
    "description": "MR Bayesian model averaging over exposure subsets (Zuber et al. 2020). Outputs marginal inclusion probabilities and top posterior models. Validation: certified parity evidence.",
    "name": "mr_bma",
    "parameters": {
      "properties": {
        "exposures": {
          "description": "exposures parameter (list).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "max_model_size": {
          "description": "max_model_size parameter (int).",
          "type": "integer"
        },
        "method": {
          "default": "bf",
          "description": "``'bf'`` is MR-BMA as Zuber, Colijn, Klaver & Burgess (2020) define and implement it (their ``summary_mvMR_BF``): the IVW-scaled regression ``beta_Y / se_Y ~ beta_X / se_Y`` without intercept, a closed-form Bayes factor under the normal prior, posterior-mean effects averaged over models. ``'bic'`` is the pre-1.30 behaviour: posterior weights ``exp(-BIC/2)`` from weighted least squares.",
          "enum": [
            "bf",
            "bic"
          ],
          "type": "string"
        },
        "outcome": {
          "default": "beta_y",
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        },
        "outcome_se": {
          "default": "se_y",
          "description": "outcome_se parameter (str).",
          "type": "string"
        },
        "prior_sd": {
          "default": 0.5,
          "description": "Standard deviation ``sigma`` of the independent normal prior on each causal effect (Zuber et al.'s ``sigma``).",
          "type": "number"
        },
        "snp_associations": {
          "description": "snp_associations parameter (DataFrame).",
          "type": "string"
        }
      },
      "required": [
        "snp_associations"
      ],
      "type": "object"
    }
  },
  {
    "description": "Sample-overlap-corrected IVW MR (Burgess-Davies-Thompson 2016 closed-form correction). Removes first-order bias when exposure and outcome GWAS share participants; requires overlap_fraction and overlap_rho (e.g. from LD-score regression). Validation: validated evidence tier (known-truth, reference, external-parity, or Monte Carlo artifact).",
    "name": "mr_lap",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "beta_exposure": {
          "description": "beta_exposure parameter (ndarray).",
          "type": "string"
        },
        "beta_outcome": {
          "description": "beta_outcome parameter (ndarray).",
          "type": "string"
        },
        "overlap_fraction": {
          "default": 1.0,
          "description": "overlap_fraction parameter (float).",
          "type": "number"
        },
        "overlap_rho": {
          "default": 0.0,
          "description": "overlap_rho parameter (float).",
          "type": "number"
        },
        "se_exposure": {
          "description": "se_exposure parameter (ndarray).",
          "type": "string"
        },
        "se_outcome": {
          "description": "se_outcome parameter (ndarray).",
          "type": "string"
        }
      },
      "required": [
        "beta_exposure",
        "beta_outcome",
        "se_exposure",
        "se_outcome"
      ],
      "type": "object"
    }
  },
  {
    "description": "Clustered Mendelian randomization via finite Gaussian mixture on Wald ratios (Foley et al. 2021). EM with SNP-specific measurement SE; optional 'null' cluster at theta=0; K selected by BIC. Returns per-cluster estimate, SNP-to-cluster responsibilities, and the K-path.",
    "name": "mr_clust",
    "parameters": {
      "properties": {
        "K_range": {
          "default": [
            1,
            5
          ],
          "description": "K_range parameter (tuple).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "beta_exposure": {
          "description": "beta_exposure parameter (ndarray).",
          "type": "string"
        },
        "beta_outcome": {
          "description": "beta_outcome parameter (ndarray).",
          "type": "string"
        },
        "include_null": {
          "default": true,
          "description": "Whether to include null.",
          "type": "boolean"
        },
        "se_exposure": {
          "description": "se_exposure parameter (ndarray).",
          "type": "string"
        },
        "se_outcome": {
          "description": "se_outcome parameter (ndarray).",
          "type": "string"
        },
        "seed": {
          "default": 0,
          "description": "Random seed for reproducible stochastic steps.",
          "type": "integer"
        }
      },
      "required": [
        "beta_exposure",
        "beta_outcome",
        "se_exposure",
        "se_outcome"
      ],
      "type": "object"
    }
  },
  {
    "description": "GRAPPLE: profile-likelihood MR with joint weak-instrument and balanced-pleiotropy robustness (Wang et al. 2021). Model: beta_y = beta*beta_x + u, Var(u) = se_y^2 + beta^2*se_x^2 + tau^2; jointly MLE over (beta, tau^2) via L-BFGS-B; SE from observed Fisher info. Validation: certified parity evidence.",
    "name": "grapple",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "beta_exposure": {
          "description": "beta_exposure parameter (ndarray).",
          "type": "string"
        },
        "beta_init": {
          "description": "beta_init parameter (float).",
          "type": "number"
        },
        "beta_outcome": {
          "description": "beta_outcome parameter (ndarray).",
          "type": "string"
        },
        "k": {
          "description": "Loss tuning constant; GRAPPLE's defaults 4.685 (Tukey), 1.345 (Huber).",
          "type": "string"
        },
        "loss": {
          "default": "tukey",
          "description": "GRAPPLE's ``loss.function`` (its default is Tukey).",
          "enum": [
            "tukey",
            "huber",
            "l2"
          ],
          "type": "string"
        },
        "max_iter": {
          "default": 200,
          "description": "Maximum number of (tau2, beta) alternation rounds; the result records whether tol was met.",
          "type": "integer"
        },
        "se_exposure": {
          "description": "se_exposure parameter (ndarray).",
          "type": "string"
        },
        "se_outcome": {
          "description": "se_outcome parameter (ndarray).",
          "type": "string"
        },
        "tau2_init": {
          "default": 0.0001,
          "description": "tau2_init parameter (float).",
          "type": "number"
        },
        "tol": {
          "default": 1e-13,
          "description": "Relative convergence tolerance of the alternation. GRAPPLE stops its ``tau2`` root at ``bound * eps^0.25`` (absolute) and its ``beta`` search at ``optim``'s default, so its reported numbers are a few significant digits short of the solution computed here.",
          "type": "number"
        }
      },
      "required": [
        "beta_exposure",
        "beta_outcome",
        "se_exposure",
        "se_outcome"
      ],
      "type": "object"
    }
  },
  {
    "description": "MR-cML-BIC: constrained maximum-likelihood MR with L0-sparse pleiotropy (Xue, Shen & Pan 2021). Block-coordinate descent jointly updates causal beta, true exposure effects, and a K-sparse pleiotropy vector; K selected by BIC. Robust to correlated + uncorrelated pleiotropy simultaneously. Validation: certified parity evidence.",
    "name": "mr_cml",
    "parameters": {
      "properties": {
        "K_max": {
          "description": "K_max parameter (int).",
          "type": "integer"
        },
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "beta_exposure": {
          "description": "beta_exposure parameter (ndarray).",
          "type": "string"
        },
        "beta_outcome": {
          "description": "beta_outcome parameter (ndarray).",
          "type": "string"
        },
        "max_iter": {
          "default": 100,
          "description": "Maximum number of optimisation iterations.",
          "type": "integer"
        },
        "model_average": {
          "default": false,
          "description": "model_average parameter (bool).",
          "type": "boolean"
        },
        "n": {
          "description": "n parameter (int).",
          "type": "integer"
        },
        "se_exposure": {
          "description": "se_exposure parameter (ndarray).",
          "type": "string"
        },
        "se_outcome": {
          "description": "se_outcome parameter (ndarray).",
          "type": "string"
        },
        "tol": {
          "default": 1e-07,
          "description": "Numerical convergence tolerance.",
          "type": "number"
        }
      },
      "required": [
        "beta_exposure",
        "beta_outcome",
        "se_exposure",
        "se_outcome"
      ],
      "type": "object"
    }
  },
  {
    "description": "MR-RAPS: Robust Adjusted Profile Score for two-sample summary-data MR (Zhao et al. 2020, Annals of Statistics). A port of the authors' mr.raps package: the simple and over-dispersed profile-score estimators, and the robust over-dispersed one with a Huber (default, the package's) or Tukey loss; weak-instrument corrected; resistant to a small fraction of gross pleiotropy outliers. Validation: certified parity evidence.",
    "name": "mr_raps",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "beta_exposure": {
          "description": "beta_exposure parameter (ndarray).",
          "type": "string"
        },
        "beta_init": {
          "description": "Deprecated since 1.28.0 and ignored (the fit starts from the L2 over-dispersed estimate, as mr.raps does); removed in 1.29.",
          "type": "number"
        },
        "beta_outcome": {
          "description": "beta_outcome parameter (ndarray).",
          "type": "string"
        },
        "loss": {
          "description": "None means huber, the mr.raps default.",
          "enum": [
            "huber",
            "tukey",
            "l2"
          ],
          "type": "string"
        },
        "niter": {
          "default": 20,
          "description": "niter parameter (int).",
          "type": "integer"
        },
        "over_dispersion": {
          "default": true,
          "description": "over_dispersion parameter (bool).",
          "type": "boolean"
        },
        "pruning": {
          "default": true,
          "description": "pruning parameter (bool).",
          "type": "boolean"
        },
        "se_exposure": {
          "description": "se_exposure parameter (ndarray).",
          "type": "string"
        },
        "se_outcome": {
          "description": "se_outcome parameter (ndarray).",
          "type": "string"
        },
        "tau2_init": {
          "description": "Deprecated since 1.28.0 and ignored; removed in 1.29.",
          "type": "number"
        },
        "tol": {
          "default": 1.4901161193847656e-08,
          "description": "Numerical convergence tolerance.",
          "type": "number"
        },
        "tuning_c": {
          "description": "tuning_c parameter (float).",
          "type": "number"
        }
      },
      "required": [
        "beta_exposure",
        "beta_outcome",
        "se_exposure",
        "se_outcome"
      ],
      "type": "object"
    }
  },
  {
    "description": "Render the JAMA/BMJ 2025 TARGET Statement 21-item reporting checklist as a completed Markdown table, auto-filled from a TargetTrialResult and flagged for any remaining TODO items. Validation: validated evidence tier (known-truth, reference, external-parity, or Monte Carlo artifact).",
    "name": "target_trial_checklist",
    "parameters": {
      "properties": {
        "fmt": {
          "default": "markdown",
          "description": "fmt parameter (str).",
          "enum": [
            "markdown",
            "text"
          ],
          "type": "string"
        },
        "result": {
          "description": "result parameter (TargetTrialResult).",
          "type": "string"
        }
      },
      "required": [
        "result"
      ],
      "type": "object"
    }
  },
  {
    "description": "Hierarchical Bayesian Causal Forest for longitudinal data (BCFLong) -- allows mu_t(X), tau_t(X) to evolve across time with unit-level random intercepts.",
    "name": "bcf_longitudinal",
    "parameters": {
      "properties": {
        "covariates": {
          "description": "Covariate matrix, DataFrame, or column names.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "n_bootstrap": {
          "default": 100,
          "description": "Number of bootstrap replications.",
          "type": "integer"
        },
        "n_trees_mu": {
          "default": 200,
          "description": "Number of trees mu.",
          "type": "integer"
        },
        "n_trees_tau": {
          "default": 50,
          "description": "Number of trees tau.",
          "type": "integer"
        },
        "outcome": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        },
        "time": {
          "description": "Time period column.",
          "type": "string"
        },
        "treatment": {
          "description": "Treatment indicator, treatment variable, or treatment array.",
          "type": "string"
        },
        "unit": {
          "description": "Unit identifier column.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "outcome",
        "treatment",
        "unit",
        "time",
        "covariates"
      ],
      "type": "object"
    }
  },
  {
    "description": "Latent-PCMCI: time-series causal discovery allowing hidden common causes. Outputs a lag-specific adjacency tensor with typed edges (directed, bidirected, uncertain).",
    "name": "lpcmci",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "tau_max": {
          "default": 3,
          "description": "tau_max parameter (int).",
          "type": "integer"
        },
        "variables": {
          "description": "variables parameter (list).",
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "required": [
        "data"
      ],
      "type": "object"
    }
  },
  {
    "description": "DYNOTEARS: continuous-optimisation structure learning for structural VARs. Returns contemporaneous (W) and lagged (A) adjacency matrices with the contemporaneous part enforced to be acyclic via the NOTEARS h(W) penalty.",
    "name": "dynotears",
    "parameters": {
      "properties": {
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "lag": {
          "default": 1,
          "description": "lag parameter (int).",
          "type": "integer"
        },
        "lambda_a": {
          "default": 0.05,
          "description": "lambda_a parameter (float).",
          "type": "number"
        },
        "lambda_w": {
          "default": 0.05,
          "description": "lambda_w parameter (float).",
          "type": "number"
        },
        "threshold": {
          "default": 0.1,
          "description": "threshold parameter (float).",
          "type": "number"
        },
        "variables": {
          "description": "variables parameter (list).",
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "required": [
        "data"
      ],
      "type": "object"
    }
  },
  {
    "description": "Cohort-by-cohort synthetic DID for staggered-adoption panels: runs sp.sdid on each adoption cohort's sub-panel with not-yet-treated donors (each ATT(g) equals synthdid::synthdid_estimate) and aggregates across cohorts. Not the Arkhangelsky-Samkov sequential SDID estimator. Validation: validated evidence tier (known-truth, reference, external-parity, or Monte Carlo artifact).",
    "name": "sequential_sdid",
    "parameters": {
      "properties": {
        "cohort": {
          "description": "First-treated period column; never-treated = 0",
          "type": "string"
        },
        "cohort_weights": {
          "default": "size",
          "description": "cohort_weights parameter (str).",
          "enum": [
            "size",
            "equal"
          ],
          "type": "string"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "n_reps": {
          "default": 200,
          "description": "Number of reps.",
          "type": "integer"
        },
        "never_treated_value": {
          "default": 0,
          "description": "never_treated_value parameter.",
          "type": "string"
        },
        "outcome": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        },
        "se_method": {
          "default": "placebo",
          "description": "se_method parameter (str).",
          "enum": [
            "placebo",
            "bootstrap",
            "jackknife"
          ],
          "type": "string"
        },
        "time": {
          "description": "Time period column.",
          "type": "string"
        },
        "unit": {
          "description": "Unit identifier column.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "outcome",
        "unit",
        "time",
        "cohort"
      ],
      "type": "object"
    }
  },
  {
    "description": "Kusner-Loftus-Russell-Silva (2018) counterfactual-fairness test: compares factual vs. SCM-intervened predictions to measure path-specific dependence of a classifier on the protected attribute. Validation: validated evidence tier (known-truth, reference, external-parity, or Monte Carlo artifact).",
    "name": "counterfactual_fairness",
    "parameters": {
      "properties": {
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "predictor": {
          "description": "Callable(DataFrame) -> predictions",
          "type": "string"
        },
        "protected": {
          "description": "protected parameter (str).",
          "type": "string"
        },
        "scm_intervention": {
          "description": "scm_intervention parameter (callable).",
          "type": "string"
        },
        "threshold": {
          "default": 0.05,
          "description": "threshold parameter (float).",
          "type": "number"
        }
      },
      "required": [
        "data",
        "predictor",
        "protected",
        "scm_intervention"
      ],
      "type": "object"
    }
  },
  {
    "description": "Residualize features against the protected attribute as a pre-processing step toward counterfactual fairness. Validation: validated evidence tier (known-truth, reference, external-parity, or Monte Carlo artifact).",
    "name": "orthogonal_to_bias",
    "parameters": {
      "properties": {
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "features": {
          "description": "Feature matrix or feature column names.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "protected": {
          "description": "protected parameter (str).",
          "type": "string"
        }
      },
      "required": [
        "data",
        "features",
        "protected"
      ],
      "type": "object"
    }
  },
  {
    "description": "Demographic-parity gap between groups defined by the protected attribute. Validation: validated evidence tier (known-truth, reference, external-parity, or Monte Carlo artifact).",
    "name": "demographic_parity",
    "parameters": {
      "properties": {
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "predictions": {
          "description": "predictions parameter (str).",
          "type": "string"
        },
        "protected": {
          "description": "protected parameter (str).",
          "type": "string"
        },
        "threshold": {
          "default": 0.1,
          "description": "threshold parameter (float).",
          "type": "number"
        }
      },
      "required": [
        "data",
        "predictions",
        "protected"
      ],
      "type": "object"
    }
  },
  {
    "description": "Hardt-Price-Srebro equalized-odds gap -- max of TPR and FPR group differences. Validation: validated evidence tier (known-truth, reference, external-parity, or Monte Carlo artifact).",
    "name": "equalized_odds",
    "parameters": {
      "properties": {
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "labels": {
          "description": "labels parameter (str).",
          "type": "string"
        },
        "predictions": {
          "description": "predictions parameter (str).",
          "type": "string"
        },
        "protected": {
          "description": "protected parameter (str).",
          "type": "string"
        },
        "threshold": {
          "default": 0.1,
          "description": "threshold parameter (float).",
          "type": "number"
        }
      },
      "required": [
        "data",
        "predictions",
        "labels",
        "protected"
      ],
      "type": "object"
    }
  },
  {
    "description": "One-shot dashboard combining demographic parity, equalized odds, and (optionally) counterfactual fairness. Validation: validated evidence tier (known-truth, reference, external-parity, or Monte Carlo artifact).",
    "name": "fairness_audit",
    "parameters": {
      "properties": {
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "labels": {
          "description": "labels parameter (str).",
          "type": "string"
        },
        "predictions": {
          "description": "predictions parameter (str).",
          "type": "string"
        },
        "predictor": {
          "description": "predictor parameter (callable).",
          "type": "string"
        },
        "protected": {
          "description": "protected parameter (str).",
          "type": "string"
        },
        "scm_intervention": {
          "description": "scm_intervention parameter (callable).",
          "type": "string"
        }
      },
      "required": [
        "data",
        "predictions",
        "protected"
      ],
      "type": "object"
    }
  },
  {
    "description": "Proximal surrogate-index estimator: long-term ATE when an unobserved U confounds S->Y, using a proxy W and 2SLS-style bridge-function identification (Imbens-Kallus-Mao-Wang 2025, JRSS-B). Validation: validated evidence tier (known-truth, reference, external-parity, or Monte Carlo artifact).",
    "name": "proximal_surrogate_index",
    "parameters": {
      "properties": {
        "covariates": {
          "description": "Covariate matrix, DataFrame, or column names.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "experimental": {
          "description": "experimental parameter (DataFrame).",
          "type": "string"
        },
        "long_term_outcome": {
          "description": "long_term_outcome parameter (str).",
          "type": "string"
        },
        "n_boot": {
          "default": 200,
          "description": "Number of bootstrap replications.",
          "type": "integer"
        },
        "observational": {
          "description": "observational parameter (DataFrame).",
          "type": "string"
        },
        "proxies": {
          "description": "proxies parameter (list).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "surrogates": {
          "description": "surrogates parameter (list).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "treatment": {
          "description": "Treatment indicator, treatment variable, or treatment array.",
          "type": "string"
        }
      },
      "required": [
        "experimental",
        "observational",
        "treatment",
        "surrogates",
        "proxies",
        "long_term_outcome"
      ],
      "type": "object"
    }
  },
  {
    "description": "Gardner (2022) two-stage DID. Stage-1 fits two-way FEs on untreated observations; Stage-2 regresses the residualised outcome on treatment dummies (ATT or event study). Standard errors are the Butts-Gardner two-stage corrected clustered variance (R/Stata did2s), reproduced to ~1e-8 on castle-doctrine; weights= gives did2s(weights=) / [aw=]. Validation: certified parity evidence.",
    "name": "gardner_did",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "boot_seed": {
          "description": "Seed for the cluster bootstrap",
          "type": "integer"
        },
        "cluster": {
          "description": "Cluster variable for the two-stage clustered SEs (defaults to group)",
          "type": "string"
        },
        "controls": {
          "description": "Additional covariates",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "event_study": {
          "default": false,
          "description": "If True, report coefficients by relative time k = t - first_treat",
          "type": "boolean"
        },
        "first_treat": {
          "description": "First-treatment-period column; 0/NaN/inf = never treated",
          "type": "string"
        },
        "group": {
          "description": "Unit/panel-id column",
          "type": "string"
        },
        "horizon": {
          "description": "Relative-time leads/lags to report (default range(-5, 6))",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "n_boot": {
          "default": 199,
          "description": "Cluster-bootstrap replications when vce='bootstrap'",
          "type": "integer"
        },
        "time": {
          "description": "Time column",
          "type": "string"
        },
        "vce": {
          "default": "analytic",
          "description": "Standard errors: 'analytic' (did2s corrected two-stage clustered variance, stage-1 estimation error propagated; the did2s/R and Stata convention), 'stage2' (pre-1.29 stage-2-only SE), 'bootstrap' (cluster bootstrap of both stages), or 'none'",
          "enum": [
            "analytic",
            "stage2",
            "bootstrap",
            "none"
          ],
          "type": "string"
        },
        "weights": {
          "description": "Strictly positive estimation weights for both stages (R did2s weights= / Stata [aw=])",
          "type": "string"
        },
        "y": {
          "description": "Outcome column",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "group",
        "time",
        "first_treat"
      ],
      "type": "object"
    }
  },
  {
    "description": "Model-averaging DML (PLR) per Ahrens et al. (2025, JAE). Fits DML-PLR under multiple candidate nuisance learners and reports a risk-weighted (or equal/single-best) average of their theta estimates with a covariance-adjusted SE. Validation: certified parity evidence.",
    "name": "dml_model_averaging",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "candidates": {
          "description": "List of (ml_g, ml_m, label) sklearn triples; defaults to Lasso/Ridge/RF/GBM",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "covariates": {
          "description": "Covariate columns X",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "fold_indices": {
          "description": "Explicit cross-fitting fold per row (column name or array of length ``len(data)``). Overrides ``n_folds`` / ``seed``; every candidate uses the same partition.",
          "type": "string"
        },
        "n_folds": {
          "default": 5,
          "description": "Number of cross-fitting or cross-validation folds.",
          "type": "integer"
        },
        "seed": {
          "default": 0,
          "description": "Random seed for reproducible stochastic steps.",
          "type": "integer"
        },
        "treat": {
          "description": "Treatment column",
          "type": "string"
        },
        "weight_rule": {
          "default": "short_stacking",
          "description": "Weighting of candidate estimators",
          "enum": [
            "short_stacking",
            "single_best",
            "inverse_risk",
            "equal"
          ],
          "type": "string"
        },
        "y": {
          "description": "Outcome column",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "treat",
        "covariates"
      ],
      "type": "object"
    }
  },
  {
    "description": "Dynamic Double/Debiased ML for a treatment assigned repeatedly over time (Lewis & Syrgkanis 2021). Use it when the treatment moves the state that drives later treatment: controlling for the later state blocks the indirect path, not controlling for it leaves the confounding in. Returns the effect of intervening on each period's treatment on the final outcome, the total sequence effect, and the joint covariance -- so a contrast across periods has an honest standard error instead of a sum of variances. Optional linear heterogeneity in baseline modifiers. Reproduces econml's DynamicDML to 1e-15 given the same folds and learners. Validation: validated evidence tier (known-truth, reference, external-parity, or Monte Carlo artifact).",
    "name": "dynamic_dml",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "baseline": {
          "description": "Time-invariant controls (first-period values).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "covariates": {
          "description": "Time-varying state read at each period.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "data": {
          "description": "Long panel",
          "type": "string"
        },
        "fold_ids": {
          "description": "One fold index per complete unit, to reproduce an external split.",
          "type": "string"
        },
        "id": {
          "description": "Unit id column; unit= is accepted as an alias.",
          "type": "string"
        },
        "lags": {
          "default": 1,
          "description": "Lagged treatments added to each period's state. lags=0 warns: omitting the treatment history moved the test design's estimates by -15%, -15% and +37%.",
          "type": "integer"
        },
        "model_t": {
          "description": "Treatment nuisance; default RidgeCV.",
          "type": "string"
        },
        "model_y": {
          "description": "Outcome nuisance; default RidgeCV.",
          "type": "string"
        },
        "modifiers": {
          "description": "Baseline effect modifiers; the final stage becomes linear in them and result.coef holds the projection.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "n_folds": {
          "default": 5,
          "description": "Folds; whole units held out.",
          "type": "integer"
        },
        "periods": {
          "description": "Periods to use, in order; default every sorted value.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "random_state": {
          "default": 0,
          "description": "Random seed or RandomState for reproducible stochastic steps.",
          "type": "integer"
        },
        "time": {
          "description": "Period column",
          "type": "string"
        },
        "treat": {
          "description": "Treatment column",
          "type": "string"
        },
        "y": {
          "description": "Outcome; only its last-period value is used.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "treat",
        "id",
        "time"
      ],
      "type": "object"
    }
  },
  {
    "description": "Long-panel Double/Debiased ML for static panel models with fixed effects (Clarke & Polselli 2025, simplified). Absorbs unit (and optional time) fixed effects via within-transform, cross-fits ML nuisance learners with folds that split units, and reports cluster-robust SE at the unit level. PLR moment (continuous or binary treatment). Validation: certified parity evidence.",
    "name": "dml_panel",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "binary_treatment": {
          "default": false,
          "description": "binary_treatment parameter (bool).",
          "type": "boolean"
        },
        "covariates": {
          "description": "Covariate columns X_it",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "fold_indices": {
          "description": "Explicit cross-fitting fold per row (a column name or a length- ``len(data)`` array), constant within unit. Overrides ``n_folds`` and ``seed``; with a shared partition the estimate is reproducible across implementations (e.g.",
          "type": "string"
        },
        "include_time_fe": {
          "default": false,
          "description": "Whether to include time fe.",
          "type": "boolean"
        },
        "ml_g": {
          "description": "Outcome nuisance learner",
          "type": "string"
        },
        "ml_m": {
          "description": "Treatment nuisance learner",
          "type": "string"
        },
        "n_folds": {
          "default": 5,
          "description": "Number of cross-fitting or cross-validation folds.",
          "type": "integer"
        },
        "seed": {
          "default": 0,
          "description": "Random seed for reproducible stochastic steps.",
          "type": "integer"
        },
        "time": {
          "description": "Time column (required if include_time_fe)",
          "type": "string"
        },
        "treat": {
          "description": "Treatment column",
          "type": "string"
        },
        "unit": {
          "description": "Unit ID column (FE + clustering)",
          "type": "string"
        },
        "y": {
          "description": "Outcome column",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "treat",
        "covariates",
        "unit"
      ],
      "type": "object"
    }
  },
  {
    "description": "Kernel IV regression with uniform confidence bands (Lob et al. 2025). Estimates the structural function h*(d) = E[Y | do(D=d)] via kernel-weighted local averaging under a continuous instrument Z, with wild-bootstrap uniform SEs.",
    "name": "kernel_iv",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "bandwidth": {
          "description": "Silverman default",
          "type": "number"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "grid": {
          "description": "Grid of d-values (default 30 quantile-evenly spaced)",
          "type": "string"
        },
        "instrument": {
          "description": "Continuous instrument Z",
          "type": "string"
        },
        "n_boot": {
          "default": 100,
          "description": "Number of bootstrap replications.",
          "type": "integer"
        },
        "ridge": {
          "default": 0.001,
          "description": "Tikhonov regularisation",
          "type": "number"
        },
        "seed": {
          "default": 0,
          "description": "Random seed for reproducible stochastic steps.",
          "type": "integer"
        },
        "treat": {
          "description": "Continuous treatment D",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "treat",
        "instrument"
      ],
      "type": "object"
    }
  },
  {
    "description": "Species-diversity indices from record-level or count data: Shannon entropy, species richness, Pielou evenness, the Simpson family (concentration / Gini-Simpson / inverse) and Hill numbers of any order. Accepts long-format sighting records or a site-by-species matrix and groups straight onto a panel index, so an ecological outcome can be built inside the same pipeline that estimates on it. min_records makes the small-sample filter explicit rather than a footnote.",
    "name": "diversity_index",
    "parameters": {
      "properties": {
        "base": {
          "description": "Log base for Shannon",
          "type": "number"
        },
        "by": {
          "description": "Grouping keys, typically the panel index",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "count": {
          "description": "Abundance column; omit when one row is one record",
          "type": "string"
        },
        "data": {
          "description": "Long-format records or a site-by-species matrix",
          "type": "string"
        },
        "index": {
          "default": "shannon",
          "description": "Index/indices to compute, or 'all'",
          "enum": [
            "shannon",
            "richness",
            "pielou",
            "simpson",
            "gini_simpson",
            "inv_simpson",
            "hill",
            "all"
          ],
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "min_records": {
          "default": 0,
          "description": "Groups below this many records return NaN",
          "type": "integer"
        },
        "q": {
          "default": 1.0,
          "description": "Hill number order",
          "type": "number"
        },
        "species": {
          "description": "Species column (required for long-format input)",
          "type": "string"
        }
      },
      "required": [
        "data"
      ],
      "type": "object"
    }
  },
  {
    "description": "Zero-first-stage (ZFS) test of the exclusion restriction, plus van Kippersluis-Rietveld (2018) pleiotropy-robust correction. Estimates the instrument's direct effect on the outcome in a subsample where it has no first stage, reports the implied bias in the main-sample IV estimate, and returns the corrected point estimate with a cluster-bootstrap interval. Accepts the same exog / absorb / cluster spec as sp.iv, so the test runs on the specification actually fitted.",
    "name": "zero_first_stage",
    "parameters": {
      "properties": {
        "absorb": {
          "description": "Fixed effects absorbed in every component regression",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "cluster": {
          "description": "Cluster identifier column for clustered standard errors.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "endog": {
          "description": "Endogenous regressor",
          "type": "string"
        },
        "exog": {
          "description": "exog parameter (list[str] | str).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "instrument": {
          "description": "The single excluded instrument being tested",
          "type": "string"
        },
        "n_boot": {
          "default": 999,
          "description": "Bootstrap reps for the corrected estimate (0 to skip)",
          "type": "integer"
        },
        "random_state": {
          "description": "Random seed or RandomState for reproducible stochastic steps.",
          "type": "integer"
        },
        "y": {
          "description": "Outcome column",
          "type": "string"
        },
        "zfs": {
          "description": "Boolean column/mask marking the zero-first-stage subsample where the instrument is believed inert",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "endog",
        "instrument",
        "zfs"
      ],
      "type": "object"
    }
  },
  {
    "description": "Modern IV reporting bundle (R `ivDiag` analogue). Combines 2SLS point estimate, analytic + pairs/wild bootstrap SEs, Olea-Pflueger effective F, Lee-McCrary-Moreira-Porter (2022) tF-corrected critical value, Anderson-Rubin / CLR / K weak-IV-robust confidence sets, Kleibergen-Paap rk LM, Conley-Hansen-Rossi (2012) plausibly-exogenous LTZ sensitivity, and a Blandhol-Bonney-Mogstad-Torgovitsky (2022/2025) / Soczynski (2024) `TSLS-as-LATE` caveat into a single IVDiagResult. Validation: certified parity evidence.",
    "name": "iv_diag",
    "parameters": {
      "properties": {
        "absorb": {
          "description": "High-dimensional fixed effects to partial out before the bundle is computed, so every statistic describes the absorbed specification (ivreghdfe-equivalent)",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "boot_methods": {
          "default": [
            "pairs"
          ],
          "description": "Subset of {'pairs','wild'}",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "cluster": {
          "description": "Cluster column for cluster-robust SE / cluster bootstrap",
          "type": "string"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "endog": {
          "description": "Single endogenous regressor",
          "type": "string"
        },
        "exog": {
          "description": "Optional included exogenous controls",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "h0": {
          "default": 0.0,
          "description": "Null hypothesis for AR/CLR/K",
          "type": "number"
        },
        "include_clr_ci": {
          "default": false,
          "description": "Whether to include clr ci.",
          "type": "boolean"
        },
        "include_k_ci": {
          "default": false,
          "description": "Whether to include k ci.",
          "type": "boolean"
        },
        "instruments": {
          "description": "instruments parameter (list[str] | str).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "ltz_gamma_sd": {
          "description": "Standard deviation of CHR (2012) LTZ Gaussian prior on gamma",
          "type": "number"
        },
        "n_boot": {
          "default": 1000,
          "description": "Bootstrap replications (0 to skip)",
          "type": "integer"
        },
        "random_state": {
          "description": "Random seed or RandomState for reproducible stochastic steps.",
          "type": "integer"
        },
        "vcov": {
          "default": "HC1",
          "description": "Heteroskedasticity-robust covariance type",
          "enum": [
            "HC0",
            "HC1",
            "classic"
          ],
          "type": "string"
        },
        "y": {
          "description": "Outcome column",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "endog",
        "instruments"
      ],
      "type": "object"
    }
  },
  {
    "description": "Run several k-class / JIVE estimators on the same IV specification and return a one-row-per-method comparison DataFrame (estimate, SE, CI, first-stage F). Useful as a sensitivity sanity check before reporting.",
    "name": "iv_compare",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "endog_name": {
          "description": "Override endogenous-coefficient name lookup",
          "type": "string"
        },
        "formula": {
          "description": "Model formula using patsy/R-style syntax.",
          "type": "string"
        },
        "methods": {
          "default": [
            "2sls",
            "liml",
            "fuller",
            "jive"
          ],
          "description": "methods parameter (tuple[str]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "required": [
        "formula",
        "data"
      ],
      "type": "object"
    }
  },
  {
    "description": "LATE with a continuous instrument (Xie et al. 2025). Estimates the LATE on the maximal complier class via quantile-bin Wald ratios, weighted by the bin-pair with the largest first-stage response. Validation: validated evidence tier (known-truth, reference, external-parity, or Monte Carlo artifact).",
    "name": "continuous_iv_late",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "instrument": {
          "description": "Continuous instrument",
          "type": "string"
        },
        "n_boot": {
          "default": 200,
          "description": "Number of bootstrap replications.",
          "type": "integer"
        },
        "n_quantiles": {
          "default": 4,
          "description": "Number of instrument quantile bins",
          "type": "integer"
        },
        "seed": {
          "default": 0,
          "description": "Random seed for reproducible stochastic steps.",
          "type": "integer"
        },
        "treat": {
          "description": "Treatment indicator or first-treatment-period column.",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "treat",
        "instrument"
      ],
      "type": "object"
    }
  },
  {
    "description": "TMLE with Highly Adaptive Lasso (HAL) nuisance learners (Qian & van der Laan 2025). The stable 'delta' variant plugs HAL into standard TMLE. The reserved 'projection' variant raises NotImplementedError until the Riesz-projection targeting step has reference parity. Validation: validated evidence tier (known-truth, reference, external-parity, or Monte Carlo artifact). Known limitations: variant='projection' raises NotImplementedError -- the Riesz-projection targeting step from Li-Qiu-Wang-vdL (2025) Section 3.2 is not yet ported (the v1.11.x code path was a no-op on the point estimate; see CHANGELOG). The implementation roadmap and parity-test gates are in docs/rfc/hal_tmle_projection.md.",
    "name": "hal_tmle",
    "parameters": {
      "properties": {
        "C_propensity": {
          "default": 1.0,
          "description": "Inverse L1 penalty for HAL propensity classifier",
          "type": "number"
        },
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "covariates": {
          "description": "Covariate matrix, DataFrame, or column names.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "estimand": {
          "default": "ATE",
          "description": "Estimand",
          "enum": [
            "ATE",
            "ATT"
          ],
          "type": "string"
        },
        "lambda_outcome": {
          "description": "Outcome L1 penalty; None -> 5-fold CV",
          "type": "number"
        },
        "max_anchors_per_col": {
          "default": 40,
          "description": "Column name for max anchors per.",
          "type": "integer"
        },
        "n_folds": {
          "default": 5,
          "description": "Number of cross-fitting or cross-validation folds.",
          "type": "integer"
        },
        "random_state": {
          "default": 42,
          "description": "Random seed or RandomState for reproducible stochastic steps.",
          "type": "integer"
        },
        "treat": {
          "description": "Binary treatment",
          "type": "string"
        },
        "variant": {
          "default": "delta",
          "description": "HAL-TMLE variant",
          "enum": [
            "delta",
            "projection"
          ],
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "treat",
        "covariates"
      ],
      "type": "object"
    }
  },
  {
    "description": "Synthetic control on survival curves (not the Han & Shah 2025 SSC estimator). Fits a convex combination of donor Kaplan-Meier curves on the complementary log-log scale to match the treated arm's pre-treatment survival, then reports the post-treatment survival gap with placebo UCBs. Validation: validated evidence tier (known-truth, reference, external-parity, or Monte Carlo artifact).",
    "name": "synth_survival",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "data": {
          "description": "Long panel with one row per (unit, time) and a precomputed KM survival",
          "type": "string"
        },
        "n_placebos": {
          "default": 100,
          "description": "Number of placebos.",
          "type": "integer"
        },
        "seed": {
          "default": 0,
          "description": "Random seed for reproducible stochastic steps.",
          "type": "integer"
        },
        "survival": {
          "description": "Column with survival probability S_i(t)",
          "type": "string"
        },
        "time": {
          "description": "Time period column.",
          "type": "string"
        },
        "treat_time": {
          "description": "treat_time parameter (float).",
          "type": "number"
        },
        "treated": {
          "description": "Boolean column or name of the single treated unit",
          "type": "string"
        },
        "unit": {
          "description": "Unit/panel-id column",
          "type": "string"
        }
      },
      "required": [
        "data",
        "unit",
        "time",
        "survival",
        "treated",
        "treat_time"
      ],
      "type": "object"
    }
  },
  {
    "description": "Unified dispatcher for six causal-inference bridging theorems (2025-2026): DiD==SC (Shi-Athey), EWM==CATE (Ferman), IPW==DR==CB (Zhao-Percival), Bunching==RDD (Lu-Wang-Xie), DR-via-Calibration (Zhang), Long-term-surrogate==PCI (Imbens-Kallus-Mao-Wang). Reports both path estimates + doubly-robust recommendation.",
    "name": "bridge",
    "parameters": {
      "properties": {
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "kind": {
          "description": "Which bridge to invoke",
          "enum": [
            "did_sc",
            "ewm_cate",
            "cb_ipw",
            "kink_rdd",
            "dr_calib",
            "surrogate_pci"
          ],
          "type": "string"
        }
      },
      "required": [
        "data",
        "kind"
      ],
      "type": "object"
    }
  },
  {
    "description": "Causal deep Q-network (Li, Zhang, Bareinboim 2025, arXiv:2510.21110) for offline policy learning under unobserved confounding. Learns a confounding-robust Q-function via bootstrap data augmentation.",
    "name": "causal_dqn",
    "parameters": {
      "properties": {
        "action": {
          "description": "action parameter (str).",
          "type": "string"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "discount": {
          "default": 0.95,
          "description": "Discount factor",
          "type": "number"
        },
        "n_iter": {
          "default": 100,
          "description": "Fitted-Q iterations",
          "type": "integer"
        },
        "next_state": {
          "description": "Next-state column(s)",
          "type": "string"
        },
        "reward": {
          "description": "reward parameter (str).",
          "type": "string"
        },
        "state": {
          "description": "State column(s)",
          "type": "string"
        }
      },
      "required": [
        "data",
        "state",
        "action",
        "reward",
        "next_state"
      ],
      "type": "object"
    }
  },
  {
    "description": "Fortified proximal causal inference (Yu, Shi & Tchetgen Tchetgen 2025). Adds a bridge-function stability constraint that gives robust ATT under mild misspecification of the outcome/treatment bridge. Validation: validated evidence tier (known-truth, reference, external-parity, or Monte Carlo artifact).",
    "name": "fortified_pci",
    "parameters": {
      "properties": {
        "covariates": {
          "description": "Covariate matrix, DataFrame, or column names.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "proxy_w": {
          "description": "Outcome-side proxies",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "proxy_z": {
          "description": "Treatment-side proxies",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "treat": {
          "description": "Treatment indicator or first-treatment-period column.",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "treat",
        "proxy_z",
        "proxy_w"
      ],
      "type": "object"
    }
  },
  {
    "description": "Bidirectional proximal causal inference (Min, Zhang & Luo 2025). Solves for both outcome and treatment bridges simultaneously in a single two-way regression system. Validation: validated evidence tier (known-truth, reference, external-parity, or Monte Carlo artifact).",
    "name": "bidirectional_pci",
    "parameters": {
      "properties": {
        "covariates": {
          "description": "Covariate matrix, DataFrame, or column names.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "proxy_w": {
          "description": "proxy_w parameter (list).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "proxy_z": {
          "description": "proxy_z parameter (list).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "treat": {
          "description": "Treatment indicator or first-treatment-period column.",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "treat",
        "proxy_z",
        "proxy_w"
      ],
      "type": "object"
    }
  },
  {
    "description": "Proximal causal inference for modified treatment policies (Park & Ying 2025). Estimates the effect of a policy that shifts the treatment distribution (e.g., raises the dose by 10%) under unobserved confounding identified by PCI.",
    "name": "pci_mtp",
    "parameters": {
      "properties": {
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "delta": {
          "description": "Additive shift applied to the treatment under the modified policy",
          "type": "number"
        },
        "proxy_w": {
          "description": "proxy_w parameter (list).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "proxy_z": {
          "description": "proxy_z parameter (list).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "treat": {
          "description": "Treatment indicator or first-treatment-period column.",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "treat",
        "proxy_z",
        "proxy_w",
        "delta"
      ],
      "type": "object"
    }
  },
  {
    "description": "Cluster-randomised trial under cross-cluster interference (Ding et al. 2025). Estimates direct + spillover effects when treatment of one cluster affects outcomes in adjacent clusters. Validation: validated evidence tier (known-truth, reference, external-parity, or Monte Carlo artifact).",
    "name": "cluster_cross_interference",
    "parameters": {
      "properties": {
        "cluster": {
          "description": "Cluster identifier column for clustered standard errors.",
          "type": "string"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "neighbour_treat_share": {
          "description": "Column with neighbours' treatment share",
          "type": "string"
        },
        "treat": {
          "description": "Treatment indicator or first-treatment-period column.",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "cluster",
        "treat",
        "neighbour_treat_share"
      ],
      "type": "object"
    }
  },
  {
    "description": "Beyond-average LATE (Xie-Wu 2025). Identifies the entire treatment-effect distribution among compliers under incomplete compliance, not just its mean. Validation: validated evidence tier (known-truth, reference, external-parity, or Monte Carlo artifact).",
    "name": "beyond_average_late",
    "parameters": {
      "properties": {
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "instrument": {
          "description": "instrument parameter (str).",
          "type": "string"
        },
        "quantiles": {
          "description": "Quantiles tau at which to evaluate QTE (default 0.1..0.9 step 0.1)",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "treat": {
          "description": "Treatment indicator or first-treatment-period column.",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "treat",
        "instrument"
      ],
      "type": "object"
    }
  },
  {
    "description": "Counterfactual-fair conformal prediction for ITE (2025). Wraps standard conformal ITE intervals with a demographic-parity adjustment, giving distribution-free coverage under protected-attribute shifts. Validation: validated evidence tier (known-truth, reference, external-parity, or Monte Carlo artifact).",
    "name": "conformal_fair_ite",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.1,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "covariates": {
          "description": "Covariate matrix, DataFrame, or column names.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "protected": {
          "description": "Protected-attribute column",
          "type": "string"
        },
        "treat": {
          "description": "Treatment indicator or first-treatment-period column.",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "treat",
        "covariates",
        "protected"
      ],
      "type": "object"
    }
  },
  {
    "description": "Synthetic-control experimental design heuristic: picks the k candidate units whose leave-one-out synthetic-control pre-period fit is best (method='loo_sc_fit_ranking'). Not the Abadie-Zhao (arXiv:2108.02196) design, which solves a joint weighting MIQP. Validation: validated evidence tier (known-truth, reference, external-parity, or Monte Carlo artifact).",
    "name": "synth_experimental_design",
    "parameters": {
      "properties": {
        "candidates": {
          "description": "candidates parameter (list).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "concentration_weight": {
          "default": 0.0,
          "description": "concentration_weight parameter (float).",
          "type": "number"
        },
        "data": {
          "description": "Long-format panel",
          "type": "string"
        },
        "donors": {
          "description": "donors parameter (list).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "k": {
          "description": "Number of units to treat",
          "type": "integer"
        },
        "n_random": {
          "default": 500,
          "description": "Number of random.",
          "type": "integer"
        },
        "outcome": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        },
        "penalization": {
          "default": 0.0,
          "description": "penalization parameter (float).",
          "type": "number"
        },
        "risk": {
          "default": "mspe",
          "description": "risk parameter (str).",
          "enum": [
            "mspe",
            "rmse"
          ],
          "type": "string"
        },
        "time": {
          "description": "Time period column.",
          "type": "string"
        },
        "unit": {
          "description": "Unit identifier column.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "unit",
        "time",
        "outcome",
        "k"
      ],
      "type": "object"
    }
  },
  {
    "description": "Kwak-Pleasants (2025) evidence-without-injustice counterfactual fairness test. Freezes admissible-evidence features at their factual values and tests whether predictions still change under do(A=a'). Validation: validated evidence tier (known-truth, reference, external-parity, or Monte Carlo artifact).",
    "name": "evidence_without_injustice",
    "parameters": {
      "properties": {
        "admissible_features": {
          "description": "admissible_features parameter (list).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "alternative_values": {
          "description": "alternative_values parameter (list).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "n_boot": {
          "default": 500,
          "description": "Number of bootstrap replications.",
          "type": "integer"
        },
        "predictor": {
          "description": "predictor parameter (callable).",
          "type": "string"
        },
        "protected": {
          "description": "protected parameter (str).",
          "type": "string"
        },
        "scm_intervention": {
          "description": "scm_intervention parameter (callable).",
          "type": "string"
        },
        "threshold": {
          "default": 0.05,
          "description": "threshold parameter (float).",
          "type": "number"
        }
      },
      "required": [
        "data",
        "predictor",
        "protected",
        "admissible_features",
        "scm_intervention"
      ],
      "type": "object"
    }
  },
  {
    "description": "Bayesian Causal Forest for ordered / dose-level treatment (Zorzetto et al. 2026). Estimates cumulative dose-response curves via chained BCF between consecutive levels.",
    "name": "bcf_ordinal",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "baseline": {
          "description": "baseline parameter (str).",
          "type": "string"
        },
        "covariates": {
          "description": "Covariate matrix, DataFrame, or column names.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "n_bootstrap": {
          "default": 100,
          "description": "Number of bootstrap replications.",
          "type": "integer"
        },
        "n_folds": {
          "default": 5,
          "description": "Number of cross-fitting or cross-validation folds.",
          "type": "integer"
        },
        "n_trees_mu": {
          "default": 200,
          "description": "Number of trees mu.",
          "type": "integer"
        },
        "n_trees_tau": {
          "default": 50,
          "description": "Number of trees tau.",
          "type": "integer"
        },
        "random_state": {
          "default": 42,
          "description": "Random seed or RandomState for reproducible stochastic steps.",
          "type": "integer"
        },
        "treat": {
          "description": "Treatment indicator or first-treatment-period column.",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "treat",
        "covariates"
      ],
      "type": "object"
    }
  },
  {
    "description": "BCF on PCA-factor scores of a high-dimensional exposure vector (arXiv:2601.16595, 2026). Compresses exposures via SVD or user-supplied loadings, then fits one BCF per factor. Validation: validated evidence tier (known-truth, reference, external-parity, or Monte Carlo artifact).",
    "name": "bcf_factor_exposure",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "binarize": {
          "default": "median",
          "description": "binarize parameter (str).",
          "enum": [
            "median",
            "zero",
            "none"
          ],
          "type": "string"
        },
        "covariates": {
          "description": "Covariate matrix, DataFrame, or column names.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "exposures": {
          "description": "exposures parameter (list).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "loadings": {
          "description": "loadings parameter (DataFrame).",
          "type": "string"
        },
        "n_bootstrap": {
          "default": 100,
          "description": "Number of bootstrap replications.",
          "type": "integer"
        },
        "n_factors": {
          "default": 3,
          "description": "Number of factors.",
          "type": "integer"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "exposures",
        "covariates"
      ],
      "type": "object"
    }
  },
  {
    "description": "Multi-agent LLM causal discovery (arXiv:2509.00987, 2025). Runs proposer / critic / domain-expert / synthesiser agents over several rounds, returns per-edge confidence + audit log.",
    "name": "causal_mas",
    "parameters": {
      "properties": {
        "client": {
          "description": "LLM chat client",
          "type": "string"
        },
        "confounders": {
          "description": "confounders parameter (list).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "domain": {
          "default": "",
          "description": "domain parameter (str).",
          "type": "string"
        },
        "final_threshold": {
          "default": 0.5,
          "description": "final_threshold parameter (float).",
          "type": "number"
        },
        "instruments": {
          "description": "instruments parameter (list).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "outcome": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        },
        "rounds": {
          "default": 3,
          "description": "rounds parameter (int).",
          "type": "integer"
        },
        "treatment": {
          "description": "Treatment indicator, treatment variable, or treatment array.",
          "type": "string"
        },
        "variables": {
          "description": "variables parameter (list).",
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "required": [
        "variables"
      ],
      "type": "object"
    }
  },
  {
    "description": "End-to-end 'data + question -> publication draft' pipeline. Parses a natural-language question, runs sp.causal() (diagnose + recommend + estimate + robustness), and assembles a Markdown / LaTeX / Word draft with EDA, identification verdict, estimator rationale, results, and robustness sections.",
    "name": "paper",
    "parameters": {
      "properties": {
        "cite": {
          "default": true,
          "description": "cite parameter (bool).",
          "type": "boolean"
        },
        "cluster": {
          "description": "Cluster identifier column for clustered standard errors.",
          "type": "string"
        },
        "cohort": {
          "description": "Treatment cohort or group identifier.",
          "type": "string"
        },
        "covariates": {
          "description": "Covariate matrix, DataFrame, or column names.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "cutoff": {
          "description": "cutoff parameter (float).",
          "type": "number"
        },
        "dag": {
          "description": "dag parameter (DAG).",
          "type": "string"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "design": {
          "description": "design parameter (str).",
          "enum": [
            "did",
            "rd",
            "iv",
            "rct",
            "observational",
            "synth"
          ],
          "type": "string"
        },
        "fmt": {
          "default": "markdown",
          "description": "fmt parameter (str).",
          "enum": [
            "markdown",
            "tex",
            "docx",
            "qmd"
          ],
          "type": "string"
        },
        "id": {
          "description": "Unit, subject, or panel identifier column.",
          "type": "string"
        },
        "include_eda": {
          "default": true,
          "description": "Whether to include eda.",
          "type": "boolean"
        },
        "include_robustness": {
          "default": true,
          "description": "Whether to include robustness.",
          "type": "boolean"
        },
        "instrument": {
          "description": "instrument parameter (str).",
          "type": "string"
        },
        "output_path": {
          "description": "Filesystem path for output.",
          "type": "string"
        },
        "question": {
          "description": "Natural-language causal question",
          "type": "string"
        },
        "running_var": {
          "description": "running_var parameter (str).",
          "type": "string"
        },
        "strict": {
          "default": false,
          "description": "strict parameter (bool).",
          "type": "boolean"
        },
        "time": {
          "description": "Time period column.",
          "type": "string"
        },
        "treatment": {
          "description": "Treatment indicator, treatment variable, or treatment array.",
          "type": "string"
        },
        "y": {
          "description": "Outcome column (overrides parser)",
          "type": "string"
        }
      },
      "required": [
        "data",
        "question"
      ],
      "type": "object"
    }
  },
  {
    "description": "Package an analysis (PaperDraft / fitted result / list of results) into a replication zip: data CSV + schema manifest, caller code, frozen environment, rendered paper (md/qmd/tex/docx), citations, and an aggregated lineage.json from any results carrying Provenance. The archive's MANIFEST.json records SHA-256 for every file plus the git SHA when available.",
    "name": "replication_pack",
    "parameters": {
      "properties": {
        "bib": {
          "default": true,
          "description": "Write paper/paper.bib from citations",
          "type": "boolean"
        },
        "code": {
          "description": "Inline script or path to .py file",
          "type": "string"
        },
        "data": {
          "description": "Explicit data; falls back to target.data / target.workflow.data",
          "type": "string"
        },
        "env": {
          "default": true,
          "description": "Capture pip freeze of the runtime",
          "type": "boolean"
        },
        "extra_files": {
          "description": "extra_files parameter (dict).",
          "type": "object"
        },
        "include_git_sha": {
          "default": true,
          "description": "Whether to include git sha.",
          "type": "boolean"
        },
        "output_path": {
          "description": "Destination .zip path",
          "type": "string"
        },
        "overwrite": {
          "default": true,
          "description": "overwrite parameter (bool).",
          "type": "boolean"
        },
        "paper_format": {
          "default": "auto",
          "description": "paper_format parameter (str).",
          "enum": [
            "auto",
            "md",
            "qmd",
            "tex",
            "docx"
          ],
          "type": "string"
        },
        "target": {
          "description": "PaperDraft, fitted result, list of results, or None for a data-only pack",
          "type": "string"
        },
        "title": {
          "default": "Replication Pack",
          "description": "title parameter (str).",
          "type": "string"
        }
      },
      "required": [
        "target",
        "output_path"
      ],
      "type": "object"
    }
  },
  {
    "description": "[experimental] Veitch-Wang-Blei (2020) text-as-treatment ATE estimation. Embeds a text column into n_components features (default hash embedder, deterministic) and uses them as confounder adjustment in OLS with HC1 SEs. Validation status: experimental. Known limitations: embedder='sbert' requires the optional sentence-transformers extra; the bundled 'hash' embedder is a deterministic fallback, not a published parity reference; Veitch et al. (2020) full BERT/topic-model recipe is not yet implemented -- see module docstring.",
    "name": "text_treatment_effect",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "covariates": {
          "description": "Covariate matrix, DataFrame, or column names.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "embedder": {
          "default": "hash",
          "description": "Text embedding backend or custom embedding callable.",
          "enum": [
            "hash",
            "sbert"
          ],
          "type": "string"
        },
        "n_components": {
          "default": 20,
          "description": "Number of generated components or embedding dimensions.",
          "type": "integer"
        },
        "outcome": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        },
        "seed": {
          "default": 0,
          "description": "Random seed for reproducible stochastic steps.",
          "type": "integer"
        },
        "text_col": {
          "description": "Text column used by causal-text estimators.",
          "type": "string"
        },
        "treatment": {
          "description": "Treatment indicator, treatment variable, or treatment array.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "text_col",
        "outcome",
        "treatment"
      ],
      "type": "object"
    }
  },
  {
    "description": "[experimental] Measurement-error correction for downstream OLS coefficients when a regressor was produced by an LLM (or any imperfect annotator). Binary T uses Hausman (1998) 1/(1 - p_01 - p_10) inflation; multi-class T (K>=3) uses the inverse-confusion-matrix transform built from the validation-set Bayes posterior (Egami et al. 2023); continuous scores (e.g. LLM sentiment) use regression calibration on the human-audited subsample (Fuller 1987; Carroll et al. 2006) -- the classical reliability-ratio correction when no covariates are present. Optional bias-corrected bootstrap jointly resamples the validation set and the unlabeled corpus for honest CIs. SE inflation factor (delta-method) always reported in diagnostics. Validation status: experimental. Known limitations: discrete corrections cover Hausman (binary) and the inverse-confusion-matrix transform (K>=3); the logistic and Bayesian variants from Egami et al. (2023) are not yet implemented; continuous correction assumes non-differential (classical) measurement error; differential measurement error is not supported.",
    "name": "llm_annotator_correct",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "annotations_human": {
          "description": "NaN where unavailable; >=30 valid rows",
          "type": "string"
        },
        "annotations_llm": {
          "description": "Binary / K-class numeric labels, or a continuous score",
          "type": "string"
        },
        "bootstrap": {
          "default": false,
          "description": "Joint resample full sample (validation rows + unlabeled rows) for bias-corrected percentile CIs reflecting validation-set noise",
          "type": "boolean"
        },
        "bootstrap_seed": {
          "description": "NumPy default_rng seed",
          "type": "integer"
        },
        "covariates": {
          "description": "Covariate matrix, DataFrame, or column names.",
          "type": "string"
        },
        "method": {
          "default": "auto",
          "description": "'auto' routes discrete labels to Hausman / confusion-matrix and continuous scores to regression calibration",
          "enum": [
            "auto",
            "hausman",
            "reliability"
          ],
          "type": "string"
        },
        "n_bootstrap": {
          "default": 500,
          "description": "Bootstrap replicates (>=50)",
          "type": "integer"
        },
        "outcome": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "annotations_llm",
        "outcome",
        "annotations_human"
      ],
      "type": "object"
    }
  },
  {
    "description": "Closed-loop LLM-assisted DAG discovery: iterate LLM-propose -> constrained PC -> CI-test validate -> demote, until edge set converges or max_iter is hit. Returns a final DAG with per-edge LLM confidence and CI-test p-value.",
    "name": "llm_dag_constrained",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "ci_test": {
          "default": "fisherz",
          "description": "ci_test parameter (str).",
          "enum": [
            "fisherz"
          ],
          "type": "string"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "descriptions": {
          "description": "Variable -> human description",
          "type": "object"
        },
        "forbid_low_conf": {
          "default": false,
          "description": "forbid_low_conf parameter (bool).",
          "type": "boolean"
        },
        "high_conf_threshold": {
          "default": 0.7,
          "description": "high_conf_threshold parameter (float).",
          "type": "number"
        },
        "low_conf_threshold": {
          "default": 0.3,
          "description": "low_conf_threshold parameter (float).",
          "type": "number"
        },
        "max_iter": {
          "default": 3,
          "description": "Maximum number of optimisation iterations.",
          "type": "integer"
        },
        "oracle": {
          "description": "LLM oracle f(vars, desc)->[(a,b[,conf])]",
          "type": "string"
        },
        "variables": {
          "description": "Subset of columns to include",
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "required": [
        "data"
      ],
      "type": "object"
    }
  },
  {
    "description": "Per-edge CI-test validation of a declared DAG. For each directed edge a->b, run partial-correlation independence test conditioning on parents(b)\\{a}. Edges with p>alpha are flagged unsupported.",
    "name": "llm_dag_validate",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "ci_test": {
          "default": "fisherz",
          "description": "ci_test parameter (str).",
          "enum": [
            "fisherz"
          ],
          "type": "string"
        },
        "dag": {
          "description": "dag parameter (DAG).",
          "type": "string"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        }
      },
      "required": [
        "dag",
        "data"
      ],
      "type": "object"
    }
  },
  {
    "description": "Long-difference shift-share (Bartik) IV for political science (Park 2026): HC1 SE as `se`, with AKM / AKM0 shock-level SE, CI and p (ShiftShareSE::ivreg_ss) in diagnostics, Rotemberg top-K weights and share-balance F-tests. Validation: certified parity evidence.",
    "name": "shift_share_political",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "covariates": {
          "description": "Covariate matrix, DataFrame, or column names.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "endog": {
          "description": "endog parameter (str).",
          "type": "string"
        },
        "leave_one_out": {
          "default": true,
          "description": "leave_one_out parameter (bool).",
          "type": "boolean"
        },
        "outcome": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        },
        "shares": {
          "description": "shares parameter (DataFrame).",
          "type": "string"
        },
        "shocks": {
          "description": "shocks parameter (Series).",
          "type": "string"
        },
        "time": {
          "description": "Time period column.",
          "type": "string"
        },
        "unit": {
          "description": "Unit identifier column.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "unit",
        "time",
        "outcome",
        "endog",
        "shares",
        "shocks"
      ],
      "type": "object"
    }
  },
  {
    "description": "Closed-form Kalman filter over a stream of causal-effect estimates + SEs. Produces a running posterior over the time-varying (or static) causal effect. Validation: validated evidence tier (known-truth, reference, external-parity, or Monte Carlo artifact).",
    "name": "causal_kalman",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "estimates": {
          "description": "estimates parameter (list).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "prior_mean": {
          "default": 0.0,
          "description": "prior_mean parameter (float).",
          "type": "number"
        },
        "prior_var": {
          "default": 1.0,
          "description": "prior_var parameter (float).",
          "type": "number"
        },
        "process_var": {
          "default": 0.0,
          "description": "process_var parameter (float).",
          "type": "number"
        },
        "standard_errors": {
          "description": "standard_errors parameter (list).",
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "required": [
        "estimates",
        "standard_errors"
      ],
      "type": "object"
    }
  },
  {
    "description": "End-to-end Assimilative Causal Inference pipeline (Nature Communications 2026): for each data batch, apply `estimator` to get (theta, SE), then fuse via Kalman filtering or particle filter. Validation: validated evidence tier (known-truth, reference, external-parity, or Monte Carlo artifact).",
    "name": "assimilative_causal",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "backend": {
          "default": "kalman",
          "description": "backend parameter (str).",
          "enum": [
            "kalman",
            "particle"
          ],
          "type": "string"
        },
        "batches": {
          "description": "batches parameter (list).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "estimator": {
          "description": "Maps a batch to (theta_hat, se)",
          "type": "string"
        },
        "prior_mean": {
          "default": 0.0,
          "description": "prior_mean parameter (float).",
          "type": "number"
        },
        "prior_var": {
          "default": 1.0,
          "description": "prior_var parameter (float).",
          "type": "number"
        },
        "process_var": {
          "default": 0.0,
          "description": "process_var parameter (float).",
          "type": "number"
        }
      },
      "required": [
        "batches",
        "estimator"
      ],
      "type": "object"
    }
  },
  {
    "description": "Multi-period panel shift-share IV: pooled 2SLS with unit/time/two-way FEs over a time-varying Bartik instrument. Reports per-period event-study, aggregate Rotemberg top-K, and share-balance F-tests. Validation: certified parity evidence.",
    "name": "shift_share_political_panel",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "cluster": {
          "default": "unit",
          "description": "Cluster identifier column for clustered standard errors.",
          "enum": [
            "unit",
            "time",
            "twoway"
          ],
          "type": "string"
        },
        "covariates": {
          "description": "Covariate matrix, DataFrame, or column names.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "endog": {
          "description": "endog parameter (str).",
          "type": "string"
        },
        "fe": {
          "default": "two-way",
          "description": "fe parameter (str).",
          "enum": [
            "two-way",
            "unit",
            "time",
            "none"
          ],
          "type": "string"
        },
        "outcome": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        },
        "shares": {
          "description": "shares parameter (DataFrame).",
          "type": "string"
        },
        "shocks": {
          "description": "shocks parameter (DataFrame).",
          "type": "string"
        },
        "time": {
          "description": "Time period column.",
          "type": "string"
        },
        "unit": {
          "description": "Unit identifier column.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "unit",
        "time",
        "outcome",
        "endog",
        "shares",
        "shocks"
      ],
      "type": "object"
    }
  },
  {
    "description": "Bootstrap SIR particle filter for non-Gaussian assimilative causal inference. Supports arbitrary prior sampler, transition sampler, and observation log-pdf, with systematic resampling triggered by an ESS threshold. Validation: validated evidence tier (known-truth, reference, external-parity, or Monte Carlo artifact).",
    "name": "particle_filter",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "ess_resample_threshold": {
          "default": 0.5,
          "description": "ess_resample_threshold parameter (float).",
          "type": "number"
        },
        "estimates": {
          "description": "estimates parameter (list).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "n_particles": {
          "default": 2000,
          "description": "Number of particles.",
          "type": "integer"
        },
        "prior_mean": {
          "default": 0.0,
          "description": "prior_mean parameter (float).",
          "type": "number"
        },
        "prior_var": {
          "default": 1.0,
          "description": "prior_var parameter (float).",
          "type": "number"
        },
        "process_sd": {
          "default": 0.0,
          "description": "process_sd parameter (float).",
          "type": "number"
        },
        "standard_errors": {
          "description": "standard_errors parameter (list).",
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "required": [
        "estimates",
        "standard_errors"
      ],
      "type": "object"
    }
  },
  {
    "description": "Construct an OpenAI-compatible LLM client for use with sp.causal_llm.causal_mas. Requires the optional openai>=1.0 extra. Supports custom base_url for Azure / vLLM / Ollama.",
    "name": "openai_client",
    "parameters": {
      "properties": {
        "api_key": {
          "description": "api_key parameter (str).",
          "type": "string"
        },
        "base_url": {
          "description": "base_url parameter (str).",
          "type": "string"
        },
        "max_retries": {
          "default": 3,
          "description": "max_retries parameter (int).",
          "type": "integer"
        },
        "max_tokens": {
          "default": 1024,
          "description": "max_tokens parameter (int).",
          "type": "integer"
        },
        "model": {
          "default": "gpt-4o-mini",
          "description": "Model variant or parameterisation to fit.",
          "type": "string"
        },
        "organization": {
          "description": "organization parameter (str).",
          "type": "string"
        },
        "temperature": {
          "default": 0.0,
          "description": "temperature parameter (float).",
          "type": "number"
        }
      },
      "required": [],
      "type": "object"
    }
  },
  {
    "description": "Construct an Anthropic-compatible LLM client for use with sp.causal_llm.causal_mas. Requires the optional anthropic>=0.30 extra. Defaults to Claude Opus 4.7.",
    "name": "anthropic_client",
    "parameters": {
      "properties": {
        "api_key": {
          "description": "api_key parameter (str).",
          "type": "string"
        },
        "base_url": {
          "description": "base_url parameter (str).",
          "type": "string"
        },
        "max_retries": {
          "default": 3,
          "description": "max_retries parameter (int).",
          "type": "integer"
        },
        "max_tokens": {
          "default": 1024,
          "description": "max_tokens parameter (int).",
          "type": "integer"
        },
        "model": {
          "default": "claude-opus-4-7",
          "description": "Model variant or parameterisation to fit.",
          "type": "string"
        },
        "temperature": {
          "default": 0.0,
          "description": "temperature parameter (float).",
          "type": "number"
        }
      },
      "required": [],
      "type": "object"
    }
  },
  {
    "description": "Deterministic scripted-response LLM client for testing sp.causal_llm.causal_mas without network access.",
    "name": "echo_client",
    "parameters": {
      "properties": {
        "response_fn": {
          "description": "Maps (role, prompt) -> str",
          "type": "string"
        }
      },
      "required": [
        "response_fn"
      ],
      "type": "object"
    }
  },
  {
    "description": "Unified Mendelian Randomization dispatcher. method= selects the estimator: 'ivw' / 'egger' / 'median' / 'penalized_median' / 'mode' / 'all' (runs IVW+Egger+Median together) / 'mvmr' / 'mediation' / 'bma' (multi-exposure) / 'presso' / 'radial' / 'leave_one_out' / 'steiger' / 'heterogeneity' / 'pleiotropy_egger' / 'f_statistic' (diagnostics). Kwargs are passed through to the target function unchanged; see sp.mendelian_family guide. Validation: certified parity evidence.",
    "name": "mr",
    "parameters": {
      "properties": {
        "method": {
          "default": "ivw",
          "description": "MR estimator / diagnostic -- call sp.mr_available_methods() for the full list.",
          "type": "string"
        }
      },
      "required": [],
      "type": "object"
    }
  },
  {
    "description": "Unified conformal causal inference dispatcher. kind= selects the estimator: 'cate' / 'counterfactual' / 'ite' (Lei-Candes 2021 base) / 'weighted' (TBCR 2019 primitive) / 'density' / 'multidp' / 'debiased' / 'fair' (2025-2026 frontier) / 'continuous' (dose-response) / 'interference' (cluster-exchangeable). Kwargs pass through to the target function; see sp.conformal_family guide.",
    "name": "conformal",
    "parameters": {
      "properties": {
        "kind": {
          "default": "cate",
          "description": "Conformal estimator -- call sp.conformal_available_kinds() for the full list.",
          "type": "string"
        }
      },
      "required": [],
      "type": "object"
    }
  },
  {
    "description": "Unified interference / spillover dispatcher. design= selects the estimator: 'partial' (Hudgens-Halloran cluster) / 'network_exposure' (Aronow-Samii HT) / 'peer_effects' (Manski / Bramoulle linear-in-means) / 'network_hte' (Wu & Yuan 2025 orthogonal, arXiv:2509.18484) / 'inward_outward' (directed network; Fang, Airoldi & Forastiere 2025, arXiv:2506.06615) / 'cluster_matched_pair' (Bai 2022) / 'cluster_cross' (Ding et al. 2025) / 'cluster_staggered' (Zhou et al. 2025) / 'dnc_gnn' (Zhao et al. 2026). Kwargs pass through to the target function; see sp.interference_family guide. Validation: validated evidence tier (known-truth, reference, external-parity, or Monte Carlo artifact).",
    "name": "interference",
    "parameters": {
      "properties": {
        "design": {
          "default": "partial",
          "description": "Interference design -- call sp.interference_available_designs() for the full list.",
          "type": "string"
        }
      },
      "required": [],
      "type": "object"
    }
  },
  {
    "description": "Quantile Difference-in-Differences (QDiD): applies the DiD contrast to quantiles, [Q11(t)-Q10(t)] - [Q01(t)-Q00(t)], on a 2x2 design with bootstrap SE. This is NOT changes-in-changes -- Athey & Imbens (2006) propose CiC and explicitly criticise QDiD; use sp.cic for CiC. Validation: certified parity evidence.",
    "name": "qdid",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "group": {
          "description": "Binary treated / control group",
          "type": "string"
        },
        "n_boot": {
          "default": 500,
          "description": "Number of bootstrap replications.",
          "type": "integer"
        },
        "quantiles": {
          "description": "Quantiles to estimate, defaults to [0.1, ..., 0.9]",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "time": {
          "description": "Binary pre / post indicator",
          "type": "string"
        },
        "y": {
          "description": "Outcome",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "group",
        "time"
      ],
      "type": "object"
    }
  },
  {
    "description": "Callaway & Li (2019) quantile treatment effect on the treated for panel data. Recovers the counterfactual DISTRIBUTION of untreated outcomes for the treated group via distributional DiD plus a copula-stability assumption. Needs a balanced THREE-period panel (the third period identifies the copula). Exact parity with R qte::panel.qtet (6.8e-12). Validation: certified parity evidence.",
    "name": "panel_qtet",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "data": {
          "description": "Long panel",
          "type": "string"
        },
        "n_boot": {
          "default": 200,
          "description": "Number of bootstrap replications.",
          "type": "integer"
        },
        "quantiles": {
          "description": "quantiles parameter (list).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "se": {
          "default": "bootstrap",
          "description": "SE method",
          "enum": [
            "bootstrap",
            "none"
          ],
          "type": "string"
        },
        "seed": {
          "default": 0,
          "description": "Random seed for reproducible stochastic steps.",
          "type": "integer"
        },
        "t": {
          "description": "Post-period VALUE of time",
          "type": "string"
        },
        "time": {
          "description": "Period",
          "type": "string"
        },
        "tmin1": {
          "description": "Pre-period VALUE of time",
          "type": "string"
        },
        "tmin2": {
          "description": "Pre-pre-period VALUE; identifies the copula",
          "type": "string"
        },
        "treat": {
          "description": "Binary treatment",
          "type": "string"
        },
        "unit": {
          "description": "Unit id",
          "type": "string"
        },
        "y": {
          "description": "Outcome",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "treat",
        "unit",
        "time",
        "t",
        "tmin1",
        "tmin2"
      ],
      "type": "object"
    }
  },
  {
    "description": "Quantile treatment effects. 'firpo_qte' / 'firpo_qtt' give Firpo (2007) efficient UNCONDITIONAL QTE / QTT by propensity reweighting (analytic influence-function SE); 'conditional_qr' gives the CONDITIONAL QTE (coefficient on D in a quantile regression, Koenker & Bassett 1978); 'distribution' gives the QTT via an IPW counterfactual distribution. Validation: certified parity evidence.",
    "name": "qte",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "controls": {
          "description": "Control-variable column names.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "method": {
          "default": "firpo_qte",
          "description": "Estimand / estimator",
          "enum": [
            "firpo_qte",
            "firpo_qtt",
            "conditional_qr",
            "distribution"
          ],
          "type": "string"
        },
        "n_boot": {
          "default": 500,
          "description": "Number of bootstrap replications.",
          "type": "integer"
        },
        "quantiles": {
          "description": "quantiles parameter (list).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "treatment": {
          "description": "Treatment indicator, treatment variable, or treatment array.",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "treatment"
      ],
      "type": "object"
    }
  },
  {
    "description": "Dose-response function for a continuous treatment under unconfoundedness. Uses generalised propensity-score weighting or double ML for the conditional expectation E[Y(d)]. Validation: certified parity evidence.",
    "name": "dose_response",
    "parameters": {
      "properties": {
        "covariates": {
          "description": "Covariate matrix, DataFrame, or column names.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "dose_range": {
          "description": "(lo, hi) over which to evaluate dose-response",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "n_bootstrap": {
          "default": 200,
          "description": "Number of bootstrap replications.",
          "type": "integer"
        },
        "n_dose_points": {
          "default": 20,
          "description": "Number of dose points.",
          "type": "integer"
        },
        "treat": {
          "description": "Continuous treatment / dose",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "treat",
        "covariates"
      ],
      "type": "object"
    }
  },
  {
    "description": "Direct + spillover treatment effect estimation under partial interference (within-cluster). Uses the Hudgens-Halloran decomposition with chosen exposure function. Validation: validated evidence tier (known-truth, reference, external-parity, or Monte Carlo artifact).",
    "name": "spillover",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "cluster": {
          "description": "Cluster column (interference boundary)",
          "type": "string"
        },
        "covariates": {
          "description": "Covariate matrix, DataFrame, or column names.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "exposure_fn": {
          "default": "fraction",
          "description": "Exposure function",
          "enum": [
            "fraction",
            "any",
            "count"
          ],
          "type": "string"
        },
        "n_bootstrap": {
          "default": 500,
          "description": "Number of bootstrap replications.",
          "type": "integer"
        },
        "treat": {
          "description": "Treatment indicator or first-treatment-period column.",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "treat",
        "cluster"
      ],
      "type": "object"
    }
  },
  {
    "description": "Construct a network Graph from an adjacency matrix or an edge list (directed/undirected, weighted). The single entry point feeding every sp.network analysis. Validation: validated evidence tier (known-truth, reference, external-parity, or Monte Carlo artifact).",
    "name": "network_graph",
    "parameters": {
      "properties": {
        "adjacency": {
          "description": "Square adjacency (or scipy.sparse / W)",
          "type": "string"
        },
        "directed": {
          "default": false,
          "description": "directed parameter (bool).",
          "type": "boolean"
        },
        "edges": {
          "description": "Edge list of (u, v) pairs",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "node_labels": {
          "description": "node_labels parameter (list).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "weights": {
          "description": "Observation weights.",
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "required": [],
      "type": "object"
    }
  },
  {
    "description": "Structural summary of a network: density, components, diameter, average path length, transitivity (global clustering), reciprocity, and Newman degree assortativity. Validation: certified parity evidence.",
    "name": "network_summary",
    "parameters": {
      "properties": {
        "graph": {
          "description": "graph parameter (Graph).",
          "type": "string"
        }
      },
      "required": [
        "graph"
      ],
      "type": "object"
    }
  },
  {
    "description": "Centrality dispatcher: degree, closeness, betweenness (Brandes), eigenvector, Katz, PageRank, Bonacich power. Returns a per-node score table. Validation: certified parity evidence.",
    "name": "centrality",
    "parameters": {
      "properties": {
        "graph": {
          "description": "graph parameter (Graph).",
          "type": "string"
        },
        "kind": {
          "default": "all",
          "description": "Measure(s) to compute",
          "enum": [
            "all",
            "degree",
            "closeness",
            "betweenness",
            "eigenvector",
            "katz",
            "pagerank",
            "bonacich"
          ],
          "type": "string"
        },
        "normalized": {
          "default": true,
          "description": "normalized parameter (bool).",
          "type": "boolean"
        }
      },
      "required": [
        "graph"
      ],
      "type": "object"
    }
  },
  {
    "description": "Partition a network into communities by modularity optimisation: Louvain (Blondel 2008), greedy/CNM (Clauset-Newman-Moore 2004), or label propagation. Validation: certified parity evidence.",
    "name": "community_detection",
    "parameters": {
      "properties": {
        "graph": {
          "description": "graph parameter (Graph).",
          "type": "string"
        },
        "method": {
          "default": "louvain",
          "description": "Detection algorithm",
          "enum": [
            "louvain",
            "greedy",
            "label_prop"
          ],
          "type": "string"
        },
        "resolution": {
          "default": 1.0,
          "description": "resolution parameter (float).",
          "type": "number"
        },
        "seed": {
          "description": "Random seed for reproducible stochastic steps.",
          "type": "integer"
        }
      },
      "required": [
        "graph"
      ],
      "type": "object"
    }
  },
  {
    "description": "MRQAP linear network regression of one relational matrix on others, with permutation inference (Dekker double-semi-partialling) robust to network autocorrelation. sna::netlm. Validation: certified parity evidence.",
    "name": "netlm",
    "parameters": {
      "properties": {
        "directed": {
          "description": "directed parameter (bool).",
          "type": "boolean"
        },
        "method": {
          "default": "dsp",
          "description": "Permutation scheme",
          "enum": [
            "dsp",
            "y"
          ],
          "type": "string"
        },
        "nperm": {
          "default": 1000,
          "description": "nperm parameter (int).",
          "type": "integer"
        },
        "predictors": {
          "description": "Predictor matrix / list / {name: matrix}",
          "type": "string"
        },
        "seed": {
          "description": "Random seed for reproducible stochastic steps.",
          "type": "integer"
        },
        "y": {
          "description": "Dependent network matrix",
          "type": "string"
        }
      },
      "required": [
        "y",
        "predictors"
      ],
      "type": "object"
    }
  },
  {
    "description": "QAP logistic network regression for a binary dependent network, with dependent-matrix-permutation inference. sna::netlogit analogue. Validation: certified parity evidence.",
    "name": "netlogit",
    "parameters": {
      "properties": {
        "directed": {
          "description": "directed parameter (bool).",
          "type": "boolean"
        },
        "nperm": {
          "default": 1000,
          "description": "nperm parameter (int).",
          "type": "integer"
        },
        "predictors": {
          "description": "predictors parameter (ndarray).",
          "type": "string"
        },
        "seed": {
          "description": "Random seed for reproducible stochastic steps.",
          "type": "integer"
        },
        "y": {
          "description": "Binary dependent network",
          "type": "string"
        }
      },
      "required": [
        "y",
        "predictors"
      ],
      "type": "object"
    }
  },
  {
    "description": "OLS on dyadic data with Aronow-Samii-Assenova dyadic-cluster-robust standard errors that allow arbitrary dependence between dyads sharing a node (Fafchamps-Gubert). Validation: certified parity evidence.",
    "name": "dyadic_regression",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "covariates": {
          "description": "Covariate matrix, DataFrame, or column names.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "i": {
          "description": "First-node id column",
          "type": "string"
        },
        "j": {
          "description": "Second-node id column",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "covariates",
        "i",
        "j"
      ],
      "type": "object"
    }
  },
  {
    "description": "Exponential random graph model (ERGM) fit by maximum pseudo-likelihood (MPLE). Terms: edges, mutual, triangles, nodematch/nodecov/absdiff. MPLE=MLE for dyad-independent models; MCMC-MLE is the roadmap. Validation: certified parity evidence.",
    "name": "ergm",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "directed": {
          "description": "directed parameter (bool).",
          "type": "boolean"
        },
        "graph": {
          "description": "graph parameter (Graph).",
          "type": "string"
        },
        "node_attrs": {
          "description": "node_attrs parameter (DataFrame).",
          "type": "string"
        },
        "terms": {
          "default": [
            "edges"
          ],
          "description": "ERGM terms, e.g. ['edges','nodematch:gender']",
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "required": [
        "graph"
      ],
      "type": "object"
    }
  },
  {
    "description": "Effects of multi-valued (3+ level) treatments via AIPW. Returns pairwise contrasts versus a reference level. Validation: certified parity evidence.",
    "name": "multi_treatment",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "covariates": {
          "description": "Covariate matrix, DataFrame, or column names.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "n_bootstrap": {
          "default": 500,
          "description": "Number of bootstrap replications.",
          "type": "integer"
        },
        "outcome_model": {
          "default": "gbm",
          "description": "Per-arm outcome regression ``mu_k(X)``. ``'gbm'`` is a gradient boosting regressor (100 trees, depth 3); ``'linear'`` is OLS with an intercept fitted separately in each arm, which is the outcome model of Stata's ``teffects aipw`` (``linear by ML``).",
          "enum": [
            "gbm",
            "linear"
          ],
          "type": "string"
        },
        "reference": {
          "description": "Reference treatment level (defaults to 0 / smallest)",
          "type": "integer"
        },
        "se_method": {
          "default": "bootstrap",
          "description": "``'bootstrap'`` re-fits the whole estimator (same outcome model) on ``n_bootstrap`` resamples. ``'influence'`` reports ``sd(phi_k - phi_ref) / sqrt(n)`` from the AIPW influence function with the nuisance fits treated as known.",
          "enum": [
            "bootstrap",
            "influence",
            "sandwich"
          ],
          "type": "string"
        },
        "treat": {
          "description": "Multi-valued treatment (int)",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "treat",
        "covariates"
      ],
      "type": "object"
    }
  },
  {
    "description": "Aronow-Samii Horvitz-Thompson estimator for arbitrary interference via a user-supplied exposure mapping. Handles Bernoulli randomisation designs with simulated conservative variance. Validation: validated evidence tier (known-truth, reference, external-parity, or Monte Carlo artifact). Known limitations: design='complete' is reserved but not implemented; passing it raises NotImplementedError. Use design='bernoulli' with p_treat=K/N as an approximation only if that matches the assignment mechanism you are willing to assume.",
    "name": "network_exposure",
    "parameters": {
      "properties": {
        "Y": {
          "description": "Outcome vector",
          "type": "string"
        },
        "Z": {
          "description": "Treatment vector (0/1)",
          "type": "string"
        },
        "adjacency": {
          "description": "Adjacency matrix (n x n) or sparse",
          "type": "string"
        },
        "design": {
          "default": "bernoulli",
          "description": "Randomisation design",
          "enum": [
            "bernoulli",
            "complete"
          ],
          "type": "string"
        },
        "mapping": {
          "default": "as4",
          "description": "Exposure mapping",
          "enum": [
            "as4",
            "as3",
            "as2",
            "custom"
          ],
          "type": "string"
        },
        "n_sim": {
          "default": 2000,
          "description": "Number of sim.",
          "type": "integer"
        },
        "p_treat": {
          "description": "Marginal treatment probability",
          "type": "number"
        }
      },
      "required": [
        "Y",
        "Z",
        "adjacency"
      ],
      "type": "object"
    }
  },
  {
    "description": "DiD with continuous treatment intensity. Four modes: (i) 'twfe' TWFE with dosexpost interaction; (ii) 'att_gt' dose-quantile group-time ATT versus the untreated (dose=0) arm with bootstrap SE (heuristic); (iii) 'dose_response' local-linear regression of DeltaY=Y_post-Y_pre on baseline dose; (iv) 'cgs' Callaway-Goodman-Bacon-Sant'Anna (2024) ATT(d|g,t) MVP -- 2-period design, OR only, bootstrap SE, [pending verification] markers on paper formulas. Full CGS parity (cohort aggregation, DR/IPW, analytical IF variance) is on the roadmap -- see docs/rfc/continuous_did_cgs.md. Validation: certified evidence with scoped limitations. Known limitations: method='cgs' is an MVP -- 2-period design, OR only, bootstrap SE; full CGS parity (cohort aggregation, DR/IPW, analytical IF variance) is on the roadmap (see docs/rfc/continuous_did_cgs.md). Other modes (twfe / att_gt / dose_response) are stable.",
    "name": "continuous_did",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "cluster": {
          "description": "Cluster variable for SE (TWFE mode)",
          "type": "string"
        },
        "controls": {
          "description": "Control variables",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "dose": {
          "description": "Continuous treatment / dose variable",
          "type": "string"
        },
        "id": {
          "description": "Unit identifier",
          "type": "string"
        },
        "method": {
          "default": "att_gt",
          "description": "Estimation mode",
          "enum": [
            "att_gt",
            "twfe",
            "dose_response",
            "cgs"
          ],
          "type": "string"
        },
        "n_boot": {
          "default": 500,
          "description": "Bootstrap replications for SE",
          "type": "integer"
        },
        "n_quantiles": {
          "default": 5,
          "description": "Number of dose quantiles for discretisation",
          "type": "integer"
        },
        "post": {
          "description": "Binary post-treatment indicator (inferred from t_pre / t_post if omitted)",
          "type": "string"
        },
        "seed": {
          "description": "Random seed for reproducible stochastic steps.",
          "type": "integer"
        },
        "t_post": {
          "description": "First post-treatment period",
          "type": "integer"
        },
        "t_pre": {
          "description": "Last pre-treatment period",
          "type": "integer"
        },
        "time": {
          "description": "Time period column",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "dose",
        "time",
        "id"
      ],
      "type": "object"
    }
  },
  {
    "description": "de Chaisemartin & D'Haultfuille (2020) DID_M estimator. Weighted average of consecutive-period DID cells where treatment 'switchers' are compared to 'stayers'. Handles treatments that switch on AND off (unlike Callaway-Sant'Anna which assumes staggered adoption). Supports placebo lags, dynamic horizons, cluster bootstrap SE, joint placebo test and average-cumulative-effect summary from dCDH (2024). The heteroskedastic-weights variant and full dCDH (2024) intertemporal event-study (did_multiplegt_dyn Stata) are on the roadmap -- see docs/rfc/multiplegt_dyn.md. Validation: certified parity evidence.",
    "name": "did_multiplegt",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "cluster": {
          "description": "Cluster variable for bootstrap (defaults to group)",
          "type": "string"
        },
        "controls": {
          "description": "Controls residualised via first differences",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "dynamic": {
          "default": 0,
          "description": "Number of post-treatment dynamic horizons",
          "type": "integer"
        },
        "group": {
          "description": "Unit identifier",
          "type": "string"
        },
        "n_boot": {
          "default": 100,
          "description": "Cluster-bootstrap replications",
          "type": "integer"
        },
        "placebo": {
          "default": 0,
          "description": "Number of pre-treatment placebo lags",
          "type": "integer"
        },
        "placebo_sign": {
          "default": "stata",
          "description": "Placebo sign convention. dCDH's own Stata and R packages disagree: on did::mpdta both give |placebo_1| = 0.024269 with identical effects, but opposite signs. Default keeps Stata's",
          "enum": [
            "stata",
            "r"
          ],
          "type": "string"
        },
        "seed": {
          "description": "Random seed for reproducible stochastic steps.",
          "type": "integer"
        },
        "time": {
          "description": "Time period column",
          "type": "string"
        },
        "treatment": {
          "description": "Binary current-treatment indicator (may switch on and off)",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "group",
        "time",
        "treatment"
      ],
      "type": "object"
    }
  },
  {
    "description": "Canonical 2x2 DID: two groups (treated / control) x two periods (pre / post). Point estimate via either group-means differencing or OLS on the treat x post interaction; optional covariates, robust / cluster SE, and sample weights. Validation: certified parity evidence. Do NOT use when: treatment timing is staggered across units -- the TWFE 2x2 estimate is a negative-weighted mix (Goodman-Bacon 2021); use sp.callaway_santanna, sp.sun_abraham or sp.did_imputation.",
    "name": "did_2x2",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "cluster": {
          "description": "Column for cluster-robust SE (defaults to treat)",
          "type": "string"
        },
        "covariates": {
          "description": "Covariates included additively; for DR use sp.drdid",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "robust": {
          "default": true,
          "description": "Heteroskedasticity-robust SE when no cluster provided",
          "type": "boolean"
        },
        "time": {
          "description": "Time / period indicator",
          "type": "string"
        },
        "treat": {
          "description": "Binary treatment-group indicator (0/1)",
          "type": "string"
        },
        "weights": {
          "description": "Optional column name for sampling weights",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "treat",
        "time"
      ],
      "type": "object"
    }
  },
  {
    "description": "Doubly-robust DiD (Sant'Anna & Zhao 2020). Combines outcome regression with IPW; consistent if either model is correct. Primary estimator for 2x2 DiD with covariates. Validation: certified parity evidence.",
    "name": "drdid",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "covariates": {
          "description": "Covariates X",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "est_method": {
          "default": "dr",
          "description": "Estimator family. With method/normalized/locally_efficient and id=, this reaches all 14 R DRDID 1.2.3 estimators: dr -> drdid_[imp_]panel / drdid_[imp_]rc[1]; ipw -> [std_]ipw_did_panel|rc; reg -> reg_did_panel|rc; twfe -> twfe_did_panel|rc. 'twfe' is for comparison, not recommendation: with covariates it is the specification Sant'Anna-Zhao and Caetano-Callaway warn about.",
          "enum": [
            "dr",
            "ipw",
            "reg",
            "twfe"
          ],
          "type": "string"
        },
        "group": {
          "description": "Unit-level treatment indicator (0/1)",
          "type": "string"
        },
        "id": {
          "description": "Unit identifier for true two-period panel DR-DID",
          "type": "string"
        },
        "locally_efficient": {
          "default": true,
          "description": "est_method='dr' on repeated cross-sections only. False drops the semiparametric-efficiency terms, giving drdid_rc1 / drdid_imp_rc1, which avoid fitting outcome regressions on the treated cells. Both are consistent.",
          "type": "boolean"
        },
        "method": {
          "default": "imp",
          "description": "Nuisance estimators for est_method='dr' (R DRDID::drdid(estMethod=)): 'imp' uses inverse probability tilting + odds-weighted least squares so the DR moment is Neyman-orthogonal by construction; 'trad' uses plain logit + OLS with the estimation effects propagated. Ignored unless est_method='dr'.",
          "enum": [
            "imp",
            "trad"
          ],
          "type": "string"
        },
        "n_boot": {
          "description": "Deprecated / inert: standard errors come from the Sant'Anna-Zhao influence function on every path.",
          "type": "integer"
        },
        "normalized": {
          "default": true,
          "description": "est_method='ipw' only. True = Hajek-normalised (std_ipw_did_*), control arm divided by its own weight mass. False = Abadie (2005) (ipw_did_*), both arms sharing the denominator E[D].",
          "type": "boolean"
        },
        "seed": {
          "description": "Random seed for reproducible stochastic steps.",
          "type": "integer"
        },
        "time": {
          "description": "Time period column",
          "type": "string"
        },
        "trim_level": {
          "default": 0.995,
          "description": "Drop control units whose propensity score reaches this cutoff (DRDID trim.level). 1.0 disables.",
          "type": "number"
        },
        "weights": {
          "description": "Observation weights column (R DRDID i.weights), renormalised to mean one.",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "group",
        "time"
      ],
      "type": "object"
    }
  },
  {
    "description": "Sun-Abraham (2021) interaction-weighted event-study. Fixes the contamination in dynamic event-study TWFE coefficients from other relative-time bins by using cohort-specific interaction weights. Canonical companion to Callaway-Sant'Anna for event studies. Validation: certified parity evidence. Do NOT use when: all units adopt treatment simultaneously -- the cohort x relative-time interactions collapse; use sp.did(method='2x2'); cohorts are very small (a handful of units each) -- interaction-weighted estimates become noisy and the cohort-share weights unstable. Cost: Builds a saturated cohort x relative-time interaction design: columns grow as (cohorts x event-time window), so a wide window on a many-cohort panel produces a large dense design matrix. Trim via event_window=.",
    "name": "sun_abraham",
    "parameters": {
      "properties": {
        "aggregation": {
          "default": "event_time",
          "description": "Headline summary convention: 'event_time' (equal weight per relative time) or 'fixest_att' (cohort-size weighted, matching fixest agg='att').",
          "enum": [
            "event_time",
            "fixest_att"
          ],
          "type": "string"
        },
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "cluster": {
          "description": "Cluster variable (defaults to i)",
          "type": "string"
        },
        "control_cohort": {
          "description": "Nominate the reference cohort explicitly: a 0/1 indicator column (Stata eventstudyinteract's control_cohort()) or a cohort value from g.",
          "type": "string"
        },
        "control_group": {
          "default": "nevertreated",
          "description": "Control arm",
          "enum": [
            "nevertreated",
            "notyettreated"
          ],
          "type": "string"
        },
        "covariates": {
          "description": "Covariate matrix, DataFrame, or column names.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "event_window": {
          "description": "(lead, lag) window for event-study coefficients",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "g": {
          "description": "First-treatment period (0 = never-treated)",
          "type": "string"
        },
        "i": {
          "description": "Unit identifier",
          "type": "string"
        },
        "pretest": {
          "default": "joint",
          "description": "Report the joint pre-trend Wald test ('joint') or skip it ('none').",
          "enum": [
            "joint",
            "none"
          ],
          "type": "string"
        },
        "pretest_periods": {
          "description": "Restrict the pre-trend test to the k estimated leads closest to treatment.",
          "type": "integer"
        },
        "share_variance": {
          "default": true,
          "description": "Carry the cohort-share estimation term of Sun & Abraham (2021, Prop. 3) in the event-study variance (True: Stata eventstudyinteract convention) or treat the interaction weights as fixed (False: fixest::sunab convention). The two coincide at single-cohort relative times; point estimates are unaffected.",
          "type": "boolean"
        },
        "t": {
          "description": "Time period column",
          "type": "string"
        },
        "weights": {
          "description": "Unit-level sampling/population weights omega. Enters the fixed-effect projection, the least-squares solve, the cluster-robust variance, and the interaction weights (which become shares of omega-mass). Changes the target parameter, not the precision.",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "g",
        "t",
        "i"
      ],
      "type": "object"
    }
  },
  {
    "description": "Counterfactual estimators for time-series cross-sectional data (Liu, Wang and Xu 2024): impute the untreated potential outcome of every treated unit-period from a model fitted on untreated cells only -- two-way fixed effects ('fe', the imputation estimator), interactive fixed effects with r factors ('ife'), or nuclear-norm matrix completion ('mc') -- and average Y - Y(0) over treated cells, with the ATT path by relative period. Native port of the R package fect; handles staggered adoption, many treated units, unbalanced panels and treatment reversals. Validation: certified evidence with scoped limitations. Known limitations: Inference is resampling-only (unit bootstrap or jackknife on request); the default returns point estimates only; r and lam are user-supplied; fect's cross-validated choice of r / lambda is not yet supported.",
    "name": "fect",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "covariates": {
          "description": "Time-varying covariates entering the Y(0) model linearly",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "criterion": {
          "default": "mspe",
          "description": "CV score minimised (fect criterion): pooled MSPE, geometric-mean MSPE, count-weighted mean absolute residual by relative period, or fect's PC information criterion (method='ife' only, no holdout).",
          "enum": [
            "mspe",
            "gmspe",
            "moment",
            "pc"
          ],
          "type": "string"
        },
        "cv": {
          "default": false,
          "description": "Choose r (method='ife') or lam (method='mc') by fect's cross-validation (fect CV = TRUE); the CV table is stored in model_info['cv'].",
          "type": "boolean"
        },
        "cv_buffer": {
          "default": 1,
          "description": "Periods hidden before the anchor in the rolling holdout (fect cv.buffer).",
          "type": "integer"
        },
        "cv_donut": {
          "default": 1,
          "description": "Cells removed at each end of a block before scoring ('block' / 'treated_units'; fect cv.donut).",
          "type": "integer"
        },
        "cv_method": {
          "default": "rolling",
          "description": "Holdout design (fect cv.method): 'rolling' anchors a block of cv_nobs periods per sampled unit and hides everything after it; 'block' hides cv_nobs-period blocks from all units' untreated cells; 'treated_units' draws those blocks from treated units' pre-treatment cells only.",
          "enum": [
            "rolling",
            "block",
            "treated_units"
          ],
          "type": "string"
        },
        "cv_nobs": {
          "default": 3,
          "description": "Periods per holdout block (fect cv.nobs).",
          "type": "integer"
        },
        "cv_prop": {
          "default": 0.1,
          "description": "fect cv.prop: share of eligible units sampled per fold ('rolling') or of untreated cells hidden per fold ('block' / 'treated_units').",
          "type": "number"
        },
        "cv_rule": {
          "default": "1se",
          "description": "Re-pick after the scan (fect cv.rule): smallest r / largest lam within one fold-level SE of the minimum, within 1 %, or the minimum.",
          "enum": [
            "1se",
            "min",
            "1pct"
          ],
          "type": "string"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "force": {
          "default": "two-way",
          "description": "Additive fixed effects in the Y(0) model.",
          "enum": [
            "none",
            "unit",
            "time",
            "two-way"
          ],
          "type": "string"
        },
        "k": {
          "default": 20,
          "description": "Number of CV folds (fect k).",
          "type": "integer"
        },
        "lam": {
          "description": "Nuclear-norm penalty on fect's raw scale (method='mc'); the result records lambda_norm = lam / largest singular value.",
          "type": "number"
        },
        "lambda_grid": {
          "description": "Explicit penalty grid for method='mc' with cv=True (fect lambda = c(...)); overrides nlambda.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "max_iter": {
          "default": 1000,
          "description": "Maximum EM iterations",
          "type": "integer"
        },
        "method": {
          "default": "fe",
          "description": "Y(0) model: 'fe' two-way fixed effects, 'ife' interactive fixed effects with r factors, 'mc' matrix completion with penalty lam.",
          "enum": [
            "fe",
            "ife",
            "mc"
          ],
          "type": "string"
        },
        "min_t0": {
          "description": "Drop units with fewer untreated periods (fect: 1 for 'fe', 5 otherwise)",
          "type": "integer"
        },
        "n_boot": {
          "default": 200,
          "description": "Bootstrap replications",
          "type": "integer"
        },
        "nlambda": {
          "default": 10,
          "description": "Length of fect's default penalty grid for method='mc' with cv=True: nlambda - 1 values log-spaced over three decades below the largest singular value, plus 0.",
          "type": "integer"
        },
        "r": {
          "default": 0,
          "description": "Number of latent factors (method='ife')",
          "type": "integer"
        },
        "r_range": {
          "description": "(r_min, r_max) factor-number grid for cv=True; default (0, 5), capped by the data as fect does.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "random_state": {
          "description": "Seed for the CV fold draws (R's fold stream is not reproducible from numpy; the selection is compared to R statistically).",
          "type": "integer"
        },
        "seed": {
          "description": "Bootstrap seed",
          "type": "integer"
        },
        "time": {
          "description": "Time period column",
          "type": "string"
        },
        "tol": {
          "default": 0.001,
          "description": "EM relative convergence tolerance (fect default)",
          "type": "number"
        },
        "treat": {
          "description": "0/1 treatment status in each unit-period (1 = treated)",
          "type": "string"
        },
        "unit": {
          "description": "Unit identifier",
          "type": "string"
        },
        "vce": {
          "description": "Resampling standard errors over units; None reports point estimates only.",
          "enum": [
            "bootstrap",
            "jackknife"
          ],
          "type": "string"
        },
        "y": {
          "description": "Outcome column",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "treat",
        "unit",
        "time"
      ],
      "type": "object"
    }
  },
  {
    "description": "Borusyak-Jaravel-Spiess (2024) imputation DiD. Fits a TWFE model on untreated observations only, imputes counterfactual Y(0) for treated obs, and averages the imputation residuals. Efficient under no-anticipation + parallel trends; analytical SE via bjs_inference. Validation: certified evidence with scoped limitations. Known limitations: R/Stata parity is for the documented untreated-only TWFE and simple ATT aggregation convention only; event-study and SE rows are backend-specific diagnostics. Do NOT use when: there is no never-treated (or not-yet-treated) group to fit the untreated-potential-outcome model on -- imputation has no estimation sample; the design is a simple 2x2 -- sp.did(method='2x2') is the same estimand and far cheaper; pre-trends are visibly non-parallel -- BJS imputes Y(0) from a two-way model that assumes them away, so violations are absorbed silently rather than surfaced. Cost: Fits the untreated two-way model once, then imputes; cheap in memory. vce='bootstrap' multiplies total runtime by n_boot -- budget accordingly before raising n_boot.",
    "name": "did_imputation",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "balanced": {
          "default": false,
          "description": "Keep only eventually-treated units observed at every non-negative requested horizon (Stata: hbalance)",
          "type": "boolean"
        },
        "boot_seed": {
          "default": 0,
          "description": "Seed for the cluster bootstrap (deterministic results).",
          "type": "integer"
        },
        "cluster": {
          "description": "Cluster identifier column for clustered standard errors.",
          "type": "string"
        },
        "controls": {
          "description": "Control-variable column names.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "fe": {
          "description": "Fixed effects in the Y(0) model, replacing the default unit+time. Stata's fe(): entries are column names or 'a#b' interacted cells; [] means no fixed effects.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "first_treat": {
          "description": "First-treatment period; 0 = never-treated",
          "type": "string"
        },
        "group": {
          "description": "Unit identifier",
          "type": "string"
        },
        "hetby": {
          "description": "Report heterogeneous ATTs by a time-invariant unit-level variable (Stata: hetby())",
          "type": "string"
        },
        "horizon": {
          "description": "Relative-time leads / lags (default: all available)",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "min_n": {
          "description": "Drop event-study horizons with fewer treated observations (Stata: minn())",
          "type": "integer"
        },
        "n_boot": {
          "default": 199,
          "description": "Cluster-bootstrap replications when vce='bootstrap'.",
          "type": "integer"
        },
        "pretrend_method": {
          "default": "bjs",
          "description": "Reference convention for the PRE-treatment event-study coefficients. 'bjs' matches Stata did_imputation, 'in-sample' is the fect/did2s residual average (attenuated by N0/N), 'symmetric' is Roth's (2026) TWFE-comparable repair for non-staggered designs. Post-treatment coefficients are identical under all three.",
          "enum": [
            "bjs",
            "in-sample",
            "symmetric"
          ],
          "type": "string"
        },
        "pretrends": {
          "description": "Estimate k placebo pre-trend coefficients (-k..-1) and report their joint Wald test (Stata: pretrends(k))",
          "type": "integer"
        },
        "project": {
          "description": "Regress the imputed treatment effects on these covariates and report constant plus slopes. Stata's project(); mutually exclusive with hetby.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "save_residuals": {
          "default": false,
          "description": "Store untreated-fit residuals in model_info (Stata: saveresid())",
          "type": "boolean"
        },
        "save_weights": {
          "default": false,
          "description": "Store exact estimation weights w with ATT = w'y in model_info (Stata: saveweights())",
          "type": "boolean"
        },
        "se_method": {
          "description": "Shared DiD spelling for vce=: 'analytic', 'bootstrap' or 'auto'. Passing both raises.",
          "enum": [
            "analytic",
            "bootstrap",
            "auto"
          ],
          "type": "string"
        },
        "time": {
          "description": "Time period column",
          "type": "string"
        },
        "time_covariates": {
          "description": "Controls interacted with the period fixed effects (one coefficient per period). Stata's timecontrols().",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "unit_covariates": {
          "description": "Controls interacted with the unit fixed effects (one slope per unit). Stata did_imputation's unitcontrols(); unit_covariates=[time] gives unit-specific trends.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "vce": {
          "default": "analytic",
          "description": "Standard-error mode for the overall ATT. 'analytic' is the exact BJS variance (reproduces Stata did_imputation and R didimputation); 'bootstrap' resamples clusters.",
          "enum": [
            "analytic",
            "bootstrap"
          ],
          "type": "string"
        },
        "weights": {
          "description": "Estimation weights omega (Stata did_imputation [aw=], R didimputation wname=): weighted least squares in the untreated Y(0) model, omega-weighted averages over treated cells, exact variance with the weighted projection. Changes the estimand, not the precision. Not combinable with project.",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "group",
        "time",
        "first_treat"
      ],
      "type": "object"
    }
  },
  {
    "description": "Count the clusters treatment is assigned at and grade the count against the simulation grid of Ulloa-Perez et al. (2025), who found that at 30 clusters every modern staggered DiD estimator they evaluated under-covered a nominal 95% interval, with coverage improving as clusters accumulated. Thirty is the smallest cell they ran, so fewer clusters is reported as outside their evidence rather than as merely worse. Also reports clusters per cohort, since a group-time effect rests on the clusters in its own cohort.",
    "name": "did_cluster_diagnostics",
    "parameters": {
      "properties": {
        "cluster": {
          "description": "Level treatment is assigned at (state, provider group, district). Defaults to unit with a warning: the two coincide only under independent unit-level assignment, and assuming so is the optimistic error.",
          "type": "string"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "first_treat": {
          "description": "First-treatment period; 0 = never-treated",
          "type": "string"
        },
        "unit": {
          "description": "Unit identifier",
          "type": "string"
        },
        "warn": {
          "default": true,
          "description": "Warn when the design sits in or below the weakest cell of the reference grid.",
          "type": "boolean"
        }
      },
      "required": [
        "data",
        "unit",
        "first_treat"
      ],
      "type": "object"
    }
  },
  {
    "description": "Report which of Baker, Callaway, Cunningham, Goodman-Bacon & Sant'Anna's (2026) eight forward-engineering steps a fitted DiD result actually pins down: target parameter, identifying assumption, estimation strategy, inference frame, estimate, sensitivity, heterogeneity. A slot the result cannot determine is reported as undetermined rather than filled with a default, because an unstated choice is still a choice the write-up owes the reader.",
    "name": "did_design_contract",
    "parameters": {
      "properties": {
        "result": {
          "description": "Output of a DiD estimator",
          "type": "string"
        }
      },
      "required": [
        "result"
      ],
      "type": "object"
    }
  },
  {
    "description": "Report how each DiD estimator builds its event-study reference periods. Two implementations can agree on every post-treatment coefficient and still plot different pre-trends, because the leads are a separate construction (Roth 2026). Returns the convention registry: what each half of the path is differenced against, whether the two halves are symmetric, and whether the path coincides with a dynamic TWFE event study in a non-staggered design.",
    "name": "event_study_convention",
    "parameters": {
      "properties": {
        "estimator": {
          "description": "Registry key such as 'callaway_santanna[base_period=varying]', or a bare estimator name for all of its option-specific rows. None returns the whole registry.",
          "type": "string"
        }
      },
      "required": [],
      "type": "object"
    }
  },
  {
    "description": "Run several DiD estimators on one non-staggered panel and measure how far each event-study path departs from the dynamic TWFE benchmark. Splits the difference into a common vertical shift within each half of the path and a residual, so a symmetric estimator scores zero asymmetry while the kink (Callaway-Sant'Anna varying base period), the jump (BJS pre-trend convention) and the N0/N attenuation (fect / did2s in-sample residuals) each get their own signature. Warns when the recorded convention disagrees with what the data show.",
    "name": "compare_event_study_conventions",
    "parameters": {
      "properties": {
        "cluster": {
          "description": "Cluster identifier column for clustered standard errors.",
          "type": "string"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "estimators": {
          "description": "Registry keys to run; defaults to every estimator with a runner.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "first_treat": {
          "description": "First-treatment period; 0 = never-treated",
          "type": "string"
        },
        "time": {
          "description": "Time period column",
          "type": "string"
        },
        "tolerance": {
          "description": "Threshold for the matches_twfe verdict; defaults to a scale-free 1e-6 * max(1, max|beta_twfe|).",
          "type": "number"
        },
        "unit": {
          "description": "Unit identifier",
          "type": "string"
        },
        "window": {
          "description": "Relative-time window; defaults to the widest the panel supports.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "unit",
        "time",
        "first_treat"
      ],
      "type": "object"
    }
  },
  {
    "description": "Wooldridge (2021) extended TWFE (ETWFE). Saturated TWFE regression with cohort x post interactions; recovers cohort-specific ATTs. Numerically equivalent to CS / SA / BJS under the saturated specification. Validation: certified parity evidence. Do NOT use when: treatment timing is not staggered -- the extended TWFE saturation buys nothing over sp.did(method='2x2'). Cost: Like sp.etwfe: regressor count grows as O(cohorts x periods) from the saturated interactions, so the design matrix -- not the sample size -- is the binding cost.",
    "name": "wooldridge_did",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "cluster": {
          "description": "Cluster identifier column for clustered standard errors.",
          "type": "string"
        },
        "controls": {
          "description": "Control-variable column names.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "first_treat": {
          "description": "First-treatment period; 0 = never-treated",
          "type": "string"
        },
        "group": {
          "description": "Unit identifier",
          "type": "string"
        },
        "time": {
          "description": "Time period column.",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "group",
        "time",
        "first_treat"
      ],
      "type": "object"
    }
  },
  {
    "description": "Extended Two-Way Fixed Effects (Wooldridge 2021). Explicit API mirroring the R etwfe package. The headline reports the treated-observation-weighted simple ATT from etwfe::emfx(type='simple') / Stata jwdid, with cgroup selecting not-yet-treated or never-treated controls. family='poisson'/'logit' switches to Wooldridge (2023) nonlinear ETWFE for count / binary outcomes, reporting the average marginal effect on the response scale. Validation: certified evidence with scoped limitations. Known limitations: cgroup='nevertreated' combined with panel=False (repeated cross-sections) is not yet supported; pass either panel=True with cgroup='nevertreated' or panel=False with cgroup='notyet'; family='poisson'/'logit' with xvar, panel=False, or cgroup='nevertreated' is not yet supported; these raise rather than being silently ignored; family='poisson'/'logit' reports an average marginal effect on the response scale (counts / probability) rather than a link-scale coefficient -- the R etwfe::emfx convention; weights= is not yet supported together with xvar or with family='poisson'/'logit'; both combinations raise; cgroup='nevertreated' combined with panel=False (repeated cross-sections) is not yet supported. Use panel=True with cgroup='nevertreated' or panel=False with cgroup='notyet'. Do NOT use when: all units adopt at the same time -- the cohort x period saturation is redundant; use sp.did(method='2x2'). Cost: Saturated cohort x period interaction design: the number of regressors grows as O(cohorts x periods), so many-cohort many-period panels produce a wide dense design and a slow solve. Check the cohort x period grid size before calling.",
    "name": "etwfe",
    "parameters": {
      "properties": {
        "agg_weights": {
          "default": "estimation",
          "description": "How cohort-by-period cells enter the emfx aggregates when weights= is set (the rules coincide otherwise). 'estimation' (Stata jwdid, estat): each cell weighted by the sum of the estimation weights over its treated observations, so the never-treated simple aggregate equals the weighted Callaway-Sant'Anna simple ATT. 'unit' (R etwfe::emfx): one unit weight per treated observation, estimation weights enter the regression only.",
          "enum": [
            "estimation",
            "unit"
          ],
          "type": "string"
        },
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "cgroup": {
          "default": "notyet",
          "description": "Control group: 'notyet' (not-yet-treated) or 'nevertreated'. The latter is only supported when panel=True.",
          "enum": [
            "notyet",
            "nevertreated"
          ],
          "type": "string"
        },
        "cluster": {
          "description": "Cluster identifier column for clustered standard errors.",
          "type": "string"
        },
        "controls": {
          "description": "Control-variable column names.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "family": {
          "description": "Outcome model. None/'gaussian' is the linear ETWFE. 'poisson' (counts) and 'logit' (binary) fit Wooldridge (2023) nonlinear ETWFE by MLE and report the average marginal effect on the response scale, matching R etwfe::emfx. The nonlinear branch requires panel=True, cgroup='notyet', and no xvar.",
          "enum": [
            "gaussian",
            "poisson",
            "logit",
            "binomial"
          ],
          "type": "string"
        },
        "first_treat": {
          "description": "first_treat parameter (str).",
          "type": "string"
        },
        "group": {
          "description": "Group or cohort identifier.",
          "type": "string"
        },
        "panel": {
          "default": true,
          "description": "If False, treat data as repeated cross-section",
          "type": "boolean"
        },
        "time": {
          "description": "Time period column.",
          "type": "string"
        },
        "weights": {
          "description": "Column of non-negative observation weights. The cohort-by-period regression becomes weighted least squares with R fixest weights= / Stata reghdfe [pw=] semantics (zero-weight rows are dropped, NaN or negative weights raise), keeping the unweighted fit's cluster-robust small-sample convention. Not available with xvar or family='poisson'/'logit'.",
          "type": "string"
        },
        "xvar": {
          "description": "R-style alias for controls",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "group",
        "time",
        "first_treat"
      ],
      "type": "object"
    }
  },
  {
    "description": "Goodman-Bacon (2021) decomposition of the TWFE DiD coefficient into a weighted sum of underlying 2x2 DID comparisons: treated-vs-never, treated-vs-notyet, earlier-vs-later. Diagnoses when TWFE is contaminated by already-treated units acting as controls. Validation: certified parity evidence. Do NOT use when: treatment timing is not staggered -- with a single adoption date there is exactly one 2x2 comparison and the decomposition is uninformative; the panel is unbalanced or has never-treated units only -- the weights are defined for staggered two-way comparisons. Cost: Enumerates every pair of timing groups: O(G^2) 2x2 regressions for G distinct adoption dates. Fine for a handful of cohorts, slow when adoption dates are near-continuous (e.g. a distinct date per unit).",
    "name": "bacon_decomposition",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "id": {
          "description": "Unit, subject, or panel identifier column.",
          "type": "string"
        },
        "time": {
          "description": "Time period column.",
          "type": "string"
        },
        "treat": {
          "description": "Time-varying binary treatment indicator",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "treat",
        "time",
        "id"
      ],
      "type": "object"
    }
  },
  {
    "description": "Changes-in-Changes (Athey & Imbens 2006). Nonparametric quantile DiD that identifies the full counterfactual outcome distribution for treated units, not just the mean. Reports quantile treatment effects (QTE) via empirical-CDF transformation; bootstrap SE. Validation: certified parity evidence.",
    "name": "cic",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "covariates": {
          "description": "Covariates for the Athey-Imbens (2006 p.466) two-step estimator; 'C(col)' / 'i.col' terms are absorbed as fixed effects",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "first_stage": {
          "default": "feols",
          "description": "First-stage residualizer (only 'feols' supported)",
          "type": "string"
        },
        "group": {
          "description": "Treatment-group indicator (0/1)",
          "type": "string"
        },
        "n_boot": {
          "default": 500,
          "description": "Number of bootstrap replications.",
          "type": "integer"
        },
        "n_grid": {
          "default": 200,
          "description": "Grid size for inverse-CDF mapping",
          "type": "integer"
        },
        "quantiles": {
          "description": "Quantile grid (default: deciles)",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "seed": {
          "description": "Random seed for reproducible stochastic steps.",
          "type": "integer"
        },
        "time": {
          "description": "Period indicator (0=pre, 1=post)",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "group",
        "time"
      ],
      "type": "object"
    }
  },
  {
    "description": "Stacked DiD (Cengiz, Dube, Lindner, Zipperer 2019). For each treatment cohort, constructs a sub-experiment with only that cohort + clean (never-treated or not-yet-treated) controls, then TWFE on the stacked panel. Robust to staggered-adoption contamination at the cost of dropping late-treated units in early sub-experiments. Validation: certified parity evidence. Do NOT use when: there is a single adoption cohort -- stacking produces one sub-experiment and is equivalent to sp.did(method='2x2'). Cost: Duplicates rows into one sub-experiment per treated cohort: the stacked dataset is roughly O(n x number of cohorts) before the event window trims it. Narrow window= to bound memory.",
    "name": "stacked_did",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "cluster": {
          "description": "Cluster identifier column for clustered standard errors.",
          "type": "string"
        },
        "controls": {
          "description": "Control-variable column names.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "first_treat": {
          "description": "first_treat parameter (str).",
          "type": "string"
        },
        "group": {
          "description": "Unit identifier",
          "type": "string"
        },
        "never_treated_only": {
          "default": true,
          "description": "Use only never-treated as controls (drops late-treated)",
          "type": "boolean"
        },
        "time": {
          "description": "Time period column.",
          "type": "string"
        },
        "window": {
          "default": [
            -5,
            5
          ],
          "description": "Event-time (lead, lag) window per sub-experiment",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "group",
        "time",
        "first_treat"
      ],
      "type": "object"
    }
  },
  {
    "description": "Traditional OLS event-study with entity and time FEs. Generates relative-time dummies around the treatment date, omits a reference period, and estimates via TWFE + optional clustered SE. Exposed for users who want the classical specification alongside CS / SA / BJS; not robust to staggered-effect heterogeneity -- use sp.sun_abraham for that. Validation: certified parity evidence. Do NOT use when: only one pre-treatment period is available -- there are no leads to test parallel trends with, so the plot cannot support a pre-trend claim; treatment timing is staggered and heterogeneous -- a pooled TWFE event study contaminates leads with other cohorts' treated periods; use sp.sun_abraham or sp.callaway_santanna.",
    "name": "event_study",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "bin_width": {
          "description": "Group relative time into bins of this width instead of one coefficient per period. Bins are anchored at the treatment boundary, so post-treatment bins tile [0, k-1], [k, 2k-1], ... and pre-treatment bins tile [-k, -1], [-2k, -k-1], ...",
          "type": "integer"
        },
        "cluster": {
          "description": "Cluster identifier column for clustered standard errors.",
          "type": "string"
        },
        "covariates": {
          "description": "Covariate matrix, DataFrame, or column names.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "expose_pre_vcov": {
          "default": false,
          "description": "Publish the true pre-period covariance into model_info['vcv_pre']. With True, pretrends_test / pretrends_power / sensitivity_rr / honest_did use the full covariance of the pre-treatment coefficients; with False (default) the key is withheld and those tools fall back to a diagonal covariance and warn.",
          "type": "boolean"
        },
        "ref_period": {
          "default": -1,
          "description": "Reference relative-time period to omit",
          "type": "integer"
        },
        "time": {
          "description": "Time period column.",
          "type": "string"
        },
        "treat_time": {
          "description": "First-treatment period column",
          "type": "string"
        },
        "unit": {
          "description": "Unit identifier",
          "type": "string"
        },
        "weights": {
          "description": "Analytical weights column, as Stata's [aweight=...].",
          "type": "string"
        },
        "window": {
          "default": [
            -4,
            4
          ],
          "description": "(lead, lag) horizons",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "treat_time",
        "time",
        "unit"
      ],
      "type": "object"
    }
  },
  {
    "description": "Workflow wrapper that runs a full DiD pipeline: auto-detects 2x2 vs. staggered, runs the right estimator (CS by default), optionally runs Bacon decomposition, event study, and Rambachan-Roth sensitivity, and aggregates into a DIDAnalysis report object.",
    "name": "did_analysis",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "cluster": {
          "description": "Cluster variable for the SEs.",
          "type": "string"
        },
        "control_group": {
          "default": "nevertreated",
          "description": "For the CS / SA estimators.",
          "enum": [
            "nevertreated",
            "notyettreated"
          ],
          "type": "string"
        },
        "covariates": {
          "description": "Control variables.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "estimator": {
          "default": "dr",
          "description": "For CS: doubly robust, IPW, or outcome regression.",
          "enum": [
            "dr",
            "ipw",
            "reg"
          ],
          "type": "string"
        },
        "event_window": {
          "description": "Event-study window, e.g. (-5, 5); auto-detected if None.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "id": {
          "description": "Unit, subject, or panel identifier column.",
          "type": "string"
        },
        "method": {
          "default": "auto",
          "description": "Estimator selection",
          "enum": [
            "auto",
            "2x2",
            "cs",
            "sa",
            "sdid"
          ],
          "type": "string"
        },
        "robust": {
          "default": true,
          "description": "HC1 robust standard errors.",
          "type": "boolean"
        },
        "run_bacon": {
          "default": true,
          "description": "run_bacon parameter (bool).",
          "type": "boolean"
        },
        "run_event_study": {
          "default": true,
          "description": "run_event_study parameter (bool).",
          "type": "boolean"
        },
        "run_sensitivity": {
          "default": true,
          "description": "run_sensitivity parameter (bool).",
          "type": "boolean"
        },
        "time": {
          "description": "Time period column.",
          "type": "string"
        },
        "treat": {
          "description": "Binary treatment or first-treat column",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "treat",
        "time",
        "id"
      ],
      "type": "object"
    }
  },
  {
    "description": "Harvest every valid 2x2 DID comparison from a staggered panel and aggregate them via precision-weighted / simple / cohort-weighted averages. Agnostic to cohort structure; useful for robustness comparisons against CS / SA / BJS. Validation: certified parity evidence.",
    "name": "harvest_did",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "cohort": {
          "description": "First-treat cohort column (for static harvesting)",
          "type": "string"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "horizons": {
          "description": "horizons parameter (list).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "never_value": {
          "default": 0,
          "description": "never_value parameter.",
          "type": "string"
        },
        "outcome": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        },
        "reference": {
          "default": -1,
          "description": "Pre-treatment reference horizon relative to each cohort",
          "type": "integer"
        },
        "time": {
          "description": "Time period column.",
          "type": "string"
        },
        "treat": {
          "description": "Time-varying treat indicator (for dynamic harvesting)",
          "type": "string"
        },
        "unit": {
          "description": "Unit identifier column.",
          "type": "string"
        },
        "weighting": {
          "default": "precision",
          "description": "How the harvested 2x2 estimates are aggregated: 'precision' inverse-variance (minimum-variance under independence), 'equal' unweighted, 'n_treated' by each comparison's treated-unit count.",
          "enum": [
            "precision",
            "equal",
            "n_treated"
          ],
          "type": "string"
        }
      },
      "required": [
        "data",
        "unit",
        "time",
        "outcome"
      ],
      "type": "object"
    }
  },
  {
    "description": "Overlap-weighted 2x2 DiD. Weights observations by e(X)(1-e(X)), where e(X) is the estimated propensity score, placing highest weight on units with the most overlap between treated and control covariate distributions. Useful when overlap is poor at the tails.",
    "name": "overlap_weighted_did",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "covariates": {
          "description": "Covariates X",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "ps_model": {
          "default": "logit",
          "description": "Propensity score model",
          "enum": [
            "logit",
            "rf",
            "gbm",
            "dl"
          ],
          "type": "string"
        },
        "time": {
          "description": "Time period column.",
          "type": "string"
        },
        "treat": {
          "description": "Binary treatment indicator",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "treat",
        "time",
        "covariates"
      ],
      "type": "object"
    }
  },
  {
    "description": "Cohort-anchored event study. Instead of averaging across cohorts at each relative-time bin (which can contaminate leads / lags with other cohorts' dynamics), estimates separate event-study paths per cohort and then aggregates with cohort weights. Standard errors are cluster-robust and carry no protection against parallel-trends violations: this is the cohort-anchored estimator Liu (2025) starts from, NOT that paper's block-bias robust-inference procedure, which is not implemented. For parallel-trends sensitivity use sp.honest_did.",
    "name": "cohort_anchored_event_study",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "cluster": {
          "description": "Cluster identifier column for clustered standard errors.",
          "type": "string"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "id": {
          "description": "Unit, subject, or panel identifier column.",
          "type": "string"
        },
        "lags": {
          "default": 4,
          "description": "lags parameter (int).",
          "type": "integer"
        },
        "leads": {
          "default": 4,
          "description": "leads parameter (int).",
          "type": "integer"
        },
        "time": {
          "description": "Time period column.",
          "type": "string"
        },
        "treat": {
          "description": "Treatment indicator or first-treatment-period column.",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "treat",
        "time",
        "id"
      ],
      "type": "object"
    }
  },
  {
    "description": "Design-robust event study with explicit negative-weight diagnostics per cohort x relative-time cell. Reports which event-study coefficients receive negative weights in TWFE and flags the affected horizons.",
    "name": "design_robust_event_study",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "cluster": {
          "description": "Cluster identifier column for clustered standard errors.",
          "type": "string"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "id": {
          "description": "Unit, subject, or panel identifier column.",
          "type": "string"
        },
        "lags": {
          "default": 4,
          "description": "lags parameter (int).",
          "type": "integer"
        },
        "leads": {
          "default": 4,
          "description": "leads parameter (int).",
          "type": "integer"
        },
        "time": {
          "description": "Time period column.",
          "type": "string"
        },
        "treat": {
          "description": "Treatment indicator or first-treatment-period column.",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "treat",
        "time",
        "id"
      ],
      "type": "object"
    }
  },
  {
    "description": "Heuristic what-if adjustment of a staggered DiD for a user-supplied timing-misclassification probability pi_misclass and anticipation horizon. A sensitivity check motivated by Augustin-Gutknecht-Liu (2025), not their estimator: it does not identify misclassification or anticipation from the data.",
    "name": "did_misclassified",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "anticipation_periods": {
          "default": 0,
          "description": "anticipation_periods parameter (int).",
          "type": "integer"
        },
        "cluster": {
          "description": "Cluster identifier column for clustered standard errors.",
          "type": "string"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "id": {
          "description": "Unit, subject, or panel identifier column.",
          "type": "string"
        },
        "pi_misclass": {
          "default": 0.0,
          "description": "P(observed treat != true treat) -- between 0 and 1",
          "type": "number"
        },
        "time": {
          "description": "Time period column.",
          "type": "string"
        },
        "treat": {
          "description": "First-treatment period (possibly noisy)",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "treat",
        "time",
        "id"
      ],
      "type": "object"
    }
  },
  {
    "description": "[experimental] dCDH (2024) intertemporal event-study DiD (MVP -- see docs/rfc/multiplegt_dyn.md). At each horizon l in {-placebo, ..., dynamic}, compares Y_{F+l} - Y_{F-1} between units first switching at F and a not-yet-treated or never-treated control set held stable across the horizon. Effects, placebos, switcher counts, the switcher-weighted aggregate and the analytic (se_method='analytic') standard errors are pinned to the authors' DIDmultiplegtDYN / Stata did_multiplegt_dyn; weight= is supported. **MVP caveats**: no controls=, trends or normalized/continuous options; heteroskedastic-weights variant pending. Validation status: experimental. Known limitations: switch-off events are handled, but the heteroskedastic-weights variant (dCDH 2023 EJ survey) is not implemented; se_method='analytic' reproduces DIDmultiplegtDYN's variance only for the options implemented here: controls=, trends and normalized/continuous variants are not implemented, and the joint placebo/overall tests come from the bootstrap only; the headline aggregation convention differs from DIDmultiplegtDYN's Av_tot_eff: the default weights horizons equally; pass aggregation='switchers' to match the R package. Cost: Bootstrap inference dominates: total runtime is roughly n_boot x (one full estimation pass). The default n_boot is fine for a final table but expensive inside a search loop -- lower it while iterating.",
    "name": "did_multiplegt_dyn",
    "parameters": {
      "properties": {
        "aggregation": {
          "default": "simple",
          "description": "Headline weighting over dynamic horizons: 'simple' (equal weight) or 'switchers' (weight by switchers per horizon -- reproduces DIDmultiplegtDYN's Av_tot_eff)",
          "enum": [
            "simple",
            "switchers"
          ],
          "type": "string"
        },
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "cluster": {
          "description": "Cluster column (defaults to group)",
          "type": "string"
        },
        "continuous": {
          "description": "Degree of the per-period polynomial in the period-one treatment that replaces the baseline match when every group's period-one treatment differs; the treatment may then be non-binary",
          "type": "integer"
        },
        "control": {
          "default": "not_yet_treated",
          "description": "Control group",
          "enum": [
            "not_yet_treated",
            "never_treated"
          ],
          "type": "string"
        },
        "controls": {
          "description": "Covariates; the outcome's first difference is replaced by its residual from a regression on the covariates' first differences and time fixed effects, fitted on the not-yet-switched (g, t)s and separately by baseline treatment",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "dynamic": {
          "default": 3,
          "description": "Number of post-treatment dynamic horizons",
          "type": "integer"
        },
        "effects_equal": {
          "default": false,
          "description": "Test H0 that the dynamic effects are all equal. True tests every effect; a (lower, upper) pair tests that horizon range. Stata's effects_equal().",
          "type": "boolean"
        },
        "group": {
          "description": "Unit identifier",
          "type": "string"
        },
        "n_boot": {
          "default": 500,
          "description": "Number of bootstrap replications.",
          "type": "integer"
        },
        "normalized": {
          "default": false,
          "description": "Report the effect per unit of treatment received between the base period and the horizon",
          "type": "boolean"
        },
        "placebo": {
          "default": 0,
          "description": "Number of pre-treatment placebo horizons",
          "type": "integer"
        },
        "same_switchers": {
          "default": false,
          "description": "Restrict the treated arm to switchers observed at every requested horizon, holding the composition fixed across relative time. Stata's same_switchers.",
          "type": "boolean"
        },
        "se_method": {
          "default": "bootstrap",
          "description": "'bootstrap' resamples clusters; 'analytic' uses the influence functions (~100x faster, but NOT pinned to DIDmultiplegtDYN -- about 1% below its reported SEs)",
          "enum": [
            "bootstrap",
            "analytic"
          ],
          "type": "string"
        },
        "seed": {
          "description": "Random seed for reproducible stochastic steps.",
          "type": "integer"
        },
        "switchers": {
          "description": "Estimate on switch-in ('in') or switch-out ('out') events only. Stata's switchers(); default pools both.",
          "enum": [
            "in",
            "out"
          ],
          "type": "string"
        },
        "time": {
          "description": "Time period column.",
          "type": "string"
        },
        "treatment": {
          "description": "Binary treatment (0/1); switch-on and switch-off events both count, see switchers=",
          "type": "string"
        },
        "trends_nonparam": {
          "description": "Time-invariant variables a control must match the switcher on, over and above the baseline treatment",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "weights": {
          "description": "Observation weights (Stata did_multiplegt_dyn weight(), R weight=, both accepted as the alias weight=): read at the row a unit contributes from; switcher means, control means, cohort weights and the switchers aggregation all become weighted.",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "group",
        "time",
        "treatment"
      ],
      "type": "object"
    }
  },
  {
    "description": "DiD with time-varying covariates frozen at baseline (Caetano, Callaway, Payne & Sant'Anna 2026, arXiv:2608.03881). Avoids the bad-controls bias that arises when treatment affects the covariates: freezes X at period g + baseline_offset (default g-1) per cohort and uses the frozen values as controls in a per-(g, t) DiD on the long difference. est_method picks the cell estimator (outcome regression, stabilised IPW, or the doubly robust combination), control_group widens the comparison set to the not-yet-treated, and vce switches between the cluster bootstrap, the influence-function plug-in and the multiplier bootstrap. covariate_pretest=True adds the paper's two placebo statistics on the covariate itself. aggregation='group' averages each cohort's post-period cells then weights cohorts by size; 'simple' weights every cell by its treated count. Validation: certified parity evidence.",
    "name": "did_timevarying_covariates",
    "parameters": {
      "properties": {
        "aggregation": {
          "default": "group",
          "description": "``\"group\"``: average ATT(g, t) over each cohort's post periods, then across cohorts weighted by cohort size -- the overall ATT reported by ``ptetools`` and ``did::aggte(type = \"group\")``. ``\"simple\"``: weight every post cell by its number of treated units, as ``did::aggte(type = \"simple\")`` does on a balanced panel.",
          "enum": [
            "group",
            "simple"
          ],
          "type": "string"
        },
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "baseline_offset": {
          "default": -1,
          "description": "Offset relative to first-treatment period for freezing",
          "type": "integer"
        },
        "cohort": {
          "description": "First-treatment period (never_value = never-treated)",
          "type": "string"
        },
        "control_group": {
          "default": "nevertreated",
          "description": "Comparison units; 'notyettreated' adds cohorts that have not switched by either period of the cell",
          "enum": [
            "nevertreated",
            "notyettreated"
          ],
          "type": "string"
        },
        "covariate_pretest": {
          "default": false,
          "description": "Also run the comparison with each covariate as the outcome, before and after treatment",
          "type": "boolean"
        },
        "covariates": {
          "description": "Time-varying covariates to freeze at baseline",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "est_method": {
          "default": "reg",
          "description": "Cell estimator: outcome regression, stabilised IPW, or the doubly robust combination",
          "enum": [
            "reg",
            "ipw",
            "dr"
          ],
          "type": "string"
        },
        "n_boot": {
          "default": 500,
          "description": "Number of bootstrap replications.",
          "type": "integer"
        },
        "never_value": {
          "default": 0,
          "description": "never_value parameter.",
          "type": "string"
        },
        "seed": {
          "description": "Random seed for reproducible stochastic steps.",
          "type": "integer"
        },
        "time": {
          "description": "Time period column.",
          "type": "string"
        },
        "unit": {
          "description": "Unit identifier column.",
          "type": "string"
        },
        "vce": {
          "default": "bootstrap",
          "description": "Standard error: unit cluster bootstrap, the influence-function plug-in, or the multiplier bootstrap",
          "enum": [
            "bootstrap",
            "analytic",
            "multiplier"
          ],
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "unit",
        "time",
        "cohort",
        "covariates"
      ],
      "type": "object"
    }
  },
  {
    "description": "Heterogeneity-robust triple differences (DDD) for staggered adoption. Decomposes DDD into per-(cohort, time) cells via a Callaway-Sant'Anna-style aggregation, with the unaffected subgroup's DID as a placebo. Avoids the negative-weight issue that textbook TWFE DDD inherits from TWFE DID (Goodman-Bacon 2021 analogue). Validation: certified evidence with scoped limitations. Known limitations: the placebo joint test is only produced on the bootstrap path; se='analytic' reports None for it, because that test needs the joint covariance of the placebo arms rather than of the DDD; control_group='notyettreated' is only partially comparable to triplediff 0.2.4: its per-control-cohort estimates agree exactly, but the reference misindexes the influence functions it combines, so the combined numbers differ by convention on cells where the comparison does not span the whole panel; the aggregation convention differs from triplediff::agg_ddd(type='simple'): the default weights cohorts by treated-eligible units; pass weight_by='cohort' to match the R package.",
    "name": "ddd_heterogeneous",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "cohort": {
          "description": "First-treatment period (never_value = never-treated)",
          "type": "string"
        },
        "control_group": {
          "default": "nevertreated",
          "description": "Control units: never-treated, or not-yet-treated cohorts combined by minimum distance (see the docstring warning -- that path deliberately diverges from triplediff 0.2.4, which misindexes its influence functions there)",
          "enum": [
            "nevertreated",
            "notyettreated"
          ],
          "type": "string"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "est_method": {
          "default": "dr",
          "description": "Nuisance combination: doubly robust, inverse probability weighting, or outcome regression",
          "enum": [
            "dr",
            "ipw",
            "reg"
          ],
          "type": "string"
        },
        "n_boot": {
          "default": 500,
          "description": "Number of bootstrap replications.",
          "type": "integer"
        },
        "never_value": {
          "default": 0,
          "description": "Value in cohort for never-treated units",
          "type": "string"
        },
        "se": {
          "description": "'analytic' influence-function variance (exact, and what triplediff reports) or 'bootstrap' clustered on unit (the only path that fills in placebo_joint_test). Defaults to bootstrap without covariates, analytic with them",
          "enum": [
            "analytic",
            "bootstrap"
          ],
          "type": "string"
        },
        "seed": {
          "description": "Random seed for reproducible stochastic steps.",
          "type": "integer"
        },
        "subgroup": {
          "description": "Binary within-group subgroup indicator (1=affected, 0=placebo)",
          "type": "string"
        },
        "time": {
          "description": "Time period column.",
          "type": "string"
        },
        "unit": {
          "description": "Unit identifier column.",
          "type": "string"
        },
        "weight_by": {
          "default": "eligible",
          "description": "Aggregation weights: 'eligible' (treated units in the affected subgroup) or 'cohort' (whole cohort, both subgroups -- reproduces triplediff::agg_ddd)",
          "enum": [
            "eligible",
            "cohort"
          ],
          "type": "string"
        },
        "x": {
          "description": "Base-period covariates; identification becomes CONDITIONAL DDD parallel trends",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "unit",
        "time",
        "cohort",
        "subgroup"
      ],
      "type": "object"
    }
  },
  {
    "description": "Local-Projections DiD (Dube-Girardi-Jorda-Taylor 2023). At each event-time horizon h in {-P, ..., H}, runs a separate OLS of Y_{t+h} - Y_{t-1} on the treatment change Deltad_{t} with time FE and cluster-robust SE, using 'not-yet-treated' or 'never-treated' units as controls. Event-study beta_h paths are returned in ``model_info['event_study']``. Validation: certified parity evidence.",
    "name": "lp_did",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "clean_controls": {
          "default": "not_yet_treated",
          "description": "Control selection",
          "enum": [
            "not_yet_treated",
            "never_treated"
          ],
          "type": "string"
        },
        "cluster": {
          "description": "Cluster variable (defaults to unit)",
          "type": "string"
        },
        "controls": {
          "description": "Control-variable column names.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "horizons": {
          "default": [
            -3,
            5
          ],
          "description": "(min, max) event-time horizons to estimate",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "time": {
          "description": "Integer period (consecutive)",
          "type": "string"
        },
        "time_fe": {
          "default": true,
          "description": "time_fe parameter (bool).",
          "type": "boolean"
        },
        "treatment": {
          "description": "Binary time-varying treatment (0/1)",
          "type": "string"
        },
        "unit": {
          "description": "Unit identifier",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "unit",
        "time",
        "treatment"
      ],
      "type": "object"
    }
  },
  {
    "description": "DiD with a BCF-style forest on long-differenced outcomes: each cohort is compared with never-treated units over its own pre and post periods, and a prognostic forest plus treatment-effect booster (bootstrap uncertainty, not MCMC) is fitted to the long difference. For heterogeneous effects with group-time inference prefer sp.did_forest.",
    "name": "did_bcf",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "covariates": {
          "description": "Covariate matrix, DataFrame, or column names.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "id": {
          "description": "Unit, subject, or panel identifier column.",
          "type": "string"
        },
        "n_bootstrap": {
          "default": 100,
          "description": "Bootstrap replications of the BCF fit per cohort (covariate path).",
          "type": "integer"
        },
        "n_trees": {
          "default": 50,
          "description": "Number of trees.",
          "type": "integer"
        },
        "seed": {
          "description": "Random seed for reproducible stochastic steps.",
          "type": "integer"
        },
        "time": {
          "description": "Time period column.",
          "type": "string"
        },
        "treat": {
          "description": "Treatment indicator or first-treatment-period column.",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "treat",
        "time",
        "id"
      ],
      "type": "object"
    }
  },
  {
    "description": "Production function estimator (Cobb-Douglas) -- unified method= dispatcher. Solves the simultaneity between input choices and unobserved productivity by inverting an input policy as a control function. method= selects: 'op' (Olley-Pakes 1996, investment proxy), 'lp' (Levinsohn-Petrin 2003, intermediate-input proxy), 'acf' (Ackerberg-Caves-Frazer 2015, corrected identification -- DEFAULT), 'wrdg' (Wooldridge 2009, joint GMM). OP/LP/ACF/WRDG are checked against Stata and R prodest (see docs/parity.md). Validation: validated evidence tier (known-truth, reference, external-parity, or Monte Carlo artifact).",
    "name": "prod_fn",
    "parameters": {
      "properties": {
        "boot_reps": {
          "default": 0,
          "description": "Firm-cluster bootstrap replications for SE.",
          "type": "integer"
        },
        "data": {
          "description": "Long panel: one row per (firm, year).",
          "type": "string"
        },
        "free": {
          "description": "Free inputs, default ['l'].",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "functional_form": {
          "default": "cobb-douglas",
          "description": "Production function form (translog adds quadratic + cross terms; acf only).",
          "enum": [
            "cobb-douglas",
            "translog"
          ],
          "type": "string"
        },
        "method": {
          "default": "acf",
          "description": "Estimator",
          "enum": [
            "op",
            "lp",
            "acf",
            "wrdg"
          ],
          "type": "string"
        },
        "output": {
          "default": "y",
          "description": "Log output column.",
          "type": "string"
        },
        "panel_id": {
          "default": "id",
          "description": "Firm identifier column.",
          "type": "string"
        },
        "polynomial_degree": {
          "description": "Control-function polynomial degree; None = estimator default (3).",
          "type": "integer"
        },
        "productivity_degree": {
          "description": "Degree of the productivity polynomial g; None = cubic.",
          "type": "integer"
        },
        "proxy": {
          "description": "Proxy column. Defaults to 'i' for OP, 'm' otherwise.",
          "type": "string"
        },
        "seed": {
          "description": "Random seed for reproducible stochastic steps.",
          "type": "integer"
        },
        "state": {
          "description": "State/predetermined inputs, default ['k'].",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "time": {
          "default": "year",
          "description": "Time identifier column.",
          "type": "string"
        }
      },
      "required": [
        "data"
      ],
      "type": "object"
    }
  },
  {
    "description": "Olley-Pakes (1996) production function estimator. Two-stage control function with INVESTMENT as the productivity proxy. Drops zero-investment firms (required for the inversion). Note: beta_l identification can fail if labor responds to current productivity (ACF critique) -- prefer sp.acf for modern work. Validation: certified parity evidence.",
    "name": "olley_pakes",
    "parameters": {
      "properties": {
        "boot_reps": {
          "default": 0,
          "description": "boot_reps parameter (int).",
          "type": "integer"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "drop_zero_proxy": {
          "default": true,
          "description": "drop_zero_proxy parameter (bool).",
          "type": "boolean"
        },
        "free": {
          "description": "Default ['l'].",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "functional_form": {
          "default": "cobb-douglas",
          "description": "Production function form (translog: use ackerberg_caves_frazer)",
          "enum": [
            "cobb-douglas"
          ],
          "type": "string"
        },
        "output": {
          "default": "y",
          "description": "output parameter (str).",
          "type": "string"
        },
        "panel_id": {
          "default": "id",
          "description": "panel_id parameter (str).",
          "type": "string"
        },
        "polynomial_degree": {
          "default": 3,
          "description": "polynomial_degree parameter (int).",
          "type": "integer"
        },
        "productivity_degree": {
          "default": 3,
          "description": "productivity_degree parameter (int).",
          "type": "integer"
        },
        "proxy": {
          "default": "i",
          "description": "Investment column; rows <= 0 dropped unless drop_zero_proxy=False.",
          "type": "string"
        },
        "seed": {
          "description": "Random seed for reproducible stochastic steps.",
          "type": "integer"
        },
        "state": {
          "description": "Default ['k'].",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "time": {
          "default": "year",
          "description": "Time period column.",
          "type": "string"
        }
      },
      "required": [
        "data"
      ],
      "type": "object"
    }
  },
  {
    "description": "Levinsohn-Petrin (2003) production function estimator. Uses intermediate inputs (materials/energy) as proxy -- avoids the OP zero-investment selection problem. Same ACF caveat applies to beta_l identification: prefer sp.acf for rigorous identification. Validation: certified parity evidence.",
    "name": "levinsohn_petrin",
    "parameters": {
      "properties": {
        "boot_reps": {
          "default": 0,
          "description": "boot_reps parameter (int).",
          "type": "integer"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "free": {
          "description": "Default ['l'].",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "functional_form": {
          "default": "cobb-douglas",
          "description": "Production function form (translog: use ackerberg_caves_frazer)",
          "enum": [
            "cobb-douglas"
          ],
          "type": "string"
        },
        "output": {
          "default": "y",
          "description": "output parameter (str).",
          "type": "string"
        },
        "panel_id": {
          "default": "id",
          "description": "panel_id parameter (str).",
          "type": "string"
        },
        "polynomial_degree": {
          "default": 3,
          "description": "polynomial_degree parameter (int).",
          "type": "integer"
        },
        "productivity_degree": {
          "default": 3,
          "description": "productivity_degree parameter (int).",
          "type": "integer"
        },
        "proxy": {
          "default": "m",
          "description": "Intermediate input.",
          "type": "string"
        },
        "seed": {
          "description": "Random seed for reproducible stochastic steps.",
          "type": "integer"
        },
        "state": {
          "description": "Default ['k'].",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "time": {
          "default": "year",
          "description": "Time period column.",
          "type": "string"
        }
      },
      "required": [
        "data"
      ],
      "type": "object"
    }
  },
  {
    "description": "Ackerberg-Caves-Frazer (2015) production function estimator. Modern default. Corrects the OP/LP identification problem: all coefficient identification moves to stage 2, with free inputs instrumented by their lagged values and state inputs at the contemporaneous level. Aliased as sp.acf. Validation: certified parity evidence.",
    "name": "ackerberg_caves_frazer",
    "parameters": {
      "properties": {
        "boot_reps": {
          "default": 0,
          "description": "boot_reps parameter (int).",
          "type": "integer"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "free": {
          "description": "Default ['l']. Instrumented with lag in stage 2.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "functional_form": {
          "default": "cobb-douglas",
          "description": "Production function form",
          "enum": [
            "cobb-douglas",
            "translog"
          ],
          "type": "string"
        },
        "output": {
          "default": "y",
          "description": "output parameter (str).",
          "type": "string"
        },
        "panel_id": {
          "default": "id",
          "description": "panel_id parameter (str).",
          "type": "string"
        },
        "polynomial_degree": {
          "default": 3,
          "description": "polynomial_degree parameter (int).",
          "type": "integer"
        },
        "productivity_degree": {
          "default": 3,
          "description": "productivity_degree parameter (int).",
          "type": "integer"
        },
        "proxy": {
          "default": "m",
          "description": "proxy parameter (str).",
          "type": "string"
        },
        "seed": {
          "description": "Random seed for reproducible stochastic steps.",
          "type": "integer"
        },
        "state": {
          "description": "Default ['k'].",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "time": {
          "default": "year",
          "description": "Time period column.",
          "type": "string"
        }
      },
      "required": [
        "data"
      ],
      "type": "object"
    }
  },
  {
    "description": "Wooldridge (2009) production function estimator: the level and productivity-substituted equations share the elasticities and the control function h(k, m) and are estimated jointly by GMM with a free Markov polynomial g. convention='prodest' reproduces Stata prodest, method(wrdg) (unit-slope g, stacked 2SLS). Analytic SEs, firm-clustered by default. Validation: validated evidence tier (known-truth, reference, external-parity, or Monte Carlo artifact).",
    "name": "wooldridge_prod",
    "parameters": {
      "properties": {
        "boot_reps": {
          "default": 0,
          "description": "Bootstrap reps; replaces analytic SEs.",
          "type": "integer"
        },
        "convention": {
          "default": "statspai",
          "description": "'statspai': GMM with free g; 'prodest': Stata prodest's unit-slope stacked 2SLS.",
          "enum": [
            "statspai",
            "prodest"
          ],
          "type": "string"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "free": {
          "description": "Default ['l'].",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "functional_form": {
          "default": "cobb-douglas",
          "description": "Production function form",
          "enum": [
            "cobb-douglas"
          ],
          "type": "string"
        },
        "output": {
          "default": "y",
          "description": "output parameter (str).",
          "type": "string"
        },
        "panel_id": {
          "default": "id",
          "description": "panel_id parameter (str).",
          "type": "string"
        },
        "polynomial_degree": {
          "default": 3,
          "description": "Degree of h(state, proxy) (Stata prodest default).",
          "type": "integer"
        },
        "productivity_degree": {
          "description": "Degree of the Markov polynomial g (None = 3); leave None with convention='prodest'.",
          "type": "integer"
        },
        "proxy": {
          "default": "m",
          "description": "proxy parameter (str).",
          "type": "string"
        },
        "seed": {
          "description": "Random seed for reproducible stochastic steps.",
          "type": "integer"
        },
        "state": {
          "description": "Default ['k'].",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "time": {
          "default": "year",
          "description": "Time period column.",
          "type": "string"
        },
        "vce": {
          "default": "cluster",
          "description": "Analytic covariance: firm-clustered, robust, or unadjusted (convention='prodest' only).",
          "enum": [
            "cluster",
            "robust",
            "unadjusted"
          ],
          "type": "string"
        }
      },
      "required": [
        "data"
      ],
      "type": "object"
    }
  },
  {
    "description": "Alias for sp.ackerberg_caves_frazer. Ackerberg-Caves-Frazer (2015) production function estimator. Modern default. Corrects the OP/LP identification problem: all coefficient identification moves to stage 2, with free inputs instrumented by their lagged values and state inputs at the contemporaneous level. Aliased as sp.acf.",
    "name": "acf",
    "parameters": {
      "properties": {
        "boot_reps": {
          "default": 0,
          "description": "boot_reps parameter (int).",
          "type": "integer"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "free": {
          "description": "Default ['l']. Instrumented with lag in stage 2.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "functional_form": {
          "default": "cobb-douglas",
          "description": "Production function form",
          "enum": [
            "cobb-douglas",
            "translog"
          ],
          "type": "string"
        },
        "output": {
          "default": "y",
          "description": "output parameter (str).",
          "type": "string"
        },
        "panel_id": {
          "default": "id",
          "description": "panel_id parameter (str).",
          "type": "string"
        },
        "polynomial_degree": {
          "default": 3,
          "description": "polynomial_degree parameter (int).",
          "type": "integer"
        },
        "productivity_degree": {
          "default": 3,
          "description": "productivity_degree parameter (int).",
          "type": "integer"
        },
        "proxy": {
          "default": "m",
          "description": "proxy parameter (str).",
          "type": "string"
        },
        "seed": {
          "description": "Random seed for reproducible stochastic steps.",
          "type": "integer"
        },
        "state": {
          "description": "Default ['k'].",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "time": {
          "default": "year",
          "description": "Time period column.",
          "type": "string"
        }
      },
      "required": [
        "data"
      ],
      "type": "object"
    }
  },
  {
    "description": "Alias for sp.olley_pakes. Olley-Pakes (1996) production function estimator. Two-stage control function with INVESTMENT as the productivity proxy. Drops zero-investment firms (required for the inversion). Note: beta_l identification can fail if labor responds to current productivity (ACF critique) -- prefer sp.acf for modern work.",
    "name": "opreg",
    "parameters": {
      "properties": {
        "boot_reps": {
          "default": 0,
          "description": "boot_reps parameter (int).",
          "type": "integer"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "drop_zero_proxy": {
          "default": true,
          "description": "drop_zero_proxy parameter (bool).",
          "type": "boolean"
        },
        "free": {
          "description": "Default ['l'].",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "functional_form": {
          "default": "cobb-douglas",
          "description": "Production function form (translog: use ackerberg_caves_frazer)",
          "enum": [
            "cobb-douglas"
          ],
          "type": "string"
        },
        "output": {
          "default": "y",
          "description": "output parameter (str).",
          "type": "string"
        },
        "panel_id": {
          "default": "id",
          "description": "panel_id parameter (str).",
          "type": "string"
        },
        "polynomial_degree": {
          "default": 3,
          "description": "polynomial_degree parameter (int).",
          "type": "integer"
        },
        "productivity_degree": {
          "default": 3,
          "description": "productivity_degree parameter (int).",
          "type": "integer"
        },
        "proxy": {
          "default": "i",
          "description": "Investment column; rows <= 0 dropped unless drop_zero_proxy=False.",
          "type": "string"
        },
        "seed": {
          "description": "Random seed for reproducible stochastic steps.",
          "type": "integer"
        },
        "state": {
          "description": "Default ['k'].",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "time": {
          "default": "year",
          "description": "Time period column.",
          "type": "string"
        }
      },
      "required": [
        "data"
      ],
      "type": "object"
    }
  },
  {
    "description": "Alias for sp.levinsohn_petrin. Levinsohn-Petrin (2003) production function estimator. Uses intermediate inputs (materials/energy) as proxy -- avoids the OP zero-investment selection problem. Same ACF caveat applies to beta_l identification: prefer sp.acf for rigorous identification.",
    "name": "levpet",
    "parameters": {
      "properties": {
        "boot_reps": {
          "default": 0,
          "description": "boot_reps parameter (int).",
          "type": "integer"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "free": {
          "description": "Default ['l'].",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "functional_form": {
          "default": "cobb-douglas",
          "description": "Production function form (translog: use ackerberg_caves_frazer)",
          "enum": [
            "cobb-douglas"
          ],
          "type": "string"
        },
        "output": {
          "default": "y",
          "description": "output parameter (str).",
          "type": "string"
        },
        "panel_id": {
          "default": "id",
          "description": "panel_id parameter (str).",
          "type": "string"
        },
        "polynomial_degree": {
          "default": 3,
          "description": "polynomial_degree parameter (int).",
          "type": "integer"
        },
        "productivity_degree": {
          "default": 3,
          "description": "productivity_degree parameter (int).",
          "type": "integer"
        },
        "proxy": {
          "default": "m",
          "description": "Intermediate input.",
          "type": "string"
        },
        "seed": {
          "description": "Random seed for reproducible stochastic steps.",
          "type": "integer"
        },
        "state": {
          "description": "Default ['k'].",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "time": {
          "default": "year",
          "description": "Time period column.",
          "type": "string"
        }
      },
      "required": [
        "data"
      ],
      "type": "object"
    }
  },
  {
    "description": "De Loecker & Warzynski (2012) firm-time markup estimator. Takes a fitted ProductionResult plus revenue and input-cost columns; returns mu_it = theta_v_it * (PQ)/(P_v V) where theta_v is the output elasticity of the flexible input. Cobb-Douglas only for now (translog forthcoming). Validation: certified parity evidence.",
    "name": "markup",
    "parameters": {
      "properties": {
        "correct_eta": {
          "default": true,
          "description": "Subtract stage-1 i.i.d. shock from log revenue.",
          "type": "boolean"
        },
        "flexible_input": {
          "default": "m",
          "description": "flexible_input parameter (str).",
          "type": "string"
        },
        "input_cost": {
          "description": "Log expenditure on flexible input.",
          "type": "string"
        },
        "result": {
          "description": "result parameter (ProductionResult).",
          "type": "string"
        },
        "revenue": {
          "description": "Log revenue column.",
          "type": "string"
        }
      },
      "required": [
        "result",
        "revenue",
        "input_cost"
      ],
      "type": "object"
    }
  },
  {
    "description": "Augmented inverse-probability weighting (AIPW) -- the canonical doubly-robust ATE estimator. Cross-fits an outcome regression and a propensity model and combines them via the efficient-influence-function formula, so the estimate is consistent if either nuisance is correctly specified (Robins, Rotnitzky & Zhao 1994). Validation: certified parity evidence.",
    "name": "aipw",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "covariates": {
          "description": "Confounders to adjust for",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "cross_fit": {
          "default": true,
          "description": "Cross-fit the nuisance models over ``n_folds`` random folds (Chernozhukov et al. 2018).",
          "type": "boolean"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "estimand": {
          "default": "ATE",
          "description": "Target estimand",
          "enum": [
            "ATE",
            "ATT",
            "ATC"
          ],
          "type": "string"
        },
        "n_folds": {
          "default": 5,
          "description": "Cross-fitting folds (>= 2)",
          "type": "integer"
        },
        "se_method": {
          "default": "influence",
          "description": "``'influence'`` reports ``sd(psi) / sqrt(n)`` from the estimated efficient influence function, treating the nuisance fits as known (the cross-fitting / DML convention; R ``AIPW`` reports the same quantity). ``'sandwich'`` stacks the logit score, the two OLS normal equations and the AIPW moment and reports the M-estimation sandwich (divisor ``n``), which accounts for the estimated nuisance parameters at finite ``n``.",
          "enum": [
            "influence",
            "sandwich"
          ],
          "type": "string"
        },
        "seed": {
          "description": "Random seed for reproducible stochastic steps.",
          "type": "integer"
        },
        "treat": {
          "description": "Binary treatment (0/1)",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "treat",
        "covariates"
      ],
      "type": "object"
    }
  },
  {
    "description": "Aggregate Callaway-Sant'Anna group-time ATTs into interpretable summaries -- overall ATT, event-study by relative time, group-specific ATT(g), or calendar-time ATT(t). Inference uses the multiplier bootstrap on the pre-stored influence functions, so SEs are correct under clustering at the unit level. Validation: validated evidence tier (known-truth, reference, external-parity, or Monte Carlo artifact).",
    "name": "aggte",
    "parameters": {
      "properties": {
        "agg_weights": {
          "default": "did",
          "description": "Aggregation-weight convention: R did's cohort shares, or Stata csdid's per-cell treated-observation counts (repeated cross-sections only; simple and group)",
          "enum": [
            "did",
            "csdid"
          ],
          "type": "string"
        },
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "balance_e": {
          "description": "For dynamic: cap event time at +/-balance_e for balanced panel",
          "type": "integer"
        },
        "boot_type": {
          "default": "multiplier",
          "description": "Bootstrap variant",
          "enum": [
            "multiplier"
          ],
          "type": "string"
        },
        "bstrap": {
          "default": true,
          "description": "bstrap parameter (bool).",
          "type": "boolean"
        },
        "cband": {
          "default": true,
          "description": "Uniform confidence band",
          "type": "boolean"
        },
        "max_e": {
          "default": Infinity,
          "description": "max_e parameter (float).",
          "type": "number"
        },
        "min_e": {
          "default": -Infinity,
          "description": "min_e parameter (float).",
          "type": "number"
        },
        "n_boot": {
          "default": 1000,
          "description": "Number of bootstrap replications.",
          "type": "integer"
        },
        "na_rm": {
          "default": true,
          "description": "Drop ATT(g,t) cells with missing / infinite SE before aggregating",
          "type": "boolean"
        },
        "random_state": {
          "description": "Random seed or RandomState for reproducible stochastic steps.",
          "type": "integer"
        },
        "result": {
          "description": "Output of sp.callaway_santanna or sp.did with staggered=True",
          "type": "string"
        },
        "share_variance": {
          "default": true,
          "description": "Carry the estimated-cohort-share term (R did:::wif) into the aggregated variance; False holds the shares fixed as Stata csdid's estat group does",
          "type": "boolean"
        },
        "type": {
          "default": "simple",
          "description": "Aggregation type",
          "enum": [
            "simple",
            "dynamic",
            "group",
            "calendar"
          ],
          "type": "string"
        }
      },
      "required": [
        "result"
      ],
      "type": "object"
    }
  },
  {
    "description": "Conley-Taber (2011) and Ferman-Pinto (2019) inference for a DiD with few treated groups: the placebo distribution of the coefficient is read off the control groups, the interval is inverted from it, and Ferman-Pinto rescales the draws for the heteroskedasticity unequal group sizes generate. Use when one or a handful of clusters are treated, where the cluster-robust variance over-rejects badly.",
    "name": "did_few_treated",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "One minus the confidence level",
          "type": "number"
        },
        "covariates": {
          "description": "Covariates partialled out",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "data": {
          "description": "Group-by-period panel",
          "type": "string"
        },
        "group_size": {
          "description": "Observations behind each cell (required by ferman_pinto)",
          "type": "string"
        },
        "id": {
          "description": "Group identifier",
          "type": "string"
        },
        "max_draws": {
          "default": 10000,
          "description": "Cap on control-group combinations",
          "type": "integer"
        },
        "method": {
          "default": "conley_taber",
          "description": "Placebo construction",
          "enum": [
            "conley_taber",
            "ferman_pinto"
          ],
          "type": "string"
        },
        "null_value": {
          "default": 0.0,
          "description": "Null the p-value tests",
          "type": "number"
        },
        "seed": {
          "default": 0,
          "description": "Seed for random combinations",
          "type": "integer"
        },
        "time": {
          "description": "Period column",
          "type": "string"
        },
        "treat": {
          "description": "0/1 treatment by group and period",
          "type": "string"
        },
        "y": {
          "description": "Outcome column",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "id",
        "time",
        "treat"
      ],
      "type": "object"
    }
  },
  {
    "description": "Calibrated placebo simulation for DiD estimator selection: strip the estimated effect out of the observed panel, redraw the adoption pattern, inject a known effect, and refit every candidate estimator. Returns bias, RMSE, coverage and rejection rate with Monte Carlo standard errors, so the choice of estimator is settled on this panel rather than by citation.",
    "name": "did_calibrated_simulation",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "One minus the nominal level",
          "type": "number"
        },
        "assignment": {
          "default": "resample_cohorts",
          "description": "How the adoption pattern is redrawn",
          "enum": [
            "resample_cohorts",
            "random_timing",
            "observed"
          ],
          "type": "string"
        },
        "calibrate": {
          "default": "imputation",
          "description": "Removal of the observed effect before injecting a known one",
          "enum": [
            "imputation",
            "none"
          ],
          "type": "string"
        },
        "cohort": {
          "description": "Period of first treatment; 0/NaN/inf = never",
          "type": "string"
        },
        "control_group": {
          "default": "nevertreated",
          "description": "Comparison group handed to the estimators that take one",
          "enum": [
            "nevertreated",
            "notyettreated"
          ],
          "type": "string"
        },
        "data": {
          "description": "Long unit x period panel",
          "type": "string"
        },
        "effect": {
          "default": 0.0,
          "description": "Effect injected into treated cells (0 = a size study); a callable of the horizon, or of (cohort, period), gives a heterogeneous one",
          "type": "number"
        },
        "estimators": {
          "description": "Candidates to score",
          "enum": [
            "twfe",
            "callaway_santanna",
            "sun_abraham",
            "did_imputation",
            "gardner_did",
            "etwfe",
            "did_multiplegt_dyn",
            "stacked_did",
            "lp_did"
          ],
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "id": {
          "description": "Unit identifier",
          "type": "string"
        },
        "n_jobs": {
          "default": 1,
          "description": "Replications in parallel (-1 = all CPUs)",
          "type": "integer"
        },
        "n_sims": {
          "default": 200,
          "description": "Replications",
          "type": "integer"
        },
        "resample": {
          "default": "units",
          "description": "Outcome noise: unit residual paths, a unit-level Rademacher multiplier, or none",
          "enum": [
            "units",
            "wild",
            "none"
          ],
          "type": "string"
        },
        "seed": {
          "default": 0,
          "description": "Base seed; draw k uses seed + k",
          "type": "integer"
        },
        "time": {
          "description": "Period column",
          "type": "string"
        },
        "y": {
          "description": "Outcome column",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "id",
        "time",
        "cohort"
      ],
      "type": "object"
    }
  },
  {
    "description": "Event-study coefficients and their joint covariance from any DiD event-study fit (callaway_santanna/aggte, event_study, sun_abraham, gardner_did, did_imputation, stacked_did, lp_did, did_multiplegt_dyn, etwfe). Feeds uniform bands and HonestDiD; flags joint=False when only diagonal or block-diagonal covariance exists.",
    "name": "event_study_vcov",
    "parameters": {
      "properties": {
        "allow_diagonal": {
          "default": true,
          "description": "Fall back to diag(se^2) with a warning when no joint covariance exists",
          "type": "boolean"
        },
        "result": {
          "description": "Fitted event-study estimator",
          "type": "string"
        }
      },
      "required": [
        "result"
      ],
      "type": "object"
    }
  },
  {
    "description": "Sup-t simultaneous confidence band for an event study from any DiD estimator: critical value = 1-alpha quantile of max|Z| with Z ~ N(0, R), R the correlation of the covered coefficients. Falls back to the conservative Sidak value when the estimator exposes no joint covariance.",
    "name": "uniform_bands",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "One minus simultaneous coverage",
          "type": "number"
        },
        "n_draws": {
          "default": 100000,
          "description": "Gaussian draws for the critical value",
          "type": "integer"
        },
        "result": {
          "description": "Fitted event-study estimator",
          "type": "string"
        },
        "seed": {
          "default": 0,
          "description": "Seed for the draws",
          "type": "integer"
        },
        "which": {
          "default": "all",
          "description": "Event times covered jointly",
          "enum": [
            "all",
            "post",
            "pre"
          ],
          "type": "string"
        },
        "window": {
          "description": "Restrict covered event times to lo <= k <= hi",
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "required": [
        "result"
      ],
      "type": "object"
    }
  },
  {
    "description": "Delete-one-cluster jackknife (CV3) standard error for a Callaway-Sant'Anna aggregate: drop each cluster, re-estimate every ATT(g,t) and the cohort shares, re-aggregate, and report (R-1)/R * sum (ATT_(-h) - ATT)^2 with t(R-1) inference. The few-cluster / few-treated-cluster alternative to the analytic and multiplier-bootstrap SEs, matching R didjack and Stata csdidjack.",
    "name": "cs_jackknife",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Level of the t(R-1) interval",
          "type": "number"
        },
        "cluster": {
          "description": "Time-invariant cluster variable; defaults to the unit",
          "type": "string"
        },
        "data": {
          "description": "Balanced long panel",
          "type": "string"
        },
        "g": {
          "description": "First-treatment period (0 = never treated)",
          "type": "string"
        },
        "id": {
          "description": "Unit column",
          "type": "string"
        },
        "time": {
          "description": "Time column",
          "type": "string"
        },
        "type": {
          "default": "simple",
          "description": "Aggregation whose overall summary is jackknifed",
          "enum": [
            "simple",
            "dynamic",
            "group",
            "calendar"
          ],
          "type": "string"
        },
        "y": {
          "description": "Outcome column",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "g",
        "time",
        "id"
      ],
      "type": "object"
    }
  },
  {
    "description": "Export the per-unit influence functions of a Callaway-Sant'Anna fit as a tidy, self-contained DataFrame (optionally written to disk) -- the StatsPAI equivalent of Stata csdid saverif(). Feed the export to sp.aggte_from_influence for post-hoc custom aggregation without refitting or re-loading the data. Validation: validated evidence tier (known-truth, reference, external-parity, or Monte Carlo artifact).",
    "name": "influence_functions",
    "parameters": {
      "properties": {
        "path": {
          "description": "Optional file path -- .parquet via to_parquet, anything else via to_csv",
          "type": "string"
        },
        "result": {
          "description": "Output of sp.callaway_santanna",
          "type": "string"
        }
      },
      "required": [
        "result"
      ],
      "type": "object"
    }
  },
  {
    "description": "Aggregate Callaway-Sant'Anna group-time ATTs directly from an influence-function export (DataFrame or file path from sp.influence_functions) -- event-study, group, calendar, or overall summaries with multiplier-bootstrap inference, no refit and no original data required. The post-hoc half of the Stata csdid saverif() workflow. Validation: validated evidence tier (known-truth, reference, external-parity, or Monte Carlo artifact).",
    "name": "aggte_from_influence",
    "parameters": {
      "properties": {
        "source": {
          "description": "Frame from sp.influence_functions, or path to one (.parquet or CSV)",
          "type": "string"
        },
        "type": {
          "default": "simple",
          "description": "Aggregation type",
          "enum": [
            "simple",
            "dynamic",
            "group",
            "calendar"
          ],
          "type": "string"
        }
      },
      "required": [
        "source"
      ],
      "type": "object"
    }
  },
  {
    "description": "Efficient DiD for a randomised staggered rollout (Roth & Sant'Anna 2023). Identifies off random adoption *timing*, not parallel trends, so it is the right estimator for policy lotteries, phased launches and wave-randomised RCTs -- and the wrong one for observational rollouts. Uses the cohort's pre-treatment moments as optimal controls; efficient=False gives the plug-in. Validation: certified parity evidence. Do NOT use when: adoption timing was not randomised -- use a parallel-trends estimator such as sp.callaway_santanna instead.",
    "name": "staggered_rollout",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "efficient": {
          "default": true,
          "description": "Use the optimal pre-period control weights; False gives the plug-in estimator (R's beta=1)",
          "type": "boolean"
        },
        "estimand": {
          "default": "simple",
          "description": "Weighting: 'simple' (per treated cell), 'cohort' (within-cohort average first), 'calendar' (within-period average first), 'eventstudy' (ATT event_time periods after adoption)",
          "enum": [
            "simple",
            "cohort",
            "calendar",
            "eventstudy"
          ],
          "type": "string"
        },
        "event_time": {
          "default": 0,
          "description": "Only read when estimand='eventstudy'. A list returns one row per event time in .detail with the joint covariance in model_info['vcov']",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "fisher": {
          "default": false,
          "description": "Run a Fisher randomisation test by permuting adoption dates across units; p-value in model_info['fisher_pvalue']",
          "type": "boolean"
        },
        "g": {
          "description": "First-treatment period; never-treated may be 0, NaN or inf",
          "type": "string"
        },
        "i": {
          "description": "Unit identifier",
          "type": "string"
        },
        "n_fisher": {
          "default": 500,
          "description": "Permutation draws for the randomisation test",
          "type": "integer"
        },
        "random_state": {
          "description": "Seed for the permutation draws",
          "type": "integer"
        },
        "se_type": {
          "default": "neyman",
          "description": "Which SE lands in .se: 'neyman' is the conservative bound; 'adjusted' subtracts the variance the randomisation identifies and is what R staggered prints. Both are always in model_info",
          "enum": [
            "neyman",
            "adjusted"
          ],
          "type": "string"
        },
        "t": {
          "description": "Time period",
          "type": "string"
        },
        "use_did_a0": {
          "default": true,
          "description": "Which controls the efficient weights are chosen over. True uses the single DiD contrast at g-1; False uses every pre-period as a separate control (the general form, weakly more efficient). False requires efficient=True",
          "type": "boolean"
        },
        "use_last_treated_only": {
          "default": false,
          "description": "Restrict controls to the last-treated cohort (the Sun-Abraham comparison group) instead of every not-yet-treated cohort",
          "type": "boolean"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "i",
        "t",
        "g"
      ],
      "type": "object"
    }
  },
  {
    "description": "Callaway-Sant'Anna's estimand with **design-based** inference (Roth & Sant'Anna 2023). Same weights as sp.callaway_santanna -- every not-yet-treated cohort is a control -- but the standard error comes from random adoption timing rather than parallel trends. Use when timing was randomised and you want the familiar CS estimand; use sp.callaway_santanna when it was not. Units already treated in the first period are dropped, since ATT(g,t) is not identified for them. Validation: certified parity evidence. Do NOT use when: adoption timing was not randomised -- use sp.callaway_santanna, whose inference rests on parallel trends instead.",
    "name": "staggered_cs",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "estimand": {
          "default": "simple",
          "description": "Weighting scheme, as in sp.staggered_rollout",
          "enum": [
            "simple",
            "cohort",
            "calendar",
            "eventstudy"
          ],
          "type": "string"
        },
        "event_time": {
          "default": 0,
          "description": "event_time parameter (float or list).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "fisher": {
          "default": false,
          "description": "fisher parameter (bool).",
          "type": "boolean"
        },
        "g": {
          "description": "First-treatment period; never-treated may be 0, NaN or inf",
          "type": "string"
        },
        "i": {
          "description": "Unit identifier",
          "type": "string"
        },
        "n_fisher": {
          "default": 500,
          "description": "Number of fisher.",
          "type": "integer"
        },
        "random_state": {
          "description": "Random seed or RandomState for reproducible stochastic steps.",
          "type": "integer"
        },
        "se_type": {
          "default": "neyman",
          "description": "Conservative bound, or the adjusted SE R staggered prints",
          "enum": [
            "neyman",
            "adjusted"
          ],
          "type": "string"
        },
        "t": {
          "description": "Time period",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "i",
        "t",
        "g"
      ],
      "type": "object"
    }
  },
  {
    "description": "Sun-Abraham's estimand with **design-based** inference (Roth & Sant'Anna 2023). Identical to sp.staggered_cs except that only the last-treated cohort serves as control, which is what Sun & Abraham's interaction-weighted estimator does. Inference identifies off random adoption timing, not parallel trends. Validation: certified parity evidence. Do NOT use when: adoption timing was not randomised -- use sp.sun_abraham, whose inference rests on parallel trends instead.",
    "name": "staggered_sa",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "estimand": {
          "default": "simple",
          "description": "Weighting scheme, as in sp.staggered_rollout",
          "enum": [
            "simple",
            "cohort",
            "calendar",
            "eventstudy"
          ],
          "type": "string"
        },
        "event_time": {
          "default": 0,
          "description": "event_time parameter (float or list).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "fisher": {
          "default": false,
          "description": "fisher parameter (bool).",
          "type": "boolean"
        },
        "g": {
          "description": "First-treatment period; never-treated may be 0, NaN or inf",
          "type": "string"
        },
        "i": {
          "description": "Unit identifier",
          "type": "string"
        },
        "n_fisher": {
          "default": 500,
          "description": "Number of fisher.",
          "type": "integer"
        },
        "random_state": {
          "description": "Random seed or RandomState for reproducible stochastic steps.",
          "type": "integer"
        },
        "se_type": {
          "default": "neyman",
          "description": "Conservative bound, or the adjusted SE R staggered prints",
          "enum": [
            "neyman",
            "adjusted"
          ],
          "type": "string"
        },
        "t": {
          "description": "Time period",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "i",
        "t",
        "g"
      ],
      "type": "object"
    }
  },
  {
    "description": "Detect non-absorbing (reverting) treatment in a panel. Cohort-based DiD estimators (callaway_santanna, sun_abraham, did_imputation, etwfe, stacked_did) represent treatment by the first-treated period, which is lossless only when treatment never turns off. Under reversal they treat post-reversal periods as still-treated and are biased toward zero -- on a 150-unit panel with a third of units reverting, callaway_santanna returns 0.71 against a true ATT of 1.5, silently, because it never sees the time-varying indicator. Run this on the raw panel before picking an estimator. Validation: validated evidence tier (known-truth, reference, external-parity, or Monte Carlo artifact).",
    "name": "check_absorbing",
    "parameters": {
      "properties": {
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "strict": {
          "default": false,
          "description": "Raise MethodIncompatibility instead of returning when treatment reverts; use as a guard in front of a cohort-based estimator.",
          "type": "boolean"
        },
        "time": {
          "description": "Time column",
          "type": "string"
        },
        "treat": {
          "description": "Time-varying 0/1 treatment indicator. A cohort / first-treatment column cannot express reversal, so passing one makes the check meaningless.",
          "type": "string"
        },
        "unit": {
          "description": "Unit id column",
          "type": "string"
        }
      },
      "required": [
        "data",
        "unit",
        "time",
        "treat"
      ],
      "type": "object"
    }
  },
  {
    "description": "Pre-trend equivalence tests (Liu, Wang & Xu 2024, the fect diagnostic panel). Reverses the usual null: instead of testing whether pre-period effects are zero, tests whether they are demonstrably *small*. Failing to reject 'no pre-trend' is often just low power (Roth 2022), so the conventional test alone overstates the evidence for parallel trends. Reports the joint F test alongside its non-central-F and TOST equivalence counterparts, where a *small* p-value is the reassuring outcome. Validation: validated evidence tier (known-truth, reference, external-parity, or Monte Carlo artifact). Known limitations: the TOST is computed only when tost_threshold is supplied; there is no universal outcome-scale default, so it is not invented.",
    "name": "pretrends_equivalence",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "f_threshold": {
          "default": 0.6,
          "description": "Dimensionless effect-size bound for the F equivalence test (fect's default)",
          "type": "number"
        },
        "result": {
          "description": "DiD result carrying an event study and influence functions (e.g. sp.callaway_santanna)",
          "type": "string"
        },
        "tost_threshold": {
          "description": "Equivalence bound in outcome units. Omitted by default because there is no defensible universal scale for 'negligible pre-trend'; the TOST is skipped when it is not supplied. fect uses 0.36 * residual SD.",
          "type": "number"
        }
      },
      "required": [
        "result"
      ],
      "type": "object"
    }
  },
  {
    "description": "Butts spillover-ring DiD. The usual fix -- a spatial lag of treatment in a TWFE regression -- measures the direct effect against controls the spillover already reached. This sorts untreated units by distance to the nearest treated unit into spillover rings plus CLEAN controls beyond every ring, and estimates the direct effect and each ring's effect against the clean controls only. Validation: certified evidence with scoped limitations. Known limitations: there is no reference implementation, so this carries design-recovery evidence only and no cross-language parity; ring boundaries are the analyst's choice; there is no selector, and a too-wide outer ring silently contaminates the clean controls; covariate adjustment is not implemented.",
    "name": "spillover_did",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "cohort": {
          "description": "First-treatment period (never_value = never)",
          "type": "string"
        },
        "coords": {
          "description": "Two columns giving each unit's position (Euclidean)",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "distances": {
          "description": "Pre-computed distance matrix, for great-circle or network distances",
          "type": "string"
        },
        "never_value": {
          "default": 0,
          "description": "never_value parameter.",
          "type": "string"
        },
        "ring_edges": {
          "default": [
            0.0,
            1.0
          ],
          "description": "Ring boundaries; untreated units beyond the last edge are the clean controls",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "time": {
          "description": "Time period column.",
          "type": "string"
        },
        "unit": {
          "description": "Unit identifier column.",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "unit",
        "time",
        "cohort"
      ],
      "type": "object"
    }
  },
  {
    "description": "Callaway, Goodman-Bacon & Sant'Anna (2024) DiD with a CONTINUOUS treatment. A dose has no single ATT: the TWFE coefficient averages the 0.2-dose and 0.8-dose comparisons with weights that can be negative. Reports ATT(d) and its derivative ACRT(d) -- the causal response at dose d, which is what a marginal-dose question asks -- from a B-spline regression of the outcome change on the dose. Validation: certified evidence with scoped limitations. Known limitations: standard errors come from the per-cell influence function; contdid routes its own through the pte aggregation layer, which is not implemented here; staggered designs aggregate cells with StatsPAI's own treated-count weights; only the per-cell estimator is pinned against the reference; the cck (nonparametric) dose estimator is not implemented.",
    "name": "cgs_continuous_did",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "cohort": {
          "description": "First-treatment period (0 = never treated)",
          "type": "string"
        },
        "control_group": {
          "default": "nevertreated",
          "description": "control_group parameter (str).",
          "enum": [
            "nevertreated",
            "notyettreated"
          ],
          "type": "string"
        },
        "curve_basis": {
          "default": "fitted",
          "description": "'fitted' evaluates the curves on the basis they were fitted on; 'reference' re-anchors to the dose grid to reproduce contdid 0.1.1's reported curves, which are a rescaled version of the fitted response",
          "enum": [
            "fitted",
            "reference"
          ],
          "type": "string"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "degree": {
          "default": 3,
          "description": "B-spline degree; degree=1 with no knots gives a constant ACRT, the 'effect per unit of dose' reading",
          "type": "integer"
        },
        "dose": {
          "description": "Treatment intensity (0 = untreated)",
          "type": "string"
        },
        "dose_grid": {
          "description": "Doses to report the curves at (default: 10th-99th pct)",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "knots": {
          "description": "Explicit interior knots",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "num_knots": {
          "default": 0,
          "description": "Interior knots at dose quantiles; more buys flexibility at the cost of variance and there is no auto-selector",
          "type": "integer"
        },
        "time": {
          "description": "Time period column.",
          "type": "string"
        },
        "unit": {
          "description": "Unit identifier column.",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "dose",
        "time",
        "unit",
        "cohort"
      ],
      "type": "object"
    }
  },
  {
    "description": "Covariate balance for a DiD design, in the shape Baker et al. (2026, Table 4) report it: Imbens-Rubin normalized differences computed twice -- once on baseline covariate LEVELS and once on covariate CHANGES across the treatment date -- optionally weighted and unweighted side by side. The changes panel is the informative half: DiD identifies off trends, so a covariate that is balanced in levels can still be moving differentially, and imbalances routinely flip sign between the two panels. Flags |norm. diff| > 0.25. Evidence about whether UNCONDITIONAL parallel trends is plausible; it cannot test parallel trends itself. Validation: validated evidence tier (known-truth, reference, external-parity, or Monte Carlo artifact). Known limitations: pooled multi-cohort balance is not implemented: one table per treated cohort only, because the normalized difference is a two-group statistic; only the reliability-weight variance correction is implemented for the weighted panel; survey-design (replicate-weight) variances are not supported; inference is not implemented: the normalized difference is reported as a descriptive effect size with no standard error or test, by design; the weighted denominator convention differs from cobalt::col_w_smd, which holds the pooled SD at its unweighted value; this follows Baker et al. (2026) instead, and the two coincide when unweighted.",
    "name": "did_balance",
    "parameters": {
      "properties": {
        "base_period": {
          "description": "Pre-treatment period for the levels panel (default g-1)",
          "type": "string"
        },
        "cohort": {
          "description": "Treated cohort to audit (default: the largest)",
          "type": "string"
        },
        "comparison_period": {
          "description": "Second period for the changes panel (default g)",
          "type": "string"
        },
        "control_group": {
          "default": "nevertreated",
          "description": "Must match the comparison group of the estimator you intend to run",
          "enum": [
            "nevertreated",
            "notyettreated"
          ],
          "type": "string"
        },
        "covariates": {
          "description": "Columns to audit in levels and in changes",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "g": {
          "description": "First-treatment period (0 = never treated)",
          "type": "string"
        },
        "i": {
          "description": "Unit identifier",
          "type": "string"
        },
        "t": {
          "description": "t parameter (str).",
          "type": "string"
        },
        "threshold": {
          "default": 0.25,
          "description": "Imbens & Rubin (2015, p. 277) rule of thumb",
          "type": "number"
        },
        "weights": {
          "description": "Unit weights; when given, weighted and unweighted statistics are reported side by side because they describe different populations",
          "type": "string"
        }
      },
      "required": [
        "data",
        "covariates",
        "g",
        "t",
        "i"
      ],
      "type": "object"
    }
  },
  {
    "description": "Treatment effect on the *distribution* of the outcome, bin by bin. Bins the outcome, runs Callaway-Sant'Anna on each bin indicator, and reports the effect on P(Y in bin). The per-bin effects sum to zero by construction -- treatment redistributes probability mass, it does not create it -- so the content is the SHAPE: which parts of the outcome distribution gained and which lost. A mean ATT of zero is perfectly consistent with large offsetting movements in the tails, and this is what shows them. R didFF::distDD. Validation: certified evidence with scoped limitations. Known limitations: reports point estimates and standard errors only; the reference runs no test here and neither does this; simultaneous (uniform) confidence bands over bins are not implemented; the standard errors are pointwise only, so reading several bins at once overstates joint confidence.",
    "name": "distributional_did",
    "parameters": {
      "properties": {
        "aggregation": {
          "default": "group",
          "description": "Which aggte aggregation defines the per-bin effect",
          "enum": [
            "group",
            "simple",
            "dynamic",
            "calendar"
          ],
          "type": "string"
        },
        "allow_unbalanced_panel": {
          "default": false,
          "description": "allow_unbalanced_panel parameter (bool).",
          "type": "boolean"
        },
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "anticipation": {
          "default": 0,
          "description": "anticipation parameter (int).",
          "type": "integer"
        },
        "balance_e": {
          "description": "balance_e parameter (int).",
          "type": "integer"
        },
        "binpoints": {
          "description": "Explicit bin edges",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "control_group": {
          "default": "nevertreated",
          "description": "control_group parameter (str).",
          "type": "string"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "estimator": {
          "default": "dr",
          "description": "estimator parameter (str).",
          "type": "string"
        },
        "g": {
          "description": "First-treatment period (0 = never treated)",
          "type": "string"
        },
        "i": {
          "description": "Unit identifier",
          "type": "string"
        },
        "max_e": {
          "description": "max_e parameter (float).",
          "type": "number"
        },
        "min_e": {
          "description": "min_e parameter (float).",
          "type": "number"
        },
        "n_bins": {
          "default": "auto",
          "description": "Equal-width bins, or 'auto' for the didFF rule. Bins span the WHOLE panel here (unlike functional_form_test, which bins untreated rows only): the estimand is about where treated mass ended up",
          "type": "integer"
        },
        "panel": {
          "default": true,
          "description": "panel parameter (bool).",
          "type": "boolean"
        },
        "t": {
          "description": "t parameter (str).",
          "type": "string"
        },
        "weights": {
          "description": "Sampling-weight column",
          "type": "string"
        },
        "x": {
          "description": "Primary running variable, regressor, or feature input for this estimator.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "g",
        "t",
        "i"
      ],
      "type": "object"
    }
  },
  {
    "description": "Roth & Sant'Anna (2023) test of whether parallel trends can hold for EVERY strictly monotonic transformation of the outcome. Bins the outcome, recovers the counterfactual probability mass the design implies for the treated group in each bin via Callaway-Sant'Anna, and tests the moment inequalities that mass must satisfy to be a density. Rejection means levels and logs are answering different questions, so the functional form is doing identifying work. Validation: certified evidence with scoped limitations. Known limitations: a large p-value is only a failure to reject, not evidence FOR functional-form insensitivity: the test has little power with few units or coarse bins; standard errors and the critical value are asymptotic; a bootstrap variant is not implemented.",
    "name": "functional_form_test",
    "parameters": {
      "properties": {
        "aggregation": {
          "default": "group",
          "description": "Which aggte aggregation defines the implied density",
          "enum": [
            "group",
            "simple",
            "dynamic",
            "calendar"
          ],
          "type": "string"
        },
        "allow_unbalanced_panel": {
          "default": false,
          "description": "allow_unbalanced_panel parameter (bool).",
          "type": "boolean"
        },
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "anticipation": {
          "default": 0,
          "description": "anticipation parameter (int).",
          "type": "integer"
        },
        "balance_e": {
          "description": "Only bites under aggregation='dynamic': keep cohorts observed for this many event times",
          "type": "integer"
        },
        "binpoints": {
          "description": "Explicit bin edges, padded to cover the outcome range if they fall short; cannot be combined with n_bins",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "control_group": {
          "default": "nevertreated",
          "description": "control_group parameter (str).",
          "type": "string"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "estimator": {
          "default": "dr",
          "description": "estimator parameter (str).",
          "type": "string"
        },
        "g": {
          "description": "First-treatment period (0 = never treated)",
          "type": "string"
        },
        "i": {
          "description": "Unit identifier",
          "type": "string"
        },
        "max_e": {
          "description": "Only bites under aggregation='dynamic': latest event time entering the aggregate",
          "type": "number"
        },
        "min_e": {
          "description": "Only bites under aggregation='dynamic': earliest event time entering the aggregate",
          "type": "number"
        },
        "n_bins": {
          "default": "auto",
          "description": "Equal-width outcome bins; too coarse a grid buys a large p-value for nothing. 'auto' follows didFF: an outcome with fewer than 20 distinct untreated values is treated as discrete (one bin per value), otherwise it is cut into min(20, n_distinct) bins",
          "type": "integer"
        },
        "n_sims": {
          "default": 100000,
          "description": "Draws behind the least-favourable critical value",
          "type": "integer"
        },
        "panel": {
          "default": true,
          "description": "panel parameter (bool).",
          "type": "boolean"
        },
        "random_state": {
          "default": 0,
          "description": "Random seed or RandomState for reproducible stochastic steps.",
          "type": "integer"
        },
        "t": {
          "description": "t parameter (str).",
          "type": "string"
        },
        "weights": {
          "description": "Sampling-weight column; unset weights every unit equally",
          "type": "string"
        },
        "x": {
          "description": "Primary running variable, regressor, or feature input for this estimator.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "g",
        "t",
        "i"
      ],
      "type": "object"
    }
  },
  {
    "description": "Joint Wald test of pre-treatment ATTs (or event-study leads) against zero -- the canonical sanity check for the parallel-trends assumption in DiD designs. Failing to reject is necessary but not sufficient evidence for parallel trends; always pair with sp.honest_did / sp.sensitivity_rr for design-robust inference.",
    "name": "pretrends_test",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "result": {
          "description": "DiD or event-study result with pre-period coefficients",
          "type": "string"
        },
        "type": {
          "default": "wald",
          "description": "Test statistic",
          "enum": [
            "wald"
          ],
          "type": "string"
        }
      },
      "required": [
        "result"
      ],
      "type": "object"
    }
  },
  {
    "description": "Rambachan-Roth (2023) honest-DiD sensitivity analysis: computes the largest violation of parallel trends (parametrised by Mbar -- relative magnitude of the post-period violation versus the worst observed pre-period one) under which the post-treatment ATT is still different from zero at level alpha. Reports both the robust confidence sets and the breakdown Mbar.",
    "name": "sensitivity_rr",
    "parameters": {
      "properties": {
        "Mbar": {
          "description": "Grid of relative-magnitude bounds; default is np.linspace(0, 2, n_grid)",
          "type": "string"
        },
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "method": {
          "default": "C-LF",
          "description": "Identification method",
          "enum": [
            "C-LF"
          ],
          "type": "string"
        },
        "n_grid": {
          "default": 20,
          "description": "Mbar grid size when Mbar=None",
          "type": "integer"
        },
        "result": {
          "description": "Event-study or DiD result with full pre/post coefficients",
          "type": "string"
        }
      },
      "required": [
        "result"
      ],
      "type": "object"
    }
  },
  {
    "description": "McCrary (2008) density test for manipulation of the running variable at the cutoff in regression-discontinuity designs. A significant discontinuity in the density of x at c is direct evidence that units are sorting around the cutoff (e.g. test-taking strategy, income manipulation), invalidating local randomisation. Validation: certified parity evidence.",
    "name": "mccrary_test",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "bin_width": {
          "description": "Histogram bin width (``DCdensity``'s ``bin``). Default: ``2 sd(x) n^(-1/2)``.",
          "type": "string"
        },
        "bw": {
          "description": "Bandwidth; auto if None",
          "type": "number"
        },
        "c": {
          "default": 0.0,
          "description": "Cutoff value",
          "type": "number"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "n_bins": {
          "description": "Histogram bins; auto if None",
          "type": "integer"
        },
        "x": {
          "description": "Running variable",
          "type": "string"
        }
      },
      "required": [
        "data",
        "x"
      ],
      "type": "object"
    }
  },
  {
    "description": "Oster (2019) sensitivity to selection on unobservables -- computes the bounding coefficient under the assumption that selection on unobservables (proportional to delta x selection on observables) brings the explained variance to r_max. The breakdown delta tells you how strong unobserved selection has to be to overturn your result. Validation: certified parity evidence.",
    "name": "oster_bounds",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "beta_long": {
          "description": "beta_long parameter (float).",
          "type": "number"
        },
        "beta_short": {
          "description": "Short-regression coefficient; if None, fit from data",
          "type": "number"
        },
        "controls": {
          "description": "Control-variable column names.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "delta": {
          "default": 1.0,
          "description": "Ratio of unobserved-to-observed selection (1.0 = equally strong)",
          "type": "number"
        },
        "r2_long": {
          "description": "r2_long parameter (float).",
          "type": "number"
        },
        "r2_short": {
          "description": "r2_short parameter (float).",
          "type": "number"
        },
        "r_max": {
          "description": "Hypothetical R^2 from a regression that includes all unobserved confounders; default 1.3*R^2_long",
          "type": "number"
        },
        "treat": {
          "description": "Treatment indicator or first-treatment-period column.",
          "type": "string"
        },
        "y": {
          "description": "Outcome (alternative to passing beta_short/long directly)",
          "type": "string"
        }
      },
      "required": [],
      "type": "object"
    }
  },
  {
    "description": "Cameron-Gelbach-Miller (2008) wild cluster bootstrap -- the canonical fix for cluster-robust inference with few clusters (G < 30). Re-samples cluster-level Rademacher weights to construct a percentile-t reference distribution that has correct size when the standard cluster-robust z-test rejects too often. Validation: certified parity evidence. Do NOT use when: the number of clusters is large (say > 50) -- ordinary CRV1 standard errors are already reliable and much cheaper. Cost: Runtime is roughly n_boot x (one restricted refit). This is the intended trade for few-cluster validity -- do not raise n_boot inside an outer search loop.",
    "name": "wild_cluster_bootstrap",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "cluster": {
          "description": "Cluster identifier",
          "type": "string"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "h0": {
          "default": 0.0,
          "description": "Null value of the coefficient",
          "type": "number"
        },
        "n_boot": {
          "default": 999,
          "description": "Number of bootstrap replications.",
          "type": "integer"
        },
        "seed": {
          "description": "Random seed for reproducible stochastic steps.",
          "type": "integer"
        },
        "test_var": {
          "description": "Variable being tested; defaults to first in x",
          "type": "string"
        },
        "weight_type": {
          "default": "rademacher",
          "description": "Bootstrap weight distribution",
          "enum": [
            "rademacher",
            "mammen",
            "webb",
            "normal"
          ],
          "type": "string"
        },
        "x": {
          "description": "Right-hand-side variables",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Outcome variable",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "x",
        "cluster"
      ],
      "type": "object"
    }
  },
  {
    "description": "Armstrong-Kolesar (2018) honest confidence intervals for sharp regression discontinuity -- the only RD inference procedure with provable finite-sample coverage without bandwidth-selection bias. M is the upper bound on the second derivative of E[Y|X] near the cutoff; smaller M means tighter CIs but riskier coverage if the true curvature is larger. Validation: certified parity evidence.",
    "name": "rd_honest",
    "parameters": {
      "properties": {
        "M": {
          "description": "Upper bound on |E[Y|X]''| near c; if None, estimated from data",
          "type": "number"
        },
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "c": {
          "default": 0.0,
          "description": "Cutoff value",
          "type": "number"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "h": {
          "description": "Bandwidth; auto-selected by opt_criterion if None",
          "type": "number"
        },
        "kernel": {
          "default": "triangular",
          "description": "Local-linear kernel",
          "enum": [
            "triangular",
            "uniform",
            "epanechnikov"
          ],
          "type": "string"
        },
        "opt_criterion": {
          "default": "mse",
          "description": "Bandwidth optimization criterion",
          "enum": [
            "mse",
            "flci",
            "oci"
          ],
          "type": "string"
        },
        "sclass": {
          "default": "H",
          "description": "Smoothness class for the bound M: 'H' (Holder, RDHonest's default -- f' is M-Lipschitz) or 'T' (Taylor)",
          "enum": [
            "H",
            "T"
          ],
          "type": "string"
        },
        "x": {
          "description": "Running variable",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "x"
      ],
      "type": "object"
    }
  },
  {
    "description": "RD with flexible covariate adjustment via cross-fit ML residualisation (Noack-Olma-Rothe 2025). Reduces variance of tau at the cutoff by subtracting an ML estimate of E[Y|W] before running rdrobust; consistent under free-of-cutoff continuity of eta, asymptotically efficient when eta converges to E[Y|X=c, W].",
    "name": "rd_flex",
    "parameters": {
      "properties": {
        "W": {
          "description": "Covariates used by the flexible adjustment",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "c": {
          "default": 0.0,
          "description": "c parameter (float).",
          "type": "number"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "fuzzy": {
          "description": "fuzzy parameter (str).",
          "type": "string"
        },
        "kernel": {
          "default": "triangular",
          "description": "Kernel function used for weighting or smoothing.",
          "enum": [
            "triangular",
            "epanechnikov",
            "uniform"
          ],
          "type": "string"
        },
        "learner": {
          "default": "boost",
          "description": "Built-in learner",
          "enum": [
            "boost",
            "forest",
            "ridge",
            "lasso"
          ],
          "type": "string"
        },
        "n_folds": {
          "default": 5,
          "description": "Cross-fit folds (1 disables CV)",
          "type": "integer"
        },
        "x": {
          "description": "Primary running variable, regressor, or feature input for this estimator.",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "x"
      ],
      "type": "object"
    }
  },
  {
    "description": "Bias-aware confidence interval for fuzzy RD via Anderson-Rubin test inversion (Noack-Rothe 2024 Econometrica). Robust to weak first stages and avoids the power asymmetry of conventional 2SLS-style fuzzy RD CIs (Kaliski-Keane-Neal 2025). Validation: certified parity evidence.",
    "name": "rd_bias_aware_fuzzy",
    "parameters": {
      "properties": {
        "M_d": {
          "description": "Bound on |g_D''|; auto if None",
          "type": "number"
        },
        "M_y": {
          "description": "Bound on |g_Y''|; auto if None",
          "type": "number"
        },
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "c": {
          "default": 0.0,
          "description": "c parameter (float).",
          "type": "number"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "fuzzy": {
          "description": "Treatment indicator column",
          "type": "string"
        },
        "h": {
          "description": "Bandwidth used for local smoothing or kernel weighting.",
          "type": "number"
        },
        "kernel": {
          "default": "triangular",
          "description": "Kernel function used for weighting or smoothing.",
          "enum": [
            "triangular",
            "epanechnikov",
            "uniform"
          ],
          "type": "string"
        },
        "x": {
          "description": "Primary running variable, regressor, or feature input for this estimator.",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "x",
        "fuzzy"
      ],
      "type": "object"
    }
  },
  {
    "description": "Honest CI for RD when the running variable takes only a moderate number of distinct values (Kolesar-Rothe 2018 AER). Uses bounded second derivative or bounded misspecification smoothness classes; robust to the loss of asymptotics that affects rdrobust under sparse mass points. Validation: certified parity evidence.",
    "name": "rd_discrete",
    "parameters": {
      "properties": {
        "K": {
          "description": "Bound on per-side linear-approximation bias; auto if None (BM method)",
          "type": "number"
        },
        "M": {
          "description": "Bound on |g''|; auto if None (BSD method)",
          "type": "number"
        },
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "c": {
          "default": 0.0,
          "description": "c parameter (float).",
          "type": "number"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "h": {
          "description": "Bandwidth used for local smoothing or kernel weighting.",
          "type": "number"
        },
        "kernel": {
          "default": "triangular",
          "description": "``'bsd'`` kernel.",
          "enum": [
            "triangular",
            "uniform",
            "epanechnikov"
          ],
          "type": "string"
        },
        "method": {
          "default": "bsd",
          "description": "Smoothness class",
          "enum": [
            "bsd",
            "bm"
          ],
          "type": "string"
        },
        "opt_criterion": {
          "default": "MSE",
          "description": "``'bsd'`` bandwidth criterion when ``h`` is None.",
          "enum": [
            "MSE",
            "FLCI",
            "OCI"
          ],
          "type": "string"
        },
        "order": {
          "default": 0,
          "description": "``'bme'``: order of the polynomial on each side (RDHonestBME's ``order``; 0 compares side means).",
          "type": "integer"
        },
        "x": {
          "description": "Discrete running variable",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "x"
      ],
      "type": "object"
    }
  },
  {
    "description": "Unified results class for econometric models",
    "name": "EconometricResults",
    "parameters": {
      "properties": {
        "data_info": {
          "description": "data_info parameter (Optional[Dict[str, Any]]).",
          "type": "object"
        },
        "diagnostics": {
          "description": "diagnostics parameter (Optional[Dict[str, Any]]).",
          "type": "object"
        },
        "model_info": {
          "description": "model_info parameter (Dict[str, Any]).",
          "type": "object"
        },
        "params": {
          "description": "params parameter (Series).",
          "type": "string"
        },
        "std_errors": {
          "description": "std_errors parameter (Series).",
          "type": "string"
        }
      },
      "required": [
        "params",
        "std_errors",
        "model_info"
      ],
      "type": "object"
    }
  },
  {
    "description": "Unified result object for all causal inference methods in StatsPAI.",
    "name": "CausalResult",
    "parameters": {
      "properties": {
        "_citation_key": {
          "description": "Key into the citation registry.",
          "type": "string"
        },
        "_influence_funcs": {
          "description": "Influence function matrix (n_units, n_estimates) for joint inference.",
          "type": "string"
        },
        "alpha": {
          "description": "Significance level used for CI.",
          "type": "number"
        },
        "ci": {
          "description": "Confidence interval (lower, upper).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "detail": {
          "description": "Detailed estimates (e.g., group-time ATTs).",
          "type": "string"
        },
        "estimand": {
          "description": "What is being estimated ('ATT', 'ATE', 'LATE').",
          "type": "string"
        },
        "estimate": {
          "description": "Point estimate of the main treatment effect.",
          "type": "number"
        },
        "method": {
          "description": "Name of the estimation method (displayed in summary).",
          "type": "string"
        },
        "model_info": {
          "description": "Model metadata and aggregated results.",
          "type": "object"
        },
        "n_obs": {
          "description": "Number of observations.",
          "type": "integer"
        },
        "pvalue": {
          "description": "Two-sided p-value for H0: effect = 0.",
          "type": "number"
        },
        "se": {
          "description": "Standard error.",
          "type": "number"
        }
      },
      "required": [
        "method",
        "estimand",
        "estimate",
        "se",
        "pvalue",
        "ci",
        "alpha",
        "n_obs"
      ],
      "type": "object"
    }
  },
  {
    "description": "``float`` subclass carrying inference alongside a point effect.",
    "name": "ScalarEffect",
    "parameters": {
      "properties": {
        "ci": {
          "description": "ci parameter (Optional[tuple]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "detail": {
          "description": "Amount of result detail to return.",
          "type": "object"
        },
        "estimand": {
          "default": "effect",
          "description": "estimand parameter (str).",
          "type": "string"
        },
        "inference_error": {
          "description": "inference_error parameter (Optional[str]).",
          "type": "string"
        },
        "pvalue": {
          "description": "pvalue parameter (Optional[float]).",
          "type": "number"
        },
        "se": {
          "description": "se parameter (Optional[float]).",
          "type": "number"
        },
        "value": {
          "description": "value parameter (float).",
          "type": "number"
        }
      },
      "required": [
        "value"
      ],
      "type": "object"
    }
  },
  {
    "description": "Container for an effect summary table and human-readable text.",
    "name": "EffectSummary",
    "parameters": {
      "properties": {
        "kind": {
          "default": "effect_summary",
          "description": "kind parameter (str).",
          "type": "string"
        },
        "metadata": {
          "default": null,
          "description": "metadata parameter (Dict[str, Any]).",
          "type": "object"
        },
        "table": {
          "description": "table parameter (pd.DataFrame).",
          "type": "string"
        },
        "text": {
          "description": "text parameter (str).",
          "type": "string"
        }
      },
      "required": [
        "table",
        "text"
      ],
      "type": "object"
    }
  },
  {
    "description": "Build a decision-ready summary for a fitted causal result.",
    "name": "effect_summary",
    "parameters": {
      "properties": {
        "alpha": {
          "description": "Forwarded to frequentist ``CausalResult.decision_summary`` or used to describe Bayesian posterior ROPE probability when available.",
          "type": "number"
        },
        "direction": {
          "default": "two-sided",
          "description": "Directional posterior probability reported for Bayesian results.",
          "enum": [
            "increase",
            "decrease",
            "two-sided"
          ],
          "type": "string"
        },
        "result": {
          "description": "A StatsPAI result object. ``CausalResult`` and ``BayesianCausalResult`` are handled natively; any object exposing an ``effect_summary`` method is delegated to.",
          "type": "string"
        },
        "rope": {
          "description": "Forwarded to frequentist ``CausalResult.decision_summary`` or used to describe Bayesian posterior ROPE probability when available.",
          "type": "string"
        },
        "sesoi": {
          "description": "Forwarded to frequentist ``CausalResult.decision_summary`` or used to describe Bayesian posterior ROPE probability when available.",
          "type": "number"
        }
      },
      "required": [
        "result"
      ],
      "type": "object"
    }
  },
  {
    "description": "Base class for all StatsPAI exceptions.",
    "name": "StatsPAIError",
    "parameters": {
      "properties": {
        "alternative_functions": {
          "description": "Ranked list of ``sp.xxx`` names the caller may try next.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "diagnostics": {
          "description": "Machine-readable details (test statistic, threshold, variable names, etc.) so an agent can branch on specifics.",
          "type": "object"
        },
        "message": {
          "description": "Human-readable error message.",
          "type": "string"
        },
        "recovery_hint": {
          "default": "",
          "description": "One-sentence, actionable suggestion for how to recover.",
          "type": "string"
        }
      },
      "required": [
        "message"
      ],
      "type": "object"
    }
  },
  {
    "description": "An identifying assumption is violated by the data.",
    "name": "AssumptionViolation",
    "parameters": {
      "properties": {
        "alternative_functions": {
          "description": "alternative_functions parameter (Optional[List[str]]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "diagnostics": {
          "description": "diagnostics parameter (Optional[Dict[str, Any]]).",
          "type": "object"
        },
        "message": {
          "description": "message parameter (str).",
          "type": "string"
        },
        "recovery_hint": {
          "default": "",
          "description": "recovery_hint parameter (str).",
          "type": "string"
        }
      },
      "required": [
        "message"
      ],
      "type": "object"
    }
  },
  {
    "description": "The target estimand is not identified under the chosen design.",
    "name": "IdentificationFailure",
    "parameters": {
      "properties": {
        "alternative_functions": {
          "description": "alternative_functions parameter (Optional[List[str]]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "diagnostics": {
          "description": "diagnostics parameter (Optional[Dict[str, Any]]).",
          "type": "object"
        },
        "message": {
          "description": "message parameter (str).",
          "type": "string"
        },
        "recovery_hint": {
          "default": "",
          "description": "recovery_hint parameter (str).",
          "type": "string"
        }
      },
      "required": [
        "message"
      ],
      "type": "object"
    }
  },
  {
    "description": "Sample or design is too small / sparse for the chosen method.",
    "name": "DataInsufficient",
    "parameters": {
      "properties": {
        "alternative_functions": {
          "description": "alternative_functions parameter (Optional[List[str]]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "diagnostics": {
          "description": "diagnostics parameter (Optional[Dict[str, Any]]).",
          "type": "object"
        },
        "message": {
          "description": "message parameter (str).",
          "type": "string"
        },
        "recovery_hint": {
          "default": "",
          "description": "recovery_hint parameter (str).",
          "type": "string"
        }
      },
      "required": [
        "message"
      ],
      "type": "object"
    }
  },
  {
    "description": "An iterative algorithm did not converge.",
    "name": "ConvergenceFailure",
    "parameters": {
      "properties": {
        "alternative_functions": {
          "description": "alternative_functions parameter (Optional[List[str]]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "diagnostics": {
          "description": "diagnostics parameter (Optional[Dict[str, Any]]).",
          "type": "object"
        },
        "message": {
          "description": "message parameter (str).",
          "type": "string"
        },
        "recovery_hint": {
          "default": "",
          "description": "recovery_hint parameter (str).",
          "type": "string"
        }
      },
      "required": [
        "message"
      ],
      "type": "object"
    }
  },
  {
    "description": "Computation hit a numerical corner case.",
    "name": "NumericalInstability",
    "parameters": {
      "properties": {
        "alternative_functions": {
          "description": "alternative_functions parameter (Optional[List[str]]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "diagnostics": {
          "description": "diagnostics parameter (Optional[Dict[str, Any]]).",
          "type": "object"
        },
        "message": {
          "description": "message parameter (str).",
          "type": "string"
        },
        "recovery_hint": {
          "default": "",
          "description": "recovery_hint parameter (str).",
          "type": "string"
        }
      },
      "required": [
        "message"
      ],
      "type": "object"
    }
  },
  {
    "description": "The requested method is incompatible with the data / options.",
    "name": "MethodIncompatibility",
    "parameters": {
      "properties": {
        "alternative_functions": {
          "description": "alternative_functions parameter (Optional[List[str]]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "diagnostics": {
          "description": "diagnostics parameter (Optional[Dict[str, Any]]).",
          "type": "object"
        },
        "message": {
          "description": "message parameter (str).",
          "type": "string"
        },
        "recovery_hint": {
          "default": "",
          "description": "recovery_hint parameter (str).",
          "type": "string"
        }
      },
      "required": [
        "message"
      ],
      "type": "object"
    }
  },
  {
    "description": "Base warning class for soft (non-raising) StatsPAI diagnostics.",
    "name": "StatsPAIWarning",
    "parameters": {
      "properties": {
        "alternative_functions": {
          "description": "alternative_functions parameter (Optional[List[str]]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "diagnostics": {
          "description": "diagnostics parameter (Optional[Dict[str, Any]]).",
          "type": "object"
        },
        "message": {
          "default": "",
          "description": "message parameter (str).",
          "type": "string"
        },
        "recovery_hint": {
          "default": "",
          "description": "recovery_hint parameter (str).",
          "type": "string"
        }
      },
      "required": [],
      "type": "object"
    }
  },
  {
    "description": "Soft convergence warning -- fit produced output but diagnostics",
    "name": "ConvergenceWarning",
    "parameters": {
      "properties": {
        "alternative_functions": {
          "description": "alternative_functions parameter (Optional[List[str]]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "diagnostics": {
          "description": "diagnostics parameter (Optional[Dict[str, Any]]).",
          "type": "object"
        },
        "message": {
          "default": "",
          "description": "message parameter (str).",
          "type": "string"
        },
        "recovery_hint": {
          "default": "",
          "description": "recovery_hint parameter (str).",
          "type": "string"
        }
      },
      "required": [],
      "type": "object"
    }
  },
  {
    "description": "Soft assumption warning -- a test flags concern but the caller",
    "name": "AssumptionWarning",
    "parameters": {
      "properties": {
        "alternative_functions": {
          "description": "alternative_functions parameter (Optional[List[str]]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "diagnostics": {
          "description": "diagnostics parameter (Optional[Dict[str, Any]]).",
          "type": "object"
        },
        "message": {
          "default": "",
          "description": "message parameter (str).",
          "type": "string"
        },
        "recovery_hint": {
          "default": "",
          "description": "recovery_hint parameter (str).",
          "type": "string"
        }
      },
      "required": [],
      "type": "object"
    }
  },
  {
    "description": "Conditional marginal effects of ``d`` across a moderator ``x`` Validation: certified parity evidence.",
    "name": "interflex",
    "parameters": {
      "properties": {
        "adaptive": {
          "default": true,
          "description": "Kernel estimator only. ``True`` scales the bandwidth at each evaluation point by the moderator's density as the R package does (``h(x) = bw * sqrt(geometric-mean density / density(x))``); ``False`` uses the fixed Gaussian kernel ``phi((X - x)/bw)`` of the Stata ``interflex`` command.",
          "type": "boolean"
        },
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "bw": {
          "description": "Kernel bandwidth on the moderator's scale (before the adaptive density scaling). ``estimator='kernel'`` needs either ``bw`` or ``cv=True``, which selects it by interflex's cross-validation (``interflex(bw = NULL)``).",
          "type": "number"
        },
        "bw_grid": {
          "default": 30,
          "description": "interflex ``grid``: an integer gives that many candidates log-spaced from ``range(x)/100`` to ``range(x)``; a sequence is used as the grid.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "cutoffs": {
          "description": "cutoffs parameter (Optional[Sequence[float]]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "cv": {
          "default": false,
          "description": "Kernel estimator only: choose ``bw`` by interflex's k-fold cross-validation. Observations are split into ``kfold`` folds stratified on ``d`` (``interflex:::createFolds``); for every candidate bandwidth the local-linear coefficients are fitted on the training folds over the evaluation grid (with the training sample's moderator density for the adaptive scaling), carried to each test observation by linear interpolation between its two nearest grid points, and the mean squared (``metric='mse'``) or absolute (``'mae'``) prediction error of ``y`` is averaged over folds; the bandwidth minimising it, divided by the number of evaluation points that produced a fit, is chosen exactly as the R package does. Requires ``bw=None``.",
          "type": "boolean"
        },
        "d": {
          "description": "Outcome, treatment (binary 0/1 or continuous) and moderator.",
          "type": "string"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "estimator": {
          "default": "binning",
          "description": "estimator parameter (str).",
          "enum": [
            "binning",
            "linear",
            "kernel"
          ],
          "type": "string"
        },
        "kfold": {
          "default": 10,
          "description": "Number of folds (interflex ``kfold``).",
          "type": "integer"
        },
        "metric": {
          "default": "mse",
          "description": "CV loss (interflex ``metric``).",
          "enum": [
            "mse",
            "mae"
          ],
          "type": "string"
        },
        "n_boot": {
          "default": 200,
          "description": "Bootstrap replications and seed.",
          "type": "integer"
        },
        "nbins": {
          "default": 3,
          "description": "Number of moderator bins (cut at sample quantiles) for the binning estimator; ``cutoffs`` overrides the quantiles.",
          "type": "integer"
        },
        "neval": {
          "default": 50,
          "description": "Evaluation points, equally spaced over the moderator's range.",
          "type": "integer"
        },
        "random_state": {
          "description": "Seed for the fold assignment. interflex draws its folds with R's stream, so the selection is compared to R statistically (across seeds), not byte for byte.",
          "type": "integer"
        },
        "seed": {
          "description": "Bootstrap replications and seed.",
          "type": "integer"
        },
        "treat_type": {
          "description": "Inferred from ``d`` when omitted (two distinct values = discrete).",
          "enum": [
            "discrete",
            "continuous"
          ],
          "type": "string"
        },
        "vce": {
          "default": "robust",
          "description": "Variance construction. ``'robust'`` is the HC1 sandwich (interflex's default) and ``'homoscedastic'`` the classical covariance for the linear and binning models and the Wald test; ``'bootstrap'`` (kernel estimator only) draws nonparametric bootstrap standard errors, the linear and binning estimators always reporting delta-method SEs.",
          "enum": [
            "robust",
            "homoscedastic",
            "bootstrap"
          ],
          "type": "string"
        },
        "wald": {
          "default": true,
          "description": "Report the Wald and LR tests of the linear interaction against the binning model (``estimator='binning'`` or ``'linear'``).",
          "type": "boolean"
        },
        "wald_full_moderate": {
          "default": true,
          "description": "Whether the fully interacted model behind the Wald / LR tests also interacts the covariates ``z`` with the bins (R interflex). The Stata command leaves the covariates uninteracted; set ``False`` to reproduce its test.",
          "type": "boolean"
        },
        "wald_test": {
          "default": "chisq",
          "description": "Reference distribution of the Wald statistic: chi-square (R ``lmtest::waldtest(test = \"Chisq\")``) or the F distribution with the full model's residual degrees of freedom (Stata interflex).",
          "enum": [
            "chisq",
            "F"
          ],
          "type": "string"
        },
        "weights": {
          "description": "Observation weights column.",
          "type": "string"
        },
        "x": {
          "description": "Outcome, treatment (binary 0/1 or continuous) and moderator.",
          "type": "string"
        },
        "x_eval": {
          "description": "Explicit evaluation points.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Outcome, treatment (binary 0/1 or continuous) and moderator.",
          "type": "string"
        },
        "z": {
          "description": "Additional covariates entering linearly.",
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "required": [
        "data",
        "y",
        "d",
        "x"
      ],
      "type": "object"
    }
  },
  {
    "description": "The interflex figure: marginal effect of ``d`` across ``x`` with its",
    "name": "interflex_plot",
    "parameters": {
      "properties": {
        "ax": {
          "description": "Draw into an existing axes; a new figure is created otherwise.",
          "type": "string"
        },
        "result": {
          "description": "Output of :func:`interflex`.",
          "type": "string"
        },
        "show_hist": {
          "default": true,
          "description": "Add the moderator histogram (treated / control) under the curve.",
          "type": "boolean"
        }
      },
      "required": [
        "result"
      ],
      "type": "object"
    }
  },
  {
    "description": "Instrumental Variables regression model.",
    "name": "IVRegression",
    "parameters": {
      "properties": {
        "X_endog": {
          "description": "Alternative to formula interface.",
          "type": "string"
        },
        "X_exog": {
          "description": "Alternative to formula interface.",
          "type": "string"
        },
        "Z": {
          "description": "Alternative to formula interface.",
          "type": "string"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "formula": {
          "description": "Formula with IV syntax: ``\"y ~ (endog ~ z1 + z2) + exog1 + exog2\"``",
          "type": "string"
        },
        "fuller_alpha": {
          "default": 1.0,
          "description": "Fuller constant (only used when method='fuller'). ``alpha=1`` gives the bias-corrected Fuller estimator; ``alpha=4`` minimises MSE under normal errors.",
          "type": "number"
        },
        "method": {
          "default": "2sls",
          "description": "Estimation method.",
          "type": "string"
        },
        "var_names": {
          "description": "Alternative to formula interface.",
          "type": "object"
        },
        "y": {
          "description": "Alternative to formula interface.",
          "type": "string"
        }
      },
      "required": [],
      "type": "object"
    }
  },
  {
    "description": "Rambachan & Roth (2023) sensitivity analysis for parallel trends. Validation: certified parity evidence. Do NOT use when: the fitted result carries fewer than two pre-treatment event study coefficients -- the relative-magnitude restriction is defined against max|pre-period violation| and has nothing to scale against; the design is a 2x2 with no leads -- there is no pre-period violation to bound; report the standard CI instead.",
    "name": "honest_did",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level.",
          "type": "number"
        },
        "backend": {
          "default": "native",
          "description": "``'native'`` solves the problem in Python. For ``method='smoothness'`` it computes the true Rambachan-Roth **fixed-length confidence interval**: the optimal affine estimator under ``Delta^SD(M)``, obtained from a convex program over the event-study covariance. With ``HonestDiD``'s Monte-Carlo folded-normal quantile replaced by the exact one, the half-length agrees with R ``HonestDiD::findOptimalFLCI`` to 1e-8 and the bounds to ~2e-5 (HonestDiD's coarser search over ``h``); against the shipped package the gap is that quantile's simulation error, ~1e-3 (``tests/reference_parity/test_did_synth_R_parity.py``). This needs the joint event-study covariance, which is recovered from a Callaway-Sant'Anna fit's influence functions; if it is unavailable the function falls back to a worst-case-bias interval and warns. For ``method='relative_magnitude'`` it inverts the Andrews-Roth-Pakes conditional test (``honestdid_method='Conditional'``) or the conditional least-favourable hybrid (``'C-LF'``, the default, as in ``HonestDiD``) over the union of ``Delta^RM`` pieces, on ``HonestDiD``'s default grid of 1,000 points spanning +/-20 standard deviations of the target. On identical inputs the ``'Conditional'`` set equals R ``HonestDiD::createSensitivityResults_relativeMagnitudes`` exactly (same accepted grid points); the ``'C-LF'`` hybrid simulates its first-stage critical value (NumPy draws, not R's), so it agrees up to that Monte Carlo error. Like the FLCI it needs the joint event-study covariance; without it the function falls back to the worst-case-bias approximation ``theta +/- M*max|delta_pre| +/- z*SE`` and warns. A set that reaches either end of the grid is reported with a warning, because its true bound lies outside the grid. ``'honestdid'``/``'r'`` delegates to the R ``HonestDiD`` package through ``Rscript``. It is handed the same full covariance when one is recoverable; only when it is not does it fall back to ``diag(se2)``, which discards the (large, positive) cross-period covariance and materially changes the confidence set.",
          "enum": [
            "native",
            "honestdid",
            "r"
          ],
          "type": "string"
        },
        "e": {
          "default": 0,
          "description": "Relative time period for which to compute robust CIs. 0 = on-impact effect, 1 = one period after, etc.",
          "type": "integer"
        },
        "honestdid_method": {
          "description": "Inference method. The default preserves HonestDiD defaults: ``'C-LF'`` for ``method='relative_magnitude'`` and ``'FLCI'`` for ``method='smoothness'``. The native backend implements ``'FLCI'`` (smoothness) and ``'C-LF'`` / ``'Conditional'`` (relative magnitudes); the R backend accepts all four.",
          "enum": [
            "C-LF",
            "Conditional",
            "FLCI",
            "C-F"
          ],
          "type": "string"
        },
        "m_grid": {
          "description": "Grid of M values (maximum violation magnitude) to evaluate.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "method": {
          "default": "smoothness",
          "description": "Type of restriction on violations: - ``'smoothness'``: |Deltadelta_t| <= M (bounded change in violation) - ``'relative_magnitude'``: |delta_post| <= M x max|delta_pre|",
          "type": "string"
        },
        "result": {
          "description": "A fitted DID result that contains event study estimates in ``result.model_info['event_study']``.",
          "type": "string"
        }
      },
      "required": [
        "result"
      ],
      "type": "object"
    }
  },
  {
    "description": "Compute the breakdown value of M. Validation: certified parity evidence.",
    "name": "breakdown_m",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "e": {
          "default": 0,
          "description": "Relative time period.",
          "type": "integer"
        },
        "method": {
          "default": "smoothness",
          "description": "The restriction whose confidence set is inverted; the same meaning as in :func:`honest_did`.",
          "enum": [
            "smoothness",
            "relative_magnitude"
          ],
          "type": "string"
        },
        "result": {
          "description": "DID result with event study.",
          "type": "string"
        }
      },
      "required": [
        "result"
      ],
      "type": "object"
    }
  },
  {
    "description": "Bundled results from a full DID analysis workflow.",
    "name": "DIDAnalysis",
    "parameters": {
      "properties": {
        "bacon": {
          "description": "bacon parameter (Optional[Dict[str, Any]]).",
          "type": "object"
        },
        "design": {
          "description": "design parameter (str).",
          "type": "string"
        },
        "diagnostics": {
          "description": "diagnostics parameter (Optional[Dict[str, Any]]).",
          "type": "object"
        },
        "event_study_result": {
          "description": "event_study_result parameter (Optional[CausalResult]).",
          "type": "string"
        },
        "main_result": {
          "description": "main_result parameter (CausalResult).",
          "type": "string"
        },
        "method_used": {
          "description": "method_used parameter (str).",
          "type": "string"
        },
        "sensitivity": {
          "description": "sensitivity parameter (Optional[pd.DataFrame]).",
          "type": "string"
        },
        "steps_log": {
          "default": null,
          "description": "steps_log parameter (List[str]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "required": [
        "design",
        "method_used",
        "main_result"
      ],
      "type": "object"
    }
  },
  {
    "description": "Borusyak, Jaravel & Spiess (2024) imputation DID estimator. Validation: certified parity evidence. Do NOT use when: no never-treated or not-yet-treated observations remain -- the imputation model cannot be fit; the design is a simple 2x2 -- use sp.did(method='2x2').",
    "name": "bjs",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals.",
          "type": "number"
        },
        "balanced": {
          "default": false,
          "description": "Stata ``did_imputation, hbalance``: keep only eventually-treated units observed at *every* requested horizon, so the event-study composition is stable across ``k`` (no cohort churn). Never-treated units are always kept. Requires ``horizon`` (or ``pretrends``). Warns with the number of units dropped.",
          "type": "boolean"
        },
        "boot_seed": {
          "default": 0,
          "description": "Seed for the cluster bootstrap (deterministic results).",
          "type": "integer"
        },
        "cluster": {
          "description": "Variable for cluster-robust standard errors. Defaults to ``group`` (unit-level clustering).",
          "type": "string"
        },
        "controls": {
          "description": "Continuous time-varying controls, entering the Y(0) model additively. Stata ``did_imputation``'s ``controls()``.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "data": {
          "description": "Panel data in long format.",
          "type": "string"
        },
        "fe": {
          "description": "Which fixed effects the Y(0) model carries, replacing the default two-way ``unit + time``. Mirrors Stata ``did_imputation``'s ``fe()``. Each entry is one fixed effect, written either as a bare column or Stata-style with ``#`` for the interacted cell:: fe=['time'] # period FE only fe=['unit', 'state#year'] # unit FE + state-by-year FE fe=[] # no fixed effects at all Levels are factorized over the whole panel, so a cell seen only among treated rows still gets a column and is imputed rather than silently absorbing the reference level.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "first_treat": {
          "description": "Column indicating the period of first treatment. Use ``np.inf``, ``np.nan``, or ``0`` for never-treated units.",
          "type": "string"
        },
        "group": {
          "description": "Unit identifier column.",
          "type": "string"
        },
        "hetby": {
          "description": "Stata ``did_imputation, hetby(varname)``: report heterogeneous overall ATTs by the levels of a **time-invariant** unit-level variable. Results land in ``model_info['hetby']`` (one row per",
          "type": "string"
        },
        "horizon": {
          "description": "Relative time periods for event study estimates, e.g. ``list(range(-5, 6))``. If ``None``, reports only the overall ATT (no event study disaggregation).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "min_n": {
          "description": "Stata ``did_imputation, minn(#)``: drop event-study horizons with fewer than ``min_n`` treated observations (they are noisy and dominated by a single cohort). Dropped horizons are listed in a warning and excluded from the pre-trend test.",
          "type": "integer"
        },
        "n_boot": {
          "default": 199,
          "description": "Number of cluster-bootstrap replications when ``vce='bootstrap'``.",
          "type": "integer"
        },
        "pretrend_method": {
          "default": "bjs",
          "description": "How the **pre-treatment** event-study coefficients are built. The post-treatment coefficients are imputation residuals either way; only the leads differ, and the difference is visible in the plot rather than in the ATT. - ``'bjs'`` -- the convention of Stata ``did_imputation, pretrends(k)``: an auxiliary dynamic TWFE regression on the untreated observations, with all relative times earlier than the requested leads pooled into the omitted category. Reproduces Stata's coefficients and standard errors. Not available with ``fe=``, ``unit_covariates=`` or ``time_covariates=``. - ``'in-sample'`` -- average the imputation residuals at pre-treatment relative times, as ``fect`` and ``did2s`` do. These are in-sample prediction errors: in a non-staggered design they equal the symmetric benchmark times the untreated unit share ``N0/N``, so they understate pre-trends, severely when most units are treated. This was StatsPAI's behaviour before v1.23.0. - ``'symmetric'`` -- Roth's (2026) repair, which uses the average of the pre-treatment periods as the reference for both halves of the path, so the plot matches a dynamic TWFE event study up to a common vertical shift and the usual visual heuristics apply. Non-staggered balanced designs without covariates only; raises otherwise rather than applying an unverified factor. The chosen convention and its caveat are recorded in ``model_info['pretrend_method']`` and ``model_info['event_study_convention']``.",
          "enum": [
            "bjs",
            "in-sample",
            "symmetric"
          ],
          "type": "string"
        },
        "pretrends": {
          "description": "Stata ``did_imputation, pretrends(k)``: estimate the ``k`` pre-treatment placebo coefficients (horizons ``-k .. -1``, added to ``horizon`` if not already requested) and report their joint Wald test in ``model_info['pretrend_test']``. Under the default ``pretrend_method='bjs'`` the test uses the full cluster-robust covariance of the auxiliary lead regression; under the other conventions it assumes the lead estimates are uncorrelated (valid but conservative), and :func:`statspai.bjs_pretrend_joint` gives the covariance-aware cluster-bootstrap version.",
          "type": "integer"
        },
        "project": {
          "description": "project parameter (Optional[List[str]]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "save_residuals": {
          "default": false,
          "description": "Stata ``did_imputation, saveresid()``: store the untreated-fit residuals ``y - y0`` in ``model_info['residuals']`` (aligned with the rows of ``data``; ``NaN`` on treated rows, whose ``y - y0`` is the treatment effect, not a residual).",
          "type": "boolean"
        },
        "save_weights": {
          "default": false,
          "description": "Stata ``did_imputation, saveweights()``: store the exact estimation weights ``w`` such that ``ATT = w'y`` in ``model_info['estimation_weights']`` (aligned with the rows of ``data``). Treated rows get ``1/N1``; untreated rows get the (negative) imputation weights implied by the FE projection -- useful for diagnosing which comparisons drive the estimate.",
          "type": "boolean"
        },
        "se_method": {
          "description": "Shared DiD spelling for ``vce=`` -- ``'analytic'``, ``'bootstrap'`` (also ``'cluster'``, ``'pairs'``) or ``'auto'``. Passing both raises. ``'auto'`` picks the cluster bootstrap when the design has at most 30 clusters (Cameron, Gelbach & Miller 2008) and the analytic BJS variance otherwise: with few clusters the cluster-score sum behind any sandwich is itself noisy, which is a small-cluster problem rather than a defect in the formula.",
          "type": "string"
        },
        "time": {
          "description": "Time period column.",
          "type": "string"
        },
        "time_covariates": {
          "description": "Controls interacted with the **period** fixed effects, i.e. one coefficient per period. Stata's ``timecontrols()``. Typically a time-invariant unit characteristic whose effect is allowed to move over calendar time.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "unit_covariates": {
          "description": "Controls interacted with the **unit** fixed effects, i.e. one slope per unit. Stata's ``unitcontrols()``. The canonical use is ``unit_covariates=[time_col]``, which makes Y(0) carry unit-specific linear trends. Identification bites hard here: a unit-specific slope needs at least two untreated periods for that unit, so early-treated cohorts can lose their imputation entirely.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "vce": {
          "default": "analytic",
          "description": "Standard-error mode for the overall ATT. ``'analytic'`` is the exact Borusyak--Jaravel--Spiess variance: the estimator is linear in the outcome, so its weights are computed rather than approximated, and the result reproduces Stata ``did_imputation`` and R ``didimputation`` to ~5e-8. Before v1.23.0 this path used an approximation that was materially anti-conservative (18-36% too small on the harness fixtures); see MIGRATION.md. Measured, not asserted. On a homogeneous-effect design with a never-treated half, 400-800 replications per cell: =========== ========== ==================== clusters coverage mean SE / sd(est) =========== ========== ==================== 30 0.925 0.985 60 0.938 0.936 120 0.948 0.978 240 0.935 1.007 480 **0.950** 0.983 =========== ========== ==================== Two things to read off it. The ratio sits at 1 throughout, so the variance formula carries no systematic bias -- the approximation it replaced ran 18-36% low. And coverage reaches nominal by 480 clusters, so the shortfall at 60 is a small-cluster effect rather than a missing term: there the standard error is simply *noisy* (dispersion 9.2% against 3.2% at 480), which fattens the studentised distribution (sd(t) = 1.04 against 1.007). A ``t(G-1)`` critical value barely helps (0.938 to 0.941 at G = 60) because the problem is the variability of the standard error, not the degrees of freedom. ``vce='bootstrap'`` is the remedy in small designs, and ``se_method='auto'`` selects it below 30 clusters. ``'bootstrap'`` resamples whole clusters and re-runs the full imputation estimator. Point estimates are identical either way; per-horizon event-study SEs are unaffected.",
          "enum": [
            "analytic",
            "bootstrap"
          ],
          "type": "string"
        },
        "weights": {
          "description": "Column of estimation weights ``omega_i`` (Stata ``[aw=]``, R ``didimputation(weights=)``). The untreated Y(0) model is fit by weighted least squares, every reported average is the ``omega``-weighted mean over the treated cells it covers, and the exact variance uses the weighted projection. Like ``weights=`` on the other staggered estimators this changes the *estimand*: the weighted ATT averages over the population the treated units represent, not over the units. Not combinable with ``project=``.",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable name.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "group",
        "time",
        "first_treat"
      ],
      "type": "object"
    }
  },
  {
    "description": "Borusyak, Jaravel & Spiess (2024) imputation DID estimator. Validation: certified parity evidence. Do NOT use when: no never-treated or not-yet-treated observations remain -- the imputation model cannot be fit; the design is a simple 2x2 -- use sp.did(method='2x2').",
    "name": "borusyak_jaravel_spiess",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals.",
          "type": "number"
        },
        "balanced": {
          "default": false,
          "description": "Stata ``did_imputation, hbalance``: keep only eventually-treated units observed at *every* requested horizon, so the event-study composition is stable across ``k`` (no cohort churn). Never-treated units are always kept. Requires ``horizon`` (or ``pretrends``). Warns with the number of units dropped.",
          "type": "boolean"
        },
        "boot_seed": {
          "default": 0,
          "description": "Seed for the cluster bootstrap (deterministic results).",
          "type": "integer"
        },
        "cluster": {
          "description": "Variable for cluster-robust standard errors. Defaults to ``group`` (unit-level clustering).",
          "type": "string"
        },
        "controls": {
          "description": "Continuous time-varying controls, entering the Y(0) model additively. Stata ``did_imputation``'s ``controls()``.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "data": {
          "description": "Panel data in long format.",
          "type": "string"
        },
        "fe": {
          "description": "Which fixed effects the Y(0) model carries, replacing the default two-way ``unit + time``. Mirrors Stata ``did_imputation``'s ``fe()``. Each entry is one fixed effect, written either as a bare column or Stata-style with ``#`` for the interacted cell:: fe=['time'] # period FE only fe=['unit', 'state#year'] # unit FE + state-by-year FE fe=[] # no fixed effects at all Levels are factorized over the whole panel, so a cell seen only among treated rows still gets a column and is imputed rather than silently absorbing the reference level.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "first_treat": {
          "description": "Column indicating the period of first treatment. Use ``np.inf``, ``np.nan``, or ``0`` for never-treated units.",
          "type": "string"
        },
        "group": {
          "description": "Unit identifier column.",
          "type": "string"
        },
        "hetby": {
          "description": "Stata ``did_imputation, hetby(varname)``: report heterogeneous overall ATTs by the levels of a **time-invariant** unit-level variable. Results land in ``model_info['hetby']`` (one row per",
          "type": "string"
        },
        "horizon": {
          "description": "Relative time periods for event study estimates, e.g. ``list(range(-5, 6))``. If ``None``, reports only the overall ATT (no event study disaggregation).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "min_n": {
          "description": "Stata ``did_imputation, minn(#)``: drop event-study horizons with fewer than ``min_n`` treated observations (they are noisy and dominated by a single cohort). Dropped horizons are listed in a warning and excluded from the pre-trend test.",
          "type": "integer"
        },
        "n_boot": {
          "default": 199,
          "description": "Number of cluster-bootstrap replications when ``vce='bootstrap'``.",
          "type": "integer"
        },
        "pretrend_method": {
          "default": "bjs",
          "description": "How the **pre-treatment** event-study coefficients are built. The post-treatment coefficients are imputation residuals either way; only the leads differ, and the difference is visible in the plot rather than in the ATT. - ``'bjs'`` -- the convention of Stata ``did_imputation, pretrends(k)``: an auxiliary dynamic TWFE regression on the untreated observations, with all relative times earlier than the requested leads pooled into the omitted category. Reproduces Stata's coefficients and standard errors. Not available with ``fe=``, ``unit_covariates=`` or ``time_covariates=``. - ``'in-sample'`` -- average the imputation residuals at pre-treatment relative times, as ``fect`` and ``did2s`` do. These are in-sample prediction errors: in a non-staggered design they equal the symmetric benchmark times the untreated unit share ``N0/N``, so they understate pre-trends, severely when most units are treated. This was StatsPAI's behaviour before v1.23.0. - ``'symmetric'`` -- Roth's (2026) repair, which uses the average of the pre-treatment periods as the reference for both halves of the path, so the plot matches a dynamic TWFE event study up to a common vertical shift and the usual visual heuristics apply. Non-staggered balanced designs without covariates only; raises otherwise rather than applying an unverified factor. The chosen convention and its caveat are recorded in ``model_info['pretrend_method']`` and ``model_info['event_study_convention']``.",
          "enum": [
            "bjs",
            "in-sample",
            "symmetric"
          ],
          "type": "string"
        },
        "pretrends": {
          "description": "Stata ``did_imputation, pretrends(k)``: estimate the ``k`` pre-treatment placebo coefficients (horizons ``-k .. -1``, added to ``horizon`` if not already requested) and report their joint Wald test in ``model_info['pretrend_test']``. Under the default ``pretrend_method='bjs'`` the test uses the full cluster-robust covariance of the auxiliary lead regression; under the other conventions it assumes the lead estimates are uncorrelated (valid but conservative), and :func:`statspai.bjs_pretrend_joint` gives the covariance-aware cluster-bootstrap version.",
          "type": "integer"
        },
        "project": {
          "description": "project parameter (Optional[List[str]]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "save_residuals": {
          "default": false,
          "description": "Stata ``did_imputation, saveresid()``: store the untreated-fit residuals ``y - y0`` in ``model_info['residuals']`` (aligned with the rows of ``data``; ``NaN`` on treated rows, whose ``y - y0`` is the treatment effect, not a residual).",
          "type": "boolean"
        },
        "save_weights": {
          "default": false,
          "description": "Stata ``did_imputation, saveweights()``: store the exact estimation weights ``w`` such that ``ATT = w'y`` in ``model_info['estimation_weights']`` (aligned with the rows of ``data``). Treated rows get ``1/N1``; untreated rows get the (negative) imputation weights implied by the FE projection -- useful for diagnosing which comparisons drive the estimate.",
          "type": "boolean"
        },
        "se_method": {
          "description": "Shared DiD spelling for ``vce=`` -- ``'analytic'``, ``'bootstrap'`` (also ``'cluster'``, ``'pairs'``) or ``'auto'``. Passing both raises. ``'auto'`` picks the cluster bootstrap when the design has at most 30 clusters (Cameron, Gelbach & Miller 2008) and the analytic BJS variance otherwise: with few clusters the cluster-score sum behind any sandwich is itself noisy, which is a small-cluster problem rather than a defect in the formula.",
          "type": "string"
        },
        "time": {
          "description": "Time period column.",
          "type": "string"
        },
        "time_covariates": {
          "description": "Controls interacted with the **period** fixed effects, i.e. one coefficient per period. Stata's ``timecontrols()``. Typically a time-invariant unit characteristic whose effect is allowed to move over calendar time.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "unit_covariates": {
          "description": "Controls interacted with the **unit** fixed effects, i.e. one slope per unit. Stata's ``unitcontrols()``. The canonical use is ``unit_covariates=[time_col]``, which makes Y(0) carry unit-specific linear trends. Identification bites hard here: a unit-specific slope needs at least two untreated periods for that unit, so early-treated cohorts can lose their imputation entirely.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "vce": {
          "default": "analytic",
          "description": "Standard-error mode for the overall ATT. ``'analytic'`` is the exact Borusyak--Jaravel--Spiess variance: the estimator is linear in the outcome, so its weights are computed rather than approximated, and the result reproduces Stata ``did_imputation`` and R ``didimputation`` to ~5e-8. Before v1.23.0 this path used an approximation that was materially anti-conservative (18-36% too small on the harness fixtures); see MIGRATION.md. Measured, not asserted. On a homogeneous-effect design with a never-treated half, 400-800 replications per cell: =========== ========== ==================== clusters coverage mean SE / sd(est) =========== ========== ==================== 30 0.925 0.985 60 0.938 0.936 120 0.948 0.978 240 0.935 1.007 480 **0.950** 0.983 =========== ========== ==================== Two things to read off it. The ratio sits at 1 throughout, so the variance formula carries no systematic bias -- the approximation it replaced ran 18-36% low. And coverage reaches nominal by 480 clusters, so the shortfall at 60 is a small-cluster effect rather than a missing term: there the standard error is simply *noisy* (dispersion 9.2% against 3.2% at 480), which fattens the studentised distribution (sd(t) = 1.04 against 1.007). A ``t(G-1)`` critical value barely helps (0.938 to 0.941 at G = 60) because the problem is the variability of the standard error, not the degrees of freedom. ``vce='bootstrap'`` is the remedy in small designs, and ``se_method='auto'`` selects it below 30 clusters. ``'bootstrap'`` resamples whole clusters and re-runs the full imputation estimator. Point estimates are identical either way; per-horizon event-study SEs are unaffected.",
          "enum": [
            "analytic",
            "bootstrap"
          ],
          "type": "string"
        },
        "weights": {
          "description": "Column of estimation weights ``omega_i`` (Stata ``[aw=]``, R ``didimputation(weights=)``). The untreated Y(0) model is fit by weighted least squares, every reported average is the ``omega``-weighted mean over the treated cells it covers, and the exact variance uses the weighted projection. Like ``weights=`` on the other staggered estimators this changes the *estimand*: the weighted ATT averages over the population the treated units represent, not over the units. Not combinable with ``project=``.",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable name.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "group",
        "time",
        "first_treat"
      ],
      "type": "object"
    }
  },
  {
    "description": "Gardner (2022) two-stage DID estimator. Validation: certified parity evidence.",
    "name": "did_2stage",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Two-sided CI level.",
          "type": "number"
        },
        "boot_seed": {
          "default": 0,
          "description": "Seed for the cluster bootstrap (deterministic results).",
          "type": "integer"
        },
        "cluster": {
          "description": "Cluster variable for the standard errors. Defaults to ``group``. At least two clusters are required.",
          "type": "string"
        },
        "controls": {
          "description": "Additional covariates included in Stage 1.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "data": {
          "description": "Long-format panel.",
          "type": "string"
        },
        "event_study": {
          "default": false,
          "description": "If True, Stage 2 reports coefficients by relative time ``k = t - first_treat_i``.",
          "type": "boolean"
        },
        "first_treat": {
          "description": "First-treatment-period column. Never-treated units should be encoded as ``0``, ``NaN``, or ``+inf``.",
          "type": "string"
        },
        "group": {
          "description": "Unit (panel-id) column.",
          "type": "string"
        },
        "horizon": {
          "description": "Relative-time leads/lags to report when ``event_study=True``; defaults to ``range(-5, 6)`` intersected with available support. Each reported coefficient is the (weighted) mean of the residualised outcome at that relative time; the Stage-2 regression has no intercept, so the set of horizons requested does not change any individual coefficient or its standard error.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "n_boot": {
          "default": 199,
          "description": "Number of cluster-bootstrap replications when ``vce='bootstrap'``.",
          "type": "integer"
        },
        "time": {
          "description": "Time column.",
          "type": "string"
        },
        "vce": {
          "default": "analytic",
          "description": "Standard-error mode. ``'analytic'`` is the two-stage corrected clustered variance of Butts & Gardner (2022): the Stage-2 influence function is adjusted for the estimation of the Stage-1 fixed effects (see the module docstring), which is what R ``did2s`` and Stata ``did2s`` report and what StatsPAI reproduces to ~1e-8 relative. ``'bootstrap'`` resamples whole clusters and re-runs the full two-step procedure. ``'none'`` skips inference (``se``, ``ci`` and ``pvalue`` are NaN); it is what the bootstrap uses internally. Point estimates are identical in every mode.",
          "enum": [
            "analytic",
            "bootstrap"
          ],
          "type": "string"
        },
        "weights": {
          "description": "Column of strictly positive estimation weights applied to both stages (weighted least squares), the counterpart of R ``did2s(weights=)`` and Stata ``did2s [aw=]``. In event-study mode the overall ATT is the weight-share average of the post-treatment coefficients.",
          "type": "string"
        },
        "y": {
          "description": "Outcome column name.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "group",
        "time",
        "first_treat"
      ],
      "type": "object"
    }
  },
  {
    "description": "Power of the pre-trend test against a hypothesised violation. Validation: certified parity evidence.",
    "name": "pretrends_power",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level of the pre-trend test.",
          "type": "number"
        },
        "delta": {
          "description": "Hypothesised trend violation in the pre-period (length = number of pre-periods). Default: linear trend ``delta[k] = (k+1) * min(|SE|)`` -- a violation equal to one SE at the furthest lag, declining linearly to near-zero.",
          "type": "string"
        },
        "result": {
          "description": "Event-study result with pre-treatment estimates and SEs.",
          "type": "string"
        },
        "test": {
          "default": "individual",
          "description": "Which pre-test the power refers to. ``\"individual\"`` is the practice Roth (2022) analyses and the one his ``pretrends`` R package implements: the analyst eyeballs the event-study plot and calls the pre-trends into question if *any* pre-period coefficient is individually significant. Power is then one minus the probability that every pre-period coefficient falls inside its own ``+/- z_{1-alpha/2} * SE`` band, integrated over the joint normal with mean ``delta`` -- a multivariate-normal rectangle probability. ``\"joint\"`` is the power of the joint Wald test that all pre-period coefficients are zero, ``chi2(K)`` with non-centrality ``delta' Sigma^-1 delta``. Reported by :func:`pretrends_test`, and a strictly different quantity -- not a tighter or looser version of the same one. The two are not even comparable at face value: the joint test has size exactly ``alpha``, while the coefficient-by-coefficient test rejects with probability above ``alpha`` under the null because each of the K coefficients gets its own ``alpha``-level look. Which comes out more powerful against a given trend depends on the design. ``power_joint`` is always reported alongside, so both are available from one call. .. versionchanged:: 1.21.0 The default moved from ``\"joint\"`` to ``\"individual\"`` so the number matches Roth's ``pretrends`` package. This changes the returned ``power`` for existing calls -- see MIGRATION.md. Pass ``test=\"joint\"`` to recover the previous behaviour.",
          "enum": [
            "individual",
            "joint"
          ],
          "type": "string"
        }
      },
      "required": [
        "result"
      ],
      "type": "object"
    }
  },
  {
    "description": "Slope of a linear pre-trend the pre-test would detect ``target_power`` Validation: certified parity evidence.",
    "name": "pretrends_slope_for_power",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level of the pre-test.",
          "type": "number"
        },
        "result": {
          "description": "Event-study result with pre-treatment estimates and SEs. As with :func:`pretrends_power`, supply the full pre-period covariance via ``model_info['vcv_pre']`` -- the diagonal fallback overstates the detectable slope.",
          "type": "string"
        },
        "target_power": {
          "default": 0.5,
          "description": "Power the returned slope achieves. 0.5 is the ``pretrends``",
          "type": "number"
        },
        "test": {
          "default": "individual",
          "description": "Which pre-test to solve against; see :func:`pretrends_power`.",
          "enum": [
            "individual",
            "joint"
          ],
          "type": "string"
        }
      },
      "required": [
        "result"
      ],
      "type": "object"
    }
  },
  {
    "description": "Result of Rambachan & Roth (2023) sensitivity analysis.",
    "name": "SensitivityResult",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "att": {
          "description": "att parameter (float).",
          "type": "number"
        },
        "att_se": {
          "description": "att_se parameter (float).",
          "type": "number"
        },
        "breakdown_mbar": {
          "description": "breakdown_mbar parameter (float).",
          "type": "number"
        },
        "ci_lower": {
          "description": "ci_lower parameter (np.ndarray).",
          "type": "string"
        },
        "ci_upper": {
          "description": "ci_upper parameter (np.ndarray).",
          "type": "string"
        },
        "mbar_grid": {
          "description": "Grid of mbar values to evaluate.",
          "type": "string"
        },
        "method": {
          "default": "C-LF",
          "description": "Estimator or algorithm variant to use.",
          "type": "string"
        }
      },
      "required": [
        "mbar_grid",
        "ci_lower",
        "ci_upper",
        "breakdown_mbar",
        "att",
        "att_se"
      ],
      "type": "object"
    }
  },
  {
    "description": "Print a combined pre-trends diagnostic report.",
    "name": "pretrends_summary",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level.",
          "type": "number"
        },
        "delta": {
          "description": "Passed to ``pretrends_power``.",
          "type": "string"
        },
        "result": {
          "description": "Event-study result.",
          "type": "string"
        }
      },
      "required": [
        "result"
      ],
      "type": "object"
    }
  },
  {
    "description": "Run the full parallel-trends robustness pipeline on a DiD result. Validation: validated evidence tier (known-truth, reference, external-parity, or Monte Carlo artifact).",
    "name": "parallel_trends_robustness",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level.",
          "type": "number"
        },
        "delta": {
          "description": "Hypothesised pre-trend violation passed to ``pretrends_power``.",
          "type": "string"
        },
        "e": {
          "default": 0,
          "description": "Relative time whose effect the sensitivity analysis targets.",
          "type": "integer"
        },
        "families": {
          "default": [
            "SD",
            "RM"
          ],
          "description": "Restriction families. ``\"SD\"`` maps to ``honest_did``'s ``method='smoothness'`` (bounded second differences); ``\"RM\"`` maps to ``method='relative_magnitude'``.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "m_grid": {
          "description": "Grid of violation magnitudes. Default: the ``honest_did`` default, multiples of the standard error at ``e``.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "result": {
          "description": "A fitted DiD/event-study result carrying event-study estimates in ``result.model_info['event_study']`` (e.g. from ``sp.event_study``, ``sp.callaway_santanna``, ``sp.sun_abraham``). .. note:: If the result does not carry a pre-period covariance matrix in ``model_info['vcv_pre']``, the pre-trend test and power fall back to assuming the pre-period coefficients are mutually independent and warn loudly. ``sp.event_study`` computes the full cluster-robust covariance; pass ``expose_pre_vcov=True`` to it to have this pipeline use the correct covariance instead of the diagonal fallback.",
          "type": "string"
        }
      },
      "required": [
        "result"
      ],
      "type": "object"
    }
  },
  {
    "description": "Bundled pre-trends power + honest-DiD sensitivity for one DiD result.",
    "name": "ParallelTrendsRobustnessResult",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "att": {
          "description": "att parameter (float).",
          "type": "number"
        },
        "att_se": {
          "description": "att_se parameter (float).",
          "type": "number"
        },
        "breakdown": {
          "description": "breakdown parameter (Dict[str, float]).",
          "type": "object"
        },
        "ci_grid": {
          "description": "Grid of ci values to evaluate.",
          "type": "string"
        },
        "e": {
          "default": 0,
          "description": "e parameter (int).",
          "type": "integer"
        },
        "families": {
          "default": null,
          "description": "families parameter (List[str]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "power_table": {
          "description": "power_table parameter (pd.DataFrame).",
          "type": "string"
        },
        "pretrend_power": {
          "default": null,
          "description": "pretrend_power parameter (Dict[str, Any]).",
          "type": "object"
        },
        "pretrend_test": {
          "default": null,
          "description": "pretrend_test parameter (Dict[str, Any]).",
          "type": "object"
        },
        "verdict": {
          "description": "verdict parameter (str).",
          "type": "string"
        }
      },
      "required": [
        "power_table",
        "ci_grid",
        "breakdown",
        "verdict",
        "att",
        "att_se"
      ],
      "type": "object"
    }
  },
  {
    "description": "Plot raw outcome means over time for treatment and control groups.",
    "name": "parallel_trends_plot",
    "parameters": {
      "properties": {
        "agg": {
          "default": "mean",
          "description": "Aggregation function: 'mean' or 'median'.",
          "type": "string"
        },
        "ax": {
          "description": "Existing axes to plot on.",
          "type": "string"
        },
        "ci": {
          "default": true,
          "description": "Show 95% confidence intervals (+/-1.96 SE of mean).",
          "type": "boolean"
        },
        "colors": {
          "description": "Colors for (treatment, control). Default: ('#E74C3C', '#2C3E50').",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "data": {
          "description": "Input dataset.",
          "type": "string"
        },
        "figsize": {
          "default": [
            10,
            6
          ],
          "description": "Figure size.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "id": {
          "description": "Unit identifier (for panel data).",
          "type": "string"
        },
        "labels": {
          "description": "Custom labels, e.g. ``{'treat': 'New Jersey', 'control': 'Pennsylvania'}``.",
          "type": "object"
        },
        "time": {
          "description": "Time period variable.",
          "type": "string"
        },
        "title": {
          "description": "Plot title.",
          "type": "string"
        },
        "treat": {
          "description": "Treatment group indicator. Binary (0/1) for 2x2, or first-treatment-period for staggered (0 = never treated).",
          "type": "string"
        },
        "treat_time": {
          "description": "Treatment onset time. Draws a vertical line if provided.",
          "type": "integer"
        },
        "y": {
          "description": "Outcome variable.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "time",
        "treat"
      ],
      "type": "object"
    }
  },
  {
    "description": "Scatter plot of Goodman-Bacon decomposition.",
    "name": "bacon_plot",
    "parameters": {
      "properties": {
        "ax": {
          "description": "ax parameter.",
          "type": "string"
        },
        "bacon_result": {
          "description": "Output from ``bacon_decomposition()``. Must contain ``'decomposition'`` DataFrame and ``'beta_twfe'``.",
          "type": "object"
        },
        "colors": {
          "description": "Map comparison type -> color. Defaults provided.",
          "type": "object"
        },
        "figsize": {
          "default": [
            10,
            6
          ],
          "description": "figsize parameter (Tuple[float, float]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "title": {
          "description": "title parameter (Optional[str]).",
          "type": "string"
        }
      },
      "required": [
        "bacon_result"
      ],
      "type": "object"
    }
  },
  {
    "description": "Plot group-time ATT estimates from Callaway-Sant'Anna.",
    "name": "group_time_plot",
    "parameters": {
      "properties": {
        "alpha_level": {
          "default": 0.05,
          "description": "Significance threshold.",
          "type": "number"
        },
        "ax": {
          "description": "ax parameter.",
          "type": "string"
        },
        "color": {
          "default": "#2C3E50",
          "description": "Default color for dot plot.",
          "type": "string"
        },
        "figsize": {
          "default": [
            12,
            7
          ],
          "description": "figsize parameter (Tuple[float, float]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "insig_color": {
          "default": "#BDC3C7",
          "description": "Color for insignificant estimates.",
          "type": "string"
        },
        "plot_type": {
          "default": "dot",
          "description": "'dot' or 'heatmap'.",
          "type": "string"
        },
        "result": {
          "description": "Result from ``callaway_santanna()`` or ``did(method='cs')``. Must have ``detail`` DataFrame with 'group', 'time', 'att' columns.",
          "type": "string"
        },
        "sig_color": {
          "default": "#E74C3C",
          "description": "Color for significant estimates.",
          "type": "string"
        },
        "title": {
          "description": "title parameter (Optional[str]).",
          "type": "string"
        }
      },
      "required": [
        "result"
      ],
      "type": "object"
    }
  },
  {
    "description": "Classic DID diagram showing treatment effect with counterfactual.",
    "name": "did_plot",
    "parameters": {
      "properties": {
        "annotate_effect": {
          "default": true,
          "description": "Annotate the treatment effect arrow on the plot.",
          "type": "boolean"
        },
        "ax": {
          "description": "ax parameter.",
          "type": "string"
        },
        "colors": {
          "description": "(treatment, control, counterfactual) colors.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "data": {
          "description": "Input dataset.",
          "type": "string"
        },
        "figsize": {
          "default": [
            10,
            6
          ],
          "description": "figsize parameter (Tuple[float, float]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "labels": {
          "description": "Custom labels: ``{'treat': ..., 'control': ..., 'counterfactual': ...}``.",
          "type": "object"
        },
        "show_counterfactual": {
          "default": true,
          "description": "Draw the dashed counterfactual line.",
          "type": "boolean"
        },
        "time": {
          "description": "Time period variable.",
          "type": "string"
        },
        "title": {
          "description": "title parameter (Optional[str]).",
          "type": "string"
        },
        "treat": {
          "description": "Binary treatment group indicator (0/1).",
          "type": "string"
        },
        "treat_time": {
          "description": "Treatment onset time. If None, inferred as the midpoint.",
          "type": "integer"
        },
        "y": {
          "description": "Outcome variable.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "time",
        "treat"
      ],
      "type": "object"
    }
  },
  {
    "description": "Enhanced event study plot with pre/post shading and significance coloring.",
    "name": "enhanced_event_study_plot",
    "parameters": {
      "properties": {
        "alpha_level": {
          "default": 0.05,
          "description": "alpha_level parameter (float).",
          "type": "number"
        },
        "ax": {
          "description": "ax parameter.",
          "type": "string"
        },
        "ci_alpha": {
          "default": 0.15,
          "description": "Confidence band transparency.",
          "type": "number"
        },
        "color": {
          "default": "#2C3E50",
          "description": "Default color for estimates.",
          "type": "string"
        },
        "figsize": {
          "default": [
            10,
            6
          ],
          "description": "figsize parameter (Tuple[float, float]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "marker": {
          "default": "o",
          "description": "marker parameter (str).",
          "type": "string"
        },
        "markersize": {
          "default": 6,
          "description": "markersize parameter (int).",
          "type": "integer"
        },
        "post_color": {
          "default": "#FDEDEC",
          "description": "Post-treatment shading color.",
          "type": "string"
        },
        "pre_color": {
          "default": "#EBF5FB",
          "description": "Pre-treatment shading color.",
          "type": "string"
        },
        "result": {
          "description": "DID result with event study in ``model_info['event_study']``.",
          "type": "string"
        },
        "shade_post": {
          "default": true,
          "description": "Shade post-treatment region.",
          "type": "boolean"
        },
        "shade_pre": {
          "default": true,
          "description": "Shade pre-treatment region.",
          "type": "boolean"
        },
        "show_zero": {
          "default": true,
          "description": "Show horizontal zero line.",
          "type": "boolean"
        },
        "sig_color": {
          "default": "#E74C3C",
          "description": "Color for significant estimates. None disables coloring.",
          "type": "string"
        },
        "title": {
          "description": "title parameter (Optional[str]).",
          "type": "string"
        },
        "uniform_band": {
          "default": false,
          "description": "Overlay the sup-t *simultaneous* band (:func:`sp.uniform_bands`), computed separately for the leads and for the post-treatment horizons from the estimator's joint event-study covariance. A pointwise interval covers one horizon; a plot invites a statement about the path, which is what this band licenses. Silently skipped -- with a warning -- when the estimator exposes no event-study covariance at all.",
          "type": "boolean"
        }
      },
      "required": [
        "result"
      ],
      "type": "object"
    }
  },
  {
    "description": "Visualise staggered treatment adoption timing.",
    "name": "treatment_rollout_plot",
    "parameters": {
      "properties": {
        "ax": {
          "description": "ax parameter.",
          "type": "string"
        },
        "data": {
          "description": "Panel data with unit, time, and treatment columns.",
          "type": "string"
        },
        "figsize": {
          "default": [
            12,
            7
          ],
          "description": "figsize parameter (Tuple[float, float]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "id": {
          "description": "Unit identifier.",
          "type": "string"
        },
        "never_color": {
          "default": "#BDC3C7",
          "description": "Color for never-treated units.",
          "type": "string"
        },
        "show_cohort_labels": {
          "default": true,
          "description": "Annotate cohort boundaries on the y-axis.",
          "type": "boolean"
        },
        "sort_by": {
          "default": "treat_time",
          "description": "Sort units by: 'treat_time' (earliest first), 'id', or 'random'.",
          "type": "string"
        },
        "time": {
          "description": "Time period variable.",
          "type": "string"
        },
        "title": {
          "description": "title parameter (Optional[str]).",
          "type": "string"
        },
        "treat": {
          "description": "First-treatment-period column (0 = never treated), or binary treatment indicator.",
          "type": "string"
        },
        "treated_color": {
          "default": "#E74C3C",
          "description": "Color for treated unit-periods.",
          "type": "string"
        },
        "untreated_color": {
          "default": "#ECF0F1",
          "description": "Color for untreated unit-periods.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "time",
        "treat",
        "id"
      ],
      "type": "object"
    }
  },
  {
    "description": "panelView-style display of a panel's treatment status and outcomes.",
    "name": "panel_view",
    "parameters": {
      "properties": {
        "ax": {
          "description": "ax parameter.",
          "type": "string"
        },
        "data": {
          "description": "Long panel.",
          "type": "string"
        },
        "figsize": {
          "default": [
            10,
            6
          ],
          "description": "figsize parameter (Tuple[float, float]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "time": {
          "description": "Unit and time identifiers.",
          "type": "string"
        },
        "treat": {
          "description": "0/1 treatment status in each unit-period.",
          "type": "string"
        },
        "type": {
          "default": "treat",
          "description": "``'treat'`` draws the unit-by-period treatment-status tiles (``sp.treatment_rollout_plot``); ``'outcome'`` draws every unit's outcome path, treated periods highlighted, with the mean path of the never-treated units.",
          "enum": [
            "treat",
            "outcome"
          ],
          "type": "string"
        },
        "unit": {
          "description": "Unit and time identifiers.",
          "type": "string"
        },
        "y": {
          "description": "Outcome column, required for ``type=\"outcome\"``.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "unit",
        "time",
        "treat"
      ],
      "type": "object"
    }
  },
  {
    "description": "Plot Rambachan & Roth (2023) sensitivity analysis.",
    "name": "sensitivity_plot",
    "parameters": {
      "properties": {
        "ax": {
          "description": "ax parameter.",
          "type": "string"
        },
        "breakdown_color": {
          "default": "#E74C3C",
          "description": "Color for the breakdown point marker.",
          "type": "string"
        },
        "color": {
          "default": "#2C3E50",
          "description": "CI band color.",
          "type": "string"
        },
        "figsize": {
          "default": [
            10,
            6
          ],
          "description": "figsize parameter (Tuple[float, float]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "original_ci": {
          "description": "Original CI (at M=0) for comparison.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "original_color": {
          "default": "#27AE60",
          "description": "Color for original estimate marker.",
          "type": "string"
        },
        "original_estimate": {
          "description": "Original point estimate.",
          "type": "number"
        },
        "sensitivity": {
          "description": "Output from ``honest_did()``.",
          "type": "string"
        },
        "title": {
          "description": "title parameter (Optional[str]).",
          "type": "string"
        }
      },
      "required": [
        "sensitivity"
      ],
      "type": "object"
    }
  },
  {
    "description": "Per-cohort event study plot (overlay).",
    "name": "cohort_event_study_plot",
    "parameters": {
      "properties": {
        "aggregate_color": {
          "default": "#2C3E50",
          "description": "Color for aggregate line.",
          "type": "string"
        },
        "ax": {
          "description": "ax parameter.",
          "type": "string"
        },
        "ci": {
          "default": true,
          "description": "Show confidence intervals for each cohort.",
          "type": "boolean"
        },
        "ci_alpha": {
          "default": 0.08,
          "description": "CI band transparency.",
          "type": "number"
        },
        "figsize": {
          "default": [
            12,
            7
          ],
          "description": "figsize parameter (Tuple[float, float]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "palette": {
          "description": "Colors for each cohort. Auto-generated if None.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "result": {
          "description": "Result from ``callaway_santanna()`` or ``did(method='cs')``. Must have ``detail`` with 'group', 'relative_time', 'att' columns, and ``model_info['event_study']`` for aggregate.",
          "type": "string"
        },
        "show_aggregate": {
          "default": true,
          "description": "Overlay the aggregate event study line.",
          "type": "boolean"
        },
        "title": {
          "description": "title parameter (Optional[str]).",
          "type": "string"
        }
      },
      "required": [
        "result"
      ],
      "type": "object"
    }
  },
  {
    "description": "Forest plot of DID method-robustness summary.",
    "name": "did_summary_plot",
    "parameters": {
      "properties": {
        "ax": {
          "description": "Existing axes to draw on. If ``None`` a new figure is created.",
          "type": "string"
        },
        "color": {
          "default": "#2C3E50",
          "description": "Color for point estimates and CIs.",
          "type": "string"
        },
        "figsize": {
          "default": [
            9,
            5
          ],
          "description": "Figure size when creating a new figure.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "highlight_color": {
          "default": "#C0392B",
          "description": "Color for the cross-method mean line.",
          "type": "string"
        },
        "reference": {
          "description": "Horizontal reference value (e.g. 0 for 'no effect'). Defaults to ``0``.",
          "type": "number"
        },
        "result": {
          "description": "Output of :func:`did_summary`. Must have a ``detail`` DataFrame with columns ``estimate``, ``ci_low``, ``ci_high``, and either ``method`` or ``estimator``.",
          "type": "string"
        },
        "sort_by": {
          "description": "If ``'estimate'``, sort methods by point estimate ascending. Otherwise keep the order in ``result.detail``.",
          "type": "string"
        },
        "title": {
          "description": "Plot title. Defaults to ``\"DID Method-Robustness Summary\"``.",
          "type": "string"
        }
      },
      "required": [
        "result"
      ],
      "type": "object"
    }
  },
  {
    "description": "R ``etwfe::emfx``-style aggregated marginal effects for an ETWFE fit. Validation: certified parity evidence.",
    "name": "etwfe_emfx",
    "parameters": {
      "properties": {
        "agg_weights": {
          "description": "For a fit estimated with ``weights=``: whether each cohort-by-period cell enters an aggregate with the sum of the estimation weights over its treated observations (``'estimation'``, Stata ``jwdid, estat``) or with its observation count (``'unit'``, R ``etwfe::emfx``). ``None`` (default) reuses the rule the fit was called with (``result.model_info['agg_weights']``). Ignored for unweighted fits, where the two rules coincide.",
          "enum": [
            "estimation",
            "unit"
          ],
          "type": "string"
        },
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals.",
          "type": "number"
        },
        "include_leads": {
          "default": false,
          "description": "For ``type='event'`` and ``type='calendar'``, whether to include pre-treatment relative times (``rel_time < 0``) in the output. These coefficients identify pre-trends and are informative for parallel-trends inspection. Default ``False`` for backward compatibility with earlier versions; set ``True`` for full event-study output matching the R ``etwfe::emfx(type='event')`` default. ``rel_time = -1`` is always the reference category and is excluded.",
          "type": "boolean"
        },
        "result": {
          "description": "Output of :func:`etwfe` or :func:`wooldridge_did`.",
          "type": "string"
        },
        "type": {
          "default": "simple",
          "description": "Aggregation type.",
          "enum": [
            "simple",
            "group",
            "event",
            "calendar"
          ],
          "type": "string"
        },
        "weighting": {
          "default": "treated",
          "description": "Aggregation weights for cohort-level marginal effects. ``'treated'`` uses the number of treated post-period observations, matching R ``etwfe::emfx(type='simple')`` and Stata ``jwdid, estat simple``. ``'cohort'`` preserves the historical StatsPAI cohort-share weighting.",
          "enum": [
            "cohort",
            "treated"
          ],
          "type": "string"
        }
      },
      "required": [
        "result"
      ],
      "type": "object"
    }
  },
  {
    "description": "One-call method-robustness comparison for staggered DID.",
    "name": "did_summary",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals.",
          "type": "number"
        },
        "cluster": {
          "description": "Cluster variable for SE (defaults to ``group`` in each sub-method).",
          "type": "string"
        },
        "controls": {
          "description": "Time-varying covariates passed to methods that support them.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "data": {
          "description": "Panel dataset (long format).",
          "type": "string"
        },
        "first_treat": {
          "description": "First-treatment period per unit; NaN (or 0) for never-treated.",
          "type": "string"
        },
        "group": {
          "description": "Unit identifier.",
          "type": "string"
        },
        "include_sensitivity": {
          "default": false,
          "description": "If ``True`` and ``'cs'`` is among the methods fit, compute the Rambachan-Roth (2023) *breakdown M\\** -- the largest relative violation of parallel trends under which the treatment effect is still significantly different from zero. The value is added to ``model_info['breakdown_m']`` and to the ``breakdown_m`` column of ``detail`` (CS row only; other methods leave ``NaN``).",
          "type": "boolean"
        },
        "methods": {
          "default": "auto",
          "description": "Methods to run. Valid keys: ``'cs'``, ``'sa'``, ``'bjs'``, ``'etwfe'``, ``'stacked'``, or ``'all'`` / ``'auto'`` for all.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "time": {
          "description": "Time / period variable (integer-valued).",
          "type": "string"
        },
        "verbose": {
          "default": false,
          "description": "Print progress for each method.",
          "type": "boolean"
        },
        "y": {
          "description": "Outcome variable.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "time",
        "first_treat",
        "group"
      ],
      "type": "object"
    }
  },
  {
    "description": "Render a :func:`did_summary` result as a GitHub-Flavoured Markdown table.",
    "name": "did_summary_to_markdown",
    "parameters": {
      "properties": {
        "digits": {
          "default": 4,
          "description": "Decimal precision for numeric columns.",
          "type": "integer"
        },
        "include_breakdown": {
          "default": true,
          "description": "Include the Rambachan-Roth breakdown M* column (CS row only, blank for others). Ignored if sensitivity was not requested.",
          "type": "boolean"
        },
        "include_ci": {
          "default": true,
          "description": "Include the 95 % CI column.",
          "type": "boolean"
        },
        "result": {
          "description": "Output of :func:`did_summary`.",
          "type": "string"
        }
      },
      "required": [
        "result"
      ],
      "type": "object"
    }
  },
  {
    "description": "Render a :func:`did_summary` result as a LaTeX",
    "name": "did_summary_to_latex",
    "parameters": {
      "properties": {
        "caption": {
          "default": "DID method-robustness summary.",
          "description": "LaTeX caption.",
          "type": "string"
        },
        "digits": {
          "default": 4,
          "description": "Decimal precision.",
          "type": "integer"
        },
        "include_breakdown": {
          "default": true,
          "description": "Include the Rambachan-Roth breakdown M* column when sensitivity was requested.",
          "type": "boolean"
        },
        "include_ci": {
          "default": true,
          "description": "Include the 95 % CI column.",
          "type": "boolean"
        },
        "label": {
          "default": "tab:did_summary",
          "description": "LaTeX label for the table.",
          "type": "string"
        },
        "result": {
          "description": "Output of :func:`did_summary`.",
          "type": "string"
        }
      },
      "required": [
        "result"
      ],
      "type": "object"
    }
  },
  {
    "description": "DID report bundle: fits selected methods and writes report artifacts.",
    "name": "did_report",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Same as :func:`did_summary`.",
          "type": "number"
        },
        "cluster": {
          "description": "Same as :func:`did_summary`.",
          "type": "string"
        },
        "controls": {
          "description": "Same as :func:`did_summary`.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "data": {
          "description": "Same as :func:`did_summary`.",
          "type": "string"
        },
        "first_treat": {
          "description": "Same as :func:`did_summary`.",
          "type": "string"
        },
        "group": {
          "description": "Same as :func:`did_summary`.",
          "type": "string"
        },
        "include_sensitivity": {
          "default": true,
          "description": "Whether to run Rambachan-Roth breakdown M*. Defaults to ``True`` in ``did_report`` (vs ``False`` in ``did_summary``) because a report is expected to be comprehensive.",
          "type": "boolean"
        },
        "methods": {
          "default": "auto",
          "description": "Same as :func:`did_summary`.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "plot_sort_by": {
          "default": "estimate",
          "description": "Sort the forest plot by estimate ascending.",
          "type": "string"
        },
        "save_to": {
          "description": "Directory path. Created if it does not exist.",
          "type": "string"
        },
        "time": {
          "description": "Same as :func:`did_summary`.",
          "type": "string"
        },
        "verbose": {
          "default": false,
          "description": "Print progress lines.",
          "type": "boolean"
        },
        "y": {
          "description": "Same as :func:`did_summary`.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "time",
        "first_treat",
        "group",
        "save_to"
      ],
      "type": "object"
    }
  },
  {
    "description": "TWFE decomposition: Goodman-Bacon (2021) + de Chaisemartin-D'Haultfoeuille weights. Validation: validated evidence tier (known-truth, reference, external-parity, or Monte Carlo artifact).",
    "name": "twfe_decomposition",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level.",
          "type": "number"
        },
        "data": {
          "description": "Panel dataset in long format.",
          "type": "string"
        },
        "first_treat": {
          "description": "Treatment timing column (NaN or 0 for never-treated).",
          "type": "string"
        },
        "group": {
          "description": "Unit identifier.",
          "type": "string"
        },
        "time": {
          "description": "Time period variable.",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "group",
        "time",
        "first_treat"
      ],
      "type": "object"
    }
  },
  {
    "description": "RD plot: binned scatter with polynomial fit on each side of the cutoff. Validation: certified parity evidence.",
    "name": "rdplot",
    "parameters": {
      "properties": {
        "ax": {
          "description": "ax parameter (Optional[Any]).",
          "type": "string"
        },
        "binselect": {
          "default": "esmv",
          "description": "Bin selection rule, as in R ``rdplot``: - 'es' : IMSE-optimal evenly spaced, spacings estimators - 'espr' : IMSE-optimal evenly spaced, polynomial regression - 'esmv' : mimicking-variance evenly spaced, spacings (default) - 'esmvpr' : mimicking-variance evenly spaced, polynomial regression - 'qs', 'qspr', 'qsmv', 'qsmvpr' : the quantile-spaced analogues With 20% or more mass points on either side the spacings variants switch to their polynomial-regression versions (R's ``masspoints='adjust'``).",
          "type": "string"
        },
        "c": {
          "default": 0,
          "description": "Cutoff.",
          "type": "number"
        },
        "ci_level": {
          "default": 0.95,
          "description": "Level of the per-bin t intervals and the polynomial CI band.",
          "type": "number"
        },
        "covs": {
          "description": "Covariates, adjusted for as R ``rdplot(covs=, covs_eval='mean')`` does.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "donut": {
          "default": 0,
          "description": "If > 0, shades the donut region |x - c| <= donut.",
          "type": "number"
        },
        "figsize": {
          "default": [
            10,
            7
          ],
          "description": "figsize parameter (Tuple[float, float]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "h": {
          "description": "Support of the global polynomial fit on each side (R's ``h``); defaults to the full range of ``x`` on that side.",
          "type": "number"
        },
        "hide_ci": {
          "default": false,
          "description": "If True, suppress all interval displays.",
          "type": "boolean"
        },
        "kernel": {
          "default": "uniform",
          "description": "Kernel weighting the global polynomial over ``[c - h, c + h]``. R's default. (Through 1.28.0 the default read 'triangular' but the argument was ignored and the fit was unweighted.)",
          "enum": [
            "uniform",
            "triangular",
            "epanechnikov"
          ],
          "type": "string"
        },
        "nbins": {
          "description": "Bins per side. If None, chosen by ``binselect``.",
          "type": "integer"
        },
        "p": {
          "default": 4,
          "description": "Order of the global polynomial fitted on each side.",
          "type": "integer"
        },
        "scatter": {
          "default": true,
          "description": "Show binned means.",
          "type": "boolean"
        },
        "shade_ci": {
          "default": true,
          "description": "Shade a pointwise CI band around the polynomial (a StatsPAI addition; not shown when ``covs`` is given).",
          "type": "boolean"
        },
        "show_bw": {
          "default": false,
          "description": "If True, shades the ``sp.rdrobust`` bandwidth window.",
          "type": "boolean"
        },
        "title": {
          "description": "title parameter (Optional[str]).",
          "type": "string"
        },
        "weights": {
          "description": "Observation weights for the polynomial fit.",
          "type": "string"
        },
        "x": {
          "description": "Outcome and running variable names.",
          "type": "string"
        },
        "x_label": {
          "description": "x_label parameter (Optional[str]).",
          "type": "string"
        },
        "y": {
          "description": "Outcome and running variable names.",
          "type": "string"
        },
        "y_label": {
          "description": "y_label parameter (Optional[str]).",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "x"
      ],
      "type": "object"
    }
  },
  {
    "description": "Density of the running variable on each side of the cutoff. Validation: certified parity evidence.",
    "name": "rdplotdensity",
    "parameters": {
      "properties": {
        "ax": {
          "description": "ax parameter (Optional[Any]).",
          "type": "string"
        },
        "c": {
          "default": 0,
          "description": "Cutoff.",
          "type": "number"
        },
        "ci_level": {
          "default": 0.95,
          "description": "Level of the robust bias-corrected band ``f_q +/- z se_q``.",
          "type": "number"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "figsize": {
          "default": [
            10,
            7
          ],
          "description": "figsize parameter (Tuple[float, float]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "h": {
          "description": "Bandwidths ``(h_left, h_right)``; by default those of :func:`sp.rddensity` at the same ``c`` and ``p``.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "hist": {
          "default": true,
          "description": "Overlay a density-scaled histogram.",
          "type": "boolean"
        },
        "n_grid": {
          "default": 50,
          "description": "Grid points per side (R's ``plotN``, whose default is 10).",
          "type": "integer"
        },
        "nbins": {
          "default": 30,
          "description": "Histogram bins per side.",
          "type": "integer"
        },
        "p": {
          "default": 2,
          "description": "Local polynomial order (``rddensity``'s ``p``).",
          "type": "integer"
        },
        "title": {
          "description": "title parameter (Optional[str]).",
          "type": "string"
        },
        "x": {
          "description": "Running variable.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "x"
      ],
      "type": "object"
    }
  },
  {
    "description": "Bandwidth selection for local polynomial RD estimation. Validation: certified parity evidence.",
    "name": "rdbwselect",
    "parameters": {
      "properties": {
        "all": {
          "default": false,
          "description": "If True, compute and return all ten bandwidth types.",
          "type": "boolean"
        },
        "bwselect": {
          "default": "mserd",
          "description": "Bandwidth selection method. One of: - ``'mserd'`` : MSE-optimal common bandwidth (default) - ``'msetwo'`` : MSE-optimal separate left/right bandwidths - ``'msesum'`` : MSE-optimal for the sum of the two intercepts - ``'msecomb1'`` : ``min(mserd, msesum)``, per side - ``'msecomb2'`` : ``median(msetwo, mserd, msesum)``, per side - ``'cerrd'``, ``'certwo'``, ``'cersum'``, ``'cercomb1'``, ``'cercomb2'`` : the CER-optimal counterparts of the above The combination rules are applied to the finished ``h`` and ``b`` of each cascade, element-wise per side -- not stage by stage.",
          "type": "string"
        },
        "c": {
          "default": 0,
          "description": "RD cutoff value.",
          "type": "number"
        },
        "cluster": {
          "description": "Cluster variable name for cluster-robust variance estimation.",
          "type": "string"
        },
        "covs": {
          "description": "Covariate column names. When provided, the variance estimates used in bandwidth selection account for covariate adjustment, typically yielding narrower bandwidths.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "data": {
          "description": "Input dataset.",
          "type": "string"
        },
        "deriv": {
          "default": 0,
          "description": "Derivative order. 0 = standard RD (jump in level), 1 = regression kink design (change in slope).",
          "type": "integer"
        },
        "fuzzy": {
          "description": "Treatment variable name for fuzzy RD. The MSE being minimised is then the one for the Wald ratio rather than for the reduced form, so every stage of the cascade changes. .. versionchanged:: 1.27.0 This argument was parsed and then discarded: it was never passed to the bandwidth cascade, so ``fuzzy=`` returned the sharp bandwidth while the docstring claimed otherwise. On a two-sided noncompliance replica of the Lee 2008 senate data that is a 9% to 16% error in ``h``. Designs with **one-sided** noncompliance were unaffected, because ``rdbwselect`` itself falls back to the sharp bandwidth there (R's ``perf_comp``) -- which is also why the defect survived: the fixture in the repository was one-sided.",
          "type": "string"
        },
        "kernel": {
          "default": "triangular",
          "description": "Kernel function: 'triangular', 'uniform', or 'epanechnikov'.",
          "type": "string"
        },
        "p": {
          "default": 1,
          "description": "Polynomial order for point estimation (1 = local linear).",
          "type": "integer"
        },
        "q": {
          "description": "Polynomial order for bias correction. Default is p + 1.",
          "type": "integer"
        },
        "x": {
          "description": "Running variable column name.",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "x"
      ],
      "type": "object"
    }
  },
  {
    "description": "Bandwidth sensitivity analysis for RD estimates.",
    "name": "rdbwsensitivity",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "ax": {
          "description": "ax parameter (Optional[Any]).",
          "type": "string"
        },
        "bw_grid": {
          "description": "Explicit bandwidth values to evaluate. If None, auto-generates a grid as multiples of the MSE-optimal bandwidth.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "bw_range": {
          "default": [
            0.5,
            2.0
          ],
          "description": "Range of multipliers for the optimal bandwidth.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "c": {
          "default": 0,
          "description": "Cutoff.",
          "type": "number"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "figsize": {
          "default": [
            10,
            6
          ],
          "description": "figsize parameter (Tuple[float, float]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "fuzzy": {
          "description": "Treatment variable for fuzzy RD.",
          "type": "string"
        },
        "kernel": {
          "default": "triangular",
          "description": "Kernel function used for weighting or smoothing.",
          "type": "string"
        },
        "n_grid": {
          "default": 15,
          "description": "Number of grid points if bw_grid is None.",
          "type": "integer"
        },
        "p": {
          "default": 1,
          "description": "Polynomial order.",
          "type": "integer"
        },
        "x": {
          "description": "Outcome and running variable names.",
          "type": "string"
        },
        "y": {
          "description": "Outcome and running variable names.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "x"
      ],
      "type": "object"
    }
  },
  {
    "description": "Covariate balance test at the RD cutoff.",
    "name": "rdbalance",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "c": {
          "default": 0,
          "description": "Cutoff.",
          "type": "number"
        },
        "covs": {
          "description": "Covariate names to test. If None, tests all numeric columns except x.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "h": {
          "description": "Manual bandwidth. If None, uses MSE-optimal per covariate.",
          "type": "number"
        },
        "kernel": {
          "default": "triangular",
          "description": "Kernel function used for weighting or smoothing.",
          "type": "string"
        },
        "p": {
          "default": 1,
          "description": "p parameter (int).",
          "type": "integer"
        },
        "x": {
          "description": "Running variable name.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "x"
      ],
      "type": "object"
    }
  },
  {
    "description": "Placebo cutoff test for RD validity.",
    "name": "rdplacebo",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "ax": {
          "description": "ax parameter (Optional[Any]).",
          "type": "string"
        },
        "c": {
          "default": 0,
          "description": "True cutoff.",
          "type": "number"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "figsize": {
          "default": [
            10,
            6
          ],
          "description": "figsize parameter (Tuple[float, float]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "fuzzy": {
          "description": "Treatment variable for fuzzy RD.",
          "type": "string"
        },
        "kernel": {
          "default": "triangular",
          "description": "Kernel function used for weighting or smoothing.",
          "type": "string"
        },
        "n_placebo": {
          "default": 10,
          "description": "Number of placebo cutoffs if auto-generating.",
          "type": "integer"
        },
        "p": {
          "default": 1,
          "description": "p parameter (int).",
          "type": "integer"
        },
        "placebo_cutoffs": {
          "description": "Explicit placebo cutoff values. If None, auto-generates from the data distribution.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "side": {
          "default": "both",
          "description": "Which side of the true cutoff to place placebos: 'left', 'right', or 'both'.",
          "type": "string"
        },
        "x": {
          "description": "Outcome and running variable names.",
          "type": "string"
        },
        "y": {
          "description": "Outcome and running variable names.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "x"
      ],
      "type": "object"
    }
  },
  {
    "description": "One-stop RD diagnostic battery.",
    "name": "rdsummary",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "c": {
          "default": 0,
          "description": "Cutoff.",
          "type": "number"
        },
        "covs": {
          "description": "Pre-treatment covariates for balance test.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "full": {
          "default": false,
          "description": "Run extended diagnostics (honest CI, power, placebos).",
          "type": "boolean"
        },
        "fuzzy": {
          "description": "Treatment variable for fuzzy RD.",
          "type": "string"
        },
        "kernel": {
          "default": "triangular",
          "description": "Kernel function used for weighting or smoothing.",
          "type": "string"
        },
        "p": {
          "default": 1,
          "description": "p parameter (int).",
          "type": "integer"
        },
        "plot": {
          "default": false,
          "description": "Generate a multi-panel diagnostic plot.",
          "type": "boolean"
        },
        "verbose": {
          "default": true,
          "description": "Print formatted summary to console.",
          "type": "boolean"
        },
        "x": {
          "description": "Outcome and running variable.",
          "type": "string"
        },
        "y": {
          "description": "Outcome and running variable.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "x"
      ],
      "type": "object"
    }
  },
  {
    "description": "Regression Kink Design estimator (Card et al., 2015). Validation: certified parity evidence.",
    "name": "rkd",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals.",
          "type": "number"
        },
        "c": {
          "default": 0,
          "description": "Kink point (cutoff).",
          "type": "number"
        },
        "cluster": {
          "description": "Cluster variable name for clustered standard errors.",
          "type": "string"
        },
        "data": {
          "description": "Input dataset.",
          "type": "string"
        },
        "h": {
          "description": "Bandwidth. If None, the CCT MSE-optimal bandwidth for the first derivative (``rdrobust(deriv=1, bwselect='mserd', vce='hc1')``) is selected.",
          "type": "number"
        },
        "kernel": {
          "default": "triangular",
          "description": "Kernel function: 'triangular', 'epanechnikov', or 'uniform'.",
          "type": "string"
        },
        "p": {
          "default": 1,
          "description": "Local polynomial order (1 = local linear, the default and most common choice for RKD).",
          "type": "integer"
        },
        "treatment": {
          "description": "Treatment variable for fuzzy RKD. If None, estimate the reduced-form kink in E[Y|X] (sharp / reduced-form RKD).",
          "type": "string"
        },
        "x": {
          "description": "Running variable name.",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable name.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "x"
      ],
      "type": "object"
    }
  },
  {
    "description": "Regression Discontinuity in Time (Hausman & Rapson, 2018).",
    "name": "rdit",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals.",
          "type": "number"
        },
        "cluster": {
          "description": "Cluster variable for clustered standard errors. If provided, cluster-robust SEs are used instead of HAC.",
          "type": "string"
        },
        "cutoff": {
          "description": "The policy change date / time cutoff.",
          "type": "integer"
        },
        "data": {
          "description": "Input dataset.",
          "type": "string"
        },
        "donut": {
          "default": 0,
          "description": "Donut hole: exclude observations within +/- donut units of the cutoff (in the same numeric time units).",
          "type": "integer"
        },
        "h": {
          "description": "Bandwidth in the same units as the numeric time axis (days if datetime). If None, an MSE-optimal bandwidth is selected automatically.",
          "type": "number"
        },
        "kernel": {
          "default": "triangular",
          "description": "Kernel function: 'triangular', 'epanechnikov', 'uniform', or 'gaussian'.",
          "type": "string"
        },
        "p": {
          "default": 1,
          "description": "Local polynomial order (1 = local linear).",
          "type": "integer"
        },
        "seasonality": {
          "description": "Deseasonalise before estimation. One of 'month', 'quarter', 'dow' (day-of-week). Regresses Y on seasonal dummies and uses residuals.",
          "type": "string"
        },
        "time": {
          "description": "Time variable name (datetime or numeric).",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable name.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "time",
        "cutoff"
      ],
      "type": "object"
    }
  },
  {
    "description": "Randomization inference for regression discontinuity designs. Validation: certified parity evidence.",
    "name": "rdrandinf",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level.",
          "type": "number"
        },
        "c": {
          "default": 0,
          "description": "RD cutoff value.",
          "type": "number"
        },
        "covs": {
          "description": "Covariate names to partial out before testing.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "data": {
          "description": "Input dataset.",
          "type": "string"
        },
        "fuzzy": {
          "description": "Actual treatment variable for fuzzy RD. The Wald (IV) estimator is computed within the window.",
          "type": "string"
        },
        "kernel": {
          "default": "uniform",
          "description": "Kernel weighting (only 'uniform' currently supported for local randomization).",
          "type": "string"
        },
        "n_perms": {
          "default": 1000,
          "description": "Number of permutations for Fisher randomization test.",
          "type": "integer"
        },
        "p": {
          "default": 0,
          "description": "Polynomial order for adjustment (0 = unadjusted).",
          "type": "integer"
        },
        "seed": {
          "default": 42,
          "description": "Random seed for reproducibility.",
          "type": "integer"
        },
        "statistic": {
          "default": "diffmeans",
          "description": "Test statistic: 'diffmeans', 'ksmirnov', 'ranksum', or 'all'. ``'ttest'`` is accepted as an alias for ``'diffmeans'``, matching rdlocrand, where both names select the same statistic.",
          "type": "string"
        },
        "wl": {
          "description": "Window left bound offset from cutoff (typically negative). The left edge of the window is ``c + wl``.",
          "type": "number"
        },
        "wr": {
          "description": "Window right bound offset from cutoff (typically positive). The right edge of the window is ``c + wr``.",
          "type": "number"
        },
        "x": {
          "description": "Running variable name.",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable name.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "x"
      ],
      "type": "object"
    }
  },
  {
    "description": "Data-driven window selection for local randomization RD. Validation: certified parity evidence.",
    "name": "rdwinselect",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.15,
          "description": "Significance level for balance (lenient by default to be conservative about window selection).",
          "type": "number"
        },
        "c": {
          "default": 0,
          "description": "RD cutoff value.",
          "type": "number"
        },
        "covs": {
          "description": "Covariate names to test balance for. If None, uses quantiles of the running variable as pseudo-covariates.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "data": {
          "description": "Input dataset.",
          "type": "string"
        },
        "nwindows": {
          "default": 10,
          "description": "Number of windows to evaluate.",
          "type": "integer"
        },
        "p": {
          "default": 0,
          "description": "Polynomial order for adjustment.",
          "type": "integer"
        },
        "seed": {
          "default": 42,
          "description": "Random seed.",
          "type": "integer"
        },
        "statistic": {
          "default": "diffmeans",
          "description": "Test statistic for balance testing.",
          "type": "string"
        },
        "wmin": {
          "description": "Minimum half-window width. Defaults to the smallest gap between adjacent observations near the cutoff.",
          "type": "number"
        },
        "wstep": {
          "description": "Window increment. Defaults to ``(max_range - wmin) / nwindows``.",
          "type": "number"
        },
        "x": {
          "description": "Running variable name.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "x"
      ],
      "type": "object"
    }
  },
  {
    "description": "Sensitivity of RD estimates across different window widths. Validation: certified parity evidence.",
    "name": "rdsensitivity",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level.",
          "type": "number"
        },
        "c": {
          "default": 0,
          "description": "RD cutoff value.",
          "type": "number"
        },
        "data": {
          "description": "Input dataset.",
          "type": "string"
        },
        "n_perms": {
          "default": 500,
          "description": "Number of permutations per window.",
          "type": "integer"
        },
        "nwindows": {
          "default": 20,
          "description": "Number of windows when ``wlist`` is None.",
          "type": "integer"
        },
        "p": {
          "default": 0,
          "description": "Polynomial order for adjustment.",
          "type": "integer"
        },
        "plot": {
          "default": false,
          "description": "plot parameter (bool).",
          "type": "boolean"
        },
        "seed": {
          "default": 42,
          "description": "Random seed.",
          "type": "integer"
        },
        "statistic": {
          "default": "diffmeans",
          "description": "Test statistic for inference.",
          "type": "string"
        },
        "wlist": {
          "description": "Symmetric half-window widths to evaluate. If None, an evenly-spaced grid is generated automatically.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "x": {
          "description": "Running variable name.",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable name.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "x"
      ],
      "type": "object"
    }
  },
  {
    "description": "Rosenbaum sensitivity bounds for RD under local randomization. Validation: certified parity evidence.",
    "name": "rdrbounds",
    "parameters": {
      "properties": {
        "c": {
          "default": 0,
          "description": "RD cutoff value.",
          "type": "number"
        },
        "data": {
          "description": "Input dataset.",
          "type": "string"
        },
        "gamma_list": {
          "description": "Odds ratios. Defaults to ``[1, 1.5, 2, 2.5, 3, 4, 5]``.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "n_perms": {
          "default": 1000,
          "description": "Bernoulli assignment draws per p-value. The same draws are reused across thresholds and ``Gamma`` values (common random numbers, as R reseeds before each p-value).",
          "type": "integer"
        },
        "seed": {
          "default": 42,
          "description": "Random seed.",
          "type": "integer"
        },
        "statistic": {
          "default": "ranksum",
          "description": "``'ranksum'`` is R's standardised rank sum (control-rank sum minus its mean over ``sqrt(n0 n1 var(ranks) / n)``); ``'diffmeans'`` the difference in means.",
          "enum": [
            "ranksum",
            "diffmeans"
          ],
          "type": "string"
        },
        "wl": {
          "description": "Window ``[c + wl, c + wr]`` (``wl`` typically negative).",
          "type": "number"
        },
        "wr": {
          "description": "Window ``[c + wl, c + wr]`` (``wl`` typically negative).",
          "type": "number"
        },
        "x": {
          "description": "Running variable name.",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable name.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "x"
      ],
      "type": "object"
    }
  },
  {
    "description": "Conditional average treatment effects (CATE) in sharp RD designs. Validation: certified parity evidence.",
    "name": "rdhte",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "b": {
          "description": "Bandwidth of the bias-correction fit. Defaults to ``h`` (R has no separate ``b``). Through 1.28.0 this argument was accepted and ignored.",
          "type": "number"
        },
        "bwselect": {
          "default": "mserd",
          "description": "Any ``rdbwselect`` rule.",
          "type": "string"
        },
        "c": {
          "default": 0,
          "description": "Cutoff.",
          "type": "number"
        },
        "cluster": {
          "description": "Cluster variable (CR1).",
          "type": "string"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "eval_points": {
          "description": "``z`` values at which to report ``CATE(z)`` (continuous ``z``).",
          "type": "string"
        },
        "h": {
          "description": "Bandwidth, or ``(h_left, h_right)``. Default: ``rdrobust``'s ``rdbwselect`` (``bwselect``) on the running variable -- separately in each subgroup for a binary ``z``, as R does.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "kernel": {
          "default": "triangular",
          "description": "Kernel function used for weighting or smoothing.",
          "enum": [
            "triangular",
            "epanechnikov",
            "uniform"
          ],
          "type": "string"
        },
        "n_eval": {
          "default": 20,
          "description": "Number of eval.",
          "type": "integer"
        },
        "p": {
          "default": 1,
          "description": "Polynomial order of the point estimate.",
          "type": "integer"
        },
        "q": {
          "description": "Order of the bias-correction fit, default ``p + 1``.",
          "type": "integer"
        },
        "vce": {
          "description": "Default ``'hc3'``, or ``'cr1'`` with ``cluster`` (R's defaults).",
          "enum": [
            "hc0",
            "hc1",
            "hc2",
            "hc3",
            "cr1"
          ],
          "type": "string"
        },
        "x": {
          "description": "Outcome and running variable.",
          "type": "string"
        },
        "y": {
          "description": "Outcome and running variable.",
          "type": "string"
        },
        "z": {
          "description": "Covariate(s) the effect varies with. A single 0/1 column is treated as two subgroups (R's factor rule).",
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "required": [
        "data",
        "y",
        "x",
        "z"
      ],
      "type": "object"
    }
  },
  {
    "description": "Bandwidth selection for :func:`rdhte`, as R ``rdhte::rdbwhte``. Validation: certified parity evidence.",
    "name": "rdbwhte",
    "parameters": {
      "properties": {
        "bwselect": {
          "default": "mserd",
          "description": "bwselect parameter (str).",
          "type": "string"
        },
        "c": {
          "default": 0,
          "description": "c parameter (float).",
          "type": "number"
        },
        "cluster": {
          "description": "Cluster identifier column for clustered standard errors.",
          "type": "string"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "kernel": {
          "default": "triangular",
          "description": "Kernel function used for weighting or smoothing.",
          "type": "string"
        },
        "p": {
          "default": 1,
          "description": "p parameter (int).",
          "type": "integer"
        },
        "q": {
          "description": "Quantile level.",
          "type": "integer"
        },
        "vce": {
          "description": "vce parameter (Optional[str]).",
          "type": "string"
        },
        "x": {
          "description": "Primary running variable, regressor, or feature input for this estimator.",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        },
        "z": {
          "description": "Instrument, proxy, or auxiliary variable used by this estimator.",
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "required": [
        "data",
        "y",
        "x",
        "z"
      ],
      "type": "object"
    }
  },
  {
    "description": "Linear combinations of :func:`rdhte` effects, as R ``rdhte_lincom``. Validation: certified parity evidence.",
    "name": "rdhte_lincom",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "linfct": {
          "description": "linfct parameter (Optional[numpy.ndarray]).",
          "type": "string"
        },
        "result": {
          "description": "result parameter (CausalResult).",
          "type": "string"
        },
        "weights": {
          "description": "Observation weights.",
          "type": "string"
        }
      },
      "required": [
        "result"
      ],
      "type": "object"
    }
  },
  {
    "description": "Causal Forest for RD -- heterogeneous treatment effect estimation.",
    "name": "rd_forest",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals.",
          "type": "number"
        },
        "c": {
          "default": 0,
          "description": "RD cutoff.",
          "type": "number"
        },
        "covs": {
          "description": "Covariate names used as features for heterogeneity detection. Must not include the running variable *x*.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "data": {
          "description": "Input dataset.",
          "type": "string"
        },
        "h": {
          "description": "Bandwidth (uses IK-style automatic selection if None).",
          "type": "number"
        },
        "honesty": {
          "default": true,
          "description": "Split-sample (honest) estimation: half the data for tree construction, the other half for leaf predictions.",
          "type": "boolean"
        },
        "min_leaf": {
          "default": 20,
          "description": "Minimum leaf size (larger -> more regularisation).",
          "type": "integer"
        },
        "n_trees": {
          "default": 500,
          "description": "Number of trees in each forest.",
          "type": "integer"
        },
        "seed": {
          "default": 42,
          "description": "Random seed.",
          "type": "integer"
        },
        "x": {
          "description": "Running variable.",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "x"
      ],
      "type": "object"
    }
  },
  {
    "description": "Gradient Boosting for RD -- flexible CATE estimation.",
    "name": "rd_boost",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level.",
          "type": "number"
        },
        "c": {
          "default": 0,
          "description": "RD cutoff.",
          "type": "number"
        },
        "covs": {
          "description": "Covariate names for heterogeneity.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "data": {
          "description": "Input dataset.",
          "type": "string"
        },
        "h": {
          "description": "Bandwidth (auto-selected if None).",
          "type": "number"
        },
        "learning_rate": {
          "default": 0.1,
          "description": "Shrinkage factor.",
          "type": "number"
        },
        "max_depth": {
          "default": 3,
          "description": "Maximum tree depth per round.",
          "type": "integer"
        },
        "n_estimators": {
          "default": 200,
          "description": "Number of boosting rounds.",
          "type": "integer"
        },
        "seed": {
          "default": 42,
          "description": "Random seed.",
          "type": "integer"
        },
        "x": {
          "description": "Running variable.",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "x"
      ],
      "type": "object"
    }
  },
  {
    "description": "LASSO-assisted RD via post-double-selection.",
    "name": "rd_lasso",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level.",
          "type": "number"
        },
        "c": {
          "default": 0,
          "description": "RD cutoff.",
          "type": "number"
        },
        "covs": {
          "description": "Candidate covariates (can be large set).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "cv_folds": {
          "default": 5,
          "description": "Cross-validation folds for LASSO penalty selection.",
          "type": "integer"
        },
        "data": {
          "description": "Input dataset.",
          "type": "string"
        },
        "h": {
          "description": "Bandwidth (auto-selected if None).",
          "type": "number"
        },
        "kernel": {
          "default": "triangular",
          "description": "Kernel for local linear regression ('triangular', 'uniform', 'epanechnikov').",
          "type": "string"
        },
        "x": {
          "description": "Running variable.",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "x"
      ],
      "type": "object"
    }
  },
  {
    "description": "Run multiple ML-RD methods and compare CATE estimates.",
    "name": "rd_cate_summary",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level.",
          "type": "number"
        },
        "c": {
          "default": 0,
          "description": "RD cutoff.",
          "type": "number"
        },
        "covs": {
          "description": "Covariates for heterogeneity / selection.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "data": {
          "description": "Input dataset.",
          "type": "string"
        },
        "h": {
          "description": "Bandwidth (shared across methods).",
          "type": "number"
        },
        "methods": {
          "description": "Subset of ``['forest', 'boost', 'lasso']``. Default: all three.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "seed": {
          "default": 42,
          "description": "Random seed.",
          "type": "integer"
        },
        "x": {
          "description": "Running variable.",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "x"
      ],
      "type": "object"
    }
  },
  {
    "description": "Angrist-Rokkanen (2015) extrapolation of RD effects away from the cutoff.",
    "name": "rd_extrapolate",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level.",
          "type": "number"
        },
        "c": {
          "default": 0,
          "description": "RD cutoff value.",
          "type": "number"
        },
        "covs": {
          "description": "Covariate names for conditional independence. Required.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "data": {
          "description": "Input dataset.",
          "type": "string"
        },
        "eval_points": {
          "description": "Running variable values at which to extrapolate the CATE. If None, ``n_eval`` equally spaced points spanning the data range.",
          "type": "string"
        },
        "h_local": {
          "description": "Bandwidth for local RD estimate at the cutoff (for comparison). If None, the MSE-optimal bandwidth from ``rdrobust`` is used.",
          "type": "number"
        },
        "method": {
          "default": "ols",
          "description": "Estimation method: ``'ols'``, ``'ipw'``, or ``'doubly_robust'``.",
          "type": "string"
        },
        "n_eval": {
          "default": 20,
          "description": "Number of evaluation points when ``eval_points`` is None.",
          "type": "integer"
        },
        "treatment": {
          "description": "Treatment variable for fuzzy RD. If None, sharp design assumed (D = 1{X >= c}).",
          "type": "string"
        },
        "x": {
          "description": "Running variable name.",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable name.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "x"
      ],
      "type": "object"
    }
  },
  {
    "description": "Multi-cutoff RD extrapolation (Cattaneo, Keele, Titiunik, Vazquez-Bare 2021).",
    "name": "rd_multi_extrapolate",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level.",
          "type": "number"
        },
        "cutoffs": {
          "description": "Cutoff values. Must contain at least 2 cutoffs.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "data": {
          "description": "Input dataset.",
          "type": "string"
        },
        "eval_points": {
          "description": "Running variable values at which to predict tau(x). Defaults to 30 equally spaced points spanning the data range.",
          "type": "string"
        },
        "method": {
          "default": "linear",
          "description": "Interpolation method: - ``'linear'``: tau(x) = a + b*x - ``'polynomial'``: polynomial of degree min(len(cutoffs)-1, 3) - ``'weighted'``: inverse-variance weighted local linear",
          "type": "string"
        },
        "x": {
          "description": "Running variable name.",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable name.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "x",
        "cutoffs"
      ],
      "type": "object"
    }
  },
  {
    "description": "Diagnostic assessment of RD external validity.",
    "name": "rd_external_validity",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level.",
          "type": "number"
        },
        "c": {
          "default": 0,
          "description": "RD cutoff value.",
          "type": "number"
        },
        "covs": {
          "description": "Covariate names for overlap and CI testing.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "data": {
          "description": "Input dataset.",
          "type": "string"
        },
        "target_x_range": {
          "description": "Running variable range ``(x_low, x_high)`` defining the target population. Defaults to the full data range.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "x": {
          "description": "Running variable name.",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable name.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "x"
      ],
      "type": "object"
    }
  },
  {
    "description": "Canonical Synthetic Control estimator (Abadie, Diamond & Hainmueller",
    "name": "SyntheticControl",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals.",
          "type": "number"
        },
        "covariates": {
          "description": "Column names whose pre-treatment means are used as predictors for the V-weighted matching problem.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "data": {
          "description": "Long-format panel ``(unit, time, outcome, ...)``.",
          "type": "string"
        },
        "n_jobs": {
          "default": 1,
          "description": "Worker processes for the in-space placebo loop; ``-1`` uses every CPU. Each placebo fit is independent and runs the same code, so the results are bit-identical to ``n_jobs=1``. This matters most with ``covariates`` / ``special_predictors``, where every placebo re-solves the nested V-W problem. Workers are started with ``spawn``: in a script, call from under ``if __name__ == \"__main__\":``. If the process pool cannot start, the loop falls back to serial with a ``RuntimeWarning`` and records why in ``model_info['placebo_parallel_fallback']``.",
          "type": "integer"
        },
        "n_random_starts": {
          "default": 4,
          "description": "Additional random Dirichlet starts for the outer V optimiser.",
          "type": "integer"
        },
        "outcome": {
          "description": "Column names.",
          "type": "string"
        },
        "penalization": {
          "default": 0.0,
          "description": "Ridge penalty on donor weights.",
          "type": "number"
        },
        "perfect_fit": {
          "default": "legacy",
          "description": "Rule for the nested V-W fit when the treated unit's predictors lie inside the convex hull of the donors' predictors, which is checked exactly by linear programming and reported as ``model_info['in_predictor_hull']``. Inside the hull every V with full support fits the predictors perfectly and infinitely many donor-weight vectors do so, so the nested weights depend on the optimiser's path and each fit can take minutes. ``'legacy'`` (default) runs the ADH outer V search regardless. ``'exact_balance'`` instead returns, among the weights that balance *every* predictor exactly, the one with the smallest pre-treatment outcome MSPE: a convex QP solved once in well under a second and certified by a Frank-Wolfe gap; ``model_info['v_identified']`` is then False and the reported V is the identity. This is a different estimator, not a faster ADH: the V search can push some V entries to zero, dropping those predictors, and so can fit the outcome better (on Prop 99 with four covariates, Montana's pre-period SSE is 12417 under ``'exact_balance'`` against 2898 under the V search, while Georgia's is 122 against 1022). Placebo fits follow the same rule. It does not apply with ``penalization > 0`` or on the outcome-lags-only equal-V path.",
          "enum": [
            "legacy",
            "exact_balance"
          ],
          "type": "string"
        },
        "special_predictors": {
          "description": "R/Stata ``Synth``-style predictor specifications. Each entry is ``(column, period_spec, op)`` where ``period_spec`` is a scalar year, a list of years, or a ``slice(start, stop)`` (inclusive), and ``op`` is ``'mean'`` or ``'sum'``. When omitted together with ``covariates``, the pre-treatment outcome vector itself is used as the predictor (V has no identifying power and is fixed to the identity, following Kaul et al. 2022).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "standardize_predictors": {
          "default": true,
          "description": "Rescale predictors to unit range before the V optimization.",
          "type": "boolean"
        },
        "time": {
          "description": "Column names.",
          "type": "string"
        },
        "treated_unit": {
          "description": "Identifier of the treated unit.",
          "type": "string"
        },
        "treatment_time": {
          "description": "First treatment period (inclusive).",
          "type": "string"
        },
        "unit": {
          "description": "Column names.",
          "type": "string"
        },
        "v_method": {
          "default": "auto",
          "description": "``'auto'`` -> nested V-W when covariates / special predictors are supplied, equal V otherwise. ``'nested'`` forces the outer V optimisation even when only Y lags are used (note: the outer problem is then under-identified, per Kaul et al. 2022). Equal V reduces to the outcome-only simplex LS estimator.",
          "enum": [
            "auto",
            "nested",
            "equal"
          ],
          "type": "string"
        }
      },
      "required": [
        "data",
        "outcome",
        "unit",
        "time",
        "treated_unit",
        "treatment_time"
      ],
      "type": "object"
    }
  },
  {
    "description": "De-meaned / De-trended Synthetic Control Method. Validation: certified parity evidence.",
    "name": "demeaned_synth",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level.",
          "type": "number"
        },
        "covariates": {
          "description": "Not supported: the weights are fitted on (de-meaned / de-trended) pre-treatment outcomes only. Passing covariates raises ``NotImplementedError`` (it used to be silently ignored).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "data": {
          "description": "Long-format panel data.",
          "type": "string"
        },
        "outcome": {
          "description": "Outcome variable name.",
          "type": "string"
        },
        "penalization": {
          "default": 0.0,
          "description": "Ridge penalty on weights.",
          "type": "number"
        },
        "placebo": {
          "default": true,
          "description": "Run in-space placebo inference.",
          "type": "boolean"
        },
        "time": {
          "description": "Time period column.",
          "type": "string"
        },
        "treated_unit": {
          "description": "Identifier of the treated unit.",
          "type": "string"
        },
        "treatment_time": {
          "description": "First treatment period (inclusive).",
          "type": "string"
        },
        "unit": {
          "description": "Unit identifier column.",
          "type": "string"
        },
        "variant": {
          "default": "demeaned",
          "description": "* ``'demeaned'`` -- subtract unit-level pre-treatment means. * ``'detrended'`` -- subtract unit-level linear time trends.",
          "enum": [
            "demeaned",
            "detrended"
          ],
          "type": "string"
        }
      },
      "required": [
        "data",
        "outcome",
        "unit",
        "time",
        "treated_unit",
        "treatment_time"
      ],
      "type": "object"
    }
  },
  {
    "description": "Robust / unconstrained Synthetic Control. Validation: certified parity evidence.",
    "name": "robust_synth",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level.",
          "type": "number"
        },
        "covariates": {
          "description": "Not supported: the weights are fitted on pre-treatment outcomes only. Passing covariates raises ``NotImplementedError`` (it used to be silently ignored).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "data": {
          "description": "Long-format panel data.",
          "type": "string"
        },
        "intercept": {
          "default": true,
          "description": "Fit an unpenalised intercept (level shift). Only for unconstrained / elastic_net.",
          "type": "boolean"
        },
        "l1_penalty": {
          "default": 0.0,
          "description": "L1 penalty ``l1_penalty * ||w||_1`` in the objective ``||y - mu - X w||^2 + l2 ||w||^2 + l1 ||w||_1``. glmnet's ``(lambda, alpha)`` on the same data maps to ``l1 = 2 n lambda alpha`` (``n`` = pre-periods) once ``y`` is scaled to unit (1/n) standard deviation.",
          "type": "number"
        },
        "l2_penalty": {
          "default": 0.01,
          "description": "Ridge penalty ``l2_penalty * ||w||^2`` (glmnet: ``l2 = n lambda (1 - alpha)``). ``0`` with ``l1_penalty=0`` is ordinary least squares with an intercept -- Doudchenko & Imbens' unconstrained estimator, identical to ``scpi::scest(w.constr = list(name = \"ols\"))`` on ``scdata(constant = TRUE)``.",
          "type": "number"
        },
        "outcome": {
          "description": "Outcome variable name.",
          "type": "string"
        },
        "placebo": {
          "default": true,
          "description": "Run in-space placebo inference.",
          "type": "boolean"
        },
        "time": {
          "description": "Time period column.",
          "type": "string"
        },
        "treated_unit": {
          "description": "Identifier of the treated unit.",
          "type": "string"
        },
        "treatment_time": {
          "description": "First treatment period.",
          "type": "string"
        },
        "unit": {
          "description": "Unit identifier column.",
          "type": "string"
        },
        "variant": {
          "default": "unconstrained",
          "description": "* ``'unconstrained'`` -- no sign / sum constraints; optional intercept. * ``'elastic_net'`` -- L1 + L2 penalty, no sign constraints. * ``'penalized'`` -- simplex constraints + ridge penalty (see the module docstring; not Abadie & L'Hour 2021).",
          "enum": [
            "unconstrained",
            "elastic_net",
            "penalized"
          ],
          "type": "string"
        }
      },
      "required": [
        "data",
        "outcome",
        "unit",
        "time",
        "treated_unit",
        "treatment_time"
      ],
      "type": "object"
    }
  },
  {
    "description": "Generalized Synthetic Control via interactive fixed effects. Validation: certified parity evidence. Do NOT use when: there is only one treated unit and a short pre-period -- the interactive fixed-effects factors are not identified; use sp.synth; fewer pre-treatment periods than the number of factors being fit -- factor estimation is degenerate. Cost: Cross-validating n_factors refits the factor model cv_folds x max_factors times, and placebo/bootstrap inference refits again per replication -- runtime is multiplicative in those three knobs. Pin n_factors to skip the CV sweep.",
    "name": "gsynth",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level.",
          "type": "number"
        },
        "backend": {
          "default": "native",
          "description": "``'native'`` uses StatsPAI's Python interactive fixed-effects implementation. ``'gsynth'``/``'r'`` delegates to the R ``gsynth`` package through ``Rscript`` using the Track-A reference specification ``force='two-way'``, ``CV=TRUE``, ``r=c(0, max_factors)``, and ``se=FALSE``. The R backend is intended for exact reference-package parity; the native path remains the dependency-light default.",
          "enum": [
            "native",
            "gsynth",
            "r"
          ],
          "type": "string"
        },
        "covariates": {
          "description": "Additional time-varying covariates.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "cv_folds": {
          "default": 5,
          "description": "Cross-validation folds for factor selection.",
          "type": "integer"
        },
        "data": {
          "description": "Long-format panel data.",
          "type": "string"
        },
        "max_factors": {
          "default": 5,
          "description": "Maximum factors to try during CV.",
          "type": "integer"
        },
        "n_factors": {
          "description": "Number of latent factors. If None, selected by cross-validation.",
          "type": "integer"
        },
        "outcome": {
          "description": "Outcome variable name.",
          "type": "string"
        },
        "placebo": {
          "default": true,
          "description": "Run placebo inference.",
          "type": "boolean"
        },
        "seed": {
          "description": "Random seed.",
          "type": "integer"
        },
        "time": {
          "description": "Time period column.",
          "type": "string"
        },
        "treated_unit": {
          "description": "Identifier of the treated unit.",
          "type": "string"
        },
        "treatment_time": {
          "description": "First treatment period (inclusive).",
          "type": "string"
        },
        "unit": {
          "description": "Unit identifier column.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "outcome",
        "unit",
        "time",
        "treated_unit",
        "treatment_time"
      ],
      "type": "object"
    }
  },
  {
    "description": "Staggered Adoption Synthetic Control (Ben-Michael, Feller & Rothstein). Validation: certified parity evidence.",
    "name": "staggered_synth",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level.",
          "type": "number"
        },
        "data": {
          "description": "Balanced long-format panel.",
          "type": "string"
        },
        "fixedeff": {
          "default": false,
          "description": "De-mean outcomes by unit before fitting (multisynth ``fixedeff``; note multisynth's own default is ``TRUE``).",
          "type": "boolean"
        },
        "method": {
          "default": "separate",
          "description": "* ``'separate'`` -- one set of weights per treated unit (multisynth ``time_cohort = FALSE``). * ``'pooled'`` -- one set of weights per adoption cohort, fitted to the cohort's summed outcomes (multisynth ``time_cohort = TRUE``).",
          "enum": [
            "separate",
            "pooled"
          ],
          "type": "string"
        },
        "n_lags": {
          "description": "Number of pre-treatment periods to balance (``None`` = all periods before the last adoption date, as in multisynth).",
          "type": "integer"
        },
        "n_leads": {
          "description": "Number of post-treatment event times in each effect window. ``None`` uses every period through the end of the panel (the longest window), which restricts donors to never-treated units.",
          "type": "integer"
        },
        "nu": {
          "default": 0.0,
          "description": "Pooling parameter. ``0`` fits each unit (cohort) separately; ``'auto'`` uses multisynth's default heuristic.",
          "type": "number"
        },
        "outcome": {
          "description": "Outcome variable name.",
          "type": "string"
        },
        "penalization": {
          "default": 0.0,
          "description": "Ridge penalty ``lambda`` on the weights (multisynth ``lambda``), in the scale of the objective in the module docstring.",
          "type": "number"
        },
        "placebo": {
          "default": true,
          "description": "Compute inference (see ``se_method``). ``False`` returns a point estimate with NaN standard error / p-value.",
          "type": "boolean"
        },
        "se_method": {
          "default": "placebo",
          "description": "* ``'placebo'`` -- in-space placebos: every never-treated unit is assigned each cohort's adoption date and fitted from the other never-treated units; SE is the standard deviation of the placebo ATTs and the p-value is the placebo rank of ``|ATT|``. * ``'jackknife'`` -- leave-one-unit-out jackknife over all units with ``nu`` held at its full-sample value, exactly as ``summary(multisynth, inf_type = \"jackknife\")``; normal-theory p-value and CI.",
          "enum": [
            "placebo",
            "jackknife"
          ],
          "type": "string"
        },
        "time": {
          "description": "Time period column.",
          "type": "string"
        },
        "treatment": {
          "description": "Binary, absorbing treatment indicator (0/1). A unit's adoption period is the first period with ``treatment == 1``.",
          "type": "string"
        },
        "unit": {
          "description": "Unit identifier column.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "outcome",
        "unit",
        "time",
        "treatment"
      ],
      "type": "object"
    }
  },
  {
    "description": "Conformal inference for synthetic control (Chernozhukov, Wuthrich & Zhu). Validation: certified parity evidence.",
    "name": "conformal_synth",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level (``scinference`` defaults to 0.1).",
          "type": "number"
        },
        "data": {
          "description": "Long-format panel data (one row per unit and period).",
          "type": "string"
        },
        "grid_range": {
          "description": "(min, max) of the hypothesis grid. Default: the point estimate +/- max(5 x SD of the pre-period SC residuals, 3 x the largest absolute post-period gap). A confidence set reaching an end of the grid is truncated there (flagged in ``model_info['ci_truncated']``, with a warning). When ``alpha`` is below the smallest attainable p-value (``1/(T0+1)`` pointwise, ``1/(T0+T1)`` joint) nothing can be rejected and the set is ``(-inf, inf)``.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "grid_size": {
          "default": 101,
          "description": "Number of points in the hypothesis grid for CI inversion.",
          "type": "integer"
        },
        "outcome": {
          "description": "Outcome variable name.",
          "type": "string"
        },
        "penalization": {
          "default": 0.0,
          "description": "Ridge penalty on the weights.",
          "type": "number"
        },
        "scm_method": {
          "default": "classic",
          "description": "Label of the counterfactual model. Both fit simplex-constrained least squares of the treated outcome on the donor outcomes (no intercept, ``w >= 0``, ``sum(w) = 1``) plus ``penalization * ||w||^2``; ``'classic'`` with ``penalization = 0`` is ``scinference``'s ``sc`` estimator.",
          "enum": [
            "classic",
            "ridge"
          ],
          "type": "string"
        },
        "time": {
          "description": "Time period column.",
          "type": "string"
        },
        "treated_unit": {
          "description": "Identifier of the treated unit.",
          "type": "string"
        },
        "treatment_time": {
          "description": "First treatment period.",
          "type": "string"
        },
        "unit": {
          "description": "Unit identifier column.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "outcome",
        "unit",
        "time",
        "treated_unit",
        "treatment_time"
      ],
      "type": "object"
    }
  },
  {
    "description": "Synthetic Difference-in-Differences estimator (and SC / DID variants). Validation: certified parity evidence. Do NOT use when: there is no clean pre-treatment block for every unit -- the unit and time weights are fit on the pre-period grid. Cost: Placebo / bootstrap standard errors refit the full weighting problem n_reps times; the point estimate alone is cheap. Lower n_reps while iterating.",
    "name": "sdid",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals.",
          "type": "number"
        },
        "backend": {
          "default": "native",
          "description": "``'native'`` uses StatsPAI's Python implementation. ``'synthdid'``/``'r'`` delegates to the R ``synthdid`` package through ``Rscript`` and returns the reference package's point estimate and ``synthdid_se`` standard error. The R backend is mainly for exact cross-language parity claims; the dependency- light native implementation remains the default.",
          "enum": [
            "native",
            "synthdid",
            "r"
          ],
          "type": "string"
        },
        "covariates": {
          "description": "Not supported by the native solver yet; passing any raises :class:`~statspai.exceptions.MethodIncompatibility` rather than silently estimating the unadjusted model (through 1.28.0 the argument was accepted and ignored).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "data": {
          "description": "Balanced panel data in long format.",
          "type": "string"
        },
        "method": {
          "default": "sdid",
          "description": "* ``'sdid'`` -- Synthetic DID (unit + time weights) * ``'sc'`` -- Synthetic Control (unit weights only) * ``'did'`` -- DID (uniform weights)",
          "enum": [
            "sdid",
            "sc",
            "did"
          ],
          "type": "string"
        },
        "n_reps": {
          "default": 200,
          "description": "Replications for placebo / bootstrap SE.",
          "type": "integer"
        },
        "outcome": {
          "description": "Outcome variable column. Alias ``y=`` accepted for R-style calls.",
          "type": "string"
        },
        "se_method": {
          "default": "placebo",
          "description": "Standard-error method (see Notes).",
          "enum": [
            "placebo",
            "bootstrap",
            "jackknife"
          ],
          "type": "string"
        },
        "seed": {
          "description": "Random seed for reproducibility.",
          "type": "integer"
        },
        "time": {
          "description": "Time period column.",
          "type": "string"
        },
        "treat_time": {
          "description": "treat_time parameter.",
          "type": "string"
        },
        "treat_unit": {
          "description": "treat_unit parameter.",
          "type": "string"
        },
        "treated_unit": {
          "description": "Treated unit(s). Alias ``treat_unit=`` accepted.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "treatment_time": {
          "description": "First treatment period (inclusive). Alias ``treat_time=`` accepted.",
          "type": "string"
        },
        "unit": {
          "description": "Unit identifier column.",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "data"
      ],
      "type": "object"
    }
  },
  {
    "description": "R-style alias: ``synthdid::synthdid_estimate``. Validation: certified parity evidence.",
    "name": "synthdid_estimate",
    "parameters": {
      "properties": {
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "time": {
          "description": "Time period column.",
          "type": "string"
        },
        "treat_time": {
          "description": "treat_time parameter.",
          "type": "string"
        },
        "treat_unit": {
          "description": "treat_unit parameter.",
          "type": "string"
        },
        "unit": {
          "description": "Unit identifier column.",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "unit",
        "time",
        "treat_unit",
        "treat_time"
      ],
      "type": "object"
    }
  },
  {
    "description": "R-style alias: ``synthdid::sc_estimate``. Validation: certified parity evidence.",
    "name": "sc_estimate",
    "parameters": {
      "properties": {
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "time": {
          "description": "Time period column.",
          "type": "string"
        },
        "treat_time": {
          "description": "treat_time parameter.",
          "type": "string"
        },
        "treat_unit": {
          "description": "treat_unit parameter.",
          "type": "string"
        },
        "unit": {
          "description": "Unit identifier column.",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "unit",
        "time",
        "treat_unit",
        "treat_time"
      ],
      "type": "object"
    }
  },
  {
    "description": "R-style alias: ``synthdid::did_estimate``. Validation: certified parity evidence.",
    "name": "did_estimate",
    "parameters": {
      "properties": {
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "time": {
          "description": "Time period column.",
          "type": "string"
        },
        "treat_time": {
          "description": "treat_time parameter.",
          "type": "string"
        },
        "treat_unit": {
          "description": "treat_unit parameter.",
          "type": "string"
        },
        "unit": {
          "description": "Unit identifier column.",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "unit",
        "time",
        "treat_unit",
        "treat_time"
      ],
      "type": "object"
    }
  },
  {
    "description": "Run placebo estimates assigning treatment to each control unit.",
    "name": "synthdid_placebo",
    "parameters": {
      "properties": {
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "method": {
          "default": "sdid",
          "description": "Estimator or algorithm variant to use.",
          "type": "string"
        },
        "time": {
          "description": "Time period column.",
          "type": "string"
        },
        "treat_time": {
          "description": "treat_time parameter.",
          "type": "string"
        },
        "treat_unit": {
          "description": "treat_unit parameter.",
          "type": "string"
        },
        "unit": {
          "description": "Unit identifier column.",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "unit",
        "time",
        "treat_unit",
        "treat_time"
      ],
      "type": "object"
    }
  },
  {
    "description": "Plot observed vs synthetic trajectory.",
    "name": "synthdid_plot",
    "parameters": {
      "properties": {
        "ax": {
          "description": "ax parameter.",
          "type": "string"
        },
        "ci_alpha": {
          "default": 0.15,
          "description": "ci_alpha parameter (float).",
          "type": "number"
        },
        "figsize": {
          "default": [
            10,
            6
          ],
          "description": "figsize parameter (Tuple[float, float]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "result": {
          "description": "Output of :func:`sdid`.",
          "type": "string"
        },
        "synth_color": {
          "default": "#E74C3C",
          "description": "synth_color parameter (str).",
          "type": "string"
        },
        "title": {
          "description": "title parameter (Optional[str]).",
          "type": "string"
        },
        "treated_color": {
          "default": "#2C3E50",
          "description": "treated_color parameter (str).",
          "type": "string"
        }
      },
      "required": [
        "result"
      ],
      "type": "object"
    }
  },
  {
    "description": "Horizontal bar chart of unit weight contributions.",
    "name": "synthdid_units_plot",
    "parameters": {
      "properties": {
        "ax": {
          "description": "ax parameter.",
          "type": "string"
        },
        "figsize": {
          "default": [
            8,
            5
          ],
          "description": "figsize parameter (Tuple[float, float]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "result": {
          "description": "result parameter (CausalResult).",
          "type": "string"
        },
        "top_n": {
          "default": 10,
          "description": "Show the top-N donors by weight.",
          "type": "integer"
        }
      },
      "required": [
        "result"
      ],
      "type": "object"
    }
  },
  {
    "description": "Pre-treatment RMSE of treated vs synthetic trajectory.",
    "name": "synthdid_rmse_plot",
    "parameters": {
      "properties": {
        "ax": {
          "description": "ax parameter.",
          "type": "string"
        },
        "figsize": {
          "default": [
            8,
            5
          ],
          "description": "figsize parameter (Tuple[float, float]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "result": {
          "description": "result parameter (CausalResult).",
          "type": "string"
        }
      },
      "required": [
        "result"
      ],
      "type": "object"
    }
  },
  {
    "description": "California Proposition 99 tobacco control dataset.",
    "name": "california_prop99",
    "parameters": {
      "properties": {},
      "required": [],
      "type": "object"
    }
  },
  {
    "description": "Matrix Completion Synthetic Control Method. Validation: certified parity evidence.",
    "name": "mc_synth",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals.",
          "type": "number"
        },
        "covariates": {
          "description": "Time-varying covariates to partial out before matrix completion.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "cv_folds": {
          "default": 5,
          "description": "Number of CV folds for automatic lambda selection.",
          "type": "integer"
        },
        "data": {
          "description": "Long-format panel data.",
          "type": "string"
        },
        "fixed_effects": {
          "default": "two-way",
          "description": "Unpenalised additive effects fitted with the low-rank part. ``\"two-way\"`` is the Athey et al. (2021) estimator and the default of R ``MCPanel`` and ``fect``; ``\"none\"`` is pure soft-impute (the estimator of StatsPAI <= 1.28.0), which shrinks the outcome level towards zero.",
          "enum": [
            "two-way",
            "unit",
            "time"
          ],
          "type": "string"
        },
        "lambda_reg": {
          "description": "Singular-value threshold on the ``1/2``-scaled squared loss. The same minimiser is R ``MCPanel::mcnnm_fit(lambda_L = 2 * lambda / n_observed)`` and ``fect(method = \"mc\", CV = FALSE, lambda = lambda / (N * T))`` (both reported in ``model_info``). If ``None`` (default), selected by this function's own random-entry K-fold cross-validation (not the ``MCPanel`` / ``fect`` CV rule).",
          "type": "number"
        },
        "max_iter": {
          "default": 5000,
          "description": "Maximum Soft-Impute iterations (``RuntimeWarning`` if hit).",
          "type": "integer"
        },
        "outcome": {
          "description": "Outcome variable name.",
          "type": "string"
        },
        "placebo": {
          "default": true,
          "description": "Run placebo (permutation) inference by treating each control unit as if it were treated.",
          "type": "boolean"
        },
        "seed": {
          "description": "Random seed for the CV fold assignment. With ``seed=None`` and ``lambda_reg=None`` the selected lambda (hence the estimate) can differ between calls.",
          "type": "integer"
        },
        "time": {
          "description": "Time period column.",
          "type": "string"
        },
        "tol": {
          "default": 1e-10,
          "description": "Convergence tolerance (relative change in Frobenius norm of the fitted matrix).",
          "type": "number"
        },
        "treated_unit": {
          "description": "Identifier of the treated unit.",
          "type": "string"
        },
        "treatment_time": {
          "description": "First treatment period (inclusive).",
          "type": "string"
        },
        "unit": {
          "description": "Unit identifier column.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "outcome",
        "unit",
        "time",
        "treated_unit",
        "treatment_time"
      ],
      "type": "object"
    }
  },
  {
    "description": "Unified matching estimator supporting multiple distance x method combinations.",
    "name": "MatchEstimator",
    "parameters": {
      "properties": {
        "ai_matches": {
          "default": 1,
          "description": "ai_matches parameter (int).",
          "type": "integer"
        },
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "bias_correction": {
          "default": false,
          "description": "bias_correction parameter (bool).",
          "type": "boolean"
        },
        "bootstrap_reps": {
          "default": 200,
          "description": "bootstrap_reps parameter (int).",
          "type": "integer"
        },
        "bootstrap_seed": {
          "description": "bootstrap_seed parameter (Optional[int]).",
          "type": "integer"
        },
        "bwidth": {
          "default": 0.06,
          "description": "bwidth parameter (float).",
          "type": "number"
        },
        "caliper": {
          "description": "caliper parameter (Optional[float]).",
          "type": "number"
        },
        "caliper_scale": {
          "default": "raw",
          "description": "caliper_scale parameter (str).",
          "type": "string"
        },
        "common_support": {
          "default": "none",
          "description": "common_support parameter (str).",
          "type": "string"
        },
        "covariates": {
          "description": "Variables to match on.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "data": {
          "description": "Input data.",
          "type": "string"
        },
        "distance": {
          "description": "``'propensity'``, ``'mahalanobis'``, ``'euclidean'`` or ``'exact'``.",
          "type": "string"
        },
        "estimand": {
          "default": "ATT",
          "description": "``'ATT'`` or ``'ATE'``.",
          "type": "string"
        },
        "kernel": {
          "default": "epan",
          "description": "Kernel function used for weighting or smoothing.",
          "type": "string"
        },
        "llr_stata_compat": {
          "default": false,
          "description": "llr_stata_compat parameter (bool).",
          "type": "boolean"
        },
        "m_order": {
          "default": "smallest_min_dist",
          "description": "m_order parameter (str).",
          "type": "string"
        },
        "mahalanobis_cov": {
          "default": "pooled",
          "description": "mahalanobis_cov parameter (str).",
          "type": "string"
        },
        "method": {
          "default": "nearest",
          "description": "``'nearest'``, ``'stratify'`` or ``'cem'`` (legacy ``'psm'`` / ``'mahalanobis'`` are also accepted).",
          "type": "string"
        },
        "n_bins": {
          "description": "Number of bins.",
          "type": "integer"
        },
        "n_matches": {
          "default": 1,
          "description": "Number of matches.",
          "type": "integer"
        },
        "n_strata": {
          "default": 5,
          "description": "Number of strata.",
          "type": "integer"
        },
        "ps_poly": {
          "default": 1,
          "description": "ps_poly parameter (int).",
          "type": "integer"
        },
        "replace": {
          "default": true,
          "description": "replace parameter (bool).",
          "type": "boolean"
        },
        "se_method": {
          "default": "auto",
          "description": "se_method parameter (str).",
          "type": "string"
        },
        "tie_tolerance": {
          "default": 0.0,
          "description": "tie_tolerance parameter (float).",
          "type": "number"
        },
        "ties": {
          "default": "first",
          "description": "ties parameter (str).",
          "type": "string"
        },
        "treat": {
          "description": "Binary (0/1) treatment column.",
          "type": "string"
        },
        "y": {
          "description": "Outcome column.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "treat",
        "covariates"
      ],
      "type": "object"
    }
  },
  {
    "description": "Entropy Balancing treatment effect estimator. Validation: certified parity evidence.",
    "name": "ebalance",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "covariates": {
          "description": "Covariates to balance on.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "moments": {
          "default": 1,
          "description": "Number of moments to balance: - 1: means only - 2: means and variances - 3: means, variances, and skewness",
          "type": "integer"
        },
        "treat": {
          "description": "Binary treatment indicator (0/1).",
          "type": "string"
        },
        "vce": {
          "default": "mestimation",
          "description": "Standard error of the ATT. - ``'mestimation'``: the sandwich variance of the stacked estimating equations -- treated moment means, the entropy-balancing dual conditions, and the two outcome means -- so the uncertainty from estimating the weights is propagated. This is the variance R ``WeightIt::lm_weightit(..., vcov = \"asympt\")`` reports for ``method = \"ebal\", estimand = \"ATT\"``, and it agrees with it to ~1e-9 (``tests/reference_parity/test_ebalance_weightit_parity.py``). Because the weights balance the covariates exactly, outcome variation that the covariates explain cancels out of the ATT, and this variance reflects that. - ``'naive'``: the pre-1.31 formula, a weighted two-sample variance that treats the weights as fixed. It ignores the balancing, so it overstates the standard error whenever the covariates predict the outcome (by about 2x on the Track B design: coverage 1.000, size 0.000). Retained only to reproduce earlier numbers.",
          "enum": [
            "mestimation",
            "naive"
          ],
          "type": "string"
        },
        "y": {
          "description": "Outcome variable.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "treat",
        "covariates"
      ],
      "type": "object"
    }
  },
  {
    "description": "Love plot: covariate balance visualization (SMD dot plot).",
    "name": "balanceplot",
    "parameters": {
      "properties": {
        "ax": {
          "description": "ax parameter.",
          "type": "string"
        },
        "figsize": {
          "default": [
            8,
            null
          ],
          "description": "Height auto-scales with number of covariates if None.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "result": {
          "description": "Result from ``match()`` or ``ebalance()``.",
          "type": "string"
        },
        "threshold": {
          "default": 0.1,
          "description": "SMD threshold lines.",
          "type": "number"
        },
        "title": {
          "description": "title parameter (Optional[str]).",
          "type": "string"
        }
      },
      "required": [
        "result"
      ],
      "type": "object"
    }
  },
  {
    "description": "Propensity score distribution plot (common support diagnostic).",
    "name": "psplot",
    "parameters": {
      "properties": {
        "ax": {
          "description": "ax parameter.",
          "type": "string"
        },
        "colors": {
          "default": [
            "#3498DB",
            "#E74C3C"
          ],
          "description": "Colors for (control, treated).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "covariates": {
          "description": "Covariates used to estimate the propensity score.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "figsize": {
          "default": [
            8,
            5
          ],
          "description": "figsize parameter (tuple).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "labels": {
          "default": [
            "Control",
            "Treated"
          ],
          "description": "Labels for (control, treated).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "n_bins": {
          "default": 40,
          "description": "Number of histogram bins.",
          "type": "integer"
        },
        "title": {
          "description": "title parameter (Optional[str]).",
          "type": "string"
        },
        "treat": {
          "description": "Binary treatment column.",
          "type": "string"
        },
        "trim": {
          "description": "If set, draw vertical lines at (trim, 1-trim) to show the recommended trimming region.",
          "type": "number"
        }
      },
      "required": [
        "data",
        "treat",
        "covariates"
      ],
      "type": "object"
    }
  },
  {
    "description": "Estimate propensity scores P(D=1|X).",
    "name": "propensity_score",
    "parameters": {
      "properties": {
        "covariates": {
          "description": "Covariate column names.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "data": {
          "description": "Input data.",
          "type": "string"
        },
        "method": {
          "default": "logit",
          "description": "Estimation method. ``'logit'`` uses IRLS (no sklearn needed). ``'probit'`` uses scipy.optimize. ``'gbm'`` tries sklearn GradientBoostingClassifier, falling back to logit with interactions.",
          "enum": [
            "logit",
            "probit",
            "gbm"
          ],
          "type": "string"
        },
        "treatment": {
          "description": "Name of binary treatment column (0/1).",
          "type": "string"
        },
        "trimming": {
          "description": "If ``'crump'``, apply Crump et al. (2009) trimming after estimation. Trimmed observations receive ``NaN`` scores.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "treatment",
        "covariates"
      ],
      "type": "object"
    }
  },
  {
    "description": "Mirrored density plot of propensity scores by treatment group.",
    "name": "overlap_plot",
    "parameters": {
      "properties": {
        "ax": {
          "description": "Axes to plot on. If None, a new figure is created.",
          "type": "string"
        },
        "covariates": {
          "description": "Covariates for PS estimation (ignored if *ps* supplied).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "data": {
          "description": "Input data.",
          "type": "string"
        },
        "figsize": {
          "default": [
            8,
            4
          ],
          "description": "Figure size (width, height).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "method": {
          "default": "logit",
          "description": "PS estimation method if *ps* is None.",
          "type": "string"
        },
        "ps": {
          "description": "Pre-estimated propensity scores.",
          "type": "string"
        },
        "title": {
          "default": "Propensity Score Overlap",
          "description": "Plot title.",
          "type": "string"
        },
        "treatment": {
          "description": "Binary treatment column.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "treatment",
        "covariates"
      ],
      "type": "object"
    }
  },
  {
    "description": "Trim sample to optimal overlap region.",
    "name": "trimming",
    "parameters": {
      "properties": {
        "covariates": {
          "description": "Covariates for PS estimation (if *ps* not supplied).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "data": {
          "description": "Input data.",
          "type": "string"
        },
        "method": {
          "default": "crump",
          "description": "``'crump'`` uses Crump et al. (2009) optimal rule. ``'sturmer'`` trims at the fixed [0.1, 0.9] interval.",
          "enum": [
            "crump",
            "sturmer"
          ],
          "type": "string"
        },
        "ps": {
          "description": "Pre-estimated propensity scores. If None, estimated via *ps_method*.",
          "type": "string"
        },
        "ps_method": {
          "default": "logit",
          "description": "Method for PS estimation if *ps* is None.",
          "type": "string"
        },
        "treatment": {
          "description": "Binary treatment column.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "treatment",
        "covariates"
      ],
      "type": "object"
    }
  },
  {
    "description": "Love plot: dot plot of standardized mean differences before/after.",
    "name": "love_plot",
    "parameters": {
      "properties": {
        "ax": {
          "description": "ax parameter.",
          "type": "string"
        },
        "covariates": {
          "description": "Covariate columns.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "data": {
          "description": "Input data.",
          "type": "string"
        },
        "figsize": {
          "default": [
            7,
            null
          ],
          "description": "(width, height). Height defaults to 0.4 * n_covariates + 1.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "ps_method": {
          "default": "logit",
          "description": "PS estimation method for balance computation.",
          "type": "string"
        },
        "threshold": {
          "default": 0.1,
          "description": "SMD threshold for the vertical dashed line (default 0.1).",
          "type": "number"
        },
        "title": {
          "default": "Covariate Balance (Love Plot)",
          "description": "Plot title.",
          "type": "string"
        },
        "treatment": {
          "description": "Binary treatment column.",
          "type": "string"
        },
        "weights": {
          "description": "IPW or matching weights. If None, inverse-PS weights are computed.",
          "type": "string"
        }
      },
      "required": [
        "data"
      ],
      "type": "object"
    }
  },
  {
    "description": "Compute comprehensive propensity score balance table.",
    "name": "ps_balance",
    "parameters": {
      "properties": {
        "covariates": {
          "description": "Covariate columns to assess balance for.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "data": {
          "description": "Input data.",
          "type": "string"
        },
        "method": {
          "default": "logit",
          "description": "PS estimation method ('logit', 'probit', 'gbm').",
          "type": "string"
        },
        "treatment": {
          "description": "Binary treatment column.",
          "type": "string"
        },
        "weights": {
          "description": "IPW or matching weights. If None, inverse-PS weights are computed automatically from estimated propensity scores.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "treatment",
        "covariates"
      ],
      "type": "object"
    }
  },
  {
    "description": "Container for propensity score balance diagnostics.",
    "name": "PSBalanceResult",
    "parameters": {
      "properties": {
        "ps": {
          "description": "ps parameter (Series).",
          "type": "string"
        },
        "table": {
          "description": "table parameter (DataFrame).",
          "type": "string"
        }
      },
      "required": [
        "table",
        "ps"
      ],
      "type": "object"
    }
  },
  {
    "description": "Unified balance diagnostics for matching and weighting estimators.",
    "name": "balance_diagnostics",
    "parameters": {
      "properties": {
        "covariates": {
          "description": "Covariates to audit.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "data": {
          "description": "Analysis frame.",
          "type": "string"
        },
        "method": {
          "default": "logit",
          "description": "Propensity-score model when ``ps`` is not supplied.",
          "enum": [
            "logit",
            "probit",
            "gbm"
          ],
          "type": "string"
        },
        "ps": {
          "description": "Propensity scores. If omitted, estimated with ``method``.",
          "type": "string"
        },
        "threshold": {
          "default": 0.1,
          "description": "Balance threshold for absolute standardized mean differences.",
          "type": "number"
        },
        "treatment": {
          "description": "Binary treatment indicator.",
          "type": "string"
        },
        "weights": {
          "description": "Observation weights after matching/weighting. If omitted, ATE inverse-propensity weights are computed from ``ps``.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "treatment",
        "covariates"
      ],
      "type": "object"
    }
  },
  {
    "description": "Container for raw/weighted matching balance diagnostics.",
    "name": "BalanceDiagnosticsResult",
    "parameters": {
      "properties": {
        "ps": {
          "description": "ps parameter (Optional[pandas.Series]).",
          "type": "string"
        },
        "summary": {
          "description": "summary parameter (Dict[str, Any]).",
          "type": "object"
        },
        "table": {
          "description": "table parameter (DataFrame).",
          "type": "string"
        },
        "weights": {
          "description": "Observation weights.",
          "type": "string"
        }
      },
      "required": [
        "table",
        "summary"
      ],
      "type": "object"
    }
  },
  {
    "description": "Legacy facade. Prefer the per-model classes directly for new code:",
    "name": "DoubleML",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "covariates": {
          "description": "Covariate matrix, DataFrame, or column names.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "fold_indices": {
          "description": "fold_indices parameter (Optional[Any]).",
          "type": "string"
        },
        "instrument": {
          "description": "instrument parameter (Union[str, List[str], NoneType]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "ml_g": {
          "description": "ml_g parameter (Optional[Any]).",
          "type": "string"
        },
        "ml_m": {
          "description": "ml_m parameter (Optional[Any]).",
          "type": "string"
        },
        "ml_r": {
          "description": "ml_r parameter (Optional[Any]).",
          "type": "string"
        },
        "model": {
          "default": "plr",
          "description": "Model variant or parameterisation to fit.",
          "type": "string"
        },
        "n_folds": {
          "default": 5,
          "description": "Number of cross-fitting or cross-validation folds.",
          "type": "integer"
        },
        "n_rep": {
          "default": 1,
          "description": "Number of rep.",
          "type": "integer"
        },
        "normalize_ipw": {
          "default": false,
          "description": "normalize_ipw parameter (bool).",
          "type": "boolean"
        },
        "random_state": {
          "default": 42,
          "description": "Random seed or RandomState for reproducible stochastic steps.",
          "type": "integer"
        },
        "sample_weight": {
          "description": "sample_weight parameter (Optional[Any]).",
          "type": "string"
        },
        "score": {
          "description": "score parameter (Optional[str]).",
          "type": "string"
        },
        "treat": {
          "description": "Treatment indicator or first-treatment-period column.",
          "type": "string"
        },
        "trimming_threshold": {
          "default": 0.01,
          "description": "trimming_threshold parameter (float).",
          "type": "number"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "treat",
        "covariates"
      ],
      "type": "object"
    }
  },
  {
    "description": "Partially linear regression DML (continuous or binary D, no IV).",
    "name": "DoubleMLPLR",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "covariates": {
          "description": "Covariate matrix, DataFrame, or column names.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "fold_indices": {
          "description": "fold_indices parameter (Optional[Any]).",
          "type": "string"
        },
        "instrument": {
          "description": "instrument parameter (Union[str, List[str], NoneType]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "ml_g": {
          "description": "ml_g parameter (Optional[Any]).",
          "type": "string"
        },
        "ml_m": {
          "description": "ml_m parameter (Optional[Any]).",
          "type": "string"
        },
        "ml_r": {
          "description": "ml_r parameter (Optional[Any]).",
          "type": "string"
        },
        "n_folds": {
          "default": 5,
          "description": "Number of cross-fitting or cross-validation folds.",
          "type": "integer"
        },
        "n_rep": {
          "default": 1,
          "description": "Number of rep.",
          "type": "integer"
        },
        "normalize_ipw": {
          "default": false,
          "description": "normalize_ipw parameter (bool).",
          "type": "boolean"
        },
        "random_state": {
          "default": 42,
          "description": "Random seed or RandomState for reproducible stochastic steps.",
          "type": "integer"
        },
        "sample_weight": {
          "description": "sample_weight parameter (Optional[Any]).",
          "type": "string"
        },
        "score": {
          "description": "score parameter (Optional[str]).",
          "type": "string"
        },
        "treat": {
          "description": "Treatment indicator or first-treatment-period column.",
          "type": "string"
        },
        "trimming_threshold": {
          "default": 0.01,
          "description": "trimming_threshold parameter (float).",
          "type": "number"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "treat",
        "covariates"
      ],
      "type": "object"
    }
  },
  {
    "description": "Interactive regression DML -- binary D, ATE via AIPW.",
    "name": "DoubleMLIRM",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "covariates": {
          "description": "Covariate matrix, DataFrame, or column names.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "fold_indices": {
          "description": "fold_indices parameter (Optional[Any]).",
          "type": "string"
        },
        "instrument": {
          "description": "instrument parameter (Union[str, List[str], NoneType]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "ml_g": {
          "description": "ml_g parameter (Optional[Any]).",
          "type": "string"
        },
        "ml_m": {
          "description": "ml_m parameter (Optional[Any]).",
          "type": "string"
        },
        "ml_r": {
          "description": "ml_r parameter (Optional[Any]).",
          "type": "string"
        },
        "n_folds": {
          "default": 5,
          "description": "Number of cross-fitting or cross-validation folds.",
          "type": "integer"
        },
        "n_rep": {
          "default": 1,
          "description": "Number of rep.",
          "type": "integer"
        },
        "normalize_ipw": {
          "default": false,
          "description": "normalize_ipw parameter (bool).",
          "type": "boolean"
        },
        "random_state": {
          "default": 42,
          "description": "Random seed or RandomState for reproducible stochastic steps.",
          "type": "integer"
        },
        "sample_weight": {
          "description": "sample_weight parameter (Optional[Any]).",
          "type": "string"
        },
        "score": {
          "description": "score parameter (Optional[str]).",
          "type": "string"
        },
        "treat": {
          "description": "Treatment indicator or first-treatment-period column.",
          "type": "string"
        },
        "trimming_threshold": {
          "default": 0.01,
          "description": "trimming_threshold parameter (float).",
          "type": "number"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "treat",
        "covariates"
      ],
      "type": "object"
    }
  },
  {
    "description": "Partially linear IV DML -- endogenous D with continuous/binary Z.",
    "name": "DoubleMLPLIV",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "covariates": {
          "description": "Covariate matrix, DataFrame, or column names.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "fold_indices": {
          "description": "fold_indices parameter (Optional[Any]).",
          "type": "string"
        },
        "instrument": {
          "description": "instrument parameter (Union[str, List[str], NoneType]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "ml_g": {
          "description": "ml_g parameter (Optional[Any]).",
          "type": "string"
        },
        "ml_m": {
          "description": "ml_m parameter (Optional[Any]).",
          "type": "string"
        },
        "ml_r": {
          "description": "ml_r parameter (Optional[Any]).",
          "type": "string"
        },
        "n_folds": {
          "default": 5,
          "description": "Number of cross-fitting or cross-validation folds.",
          "type": "integer"
        },
        "n_rep": {
          "default": 1,
          "description": "Number of rep.",
          "type": "integer"
        },
        "normalize_ipw": {
          "default": false,
          "description": "normalize_ipw parameter (bool).",
          "type": "boolean"
        },
        "random_state": {
          "default": 42,
          "description": "Random seed or RandomState for reproducible stochastic steps.",
          "type": "integer"
        },
        "sample_weight": {
          "description": "sample_weight parameter (Optional[Any]).",
          "type": "string"
        },
        "score": {
          "description": "score parameter (Optional[str]).",
          "type": "string"
        },
        "treat": {
          "description": "Treatment indicator or first-treatment-period column.",
          "type": "string"
        },
        "trimming_threshold": {
          "default": 0.01,
          "description": "trimming_threshold parameter (float).",
          "type": "number"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "treat",
        "covariates"
      ],
      "type": "object"
    }
  },
  {
    "description": "Interactive IV DML -- binary D, binary Z, LATE via Wald.",
    "name": "DoubleMLIIVM",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "covariates": {
          "description": "Covariate matrix, DataFrame, or column names.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "fold_indices": {
          "description": "fold_indices parameter (Optional[Any]).",
          "type": "string"
        },
        "instrument": {
          "description": "instrument parameter (Union[str, List[str], NoneType]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "ml_g": {
          "description": "ml_g parameter (Optional[Any]).",
          "type": "string"
        },
        "ml_m": {
          "description": "ml_m parameter (Optional[Any]).",
          "type": "string"
        },
        "ml_r": {
          "description": "ml_r parameter (Optional[Any]).",
          "type": "string"
        },
        "n_folds": {
          "default": 5,
          "description": "Number of cross-fitting or cross-validation folds.",
          "type": "integer"
        },
        "n_rep": {
          "default": 1,
          "description": "Number of rep.",
          "type": "integer"
        },
        "normalize_ipw": {
          "default": false,
          "description": "normalize_ipw parameter (bool).",
          "type": "boolean"
        },
        "random_state": {
          "default": 42,
          "description": "Random seed or RandomState for reproducible stochastic steps.",
          "type": "integer"
        },
        "sample_weight": {
          "description": "sample_weight parameter (Optional[Any]).",
          "type": "string"
        },
        "score": {
          "description": "score parameter (Optional[str]).",
          "type": "string"
        },
        "treat": {
          "description": "Treatment indicator or first-treatment-period column.",
          "type": "string"
        },
        "trimming_threshold": {
          "default": 0.01,
          "description": "trimming_threshold parameter (float).",
          "type": "number"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "treat",
        "covariates"
      ],
      "type": "object"
    }
  },
  {
    "description": "Model-averaging / stacking DML-PLR estimator. Validation: certified parity evidence.",
    "name": "model_averaging_dml",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Two-sided CI level.",
          "type": "number"
        },
        "candidates": {
          "description": "Candidate nuisance learners. ``ml_g`` regresses ``y`` on ``X``; ``ml_m`` regresses ``treat`` on ``X``. Defaults to a Lasso/Ridge/ RandomForest/GradientBoosting roster.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "covariates": {
          "description": "Covariate columns ``X``.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "fold_indices": {
          "description": "Explicit cross-fitting fold per row (column name or array of length ``len(data)``). Overrides ``n_folds`` / ``seed``; every candidate uses the same partition.",
          "type": "string"
        },
        "n_folds": {
          "default": 5,
          "description": "Cross-fitting folds per candidate.",
          "type": "integer"
        },
        "sample_weight": {
          "description": "Per-observation weights. If supplied, every nuisance fit uses ``sample_weight=`` (with a graceful fallback warning if the learner does not accept it), the CLS stacking objective becomes weighted least squares, and the PLR moment + sandwich variance use weighted sums. The MSE used for ``inverse_risk`` / ``single_best`` weighting is also the weighted MSE.",
          "type": "string"
        },
        "seed": {
          "default": 0,
          "description": "Random seed for reproducible stochastic steps.",
          "type": "integer"
        },
        "treat": {
          "description": "Continuous-or-binary treatment column.",
          "type": "string"
        },
        "weight_rule": {
          "default": "short_stacking",
          "description": "How to combine candidate nuisance predictions or estimates. * ``\"short_stacking\"`` *(default; Ahrens et al. 2025 eq. 7)* -- solve constrained least squares on cross-fitted predictions for each nuisance separately (``y`` and ``D``), produce stacked nuisances, plug into the PLR moment equation. * ``\"single_best\"`` -- Ahrens et al. (2025, fn. 8): pick the candidate with lowest joint nuisance MSE. * ``\"inverse_risk\"`` -- :math:`w_k \\propto 1/(\\text{MSE}_g + \\text{MSE}_m)`. Convenience baseline; **not** in the paper. * ``\"equal\"`` -- :math:`w_k = 1/K`. Convenience baseline; **not** in the paper. For the non-stacking rules (``inverse_risk`` / ``equal`` / ``single_best``) the function computes per-candidate :math:`\\hat\\theta_k` and reports the weighted average with a between-candidate-covariance-corrected SE; for ``\"short_stacking\"`` it reports the standard PLR sandwich SE on the stacked-nuisance score (Neyman orthogonality is preserved).",
          "enum": [
            "short_stacking",
            "single_best",
            "inverse_risk",
            "equal"
          ],
          "type": "string"
        },
        "y": {
          "description": "Outcome column.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "treat",
        "covariates"
      ],
      "type": "object"
    }
  },
  {
    "description": "CausalResult extended with per-candidate and weight details.",
    "name": "DMLAveragingResult",
    "parameters": {
      "properties": {
        "_citation_key": {
          "description": "_citation_key parameter (Optional[str]).",
          "type": "string"
        },
        "_influence_funcs": {
          "description": "_influence_funcs parameter (Optional[numpy.ndarray]).",
          "type": "string"
        },
        "alpha": {
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "ci": {
          "description": "ci parameter (tuple).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "detail": {
          "description": "Amount of result detail to return.",
          "type": "string"
        },
        "estimand": {
          "description": "estimand parameter (str).",
          "type": "string"
        },
        "estimate": {
          "description": "estimate parameter (float).",
          "type": "number"
        },
        "method": {
          "description": "Estimator or algorithm variant to use.",
          "type": "string"
        },
        "model_info": {
          "description": "model_info parameter (Optional[Dict[str, Any]]).",
          "type": "object"
        },
        "n_obs": {
          "description": "Number of obs.",
          "type": "integer"
        },
        "pvalue": {
          "description": "pvalue parameter (float).",
          "type": "number"
        },
        "se": {
          "description": "se parameter (float).",
          "type": "number"
        }
      },
      "required": [
        "method",
        "estimand",
        "estimate",
        "se",
        "pvalue",
        "ci",
        "alpha",
        "n_obs"
      ],
      "type": "object"
    }
  },
  {
    "description": "Compute DML-OVB sensitivity for a fitted DML CausalResult. Validation: certified parity evidence.",
    "name": "dml_sensitivity",
    "parameters": {
      "properties": {
        "benchmark_covariates": {
          "description": "Subset of the original covariates to benchmark against. For each ``X_k``, the benchmark sets ``cf_y_bench, cf_d_bench`` to the partial R2 that ``X_k`` itself contributes (multiplied by ``k_y, k_d`` to express \"what if a confounder were kx as strong as ``X_k``?\").",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "cf_d": {
          "description": "Hypothesized partial-R2 of an unobserved confounder with the residualised outcome and treatment. If both are given, the report includes a bias bound and adjusted-estimate range.",
          "type": "number"
        },
        "cf_y": {
          "description": "Hypothesized partial-R2 of an unobserved confounder with the residualised outcome and treatment. If both are given, the report includes a bias bound and adjusted-estimate range.",
          "type": "number"
        },
        "k_d": {
          "default": 1.0,
          "description": "Multipliers for the benchmark strengths.",
          "type": "number"
        },
        "k_y": {
          "default": 1.0,
          "description": "Multipliers for the benchmark strengths.",
          "type": "number"
        },
        "q": {
          "default": 1.0,
          "description": "Bias threshold as a fraction of |theta|. ``q=1`` => confounder needed to shrink estimate to zero; ``q=0.5`` => half the estimate.",
          "type": "number"
        },
        "result": {
          "description": "result parameter.",
          "type": "string"
        }
      },
      "required": [
        "result"
      ],
      "type": "object"
    }
  },
  {
    "description": "Output of :func:`dml_sensitivity`.",
    "name": "DMLSensitivityResult",
    "parameters": {
      "properties": {
        "adjusted_estimate_high": {
          "description": "adjusted_estimate_high parameter (float).",
          "type": "number"
        },
        "adjusted_estimate_low": {
          "description": "adjusted_estimate_low parameter (float).",
          "type": "number"
        },
        "alpha": {
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "benchmarks": {
          "description": "benchmarks parameter (pd.DataFrame).",
          "type": "string"
        },
        "bias_bound": {
          "description": "bias_bound parameter (float).",
          "type": "number"
        },
        "cf_d": {
          "description": "cf_d parameter (Optional[float]).",
          "type": "number"
        },
        "cf_y": {
          "description": "cf_y parameter (Optional[float]).",
          "type": "number"
        },
        "estimate": {
          "description": "estimate parameter (float).",
          "type": "number"
        },
        "method": {
          "default": "DML-OVB (Chernozhukov-Cinelli-Newey 2022)",
          "description": "Estimator or algorithm variant to use.",
          "type": "string"
        },
        "q": {
          "description": "Quantile level.",
          "type": "number"
        },
        "rv_q": {
          "description": "rv_q parameter (float).",
          "type": "number"
        },
        "rv_qa": {
          "description": "rv_qa parameter (float).",
          "type": "number"
        },
        "s": {
          "description": "s parameter (float).",
          "type": "number"
        },
        "se": {
          "description": "se parameter (float).",
          "type": "number"
        }
      },
      "required": [
        "estimate",
        "se",
        "rv_q",
        "rv_qa",
        "bias_bound",
        "adjusted_estimate_low",
        "adjusted_estimate_high",
        "benchmarks",
        "s",
        "q",
        "alpha"
      ],
      "type": "object"
    }
  },
  {
    "description": "Build a :class:`DMLDiagnostics` report from a DML CausalResult. Validation: validated evidence tier (known-truth, reference, external-parity, or Monte Carlo artifact).",
    "name": "dml_diagnostics",
    "parameters": {
      "properties": {
        "clip": {
          "default": 0.02,
          "description": "For IRM-style overlap: count units with propensity within ``[0, clip] [1-clip, 1]`` as overlap-violating.",
          "type": "number"
        },
        "result": {
          "description": "Result returned by :func:`statspai.dml.dml`. Must include the post-fit residuals (``model_info['_y_resid']``, ``model_info['_d_resid']``); for IRM, additionally the propensity ``model_info['diagnostics']['pscore_min']`` etc. are surfaced.",
          "type": "string"
        }
      },
      "required": [
        "result"
      ],
      "type": "object"
    }
  },
  {
    "description": "Bundled DML diagnostics returned by :func:`dml_diagnostics`.",
    "name": "DMLDiagnostics",
    "parameters": {
      "properties": {
        "_overlap_label": {
          "default": "",
          "description": "_overlap_label parameter (str).",
          "type": "string"
        },
        "_overlap_values": {
          "default": null,
          "description": "_overlap_values parameter (np.ndarray).",
          "type": "string"
        },
        "_score": {
          "description": "_score parameter (Optional[np.ndarray]).",
          "type": "string"
        },
        "_score_label": {
          "default": "Centered stored residual",
          "description": "_score_label parameter (str).",
          "type": "string"
        },
        "balance_table": {
          "default": null,
          "description": "balance_table parameter (pd.DataFrame).",
          "type": "string"
        },
        "estimate": {
          "description": "estimate parameter (float).",
          "type": "number"
        },
        "method": {
          "description": "Estimator or algorithm variant to use.",
          "type": "string"
        },
        "n_clipped_high": {
          "default": 0,
          "description": "Number of clipped high.",
          "type": "integer"
        },
        "n_clipped_low": {
          "default": 0,
          "description": "Number of clipped low.",
          "type": "integer"
        },
        "n_obs": {
          "description": "Number of obs.",
          "type": "integer"
        },
        "orth_pvalue": {
          "description": "orth_pvalue parameter (Optional[float]).",
          "type": "number"
        },
        "orth_stat": {
          "description": "orth_stat parameter (Optional[float]).",
          "type": "number"
        },
        "orth_warning": {
          "description": "orth_warning parameter (Optional[str]).",
          "type": "string"
        },
        "orthogonality_reason": {
          "default": "A solved estimating equation is not a test of Neyman orthogonality or identification.",
          "description": "orthogonality_reason parameter (str).",
          "type": "string"
        },
        "orthogonality_status": {
          "default": "not_tested",
          "description": "orthogonality_status parameter (str).",
          "type": "string"
        },
        "overlap_table": {
          "description": "overlap_table parameter (pd.DataFrame).",
          "type": "string"
        },
        "overlap_warning": {
          "description": "overlap_warning parameter (Optional[str]).",
          "type": "string"
        },
        "score_kurtosis": {
          "default": 0.0,
          "description": "score_kurtosis parameter (float).",
          "type": "number"
        },
        "score_mean": {
          "default": 0.0,
          "description": "score_mean parameter (float).",
          "type": "number"
        },
        "score_sd": {
          "default": 0.0,
          "description": "score_sd parameter (float).",
          "type": "number"
        },
        "score_skew": {
          "default": 0.0,
          "description": "score_skew parameter (float).",
          "type": "number"
        },
        "se": {
          "description": "se parameter (float).",
          "type": "number"
        }
      },
      "required": [
        "n_obs",
        "method",
        "estimate",
        "se",
        "overlap_table"
      ],
      "type": "object"
    }
  },
  {
    "description": "Output of :func:`dml_panel`.",
    "name": "DMLPanelResult",
    "parameters": {
      "properties": {
        "ci_lower": {
          "description": "ci_lower parameter (float).",
          "type": "number"
        },
        "ci_upper": {
          "description": "ci_upper parameter (float).",
          "type": "number"
        },
        "diagnostics": {
          "default": null,
          "description": "diagnostics parameter (dict).",
          "type": "object"
        },
        "estimate": {
          "description": "estimate parameter (float).",
          "type": "number"
        },
        "include_time_fe": {
          "description": "Whether to include time fe.",
          "type": "boolean"
        },
        "method": {
          "default": "dml_panel",
          "description": "Estimator or algorithm variant to use.",
          "type": "string"
        },
        "ml_g_name": {
          "description": "ml_g_name parameter (str).",
          "type": "string"
        },
        "ml_m_name": {
          "description": "ml_m_name parameter (str).",
          "type": "string"
        },
        "n_folds": {
          "description": "Number of cross-fitting or cross-validation folds.",
          "type": "integer"
        },
        "n_obs": {
          "description": "Number of obs.",
          "type": "integer"
        },
        "n_units": {
          "description": "Number of units.",
          "type": "integer"
        },
        "p_value": {
          "description": "p_value parameter (float).",
          "type": "number"
        },
        "se": {
          "description": "se parameter (float).",
          "type": "number"
        },
        "t_stat": {
          "description": "t_stat parameter (float).",
          "type": "number"
        }
      },
      "required": [
        "estimate",
        "se",
        "ci_lower",
        "ci_upper",
        "p_value",
        "t_stat",
        "n_units",
        "n_obs",
        "n_folds",
        "include_time_fe",
        "ml_g_name",
        "ml_m_name"
      ],
      "type": "object"
    }
  },
  {
    "description": "Output of :func:`dynamic_dml`.",
    "name": "DynamicDMLResult",
    "parameters": {
      "properties": {
        "alpha": {
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "ci_lower": {
          "description": "ci_lower parameter (float).",
          "type": "number"
        },
        "ci_upper": {
          "description": "ci_upper parameter (float).",
          "type": "number"
        },
        "coef": {
          "description": "coef parameter (Optional[pd.DataFrame]).",
          "type": "string"
        },
        "diagnostics": {
          "default": null,
          "description": "diagnostics parameter (Dict[str, Any]).",
          "type": "object"
        },
        "estimate": {
          "description": "estimate parameter (float).",
          "type": "number"
        },
        "lags": {
          "description": "lags parameter (int).",
          "type": "integer"
        },
        "method": {
          "default": "dynamic_dml",
          "description": "Estimator or algorithm variant to use.",
          "type": "string"
        },
        "n_folds": {
          "description": "Number of cross-fitting or cross-validation folds.",
          "type": "integer"
        },
        "n_periods": {
          "description": "Number of periods.",
          "type": "integer"
        },
        "n_units": {
          "description": "Number of units.",
          "type": "integer"
        },
        "p_value": {
          "description": "p_value parameter (float).",
          "type": "number"
        },
        "period_labels": {
          "description": "period_labels parameter (List[Any]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "periods": {
          "description": "periods parameter (pd.DataFrame).",
          "type": "string"
        },
        "se": {
          "description": "se parameter (float).",
          "type": "number"
        },
        "vcov": {
          "description": "Variance-covariance estimator option.",
          "type": "string"
        },
        "z_stat": {
          "description": "z_stat parameter (float).",
          "type": "number"
        }
      },
      "required": [
        "periods",
        "estimate",
        "se",
        "ci_lower",
        "ci_upper",
        "p_value",
        "z_stat",
        "vcov",
        "n_units",
        "n_periods",
        "n_folds",
        "period_labels",
        "lags",
        "alpha"
      ],
      "type": "object"
    }
  },
  {
    "description": "Estimate causal effects using Deep Instrumental Variables.",
    "name": "deepiv",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence interval.",
          "type": "number"
        },
        "batch_size": {
          "default": 256,
          "description": "Mini-batch size.",
          "type": "integer"
        },
        "covariates": {
          "description": "Exogenous control variable names.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "data": {
          "description": "Input data.",
          "type": "string"
        },
        "first_stage_epochs": {
          "default": 100,
          "description": "Training epochs for the treatment model (MDN).",
          "type": "integer"
        },
        "hidden_layers": {
          "default": [
            128,
            64
          ],
          "description": "Hidden layer sizes for both networks.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "instruments": {
          "description": "Excluded instrument variable names.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "learning_rate": {
          "default": 0.001,
          "description": "Adam learning rate.",
          "type": "number"
        },
        "n_components": {
          "default": 10,
          "description": "Number of Gaussian mixture components in the MDN (stage 1).",
          "type": "integer"
        },
        "n_gradient_samples": {
          "default": 0,
          "description": "Number of **independent** additional samples used for the gradient path. When ``0`` (default), a single set of samples is used, matching Microsoft EconML's default behaviour and producing a biased but variance-regularized gradient estimator. When ``> 0``, two independent sample sets are drawn and the unbiased paired-sample estimator ``mean((y - h(p, x)) * (y - h(p', x)))`` from Hartford et al. Section 3.2 is used instead. Set to ``1`` or higher if you specifically need unbiased gradients (e.g. for asymptotic consistency arguments).",
          "type": "integer"
        },
        "n_samples": {
          "default": 1,
          "description": "Number of MC samples per observation used to form the Stage-2 residual ``(y - h(t, x))``.",
          "type": "integer"
        },
        "random_state": {
          "default": 42,
          "description": "Random seed for reproducibility.",
          "type": "integer"
        },
        "second_stage_epochs": {
          "default": 100,
          "description": "Training epochs for the response model.",
          "type": "integer"
        },
        "treat": {
          "description": "Endogenous treatment variable name (continuous).",
          "type": "string"
        },
        "verbose": {
          "default": false,
          "description": "Print training progress.",
          "type": "boolean"
        },
        "y": {
          "description": "Outcome variable name.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "treat",
        "instruments",
        "covariates"
      ],
      "type": "object"
    }
  },
  {
    "description": "Deep Instrumental Variables estimator (Hartford et al. 2017).",
    "name": "DeepIV",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level.",
          "type": "number"
        },
        "batch_size": {
          "default": 256,
          "description": "batch_size parameter (int).",
          "type": "integer"
        },
        "covariates": {
          "description": "Exogenous controls.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "first_stage_epochs": {
          "default": 100,
          "description": "Stage 1 training epochs.",
          "type": "integer"
        },
        "hidden_layers": {
          "default": [
            128,
            64
          ],
          "description": "Hidden layer sizes.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "instruments": {
          "description": "Excluded instruments.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "learning_rate": {
          "default": 0.001,
          "description": "learning_rate parameter (float).",
          "type": "number"
        },
        "n_components": {
          "default": 10,
          "description": "Gaussian mixture components in the MDN.",
          "type": "integer"
        },
        "n_gradient_samples": {
          "default": 0,
          "description": "Independent additional samples for the gradient path. ``0`` reproduces EconML's default (biased but variance-regularized); ``>= 1`` activates Hartford et al.'s paired-sample unbiased gradient estimator.",
          "type": "integer"
        },
        "n_samples": {
          "default": 1,
          "description": "MC samples per observation used to form the Stage-2 residual.",
          "type": "integer"
        },
        "random_state": {
          "default": 42,
          "description": "Random seed or RandomState for reproducible stochastic steps.",
          "type": "integer"
        },
        "second_stage_epochs": {
          "default": 100,
          "description": "Stage 2 training epochs.",
          "type": "integer"
        },
        "treat": {
          "description": "Endogenous treatment variable (continuous).",
          "type": "string"
        },
        "verbose": {
          "default": false,
          "description": "verbose parameter (bool).",
          "type": "boolean"
        },
        "y": {
          "description": "Outcome variable.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "treat",
        "instruments",
        "covariates"
      ],
      "type": "object"
    }
  },
  {
    "description": "Balance a panel by keeping only units observed in every time period. Validation: certified parity evidence.",
    "name": "balance_panel",
    "parameters": {
      "properties": {
        "data": {
          "description": "Panel data in long format.",
          "type": "string"
        },
        "entity": {
          "description": "Entity (unit) identifier column.",
          "type": "string"
        },
        "time": {
          "description": "Time period column.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "entity",
        "time"
      ],
      "type": "object"
    }
  },
  {
    "description": "Panel regression results with built-in diagnostics.",
    "name": "PanelResults",
    "parameters": {
      "properties": {
        "_dep_var": {
          "description": "_dep_var parameter (Optional[str]).",
          "type": "string"
        },
        "_entity": {
          "description": "_entity parameter (Optional[str]).",
          "type": "string"
        },
        "_formula": {
          "description": "Formula for the component.",
          "type": "string"
        },
        "_indep_vars": {
          "description": "_indep_vars parameter (Optional[List[str]]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "_lm_result": {
          "description": "_lm_result parameter (Optional[Any]).",
          "type": "string"
        },
        "_method": {
          "description": "_method parameter (Optional[str]).",
          "type": "string"
        },
        "_panel_data": {
          "description": "_panel_data parameter (Optional[pandas.DataFrame]).",
          "type": "string"
        },
        "_time": {
          "description": "_time parameter (Optional[str]).",
          "type": "string"
        },
        "data_info": {
          "description": "data_info parameter (Optional[Dict[str, Any]]).",
          "type": "object"
        },
        "diagnostics": {
          "description": "diagnostics parameter (Optional[Dict[str, Any]]).",
          "type": "object"
        },
        "model_info": {
          "description": "model_info parameter (Dict[str, Any]).",
          "type": "object"
        },
        "params": {
          "description": "params parameter (Series).",
          "type": "string"
        },
        "std_errors": {
          "description": "std_errors parameter (Series).",
          "type": "string"
        }
      },
      "required": [
        "params",
        "std_errors",
        "model_info"
      ],
      "type": "object"
    }
  },
  {
    "description": "Side-by-side comparison of two panel models.",
    "name": "PanelCompareResults",
    "parameters": {
      "properties": {
        "model_a": {
          "description": "model_a parameter (PanelResults).",
          "type": "string"
        },
        "model_b": {
          "description": "model_b parameter (PanelResults).",
          "type": "string"
        }
      },
      "required": [
        "model_a",
        "model_b"
      ],
      "type": "object"
    }
  },
  {
    "description": "Deprecated: use ``panel()`` directly. Kept for backward compatibility.",
    "name": "PanelRegression",
    "parameters": {
      "properties": {},
      "required": [],
      "type": "object"
    }
  },
  {
    "description": "Causal Impact estimator using a structural time-series model.",
    "name": "CausalImpactEstimator",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for the credible intervals.",
          "type": "number"
        },
        "covariates": {
          "description": "Control series unaffected by the intervention.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "data": {
          "description": "Time-series data, one row per period.",
          "type": "string"
        },
        "intervention_time": {
          "description": "First post-intervention period (inclusive).",
          "type": "string"
        },
        "n_seasons": {
          "description": "Not used (no seasonal component); a value > 1 warns.",
          "type": "integer"
        },
        "time": {
          "description": "Time column used for ordering.",
          "type": "string"
        },
        "y": {
          "description": "Outcome (intervened) series.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "time",
        "intervention_time"
      ],
      "type": "object"
    }
  },
  {
    "description": "Causal Forest for heterogeneous treatment effect estimation",
    "name": "CausalForest",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "grf ``alpha``: minimum share of the parent's treatment variation each child of a stabilised split must retain.",
          "type": "number"
        },
        "bootstrap": {
          "description": "Not used by the grf engine (trees are grown on subsamples drawn without replacement). Retained for the legacy engine.",
          "type": "boolean"
        },
        "ci_group_size": {
          "description": "Trees per little bag (``1`` disables variance estimates). Defaults to 2 when ``max_samples <= 0.5`` and to 1, with a warning, otherwise.",
          "type": "integer"
        },
        "discrete_treatment": {
          "default": true,
          "description": "Whether treatment is binary (0/1). Continuous treatments are supported by the causal forest itself; doubly-robust averages require a binary treatment.",
          "type": "boolean"
        },
        "equalize_cluster_weights": {
          "default": false,
          "description": "grf ``equalize.cluster.weights``: draw the same number of observations from every cluster and weight clusters equally in averages.",
          "type": "boolean"
        },
        "fe": {
          "description": "Panel fixed effects removed within every node and leaf. Requires ``id=`` (and ``time=`` for ``\"twoway\"``) at fit time; trees are then sampled by unit.",
          "enum": [
            "unit",
            "twoway"
          ],
          "type": "string"
        },
        "honest": {
          "default": true,
          "description": "grf ``honesty``: grow the tree on one half of the drawn clusters and estimate leaves on the other.",
          "type": "boolean"
        },
        "honesty_fraction": {
          "default": 0.5,
          "description": "honesty_fraction parameter (float).",
          "type": "number"
        },
        "honesty_prune_leaves": {
          "default": true,
          "description": "honesty_prune_leaves parameter (bool).",
          "type": "boolean"
        },
        "imbalance_penalty": {
          "default": 0.0,
          "description": "imbalance_penalty parameter (float).",
          "type": "number"
        },
        "max_depth": {
          "description": "Optional depth cap (grf has none).",
          "type": "integer"
        },
        "max_samples": {
          "default": 0.5,
          "description": "Fraction of clusters drawn per tree (grf ``sample.fraction``); at most 0.5 when ``ci_group_size > 1``.",
          "type": "number"
        },
        "min_samples_leaf": {
          "default": 5,
          "description": "grf ``min.node.size``: a node with at most this many growing samples is not split, and each child of a stabilised split must hold at least this many treated-like and control-like samples.",
          "type": "integer"
        },
        "model_t": {
          "description": "scikit-learn estimators for ``E[Y | X, W]`` and ``E[T | X, W]``. When omitted, grf regression forests with out-of-bag predictions are used. Supplied estimators are cross-fitted in ``nuisance_folds`` folds that never split a cluster.",
          "type": "string"
        },
        "model_y": {
          "description": "scikit-learn estimators for ``E[Y | X, W]`` and ``E[T | X, W]``. When omitted, grf regression forests with out-of-bag predictions are used. Supplied estimators are cross-fitted in ``nuisance_folds`` folds that never split a cluster.",
          "type": "string"
        },
        "mtry": {
          "description": "Mean number of candidate variables per split (grf default ``min(ceil(sqrt(p) + 20), p)``).",
          "type": "integer"
        },
        "n_estimators": {
          "default": 2000,
          "description": "Number of trees (grf ``num.trees``). Rounded up to a multiple of ``ci_group_size``.",
          "type": "integer"
        },
        "n_jobs": {
          "default": 1,
          "description": "Threads used to grow trees (-1 = all cores).",
          "type": "integer"
        },
        "nuisance_folds": {
          "default": 3,
          "description": "Cross-fitting folds for user-supplied nuisance estimators.",
          "type": "integer"
        },
        "random_state": {
          "description": "Seed. Results are reproducible for a given seed and ``n_jobs``- independent.",
          "type": "integer"
        },
        "split_rule": {
          "default": "grf",
          "description": "``\"cffe\"`` (``fe=`` forests only) replaces the GRF gradient criterion with the tau-heterogeneity criterion of Kattenberg, Scheer and Thiel (2023), the rule the ``causalfe`` package implements; see :func:`statspai.causal_forest`. ``\"legacy\"`` reproduces the pre-1.29 estimator and is deprecated.",
          "enum": [
            "grf",
            "cffe",
            "legacy"
          ],
          "type": "string"
        },
        "stabilize_splits": {
          "default": true,
          "description": "stabilize_splits parameter (bool).",
          "type": "boolean"
        },
        "verbose": {
          "default": 0,
          "description": "Verbosity level",
          "type": "integer"
        }
      },
      "required": [],
      "type": "object"
    }
  },
  {
    "description": "Rescale CATE predictions by their best-linear-predictor calibration. Validation: validated evidence tier (known-truth, reference, external-parity, or Monte Carlo artifact).",
    "name": "calibrate_cate",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Level for the reported slope confidence intervals.",
          "type": "number"
        },
        "covariates": {
          "default": "none",
          "description": "Imputation regression only; see :func:`average_treatment_effect`.",
          "type": "string"
        },
        "forest": {
          "description": "A forest fitted with the GRF engine (``split_rule=\"grf\"``).",
          "type": "string"
        },
        "method": {
          "default": "auto",
          "description": "Forests with fixed effects only; see :func:`calibration_test`.",
          "enum": [
            "auto",
            "imputation",
            "within"
          ],
          "type": "string"
        },
        "newdata": {
          "description": "Effect-modifier rows to calibrate. ``None`` calibrates the out-of-bag predictions of the training rows. ``X=`` is accepted as an alias.",
          "type": "string"
        }
      },
      "required": [
        "forest"
      ],
      "type": "object"
    }
  },
  {
    "description": "Best-linear-predictor calibration test of CATEs [@chernozhukov2025generic]. Validation: certified parity evidence.",
    "name": "calibration_test",
    "parameters": {
      "properties": {
        "T": {
          "description": "The training sample (defaults to the stored arrays).",
          "type": "string"
        },
        "X": {
          "description": "The training sample (defaults to the stored arrays).",
          "type": "string"
        },
        "Y": {
          "description": "The training sample (defaults to the stored arrays).",
          "type": "string"
        },
        "alpha": {
          "default": 0.05,
          "description": "Level for ``ci_low`` / ``ci_high``.",
          "type": "number"
        },
        "covariates": {
          "default": "none",
          "description": "Covariates in the untreated outcome model of the imputation regression (see :func:`average_treatment_effect`).",
          "type": "string"
        },
        "forest": {
          "description": "forest parameter ('CausalForest').",
          "type": "string"
        },
        "method": {
          "default": "auto",
          "description": "Forests with fixed effects only: ``'auto'`` is ``'imputation'`` for ``fe='twoway'`` with a binary treatment and ``'within'`` otherwise. Other forests accept only ``'auto'``.",
          "enum": [
            "auto",
            "imputation",
            "within"
          ],
          "type": "string"
        },
        "vce": {
          "default": "HC3",
          "description": "Heteroskedasticity-robust covariance (``grf``'s ``vcov.type``).",
          "enum": [
            "HC3",
            "HC2",
            "HC1",
            "HC0"
          ],
          "type": "string"
        }
      },
      "required": [
        "forest"
      ],
      "type": "object"
    }
  },
  {
    "description": "Best-linear-predictor calibration test of CATEs [@chernozhukov2025generic]. Validation: certified parity evidence.",
    "name": "test_calibration",
    "parameters": {
      "properties": {
        "T": {
          "description": "The training sample (defaults to the stored arrays).",
          "type": "string"
        },
        "X": {
          "description": "The training sample (defaults to the stored arrays).",
          "type": "string"
        },
        "Y": {
          "description": "The training sample (defaults to the stored arrays).",
          "type": "string"
        },
        "alpha": {
          "default": 0.05,
          "description": "Level for ``ci_low`` / ``ci_high``.",
          "type": "number"
        },
        "covariates": {
          "default": "none",
          "description": "Covariates in the untreated outcome model of the imputation regression (see :func:`average_treatment_effect`).",
          "type": "string"
        },
        "forest": {
          "description": "forest parameter ('CausalForest').",
          "type": "string"
        },
        "method": {
          "default": "auto",
          "description": "Forests with fixed effects only: ``'auto'`` is ``'imputation'`` for ``fe='twoway'`` with a binary treatment and ``'within'`` otherwise. Other forests accept only ``'auto'``.",
          "enum": [
            "auto",
            "imputation",
            "within"
          ],
          "type": "string"
        },
        "vce": {
          "default": "HC3",
          "description": "Heteroskedasticity-robust covariance (``grf``'s ``vcov.type``).",
          "enum": [
            "HC3",
            "HC2",
            "HC1",
            "HC0"
          ],
          "type": "string"
        }
      },
      "required": [
        "forest"
      ],
      "type": "object"
    }
  },
  {
    "description": "Rank-weighted average treatment effect (RATE) [@yadlowsky2025evaluating]. Validation: certified parity evidence.",
    "name": "rate",
    "parameters": {
      "properties": {
        "T": {
          "description": "The training sample (defaults to the stored arrays).",
          "type": "string"
        },
        "X": {
          "description": "The training sample (defaults to the stored arrays).",
          "type": "string"
        },
        "Y": {
          "description": "The training sample (defaults to the stored arrays).",
          "type": "string"
        },
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "cluster": {
          "description": "Fixed-effects forests only; ``'dyadic'`` needs ``members=``.",
          "type": "string"
        },
        "covariates": {
          "default": "none",
          "description": "Fixed-effects forests only: time-varying covariates for the untreated model behind the imputation scores. Returns dict with keys ``estimate``, ``se``, ``ci_low``, ``ci_high``, ``target``, ``toc_curve`` (``(q_grid, 2)``), ``n``, ``method``, ``priority_source``, ``n_clusters``. References [@yadlowsky2025evaluating] Examples >>> import numpy as np >>> import pandas as pd >>> import statspai as sp >>> rng = np.random.default_rng(42) >>> n = 400 >>> X = rng.normal(size=(n, 3)) >>> T = rng.binomial(1, 0.5, size=n) >>> tau = 1.0 + X[:, 0] # heterogeneous effect >>> Y = X[:, 1] + tau * T + rng.normal(scale=0.5, size=n) >>> df = pd.DataFrame({ ... \"y\": Y, \"d\": T, ... \"x0\": X[:, 0], \"x1\": X[:, 1], \"x2\": X[:, 2], ... }) >>> cf = sp.causal_forest( ... data=df, formula=\"y ~ d | x0 + x1 + x2\", ... n_estimators=50, random_state=0, ... ) >>> res = sp.rate(cf, target=\"AUTOC\") >>> sorted(res.keys()) ['ci_high', 'ci_low', 'estimate', 'method', 'n', 'n_clusters', \\ >>> res[\"toc_curve\"].shape (100, 2)",
          "type": "string"
        },
        "forest": {
          "description": "forest parameter ('CausalForest').",
          "type": "string"
        },
        "members": {
          "description": "Fixed-effects forests only: the two members of each dyadic row.",
          "type": "string"
        },
        "n_bootstrap": {
          "default": 200,
          "description": "Half-sample draws for ``se_method='half_sample'``.",
          "type": "integer"
        },
        "priorities": {
          "description": "Priority score per training unit (higher = treat first). Defaults to the forest's CATE predictions.",
          "type": "string"
        },
        "q_grid": {
          "default": 100,
          "description": "Number of equally spaced points in ``(0, 1]`` at which the TOC curve is reported. Does not affect the estimate.",
          "type": "integer"
        },
        "se_method": {
          "default": "auto",
          "description": "``'auto'`` is ``'influence'``, or ``'imputation'`` for a forest with fixed effects. ``'imputation'`` is defined only for those.",
          "enum": [
            "auto",
            "influence",
            "half_sample",
            "imputation"
          ],
          "type": "string"
        },
        "seed": {
          "description": "Seed for ``se_method='half_sample'``.",
          "type": "integer"
        },
        "target": {
          "default": "AUTOC",
          "description": "target parameter (str).",
          "enum": [
            "AUTOC",
            "QINI"
          ],
          "type": "string"
        },
        "variance": {
          "default": "bjs",
          "description": "Fixed-effects forests only: how treated residuals are centred before the cohort x event-time blocks. ``'bjs'`` (default here) is conservative for the population RATE, ``'forest'`` exact for the sample's -- see above.",
          "enum": [
            "bjs",
            "forest"
          ],
          "type": "string"
        }
      },
      "required": [
        "forest"
      ],
      "type": "object"
    }
  },
  {
    "description": "Half-sample spread of the mean CATE prediction (deprecated). Validation: validated evidence tier (known-truth, reference, external-parity, or Monte Carlo artifact).",
    "name": "honest_variance",
    "parameters": {
      "properties": {
        "X": {
          "description": "Feature matrix or covariate DataFrame.",
          "type": "string"
        },
        "forest": {
          "description": "forest parameter ('CausalForest').",
          "type": "string"
        },
        "n_splits": {
          "default": 25,
          "description": "Number of random half-sample draws.",
          "type": "integer"
        },
        "seed": {
          "description": "Random seed for reproducible stochastic steps.",
          "type": "integer"
        }
      },
      "required": [
        "forest"
      ],
      "type": "object"
    }
  },
  {
    "description": "Aggregate CATE predictions into ATE/ATT/ATC/ATO targets. Validation: certified parity evidence.",
    "name": "average_treatment_effect",
    "parameters": {
      "properties": {
        "T": {
          "description": "Rows to aggregate over (default: the training sample).",
          "type": "string"
        },
        "X": {
          "description": "Rows to aggregate over (default: the training sample).",
          "type": "string"
        },
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "clip": {
          "default": 0.01,
          "description": "Propensity scores are clipped to ``[clip, 1-clip]`` before the inverse-propensity terms of the ATE / ATT / ATC scores. ``grf`` does not clip; ``clip=0`` reproduces it, and the clip is inert whenever all propensities already lie inside the band.",
          "type": "number"
        },
        "covariates": {
          "default": "none",
          "description": "Forests with fixed effects only: covariates in the untreated outcome model ``Y(0) = alpha_i + gamma_t + C' beta``. ``'none'`` is :func:`statspai.did_imputation` without covariates (same estimate; same standard error with ``variance='bjs'``). ``'auto'`` adds the effect modifiers and covariates that vary within units; use it when a time-varying covariate drives the untreated outcome and is not itself affected by the treatment.",
          "type": "string"
        },
        "forest": {
          "description": "forest parameter ('CausalForest').",
          "type": "string"
        },
        "target_sample": {
          "default": "all",
          "description": "target_sample parameter (str).",
          "enum": [
            "all",
            "treated",
            "control",
            "overlap"
          ],
          "type": "string"
        },
        "variance": {
          "default": "forest",
          "description": "Forests with fixed effects only (see above); ignored otherwise.",
          "enum": [
            "forest",
            "bjs"
          ],
          "type": "string"
        }
      },
      "required": [
        "forest"
      ],
      "type": "object"
    }
  },
  {
    "description": "Return overlap and CATE-distribution diagnostics for a fitted forest. Validation: validated evidence tier (known-truth, reference, external-parity, or Monte Carlo artifact).",
    "name": "forest_diagnostics",
    "parameters": {
      "properties": {
        "T": {
          "description": "T parameter (Optional[np.ndarray]).",
          "type": "string"
        },
        "X": {
          "description": "Feature matrix or covariate DataFrame.",
          "type": "string"
        },
        "forest": {
          "description": "forest parameter ('CausalForest').",
          "type": "string"
        },
        "propensity_bounds": {
          "default": [
            0.05,
            0.95
          ],
          "description": "propensity_bounds parameter (Tuple[float, float]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "required": [
        "forest"
      ],
      "type": "object"
    }
  },
  {
    "description": "Reusable HDFE demean operator.",
    "name": "Absorber",
    "parameters": {
      "properties": {
        "accelerate": {
          "default": true,
          "description": "Enable Irons-Tuck Delta2 acceleration.",
          "type": "boolean"
        },
        "drop_singletons": {
          "default": true,
          "description": "If True, singleton observations (FE groups of size 1) are pruned before building the absorber. ``keep_mask`` stores the surviving rows.",
          "type": "boolean"
        },
        "fe_data": {
          "description": "FE columns. Must have no NaN. May be ``None`` (or an ``(n, 0)`` array) when only varying-slope terms are absorbed, in which case ``n_obs`` is inferred from ``slopes``.",
          "type": "string"
        },
        "maxiter": {
          "default": 10000,
          "description": "Maximum alternating-projection iterations.",
          "type": "integer"
        },
        "n_obs": {
          "description": "Row count, required only when ``fe_data`` is ``None`` and ``slopes`` is empty.",
          "type": "integer"
        },
        "slopes": {
          "description": "Varying-slope terms to absorb alongside ``fe_data``. Each counts as one more absorbed dimension in the alternating projections.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "solver": {
          "default": "map",
          "description": "Within-transformation backend. ``\"map\"`` uses alternating projections with Irons-Tuck acceleration (default, typically fastest on well-conditioned panels). ``\"lsmr\"`` / ``\"lsqr\"`` delegate to ``scipy.sparse.linalg.lsmr`` / ``lsqr`` on the sparse FE design matrix -- more robust for ill-conditioned or highly nested FE structures. See the migration guide for how this maps to ``pyreghdfe``.",
          "enum": [
            "map",
            "lsmr",
            "lsqr"
          ],
          "type": "string"
        },
        "tol": {
          "default": 1e-08,
          "description": "Convergence threshold on max |dx| per iteration.",
          "type": "number"
        },
        "weights": {
          "description": "Observation weights. If given, weighted means are used.",
          "type": "string"
        }
      },
      "required": [
        "fe_data"
      ],
      "type": "object"
    }
  },
  {
    "description": "A varying-slope absorbed term.",
    "name": "SlopeSpec",
    "parameters": {
      "properties": {
        "group": {
          "description": "Group or cohort identifier.",
          "type": "string"
        },
        "name": {
          "default": "slope",
          "description": "name parameter (ForwardRef('str')).",
          "type": "string"
        },
        "with_intercept": {
          "default": false,
          "description": "with_intercept parameter (ForwardRef('bool')).",
          "type": "boolean"
        },
        "x": {
          "description": "Primary running variable, regressor, or feature input for this estimator.",
          "type": "string"
        }
      },
      "required": [
        "group",
        "x"
      ],
      "type": "object"
    }
  },
  {
    "description": "Return the within-transformed ``x`` and the singleton keep mask. Validation: certified parity evidence.",
    "name": "demean",
    "parameters": {
      "properties": {
        "drop_singletons": {
          "default": true,
          "description": "drop_singletons parameter (bool).",
          "type": "boolean"
        },
        "fe": {
          "description": "fe parameter (Union[pd.DataFrame, np.ndarray, None]).",
          "type": "string"
        },
        "maxiter": {
          "default": 10000,
          "description": "maxiter parameter (int).",
          "type": "integer"
        },
        "slopes": {
          "description": "slopes parameter (Optional[Sequence[SlopeSpec]]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "solver": {
          "default": "map",
          "description": "solver parameter (str).",
          "type": "string"
        },
        "tol": {
          "default": 1e-08,
          "description": "Numerical convergence tolerance.",
          "type": "number"
        },
        "weights": {
          "description": "Observation weights.",
          "type": "string"
        },
        "x": {
          "description": "Primary running variable, regressor, or feature input for this estimator.",
          "type": "string"
        }
      },
      "required": [
        "x",
        "fe"
      ],
      "type": "object"
    }
  },
  {
    "description": "OLS with absorbed high-dimensional fixed effects (reghdfe-style). Validation: certified parity evidence.",
    "name": "absorb_ols",
    "parameters": {
      "properties": {
        "X": {
          "description": "Regressors *excluding* the absorbed FEs and the constant (the constant is absorbed by any FE dimension).",
          "type": "string"
        },
        "cluster": {
          "description": "One-way or multi-way cluster variables for robust SEs. If provided, returns cluster-robust SEs (one-way: Liang-Zeger sandwich; multi-way: inclusion-exclusion Cameron-Gelbach-Miller).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "cluster_df": {
          "default": "per_term",
          "description": "Small-sample factor of the multi-way (inclusion-exclusion) cluster variance. ``\"per_term\"`` scales each term by its own ``G_S/(G_S - 1)`` (R ``sandwich::vcovCL``, :func:`multiway_cluster_vcov`); ``\"min\"`` scales every term by ``G_min/(G_min - 1)`` with ``G_min`` the smallest one-way cluster count -- Stata ``reghdfe``'s convention (and ``fixest``'s ``cluster.df = \"min\"``). Identical for one-way clustering.",
          "enum": [
            "per_term",
            "min"
          ],
          "type": "string"
        },
        "drop_singletons": {
          "default": true,
          "description": "drop_singletons parameter (bool).",
          "type": "boolean"
        },
        "fe": {
          "description": "Fixed-effect columns.",
          "type": "string"
        },
        "maxiter": {
          "default": 10000,
          "description": "Demean convergence controls.",
          "type": "integer"
        },
        "return_absorber": {
          "default": false,
          "description": "If True, also return the ``Absorber`` object for reuse.",
          "type": "boolean"
        },
        "slopes": {
          "description": "slopes parameter (Optional[Sequence[SlopeSpec]]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "solver": {
          "default": "map",
          "description": "Within-transformation backend. See :class:`Absorber`.",
          "enum": [
            "map",
            "lsmr",
            "lsqr"
          ],
          "type": "string"
        },
        "tol": {
          "default": 1e-08,
          "description": "Demean convergence controls.",
          "type": "number"
        },
        "weights": {
          "description": "Observation weights.",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "y",
        "X",
        "fe"
      ],
      "type": "object"
    }
  },
  {
    "description": "reghdfe-style OLS with high-dimensional fixed effects. Validation: certified parity evidence. Do NOT use when: vce='conley' on more than ~20,000 rows -- dense O(n^2) memory (see cost); use sp.conley on the fitted result instead. Cost: Absorption is linear in n. vce='conley' is the exception: the within-transformed design goes through conley_vcov_matrix, which builds dense n x n distance and kernel matrices -- ~80 GB at n=100,000. vce='cr2'/'cr3' are per-cluster and cheap by comparison.",
    "name": "hdfe_ols",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "cluster": {
          "description": "One-way or multi-way cluster column(s).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "conley_cutoff": {
          "description": "Conley distance cutoff in km for ``vce=\"conley\"``.",
          "type": "number"
        },
        "conley_lat": {
          "description": "Coordinate columns (decimal degrees) for ``vce=\"conley\"``.",
          "type": "string"
        },
        "conley_lon": {
          "description": "Coordinate columns (decimal degrees) for ``vce=\"conley\"``.",
          "type": "string"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "drop_singletons": {
          "default": true,
          "description": "drop_singletons parameter (bool).",
          "type": "boolean"
        },
        "formula": {
          "description": "``\"y ~ x1 + x2 | fe1 + fe2 + fe3\"``. The ``| fe...`` part is optional. Both sides accept bare names, ``c.x`` / ``i.f``, ``a:b``, ``a*b``, ``f1^f2`` and the varying-slope forms ``i.f#c.x`` / ``i.f##c.x`` / ``f[[x]]`` / ``f[x]`` -- see the module docstring for the full grammar.",
          "type": "string"
        },
        "maxiter": {
          "default": 10000,
          "description": "maxiter parameter (int).",
          "type": "integer"
        },
        "se_type": {
          "description": "Override automatic inference of SE type. Usually inferred from ``cluster`` / ``wild``.",
          "enum": [
            "iid",
            "cluster",
            "multiway_cluster",
            "wild_cluster"
          ],
          "type": "string"
        },
        "tol": {
          "default": 1e-08,
          "description": "Numerical convergence tolerance.",
          "type": "number"
        },
        "vce": {
          "description": "Canonical SE-menu keyword (matches ``sp.regress`` / ``sp.feols``): - ``\"robust\"`` / ``\"hc1\"`` -- heteroskedasticity-robust on the FE-absorbed design with reghdfe's small-sample factor ``N/(N-k-df_a)``; matches Stata ``reghdfe ..., vce(robust)``. - ``\"hc0\"`` -- no small-sample factor. - ``\"CR2\"`` / ``\"CR3\"`` / ``\"jackknife\"`` -- Pustejovsky-Tipton (2018) bias-reduced cluster-robust on the within design (requires ``cluster=``, one-way); matches R ``clubSandwich::vcovCR(plm)``. - ``\"conley\"`` -- Conley spatial HAC on the within design (requires ``conley_lat=/conley_lon=/conley_cutoff=``; Stata ``acreg`` planar distance convention). - ``\"wild\"`` -- shorthand for ``wild=True`` (requires ``cluster=``).",
          "type": "string"
        },
        "weights": {
          "description": "Observation weights. Column name or raw array.",
          "type": "string"
        },
        "wild": {
          "default": false,
          "description": "If True (and ``cluster`` is given), return wild-cluster-bootstrap p-values / CIs alongside classical cluster SE. Applied variable- by-variable. Only supported with a single cluster column.",
          "type": "boolean"
        },
        "wild_n_boot": {
          "default": 999,
          "description": "Bootstrap replications.",
          "type": "integer"
        },
        "wild_seed": {
          "description": "wild_seed parameter (Optional[int]).",
          "type": "integer"
        },
        "wild_weight_type": {
          "default": "webb",
          "description": "wild_weight_type parameter (str).",
          "enum": [
            "rademacher",
            "webb",
            "mammen"
          ],
          "type": "string"
        }
      },
      "required": [
        "formula",
        "data"
      ],
      "type": "object"
    }
  },
  {
    "description": "Result of ``sp.feols()``.",
    "name": "FEOLSResult",
    "parameters": {
      "properties": {
        "absorber": {
          "description": "absorber parameter (Absorber).",
          "type": "string"
        },
        "cluster_info": {
          "description": "cluster_info parameter (Dict[str, Any]).",
          "type": "object"
        },
        "conf_int_lower": {
          "description": "conf_int_lower parameter (pd.Series).",
          "type": "string"
        },
        "conf_int_upper": {
          "description": "conf_int_upper parameter (pd.Series).",
          "type": "string"
        },
        "converged": {
          "description": "converged parameter (bool).",
          "type": "boolean"
        },
        "df_resid": {
          "description": "df_resid parameter (int).",
          "type": "integer"
        },
        "dof_fe": {
          "description": "dof_fe parameter (int).",
          "type": "integer"
        },
        "fitted_within": {
          "description": "fitted_within parameter (np.ndarray).",
          "type": "string"
        },
        "formula": {
          "description": "Model formula using patsy/R-style syntax.",
          "type": "string"
        },
        "iters": {
          "description": "iters parameter (int).",
          "type": "integer"
        },
        "n_fe": {
          "description": "Number of fe.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "n_obs": {
          "description": "Number of obs.",
          "type": "integer"
        },
        "n_singletons_dropped": {
          "description": "Number of singletons dropped.",
          "type": "integer"
        },
        "params": {
          "description": "params parameter (pd.Series).",
          "type": "string"
        },
        "pvalues": {
          "description": "pvalues parameter (pd.Series).",
          "type": "string"
        },
        "r2_within": {
          "description": "r2_within parameter (float).",
          "type": "number"
        },
        "residuals": {
          "description": "residuals parameter (np.ndarray).",
          "type": "string"
        },
        "se_type": {
          "description": "se_type parameter (str).",
          "type": "string"
        },
        "std_errors": {
          "description": "std_errors parameter (pd.Series).",
          "type": "string"
        },
        "tvalues": {
          "description": "tvalues parameter (pd.Series).",
          "type": "string"
        },
        "vcov": {
          "description": "Variance-covariance estimator option.",
          "type": "string"
        }
      },
      "required": [
        "params",
        "std_errors",
        "vcov",
        "tvalues",
        "pvalues",
        "conf_int_lower",
        "conf_int_upper",
        "residuals",
        "fitted_within",
        "n_obs",
        "n_singletons_dropped",
        "n_fe",
        "dof_fe",
        "df_resid",
        "r2_within",
        "se_type",
        "cluster_info",
        "formula",
        "absorber",
        "converged",
        "iters"
      ],
      "type": "object"
    }
  },
  {
    "description": "Overlap-weight (ATO) treatment effect estimator. Validation: certified parity evidence.",
    "name": "overlap_weights",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "covariates": {
          "description": "Covariates for the logistic propensity-score model.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "estimand": {
          "default": "ATO",
          "description": "Which generalized-weight scheme to use. All follow Li-Li-Li (2019) Table 1; 'ATO' uses the overlap weights; 'matching' uses the ``min(e, 1-e)`` weight; 'entropy' uses ``-e*log(e) - (1-e)*log(1-e)``; 'ATE/ATT/ATC' reduce to standard IPW for comparison.",
          "enum": [
            "ATO",
            "ATE",
            "ATT",
            "ATC",
            "matching",
            "entropy"
          ],
          "type": "string"
        },
        "n_bootstrap": {
          "default": 500,
          "description": "Paired-sample bootstrap replications for SE.",
          "type": "integer"
        },
        "seed": {
          "description": "Random seed for reproducible stochastic steps.",
          "type": "integer"
        },
        "treat": {
          "description": "Binary 0/1 treatment column.",
          "type": "string"
        },
        "trim": {
          "default": 0.0,
          "description": "Optional clip of pscore to ``[trim, 1-trim]``. For overlap weights this is rarely needed -- set to 0 by default.",
          "type": "number"
        },
        "y": {
          "description": "Outcome column.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "treat",
        "covariates"
      ],
      "type": "object"
    }
  },
  {
    "description": "Covariate-Balancing Propensity Score estimator (Imai-Ratkovic 2014). Validation: certified parity evidence.",
    "name": "cbps",
    "parameters": {
      "properties": {
        "add_intercept": {
          "default": true,
          "description": "Prepend a constant to the covariate matrix.",
          "type": "boolean"
        },
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "covariates": {
          "description": "Covariates entering the logit score.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "estimand": {
          "default": "ATE",
          "description": "estimand parameter (Literal['ATE', 'ATT']).",
          "enum": [
            "ATE",
            "ATT"
          ],
          "type": "string"
        },
        "n_bootstrap": {
          "default": 500,
          "description": "Number of bootstrap replications.",
          "type": "integer"
        },
        "seed": {
          "description": "Random seed for reproducible stochastic steps.",
          "type": "integer"
        },
        "treat": {
          "description": "Binary 0/1 treatment column.",
          "type": "string"
        },
        "trim": {
          "default": 0.0,
          "description": "Optional pscore clip for stability.",
          "type": "number"
        },
        "variant": {
          "default": "over",
          "description": "'exact': just-identified CBPS (only balance moments). 'over': over-identified CBPS (MLE + balance, solved via two-step GMM).",
          "enum": [
            "exact",
            "over"
          ],
          "type": "string"
        },
        "y": {
          "description": "Outcome column.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "treat",
        "covariates"
      ],
      "type": "object"
    }
  },
  {
    "description": "Stable Balancing Weights (Zubizarreta 2015) with optional ATT/ATE Validation: certified parity evidence.",
    "name": "sbw",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for inference on the outcome.",
          "type": "number"
        },
        "covariates": {
          "description": "Columns whose means must be balanced.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "delta": {
          "default": 0.02,
          "description": "Balance tolerance. With ``tolerance_scale='sd'`` the constraint is ``|mean_T(X_j) - weighted mean_C(X_j)| <= delta_j * sd(X_j)``.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "estimand": {
          "default": "att",
          "description": "``'att'`` reweights controls to match treated means (standard); ``'atc'`` reweights treated to match control means; ``'ate'`` reweights each group to match the pooled means.",
          "enum": [
            "att",
            "ate",
            "atc"
          ],
          "type": "string"
        },
        "include_squares": {
          "default": false,
          "description": "Also balance second-moments (w_j2 columns).",
          "type": "boolean"
        },
        "objective": {
          "default": "variance",
          "description": "Dispersion objective. ``'variance'`` minimises Sigma w_i2; ``'entropy'`` minimises Sigma w_i log(n * w_i) (KL from uniform).",
          "enum": [
            "variance",
            "entropy"
          ],
          "type": "string"
        },
        "solver_options": {
          "description": "Passed to ``scipy.optimize.minimize``.",
          "type": "object"
        },
        "tolerance_scale": {
          "default": "sd",
          "description": "Standard deviation ``delta`` is quoted in. ``'sd'`` is the full-sample sd; ``'target'`` the sd of the group being matched to and ``'group'`` the sd of the group being reweighted, matching ``sbw::sbw``'s ``bal_std=\"target\"`` / ``\"group\"``; ``'raw'`` is unstandardised.",
          "enum": [
            "sd",
            "raw",
            "target",
            "group"
          ],
          "type": "string"
        },
        "treat": {
          "description": "Binary 0/1 treatment indicator column.",
          "type": "string"
        },
        "y": {
          "description": "Outcome column. If provided, a weighted ATT/ATE estimate with HC-robust SE is attached to the returned :class:`SBWResult`.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "treat",
        "covariates"
      ],
      "type": "object"
    }
  },
  {
    "description": "Stable balancing weights with a diagnostic panel.",
    "name": "SBWResult",
    "parameters": {
      "properties": {
        "alpha": {
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "balance": {
          "description": "balance parameter (pd.DataFrame).",
          "type": "string"
        },
        "ci": {
          "description": "ci parameter (tuple).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "effective_sample_size": {
          "description": "effective_sample_size parameter (float).",
          "type": "number"
        },
        "estimand": {
          "description": "estimand parameter (str).",
          "type": "string"
        },
        "estimate": {
          "description": "estimate parameter (float).",
          "type": "number"
        },
        "method": {
          "description": "Estimator or algorithm variant to use.",
          "type": "string"
        },
        "n_obs": {
          "description": "Number of obs.",
          "type": "integer"
        },
        "pvalue": {
          "description": "pvalue parameter (float).",
          "type": "number"
        },
        "se": {
          "description": "se parameter (float).",
          "type": "number"
        },
        "solver_status": {
          "description": "solver_status parameter (str).",
          "type": "string"
        },
        "weights": {
          "description": "Observation weights.",
          "type": "string"
        }
      },
      "required": [
        "method",
        "estimand",
        "estimate",
        "se",
        "pvalue",
        "ci",
        "alpha",
        "n_obs",
        "weights",
        "effective_sample_size",
        "balance",
        "solver_status"
      ],
      "type": "object"
    }
  },
  {
    "description": "Genetic Matching for ATT estimation. Validation: certified parity evidence. Cost: Genetic search: population_size x generations full matching + balance evaluations (default 40 x 20 = 800 matching passes), each of which builds a pairwise distance matrix. Budget it as hundreds of sp.match calls, not one.",
    "name": "genmatch",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "covariates": {
          "description": "Covariate matrix, DataFrame, or column names.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "generations": {
          "default": 20,
          "description": "generations parameter (int).",
          "type": "integer"
        },
        "k": {
          "default": 1,
          "description": "Number of matches per treated unit.",
          "type": "integer"
        },
        "mutation_rate": {
          "default": 0.2,
          "description": "mutation_rate parameter (float).",
          "type": "number"
        },
        "population_size": {
          "default": 40,
          "description": "population_size parameter (int).",
          "type": "integer"
        },
        "random_state": {
          "default": 42,
          "description": "Random seed or RandomState for reproducible stochastic steps.",
          "type": "integer"
        },
        "treat": {
          "description": "Binary treatment indicator.",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "treat",
        "covariates"
      ],
      "type": "object"
    }
  },
  {
    "description": "Output of :func:`sp.genmatch` (Diamond-Sekhon genetic matching).",
    "name": "GenMatchResult",
    "parameters": {
      "properties": {
        "att": {
          "description": "att parameter (float).",
          "type": "number"
        },
        "att_se": {
          "description": "att_se parameter (float).",
          "type": "number"
        },
        "balance": {
          "description": "balance parameter (pd.DataFrame).",
          "type": "string"
        },
        "ci": {
          "description": "ci parameter (tuple).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "detail": {
          "default": null,
          "description": "Amount of result detail to return.",
          "type": "object"
        },
        "matches": {
          "description": "matches parameter (np.ndarray).",
          "type": "string"
        },
        "n_obs": {
          "description": "Number of obs.",
          "type": "integer"
        },
        "n_treated": {
          "description": "Number of treated.",
          "type": "integer"
        },
        "pvalue": {
          "description": "pvalue parameter (float).",
          "type": "number"
        },
        "weights": {
          "description": "Observation weights.",
          "type": "string"
        }
      },
      "required": [
        "att",
        "att_se",
        "ci",
        "pvalue",
        "weights",
        "balance",
        "matches",
        "n_treated",
        "n_obs"
      ],
      "type": "object"
    }
  },
  {
    "description": "Container for a ``sp.psmatch2`` run.",
    "name": "PSMatch2Result",
    "parameters": {
      "properties": {
        "common_support": {
          "description": "common_support parameter (str).",
          "type": "string"
        },
        "covariates": {
          "description": "Covariate matrix, DataFrame, or column names.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "matched_data": {
          "description": "matched_data parameter (pd.DataFrame).",
          "type": "string"
        },
        "method": {
          "default": "neighbor",
          "description": "Estimator or algorithm variant to use.",
          "type": "string"
        },
        "n_matches": {
          "description": "Number of matches.",
          "type": "integer"
        },
        "outcome": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        },
        "result": {
          "description": "result parameter (CausalResult).",
          "type": "string"
        },
        "treat": {
          "description": "Treatment indicator or first-treatment-period column.",
          "type": "string"
        }
      },
      "required": [
        "result",
        "matched_data",
        "treat",
        "covariates",
        "outcome",
        "n_matches",
        "common_support"
      ],
      "type": "object"
    }
  },
  {
    "description": "Subcluster wild cluster bootstrap for few-treated-clusters. Validation: certified parity evidence.",
    "name": "subcluster_wild_bootstrap",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "cluster": {
          "description": "Primary cluster column (for SE computation).",
          "type": "string"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "h0": {
          "default": 0.0,
          "description": "Null value.",
          "type": "number"
        },
        "n_boot": {
          "default": 999,
          "description": "Bootstrap replications.",
          "type": "integer"
        },
        "seed": {
          "description": "Random seed for reproducible stochastic steps.",
          "type": "integer"
        },
        "subcluster": {
          "description": "Finer grouping at which sign-flips occur. If ``None``, every observation is its own subcluster (pure Rademacher at obs level).",
          "type": "string"
        },
        "test_var": {
          "description": "Parameter to test; default last element of ``x``.",
          "type": "string"
        },
        "weight_type": {
          "default": "webb",
          "description": "Distribution of sign flips. ``'webb'`` (6-point) recommended when treatment has <= 5 treated clusters.",
          "enum": [
            "rademacher",
            "webb",
            "mammen"
          ],
          "type": "string"
        },
        "x": {
          "description": "Primary running variable, regressor, or feature input for this estimator.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "x",
        "cluster"
      ],
      "type": "object"
    }
  },
  {
    "description": "Confidence interval via bootstrap p-value inversion. Validation: certified parity evidence.",
    "name": "wild_cluster_ci_inv",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "cluster": {
          "description": "Cluster identifier column for clustered standard errors.",
          "type": "string"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "grid_size": {
          "default": 41,
          "description": "Number of null-value grid points to evaluate (odd preferred).",
          "type": "integer"
        },
        "grid_span": {
          "default": 6.0,
          "description": "Half-width of the search grid in units of cluster-robust SE.",
          "type": "number"
        },
        "n_boot": {
          "default": 999,
          "description": "Number of bootstrap replications.",
          "type": "integer"
        },
        "seed": {
          "description": "Random seed for reproducible stochastic steps.",
          "type": "integer"
        },
        "test_var": {
          "description": "test_var parameter (Optional[str]).",
          "type": "string"
        },
        "weight_type": {
          "default": "webb",
          "description": "weight_type parameter (str).",
          "type": "string"
        },
        "x": {
          "description": "Primary running variable, regressor, or feature input for this estimator.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "x",
        "cluster"
      ],
      "type": "object"
    }
  },
  {
    "description": "Compute N-way cluster-robust variance of an OLS coefficient vector. Validation: certified parity evidence.",
    "name": "multiway_cluster_vcov",
    "parameters": {
      "properties": {
        "X": {
          "description": "Design matrix used in the regression.",
          "type": "string"
        },
        "clusters": {
          "description": "One or more cluster variables, one per dimension. Non-numeric labels are supported.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "df_adjust": {
          "default": true,
          "description": "If True, apply the G/(G-1) * (n-1)/(n-k) CR1 finite-sample correction per component variance. If False, uses raw sandwich (useful when the caller has already degreed-freedom adjusted).",
          "type": "boolean"
        },
        "n_params": {
          "description": "Override for the ``k`` used in DOF adjustment; useful when FEs have been absorbed (pass total absorbed DOF here).",
          "type": "integer"
        },
        "psd_correct": {
          "default": true,
          "description": "Project V onto PSD cone by zeroing negative eigenvalues.",
          "type": "boolean"
        },
        "resid": {
          "description": "OLS residuals.",
          "type": "string"
        }
      },
      "required": [
        "X",
        "resid",
        "clusters"
      ],
      "type": "object"
    }
  },
  {
    "description": "Return cluster-robust standard errors (diagonal sqrt of vcov). Validation: certified parity evidence.",
    "name": "cluster_robust_se",
    "parameters": {
      "properties": {
        "X": {
          "description": "Feature matrix or covariate DataFrame.",
          "type": "string"
        },
        "clusters": {
          "description": "Cluster labels for clustered inference.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "resid": {
          "description": "resid parameter (np.ndarray).",
          "type": "string"
        }
      },
      "required": [
        "X",
        "resid",
        "clusters"
      ],
      "type": "object"
    }
  },
  {
    "description": "CR3 cluster-jackknife variance (delete-one-cluster). Validation: certified parity evidence.",
    "name": "cr3_jackknife_vcov",
    "parameters": {
      "properties": {
        "X": {
          "description": "Feature matrix or covariate DataFrame.",
          "type": "string"
        },
        "cluster": {
          "description": "Cluster identifier column for clustered standard errors.",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "X",
        "y",
        "cluster"
      ],
      "type": "object"
    }
  },
  {
    "description": "Stata-style stateful regression-table builder.",
    "name": "OutReg2",
    "parameters": {
      "properties": {},
      "required": [],
      "type": "object"
    }
  },
  {
    "description": "Forest plot comparing coefficients across models.",
    "name": "coefplot",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for CIs.",
          "type": "number"
        },
        "ax": {
          "description": "ax parameter.",
          "type": "string"
        },
        "colors": {
          "description": "colors parameter (Optional[List[str]]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "figsize": {
          "default": [
            8,
            6
          ],
          "description": "figsize parameter (tuple).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "model_names": {
          "description": "model_names parameter (Optional[List[str]]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "title": {
          "description": "title parameter (Optional[str]).",
          "type": "string"
        },
        "variables": {
          "description": "Which variables to plot. Default: all shared variables.",
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "required": [],
      "type": "object"
    }
  },
  {
    "description": "Return ``pgfplots`` / TikZ source for a coefficient forest plot.",
    "name": "coefplot_tikz",
    "parameters": {
      "properties": {
        "coef_labels": {
          "description": "Rename variables on the y-axis, e.g. ``{\"x\": \"Treatment\"}``.",
          "type": "object"
        },
        "level": {
          "default": 0.95,
          "description": "Confidence level for the error bars (normal approximation ``b +/- z*se``, matching :func:`coefplot`).",
          "type": "number"
        },
        "model_names": {
          "description": "Legend labels. Default ``Model 1``, ``Model 2``, ...",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "standalone": {
          "default": false,
          "description": "When ``True``, wrap the ``tikzpicture`` in a compilable ``standalone`` document (with the required ``\\usepackage{pgfplots}``). Otherwise return just the ``tikzpicture`` to ``\\input`` into a paper (needs ``\\usepackage{pgfplots}`` in the preamble).",
          "type": "boolean"
        },
        "title": {
          "description": "Plot title. Default ``\"Coefficient plot\"``.",
          "type": "string"
        },
        "variables": {
          "description": "Which coefficients to plot. Default: every shared variable, sorted.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "xlabel": {
          "default": "Coefficient estimate",
          "description": "x-axis label.",
          "type": "string"
        }
      },
      "required": [],
      "type": "object"
    }
  },
  {
    "description": "Generate descriptive statistics table.",
    "name": "sumstats",
    "parameters": {
      "properties": {
        "by": {
          "description": "Group-by variable for stratified summaries.",
          "type": "string"
        },
        "by_labels": {
          "description": "Group labels for ``by=`` panel headers, e.g. ``{0: 'Control', 1: 'Treated'}``. When ``by`` is binary 0/1 and no ``by_labels`` is supplied, ``Control``/``Treated`` is auto-applied so academic Table 1 reads correctly out of the box.",
          "type": "object"
        },
        "data": {
          "description": "Input data.",
          "type": "string"
        },
        "digits": {
          "description": "digits parameter (Optional[int]).",
          "type": "integer"
        },
        "fmt": {
          "description": "Number format.",
          "type": "integer"
        },
        "labels": {
          "description": "Variable labels: ``{'x1': 'Education (years)'}``.",
          "type": "object"
        },
        "output": {
          "default": "text",
          "description": "'text', 'latex', 'html', 'dataframe', or filepath (.xlsx/.docx).",
          "type": "string"
        },
        "stats": {
          "description": "Statistics to compute. Default: ['n', 'mean', 'sd', 'min', 'p25', 'median', 'p75', 'max'].",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "title": {
          "default": "Summary Statistics",
          "description": "Table title.",
          "type": "string"
        },
        "vars": {
          "description": "Variables to summarize. Defaults to all numeric columns.",
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "required": [
        "data"
      ],
      "type": "object"
    }
  },
  {
    "description": "Generate a balance table comparing treated and control groups.",
    "name": "balance_table",
    "parameters": {
      "properties": {
        "covariates": {
          "description": "Variables to check balance on.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "data": {
          "description": "Input data.",
          "type": "string"
        },
        "digits": {
          "description": "digits parameter (Optional[int]).",
          "type": "integer"
        },
        "fmt": {
          "description": "Number format.",
          "type": "integer"
        },
        "labels": {
          "description": "Variable labels.",
          "type": "object"
        },
        "output": {
          "default": "text",
          "description": "'text', 'latex', 'html', 'dataframe', or filepath.",
          "type": "string"
        },
        "test": {
          "default": "ttest",
          "description": "Test for difference: 'ttest' or 'ranksum'.",
          "type": "string"
        },
        "title": {
          "default": "Balance Table",
          "description": "Table title.",
          "type": "string"
        },
        "treat": {
          "description": "Binary treatment variable (0/1).",
          "type": "string"
        }
      },
      "required": [
        "data",
        "treat",
        "covariates"
      ],
      "type": "object"
    }
  },
  {
    "description": "Cross-tabulation with chi-squared / Fisher's exact test.",
    "name": "tab",
    "parameters": {
      "properties": {
        "col": {
          "description": "Column variable. If None, produces a one-way frequency table.",
          "type": "string"
        },
        "data": {
          "description": "Input data.",
          "type": "string"
        },
        "margins": {
          "default": true,
          "description": "Show row/column totals.",
          "type": "boolean"
        },
        "normalize": {
          "description": "'row', 'col', 'all', or None. Normalize to proportions.",
          "type": "string"
        },
        "output": {
          "default": "text",
          "description": "'text', 'dataframe', 'latex', or filepath (.xlsx/.docx).",
          "type": "string"
        },
        "row": {
          "description": "Row variable.",
          "type": "string"
        },
        "test": {
          "default": true,
          "description": "Include chi-squared test (and Fisher's exact for 2x2).",
          "type": "boolean"
        },
        "title": {
          "description": "Table title.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "row"
      ],
      "type": "object"
    }
  },
  {
    "description": "Store a model result (like Stata's ``estimates store``).",
    "name": "eststo",
    "parameters": {
      "properties": {
        "name": {
          "description": "name parameter (Optional[str]).",
          "type": "string"
        },
        "result": {
          "description": "result parameter.",
          "type": "string"
        }
      },
      "required": [
        "result"
      ],
      "type": "object"
    }
  },
  {
    "description": "Clear all stored model results.",
    "name": "estclear",
    "parameters": {
      "properties": {},
      "required": [],
      "type": "object"
    }
  },
  {
    "description": "Rich result object for regression tables with multi-format export.",
    "name": "RegtableResult",
    "parameters": {
      "properties": {
        "add_rows": {
          "description": "add_rows parameter (Optional[Dict[str, List[str]]]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "apply_coef": {
          "description": "apply_coef parameter (Optional[Any]).",
          "type": "string"
        },
        "apply_coef_deriv": {
          "description": "apply_coef_deriv parameter (Optional[Any]).",
          "type": "string"
        },
        "coef_labels": {
          "description": "coef_labels parameter (Optional[Dict[str, str]]).",
          "type": "object"
        },
        "column_spanners": {
          "description": "column_spanners parameter (Optional[List[Tuple[str, int]]]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "dep_var_labels": {
          "description": "dep_var_labels parameter (Optional[List[str]]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "drop": {
          "description": "drop parameter (Optional[List[str]]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "eform_flags": {
          "description": "eform_flags parameter (Optional[List[bool]]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "escape": {
          "default": true,
          "description": "escape parameter (bool).",
          "type": "boolean"
        },
        "estimate_template": {
          "description": "estimate_template parameter (Optional[str]).",
          "type": "string"
        },
        "fmt": {
          "description": "fmt parameter (str).",
          "type": "string"
        },
        "keep": {
          "description": "keep parameter (Optional[List[str]]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "model_labels": {
          "description": "model_labels parameter (List[str]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "multi_se": {
          "description": "multi_se parameter (Optional[List[Tuple[str, List[Dict[str, float]]]]]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "notation": {
          "default": "stars",
          "description": "notation parameter (Union[str, Tuple[str, ...]]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "notes": {
          "description": "notes parameter (Optional[List[str]]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "order": {
          "description": "order parameter (Optional[List[str]]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "output": {
          "default": "text",
          "description": "output parameter (str).",
          "type": "string"
        },
        "panel_labels": {
          "description": "panel_labels parameter (Optional[List[str]]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "panels": {
          "description": "panels parameter (List['_PanelData']).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "quarto_caption": {
          "description": "quarto_caption parameter (Optional[str]).",
          "type": "string"
        },
        "quarto_label": {
          "description": "quarto_label parameter (Optional[str]).",
          "type": "string"
        },
        "rules": {
          "default": "auto",
          "description": "rules parameter (str).",
          "type": "string"
        },
        "se_fmt": {
          "description": "se_fmt parameter (Optional[str]).",
          "type": "string"
        },
        "se_label": {
          "description": "se_label parameter (Optional[str]).",
          "type": "string"
        },
        "se_type": {
          "description": "se_type parameter (str).",
          "type": "string"
        },
        "star_levels": {
          "description": "star_levels parameter (Tuple[float, ...]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "stars": {
          "description": "stars parameter (bool).",
          "type": "boolean"
        },
        "statistic_template": {
          "description": "statistic_template parameter (Optional[str]).",
          "type": "string"
        },
        "stats": {
          "description": "stats parameter (Optional[List[str]]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "stats_fmt": {
          "default": "%.3f",
          "description": "stats_fmt parameter (str).",
          "type": "string"
        },
        "template": {
          "description": "template parameter (Optional[str]).",
          "type": "string"
        },
        "tests_rows": {
          "description": "tests_rows parameter (Optional[List[Tuple[str, List[str]]]]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "title": {
          "description": "title parameter (Optional[str]).",
          "type": "string"
        },
        "transpose": {
          "default": false,
          "description": "transpose parameter (bool).",
          "type": "boolean"
        }
      },
      "required": [
        "panels",
        "panel_labels",
        "model_labels",
        "dep_var_labels",
        "coef_labels",
        "keep",
        "drop",
        "order",
        "se_type",
        "stars",
        "star_levels",
        "fmt",
        "title",
        "notes",
        "add_rows",
        "stats"
      ],
      "type": "object"
    }
  },
  {
    "description": "Rich result object for balance / mean comparison tables.",
    "name": "MeanComparisonResult",
    "parameters": {
      "properties": {
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "fmt": {
          "description": "fmt parameter (str).",
          "type": "string"
        },
        "group": {
          "description": "Group or cohort identifier.",
          "type": "string"
        },
        "group_labels": {
          "description": "group_labels parameter (Tuple[str, str]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "output": {
          "default": "text",
          "description": "output parameter (str).",
          "type": "string"
        },
        "test": {
          "description": "test parameter (str).",
          "type": "string"
        },
        "title": {
          "description": "title parameter (str).",
          "type": "string"
        },
        "variables": {
          "description": "variables parameter (List[str]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "weights": {
          "description": "Observation weights.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "variables",
        "group",
        "group_labels",
        "test",
        "fmt",
        "title",
        "weights"
      ],
      "type": "object"
    }
  },
  {
    "description": "A named, ordered bundle of tables and prose for a single document.",
    "name": "Collection",
    "parameters": {
      "properties": {
        "template": {
          "default": "aer",
          "description": "Forwarded to ``paper_tables`` style; also drives the default star levels used by ``add_regression``.",
          "enum": [
            "aer",
            "qje",
            "econometrica",
            "restat"
          ],
          "type": "string"
        },
        "title": {
          "description": "Displayed at the top of the rendered document.",
          "type": "string"
        }
      },
      "required": [],
      "type": "object"
    }
  },
  {
    "description": "One entry in a :class:`Collection`.",
    "name": "CollectionItem",
    "parameters": {
      "properties": {
        "kind": {
          "description": "kind parameter (ItemKind).",
          "type": "string"
        },
        "name": {
          "description": "name parameter (str).",
          "type": "string"
        },
        "options": {
          "default": null,
          "description": "options parameter (Dict[str, Any]).",
          "type": "object"
        },
        "payload": {
          "description": "payload parameter.",
          "type": "string"
        },
        "title": {
          "description": "title parameter (Optional[str]).",
          "type": "string"
        }
      },
      "required": [
        "name",
        "kind",
        "title",
        "payload"
      ],
      "type": "object"
    }
  },
  {
    "description": "Container for the multi-panel paper-table bundle.",
    "name": "PaperTables",
    "parameters": {
      "properties": {
        "heterogeneity": {
          "description": "heterogeneity parameter (Optional[RegtableResult]).",
          "type": "string"
        },
        "main": {
          "description": "main parameter (Optional[RegtableResult]).",
          "type": "string"
        },
        "placebo": {
          "description": "placebo parameter (Optional[RegtableResult]).",
          "type": "string"
        },
        "robustness": {
          "description": "robustness parameter (Optional[RegtableResult]).",
          "type": "string"
        },
        "template": {
          "default": "aer",
          "description": "template parameter (str).",
          "type": "string"
        }
      },
      "required": [],
      "type": "object"
    }
  },
  {
    "description": "Return a citation string for StatsPAI.",
    "name": "citation",
    "parameters": {
      "properties": {
        "format": {
          "default": "bibtex",
          "description": "- ``\"bibtex\"`` -- BibTeX entry suitable for LaTeX bibliographies. - ``\"apa\"`` -- APA-style human-readable string. - ``\"plain\"`` -- Minimal plain-text string. - ``\"cff\"`` -- Raw contents of the repository ``CITATION.cff`` file (ships with the package; ``which`` is ignored).",
          "enum": [
            "bibtex",
            "apa",
            "plain",
            "cff"
          ],
          "type": "string"
        },
        "which": {
          "default": "paper",
          "description": "- ``\"paper\"`` -- the peer-reviewed JOSS article (Wang & Rozelle, 2026, *J. Open Source Softw.* 11(125), 10604). This is the preferred citation. - ``\"software\"`` -- the versioned software entry (Zenodo concept DOI plus the installed ``__version__``); use it when the exact release matters, e.g. in a replication package. - ``\"both\"`` -- the two entries separated by a blank line.",
          "enum": [
            "paper",
            "software",
            "both"
          ],
          "type": "string"
        }
      },
      "required": [],
      "type": "object"
    }
  },
  {
    "description": "Return the canonical names of every registered journal preset.",
    "name": "list_journal_templates",
    "parameters": {
      "properties": {},
      "required": [],
      "type": "object"
    }
  },
  {
    "description": "Look up a journal preset by name (case-insensitive).",
    "name": "get_journal_template",
    "parameters": {
      "properties": {
        "name": {
          "description": "Template identifier (e.g. ``\"aer\"`` / ``\"AER\"`` / ``\"jf\"``).",
          "type": "string"
        }
      },
      "required": [
        "name"
      ],
      "type": "object"
    }
  },
  {
    "description": "A traceable record of how a single estimate was produced.",
    "name": "Provenance",
    "parameters": {
      "properties": {
        "data_hash": {
          "description": "data_hash parameter (Optional[str]).",
          "type": "string"
        },
        "data_shape": {
          "description": "data_shape parameter (Optional[list]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "function": {
          "description": "function parameter (str).",
          "type": "string"
        },
        "params": {
          "default": null,
          "description": "params parameter (Dict[str, Any]).",
          "type": "object"
        },
        "python_version": {
          "default": null,
          "description": "python_version parameter (str).",
          "type": "string"
        },
        "run_id": {
          "default": null,
          "description": "run_id parameter (str).",
          "type": "string"
        },
        "statspai_version": {
          "default": null,
          "description": "statspai_version parameter (str).",
          "type": "string"
        },
        "timestamp": {
          "default": null,
          "description": "timestamp parameter (str).",
          "type": "string"
        }
      },
      "required": [
        "function"
      ],
      "type": "object"
    }
  },
  {
    "description": "Attach a :class:`Provenance` record as ``result._provenance``.",
    "name": "attach_provenance",
    "parameters": {
      "properties": {
        "data": {
          "description": "The estimator's input data. Used to compute a 12-char SHA-256 fingerprint.",
          "type": "string"
        },
        "enabled": {
          "default": true,
          "description": "Set to False to skip provenance entirely (zero-overhead path).",
          "type": "boolean"
        },
        "function": {
          "description": "Logical name of the producing call, e.g. ``\"statspai.did.callaway_santanna\"``.",
          "type": "string"
        },
        "overwrite": {
          "default": false,
          "description": "If False (default) and ``result._provenance`` already exists, do nothing -- preserves the *first* (most-specific) record set by an inner estimator.",
          "type": "boolean"
        },
        "params": {
          "description": "Call arguments. Will be summarised (frames hashed; long sequences truncated; non-serialisable values reduced to repr).",
          "type": "object"
        },
        "result": {
          "description": "The estimator result. Must accept attribute assignment; ``CausalResult`` / ``ResultBase`` / dataclasses / SimpleNamespace all work. Tuples / dicts / immutable types do not -- for those the call is a silent no-op.",
          "type": "string"
        }
      },
      "required": [
        "result",
        "function"
      ],
      "type": "object"
    }
  },
  {
    "description": "Return ``result._provenance`` if present, else ``None``.",
    "name": "get_provenance",
    "parameters": {
      "properties": {
        "result": {
          "description": "result parameter.",
          "type": "string"
        }
      },
      "required": [
        "result"
      ],
      "type": "object"
    }
  },
  {
    "description": "Return a short SHA-256 fingerprint of *data*, or ``None``.",
    "name": "compute_data_hash",
    "parameters": {
      "properties": {
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "length": {
          "default": 12,
          "description": "length parameter (int).",
          "type": "integer"
        }
      },
      "required": [
        "data"
      ],
      "type": "object"
    }
  },
  {
    "description": "Pretty multi-line rendering of a :class:`Provenance` record.",
    "name": "format_provenance",
    "parameters": {
      "properties": {
        "indent": {
          "default": 2,
          "description": "indent parameter (int).",
          "type": "integer"
        },
        "prov": {
          "description": "prov parameter (Provenance).",
          "type": "string"
        }
      },
      "required": [
        "prov"
      ],
      "type": "object"
    }
  },
  {
    "description": "Aggregate a lineage report across multiple results.",
    "name": "lineage_summary",
    "parameters": {
      "properties": {},
      "required": [],
      "type": "object"
    }
  },
  {
    "description": "Lightweight summary returned by :func:`replication_pack`.",
    "name": "ReplicationPack",
    "parameters": {
      "properties": {
        "manifest": {
          "description": "manifest parameter (Dict[str, Any]).",
          "type": "object"
        },
        "output_path": {
          "description": "Filesystem path for output.",
          "type": "string"
        },
        "warnings": {
          "description": "warnings parameter (List[str]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "required": [
        "output_path",
        "manifest",
        "warnings"
      ],
      "type": "object"
    }
  },
  {
    "description": "Convert StatsPAI table / DataFrame / Collection into ``GT``.",
    "name": "gt",
    "parameters": {
      "properties": {
        "apply_theme": {
          "default": true,
          "description": "Apply the journal-preset gt theme. Set False to keep gt's defaults (useful when the caller wants to compose their own ``tab_style`` chain).",
          "type": "boolean"
        },
        "notes": {
          "description": "Footer notes. When omitted, ``result.notes`` is used.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "result": {
          "description": "See dispatch description above.",
          "type": "string"
        },
        "rowname_col": {
          "description": "Column to elevate to GT's row label position. For ``RegtableResult`` we default to the variable column automatically; for plain DataFrames, pass the column name.",
          "type": "string"
        },
        "subtitle": {
          "description": "Subtitle line (``tab_header(subtitle=...)``).",
          "type": "string"
        },
        "template": {
          "description": "Journal preset (``\"aer\"`` / ``\"qje\"`` / ...). Default: pulled from ``result.template`` when present.",
          "type": "string"
        },
        "title": {
          "description": "Override the table title. Default: pulled from ``result.title`` when present.",
          "type": "string"
        }
      },
      "required": [
        "result"
      ],
      "type": "object"
    }
  },
  {
    "description": "Return True iff ``great_tables`` can be imported in this env.",
    "name": "is_great_tables_available",
    "parameters": {
      "properties": {},
      "required": [],
      "type": "object"
    }
  },
  {
    "description": "Return the canonical Zotero/styles URL for a CSL preset.",
    "name": "csl_url",
    "parameters": {
      "properties": {
        "name": {
          "description": "name parameter (str).",
          "type": "string"
        }
      },
      "required": [
        "name"
      ],
      "type": "object"
    }
  },
  {
    "description": "Return the canonical ``.csl`` *filename* (no path) for a preset.",
    "name": "csl_filename",
    "parameters": {
      "properties": {
        "name": {
          "description": "name parameter (str).",
          "type": "string"
        }
      },
      "required": [
        "name"
      ],
      "type": "object"
    }
  },
  {
    "description": "List ``(short_name, full_label)`` pairs for every registered style.",
    "name": "list_csl_styles",
    "parameters": {
      "properties": {},
      "required": [],
      "type": "object"
    }
  },
  {
    "description": "Parse a citation string into a BibTeX-shaped dict.",
    "name": "parse_citation_to_bib",
    "parameters": {
      "properties": {
        "citation": {
          "description": "citation parameter (str).",
          "type": "string"
        },
        "key": {
          "description": "key parameter (Optional[str]).",
          "type": "string"
        }
      },
      "required": [
        "citation"
      ],
      "type": "object"
    }
  },
  {
    "description": "Compute a stable BibTeX key from a free-form citation string.",
    "name": "make_bib_key",
    "parameters": {
      "properties": {
        "citation": {
          "description": "citation parameter (str).",
          "type": "string"
        }
      },
      "required": [
        "citation"
      ],
      "type": "object"
    }
  },
  {
    "description": "Parse a sequence of citation strings into BibTeX-entry dicts.",
    "name": "citations_to_bib_entries",
    "parameters": {
      "properties": {
        "citations": {
          "description": "citations parameter (Iterable[str]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "required": [
        "citations"
      ],
      "type": "object"
    }
  },
  {
    "description": "Write a clean BibTeX file from citation strings or entry dicts.",
    "name": "write_bib",
    "parameters": {
      "properties": {
        "append": {
          "default": false,
          "description": "Append to an existing file rather than overwriting.",
          "type": "boolean"
        },
        "citations": {
          "description": "Either free-form citation strings (parsed via :func:`parse_citation_to_bib`) or pre-built entry dicts ``{\"key\": ..., \"type\": ..., \"fields\": {...}}``.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "header": {
          "default": true,
          "description": "Prepend a one-line ``% Auto-generated by StatsPAI ...`` comment at the top of fresh files (skipped on append).",
          "type": "boolean"
        },
        "path": {
          "description": "Destination ``.bib`` file. Parent dirs are created.",
          "type": "string"
        }
      },
      "required": [
        "citations",
        "path"
      ],
      "type": "object"
    }
  },
  {
    "description": "Covariate-adjusted comparison of group means (ANCOVA). Validation: certified parity evidence.",
    "name": "ancova",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for the reported confidence interval.",
          "type": "number"
        },
        "cluster": {
          "description": "Cluster-robust SE column.",
          "type": "string"
        },
        "covariates": {
          "description": "Adjustment covariates. Non-numeric covariates enter as categorical (``C(col)``).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "data": {
          "description": "One row per unit.",
          "type": "string"
        },
        "group": {
          "description": "Treatment-group column. A 0/1 numeric column is used directly; any other two-level column is encoded (pass ``group_value`` to name the treated level explicitly).",
          "type": "string"
        },
        "group_value": {
          "description": "The level of ``group`` that denotes treatment.",
          "type": "string"
        },
        "outcome": {
          "description": "Outcome column.",
          "type": "string"
        },
        "robust": {
          "default": "hc1",
          "description": "Heteroskedasticity-robust SE type forwarded to :func:`sp.regress`.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "outcome",
        "group"
      ],
      "type": "object"
    }
  },
  {
    "description": "Binned scatter plot with optional residualization.",
    "name": "binscatter",
    "parameters": {
      "properties": {
        "absorb": {
          "description": "Fixed-effect variables to absorb (demeaned within groups) before binning. More efficient than including as dummies in ``controls``.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "ax": {
          "description": "Axes to draw on. Creates new figure if None.",
          "type": "string"
        },
        "by": {
          "description": "Grouping variable -- produces separate series on the same plot (e.g., ``by='female'`` overlays male and female).",
          "type": "string"
        },
        "ci": {
          "default": false,
          "description": "Show pointwise confidence intervals within each bin.",
          "type": "boolean"
        },
        "ci_level": {
          "default": 0.95,
          "description": "Confidence level for CIs.",
          "type": "number"
        },
        "colors": {
          "description": "Colors for each ``by`` group.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "controls": {
          "description": "Control variables to partial out from both Y and X before binning. Uses OLS residualization.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "data": {
          "description": "Input data.",
          "type": "string"
        },
        "figsize": {
          "default": [
            8,
            6
          ],
          "description": "figsize parameter (Tuple[float, float]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "fit": {
          "default": "linear",
          "description": "Overlay fit line: ``'linear'``, ``'quadratic'``, ``'cubic'``, ``'poly4'``, or ``'none'``.",
          "type": "string"
        },
        "fit_on_raw": {
          "default": false,
          "description": "If True, fit the overlay line on the raw (residualized) micro data, not the bin means. More accurate for nonlinear fits.",
          "type": "boolean"
        },
        "legend": {
          "default": true,
          "description": "legend parameter (bool).",
          "type": "boolean"
        },
        "line_kw": {
          "description": "Extra kwargs for ``ax.plot()`` (the fit line).",
          "type": "object"
        },
        "markers": {
          "description": "Marker styles for each ``by`` group.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "n_bins": {
          "description": "Number of bins. Default: min(20, ceil(n^(1/3))) following the IMSE-optimal rate from Cattaneo et al. (2024).",
          "type": "integer"
        },
        "quantiles": {
          "default": true,
          "description": "If True, bins are quantile-spaced (equal number of obs per bin). If False, bins are evenly spaced on the X-axis.",
          "type": "boolean"
        },
        "scatter_kw": {
          "description": "Extra kwargs for ``ax.scatter()``.",
          "type": "object"
        },
        "title": {
          "description": "title parameter (Optional[str]).",
          "type": "string"
        },
        "weights": {
          "description": "Analytic weight variable.",
          "type": "string"
        },
        "x": {
          "description": "Running variable (X-axis).",
          "type": "string"
        },
        "x_label": {
          "description": "x_label parameter (Optional[str]).",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable (Y-axis).",
          "type": "string"
        },
        "y_label": {
          "description": "y_label parameter (Optional[str]).",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "x"
      ],
      "type": "object"
    }
  },
  {
    "description": "Normalise a fitted result into a tidy counterfactual frame.",
    "name": "counterfactual_data",
    "parameters": {
      "properties": {
        "result": {
          "description": "A StatsPAI result carrying an observed-vs-counterfactual time series: a ``sp.causal_impact`` result, any ``sp.synth`` family result, or an ``sp.its`` ``ITSResult``.",
          "type": "string"
        }
      },
      "required": [
        "result"
      ],
      "type": "object"
    }
  },
  {
    "description": "Plot observed vs counterfactual with an uncertainty band.",
    "name": "counterfactual_plot",
    "parameters": {
      "properties": {
        "ax": {
          "description": "Draw the trajectory panel into this axis instead of a new figure. When given, the effect panel is suppressed (single-axis mode).",
          "type": "string"
        },
        "bands": {
          "default": true,
          "description": "Shade the counterfactual / effect uncertainty bands when available.",
          "type": "boolean"
        },
        "figsize": {
          "default": [
            10.0,
            7.0
          ],
          "description": "Figure size when a new figure is created.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "result": {
          "description": "Any result supported by :func:`counterfactual_data`.",
          "type": "string"
        },
        "show_effect": {
          "default": true,
          "description": "Add the lower pointwise-effect panel.",
          "type": "boolean"
        },
        "title": {
          "description": "Figure title. Defaults to the result's method.",
          "type": "string"
        }
      },
      "required": [
        "result"
      ],
      "type": "object"
    }
  },
  {
    "description": "Estimate geo-experiment lift via synthetic control. Validation: validated evidence tier (known-truth, reference, external-parity, or Monte Carlo artifact).",
    "name": "geolift",
    "parameters": {
      "properties": {
        "agg": {
          "default": "mean",
          "description": "How to aggregate multiple treated geos into one treated series.",
          "enum": [
            "mean",
            "sum"
          ],
          "type": "string"
        },
        "alpha": {
          "default": 0.05,
          "description": "Significance level forwarded to :func:`sp.synth`.",
          "type": "number"
        },
        "data": {
          "description": "Long panel: one row per (geo, time).",
          "type": "string"
        },
        "geo": {
          "description": "Geography (market) id column.",
          "type": "string"
        },
        "method": {
          "default": "classic",
          "description": "Synthetic-control method passed to :func:`sp.synth` (e.g. ``'classic'``, ``'augmented'``, ``'sdid'``).",
          "type": "string"
        },
        "outcome": {
          "description": "Outcome / KPI column (e.g. sales, conversions).",
          "type": "string"
        },
        "time": {
          "description": "Time column.",
          "type": "string"
        },
        "treated_geos": {
          "description": "The geography (or geographies) the intervention was switched on in.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "treatment_time": {
          "description": "First treated period (inclusive). Earlier periods train the synthetic control.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "outcome",
        "geo",
        "time",
        "treated_geos",
        "treatment_time"
      ],
      "type": "object"
    }
  },
  {
    "description": "Pre/post non-equivalent group design (NEGD). Validation: certified parity evidence.",
    "name": "negd",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "As in :func:`ancova`.",
          "type": "number"
        },
        "cluster": {
          "description": "As in :func:`ancova`.",
          "type": "string"
        },
        "covariates": {
          "description": "Additional adjustment covariates.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "data": {
          "description": "One row per unit (wide format: a ``pre`` and a ``post`` column).",
          "type": "string"
        },
        "group": {
          "description": "Treatment-group column (see :func:`ancova` for encoding).",
          "type": "string"
        },
        "group_value": {
          "description": "As in :func:`ancova`.",
          "type": "string"
        },
        "method": {
          "default": "ancova",
          "description": "Estimator (see above).",
          "enum": [
            "ancova",
            "change_score"
          ],
          "type": "string"
        },
        "post": {
          "description": "Baseline and follow-up outcome columns.",
          "type": "string"
        },
        "pre": {
          "description": "Baseline and follow-up outcome columns.",
          "type": "string"
        },
        "robust": {
          "default": "hc1",
          "description": "As in :func:`ancova`.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "group",
        "pre",
        "post"
      ],
      "type": "object"
    }
  },
  {
    "description": "Set global matplotlib theme for publication-quality plots.",
    "name": "set_theme",
    "parameters": {
      "properties": {
        "font_scale": {
          "default": 1.0,
          "description": "Scale factor for all font sizes.",
          "type": "number"
        },
        "name": {
          "default": "academic",
          "description": "Theme name. Use ``list_themes()`` to see all available options. ``'default'`` resets to matplotlib defaults.",
          "type": "string"
        },
        "palette": {
          "description": "Color palette name. Defaults to matching the theme. Only applies to StatsPAI themes.",
          "type": "string"
        }
      },
      "required": [],
      "type": "object"
    }
  },
  {
    "description": "List all available themes grouped by source.",
    "name": "list_themes",
    "parameters": {
      "properties": {},
      "required": [],
      "type": "object"
    }
  },
  {
    "description": "One-line fix for Chinese text rendering in matplotlib.",
    "name": "use_chinese",
    "parameters": {
      "properties": {
        "style": {
          "default": "auto",
          "description": "- ``'auto'``: auto-detect the best available Chinese font - ``'serif'``: prefer serif fonts ( Songti SC, SimSun) - ``'sans'``: prefer sans-serif fonts ( PingFang, SimHei) - Any specific font name, e.g. ``'Songti SC'``, ``'SimHei'``",
          "type": "string"
        }
      },
      "required": [],
      "type": "object"
    }
  },
  {
    "description": "Open an interactive editor for a matplotlib figure.",
    "name": "interactive",
    "parameters": {
      "properties": {
        "fig": {
          "description": "The figure to edit. Typically from ``result.plot()`` or ``sp.binscatter()``.",
          "type": "string"
        },
        "protect_data": {
          "default": true,
          "description": "Lock data-representing elements (scatter points, regression lines, confidence intervals) from modification.",
          "type": "boolean"
        }
      },
      "required": [
        "fig"
      ],
      "type": "object"
    }
  },
  {
    "description": "Get reproducible code for all interactive edits made to a figure.",
    "name": "get_code",
    "parameters": {
      "properties": {
        "fig": {
          "description": "fig parameter.",
          "type": "string"
        }
      },
      "required": [
        "fig"
      ],
      "type": "object"
    }
  },
  {
    "description": "Translate a Stata command line to a StatsPAI tool-call payload.",
    "name": "from_stata",
    "parameters": {
      "properties": {
        "line": {
          "description": "One Stata command. Multi-line ``do`` files must be split by the caller.",
          "type": "string"
        }
      },
      "required": [
        "line"
      ],
      "type": "object"
    }
  },
  {
    "description": "Execute Stata estimation / post-estimation commands on a DataFrame.",
    "name": "stata",
    "parameters": {
      "properties": {
        "commands": {
          "description": "One Stata command, or several separated by newlines or ``;``. ``*`` and ``//`` comments are ignored. Post-estimation commands (``margins``, ``test``, ``lincom`` ...) apply to the most recent estimation result.",
          "type": "string"
        },
        "data": {
          "description": "The dataset estimation commands run on. Required unless every command is a post-estimation command applied to ``result``.",
          "type": "string"
        },
        "result": {
          "description": "The estimation result that leading post-estimation commands apply to.",
          "type": "string"
        }
      },
      "required": [
        "commands"
      ],
      "type": "object"
    }
  },
  {
    "description": "Translate one R / fixest / felm call to a StatsPAI tool-call payload.",
    "name": "from_r",
    "parameters": {
      "properties": {
        "line": {
          "description": "Single R expression of the form ``fn(...)``. Multi-line scripts must be split by the caller.",
          "type": "string"
        }
      },
      "required": [
        "line"
      ],
      "type": "object"
    }
  },
  {
    "description": "Coverage matrix for the ``sp.from_stata`` / ``sp.from_r`` translators.",
    "name": "translation_coverage",
    "parameters": {
      "properties": {
        "fmt": {
          "default": "dict",
          "description": "``\"dict\"`` returns a structured mapping (agent / JSON friendly); ``\"markdown\"`` returns a ready-to-publish table string.",
          "enum": [
            "dict",
            "markdown"
          ],
          "type": "string"
        }
      },
      "required": [],
      "type": "object"
    }
  },
  {
    "description": "Attach a human-readable label to a variable.",
    "name": "label_var",
    "parameters": {
      "properties": {
        "df": {
          "description": "DataFrame to label (modified in place).",
          "type": "string"
        },
        "label": {
          "description": "Human-readable label.",
          "type": "string"
        },
        "var": {
          "description": "Column name.",
          "type": "string"
        }
      },
      "required": [
        "df",
        "var",
        "label"
      ],
      "type": "object"
    }
  },
  {
    "description": "Attach labels to multiple variables at once.",
    "name": "label_vars",
    "parameters": {
      "properties": {
        "df": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "labels": {
          "description": "{column_name: label_string} mapping.",
          "type": "object"
        }
      },
      "required": [
        "df",
        "labels"
      ],
      "type": "object"
    }
  },
  {
    "description": "Get the label for a variable, falling back to the column name.",
    "name": "get_label",
    "parameters": {
      "properties": {
        "df": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "var": {
          "description": "var parameter (str).",
          "type": "string"
        }
      },
      "required": [
        "df",
        "var"
      ],
      "type": "object"
    }
  },
  {
    "description": "Get all variable labels as a dictionary.",
    "name": "get_labels",
    "parameters": {
      "properties": {
        "df": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        }
      },
      "required": [
        "df"
      ],
      "type": "object"
    }
  },
  {
    "description": "Stata-style ``describe`` -- variable names, types, labels, and",
    "name": "describe",
    "parameters": {
      "properties": {
        "columns": {
          "description": "Subset of columns. Default: all.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "df": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        }
      },
      "required": [
        "df"
      ],
      "type": "object"
    }
  },
  {
    "description": "Pairwise correlation matrix with significance stars.",
    "name": "pwcorr",
    "parameters": {
      "properties": {
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "decimals": {
          "default": 3,
          "description": "Decimal places.",
          "type": "integer"
        },
        "method": {
          "default": "pearson",
          "description": "``'pearson'``, ``'spearman'``, or ``'kendall'``.",
          "type": "string"
        },
        "output": {
          "default": "text",
          "description": "``'text'``, ``'dataframe'``, ``'latex'``, ``'html'``.",
          "type": "string"
        },
        "stars": {
          "default": true,
          "description": "Show significance stars (* p<0.1, ** p<0.05, *** p<0.01).",
          "type": "boolean"
        },
        "vars": {
          "description": "Variables to correlate. Default: all numeric columns.",
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "required": [
        "data"
      ],
      "type": "object"
    }
  },
  {
    "description": "Winsorize variables at specified percentiles.",
    "name": "winsor",
    "parameters": {
      "properties": {
        "cuts": {
          "default": [
            1,
            99
          ],
          "description": "Lower and upper percentile cutoffs.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "replace": {
          "default": false,
          "description": "If True, overwrite original columns. If False, create new columns with ``suffix``.",
          "type": "boolean"
        },
        "suffix": {
          "default": "_w",
          "description": "Suffix for new winsorized columns (when ``replace=False``).",
          "type": "string"
        },
        "vars": {
          "description": "Variables to winsorize. Default: all numeric columns.",
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "required": [
        "data"
      ],
      "type": "object"
    }
  },
  {
    "description": "Row-wise mean across ``vars``, ignoring NaN (Stata ``egen rowmean``).",
    "name": "rowmean",
    "parameters": {
      "properties": {
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "vars": {
          "description": "vars parameter (Sequence[str]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "required": [
        "data",
        "vars"
      ],
      "type": "object"
    }
  },
  {
    "description": "Row-wise sum across ``vars``, treating NaN as 0 (Stata ``egen rowtotal``).",
    "name": "rowtotal",
    "parameters": {
      "properties": {
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "vars": {
          "description": "vars parameter (Sequence[str]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "required": [
        "data",
        "vars"
      ],
      "type": "object"
    }
  },
  {
    "description": "Row-wise max across ``vars``, ignoring NaN.",
    "name": "rowmax",
    "parameters": {
      "properties": {
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "vars": {
          "description": "vars parameter (Sequence[str]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "required": [
        "data",
        "vars"
      ],
      "type": "object"
    }
  },
  {
    "description": "Row-wise min across ``vars``, ignoring NaN.",
    "name": "rowmin",
    "parameters": {
      "properties": {
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "vars": {
          "description": "vars parameter (Sequence[str]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "required": [
        "data",
        "vars"
      ],
      "type": "object"
    }
  },
  {
    "description": "Row-wise standard deviation across ``vars`` (Stata ``egen rowsd``).",
    "name": "rowsd",
    "parameters": {
      "properties": {
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "vars": {
          "description": "vars parameter (Sequence[str]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "required": [
        "data",
        "vars"
      ],
      "type": "object"
    }
  },
  {
    "description": "Row-wise count of non-missing values (Stata ``egen rownonmiss``).",
    "name": "rowcount",
    "parameters": {
      "properties": {
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "vars": {
          "description": "vars parameter (Sequence[str]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "required": [
        "data",
        "vars"
      ],
      "type": "object"
    }
  },
  {
    "description": "Rank a variable, optionally within groups.",
    "name": "rank",
    "parameters": {
      "properties": {
        "ascending": {
          "default": true,
          "description": "ascending parameter (bool).",
          "type": "boolean"
        },
        "by": {
          "description": "Group variables. If given, ranks are computed within each group.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "method": {
          "default": "average",
          "description": "Estimator or algorithm variant to use.",
          "enum": [
            "average",
            "min",
            "max",
            "dense",
            "first"
          ],
          "type": "string"
        },
        "var": {
          "description": "Column to rank.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "var"
      ],
      "type": "object"
    }
  },
  {
    "description": "Flag outlier observations by percentile cutoffs.",
    "name": "outlier_indicator",
    "parameters": {
      "properties": {
        "combined": {
          "default": false,
          "description": "If True, also add a column ``_outlier_any`` marking rows that are outliers on *any* variable.",
          "type": "boolean"
        },
        "cuts": {
          "default": [
            1,
            99
          ],
          "description": "Lower and upper percentile cutoffs.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "vars": {
          "description": "Columns to check.",
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "required": [
        "data",
        "vars"
      ],
      "type": "object"
    }
  },
  {
    "description": "Read data from any common format, preserving variable labels.",
    "name": "read_data",
    "parameters": {
      "properties": {
        "encoding": {
          "description": "Character encoding (for CSV).",
          "type": "string"
        },
        "path": {
          "description": "File path. Supported: .dta, .csv, .xlsx, .xls, .parquet, .sas7bdat, .sav, .feather, .json.",
          "type": "string"
        }
      },
      "required": [
        "path"
      ],
      "type": "object"
    }
  },
  {
    "description": "Project a vector of instruments onto a scalar first-stage index.",
    "name": "scalar_iv_projection",
    "parameters": {
      "properties": {
        "covariates": {
          "description": "Exogenous controls to include in the first stage.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "instruments": {
          "description": "Excluded-instrument column names. Must have length >= 1.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "new_col": {
          "description": "Name of the column for the scalar index. Defaults to ``f'{treat}_iv_hat'``.",
          "type": "string"
        },
        "return_column": {
          "default": false,
          "description": "If True, return just the fitted pandas Series. If False, return a copy of ``data`` with the new column appended.",
          "type": "boolean"
        },
        "treat": {
          "description": "Endogenous treatment variable name.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "treat",
        "instruments"
      ],
      "type": "object"
    }
  },
  {
    "description": "Bias-corrected LSDV (LSDVC) estimator for dynamic panels. Validation: certified parity evidence.",
    "name": "xtlsdvc",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level.",
          "type": "number"
        },
        "bias_order": {
          "default": 2,
          "description": "How many terms of the Bun-Kiviet expansion to subtract: ``O(1/T)``, ``O(1/NT)``, or ``O(1/NT^2)``. Bun & Kiviet (2003) find diminishing returns beyond the second.",
          "enum": [
            "1",
            "2",
            "3"
          ],
          "type": "integer"
        },
        "bootstrap": {
          "default": 0,
          "description": "Number of parametric-bootstrap replications for the standard errors. **The reported analytic standard errors are the LSDV ones and do not account for the bias correction**, exactly as in Stata's ``xtlsdvc``; set this to get honest ones. 0 skips the bootstrap and warns.",
          "type": "integer"
        },
        "data": {
          "description": "Panel in long format.",
          "type": "string"
        },
        "id": {
          "default": "id",
          "description": "Unit and period identifiers.",
          "type": "string"
        },
        "initial": {
          "default": "ab",
          "description": "Consistent estimator used to evaluate the bias expression: Arellano-Bond, Anderson-Hsiao, or Blundell-Bond system GMM. The correction is only as good as this input, and the three can disagree materially on a persistent series -- ``'bb'`` is the safer choice there, for the same reason system GMM is.",
          "enum": [
            "ab",
            "ah",
            "bb"
          ],
          "type": "string"
        },
        "seed": {
          "description": "Seed for the bootstrap.",
          "type": "integer"
        },
        "time": {
          "default": "time",
          "description": "Unit and period identifiers.",
          "type": "string"
        },
        "x": {
          "description": "Strictly exogenous regressors. Accepts the same lag-operator syntax",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Dependent variable. Its first lag is added automatically -- do **not** pass a hand-built lag in ``x``.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y"
      ],
      "type": "object"
    }
  },
  {
    "description": "Simultaneous quantile regression at multiple quantiles. Validation: certified parity evidence.",
    "name": "sqreg",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "quantiles": {
          "description": "quantiles parameter (Optional[List[float]]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "x": {
          "description": "Primary running variable, regressor, or feature input for this estimator.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "x"
      ],
      "type": "object"
    }
  },
  {
    "description": "Logit (logistic) regression via maximum likelihood. Validation: certified parity evidence.",
    "name": "logit",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals.",
          "type": "number"
        },
        "at_values": {
          "description": "Variable values for ``marginal_effects='at'``.",
          "type": "object"
        },
        "cluster": {
          "description": "Column name for clustered standard errors.",
          "type": "string"
        },
        "data": {
          "description": "Data containing the variables.",
          "type": "string"
        },
        "formula": {
          "description": "Formula like ``\"y ~ x1 + x2\"``.",
          "type": "string"
        },
        "marginal_effects": {
          "description": "``'average'`` (AME), ``'mean'`` (MEM), or ``'at'`` (MER).",
          "type": "string"
        },
        "maxiter": {
          "default": 100,
          "description": "Maximum Newton-Raphson iterations.",
          "type": "integer"
        },
        "odds_ratio": {
          "default": false,
          "description": "Report odds ratios instead of log-odds coefficients.",
          "type": "boolean"
        },
        "robust": {
          "default": "nonrobust",
          "description": "``'nonrobust'`` for MLE SE, ``'hc1'`` / ``'robust'`` for sandwich SE.",
          "type": "string"
        },
        "tol": {
          "default": 1e-08,
          "description": "Convergence tolerance on log-likelihood change.",
          "type": "number"
        },
        "weights": {
          "description": "Column name for frequency/analytic weights.",
          "type": "string"
        },
        "x": {
          "description": "Regressor names (alternative to formula).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Dependent variable name (alternative to formula).",
          "type": "string"
        }
      },
      "required": [],
      "type": "object"
    }
  },
  {
    "description": "Probit regression via maximum likelihood. Validation: certified parity evidence.",
    "name": "probit",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals.",
          "type": "number"
        },
        "at_values": {
          "description": "Variable values for ``marginal_effects='at'``.",
          "type": "object"
        },
        "cluster": {
          "description": "Column name for clustered standard errors.",
          "type": "string"
        },
        "data": {
          "description": "Data containing the variables.",
          "type": "string"
        },
        "formula": {
          "description": "Formula like ``\"y ~ x1 + x2\"``.",
          "type": "string"
        },
        "marginal_effects": {
          "description": "``'average'`` (AME), ``'mean'`` (MEM), or ``'at'`` (MER).",
          "type": "string"
        },
        "maxiter": {
          "default": 100,
          "description": "Maximum Newton-Raphson iterations.",
          "type": "integer"
        },
        "robust": {
          "default": "nonrobust",
          "description": "``'nonrobust'`` for MLE SE, ``'hc1'`` / ``'robust'`` for sandwich SE.",
          "type": "string"
        },
        "tol": {
          "default": 1e-08,
          "description": "Convergence tolerance on log-likelihood change.",
          "type": "number"
        },
        "weights": {
          "description": "Column name for frequency/analytic weights.",
          "type": "string"
        },
        "x": {
          "description": "Regressor names (alternative to formula).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Dependent variable name (alternative to formula).",
          "type": "string"
        }
      },
      "required": [],
      "type": "object"
    }
  },
  {
    "description": "Complementary log-log regression via maximum likelihood. Validation: certified parity evidence.",
    "name": "cloglog",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals.",
          "type": "number"
        },
        "at_values": {
          "description": "Variable values for ``marginal_effects='at'``.",
          "type": "object"
        },
        "cluster": {
          "description": "Column name for clustered standard errors.",
          "type": "string"
        },
        "data": {
          "description": "Data containing the variables.",
          "type": "string"
        },
        "formula": {
          "description": "Formula like ``\"y ~ x1 + x2\"``.",
          "type": "string"
        },
        "marginal_effects": {
          "description": "``'average'`` (AME), ``'mean'`` (MEM), or ``'at'`` (MER).",
          "type": "string"
        },
        "maxiter": {
          "default": 100,
          "description": "Maximum Newton-Raphson iterations.",
          "type": "integer"
        },
        "robust": {
          "default": "nonrobust",
          "description": "``'nonrobust'`` for MLE SE, ``'hc1'`` / ``'robust'`` for sandwich SE.",
          "type": "string"
        },
        "tol": {
          "default": 1e-08,
          "description": "Convergence tolerance on log-likelihood change.",
          "type": "number"
        },
        "weights": {
          "description": "Column name for frequency/analytic weights.",
          "type": "string"
        },
        "x": {
          "description": "Regressor names (alternative to formula).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Dependent variable name (alternative to formula).",
          "type": "string"
        }
      },
      "required": [],
      "type": "object"
    }
  },
  {
    "description": "Fit a Generalized Linear Model. Validation: certified parity evidence.",
    "name": "glm",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals.",
          "type": "number"
        },
        "cluster": {
          "description": "Variable name for clustered standard errors.",
          "type": "string"
        },
        "data": {
          "description": "Data frame containing the variables.",
          "type": "string"
        },
        "exposure": {
          "description": "Variable name for exposure (``log(exposure)`` is added as offset).",
          "type": "string"
        },
        "family": {
          "default": "gaussian",
          "description": "Distribution family. One of ``\"gaussian\"``, ``\"binomial\"``, ``\"poisson\"``, ``\"gamma\"``, ``\"inverse_gaussian\"``, ``\"negative_binomial\"``.",
          "type": "string"
        },
        "formula": {
          "description": "Model formula (e.g. ``\"y ~ x1 + x2\"``).",
          "type": "string"
        },
        "information": {
          "default": "observed",
          "description": "Information matrix used as the variance bread. ``'observed'`` is Stata's ``glm`` default ``vce(oim)`` (and the bread of its robust / cluster sandwiches); ``'expected'`` is the IRLS Fisher information that R's ``glm`` reports. They coincide for canonical links (logit binomial, log Poisson, identity Gaussian) and differ otherwise.",
          "enum": [
            "observed",
            "expected"
          ],
          "type": "string"
        },
        "link": {
          "description": "Link function. If ``None`` the canonical link for the chosen family is used. Options: ``\"identity\"``, ``\"log\"``, ``\"logit\"``, ``\"probit\"``, ``\"inverse\"``, ``\"cloglog\"``, ``\"power\"``, ``\"sqrt\"``.",
          "type": "string"
        },
        "maxiter": {
          "default": 100,
          "description": "Maximum number of IRLS iterations.",
          "type": "integer"
        },
        "offset": {
          "description": "Variable name for offset.",
          "type": "string"
        },
        "robust": {
          "default": "nonrobust",
          "description": "Standard-error type (``\"nonrobust\"``, ``\"hc0\"``-``\"hc3\"``, ``\"hac\"``).",
          "type": "string"
        },
        "tol": {
          "default": 1e-08,
          "description": "Convergence tolerance on the relative change in deviance.",
          "type": "number"
        },
        "weights": {
          "description": "Variable name for observation weights.",
          "type": "string"
        },
        "x": {
          "description": "Names of independent variables (alternative to formula).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Name of the dependent variable (alternative to formula).",
          "type": "string"
        }
      },
      "required": [],
      "type": "object"
    }
  },
  {
    "description": "Generalized Linear Model with IRLS estimation.",
    "name": "GLMRegression",
    "parameters": {
      "properties": {
        "X": {
          "description": "Design matrix (alternative to formula).",
          "type": "string"
        },
        "data": {
          "description": "Data frame containing the variables.",
          "type": "string"
        },
        "family": {
          "default": "gaussian",
          "description": "Distribution family.",
          "type": "string"
        },
        "formula": {
          "description": "Model formula (e.g. ``\"y ~ x1 + x2\"``).",
          "type": "string"
        },
        "link": {
          "description": "Link function (``None`` selects canonical link).",
          "type": "string"
        },
        "var_names": {
          "description": "Variable names when using ``y``/``X`` directly.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Response array (alternative to formula).",
          "type": "string"
        }
      },
      "required": [],
      "type": "object"
    }
  },
  {
    "description": "Generalized Linear Model estimator using IRLS",
    "name": "GLMEstimator",
    "parameters": {
      "properties": {},
      "required": [],
      "type": "object"
    }
  },
  {
    "description": "Poisson regression via MLE (IRLS). Validation: certified parity evidence.",
    "name": "poisson",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals.",
          "type": "number"
        },
        "cluster": {
          "description": "Variable name for clustered standard errors.",
          "type": "string"
        },
        "data": {
          "description": "Data containing all variables.",
          "type": "string"
        },
        "exposure": {
          "description": "Exposure variable (will be logged and used as offset).",
          "type": "string"
        },
        "formula": {
          "description": "Model formula, e.g. \"y ~ x1 + x2\".",
          "type": "string"
        },
        "irr": {
          "default": false,
          "description": "If True, report Incidence Rate Ratios (exp(beta)) instead of raw coefficients.",
          "type": "boolean"
        },
        "maxiter": {
          "default": 100,
          "description": "Maximum IRLS iterations.",
          "type": "integer"
        },
        "offset": {
          "description": "Offset variable (log of exposure already computed).",
          "type": "string"
        },
        "robust": {
          "default": "nonrobust",
          "description": "Standard error type: \"nonrobust\", \"robust\"/\"hc0\", \"hc1\".",
          "type": "string"
        },
        "tol": {
          "default": 1e-08,
          "description": "Convergence tolerance.",
          "type": "number"
        },
        "weights": {
          "description": "Frequency/analytic weight variable.",
          "type": "string"
        },
        "x": {
          "description": "Independent variable names (alternative to formula).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Dependent variable name (alternative to formula).",
          "type": "string"
        }
      },
      "required": [],
      "type": "object"
    }
  },
  {
    "description": "Pseudo-Poisson Maximum Likelihood with high-dimensional fixed effects. Validation: certified parity evidence. Do NOT use when: vce='conley' on more than ~20,000 rows -- dense O(n^2) memory (see cost); vce='conley' with high-dimensional fixed effects -- the conleyreg-matching construction is dummy-based and raises MethodIncompatibility past 1,000 dummy columns; use cluster= (CRV1) there. Cost: IRLS is linear in n. vce='conley' builds the FE-as-dummies design plus dense n x n great-circle distance and kernel matrices (glm_conley_vcov) -- ~0.8 GB at n=10,000 and ~80 GB at n=100,000 -- and it refuses designs with >= n or > 1,000 dummy columns. Use cluster= (CRV1) instead at that scale.",
    "name": "ppmlhdfe",
    "parameters": {
      "properties": {
        "absorb": {
          "description": "Fixed effects to absorb, e.g. ``\"origin + destination + year\"``. Overrides any FE specification in the formula.",
          "type": "string"
        },
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals.",
          "type": "number"
        },
        "cluster": {
          "description": "Variable name for clustered standard errors (recommended for gravity models, e.g. cluster on country-pair). A pair ``cluster=[\"a\", \"b\"]`` requests two-way clustering (Cameron-Gelbach-Miller 2011 inclusion-exclusion with the single ``G_min/(G_min-1)`` small-sample factor -- byte-identical to Stata ``ppmlhdfe ..., cluster(a b)``).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "conley_cutoff": {
          "description": "conley_cutoff parameter (Optional[float]).",
          "type": "number"
        },
        "conley_lat": {
          "description": "conley_lat parameter (Optional[str]).",
          "type": "string"
        },
        "conley_lon": {
          "description": "conley_lon parameter (Optional[str]).",
          "type": "string"
        },
        "data": {
          "description": "Data containing all variables.",
          "type": "string"
        },
        "formula": {
          "description": "Model formula. Fixed effects can be specified via ``|``: ``\"trade ~ dist + contig | origin + destination + year\"``",
          "type": "string"
        },
        "maxiter": {
          "default": 1000,
          "description": "Maximum IRLS iterations.",
          "type": "integer"
        },
        "robust": {
          "default": "robust",
          "description": "Default is robust SE (as in Stata's ppmlhdfe). Options: \"robust\"/\"hc1\" (sandwich with the ``ssc`` small-sample factor), \"hc0\" (sandwich, no factor), \"nonrobust\".",
          "type": "string"
        },
        "seed": {
          "description": "RNG seed for sampled (non-enumerated) wild draws.",
          "type": "integer"
        },
        "separation": {
          "default": true,
          "description": "If True, check for separation (perfect prediction of zeros) and warn. Observations causing separation are not dropped automatically.",
          "type": "boolean"
        },
        "ssc": {
          "default": "stata",
          "description": "Small-sample factor applied to the heteroskedasticity-robust sandwich. ``\"stata\"`` multiplies by ``N/(N-1)``, the Stata ``glm``/``ppmlhdfe vce(robust)`` convention (matches ``ppmlhdfe`` at machine precision). ``\"fixest\"`` multiplies by ``(N-1)/(N-K)`` with ``K`` counting the slopes plus the absorbed fixed-effect levels (minus one per additional fixed-effect dimension for collinearity), the ``fixest::fepois`` default ``ssc(adj = TRUE, fixef.K = \"full\")``. ``\"none\"`` applies no factor. The three differ by less than ``sqrt(N/(N-K))`` and are documented, not competing, conventions; point estimates are unaffected. Clustered variances keep the ``G/(G-1)`` factor.",
          "enum": [
            "stata",
            "fixest"
          ],
          "type": "string"
        },
        "tol": {
          "default": 1e-08,
          "description": "Convergence tolerance.",
          "type": "number"
        },
        "vce": {
          "description": "Canonical SE-menu keyword. ``\"robust\"``/``\"hc1\"``/``\"hc0\"`` alias the ``robust=`` parameter. ``\"wild\"`` (with ``cluster=``) runs the boottest-convention score wild cluster bootstrap on the FE-absorbed design -- exact at any FE dimensionality (the weighted-FWL reduction of the score numerator is exact) and byte-identical to ``sp.fepois(vce=\"wild\")`` on low-dimensional FE. ``\"CR2\"``/``\"CR3\"``/``\"jackknife\"`` (with ``cluster=``) compute the clubSandwich glm bias-reduced SEs on the FE-as-dummies design (guarded against high-dimensional FE).",
          "type": "string"
        },
        "weights": {
          "description": "Weight variable name.",
          "type": "string"
        },
        "wild_reps": {
          "default": 9999,
          "description": "Replications for ``vce=\"wild\"`` (enumerates the 2^G grid when ``2**G <= wild_reps``).",
          "type": "integer"
        },
        "wild_weight_type": {
          "default": "rademacher",
          "description": "Wild weight distribution.",
          "type": "string"
        },
        "x": {
          "description": "Independent variable names (alternative to formula).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Dependent variable name (alternative to formula).",
          "type": "string"
        }
      },
      "required": [],
      "type": "object"
    }
  },
  {
    "description": "Compute marginal effects from a fitted model. Validation: certified parity evidence.",
    "name": "margins",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level.",
          "type": "number"
        },
        "at": {
          "description": "Fix covariates at specific values for conditional margins. E.g., ``{'age': 30, 'female': 1}``.",
          "type": "object"
        },
        "data": {
          "description": "Data to compute margins on. Defaults to the estimation sample.",
          "type": "string"
        },
        "eps": {
          "default": 1e-05,
          "description": "Step size for numerical differentiation.",
          "type": "number"
        },
        "method": {
          "default": "ame",
          "description": "- 'ame': Average Marginal Effect (average dy/dx across all obs) - 'mem': Marginal Effect at the Mean (dy/dx at mean of X)",
          "type": "string"
        },
        "result": {
          "description": "Fitted model result (must have ``.params`` and associated data).",
          "type": "string"
        },
        "variables": {
          "description": "Variables to compute dy/dx for. Default: all regressors.",
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "required": [
        "result"
      ],
      "type": "object"
    }
  },
  {
    "description": "Marginal-effects result that pipes straight into ``sp.regtable``.",
    "name": "margins_table",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Forwarded verbatim to :func:`margins`.",
          "type": "number"
        },
        "at": {
          "description": "Forwarded verbatim to :func:`margins`.",
          "type": "object"
        },
        "data": {
          "description": "Forwarded verbatim to :func:`margins`.",
          "type": "string"
        },
        "eps": {
          "default": 1e-05,
          "description": "Forwarded verbatim to :func:`margins`.",
          "type": "number"
        },
        "method": {
          "default": "ame",
          "description": "Forwarded verbatim to :func:`margins`.",
          "type": "string"
        },
        "result": {
          "description": "Fitted model -- same input ``sp.margins`` accepts.",
          "type": "string"
        },
        "variables": {
          "description": "Forwarded verbatim to :func:`margins`.",
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "required": [
        "result"
      ],
      "type": "object"
    }
  },
  {
    "description": "Adapter that turns an event-study fit into a regtable input.",
    "name": "event_study_table",
    "parameters": {
      "properties": {
        "include_reference": {
          "default": false,
          "description": "Whether to render the reference period row (typically ``t=-1``) where the estimate is identically zero.",
          "type": "boolean"
        },
        "label_fmt": {
          "default": "t={t}",
          "description": "Format string for the row label of each event-time bin. The ``{t}`` placeholder receives the integer relative time.",
          "type": "string"
        },
        "regex": {
          "description": "Pattern with one capture group that matches the relative time in coefficient names. Required when the CausalResult fast path is not applicable. Examples: ``r\"^tau_(-?\\d+)$\"``, ``r\"^lag(\\d+)$\"``, ``r\"::(-?\\d+)$\"``.",
          "type": "string"
        },
        "result": {
          "description": "Fitted model. The CausalResult fast path is used automatically when ``model_info['event_study']`` is present.",
          "type": "string"
        }
      },
      "required": [
        "result"
      ],
      "type": "object"
    }
  },
  {
    "description": "Plot marginal effects with confidence intervals.",
    "name": "marginsplot",
    "parameters": {
      "properties": {
        "ax": {
          "description": "ax parameter.",
          "type": "string"
        },
        "color": {
          "default": "#2C3E50",
          "description": "color parameter (str).",
          "type": "string"
        },
        "figsize": {
          "default": [
            8,
            5
          ],
          "description": "figsize parameter (Tuple[float, float]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "margins_df": {
          "description": "Output from ``margins()``.",
          "type": "string"
        },
        "title": {
          "description": "title parameter (Optional[str]).",
          "type": "string"
        }
      },
      "required": [
        "margins_df"
      ],
      "type": "object"
    }
  },
  {
    "description": "Compute predictive margins at specific covariate values. Validation: certified parity evidence.",
    "name": "margins_at",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals.",
          "type": "number"
        },
        "at": {
          "description": "Mapping of variable names to lists/arrays of values. If multiple variables are given, the Cartesian product of all value lists is used. Example:: at={\"experience\": [1, 5, 10], \"female\": [0, 1]} produces 6 grid points.",
          "type": "object"
        },
        "data": {
          "description": "Data to compute margins on.",
          "type": "string"
        },
        "result": {
          "description": "Fitted model result (must have ``.params`` and associated data).",
          "type": "string"
        }
      },
      "required": [
        "result",
        "data",
        "at"
      ],
      "type": "object"
    }
  },
  {
    "description": "Plot predictive margins from ``margins_at()`` with confidence bands.",
    "name": "margins_at_plot",
    "parameters": {
      "properties": {
        "ax": {
          "description": "ax parameter.",
          "type": "string"
        },
        "by": {
          "description": "Variable to produce separate lines for (legend grouping).",
          "type": "string"
        },
        "figsize": {
          "default": [
            8,
            5
          ],
          "description": "figsize parameter (Tuple[float, float]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "margins_at_df": {
          "description": "Output from ``margins_at()``.",
          "type": "string"
        },
        "palette": {
          "description": "Colours for each ``by`` group.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "title": {
          "description": "title parameter (Optional[str]).",
          "type": "string"
        },
        "x": {
          "description": "Variable to place on the x-axis. If *None*, inferred as the at-variable with the most unique values.",
          "type": "string"
        },
        "xlabel": {
          "description": "xlabel parameter (Optional[str]).",
          "type": "string"
        },
        "ylabel": {
          "default": "Predicted Value",
          "description": "ylabel parameter (str).",
          "type": "string"
        }
      },
      "required": [
        "margins_at_df"
      ],
      "type": "object"
    }
  },
  {
    "description": "Compute contrasts of predictive margins across levels of a variable. Validation: certified parity evidence.",
    "name": "contrast",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level.",
          "type": "number"
        },
        "data": {
          "description": "Estimation data.",
          "type": "string"
        },
        "method": {
          "default": "r",
          "description": "Contrast type: - ``'r'`` (reference): each level vs *reference* level. - ``'ar'`` (adjacent): each level vs the previous level. - ``'gw'`` (grand-mean weighted): each level vs the weighted grand mean of all levels.",
          "type": "string"
        },
        "reference": {
          "description": "Reference level when ``method='r'``. Defaults to the smallest observed level.",
          "type": "string"
        },
        "result": {
          "description": "Fitted model result.",
          "type": "string"
        },
        "variable": {
          "description": "Categorical variable whose levels are contrasted.",
          "type": "string"
        }
      },
      "required": [
        "result",
        "data",
        "variable"
      ],
      "type": "object"
    }
  },
  {
    "description": "Pairwise comparisons of predictive margins across all levels. Validation: certified parity evidence.",
    "name": "pwcompare",
    "parameters": {
      "properties": {
        "adjust": {
          "default": "none",
          "description": "P-value adjustment method: - ``'none'``: unadjusted. - ``'bonferroni'``: Bonferroni correction. - ``'sidak'``: Sidak correction. - ``'holm'``: Holm step-down procedure.",
          "type": "string"
        },
        "alpha": {
          "default": 0.05,
          "description": "Significance level for (adjusted) confidence intervals.",
          "type": "number"
        },
        "data": {
          "description": "Estimation data.",
          "type": "string"
        },
        "result": {
          "description": "Fitted model result.",
          "type": "string"
        },
        "variable": {
          "description": "Categorical variable whose levels are compared pairwise.",
          "type": "string"
        }
      },
      "required": [
        "result",
        "data",
        "variable"
      ],
      "type": "object"
    }
  },
  {
    "description": "Wald test for linear restrictions on coefficients. Validation: certified parity evidence.",
    "name": "test",
    "parameters": {
      "properties": {
        "hypothesis": {
          "description": "Hypothesis specification (Stata ``test`` syntax). Examples: - ``\"x1 = 0\"`` -- beta_x1 = 0 - ``\"x1 = x2\"`` -- beta_x1 = beta_x2 - ``\"x1 = x2 = 0\"`` -- joint: beta_x1 = beta_x2 and beta_x2 = 0 - ``\"x1 x2\"`` -- joint: beta_x1 = 0 and beta_x2 = 0 - ``\"(x1 = 0) (x2 + x3 = 1)\"`` -- grouped restrictions - ``\"x1 - 2*x2 = 0\"`` -- linear restriction; ``_cons`` names the intercept whatever the estimator calls it.",
          "type": "string"
        },
        "result": {
          "description": "Fitted model with ``.params`` and ``.std_errors``; restrictions that involve several coefficients also need its covariance matrix.",
          "type": "string"
        }
      },
      "required": [
        "result",
        "hypothesis"
      ],
      "type": "object"
    }
  },
  {
    "description": "Estimate a linear combination of coefficients with inference. Validation: certified parity evidence.",
    "name": "lincom",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level.",
          "type": "number"
        },
        "expression": {
          "description": "Linear combination (Stata ``lincom`` syntax). Examples: - ``\"x1 + x2\"`` -- beta_x1 + beta_x2 - ``\"x1 - x2\"`` -- beta_x1 - beta_x2 - ``\"2*x1 + 3*x2\"`` -- 2*beta_x1 + 3*beta_x2 - ``\"_cons + x1\"`` -- intercept plus slope",
          "type": "string"
        },
        "result": {
          "description": "Fitted model.",
          "type": "string"
        }
      },
      "required": [
        "result",
        "expression"
      ],
      "type": "object"
    }
  },
  {
    "description": "Return the post-estimation actions supported by *result*.",
    "name": "postestimation_contract",
    "parameters": {
      "properties": {
        "data": {
          "description": "Analysis frame available for data-dependent actions. When supplied, the contract marks ``margins``/``predict`` paths as ready rather than merely method-available.",
          "type": "string"
        },
        "include_diagnostics": {
          "default": true,
          "description": "Include scalar diagnostics from ``model_info`` / ``diagnostics``.",
          "type": "boolean"
        },
        "result": {
          "description": "Fitted StatsPAI result, estimator, or compatible object.",
          "type": "string"
        }
      },
      "required": [
        "result"
      ],
      "type": "object"
    }
  },
  {
    "description": "Return the post-estimation actions supported by *result*.",
    "name": "postestimation_report",
    "parameters": {
      "properties": {
        "data": {
          "description": "Analysis frame available for data-dependent actions. When supplied, the contract marks ``margins``/``predict`` paths as ready rather than merely method-available.",
          "type": "string"
        },
        "include_diagnostics": {
          "default": true,
          "description": "Include scalar diagnostics from ``model_info`` / ``diagnostics``.",
          "type": "boolean"
        },
        "result": {
          "description": "Fitted StatsPAI result, estimator, or compatible object.",
          "type": "string"
        }
      },
      "required": [
        "result"
      ],
      "type": "object"
    }
  },
  {
    "description": "Causal Mediation Analysis estimator.",
    "name": "MediationAnalysis",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "covariates": {
          "description": "Covariate matrix, DataFrame, or column names.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "inference": {
          "default": "bootstrap",
          "description": "inference parameter (str).",
          "type": "string"
        },
        "mediator": {
          "description": "mediator parameter (str).",
          "type": "string"
        },
        "n_boot": {
          "default": 1000,
          "description": "Number of bootstrap replications.",
          "type": "integer"
        },
        "pvalue_method": {
          "default": "bootstrap_sign",
          "description": "pvalue_method parameter (str).",
          "type": "string"
        },
        "seed": {
          "default": 42,
          "description": "Random seed for reproducible stochastic steps.",
          "type": "integer"
        },
        "treat": {
          "description": "Treatment indicator or first-treatment-period column.",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "treat",
        "mediator"
      ],
      "type": "object"
    }
  },
  {
    "description": "Bartik Shift-Share IV estimator.",
    "name": "BartikIV",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "covariates": {
          "description": "Covariate matrix, DataFrame, or column names.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "endog": {
          "description": "endog parameter (str).",
          "type": "string"
        },
        "leave_one_out": {
          "default": true,
          "description": "leave_one_out parameter (bool).",
          "type": "boolean"
        },
        "regional_shocks": {
          "description": "regional_shocks parameter (Optional[pandas.DataFrame]).",
          "type": "string"
        },
        "robust": {
          "default": "hc1",
          "description": "Robust standard-error or covariance estimator option.",
          "type": "string"
        },
        "shares": {
          "description": "shares parameter (DataFrame).",
          "type": "string"
        },
        "shocks": {
          "description": "shocks parameter (Series).",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "endog",
        "shares",
        "shocks"
      ],
      "type": "object"
    }
  },
  {
    "description": "Shift-share IV with AKM (2019) inference and the BHJ shock-level view. Validation: certified parity evidence.",
    "name": "ssaggregate",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Level of the AKM / AKM0 confidence intervals in the diagnostics.",
          "type": "number"
        },
        "cluster": {
          "description": "Location-level cluster variable (e.g. state) for the ``SE (Reg. cluster)`` diagnostic. The reported SE is AKM.",
          "type": "string"
        },
        "controls": {
          "description": "Location-level controls (an intercept is always added).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "data": {
          "description": "Observation-level data (n rows).",
          "type": "string"
        },
        "shares": {
          "description": "Exposure-share matrix. ``shares[i, k]`` is unit *i*'s exposure to shock *k*; rows need not sum to one.",
          "type": "string"
        },
        "shock_data": {
          "description": "Shock-level DataFrame (K rows) when *shocks* is a column name.",
          "type": "string"
        },
        "shocks": {
          "description": "Shock vector, or a column name in *shock_data*.",
          "type": "string"
        },
        "x": {
          "description": "Endogenous regressor (IV mode, *shocks* given) or the shift-share variable itself (OLS mode, *shocks* omitted).",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable name.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "x",
        "shares"
      ],
      "type": "object"
    }
  },
  {
    "description": "Replace the SE of a shift-share regression with the AKM (2019) SE. Validation: certified parity evidence.",
    "name": "shift_share_se",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Level for the AKM / AKM0 confidence intervals.",
          "type": "number"
        },
        "iv_result": {
          "description": "Result of ``sp.bartik`` or ``sp.ssaggregate``. These record the outcome, endogenous regressor, shift-share instrument and controls the AKM formula needs; results from other estimators do not, and are rejected.",
          "type": "string"
        },
        "shares": {
          "description": "Exposure-share matrix (need not sum to one).",
          "type": "string"
        }
      },
      "required": [
        "iv_result",
        "shares"
      ],
      "type": "object"
    }
  },
  {
    "description": "Comprehensive regression diagnostics in one call.",
    "name": "diagnose",
    "parameters": {
      "properties": {
        "data": {
          "description": "Data used in the regression.",
          "type": "string"
        },
        "print_results": {
          "default": true,
          "description": "Print formatted output.",
          "type": "boolean"
        },
        "x": {
          "description": "Independent variable names (excluding constant).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Dependent variable name.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "x"
      ],
      "type": "object"
    }
  },
  {
    "description": "Breusch-Pagan test for heteroskedasticity. Validation: certified parity evidence.",
    "name": "het_test",
    "parameters": {
      "properties": {
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "x": {
          "description": "Primary running variable, regressor, or feature input for this estimator.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "x"
      ],
      "type": "object"
    }
  },
  {
    "description": "Ramsey RESET test for functional form misspecification. Validation: certified parity evidence.",
    "name": "reset_test",
    "parameters": {
      "properties": {
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "powers": {
          "default": 3,
          "description": "Include y2, y3, ..., y^powers in the auxiliary regression.",
          "type": "integer"
        },
        "x": {
          "description": "Primary running variable, regressor, or feature input for this estimator.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "x"
      ],
      "type": "object"
    }
  },
  {
    "description": "Variance Inflation Factors for multicollinearity detection. Validation: certified parity evidence.",
    "name": "vif",
    "parameters": {
      "properties": {
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "x": {
          "description": "Independent variables.",
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "required": [
        "data",
        "x"
      ],
      "type": "object"
    }
  },
  {
    "description": "CJM (2020) density discontinuity test for RD manipulation. Validation: certified evidence with scoped limitations. Known limitations: Certified native reference-parity evidence covers the default rddensity::rddensity unrestricted triangular-kernel selector and test path on the JSS Lee/RD Senate fixture. Manual side-specific bandwidths follow an explicit user-control convention, not a reference-parity guarantee; backend='r' remains available when direct R package execution is required.",
    "name": "rddensity",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "backend": {
          "default": "native",
          "description": "``\"native\"`` uses StatsPAI's Python port of the default ``rddensity`` unrestricted triangular-kernel selector/test path. ``\"r\"`` delegates to ``rddensity::rddensity`` through ``Rscript`` when the R package is installed, matching the reference package's selector and test statistic.",
          "enum": [
            "native",
            "r"
          ],
          "type": "string"
        },
        "c": {
          "default": 0,
          "description": "RD cutoff.",
          "type": "number"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "h": {
          "description": "Bandwidth. A scalar applies the same bandwidth on both sides; a length-2 sequence is interpreted as ``(h_left, h_right)``.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "p": {
          "default": 2,
          "description": "Polynomial order for density estimation.",
          "type": "integer"
        },
        "x": {
          "description": "Running variable.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "x"
      ],
      "type": "object"
    }
  },
  {
    "description": "Hausman test for FE vs RE in panel data. Validation: validated evidence tier (known-truth, reference, external-parity, or Monte Carlo artifact).",
    "name": "hausman_test",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "data": {
          "description": "Panel data in long format.",
          "type": "string"
        },
        "id": {
          "description": "Unit identifier.",
          "type": "string"
        },
        "time": {
          "description": "Time period identifier.",
          "type": "string"
        },
        "x": {
          "description": "Independent variables (time-varying).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Dependent variable.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "x",
        "id",
        "time"
      ],
      "type": "object"
    }
  },
  {
    "description": "Anderson-Rubin (1949) test -- size-correct under weak instruments. Validation: certified parity evidence.",
    "name": "anderson_rubin_test",
    "parameters": {
      "properties": {
        "absorb": {
          "description": "High-dimensional fixed effects to partial out of ``y``, the endogenous regressor, the instruments and the controls before the test -- the same residualisation ``sp.iv(absorb=...)`` performs.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "alpha": {
          "default": 0.05,
          "description": "Significance level.",
          "type": "number"
        },
        "cluster": {
          "description": "Cluster the AR statistic. Switches from the homoskedastic F-form to the (multiway) cluster-robust quadratic form ``(Z'e)' Omega^-1 (Z'e)`` with ``Omega`` the cluster-sum variance of the moment vector, referred to ``F(k_z, G-1)``. Without this, a panel AR test over-rejects exactly as a homoskedastic t-test",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "endog": {
          "description": "Endogenous regressor (single).",
          "type": "string"
        },
        "exog": {
          "description": "Included exogenous controls.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "h0": {
          "default": 0,
          "description": "Null hypothesis value for the endogenous coefficient.",
          "type": "number"
        },
        "instruments": {
          "description": "Excluded instruments.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "vcov": {
          "default": "HC1",
          "description": "Variance estimator used for the Olea-Pflueger effective F reported alongside AR.",
          "enum": [
            "HC0",
            "HC1",
            "classic"
          ],
          "type": "string"
        },
        "y": {
          "description": "Outcome variable.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "endog",
        "instruments"
      ],
      "type": "object"
    }
  },
  {
    "description": "Olea-Pflueger (2013) robust effective F statistic for weak instruments. Validation: certified parity evidence.",
    "name": "effective_f_test",
    "parameters": {
      "properties": {
        "absorb": {
          "description": "High-dimensional fixed effects to partial out of the endogenous regressor, the instruments and the controls before the first stage -- the same residualisation ``sp.iv(absorb=...)`` performs, so the effective F describes the specification actually fitted. The absorbed degrees of freedom are charged to ``df_resid``.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "cluster": {
          "description": "Cluster the first-stage moment variance. ``Omega`` becomes the (multiway, Cameron-Gelbach-Miller) cluster-sum meat with the ``ivreg2`` finite-sample factor ``G_min/(G_min-1) * (n-1)/(n-K)``. This is the right diagnostic whenever the second stage is",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "endog": {
          "description": "Endogenous regressor (single endogenous variable).",
          "type": "string"
        },
        "exog": {
          "description": "Included exogenous controls (a constant is added automatically).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "instruments": {
          "description": "Excluded instruments.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "vcov": {
          "default": "HC1",
          "description": "Variance estimator for the first-stage residuals: - ``'classic'`` -- homoskedastic; F_eff equals first-stage F. - ``'HC0'`` -- White heteroskedasticity-robust. - ``'HC1'`` -- HC0 with small-sample correction ``n/(n-k)``. Ignored when ``cluster`` is given.",
          "enum": [
            "HC0",
            "HC1",
            "classic"
          ],
          "type": "string"
        }
      },
      "required": [
        "data",
        "endog",
        "instruments"
      ],
      "type": "object"
    }
  },
  {
    "description": "Lee-McCrary-Moreira-Porter (2022, AER) tF adjusted critical value. Validation: certified parity evidence.",
    "name": "tF_critical_value",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level. Only ``0.05`` is implemented (the level LMMP tabulate).",
          "type": "number"
        },
        "first_stage_F": {
          "description": "First-stage F statistic of the single excluded instrument, computed with the *same* variance estimator as the t-ratio it adjusts (the robust / cluster Wald F -- equal to the Olea-Pflueger effective F with one instrument). This is what ``ivDiag`` passes.",
          "type": "number"
        }
      },
      "required": [
        "first_stage_F"
      ],
      "type": "object"
    }
  },
  {
    "description": "Stata-style unified weak-instrument-robust diagnostic panel. Validation: certified parity evidence.",
    "name": "weakrobust",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for the robust confidence sets.",
          "type": "number"
        },
        "clr_method": {
          "default": "exact",
          "description": "How the CLR conditional null distribution is evaluated. ``'exact'`` integrates it numerically (the closed form R ``ivmodel::CLR`` and Stata ``weakiv`` use), making the CLR p-value and set deterministic. ``'simulate'`` is the pre-1.29 Monte-Carlo version.",
          "enum": [
            "exact",
            "simulate"
          ],
          "type": "string"
        },
        "clr_simulations": {
          "default": 20000,
          "description": "Monte-Carlo draws for the CLR null distribution; used only with ``clr_method='simulate'``.",
          "type": "integer"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "endog": {
          "description": "Outcome and single endogenous regressor (column names in ``data``).",
          "type": "string"
        },
        "exog": {
          "description": "Included exogenous controls. An intercept is always added.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "grid_size": {
          "default": 401,
          "description": "Grid resolution used by AR/CLR/K confidence-set inversion.",
          "type": "integer"
        },
        "h0": {
          "default": 0.0,
          "description": "Null value for the endogenous coefficient. All under-H0 tests (AR, CLR, K) are evaluated at ``beta = h0``.",
          "type": "number"
        },
        "include_clr": {
          "default": true,
          "description": "Also run the CLR test and invert it for a CLR confidence set.",
          "type": "boolean"
        },
        "include_k": {
          "default": true,
          "description": "Also run the Kleibergen K score test and K confidence set.",
          "type": "boolean"
        },
        "instruments": {
          "description": "Excluded instruments.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "random_state": {
          "description": "Random seed or RandomState for reproducible stochastic steps.",
          "type": "integer"
        },
        "vcov": {
          "default": "HC1",
          "description": "Used by the Olea-Pflueger effective F.",
          "enum": [
            "HC0",
            "HC1",
            "classic"
          ],
          "type": "string"
        },
        "y": {
          "description": "Outcome and single endogenous regressor (column names in ``data``).",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "endog",
        "instruments"
      ],
      "type": "object"
    }
  },
  {
    "description": "Container holding the unified weak-IV-robust panel.",
    "name": "WeakRobustResult",
    "parameters": {
      "properties": {
        "alpha": {
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "object"
        },
        "endog": {
          "description": "endog parameter (str).",
          "type": "string"
        },
        "h0": {
          "description": "h0 parameter (float).",
          "type": "number"
        },
        "instruments": {
          "description": "instruments parameter (List[str]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "n": {
          "description": "n parameter (int).",
          "type": "integer"
        }
      },
      "required": [
        "data",
        "endog",
        "instruments",
        "n",
        "h0",
        "alpha"
      ],
      "type": "object"
    }
  },
  {
    "description": "Compute the E-value for sensitivity to unmeasured confounding. Validation: certified parity evidence.",
    "name": "evalue",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level used to build a CI from ``se`` when ``ci`` is not supplied (ratio measures).",
          "type": "number"
        },
        "ci": {
          "description": "Confidence interval on the ``measure`` scale. Takes precedence over ``se`` for ratio measures.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "delta": {
          "default": 1.0,
          "description": "Contrast size for ``OLS`` (E-value for a ``delta``-unit change in the exposure).",
          "type": "number"
        },
        "estimate": {
          "description": "Point estimate on the scale given by ``measure``: - ``'RR'`` risk ratio, ``'OR'`` odds ratio, ``'HR'`` hazard ratio (all must be > 0); - ``'MD'`` / ``'SMD'`` standardised mean difference; - ``'OLS'`` raw linear-regression coefficient (supply ``sd``); - ``'DIFF'`` / ``'RD'`` risk difference (approximate scalar path;",
          "type": "number"
        },
        "measure": {
          "default": "RR",
          "description": "One of ``'RR'``, ``'OR'``, ``'HR'``, ``'MD'``, ``'SMD'``, ``'OLS'``, ``'DIFF'``, ``'RD'``.",
          "type": "string"
        },
        "rare": {
          "description": "For ``OR`` / ``HR`` only: whether the rare-outcome approximation applies. ``rare=True`` treats OR/HR ~ RR; ``rare=False`` (the default) uses the exact common-outcome conversion to the RR scale (``sqrt(OR)`` for OR; the Ding-VanderWeele formula for HR).",
          "type": "boolean"
        },
        "rare_outcome": {
          "description": "Deprecated alias for ``rare`` (kept for backwards compatibility).",
          "type": "boolean"
        },
        "sd": {
          "description": "Outcome standard deviation, required for ``measure='OLS'`` to standardise the coefficient.",
          "type": "number"
        },
        "se": {
          "description": "Standard error of ``estimate``. Required for ``MD`` / ``OLS`` to obtain a confidence-interval E-value; for ratio measures it is used (with ``alpha``) to build a CI when ``ci`` is not given.",
          "type": "number"
        },
        "true": {
          "description": "Reference value the confounding would have to move the estimate to. Defaults to the null: 1 for ratio measures, 0 for difference measures. A non-null ``true`` gives a \"non-null\" E-value.",
          "type": "number"
        }
      },
      "required": [
        "estimate"
      ],
      "type": "object"
    }
  },
  {
    "description": "Compute an E-value from a StatsPAI CausalResult object. Validation: certified parity evidence.",
    "name": "evalue_from_result",
    "parameters": {
      "properties": {
        "measure": {
          "default": "SMD",
          "description": "How to interpret ``result.estimate`` (for ATE/ATT on continuous outcomes ``'SMD'`` is appropriate; pass ``'RR'`` / ``'OR'`` / ``'HR'`` for ratio estimates). Passed through to :func:`evalue`.",
          "type": "string"
        },
        "rare": {
          "description": "rare parameter (Optional[bool]).",
          "type": "boolean"
        },
        "rare_outcome": {
          "description": "rare_outcome parameter (Optional[bool]).",
          "type": "boolean"
        },
        "result": {
          "description": "Result from any StatsPAI causal estimator exposing a scalar ``estimate`` (and ideally ``se`` / ``ci``).",
          "type": "string"
        },
        "true": {
          "description": "true parameter (Optional[float]).",
          "type": "number"
        }
      },
      "required": [
        "result"
      ],
      "type": "object"
    }
  },
  {
    "description": "Exact E-value for a risk difference from a 2x2 table. Validation: certified parity evidence.",
    "name": "evalue_rd",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for the confidence-limit E-value.",
          "type": "number"
        },
        "grid": {
          "default": 0.0001,
          "description": "Step size of the bias-factor grid search for the CI E-value.",
          "type": "number"
        },
        "n00": {
          "description": "Unexposed cases and unexposed non-cases.",
          "type": "number"
        },
        "n01": {
          "description": "Unexposed cases and unexposed non-cases.",
          "type": "number"
        },
        "n10": {
          "description": "Exposed cases and exposed non-cases.",
          "type": "number"
        },
        "n11": {
          "description": "Exposed cases and exposed non-cases.",
          "type": "number"
        },
        "true": {
          "default": 0.0,
          "description": "Reference risk difference (must be <= the observed RD).",
          "type": "number"
        }
      },
      "required": [
        "n11",
        "n10",
        "n01",
        "n00"
      ],
      "type": "object"
    }
  },
  {
    "description": "Confounding bias factor ``B`` (Ding & VanderWeele 2016). Validation: certified parity evidence.",
    "name": "bias_factor",
    "parameters": {
      "properties": {
        "rr_eu": {
          "description": "rr_eu parameter (float).",
          "type": "number"
        },
        "rr_ud": {
          "description": "rr_ud parameter (float).",
          "type": "number"
        }
      },
      "required": [
        "rr_eu",
        "rr_ud"
      ],
      "type": "object"
    }
  },
  {
    "description": "Unified post-estimation diagnostics dispatcher.",
    "name": "estat",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for interpretation strings.",
          "type": "number"
        },
        "lags": {
          "default": 1,
          "description": "Number of lags for the Breusch-Godfrey test.",
          "type": "integer"
        },
        "powers": {
          "default": 3,
          "description": "Highest power of y-hat for the RESET test.",
          "type": "integer"
        },
        "print_results": {
          "default": true,
          "description": "If True, print a formatted table to stdout.",
          "type": "boolean"
        },
        "result": {
          "description": "A fitted result object with ``params``, ``data_info``, etc.",
          "type": "string"
        },
        "test": {
          "default": "all",
          "description": "Name of the diagnostic test. One of ``'hettest'``, ``'white'``, ``'reset'``, ``'ovtest'``, ``'bgodfrey'``, ``'dwatson'``, ``'vif'``, ``'ic'``, ``'linktest'``, ``'normality'``, ``'leverage'``, ``'endogenous'``, ``'overid'``, ``'firststage'``, ``'all'``.",
          "type": "string"
        }
      },
      "required": [
        "result"
      ],
      "type": "object"
    }
  },
  {
    "description": "Kitagawa (2015) specification test for the validity of LATE.",
    "name": "kitagawa_test",
    "parameters": {
      "properties": {
        "data": {
          "description": "Input data.",
          "type": "string"
        },
        "instrument": {
          "description": "Binary instrument variable (Z).",
          "type": "string"
        },
        "n_boot": {
          "default": 1000,
          "description": "Number of bootstrap replications for the p-value.",
          "type": "integer"
        },
        "n_grid": {
          "default": 100,
          "description": "Number of grid points for evaluating the CDF conditions.",
          "type": "integer"
        },
        "seed": {
          "description": "Random seed for reproducibility.",
          "type": "integer"
        },
        "treatment": {
          "description": "Endogenous binary treatment variable (D).",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable name.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "treatment",
        "instrument"
      ],
      "type": "object"
    }
  },
  {
    "description": "Result of the Kitagawa (2015) LATE validity test.",
    "name": "KitagawaResult",
    "parameters": {
      "properties": {
        "first_stage": {
          "description": "first_stage parameter (float).",
          "type": "number"
        },
        "max_violation_treated": {
          "description": "max_violation_treated parameter (float).",
          "type": "number"
        },
        "max_violation_untreated": {
          "description": "max_violation_untreated parameter (float).",
          "type": "number"
        },
        "n_boot": {
          "description": "Number of bootstrap replications.",
          "type": "integer"
        },
        "n_obs": {
          "description": "Number of obs.",
          "type": "integer"
        },
        "p_value": {
          "description": "p_value parameter (float).",
          "type": "number"
        },
        "statistic": {
          "description": "statistic parameter (float).",
          "type": "number"
        }
      },
      "required": [
        "statistic",
        "p_value",
        "first_stage",
        "n_boot",
        "n_obs",
        "max_violation_treated",
        "max_violation_untreated"
      ],
      "type": "object"
    }
  },
  {
    "description": "Randomization inference p-value. Validation: certified parity evidence.",
    "name": "ri_test",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "cluster": {
          "description": "Cluster-level permutation (permute treatment at cluster level).",
          "type": "string"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "n_perms": {
          "default": 1000,
          "description": "Number of random permutations. Use 10000+ for publications. When the design admits no more than ``n_perms`` distinct assignments (``C(n, n_1)``, or ``C(G, G_1)`` under ``cluster``) every assignment is enumerated instead and the p-value is exact -- the rule R ``ri2`` / ``randomizr::obtain_permutation_matrix`` apply.",
          "type": "integer"
        },
        "seed": {
          "description": "Random seed.",
          "type": "integer"
        },
        "stat": {
          "default": "diff_means",
          "description": "Test statistic: - ``'diff_means'``: difference in means (Y_bar_1 - Y_bar_0) - ``'ks'``: Kolmogorov-Smirnov statistic - ``'t'``: t-statistic - A callable ``f(Y, D) -> float`` for custom statistics.",
          "type": "string"
        },
        "treat": {
          "description": "Binary treatment indicator (0/1).",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "treat"
      ],
      "type": "object"
    }
  },
  {
    "description": "Container for bootstrap inference results.",
    "name": "BootstrapResult",
    "parameters": {
      "properties": {
        "alpha": {
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "boot_distribution": {
          "description": "boot_distribution parameter (np.ndarray).",
          "type": "string"
        },
        "ci_lower": {
          "description": "ci_lower parameter (float).",
          "type": "number"
        },
        "ci_method": {
          "description": "ci_method parameter (str).",
          "type": "string"
        },
        "ci_upper": {
          "description": "ci_upper parameter (float).",
          "type": "number"
        },
        "estimate": {
          "description": "estimate parameter (float).",
          "type": "number"
        },
        "n_boot": {
          "description": "Number of bootstrap replications.",
          "type": "integer"
        },
        "pvalue": {
          "description": "pvalue parameter (float).",
          "type": "number"
        },
        "se": {
          "description": "se parameter (float).",
          "type": "number"
        }
      },
      "required": [
        "estimate",
        "se",
        "ci_lower",
        "ci_upper",
        "pvalue",
        "alpha",
        "n_boot",
        "boot_distribution",
        "ci_method"
      ],
      "type": "object"
    }
  },
  {
    "description": "Compute two-way cluster-robust standard errors. Validation: certified parity evidence.",
    "name": "twoway_cluster",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals.",
          "type": "number"
        },
        "cluster1": {
          "description": "Column name for the first clustering dimension.",
          "type": "string"
        },
        "cluster2": {
          "description": "Column name for the second clustering dimension.",
          "type": "string"
        },
        "data": {
          "description": "Original data containing the cluster variables.",
          "type": "string"
        },
        "result": {
          "description": "Fitted OLS result. Must have ``data_info`` containing ``'X'`` (design matrix), ``'y'`` (response), and ``'residuals'``.",
          "type": "string"
        }
      },
      "required": [
        "result",
        "data",
        "cluster1",
        "cluster2"
      ],
      "type": "object"
    }
  },
  {
    "description": "Compute Conley (1999) spatial -- and optionally spatio-temporal -- HAC Validation: certified parity evidence. Cost: Sparse and scale-safe: a scipy cKDTree ball query enumerates only observation pairs within dist_cutoff, so memory is O(n + pairs-within-cutoff) rather than O(n^2). This is the recommended Conley path on large samples -- unlike feols(vce='conley') / hdfe_ols(vce='conley'), which are dense. Cost still grows with dist_cutoff: a cutoff large enough to link most observations recovers the quadratic pair count.",
    "name": "conley",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals.",
          "type": "number"
        },
        "data": {
          "description": "Data with latitude and longitude columns (in **degrees**), row-aligned to the fitted sample.",
          "type": "string"
        },
        "dist_cutoff": {
          "description": "Distance cutoff *h* in kilometres. Pairs farther apart than this receive zero weight.",
          "type": "number"
        },
        "distance": {
          "default": "haversine",
          "description": "``\"haversine\"`` uses great-circle distance on a sphere of radius 6371 km, and is symmetric. ``\"planar\"`` reproduces ``acreg``'s convention exactly: 111 km per degree of latitude and ``cos(lat_ref) * 111`` km per degree of longitude, anchored at the *reference* point, which makes the distance asymmetric; the resulting covariance is symmetrised the same way ``acreg`` does (Mata ``_makesymmetric``, which mirrors the lower triangle). Choose ``\"planar\"`` for bit-level ``acreg`` parity, ``\"haversine\"`` for a proper great-circle metric.",
          "type": "string"
        },
        "kernel": {
          "default": "uniform",
          "description": "Spatial kernel: ``\"uniform\"`` (indicator) or ``\"bartlett\"`` (linearly declining weight ``1 - d/h``).",
          "type": "string"
        },
        "lag_cutoff": {
          "description": "Serial-correlation bandwidth in periods, for pairs within the same unit. Requires ``time``.",
          "type": "integer"
        },
        "lag_cutoff_cross": {
          "description": "Time bandwidth for pairs drawn from *different* units -- ``acreg``'s ``lagdist()``. Defaults to ``0`` (``acreg``'s default), i.e. contemporaneous cross-unit correlation only.",
          "type": "integer"
        },
        "lat": {
          "description": "Column name for latitude.",
          "type": "string"
        },
        "lon": {
          "description": "Column name for longitude.",
          "type": "string"
        },
        "result": {
          "description": "Fitted OLS result. Must have ``data_info`` containing ``'X'`` (design matrix), ``'y'`` (response), and ``'residuals'``.",
          "type": "string"
        },
        "time": {
          "description": "Column name holding the time period. Must be integer-valued. Requires ``lag_cutoff`` and ``unit``.",
          "type": "string"
        },
        "time_kernel": {
          "default": "bartlett",
          "description": "Time kernel: ``\"bartlett\"`` (``1 - |dt| / (lag_cutoff + 1)``, the Newey-West convention, matching ``acreg``'s ``hac bartlett``) or ``\"uniform\"`` (indicator, matching ``acreg`` without ``hac``).",
          "type": "string"
        },
        "unit": {
          "description": "Column name identifying the panel unit. Enables panel de-duplication: the spatial neighbour search runs on the distinct unit coordinates rather than on every row, so a unit observed T times costs one point in the KD-tree instead of T.",
          "type": "string"
        }
      },
      "required": [
        "result",
        "data",
        "lat",
        "lon",
        "dist_cutoff"
      ],
      "type": "object"
    }
  },
  {
    "description": "Estimate the Population Average Treatment Effect (PATE). Validation: validated evidence tier (known-truth, reference, external-parity, or Monte Carlo artifact).",
    "name": "pate",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for the confidence interval.",
          "type": "number"
        },
        "covariates": {
          "description": "Shared covariates present in both datasets.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "data_experiment": {
          "description": "Experimental/study sample. Must contain *y*, *treatment*, and all *covariates*.",
          "type": "string"
        },
        "data_target": {
          "description": "Target population sample. Must contain all *covariates*. Need not contain *y* or *treatment*.",
          "type": "string"
        },
        "method": {
          "default": "ipw",
          "description": "Estimation strategy: * ``'ipw'`` -- Inverse probability of sampling weights. * ``'aipw'`` -- Augmented IPW (doubly robust). * ``'calibration'`` -- Entropy balancing on covariate moments.",
          "enum": [
            "ipw",
            "aipw",
            "calibration"
          ],
          "type": "string"
        },
        "n_boot": {
          "default": 500,
          "description": "Number of bootstrap replications for standard-error estimation.",
          "type": "integer"
        },
        "seed": {
          "description": "Random seed for reproducibility.",
          "type": "integer"
        },
        "treatment": {
          "description": "Binary treatment indicator (only in data_experiment).",
          "type": "string"
        },
        "trim": {
          "default": 0.01,
          "description": "Trimming threshold for participation propensities (values below *trim* or above 1 - *trim* are clipped).",
          "type": "number"
        },
        "y": {
          "description": "Outcome variable (only used from data_experiment).",
          "type": "string"
        }
      },
      "required": [
        "data_experiment",
        "data_target",
        "y",
        "treatment",
        "covariates"
      ],
      "type": "object"
    }
  },
  {
    "description": "Population Average Treatment Effect estimator.",
    "name": "PATEEstimator",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "covariates": {
          "description": "Covariate matrix, DataFrame, or column names.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "data_experiment": {
          "description": "data_experiment parameter (DataFrame).",
          "type": "string"
        },
        "data_target": {
          "description": "data_target parameter (DataFrame).",
          "type": "string"
        },
        "method": {
          "default": "ipw",
          "description": "Estimator or algorithm variant to use.",
          "type": "string"
        },
        "n_boot": {
          "default": 500,
          "description": "Number of bootstrap replications.",
          "type": "integer"
        },
        "seed": {
          "description": "Random seed for reproducible stochastic steps.",
          "type": "integer"
        },
        "treatment": {
          "description": "Treatment indicator, treatment variable, or treatment array.",
          "type": "string"
        },
        "trim": {
          "default": 0.01,
          "description": "trim parameter (float).",
          "type": "number"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "data_experiment",
        "data_target",
        "y",
        "treatment",
        "covariates"
      ],
      "type": "object"
    }
  },
  {
    "description": "Fisher's exact randomization test with enhanced features. Validation: certified parity evidence.",
    "name": "fisher_exact",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for the Hodges-Lehmann confidence interval.",
          "type": "number"
        },
        "cluster": {
          "description": "Variable for cluster-level randomization (permute by cluster).",
          "type": "string"
        },
        "controls": {
          "description": "Control variables for covariate-adjusted inference. When provided, the test statistic is computed on residuals from regressing Y on controls.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "data": {
          "description": "Input data.",
          "type": "string"
        },
        "n_perm": {
          "default": 10000,
          "description": "Number of random permutations. When the design admits no more than ``n_perm`` distinct assignments (complete, cluster or stratified randomization) they are all enumerated instead, so the p-value is exact (the R ``ri2`` / ``randomizr`` rule), and ``n_perm`` on the result reports the number of assignments.",
          "type": "integer"
        },
        "seed": {
          "description": "Random seed for reproducibility.",
          "type": "integer"
        },
        "statistic": {
          "default": "ate",
          "description": "Test statistic to use: - ``'ate'``: Average treatment effect (difference in means). - ``'ks'``: Kolmogorov-Smirnov statistic. - ``'rank_sum'``: Wilcoxon rank-sum statistic.",
          "type": "string"
        },
        "stratify": {
          "description": "Variable for stratified permutation (permute within strata).",
          "type": "string"
        },
        "treatment": {
          "description": "Binary treatment variable name (0/1).",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable name.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "treatment"
      ],
      "type": "object"
    }
  },
  {
    "description": "Result container for Fisher's exact permutation test.",
    "name": "FisherResult",
    "parameters": {
      "properties": {
        "ci": {
          "description": "ci parameter (Tuple[float, float]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "n_obs": {
          "description": "Number of obs.",
          "type": "integer"
        },
        "n_perm": {
          "description": "Number of permutation replications.",
          "type": "integer"
        },
        "n_treated": {
          "description": "Number of treated.",
          "type": "integer"
        },
        "p_one_sided": {
          "description": "p_one_sided parameter (float).",
          "type": "number"
        },
        "p_value": {
          "description": "p_value parameter (float).",
          "type": "number"
        },
        "perm_dist": {
          "description": "perm_dist parameter (ndarray).",
          "type": "string"
        },
        "statistic": {
          "description": "statistic parameter (float).",
          "type": "number"
        },
        "statistic_type": {
          "description": "statistic_type parameter (str).",
          "type": "string"
        }
      },
      "required": [
        "statistic",
        "p_value",
        "p_one_sided",
        "ci",
        "perm_dist",
        "statistic_type",
        "n_perm",
        "n_obs",
        "n_treated"
      ],
      "type": "object"
    }
  },
  {
    "description": "Leave-one-cluster-out jackknife standard errors. Validation: certified parity evidence.",
    "name": "jackknife_se",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals.",
          "type": "number"
        },
        "cluster": {
          "description": "Name of the cluster variable in ``data``.",
          "type": "string"
        },
        "data": {
          "description": "The original data used for estimation.",
          "type": "string"
        },
        "result": {
          "description": "A fitted regression result from ``sp.regress()``.",
          "type": "string"
        }
      },
      "required": [
        "result",
        "data",
        "cluster"
      ],
      "type": "object"
    }
  },
  {
    "description": "CR2 bias-corrected cluster-robust standard errors (Bell & McCaffrey 2002). Validation: certified parity evidence.",
    "name": "cr2_se",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals.",
          "type": "number"
        },
        "cluster": {
          "description": "Name of the cluster variable in ``data``.",
          "type": "string"
        },
        "data": {
          "description": "The original data used for estimation.",
          "type": "string"
        },
        "result": {
          "description": "A fitted regression result from ``sp.regress()``.",
          "type": "string"
        }
      },
      "required": [
        "result",
        "data",
        "cluster"
      ],
      "type": "object"
    }
  },
  {
    "description": "Wild cluster bootstrap t-test for a single coefficient. Validation: certified parity evidence.",
    "name": "wild_cluster_boot",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence interval.",
          "type": "number"
        },
        "cluster": {
          "description": "Name of the cluster variable.",
          "type": "string"
        },
        "data": {
          "description": "The original data used for estimation.",
          "type": "string"
        },
        "n_boot": {
          "default": 999,
          "description": "Number of bootstrap replications (use odd number).",
          "type": "integer"
        },
        "result": {
          "description": "A fitted regression result from ``sp.regress()``.",
          "type": "string"
        },
        "seed": {
          "description": "Random seed for reproducibility.",
          "type": "integer"
        },
        "variable": {
          "description": "Name of the coefficient to test (H0: beta = 0).",
          "type": "string"
        },
        "weight_type": {
          "default": "rademacher",
          "description": "Bootstrap weight distribution: - ``'rademacher'``: +/-1 with equal probability. - ``'webb'``: Webb (2014) 6-point distribution for G < 12. - ``'mammen'``: Mammen (1993) 2-point distribution.",
          "type": "string"
        }
      },
      "required": [
        "result",
        "data",
        "cluster",
        "variable"
      ],
      "type": "object"
    }
  },
  {
    "description": "Summary-data meta-analysis with fixed- and random-effects pooling. Validation: certified parity evidence.",
    "name": "meta_analysis",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence/prediction intervals.",
          "type": "number"
        },
        "effects": {
          "description": "Per-study effect sizes (e.g. log odds ratios, mean differences).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "labels": {
          "description": "Study labels for the forest plot.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "method": {
          "default": "DL",
          "description": "Which model the headline ``estimate`` reports: DerSimonian-Laird random effects (default) or fixed-effect inverse-variance. Both are always computed and available on the result.",
          "enum": [
            "DL",
            "fixed"
          ],
          "type": "string"
        },
        "se": {
          "description": "Per-study standard errors (must be positive).",
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "required": [
        "effects",
        "se"
      ],
      "type": "object"
    }
  },
  {
    "description": "Result of a summary-data meta-analysis.",
    "name": "MetaAnalysisResult",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "ci": {
          "description": "ci parameter (tuple).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "effects": {
          "description": "effects parameter (np.ndarray).",
          "type": "string"
        },
        "estimate": {
          "description": "estimate parameter (float).",
          "type": "number"
        },
        "fixed_estimate": {
          "description": "fixed_estimate parameter (float).",
          "type": "number"
        },
        "fixed_se": {
          "description": "fixed_se parameter (float).",
          "type": "number"
        },
        "h2": {
          "description": "h2 parameter (float).",
          "type": "number"
        },
        "i2": {
          "description": "i2 parameter (float).",
          "type": "number"
        },
        "labels": {
          "description": "labels parameter (List[str]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "method": {
          "description": "Estimator or algorithm variant to use.",
          "type": "string"
        },
        "p_value": {
          "description": "p_value parameter (float).",
          "type": "number"
        },
        "prediction_interval": {
          "description": "prediction_interval parameter (Optional[tuple]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "q": {
          "description": "Quantile level.",
          "type": "number"
        },
        "q_df": {
          "description": "q_df parameter (int).",
          "type": "integer"
        },
        "q_pvalue": {
          "description": "q_pvalue parameter (float).",
          "type": "number"
        },
        "random_estimate": {
          "description": "random_estimate parameter (float).",
          "type": "number"
        },
        "random_se": {
          "description": "random_se parameter (float).",
          "type": "number"
        },
        "se": {
          "description": "se parameter (float).",
          "type": "number"
        },
        "se_studies": {
          "description": "se_studies parameter (np.ndarray).",
          "type": "string"
        },
        "tau2": {
          "description": "tau2 parameter (float).",
          "type": "number"
        },
        "weights": {
          "description": "Observation weights.",
          "type": "string"
        }
      },
      "required": [
        "estimate",
        "se",
        "ci",
        "p_value",
        "method",
        "fixed_estimate",
        "fixed_se",
        "random_estimate",
        "random_se",
        "tau2",
        "q",
        "q_df",
        "q_pvalue",
        "i2",
        "h2",
        "prediction_interval",
        "weights",
        "effects",
        "se_studies",
        "labels"
      ],
      "type": "object"
    }
  },
  {
    "description": "Prediction-powered estimate of a population mean.",
    "name": "ppi_mean",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "CI level (1 - alpha confidence).",
          "type": "number"
        },
        "tune": {
          "default": true,
          "description": "Use the PPI++ power-tuning weight ``lambda in [0, 1]``. ``False`` fixes ``lambda = 1`` (the original PPI estimator).",
          "type": "boolean"
        },
        "y": {
          "description": "Gold-standard (human) outcomes on the labeled sample.",
          "type": "string"
        },
        "yhat": {
          "description": "Model predictions on the *same* labeled rows.",
          "type": "string"
        },
        "yhat_unlabeled": {
          "description": "Model predictions on the unlabeled rows.",
          "type": "string"
        }
      },
      "required": [
        "y",
        "yhat",
        "yhat_unlabeled"
      ],
      "type": "object"
    }
  },
  {
    "description": "Prediction-powered OLS with a labeled audit sample.",
    "name": "ppi_ols",
    "parameters": {
      "properties": {
        "X": {
          "description": "Covariates for the labeled rows.",
          "type": "string"
        },
        "X_unlabeled": {
          "description": "Covariates for the unlabeled rows (same columns as ``X``).",
          "type": "string"
        },
        "add_intercept": {
          "default": true,
          "description": "Prepend a constant column to both design matrices.",
          "type": "boolean"
        },
        "alpha": {
          "default": 0.05,
          "description": "CI level (1 - alpha confidence).",
          "type": "number"
        },
        "target": {
          "description": "Which coefficient is the headline ``estimate``. Defaults to the first non-intercept term.",
          "type": "integer"
        },
        "tune": {
          "default": true,
          "description": "Per-coefficient PPI++ power tuning. ``False`` fixes ``lambda = 1``.",
          "type": "boolean"
        },
        "y": {
          "description": "Gold-standard outcomes for the labeled rows.",
          "type": "string"
        },
        "yhat": {
          "description": "Model-predicted outcomes for the same labeled rows.",
          "type": "string"
        },
        "yhat_unlabeled": {
          "description": "Model-predicted outcomes for the unlabeled rows.",
          "type": "string"
        }
      },
      "required": [
        "X",
        "y",
        "yhat",
        "X_unlabeled",
        "yhat_unlabeled"
      ],
      "type": "object"
    }
  },
  {
    "description": "Class wrapper around :func:`msm` for programmatic access.",
    "name": "MarginalStructuralModel",
    "parameters": {
      "properties": {},
      "required": [],
      "type": "object"
    }
  },
  {
    "description": "Compute stabilized IPTW weights for time-varying treatments. Validation: certified parity evidence.",
    "name": "stabilized_weights",
    "parameters": {
      "properties": {
        "baseline": {
          "description": "Baseline covariates.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "data": {
          "description": "Sorted long-format panel (one row per unit-time).",
          "type": "string"
        },
        "density_sd": {
          "default": "unbiased",
          "description": "Residual standard deviation used in the Gaussian densities of a continuous treatment: ``'unbiased'`` divides the residual sum of squares by ``N - k`` (what ``lm``'s ``sigma`` reports); ``'ml'`` divides by ``N``, the maximum-likelihood estimate, which is the dispersion ``ipw::ipwtm(family = \"gaussian\")`` takes from ``geepack::geeglm``. Ignored for binary treatments.",
          "enum": [
            "unbiased",
            "ml"
          ],
          "type": "string"
        },
        "id": {
          "description": "Unit and time identifiers.",
          "type": "string"
        },
        "time": {
          "description": "Unit and time identifiers.",
          "type": "string"
        },
        "time_varying": {
          "description": "Time-varying confounders (already lagged to pre-treatment).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "treat": {
          "description": "Treatment column (binary 0/1 or continuous).",
          "type": "string"
        },
        "treat_type": {
          "default": "auto",
          "description": "Overrides auto-detection.",
          "enum": [
            "auto",
            "binary",
            "continuous"
          ],
          "type": "string"
        },
        "trim_per_period": {
          "default": 0.0,
          "description": "If > 0, trim each period's density ratio at the symmetric quantile ``[trim_per_period, 1 - trim_per_period]`` *before* taking the cumulative product. A common value is 0.01. Set to 0 to disable per-period trimming (the default).",
          "type": "number"
        }
      },
      "required": [
        "data",
        "treat",
        "id",
        "time",
        "time_varying"
      ],
      "type": "object"
    }
  },
  {
    "description": "Class wrapper for :func:`proximal`.",
    "name": "ProximalCausalInference",
    "parameters": {
      "properties": {},
      "required": [],
      "type": "object"
    }
  },
  {
    "description": "Principal stratification result.",
    "name": "PrincipalStratResult",
    "parameters": {
      "properties": {
        "alpha": {
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "bounds": {
          "description": "bounds parameter (Optional[pandas.DataFrame]).",
          "type": "string"
        },
        "effects": {
          "description": "effects parameter (DataFrame).",
          "type": "string"
        },
        "method": {
          "description": "Estimator or algorithm variant to use.",
          "type": "string"
        },
        "model_info": {
          "description": "model_info parameter (Dict[str, Any]).",
          "type": "object"
        },
        "n_obs": {
          "description": "Number of obs.",
          "type": "integer"
        },
        "strata_proportions": {
          "description": "strata_proportions parameter (Dict[str, float]).",
          "type": "object"
        }
      },
      "required": [
        "method",
        "strata_proportions",
        "effects",
        "bounds",
        "n_obs",
        "alpha",
        "model_info"
      ],
      "type": "object"
    }
  },
  {
    "description": "Zhang-Rubin (2003) sharp bounds on the Survivor Average Causal Effect. Validation: certified parity evidence.",
    "name": "survivor_average_causal_effect",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "n_boot": {
          "default": 500,
          "description": "Bootstrap replications for the endpoint CIs.",
          "type": "integer"
        },
        "seed": {
          "description": "Random seed for reproducible stochastic steps.",
          "type": "integer"
        },
        "survival": {
          "description": "Binary survival indicator (0/1), assumed monotone in ``treat``.",
          "type": "string"
        },
        "treat": {
          "description": "Binary randomised treatment (0/1).",
          "type": "string"
        },
        "trimming": {
          "default": "quantile",
          "description": "Trimming rule; see :func:`sp.lee_bounds`. Under monotonicity the Zhang-Rubin SACE bounds are Lee (2009) bounds, and the two functions return the same endpoints.",
          "enum": [
            "quantile",
            "exact"
          ],
          "type": "string"
        },
        "y": {
          "description": "Outcome; may be missing (NaN) for non-survivors, as it is under truncation by death, but must be observed wherever ``survival == 1``.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "treat",
        "survival"
      ],
      "type": "object"
    }
  },
  {
    "description": "Unified spatial regression estimator.",
    "name": "SpatialModel",
    "parameters": {
      "properties": {
        "W": {
          "description": "Covariates, proxy variables, or weights used by this estimator.",
          "type": "string"
        },
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "formula": {
          "description": "Model formula using patsy/R-style syntax.",
          "type": "string"
        },
        "model_type": {
          "default": "sar",
          "description": "model_type parameter (str).",
          "type": "string"
        },
        "row_normalize": {
          "default": true,
          "description": "row_normalize parameter (bool).",
          "type": "boolean"
        }
      },
      "required": [
        "W",
        "data",
        "formula"
      ],
      "type": "object"
    }
  },
  {
    "description": "Spatial DiD with direct and spillover treatment effects. Validation: validated evidence tier (known-truth, reference, external-parity, or Monte Carlo artifact).",
    "name": "spatial_did",
    "parameters": {
      "properties": {
        "W": {
          "description": "Spatial weights matrix over units. If a StatsPAI ``W`` object carries an ``id_order``, that order is respected.",
          "type": "string"
        },
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals.",
          "type": "number"
        },
        "cluster": {
          "description": "Cluster variable for ``se_type='cluster'``. Defaults to ``unit``.",
          "type": "string"
        },
        "conley_cutoff": {
          "description": "Spatial cutoff for Conley-HAC standard errors.",
          "type": "number"
        },
        "conley_kernel": {
          "default": "bartlett",
          "description": "conley_kernel parameter (str).",
          "enum": [
            "uniform",
            "bartlett"
          ],
          "type": "string"
        },
        "coords": {
          "description": "Convenience pair ``(lat, lon)`` in degrees.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "covariates": {
          "description": "Additional controls included after absorbing unit and time effects.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "data": {
          "description": "Long panel with one row per unit-period.",
          "type": "string"
        },
        "distance_matrix": {
          "description": "Unit-by-unit distances in the same units as ``conley_cutoff``.",
          "type": "string"
        },
        "event_base": {
          "default": -1,
          "description": "Omitted relative-time period.",
          "type": "integer"
        },
        "event_study": {
          "default": false,
          "description": "Estimate direct and spillover event-study paths by relative time.",
          "type": "boolean"
        },
        "event_window": {
          "description": "Inclusive event-time window. Defaults to observed support capped at [-5, 5] when ``event_study=True``.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "lat": {
          "description": "Latitude/longitude columns in degrees.",
          "type": "string"
        },
        "lon": {
          "description": "Latitude/longitude columns in degrees.",
          "type": "string"
        },
        "normalize_W": {
          "default": true,
          "description": "Row-normalise weights before constructing ``WD``.",
          "type": "boolean"
        },
        "se_type": {
          "default": "cluster",
          "description": "Covariance estimator. ``conley`` uses spatial-HAC dependence within each period and requires ``conley_cutoff`` plus coordinates or a unit-level distance matrix.",
          "enum": [
            "cluster",
            "robust",
            "conley"
          ],
          "type": "string"
        },
        "time": {
          "description": "Outcome, treatment, unit-id, and time-id columns. ``treat`` may be binary or a continuous exposure intensity; the spatial lag uses the same scale.",
          "type": "string"
        },
        "treat": {
          "description": "Outcome, treatment, unit-id, and time-id columns. ``treat`` may be binary or a continuous exposure intensity; the spatial lag uses the same scale.",
          "type": "string"
        },
        "unit": {
          "description": "Outcome, treatment, unit-id, and time-id columns. ``treat`` may be binary or a continuous exposure intensity; the spatial lag uses the same scale.",
          "type": "string"
        },
        "unit_order": {
          "description": "Explicit row/column order of ``W``.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Outcome, treatment, unit-id, and time-id columns. ``treat`` may be binary or a continuous exposure intensity; the spatial lag uses the same scale.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "treat",
        "unit",
        "time",
        "W"
      ],
      "type": "object"
    }
  },
  {
    "description": "Result object for :func:`spatial_did`.",
    "name": "SpatialDiDResult",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "ci_direct": {
          "description": "ci_direct parameter (tuple).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "ci_spillover": {
          "description": "ci_spillover parameter (tuple).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "ci_total": {
          "default": [
            NaN,
            NaN
          ],
          "description": "ci_total parameter (tuple).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "coefficients": {
          "description": "coefficients parameter (pd.DataFrame).",
          "type": "string"
        },
        "data_info": {
          "default": null,
          "description": "data_info parameter (Dict[str, Any]).",
          "type": "object"
        },
        "detail": {
          "default": null,
          "description": "Amount of result detail to return.",
          "type": "object"
        },
        "direct_effect": {
          "description": "direct_effect parameter (float).",
          "type": "number"
        },
        "model_info": {
          "default": null,
          "description": "model_info parameter (Dict[str, Any]).",
          "type": "object"
        },
        "n_obs": {
          "description": "Number of obs.",
          "type": "integer"
        },
        "pvalue_direct": {
          "description": "pvalue_direct parameter (float).",
          "type": "number"
        },
        "pvalue_spillover": {
          "description": "pvalue_spillover parameter (float).",
          "type": "number"
        },
        "pvalue_total": {
          "default": NaN,
          "description": "pvalue_total parameter (float).",
          "type": "number"
        },
        "se_direct": {
          "description": "se_direct parameter (float).",
          "type": "number"
        },
        "se_spillover": {
          "description": "se_spillover parameter (float).",
          "type": "number"
        },
        "se_total": {
          "default": NaN,
          "description": "se_total parameter (float).",
          "type": "number"
        },
        "se_type": {
          "default": "cluster",
          "description": "se_type parameter (str).",
          "type": "string"
        },
        "spillover_effect": {
          "description": "spillover_effect parameter (float).",
          "type": "number"
        },
        "total_effect": {
          "default": NaN,
          "description": "total_effect parameter (float).",
          "type": "number"
        },
        "vcov": {
          "description": "Variance-covariance estimator option.",
          "type": "string"
        }
      },
      "required": [
        "direct_effect",
        "spillover_effect",
        "se_direct",
        "se_spillover",
        "ci_direct",
        "ci_spillover",
        "pvalue_direct",
        "pvalue_spillover",
        "coefficients",
        "n_obs"
      ],
      "type": "object"
    }
  },
  {
    "description": "Spatial 2SLS estimator. Validation: certified parity evidence.",
    "name": "spatial_iv",
    "parameters": {
      "properties": {
        "W": {
          "description": "Spatial weights matrix (n_obs x n_obs). Should be row-normalised.",
          "type": "string"
        },
        "alpha": {
          "default": 0.05,
          "description": "Significance level of the normal confidence intervals in ``coefficients`` (``ci_lower`` / ``ci_upper``).",
          "type": "number"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "endog": {
          "description": "Names of additional endogenous regressors.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "exog": {
          "description": "Names of exogenous regressors (included instruments).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "include_WY": {
          "default": true,
          "description": "If True, include WY as a regressor (spatial autoregressive coefficient rho).",
          "type": "boolean"
        },
        "instruments": {
          "description": "Extra excluded instruments beyond the spatial lags of X.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "endog",
        "exog",
        "W"
      ],
      "type": "object"
    }
  },
  {
    "description": "Container for :func:`spatial_iv` Kelejian-Prucha S2SLS estimates.",
    "name": "SpatialIVResult",
    "parameters": {
      "properties": {
        "coefficients": {
          "description": "coefficients parameter (pd.DataFrame).",
          "type": "string"
        },
        "detail": {
          "default": null,
          "description": "Amount of result detail to return.",
          "type": "object"
        },
        "n_obs": {
          "description": "Number of obs.",
          "type": "integer"
        },
        "rho": {
          "description": "rho parameter (float).",
          "type": "number"
        },
        "rho_se": {
          "description": "rho_se parameter (float).",
          "type": "number"
        }
      },
      "required": [
        "rho",
        "rho_se",
        "coefficients",
        "n_obs"
      ],
      "type": "object"
    }
  },
  {
    "description": "S-Learner: single model, treatment as a feature.",
    "name": "SLearner",
    "parameters": {
      "properties": {
        "model": {
          "description": "Outcome model mu(X, D). Default: GradientBoostingRegressor.",
          "type": "string"
        }
      },
      "required": [],
      "type": "object"
    }
  },
  {
    "description": "T-Learner: separate models for each treatment arm.",
    "name": "TLearner",
    "parameters": {
      "properties": {
        "model_0": {
          "description": "Control outcome model. Default: GradientBoostingRegressor.",
          "type": "string"
        },
        "model_1": {
          "description": "Treated outcome model. Default: same type as model_0.",
          "type": "string"
        }
      },
      "required": [],
      "type": "object"
    }
  },
  {
    "description": "X-Learner (Kunzel et al. 2019).",
    "name": "XLearner",
    "parameters": {
      "properties": {
        "cate_model_0": {
          "description": "CATE model for control-side imputed effects.",
          "type": "string"
        },
        "cate_model_1": {
          "description": "CATE model for treated-side imputed effects.",
          "type": "string"
        },
        "model_0": {
          "description": "Control outcome model.",
          "type": "string"
        },
        "model_1": {
          "description": "Treated outcome model.",
          "type": "string"
        },
        "propensity_model": {
          "description": "Model for e(x) = P(D=1|X).",
          "type": "string"
        }
      },
      "required": [],
      "type": "object"
    }
  },
  {
    "description": "R-Learner (Nie & Wager 2021).",
    "name": "RLearner",
    "parameters": {
      "properties": {
        "cate_model": {
          "description": "Model for tau(X). Fit on pseudo-outcome.",
          "type": "string"
        },
        "fold_indices": {
          "description": "Explicit cross-fitting partition, one label in ``0..n_folds-1`` per row passed to :meth:`fit`. Default ``None`` keeps the historical ``KFold(n_folds, shuffle=True, random_state=42)``.",
          "type": "string"
        },
        "n_folds": {
          "default": 5,
          "description": "Cross-fitting folds for nuisance estimation.",
          "type": "integer"
        },
        "outcome_model": {
          "description": "Model for m(X) = E[Y|X].",
          "type": "string"
        },
        "propensity_model": {
          "description": "Model for e(X) = P(D=1|X).",
          "type": "string"
        }
      },
      "required": [],
      "type": "object"
    }
  },
  {
    "description": "DR-Learner (Kennedy 2023): doubly robust CATE estimation.",
    "name": "DRLearner",
    "parameters": {
      "properties": {
        "cate_model": {
          "description": "Final-stage model for tau(X).",
          "type": "string"
        },
        "fold_indices": {
          "description": "Explicit cross-fitting partition, one label in ``0..n_folds-1`` per row passed to :meth:`fit`. Default ``None`` keeps the historical ``KFold(n_folds, shuffle=True, random_state=42)``.",
          "type": "string"
        },
        "n_folds": {
          "default": 5,
          "description": "Cross-fitting folds for nuisance estimation.",
          "type": "integer"
        },
        "outcome_model": {
          "description": "Model for mu_d(X) = E[Y|X, D=d].",
          "type": "string"
        },
        "propensity_model": {
          "description": "Model for e(X) = P(D=1|X).",
          "type": "string"
        }
      },
      "required": [],
      "type": "object"
    }
  },
  {
    "description": "Descriptive statistics of the CATE distribution.",
    "name": "cate_summary",
    "parameters": {
      "properties": {
        "result": {
          "description": "Result from ``metalearner()`` containing ``model_info['cate']``.",
          "type": "string"
        }
      },
      "required": [
        "result"
      ],
      "type": "object"
    }
  },
  {
    "description": "Group-level average treatment effects.",
    "name": "cate_by_group",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for CIs.",
          "type": "number"
        },
        "by": {
          "description": "Column name to group by, or 'cate' to group by CATE quartiles.",
          "type": "string"
        },
        "data": {
          "description": "Original data (same rows as the estimation sample).",
          "type": "string"
        },
        "n_groups": {
          "default": 4,
          "description": "Number of quantile groups when ``by='cate'`` or when the grouping variable is continuous.",
          "type": "integer"
        },
        "result": {
          "description": "Result from ``metalearner()`` / ``tarnet()``, a fitted ``causal_forest()`` model, or a raw array of per-unit CATE estimates.",
          "type": "string"
        }
      },
      "required": [
        "result",
        "data",
        "by"
      ],
      "type": "object"
    }
  },
  {
    "description": "Plot the CATE distribution.",
    "name": "cate_plot",
    "parameters": {
      "properties": {
        "ax": {
          "description": "ax parameter.",
          "type": "string"
        },
        "color": {
          "default": "#2C3E50",
          "description": "color parameter (str).",
          "type": "string"
        },
        "figsize": {
          "default": [
            8,
            5
          ],
          "description": "figsize parameter (tuple).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "kind": {
          "default": "hist",
          "description": "'hist' for histogram, 'kde' for kernel density, 'both'.",
          "type": "string"
        },
        "result": {
          "description": "Result from ``metalearner()``.",
          "type": "string"
        },
        "title": {
          "description": "title parameter (Optional[str]).",
          "type": "string"
        }
      },
      "required": [
        "result"
      ],
      "type": "object"
    }
  },
  {
    "description": "Plot group-level CATEs with confidence intervals.",
    "name": "cate_group_plot",
    "parameters": {
      "properties": {
        "ax": {
          "description": "ax parameter.",
          "type": "string"
        },
        "color": {
          "default": "#2C3E50",
          "description": "color parameter (str).",
          "type": "string"
        },
        "figsize": {
          "default": [
            8,
            5
          ],
          "description": "figsize parameter (tuple).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "group_df": {
          "description": "Output from ``cate_by_group()``.",
          "type": "string"
        },
        "title": {
          "description": "title parameter (Optional[str]).",
          "type": "string"
        }
      },
      "required": [
        "group_df"
      ],
      "type": "object"
    }
  },
  {
    "description": "Predict CATE on new (out-of-sample) data.",
    "name": "predict_cate",
    "parameters": {
      "properties": {
        "new_data": {
          "description": "New data with the same covariate columns used in estimation.",
          "type": "string"
        },
        "result": {
          "description": "Result from ``metalearner()`` containing a fitted estimator.",
          "type": "string"
        }
      },
      "required": [
        "result",
        "new_data"
      ],
      "type": "object"
    }
  },
  {
    "description": "Fit multiple meta-learners and compare their ATE estimates.",
    "name": "compare_metalearners",
    "parameters": {
      "properties": {
        "covariates": {
          "description": "Covariate / effect modifier variables.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "data": {
          "description": "Input data.",
          "type": "string"
        },
        "learners": {
          "description": "Which learners to compare. Default: all five ('s','t','x','r','dr').",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "treat": {
          "description": "Binary treatment variable (0/1).",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "treat",
        "covariates"
      ],
      "type": "object"
    }
  },
  {
    "description": "Sorted group average treatment effects (GATES) and heterogeneity tests. Validation: certified parity evidence.",
    "name": "gate_test",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "baseline": {
          "description": "As in :func:`blp_test`.",
          "type": "string"
        },
        "by": {
          "description": "``'cate'`` or a column name.",
          "type": "string"
        },
        "covariates": {
          "description": "Outcome, binary treatment and covariates; required for GATES.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "n_folds": {
          "default": 5,
          "description": "As in :func:`blp_test`.",
          "type": "integer"
        },
        "n_groups": {
          "default": 4,
          "description": "Number of groups.",
          "type": "integer"
        },
        "propensity": {
          "description": "As in :func:`blp_test`.",
          "type": "string"
        },
        "proxy": {
          "default": "cross_fit",
          "description": "As in :func:`blp_test`.",
          "type": "string"
        },
        "result": {
          "description": "result parameter (CausalResult).",
          "type": "string"
        },
        "seed": {
          "default": 42,
          "description": "As in :func:`blp_test`.",
          "type": "integer"
        },
        "treat": {
          "description": "Outcome, binary treatment and covariates; required for GATES.",
          "type": "string"
        },
        "vce": {
          "default": "HC1",
          "description": "As in :func:`blp_test`.",
          "type": "string"
        },
        "y": {
          "description": "Outcome, binary treatment and covariates; required for GATES.",
          "type": "string"
        }
      },
      "required": [
        "result",
        "data",
        "by"
      ],
      "type": "object"
    }
  },
  {
    "description": "Best Linear Predictor (BLP) of the CATE, Chernozhukov-Demirer-Duflo-Fernandez-Val. Validation: certified parity evidence.",
    "name": "blp_test",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "baseline": {
          "description": "Baseline proxy ``B(Z)`` added as a control.",
          "type": "string"
        },
        "covariates": {
          "description": "Covariates ``Z`` (for the propensity and the proxy refit).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "n_folds": {
          "default": 5,
          "description": "Folds for the propensity and proxy cross-fits.",
          "type": "integer"
        },
        "propensity": {
          "description": "Known or estimated ``p(Z)``. Default: 5-fold gradient-boosting classifier, clipped to [0.01, 0.99]. In a randomized experiment pass the design probabilities.",
          "type": "string"
        },
        "proxy": {
          "default": "cross_fit",
          "description": "Source of ``S(Z)``.",
          "enum": [
            "cross_fit",
            "in_sample"
          ],
          "type": "string"
        },
        "result": {
          "description": "A ``metalearner()`` result (its learner is refit out of fold), or per-unit CATE predictions used as supplied.",
          "type": "string"
        },
        "seed": {
          "default": 42,
          "description": "Fold seed for the proxy refit.",
          "type": "integer"
        },
        "treat": {
          "description": "Outcome and binary treatment columns.",
          "type": "string"
        },
        "vce": {
          "default": "HC1",
          "description": "Covariance of the WLS fit (``sandwich::vcovHC`` types; GenericML's default is ``'const'``).",
          "enum": [
            "HC1",
            "const",
            "HC0",
            "HC2",
            "HC3"
          ],
          "type": "string"
        },
        "y": {
          "description": "Outcome and binary treatment columns.",
          "type": "string"
        }
      },
      "required": [
        "result",
        "data",
        "y",
        "treat",
        "covariates"
      ],
      "type": "object"
    }
  },
  {
    "description": "Race several meta-learners and return a scored leaderboard + winner. Validation: validated evidence tier (known-truth, reference, external-parity, or Monte Carlo artifact).",
    "name": "auto_cate",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for both learner confidence intervals and the BLP-beta1 acceptance region used by the selection rule.",
          "type": "number"
        },
        "cate_model": {
          "description": "Override the default gradient-boosting models used for nuisance and final CATE fitting.",
          "type": "string"
        },
        "covariates": {
          "description": "Effect-modifier columns used as features for every nuisance and CATE model.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "data": {
          "description": "Input data. Must contain ``y``, ``treat`` and all ``covariates``.",
          "type": "string"
        },
        "learners": {
          "default": [
            "s",
            "t",
            "x",
            "r",
            "dr"
          ],
          "description": "Short codes of the meta-learners to race. Duplicates are ignored.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "n_bootstrap": {
          "default": 200,
          "description": "Bootstrap iterations for ATE standard error on non-DR learners (passed through to ``metalearner``).",
          "type": "integer"
        },
        "n_folds": {
          "default": 5,
          "description": "Number of folds used for both the shared nuisance cross-fit and each learner's honest CATE prediction.",
          "type": "integer"
        },
        "outcome_model": {
          "description": "Override the default gradient-boosting models used for nuisance and final CATE fitting.",
          "type": "string"
        },
        "propensity_model": {
          "description": "Override the default gradient-boosting models used for nuisance and final CATE fitting.",
          "type": "string"
        },
        "random_state": {
          "default": 42,
          "description": "Seed for all K-fold splits.",
          "type": "integer"
        },
        "score": {
          "default": "r_loss",
          "description": "Currently only ``'r_loss'`` is implemented. Reserved for future expansion.",
          "type": "string"
        },
        "treat": {
          "description": "Binary treatment column name (values in {0, 1}).",
          "type": "string"
        },
        "y": {
          "description": "Outcome column name.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "treat",
        "covariates"
      ],
      "type": "object"
    }
  },
  {
    "description": "Leaderboard + winner from ``sp.auto_cate``.",
    "name": "AutoCATEResult",
    "parameters": {
      "properties": {
        "agreement": {
          "description": "agreement parameter (pd.DataFrame).",
          "type": "string"
        },
        "best_learner": {
          "description": "best_learner parameter (str).",
          "type": "string"
        },
        "best_result": {
          "description": "best_result parameter (CausalResult).",
          "type": "string"
        },
        "leaderboard": {
          "description": "leaderboard parameter (pd.DataFrame).",
          "type": "string"
        },
        "n_obs": {
          "description": "Number of obs.",
          "type": "integer"
        },
        "results": {
          "description": "results parameter (Dict[str, CausalResult]).",
          "type": "object"
        },
        "selection_rule": {
          "description": "selection_rule parameter (str).",
          "type": "string"
        }
      },
      "required": [
        "leaderboard",
        "best_learner",
        "best_result",
        "results",
        "agreement",
        "selection_rule",
        "n_obs"
      ],
      "type": "object"
    }
  },
  {
    "description": "Optuna-tuned CATE learner race -- nuisance, per-learner, or both. Validation: validated evidence tier (known-truth, reference, external-parity, or Monte Carlo artifact).",
    "name": "auto_cate_tuned",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "covariates": {
          "description": "Covariate matrix, DataFrame, or column names.",
          "type": "string"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "learners": {
          "default": [
            "s",
            "t",
            "x",
            "r",
            "dr"
          ],
          "description": "learners parameter (LearnersArg).",
          "type": "string"
        },
        "n_bootstrap": {
          "default": 200,
          "description": "Number of bootstrap replications.",
          "type": "integer"
        },
        "n_folds": {
          "default": 5,
          "description": "Number of cross-fitting or cross-validation folds.",
          "type": "integer"
        },
        "n_trials": {
          "default": 25,
          "description": "Budget for the nuisance-tuning study (ignored when ``tune == 'per_learner'``).",
          "type": "integer"
        },
        "n_trials_per_learner": {
          "description": "Budget for each per-learner study. Defaults to ``max(5, n_trials // 3)``.",
          "type": "integer"
        },
        "per_learner_search_space": {
          "description": "Override default spaces. See :data:`DEFAULT_SEARCH_SPACE` and :data:`DEFAULT_PER_LEARNER_SEARCH_SPACE`. Passed through / see :func:`auto_cate`.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "random_state": {
          "default": 42,
          "description": "Random seed or RandomState for reproducible stochastic steps.",
          "type": "integer"
        },
        "sampler": {
          "description": "sampler parameter (Optional[Any]).",
          "type": "string"
        },
        "search_space": {
          "description": "Override default spaces. See :data:`DEFAULT_SEARCH_SPACE` and :data:`DEFAULT_PER_LEARNER_SEARCH_SPACE`. Passed through / see :func:`auto_cate`.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "timeout": {
          "description": "Wall-clock limit per study (seconds).",
          "type": "number"
        },
        "treat": {
          "description": "Treatment indicator or first-treatment-period column.",
          "type": "string"
        },
        "tune": {
          "default": "nuisance",
          "description": "Tuning regime: - ``'nuisance'`` -- tune the shared outcome / propensity GBMs against held-out R-loss, then hand them to ``auto_cate``. (v0.9.5 behaviour.) - ``'per_learner'`` -- keep default nuisance models; for each learner, tune its final-stage CATE model against held-out R-loss. - ``'both'`` -- run ``'nuisance'`` first, then ``'per_learner'`` using the tuned nuisance. Most expensive; most thorough.",
          "enum": [
            "nuisance",
            "per_learner",
            "both"
          ],
          "type": "string"
        },
        "verbose": {
          "default": false,
          "description": "verbose parameter (bool).",
          "type": "boolean"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "treat",
        "covariates"
      ],
      "type": "object"
    }
  },
  {
    "description": "Bayesian interrupted time series (segmented regression).",
    "name": "bayes_its",
    "parameters": {
      "properties": {
        "advi_iterations": {
          "default": 20000,
          "description": "advi_iterations parameter (int).",
          "type": "integer"
        },
        "chains": {
          "default": 4,
          "description": "chains parameter (int).",
          "type": "integer"
        },
        "data": {
          "description": "Observed series, sorted in time.",
          "type": "string"
        },
        "draws": {
          "default": 2000,
          "description": "draws parameter (int).",
          "type": "integer"
        },
        "hdi_prob": {
          "default": 0.95,
          "description": "Sampler controls; see :func:`sp.bayes_did`.",
          "type": "number"
        },
        "inference": {
          "default": "nuts",
          "description": "inference parameter (str).",
          "type": "string"
        },
        "intervention": {
          "description": "Integer row position where the intervention begins (``0 < intervention < n``).",
          "type": "integer"
        },
        "prior_level": {
          "default": [
            0.0,
            10.0
          ],
          "description": "Mean / SD of the Normal prior on the level change (the estimand).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "prior_noise": {
          "default": 5.0,
          "description": "Prior scales for the slope change, the pre-trend, and the residual SD.",
          "type": "number"
        },
        "prior_slope_sigma": {
          "default": 10.0,
          "description": "Prior scales for the slope change, the pre-trend, and the residual SD.",
          "type": "number"
        },
        "prior_trend_sigma": {
          "default": 10.0,
          "description": "Prior scales for the slope change, the pre-trend, and the residual SD.",
          "type": "number"
        },
        "progressbar": {
          "default": false,
          "description": "progressbar parameter (bool).",
          "type": "boolean"
        },
        "random_state": {
          "default": 42,
          "description": "Random seed or RandomState for reproducible stochastic steps.",
          "type": "integer"
        },
        "rope": {
          "description": "Region of practical equivalence for the level change.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "target_accept": {
          "default": 0.9,
          "description": "target_accept parameter (float).",
          "type": "number"
        },
        "time": {
          "description": "Time column (used for the trend term and the x-axis). Defaults to the row index.",
          "type": "string"
        },
        "tune": {
          "default": 1000,
          "description": "tune parameter (int).",
          "type": "integer"
        },
        "y": {
          "description": "Outcome column.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y"
      ],
      "type": "object"
    }
  },
  {
    "description": "Bayesian synthetic control. Validation: validated evidence tier (known-truth, reference, external-parity, or Monte Carlo artifact).",
    "name": "bayes_synth",
    "parameters": {
      "properties": {
        "advi_iterations": {
          "default": 20000,
          "description": "advi_iterations parameter (int).",
          "type": "integer"
        },
        "chains": {
          "default": 4,
          "description": "chains parameter (int).",
          "type": "integer"
        },
        "data": {
          "description": "Long panel (one row per unit-time).",
          "type": "string"
        },
        "donors": {
          "description": "Donor pool. Defaults to every unit except the treated one.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "draws": {
          "default": 2000,
          "description": "draws parameter (int).",
          "type": "integer"
        },
        "hdi_prob": {
          "default": 0.95,
          "description": "Sampler controls; see :func:`sp.bayes_did`.",
          "type": "number"
        },
        "inference": {
          "default": "nuts",
          "description": "inference parameter (str).",
          "type": "string"
        },
        "outcome": {
          "description": "Outcome, unit-id and time column names.",
          "type": "string"
        },
        "prior_noise": {
          "default": 5.0,
          "description": "Half-Normal scale for the pre-period fit residual SD.",
          "type": "number"
        },
        "prior_weights_alpha": {
          "default": 1.0,
          "description": "Dirichlet concentration. ``1.0`` is uniform over the simplex; ``<1`` favours sparse weights.",
          "type": "number"
        },
        "progressbar": {
          "default": false,
          "description": "progressbar parameter (bool).",
          "type": "boolean"
        },
        "random_state": {
          "default": 42,
          "description": "Random seed or RandomState for reproducible stochastic steps.",
          "type": "integer"
        },
        "rope": {
          "description": "Region of practical equivalence for the ATT.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "target_accept": {
          "default": 0.9,
          "description": "target_accept parameter (float).",
          "type": "number"
        },
        "time": {
          "description": "Outcome, unit-id and time column names.",
          "type": "string"
        },
        "treated_unit": {
          "description": "The treated unit's id.",
          "type": "string"
        },
        "treatment_time": {
          "description": "First treated period (inclusive); periods ``< treatment_time`` are the pre-treatment fitting window.",
          "type": "string"
        },
        "tune": {
          "default": 1000,
          "description": "tune parameter (int).",
          "type": "integer"
        },
        "unit": {
          "description": "Outcome, unit-id and time column names.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "outcome",
        "unit",
        "time",
        "treated_unit",
        "treatment_time"
      ],
      "type": "object"
    }
  },
  {
    "description": "Bayesian IV with linear CATE-by-covariate heterogeneity.",
    "name": "bayes_hte_iv",
    "parameters": {
      "properties": {
        "advi_iterations": {
          "default": 20000,
          "description": "ADVI iterations (ignored for NUTS).",
          "type": "integer"
        },
        "chains": {
          "default": 4,
          "description": ":func:`bayes_did`.",
          "type": "integer"
        },
        "covariates": {
          "description": "Covariate matrix, DataFrame, or column names.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "draws": {
          "default": 2000,
          "description": "draws parameter (int).",
          "type": "integer"
        },
        "effect_modifiers": {
          "description": "Covariates whose linear interaction with ``treat`` is the heterogeneity signal. The model centres these at their sample mean so ``tau_0`` is interpretable as the average LATE at ``modifiers = mean``.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "hdi_prob": {
          "default": 0.95,
          "description": "hdi_prob parameter (float).",
          "type": "number"
        },
        "inference": {
          "default": "nuts",
          "description": "Sampler. ADVI is a fast mean-field approximation; see the module-level caveat.",
          "enum": [
            "nuts",
            "advi"
          ],
          "type": "string"
        },
        "instrument": {
          "description": "instrument parameter (Union[str, Sequence[str]]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "prior_coef_sigma": {
          "default": 10.0,
          "description": "prior_coef_sigma parameter (float).",
          "type": "number"
        },
        "prior_hte_sigma": {
          "default": 5.0,
          "description": "Prior SD on each element of ``tau_hte`` (the slope of LATE on the corresponding modifier).",
          "type": "number"
        },
        "prior_late": {
          "default": [
            0.0,
            10.0
          ],
          "description": "prior_late parameter (Tuple[float, float]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "prior_noise": {
          "default": 5.0,
          "description": "prior_noise parameter (float).",
          "type": "number"
        },
        "progressbar": {
          "default": false,
          "description": ":func:`bayes_did`.",
          "type": "boolean"
        },
        "random_state": {
          "default": 42,
          "description": ":func:`bayes_did`.",
          "type": "integer"
        },
        "rope": {
          "description": "rope parameter (Optional[Tuple[float, float]]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "target_accept": {
          "default": 0.9,
          "description": ":func:`bayes_did`.",
          "type": "number"
        },
        "treat": {
          "description": "Treatment indicator or first-treatment-period column.",
          "type": "string"
        },
        "tune": {
          "default": 1000,
          "description": ":func:`bayes_did`.",
          "type": "integer"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "treat",
        "instrument",
        "effect_modifiers"
      ],
      "type": "object"
    }
  },
  {
    "description": "Summary of a Bayesian causal fit.",
    "name": "BayesianCausalResult",
    "parameters": {
      "properties": {
        "ess": {
          "description": "ess parameter (float).",
          "type": "number"
        },
        "estimand": {
          "description": "estimand parameter (str).",
          "type": "string"
        },
        "hdi_lower": {
          "description": "hdi_lower parameter (float).",
          "type": "number"
        },
        "hdi_prob": {
          "default": 0.95,
          "description": "hdi_prob parameter (float).",
          "type": "number"
        },
        "hdi_upper": {
          "description": "hdi_upper parameter (float).",
          "type": "number"
        },
        "method": {
          "description": "Estimator or algorithm variant to use.",
          "type": "string"
        },
        "model_info": {
          "default": null,
          "description": "model_info parameter (Dict[str, Any]).",
          "type": "object"
        },
        "n_obs": {
          "description": "Number of obs.",
          "type": "integer"
        },
        "posterior_mean": {
          "description": "posterior_mean parameter (float).",
          "type": "number"
        },
        "posterior_median": {
          "description": "posterior_median parameter (float).",
          "type": "number"
        },
        "posterior_sd": {
          "description": "posterior_sd parameter (float).",
          "type": "number"
        },
        "prob_positive": {
          "description": "prob_positive parameter (float).",
          "type": "number"
        },
        "prob_rope": {
          "description": "prob_rope parameter (Optional[float]).",
          "type": "number"
        },
        "rhat": {
          "description": "rhat parameter (float).",
          "type": "number"
        },
        "trace": {
          "description": "trace parameter.",
          "type": "string"
        }
      },
      "required": [
        "method",
        "estimand",
        "posterior_mean",
        "posterior_median",
        "posterior_sd",
        "hdi_lower",
        "hdi_upper",
        "prob_positive",
        "rhat",
        "ess",
        "n_obs"
      ],
      "type": "object"
    }
  },
  {
    "description": "Bayesian DID result with optional per-cohort ATT posteriors.",
    "name": "BayesianDIDResult",
    "parameters": {
      "properties": {
        "cohort_labels": {
          "default": null,
          "description": "cohort_labels parameter (List[Any]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "cohort_summaries": {
          "default": null,
          "description": "cohort_summaries parameter (Dict[str, Dict[str, float]]).",
          "type": "object"
        },
        "ess": {
          "description": "ess parameter (float).",
          "type": "number"
        },
        "estimand": {
          "description": "estimand parameter (str).",
          "type": "string"
        },
        "hdi_lower": {
          "description": "hdi_lower parameter (float).",
          "type": "number"
        },
        "hdi_prob": {
          "default": 0.95,
          "description": "hdi_prob parameter (float).",
          "type": "number"
        },
        "hdi_upper": {
          "description": "hdi_upper parameter (float).",
          "type": "number"
        },
        "method": {
          "description": "Estimator or algorithm variant to use.",
          "type": "string"
        },
        "model_info": {
          "default": null,
          "description": "model_info parameter (Dict[str, Any]).",
          "type": "object"
        },
        "n_obs": {
          "description": "Number of obs.",
          "type": "integer"
        },
        "posterior_mean": {
          "description": "posterior_mean parameter (float).",
          "type": "number"
        },
        "posterior_median": {
          "description": "posterior_median parameter (float).",
          "type": "number"
        },
        "posterior_sd": {
          "description": "posterior_sd parameter (float).",
          "type": "number"
        },
        "prob_positive": {
          "description": "prob_positive parameter (float).",
          "type": "number"
        },
        "prob_rope": {
          "description": "prob_rope parameter (Optional[float]).",
          "type": "number"
        },
        "rhat": {
          "description": "rhat parameter (float).",
          "type": "number"
        },
        "trace": {
          "description": "trace parameter.",
          "type": "string"
        }
      },
      "required": [
        "method",
        "estimand",
        "posterior_mean",
        "posterior_median",
        "posterior_sd",
        "hdi_lower",
        "hdi_upper",
        "prob_positive",
        "rhat",
        "ess",
        "n_obs"
      ],
      "type": "object"
    }
  },
  {
    "description": "Extension of :class:`BayesianCausalResult` for heterogeneous-effect IV.",
    "name": "BayesianHTEIVResult",
    "parameters": {
      "properties": {
        "_modifier_means": {
          "description": "_modifier_means parameter (Optional[np.ndarray]).",
          "type": "string"
        },
        "cate_slopes": {
          "default": null,
          "description": "cate_slopes parameter (pd.DataFrame).",
          "type": "string"
        },
        "effect_modifiers": {
          "default": null,
          "description": "effect_modifiers parameter (List[str]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "ess": {
          "description": "ess parameter (float).",
          "type": "number"
        },
        "estimand": {
          "description": "estimand parameter (str).",
          "type": "string"
        },
        "hdi_lower": {
          "description": "hdi_lower parameter (float).",
          "type": "number"
        },
        "hdi_prob": {
          "default": 0.95,
          "description": "hdi_prob parameter (float).",
          "type": "number"
        },
        "hdi_upper": {
          "description": "hdi_upper parameter (float).",
          "type": "number"
        },
        "method": {
          "description": "Estimator or algorithm variant to use.",
          "type": "string"
        },
        "model_info": {
          "default": null,
          "description": "model_info parameter (Dict[str, Any]).",
          "type": "object"
        },
        "n_obs": {
          "description": "Number of obs.",
          "type": "integer"
        },
        "posterior_mean": {
          "description": "posterior_mean parameter (float).",
          "type": "number"
        },
        "posterior_median": {
          "description": "posterior_median parameter (float).",
          "type": "number"
        },
        "posterior_sd": {
          "description": "posterior_sd parameter (float).",
          "type": "number"
        },
        "prob_positive": {
          "description": "prob_positive parameter (float).",
          "type": "number"
        },
        "prob_rope": {
          "description": "prob_rope parameter (Optional[float]).",
          "type": "number"
        },
        "rhat": {
          "description": "rhat parameter (float).",
          "type": "number"
        },
        "trace": {
          "description": "trace parameter.",
          "type": "string"
        }
      },
      "required": [
        "method",
        "estimand",
        "posterior_mean",
        "posterior_median",
        "posterior_sd",
        "hdi_lower",
        "hdi_upper",
        "prob_positive",
        "rhat",
        "ess",
        "n_obs"
      ],
      "type": "object"
    }
  },
  {
    "description": "Bayesian IV result with optional per-instrument LATE posteriors.",
    "name": "BayesianIVResult",
    "parameters": {
      "properties": {
        "ess": {
          "description": "ess parameter (float).",
          "type": "number"
        },
        "estimand": {
          "description": "estimand parameter (str).",
          "type": "string"
        },
        "hdi_lower": {
          "description": "hdi_lower parameter (float).",
          "type": "number"
        },
        "hdi_prob": {
          "default": 0.95,
          "description": "hdi_prob parameter (float).",
          "type": "number"
        },
        "hdi_upper": {
          "description": "hdi_upper parameter (float).",
          "type": "number"
        },
        "instrument_labels": {
          "default": null,
          "description": "instrument_labels parameter (List[str]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "instrument_summaries": {
          "default": null,
          "description": "instrument_summaries parameter (Dict[str, Dict[str, float]]).",
          "type": "object"
        },
        "method": {
          "description": "Estimator or algorithm variant to use.",
          "type": "string"
        },
        "model_info": {
          "default": null,
          "description": "model_info parameter (Dict[str, Any]).",
          "type": "object"
        },
        "n_obs": {
          "description": "Number of obs.",
          "type": "integer"
        },
        "posterior_mean": {
          "description": "posterior_mean parameter (float).",
          "type": "number"
        },
        "posterior_median": {
          "description": "posterior_median parameter (float).",
          "type": "number"
        },
        "posterior_sd": {
          "description": "posterior_sd parameter (float).",
          "type": "number"
        },
        "prob_positive": {
          "description": "prob_positive parameter (float).",
          "type": "number"
        },
        "prob_rope": {
          "description": "prob_rope parameter (Optional[float]).",
          "type": "number"
        },
        "rhat": {
          "description": "rhat parameter (float).",
          "type": "number"
        },
        "trace": {
          "description": "trace parameter.",
          "type": "string"
        }
      },
      "required": [
        "method",
        "estimand",
        "posterior_mean",
        "posterior_median",
        "posterior_sd",
        "hdi_lower",
        "hdi_upper",
        "prob_positive",
        "rhat",
        "ess",
        "n_obs"
      ],
      "type": "object"
    }
  },
  {
    "description": "Bayesian Marginal Treatment Effect result.",
    "name": "BayesianMTEResult",
    "parameters": {
      "properties": {
        "ate": {
          "default": NaN,
          "description": "ate parameter (float).",
          "type": "number"
        },
        "att": {
          "default": NaN,
          "description": "att parameter (float).",
          "type": "number"
        },
        "att_hdi_lower": {
          "default": NaN,
          "description": "att_hdi_lower parameter (float).",
          "type": "number"
        },
        "att_hdi_upper": {
          "default": NaN,
          "description": "att_hdi_upper parameter (float).",
          "type": "number"
        },
        "att_prob_positive": {
          "default": NaN,
          "description": "att_prob_positive parameter (float).",
          "type": "number"
        },
        "att_sd": {
          "default": NaN,
          "description": "att_sd parameter (float).",
          "type": "number"
        },
        "atu": {
          "default": NaN,
          "description": "atu parameter (float).",
          "type": "number"
        },
        "atu_hdi_lower": {
          "default": NaN,
          "description": "atu_hdi_lower parameter (float).",
          "type": "number"
        },
        "atu_hdi_upper": {
          "default": NaN,
          "description": "atu_hdi_upper parameter (float).",
          "type": "number"
        },
        "atu_prob_positive": {
          "default": NaN,
          "description": "atu_prob_positive parameter (float).",
          "type": "number"
        },
        "atu_sd": {
          "default": NaN,
          "description": "atu_sd parameter (float).",
          "type": "number"
        },
        "ess": {
          "description": "ess parameter (float).",
          "type": "number"
        },
        "estimand": {
          "description": "estimand parameter (str).",
          "type": "string"
        },
        "hdi_lower": {
          "description": "hdi_lower parameter (float).",
          "type": "number"
        },
        "hdi_prob": {
          "default": 0.95,
          "description": "hdi_prob parameter (float).",
          "type": "number"
        },
        "hdi_upper": {
          "description": "hdi_upper parameter (float).",
          "type": "number"
        },
        "method": {
          "description": "Estimator or algorithm variant to use.",
          "type": "string"
        },
        "model_info": {
          "default": null,
          "description": "model_info parameter (Dict[str, Any]).",
          "type": "object"
        },
        "mte_curve": {
          "default": null,
          "description": "mte_curve parameter (pd.DataFrame).",
          "type": "string"
        },
        "n_obs": {
          "description": "Number of obs.",
          "type": "integer"
        },
        "posterior_mean": {
          "description": "posterior_mean parameter (float).",
          "type": "number"
        },
        "posterior_median": {
          "description": "posterior_median parameter (float).",
          "type": "number"
        },
        "posterior_sd": {
          "description": "posterior_sd parameter (float).",
          "type": "number"
        },
        "prob_positive": {
          "description": "prob_positive parameter (float).",
          "type": "number"
        },
        "prob_rope": {
          "description": "prob_rope parameter (Optional[float]).",
          "type": "number"
        },
        "rhat": {
          "description": "rhat parameter (float).",
          "type": "number"
        },
        "selection": {
          "default": "uniform",
          "description": "selection parameter (str).",
          "type": "string"
        },
        "trace": {
          "description": "trace parameter.",
          "type": "string"
        },
        "u_grid": {
          "description": "Grid of u values to evaluate.",
          "type": "string"
        }
      },
      "required": [
        "method",
        "estimand",
        "posterior_mean",
        "posterior_median",
        "posterior_sd",
        "hdi_lower",
        "hdi_upper",
        "prob_positive",
        "rhat",
        "ess",
        "n_obs"
      ],
      "type": "object"
    }
  },
  {
    "description": "Uniform weight = 1 on every grid point. Validation: validated evidence tier (known-truth, reference, external-parity, or Monte Carlo artifact).",
    "name": "policy_weight_ate",
    "parameters": {
      "properties": {},
      "required": [],
      "type": "object"
    }
  },
  {
    "description": "Weight = 1 on ``[u_lo, u_hi]``; 0 elsewhere. Validation: validated evidence tier (known-truth, reference, external-parity, or Monte Carlo artifact).",
    "name": "policy_weight_subsidy",
    "parameters": {
      "properties": {
        "u_hi": {
          "description": "Band endpoints on the propensity (``U_D``) scale. Both must lie in ``[0, 1]`` and ``u_lo < u_hi``.",
          "type": "number"
        },
        "u_lo": {
          "description": "Band endpoints on the propensity (``U_D``) scale. Both must lie in ``[0, 1]`` and ``u_lo < u_hi``.",
          "type": "number"
        }
      },
      "required": [
        "u_lo",
        "u_hi"
      ],
      "type": "object"
    }
  },
  {
    "description": "**Stylised** PRTE weights -- convenience builder, NOT the",
    "name": "policy_weight_prte",
    "parameters": {
      "properties": {
        "shift": {
          "description": "Size of the propensity-scale shift. Must be in ``(-1, 1)`` and non-zero.",
          "type": "number"
        }
      },
      "required": [
        "shift"
      ],
      "type": "object"
    }
  },
  {
    "description": "Marginal PRTE at a specific propensity level ``u_star``. Validation: validated evidence tier (known-truth, reference, external-parity, or Monte Carlo artifact).",
    "name": "policy_weight_marginal",
    "parameters": {
      "properties": {
        "bandwidth": {
          "default": 0.05,
          "description": "Half-width of the averaging window.",
          "type": "number"
        },
        "u_star": {
          "description": "Target propensity level, in ``[0, 1]``.",
          "type": "number"
        }
      },
      "required": [
        "u_star"
      ],
      "type": "object"
    }
  },
  {
    "description": "True **CHV-2011 PRTE** weights from the observed propensity Validation: validated evidence tier (known-truth, reference, external-parity, or Monte Carlo artifact).",
    "name": "policy_weight_observed_prte",
    "parameters": {
      "properties": {
        "bw_method": {
          "description": "Passed to :class:`scipy.stats.gaussian_kde`. ``None`` uses Scott's rule, which is a good default for smooth propensity densities.",
          "type": "number"
        },
        "propensity_sample": {
          "description": "1-D array of observed propensity scores in ``[0, 1]``. Typical source: ``sp.bayes_mte(...).model_info['propensity']`` or a direct logit fit on your sample.",
          "type": "string"
        },
        "shift": {
          "description": "Policy-scale propensity shift. Must be non-zero and in ``(-1, 1)``. Positive = expand treatment uptake by ``shift`` at every propensity level; negative = contraction.",
          "type": "number"
        }
      },
      "required": [
        "propensity_sample",
        "shift"
      ],
      "type": "object"
    }
  },
  {
    "description": "Treatment-Agnostic Representation Network (Shalit et al. 2017).",
    "name": "TARNet",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level.",
          "type": "number"
        },
        "batch_size": {
          "default": 256,
          "description": "batch_size parameter (int).",
          "type": "integer"
        },
        "covariates": {
          "description": "Covariates.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "dropout": {
          "default": 0.1,
          "description": "dropout parameter (float).",
          "type": "number"
        },
        "early_stopping": {
          "default": false,
          "description": "early_stopping parameter (bool).",
          "type": "boolean"
        },
        "epochs": {
          "default": 300,
          "description": "Training epochs.",
          "type": "integer"
        },
        "head_layers": {
          "default": [
            100
          ],
          "description": "Outcome head hidden layer sizes.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "learning_rate": {
          "default": 0.001,
          "description": "learning_rate parameter (float).",
          "type": "number"
        },
        "min_delta": {
          "default": 0.0001,
          "description": "min_delta parameter (float).",
          "type": "number"
        },
        "n_bootstrap": {
          "default": 500,
          "description": "Number of bootstrap replications.",
          "type": "integer"
        },
        "patience": {
          "default": 20,
          "description": "patience parameter (int).",
          "type": "integer"
        },
        "random_state": {
          "default": 42,
          "description": "Random seed or RandomState for reproducible stochastic steps.",
          "type": "integer"
        },
        "repr_layers": {
          "default": [
            200,
            200
          ],
          "description": "Representation network hidden layer sizes.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "treat": {
          "description": "Binary treatment variable (0/1).",
          "type": "string"
        },
        "validation_fraction": {
          "default": 0.0,
          "description": "validation_fraction parameter (float).",
          "type": "number"
        },
        "verbose": {
          "default": false,
          "description": "verbose parameter (bool).",
          "type": "boolean"
        },
        "weight_decay": {
          "default": 0.0001,
          "description": "L2 regularisation.",
          "type": "number"
        },
        "y": {
          "description": "Outcome variable.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "treat",
        "covariates"
      ],
      "type": "object"
    }
  },
  {
    "description": "Counterfactual Regression Network (Shalit et al. 2017).",
    "name": "CFRNet",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "batch_size": {
          "default": 256,
          "description": "batch_size parameter (int).",
          "type": "integer"
        },
        "covariates": {
          "description": "Covariate matrix, DataFrame, or column names.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "dropout": {
          "default": 0.1,
          "description": "dropout parameter (float).",
          "type": "number"
        },
        "early_stopping": {
          "default": false,
          "description": "early_stopping parameter (bool).",
          "type": "boolean"
        },
        "epochs": {
          "default": 300,
          "description": "epochs parameter (int).",
          "type": "integer"
        },
        "head_layers": {
          "default": [
            100
          ],
          "description": "head_layers parameter (Tuple[int, ...]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "ipm_weight": {
          "default": 1.0,
          "description": "Weight for the IPM regularisation term.",
          "type": "number"
        },
        "learning_rate": {
          "default": 0.001,
          "description": "learning_rate parameter (float).",
          "type": "number"
        },
        "min_delta": {
          "default": 0.0001,
          "description": "min_delta parameter (float).",
          "type": "number"
        },
        "n_bootstrap": {
          "default": 500,
          "description": "Number of bootstrap replications.",
          "type": "integer"
        },
        "patience": {
          "default": 20,
          "description": "patience parameter (int).",
          "type": "integer"
        },
        "random_state": {
          "default": 42,
          "description": "Random seed or RandomState for reproducible stochastic steps.",
          "type": "integer"
        },
        "repr_layers": {
          "default": [
            200,
            200
          ],
          "description": "repr_layers parameter (Tuple[int, ...]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "treat": {
          "description": "Binary treatment variable (0/1).",
          "type": "string"
        },
        "validation_fraction": {
          "default": 0.0,
          "description": "validation_fraction parameter (float).",
          "type": "number"
        },
        "verbose": {
          "default": false,
          "description": "verbose parameter (bool).",
          "type": "boolean"
        },
        "weight_decay": {
          "default": 0.0001,
          "description": "weight_decay parameter (float).",
          "type": "number"
        },
        "y": {
          "description": "Outcome variable.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "treat",
        "covariates"
      ],
      "type": "object"
    }
  },
  {
    "description": "DragonNet: Targeted Regularisation (Shi et al. 2019).",
    "name": "DragonNet",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "batch_size": {
          "default": 256,
          "description": "batch_size parameter (int).",
          "type": "integer"
        },
        "covariates": {
          "description": "Covariate matrix, DataFrame, or column names.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "dropout": {
          "default": 0.1,
          "description": "dropout parameter (float).",
          "type": "number"
        },
        "early_stopping": {
          "default": false,
          "description": "early_stopping parameter (bool).",
          "type": "boolean"
        },
        "epochs": {
          "default": 300,
          "description": "epochs parameter (int).",
          "type": "integer"
        },
        "head_layers": {
          "default": [
            100
          ],
          "description": "head_layers parameter (Tuple[int, ...]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "learning_rate": {
          "default": 0.001,
          "description": "learning_rate parameter (float).",
          "type": "number"
        },
        "min_delta": {
          "default": 0.0001,
          "description": "min_delta parameter (float).",
          "type": "number"
        },
        "n_bootstrap": {
          "default": 500,
          "description": "Number of bootstrap replications.",
          "type": "integer"
        },
        "patience": {
          "default": 20,
          "description": "patience parameter (int).",
          "type": "integer"
        },
        "propensity_weight": {
          "default": 1.0,
          "description": "Weight for propensity cross-entropy loss (beta).",
          "type": "number"
        },
        "random_state": {
          "default": 42,
          "description": "Random seed or RandomState for reproducible stochastic steps.",
          "type": "integer"
        },
        "repr_layers": {
          "default": [
            200,
            200
          ],
          "description": "repr_layers parameter (Tuple[int, ...]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "targeted_reg_weight": {
          "default": 1.0,
          "description": "Weight for targeted regularisation term (gamma).",
          "type": "number"
        },
        "treat": {
          "description": "Binary treatment variable (0/1).",
          "type": "string"
        },
        "validation_fraction": {
          "default": 0.0,
          "description": "validation_fraction parameter (float).",
          "type": "number"
        },
        "verbose": {
          "default": false,
          "description": "verbose parameter (bool).",
          "type": "boolean"
        },
        "weight_decay": {
          "default": 0.0001,
          "description": "weight_decay parameter (float).",
          "type": "number"
        },
        "y": {
          "description": "Outcome variable.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "treat",
        "covariates"
      ],
      "type": "object"
    }
  },
  {
    "description": "Return unit-level neural causal predictions as a tidy DataFrame.",
    "name": "neural_effects_frame",
    "parameters": {
      "properties": {
        "result": {
          "description": "Result returned by ``sp.tarnet``, ``sp.cfrnet``, or ``sp.dragonnet``.",
          "type": "string"
        },
        "sort_by": {
          "description": "Sort rows by a diagnostic column.",
          "enum": [
            "cate",
            "propensity"
          ],
          "type": "string"
        }
      },
      "required": [
        "result"
      ],
      "type": "object"
    }
  },
  {
    "description": "Return a one-row summary table for a neural causal result.",
    "name": "neural_summary_frame",
    "parameters": {
      "properties": {
        "result": {
          "description": "result parameter.",
          "type": "string"
        }
      },
      "required": [
        "result"
      ],
      "type": "object"
    }
  },
  {
    "description": "Return per-epoch training diagnostics if recorded.",
    "name": "neural_training_frame",
    "parameters": {
      "properties": {
        "result": {
          "description": "result parameter.",
          "type": "string"
        }
      },
      "required": [
        "result"
      ],
      "type": "object"
    }
  },
  {
    "description": "Render a neural causal result to GitHub-flavoured Markdown.",
    "name": "neural_causal_to_markdown",
    "parameters": {
      "properties": {
        "digits": {
          "description": "digits parameter (Optional[int]).",
          "type": "integer"
        },
        "effects_head": {
          "default": 20,
          "description": "effects_head parameter (int).",
          "type": "integer"
        },
        "path": {
          "description": "path parameter (Optional[str]).",
          "type": "string"
        },
        "result": {
          "description": "result parameter.",
          "type": "string"
        }
      },
      "required": [
        "result"
      ],
      "type": "object"
    }
  },
  {
    "description": "Render a neural causal result to a compact HTML report.",
    "name": "neural_causal_to_html",
    "parameters": {
      "properties": {
        "digits": {
          "description": "digits parameter (Optional[int]).",
          "type": "integer"
        },
        "effects_head": {
          "default": 50,
          "description": "effects_head parameter (int).",
          "type": "integer"
        },
        "path": {
          "description": "path parameter (Optional[str]).",
          "type": "string"
        },
        "result": {
          "description": "result parameter.",
          "type": "string"
        }
      },
      "required": [
        "result"
      ],
      "type": "object"
    }
  },
  {
    "description": "Write a multi-sheet Excel workbook for a neural causal result.",
    "name": "neural_causal_to_excel",
    "parameters": {
      "properties": {
        "digits": {
          "default": 6,
          "description": "digits parameter (int).",
          "type": "integer"
        },
        "path": {
          "description": "path parameter (str).",
          "type": "string"
        },
        "result": {
          "description": "result parameter.",
          "type": "string"
        }
      },
      "required": [
        "result",
        "path"
      ],
      "type": "object"
    }
  },
  {
    "description": "Plot diagnostics for TARNet/CFRNet/DragonNet results.",
    "name": "neural_causal_plot",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.72,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "ax": {
          "description": "ax parameter.",
          "type": "string"
        },
        "bins": {
          "default": 30,
          "description": "bins parameter (int).",
          "type": "integer"
        },
        "color": {
          "default": "#2563EB",
          "description": "color parameter (str).",
          "type": "string"
        },
        "control_color": {
          "default": "#64748B",
          "description": "control_color parameter (str).",
          "type": "string"
        },
        "figsize": {
          "default": [
            8,
            5
          ],
          "description": "figsize parameter.",
          "type": "string"
        },
        "result": {
          "description": "Neural causal result.",
          "type": "string"
        },
        "title": {
          "description": "title parameter (Optional[str]).",
          "type": "string"
        },
        "treated_color": {
          "default": "#DC2626",
          "description": "treated_color parameter (str).",
          "type": "string"
        },
        "type": {
          "default": "cate",
          "description": "``\"cate\"`` draws a CATE histogram, ``\"effects\"`` draws sorted unit-level CATEs, ``\"propensity\"`` draws DragonNet overlap, and ``\"loss\"`` draws per-epoch training/validation loss.",
          "enum": [
            "cate",
            "effects",
            "propensity",
            "loss"
          ],
          "type": "string"
        }
      },
      "required": [
        "result"
      ],
      "type": "object"
    }
  },
  {
    "description": "Learn a DAG from data using NOTEARS (Zheng et al. 2018). Validation: validated evidence tier (known-truth, reference, external-parity, or Monte Carlo artifact).",
    "name": "notears",
    "parameters": {
      "properties": {
        "data": {
          "description": "Observational data (n_samples x d_variables).",
          "type": "string"
        },
        "h_tol": {
          "default": 1e-08,
          "description": "Convergence threshold for acyclicity constraint h(W).",
          "type": "number"
        },
        "lambda1": {
          "default": 0.1,
          "description": "L1 penalty weight for sparsity. Higher = sparser graph.",
          "type": "number"
        },
        "max_iter": {
          "default": 100,
          "description": "Maximum augmented Lagrangian iterations.",
          "type": "integer"
        },
        "random_state": {
          "default": 42,
          "description": "Random seed.",
          "type": "integer"
        },
        "rho_max": {
          "default": 1e+16,
          "description": "Maximum penalty parameter rho.",
          "type": "number"
        },
        "variables": {
          "description": "Column names to use. If None, uses all numeric columns.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "w_threshold": {
          "default": 0.3,
          "description": "Threshold for pruning small edge weights. Edges with |W_ij| < w_threshold are removed.",
          "type": "number"
        }
      },
      "required": [
        "data"
      ],
      "type": "object"
    }
  },
  {
    "description": "NOTEARS: Continuous optimization for DAG structure learning.",
    "name": "NOTEARS",
    "parameters": {
      "properties": {
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "h_tol": {
          "default": 1e-08,
          "description": "h_tol parameter (float).",
          "type": "number"
        },
        "lambda1": {
          "default": 0.1,
          "description": "lambda1 parameter (float).",
          "type": "number"
        },
        "max_iter": {
          "default": 100,
          "description": "Maximum number of optimisation iterations.",
          "type": "integer"
        },
        "random_state": {
          "default": 42,
          "description": "Random seed or RandomState for reproducible stochastic steps.",
          "type": "integer"
        },
        "rho_max": {
          "default": 1e+16,
          "description": "rho_max parameter (float).",
          "type": "number"
        },
        "variables": {
          "description": "variables parameter (Optional[List[str]]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "w_threshold": {
          "default": 0.3,
          "description": "w_threshold parameter (float).",
          "type": "number"
        }
      },
      "required": [
        "data"
      ],
      "type": "object"
    }
  },
  {
    "description": "Learn causal structure using the PC algorithm. Validation: validated evidence tier (known-truth, reference, external-parity, or Monte Carlo artifact).",
    "name": "pc_algorithm",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for conditional independence tests. Lower alpha = sparser graph (fewer edges).",
          "type": "number"
        },
        "ci_test": {
          "default": "fisherz",
          "description": "Conditional independence test: 'fisherz' (partial correlation) or 'hsic' (kernel-based, non-linear).",
          "type": "string"
        },
        "data": {
          "description": "Observational data (n_samples x d_variables).",
          "type": "string"
        },
        "forbidden": {
          "description": "Background knowledge: edges that must NOT appear in the final graph (treated as undirected -- both ``(a, b)`` and ``(b, a)`` are forbidden when either is given). The skeleton phase keeps these absent regardless of CI test outcomes.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "max_cond_size": {
          "description": "Maximum conditioning set size. If None, goes up to d-2.",
          "type": "integer"
        },
        "required": {
          "description": "Background knowledge: directed edges ``a -> b`` that must appear in the CPDAG. The skeleton phase preserves them regardless of CI rejection, and the orientation phase pins their direction.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "variables": {
          "description": "Column names to use. If None, uses all numeric columns.",
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "required": [
        "data"
      ],
      "type": "object"
    }
  },
  {
    "description": "PC Algorithm for causal discovery.",
    "name": "PCAlgorithm",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "ci_test": {
          "default": "fisherz",
          "description": "ci_test parameter (str).",
          "type": "string"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "forbidden": {
          "description": "forbidden parameter (Optional[List[Tuple[str, str]]]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "max_cond_size": {
          "description": "max_cond_size parameter (Optional[int]).",
          "type": "integer"
        },
        "required": {
          "description": "required parameter (Optional[List[Tuple[str, str]]]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "variables": {
          "description": "variables parameter (Optional[List[str]]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "required": [
        "data"
      ],
      "type": "object"
    }
  },
  {
    "description": "Targeted Maximum Likelihood Estimation.",
    "name": "TMLE",
    "parameters": {
      "properties": {
        "Q": {
          "description": "Q parameter (Optional[np.ndarray]).",
          "type": "string"
        },
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "covariates": {
          "description": "Covariate matrix, DataFrame, or column names.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "estimand": {
          "default": "ATE",
          "description": "estimand parameter (str).",
          "type": "string"
        },
        "fluctuation": {
          "default": "single",
          "description": "fluctuation parameter (str).",
          "type": "string"
        },
        "fold_indices": {
          "description": "Opt-in cross-validated TMLE partition (labels ``0..K-1``, one per row of ``data``); see :func:`statspai.tmle`. ``None`` (default) fits Q and g on the full sample, i.e. not cross-fitted.",
          "type": "string"
        },
        "g1W": {
          "description": "g1W parameter (Optional[np.ndarray]).",
          "type": "string"
        },
        "n_folds": {
          "default": 5,
          "description": "Number of cross-fitting or cross-validation folds.",
          "type": "integer"
        },
        "outcome_library": {
          "description": "outcome_library parameter (Optional[List[BaseEstimator]]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "propensity_bounds": {
          "default": [
            0.025,
            0.975
          ],
          "description": "propensity_bounds parameter (Tuple[float, float]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "propensity_library": {
          "description": "propensity_library parameter (Optional[List[BaseEstimator]]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "q_bound": {
          "default": 1e-05,
          "description": "q_bound parameter (float).",
          "type": "number"
        },
        "random_state": {
          "default": 42,
          "description": "Random seed or RandomState for reproducible stochastic steps.",
          "type": "integer"
        },
        "treat": {
          "description": "Treatment indicator or first-treatment-period column.",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "treat",
        "covariates"
      ],
      "type": "object"
    }
  },
  {
    "description": "Fit a Super Learner ensemble. Validation: validated evidence tier (known-truth, reference, external-parity, or Monte Carlo artifact).",
    "name": "super_learner",
    "parameters": {
      "properties": {
        "X": {
          "description": "Feature matrix or covariate DataFrame.",
          "type": "string"
        },
        "library": {
          "description": "Candidate learners. If None, uses a default library.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "n_folds": {
          "default": 5,
          "description": "Cross-validation folds.",
          "type": "integer"
        },
        "random_state": {
          "default": 42,
          "description": "Random seed or RandomState for reproducible stochastic steps.",
          "type": "integer"
        },
        "task": {
          "default": "regression",
          "description": "'regression' or 'classification'.",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "X",
        "y"
      ],
      "type": "object"
    }
  },
  {
    "description": "Super Learner ensemble (van der Laan et al. 2007).",
    "name": "SuperLearner",
    "parameters": {
      "properties": {
        "library": {
          "description": "Candidate learners. If None, uses a default diverse library.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "n_folds": {
          "default": 5,
          "description": "Number of cross-validation folds.",
          "type": "integer"
        },
        "random_state": {
          "default": 42,
          "description": "Random seed or RandomState for reproducible stochastic steps.",
          "type": "integer"
        },
        "task": {
          "default": "regression",
          "description": "'regression' or 'classification'.",
          "type": "string"
        }
      },
      "required": [],
      "type": "object"
    }
  },
  {
    "description": "L1-penalised HAL regressor (sklearn-compatible duck-typed API).",
    "name": "HALRegressor",
    "parameters": {
      "properties": {
        "cv": {
          "default": 5,
          "description": "Folds for the CV penalty search (used only when ``lambda_`` is None).",
          "type": "integer"
        },
        "lambda_": {
          "description": "L1 penalty. ``None`` selects it via cross-validation.",
          "type": "number"
        },
        "max_anchors_per_col": {
          "default": 40,
          "description": "Cap on step-function anchor points per feature.",
          "type": "integer"
        },
        "random_state": {
          "default": 0,
          "description": "Random seed or RandomState for reproducible stochastic steps.",
          "type": "integer"
        }
      },
      "required": [],
      "type": "object"
    }
  },
  {
    "description": "L1-penalised HAL logistic classifier (sklearn-compatible duck-typed API).",
    "name": "HALClassifier",
    "parameters": {
      "properties": {
        "C": {
          "default": 1.0,
          "description": "Inverse L1 penalty (larger = less shrinkage), as in scikit-learn.",
          "type": "number"
        },
        "max_anchors_per_col": {
          "default": 40,
          "description": "Cap on step-function anchor points per feature.",
          "type": "integer"
        },
        "random_state": {
          "default": 0,
          "description": "Random seed or RandomState for reproducible stochastic steps.",
          "type": "integer"
        }
      },
      "required": [],
      "type": "object"
    }
  },
  {
    "description": "Doubly-robust policy-tree -- article-facing alias. Validation: certified parity evidence.",
    "name": "policy_tree",
    "parameters": {
      "properties": {
        "X": {
          "description": "Feature matrix or covariate DataFrame.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "covariates": {
          "description": "Covariate matrix, DataFrame, or column names.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "d": {
          "description": "d parameter (Optional[str]).",
          "type": "string"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "depth": {
          "description": "depth parameter (Optional[int]).",
          "type": "integer"
        },
        "max_depth": {
          "description": "max_depth parameter (Optional[int]).",
          "type": "integer"
        },
        "treat": {
          "description": "Treatment indicator or first-treatment-period column.",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y"
      ],
      "type": "object"
    }
  },
  {
    "description": "Optimal depth-limited policy tree.",
    "name": "PolicyTree",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "covariates": {
          "description": "Covariate matrix, DataFrame, or column names.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "max_depth": {
          "default": 2,
          "description": "max_depth parameter (int).",
          "type": "integer"
        },
        "min_leaf_size": {
          "default": 25,
          "description": "min_leaf_size parameter (int).",
          "type": "integer"
        },
        "n_folds": {
          "default": 5,
          "description": "Number of cross-fitting or cross-validation folds.",
          "type": "integer"
        },
        "policy_covariates": {
          "description": "policy_covariates parameter (Optional[List[str]]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "random_state": {
          "default": 42,
          "description": "Random seed or RandomState for reproducible stochastic steps.",
          "type": "integer"
        },
        "scores": {
          "description": "scores parameter (Optional[numpy.ndarray]).",
          "type": "string"
        },
        "search": {
          "default": "auto",
          "description": "search parameter (str).",
          "type": "string"
        },
        "split_step": {
          "default": 1,
          "description": "split_step parameter (int).",
          "type": "integer"
        },
        "treat": {
          "description": "Treatment indicator or first-treatment-period column.",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "treat",
        "covariates"
      ],
      "type": "object"
    }
  },
  {
    "description": "Result of :func:`policy_tree`.",
    "name": "PolicyTreeResult",
    "parameters": {
      "properties": {
        "fraction_treated": {
          "description": "fraction_treated parameter (float).",
          "type": "number"
        },
        "max_depth": {
          "default": 0,
          "description": "max_depth parameter (int).",
          "type": "integer"
        },
        "n_obs": {
          "description": "Number of obs.",
          "type": "integer"
        },
        "policy": {
          "description": "policy parameter (ndarray).",
          "type": "string"
        },
        "policy_covariates": {
          "default": [],
          "description": "policy_covariates parameter (Tuple[str, ...]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "rules": {
          "description": "rules parameter (str).",
          "type": "string"
        },
        "scores": {
          "description": "scores parameter (Optional[numpy.ndarray]).",
          "type": "string"
        },
        "search_mode": {
          "default": "exact",
          "description": "search_mode parameter (str).",
          "type": "string"
        },
        "tree": {
          "description": "tree parameter (PolicyTree).",
          "type": "string"
        },
        "value_gain": {
          "description": "value_gain parameter (float).",
          "type": "number"
        },
        "value_gain_se": {
          "default": NaN,
          "description": "value_gain_se parameter (float).",
          "type": "number"
        },
        "value_policy": {
          "description": "value_policy parameter (float).",
          "type": "number"
        },
        "value_policy_ci": {
          "default": [
            NaN,
            NaN
          ],
          "description": "value_policy_ci parameter (Tuple[float, float]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "value_policy_se": {
          "default": NaN,
          "description": "value_policy_se parameter (float).",
          "type": "number"
        },
        "value_treat_all": {
          "description": "value_treat_all parameter (float).",
          "type": "number"
        },
        "value_treat_none": {
          "description": "value_treat_none parameter (float).",
          "type": "number"
        }
      },
      "required": [
        "tree",
        "policy",
        "value_treat_all",
        "value_treat_none",
        "value_policy",
        "value_gain",
        "fraction_treated",
        "rules",
        "n_obs"
      ],
      "type": "object"
    }
  },
  {
    "description": "Evaluate the value of a treatment policy relative to treating no one. Validation: certified parity evidence.",
    "name": "policy_value",
    "parameters": {
      "properties": {
        "policy": {
          "description": "Policy recommendations (0 or 1; a probability of treatment gives the value of the stochastic policy). Same length as ``scores``, or a scalar for a constant policy.",
          "type": "string"
        },
        "scores": {
          "description": "Doubly robust scores (AIPW pseudo-outcomes for treatment). Positive scores indicate the individual benefits from treatment. Must be one-dimensional.",
          "type": "string"
        }
      },
      "required": [
        "scores",
        "policy"
      ],
      "type": "object"
    }
  },
  {
    "description": "Rank-and-treat policy under a budget constraint.",
    "name": "policy_targeting",
    "parameters": {
      "properties": {
        "budget": {
          "description": "Maximum number of units that can be treated. Mutually exclusive with ``frac``.",
          "type": "integer"
        },
        "cate": {
          "description": "Per-unit effect estimates -- a raw array, a ``metalearner()`` / ``tarnet()`` result, or a fitted ``causal_forest()`` model (training-sample effects are used).",
          "type": "string"
        },
        "frac": {
          "description": "Maximum *fraction* of units that can be treated (in ``(0, 1]``).",
          "type": "number"
        },
        "min_effect": {
          "default": 0.0,
          "description": "Never treat a unit whose predicted effect is at or below this threshold, even with budget left over -- treating predicted non-responders wastes budget and can do harm.",
          "type": "number"
        }
      },
      "required": [
        "cate"
      ],
      "type": "object"
    }
  },
  {
    "description": "Compute conformal prediction intervals for CATE. Validation: validated evidence tier (known-truth, reference, external-parity, or Monte Carlo artifact).",
    "name": "conformal_cate",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Miscoverage level. Intervals have (1-alpha) coverage.",
          "type": "number"
        },
        "calib_fraction": {
          "default": 0.25,
          "description": "Fraction of data used for calibration.",
          "type": "number"
        },
        "covariates": {
          "description": "Covariate names.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "data": {
          "description": "Input data.",
          "type": "string"
        },
        "model": {
          "description": "Outcome model for mu_d(X). If None, uses GBM.",
          "type": "string"
        },
        "random_state": {
          "default": 42,
          "description": "Random seed or RandomState for reproducible stochastic steps.",
          "type": "integer"
        },
        "treat": {
          "description": "Binary treatment variable (0/1).",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "treat",
        "covariates"
      ],
      "type": "object"
    }
  },
  {
    "description": "Conformal prediction intervals for individual treatment effects.",
    "name": "ConformalCATE",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "calib_fraction": {
          "default": 0.25,
          "description": "calib_fraction parameter (float).",
          "type": "number"
        },
        "covariates": {
          "description": "Covariate matrix, DataFrame, or column names.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "model": {
          "description": "Model variant or parameterisation to fit.",
          "type": "string"
        },
        "random_state": {
          "default": 42,
          "description": "Random seed or RandomState for reproducible stochastic steps.",
          "type": "integer"
        },
        "treat": {
          "description": "Treatment indicator or first-treatment-period column.",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "treat",
        "covariates"
      ],
      "type": "object"
    }
  },
  {
    "description": "Estimate heterogeneous treatment effects using Bayesian Causal Forest. Validation: validated evidence tier (known-truth, reference, external-parity, or Monte Carlo artifact).",
    "name": "bcf",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level.",
          "type": "number"
        },
        "covariates": {
          "description": "Covariate names.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "data": {
          "description": "Input data.",
          "type": "string"
        },
        "n_bootstrap": {
          "default": 200,
          "description": "Bootstrap iterations for uncertainty quantification.",
          "type": "integer"
        },
        "n_folds": {
          "default": 5,
          "description": "Cross-fitting folds for propensity estimation.",
          "type": "integer"
        },
        "n_trees_mu": {
          "default": 200,
          "description": "Number of trees for the prognostic function mu(X).",
          "type": "integer"
        },
        "n_trees_tau": {
          "default": 50,
          "description": "Number of trees for the treatment effect function tau(X). Fewer trees = stronger shrinkage toward homogeneous effects.",
          "type": "integer"
        },
        "random_state": {
          "default": 42,
          "description": "Random seed or RandomState for reproducible stochastic steps.",
          "type": "integer"
        },
        "treat": {
          "description": "Binary treatment variable (0/1).",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "treat",
        "covariates"
      ],
      "type": "object"
    }
  },
  {
    "description": "Bayesian Causal Forest estimator.",
    "name": "BayesianCausalForest",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "covariates": {
          "description": "Covariate matrix, DataFrame, or column names.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "n_bootstrap": {
          "default": 200,
          "description": "Number of bootstrap replications.",
          "type": "integer"
        },
        "n_folds": {
          "default": 5,
          "description": "Number of cross-fitting or cross-validation folds.",
          "type": "integer"
        },
        "n_trees_mu": {
          "default": 200,
          "description": "Number of trees mu.",
          "type": "integer"
        },
        "n_trees_tau": {
          "default": 50,
          "description": "Number of trees tau.",
          "type": "integer"
        },
        "random_state": {
          "default": 42,
          "description": "Random seed or RandomState for reproducible stochastic steps.",
          "type": "integer"
        },
        "treat": {
          "description": "Treatment indicator or first-treatment-period column.",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "treat",
        "covariates"
      ],
      "type": "object"
    }
  },
  {
    "description": "Estimate bunching at a policy threshold. Validation: validated evidence tier (known-truth, reference, external-parity, or Monte Carlo artifact).",
    "name": "bunching",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level.",
          "type": "number"
        },
        "bin_width": {
          "description": "Width of bins. If None, computed from data range / n_bins.",
          "type": "number"
        },
        "bunch_region": {
          "description": "(lower, upper) bounds of the bunching region. If None, uses [threshold - 2*bin_width, threshold + 2*bin_width].",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "data": {
          "description": "Input data.",
          "type": "string"
        },
        "design": {
          "default": "kink",
          "description": "'kink' or 'notch'.",
          "type": "string"
        },
        "dt": {
          "description": "Change in marginal tax rate at the kink (for elasticity). E.g., 0.10 for a 10pp increase.",
          "type": "number"
        },
        "exclude_region": {
          "description": "Same as bunch_region unless otherwise specified.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "n_bins": {
          "default": 50,
          "description": "Number of bins on each side of the threshold.",
          "type": "integer"
        },
        "n_bootstrap": {
          "default": 200,
          "description": "Bootstrap iterations for standard errors.",
          "type": "integer"
        },
        "poly_order": {
          "default": 7,
          "description": "Order of the counterfactual polynomial.",
          "type": "integer"
        },
        "random_state": {
          "default": 42,
          "description": "Random seed or RandomState for reproducible stochastic steps.",
          "type": "integer"
        },
        "running_var": {
          "description": "Name of the running variable (e.g., income).",
          "type": "string"
        },
        "threshold": {
          "description": "Policy threshold (kink/notch point).",
          "type": "number"
        }
      },
      "required": [
        "data",
        "running_var",
        "threshold"
      ],
      "type": "object"
    }
  },
  {
    "description": "Bunching estimator for kink/notch designs.",
    "name": "BunchingEstimator",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "bin_width": {
          "description": "bin_width parameter (Optional[float]).",
          "type": "number"
        },
        "bunch_region": {
          "description": "bunch_region parameter (Optional[Tuple[float, float]]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "design": {
          "default": "kink",
          "description": "design parameter (str).",
          "type": "string"
        },
        "dt": {
          "description": "dt parameter (Optional[float]).",
          "type": "number"
        },
        "exclude_region": {
          "description": "exclude_region parameter (Optional[Tuple[float, float]]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "n_bins": {
          "default": 50,
          "description": "Number of bins.",
          "type": "integer"
        },
        "n_bootstrap": {
          "default": 200,
          "description": "Number of bootstrap replications.",
          "type": "integer"
        },
        "poly_order": {
          "default": 7,
          "description": "poly_order parameter (int).",
          "type": "integer"
        },
        "random_state": {
          "default": 42,
          "description": "Random seed or RandomState for reproducible stochastic steps.",
          "type": "integer"
        },
        "running_var": {
          "description": "running_var parameter (str).",
          "type": "string"
        },
        "threshold": {
          "description": "threshold parameter (float).",
          "type": "number"
        }
      },
      "required": [
        "data",
        "running_var",
        "threshold"
      ],
      "type": "object"
    }
  },
  {
    "description": "Bunching at Notches estimator (Kleven & Waseem 2013). Validation: validated evidence tier (known-truth, reference, external-parity, or Monte Carlo artifact).",
    "name": "notch",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level.",
          "type": "number"
        },
        "bin_width": {
          "default": 500,
          "description": "Bin width for the histogram.",
          "type": "number"
        },
        "data": {
          "description": "Input dataset.",
          "type": "string"
        },
        "exclude_range": {
          "description": "Range around the notch to exclude from the counterfactual fit. If None, defaults to (notch_point - 3*bin_width, notch_point + 5*bin_width).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "n_boot": {
          "default": 500,
          "description": "Number of bootstrap replications for standard errors.",
          "type": "integer"
        },
        "notch_point": {
          "description": "Location of the notch in the running variable.",
          "type": "number"
        },
        "notch_size": {
          "description": "Size of the discontinuous jump (delta-tau). If provided, a structural elasticity is estimated.",
          "type": "number"
        },
        "poly_order": {
          "default": 7,
          "description": "Polynomial order for the counterfactual distribution.",
          "type": "integer"
        },
        "seed": {
          "default": 42,
          "description": "Random seed for reproducibility.",
          "type": "integer"
        },
        "x": {
          "description": "Running variable name (e.g., 'income').",
          "type": "string"
        }
      },
      "required": [
        "data",
        "x",
        "notch_point"
      ],
      "type": "object"
    }
  },
  {
    "description": "Result container for notch bunching analysis.",
    "name": "NotchResult",
    "parameters": {
      "properties": {
        "alpha": {
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "bin_centers": {
          "description": "bin_centers parameter (ndarray).",
          "type": "string"
        },
        "causal_result": {
          "description": "causal_result parameter (CausalResult).",
          "type": "string"
        },
        "ci": {
          "description": "ci parameter (Tuple[float, float]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "counterfactual": {
          "description": "counterfactual parameter (ndarray).",
          "type": "string"
        },
        "elasticity": {
          "description": "elasticity parameter (Optional[float]).",
          "type": "number"
        },
        "excess_bunching": {
          "description": "excess_bunching parameter (float).",
          "type": "number"
        },
        "marginal_buncher": {
          "description": "marginal_buncher parameter (float).",
          "type": "number"
        },
        "missing_mass": {
          "description": "missing_mass parameter (float).",
          "type": "number"
        },
        "n_obs": {
          "description": "Number of obs.",
          "type": "integer"
        },
        "notch_point": {
          "description": "notch_point parameter (float).",
          "type": "number"
        },
        "notch_size": {
          "description": "notch_size parameter (Optional[float]).",
          "type": "number"
        },
        "observed": {
          "description": "observed parameter (ndarray).",
          "type": "string"
        },
        "pvalue": {
          "description": "pvalue parameter (float).",
          "type": "number"
        },
        "se_bunching": {
          "description": "se_bunching parameter (float).",
          "type": "number"
        },
        "se_elasticity": {
          "description": "se_elasticity parameter (Optional[float]).",
          "type": "number"
        }
      },
      "required": [
        "excess_bunching",
        "missing_mass",
        "marginal_buncher",
        "elasticity",
        "se_bunching",
        "se_elasticity",
        "pvalue",
        "ci",
        "alpha",
        "notch_point",
        "bin_centers",
        "observed",
        "counterfactual",
        "n_obs",
        "causal_result"
      ],
      "type": "object"
    }
  },
  {
    "description": "Estimate treatment effects using matrix completion. Validation: certified parity evidence. Do NOT use when: the panel is nearly fully treated -- matrix completion needs a substantial observed-control block to recover the low-rank structure. Cost: Iterative soft-impute: one SVD of the N x T outcome matrix per iteration, i.e. O(max_iter x N x T x min(N,T)). n_bootstrap multiplies the whole loop -- this is the dominant cost on wide panels.",
    "name": "mc_panel",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level.",
          "type": "number"
        },
        "data": {
          "description": "Panel data in long format.",
          "type": "string"
        },
        "fixed_effects": {
          "default": "two-way",
          "description": "Unpenalised additive effects fitted alongside the low-rank component. ``\"two-way\"`` is the Athey et al. (2021) estimator and the default of R ``MCPanel`` (``to_estimate_u = to_estimate_v = TRUE``) and ``fect`` (``force = \"two-way\"``). ``\"none\"`` is pure soft-impute (``MCPanel`` with both switches off; no intercept, so the outcome level is shrunk towards zero); it is the estimator this function computed in StatsPAI <= 1.28.0.",
          "enum": [
            "two-way",
            "unit",
            "time"
          ],
          "type": "string"
        },
        "lambda_reg": {
          "description": "Singular-value threshold ``lambda`` on the objective above (the ``1/2``-scaled squared loss). The same minimiser is obtained from R ``MCPanel::mcnnm_fit(lambda_L = 2 * lambda / |Omega|)`` and from ``fect(method = \"mc\", CV = FALSE, lambda = lambda / (N * T))``; both conversions are reported in ``model_info``. If None, a heuristic (no reference implementation uses it) is applied: ``lambda = sd(Y[Omega]) * sqrt(max(N, T)) / 10``.",
          "type": "number"
        },
        "max_iter": {
          "default": 5000,
          "description": "Maximum soft-impute iterations. A ``RuntimeWarning`` is raised if the point-estimate fit hits the cap before ``tol``.",
          "type": "integer"
        },
        "max_rank": {
          "description": "Hard cap on the rank of the low-rank component (not part of the reference problem). If None, no constraint.",
          "type": "integer"
        },
        "n_bootstrap": {
          "default": 200,
          "description": "Unit-bootstrap replications for the standard error.",
          "type": "integer"
        },
        "random_state": {
          "default": 42,
          "description": "Random seed or RandomState for reproducible stochastic steps.",
          "type": "integer"
        },
        "time": {
          "description": "Time period variable.",
          "type": "string"
        },
        "tol": {
          "default": 1e-10,
          "description": "Convergence tolerance on the relative Frobenius change of the fitted counterfactual matrix.",
          "type": "number"
        },
        "treat": {
          "description": "Binary treatment indicator (0/1). Can be staggered.",
          "type": "string"
        },
        "unit": {
          "description": "Unit identifier variable.",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "unit",
        "time",
        "treat"
      ],
      "type": "object"
    }
  },
  {
    "description": "Matrix Completion for Causal Panels.",
    "name": "MCPanel",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "fixed_effects": {
          "default": "two-way",
          "description": "fixed_effects parameter (str).",
          "enum": [
            "two-way",
            "unit",
            "time"
          ],
          "type": "string"
        },
        "lambda_reg": {
          "description": "lambda_reg parameter (Optional[float]).",
          "type": "number"
        },
        "max_iter": {
          "default": 5000,
          "description": "Maximum number of optimisation iterations.",
          "type": "integer"
        },
        "max_rank": {
          "description": "max_rank parameter (Optional[int]).",
          "type": "integer"
        },
        "n_bootstrap": {
          "default": 200,
          "description": "Number of bootstrap replications.",
          "type": "integer"
        },
        "random_state": {
          "default": 42,
          "description": "Random seed or RandomState for reproducible stochastic steps.",
          "type": "integer"
        },
        "time": {
          "description": "Time period column.",
          "type": "string"
        },
        "tol": {
          "default": 1e-10,
          "description": "Numerical convergence tolerance.",
          "type": "number"
        },
        "treat": {
          "description": "Treatment indicator or first-treatment-period column.",
          "type": "string"
        },
        "unit": {
          "description": "Unit identifier column.",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "unit",
        "time",
        "treat"
      ],
      "type": "object"
    }
  },
  {
    "description": "Generalized Propensity Score dose-response estimator.",
    "name": "DoseResponse",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "covariates": {
          "description": "Covariate matrix, DataFrame, or column names.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "dose_range": {
          "description": "dose_range parameter (Optional[Tuple[float, float]]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "n_bootstrap": {
          "default": 200,
          "description": "Number of bootstrap replications.",
          "type": "integer"
        },
        "n_dose_points": {
          "default": 20,
          "description": "Number of dose points.",
          "type": "integer"
        },
        "outcome_model": {
          "description": "outcome_model parameter (Optional[BaseEstimator]).",
          "type": "string"
        },
        "random_state": {
          "default": 42,
          "description": "Random seed or RandomState for reproducible stochastic steps.",
          "type": "integer"
        },
        "treat": {
          "description": "Treatment indicator or first-treatment-period column.",
          "type": "string"
        },
        "treatment_model": {
          "description": "treatment_model parameter (Optional[BaseEstimator]).",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "treat",
        "covariates"
      ],
      "type": "object"
    }
  },
  {
    "description": "Compute Lee (2009) bounds for ATE under sample selection. Validation: certified parity evidence.",
    "name": "lee_bounds",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level.",
          "type": "number"
        },
        "covariates": {
          "description": "Reserved for covariate-tightened bounds, which are not implemented; a non-empty list is ignored with a ``UserWarning`` (it used to be ignored silently).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "data": {
          "description": "Input data (including units with missing outcomes).",
          "type": "string"
        },
        "n_bootstrap": {
          "default": 500,
          "description": "Bootstrap iterations for inference.",
          "type": "integer"
        },
        "random_state": {
          "default": 42,
          "description": "Random seed or RandomState for reproducible stochastic steps.",
          "type": "integer"
        },
        "se_method": {
          "default": "bootstrap",
          "description": "Standard errors of the two bounds. ``'analytic'`` is the asymptotic variance of Lee (2009, Proposition 3) exactly as Stata's ``leebounds`` (default ``vce(analytic)``) computes it; ``'bootstrap'`` resamples units ``n_bootstrap`` times.",
          "enum": [
            "bootstrap",
            "analytic"
          ],
          "type": "string"
        },
        "selection": {
          "description": "Binary selection/retention indicator (1 = observed, 0 = missing).",
          "type": "string"
        },
        "treat": {
          "description": "Binary treatment variable (0/1).",
          "type": "string"
        },
        "trimming": {
          "default": "quantile",
          "description": "How the ``p = |p1 - p0| / max(p1, p0)`` share is trimmed from the arm with higher retention. ``'quantile'`` is Lee's (2009) sample",
          "enum": [
            "quantile",
            "exact"
          ],
          "type": "string"
        },
        "y": {
          "description": "Outcome variable (may have NaN for selected-out units).",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "treat",
        "selection"
      ],
      "type": "object"
    }
  },
  {
    "description": "Compute Manski (1990) worst-case bounds on ATE. Validation: certified parity evidence.",
    "name": "manski_bounds",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "assumption": {
          "default": "none",
          "description": "Additional assumption: - 'none': no assumptions (widest bounds) - 'mtr': Monotone Treatment Response (Y(1) >= Y(0) for all): ``[0, worst-case upper]``. - 'mts': Monotone Treatment Selection with positive selection, ``E[Y(d) | D=1] >= E[Y(d) | D=0]`` (Manski & Pepper 2000): ``[worst-case lower, E[Y|D=1] - E[Y|D=0]]``. - 'mts_mtr': MTS and MTR jointly: ``[0, E[Y|D=1] - E[Y|D=0]]``. Raises ``ValueError`` when ``E[Y|D=1] < E[Y|D=0]``: the two assumptions are then jointly refuted and the identified set is empty. Before 1.29, ``'mts'`` returned the ``'mts_mtr'`` interval (i.e. silently imposed MTR as well) and, when the naive difference was negative, swapped its endpoints instead of reporting the empty set. These match the \"worst case\", \"MTS positive selection\" and \"MTS and MTR positive selection\" rows of Stata's ``tebounds`` at zero misclassification.",
          "type": "string"
        },
        "data": {
          "description": "Input data.",
          "type": "string"
        },
        "n_bootstrap": {
          "default": 500,
          "description": "Number of bootstrap replications.",
          "type": "integer"
        },
        "random_state": {
          "default": 42,
          "description": "Random seed or RandomState for reproducible stochastic steps.",
          "type": "integer"
        },
        "treat": {
          "description": "Binary treatment variable (0/1).",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable.",
          "type": "string"
        },
        "y_lower": {
          "description": "Known lower bound of the outcome. If None, uses observed min.",
          "type": "number"
        },
        "y_upper": {
          "description": "Known upper bound of the outcome. If None, uses observed max.",
          "type": "number"
        }
      },
      "required": [
        "data",
        "y",
        "treat"
      ],
      "type": "object"
    }
  },
  {
    "description": "Result container for partial identification / bounds estimation.",
    "name": "BoundsResult",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "ci_lower": {
          "description": "ci_lower parameter (Tuple[float, float]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "ci_upper": {
          "description": "ci_upper parameter (Tuple[float, float]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "lower": {
          "description": "lower parameter (float).",
          "type": "number"
        },
        "method": {
          "description": "Estimator or algorithm variant to use.",
          "type": "string"
        },
        "model_info": {
          "default": null,
          "description": "model_info parameter (Dict[str, Any]).",
          "type": "object"
        },
        "n_obs": {
          "default": 0,
          "description": "Number of obs.",
          "type": "integer"
        },
        "se_lower": {
          "description": "se_lower parameter (float).",
          "type": "number"
        },
        "se_upper": {
          "description": "se_upper parameter (float).",
          "type": "number"
        },
        "upper": {
          "description": "upper parameter (float).",
          "type": "number"
        }
      },
      "required": [
        "lower",
        "upper",
        "se_lower",
        "se_upper",
        "ci_lower",
        "ci_upper",
        "method"
      ],
      "type": "object"
    }
  },
  {
    "description": "Horowitz-Manski (2000) bounds conditioning on covariates. Validation: certified parity evidence.",
    "name": "horowitz_manski",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "covariates": {
          "description": "Covariates to condition on (discretised via quartiles for continuous variables).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "data": {
          "description": "Input data.",
          "type": "string"
        },
        "n_boot": {
          "default": 500,
          "description": "Bootstrap replications.",
          "type": "integer"
        },
        "random_state": {
          "default": 42,
          "description": "Random seed or RandomState for reproducible stochastic steps.",
          "type": "integer"
        },
        "treatment": {
          "description": "Binary treatment variable (0/1).",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable.",
          "type": "string"
        },
        "y_lower": {
          "description": "Known lower bound of Y. Defaults to observed min.",
          "type": "number"
        },
        "y_upper": {
          "description": "Known upper bound of Y. Defaults to observed max.",
          "type": "number"
        }
      },
      "required": [
        "data",
        "y",
        "treatment",
        "covariates"
      ],
      "type": "object"
    }
  },
  {
    "description": "Nevo-Rosen (2012) bounds for LATE under imperfect instruments.",
    "name": "iv_bounds",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "assumption": {
          "default": "monotone_iv",
          "description": "- ``'monotone_iv'``: instrument has same-sign direct effect as through the treatment (Nevo-Rosen Proposition 2). - ``'less_than_late'``: direct effect of Z on Y is weakly less than the indirect effect (tighter).",
          "type": "string"
        },
        "controls": {
          "description": "Control variables (residualized out via OLS).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "instrument": {
          "description": "Instrument variable (binary).",
          "type": "string"
        },
        "n_boot": {
          "default": 500,
          "description": "Number of bootstrap replications.",
          "type": "integer"
        },
        "random_state": {
          "default": 42,
          "description": "Random seed or RandomState for reproducible stochastic steps.",
          "type": "integer"
        },
        "treatment": {
          "description": "Endogenous treatment (binary).",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "treatment",
        "instrument"
      ],
      "type": "object"
    }
  },
  {
    "description": "Oster (2019) coefficient stability bounds and delta* computation. Validation: certified parity evidence.",
    "name": "oster_delta",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "delta_range": {
          "default": [
            -2.0,
            2.0
          ],
          "description": "Range of proportional selection parameter delta.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "n_boot": {
          "default": 500,
          "description": "Number of bootstrap replications.",
          "type": "integer"
        },
        "n_grid": {
          "default": 200,
          "description": "Grid points for delta in the identified set computation.",
          "type": "integer"
        },
        "r_max": {
          "default": 1.3,
          "description": "Maximum R-squared assumption. A value in ``(R_full, 1]`` is used as R_max itself. A value above 1 cannot be an R-squared and is read as a multiplier of the controlled R-squared, so the default 1.3 gives Oster's recommended ``R_max = min(1, 1.3 * R_full)``; ``<= 0`` means the same. A value in ``(0, R_full]`` falls back to that recommendation with a warning.",
          "type": "number"
        },
        "random_state": {
          "default": 42,
          "description": "Random seed or RandomState for reproducible stochastic steps.",
          "type": "integer"
        },
        "x_base": {
          "description": "Key treatment/variable(s) of interest.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "x_controls": {
          "description": "Additional controls whose inclusion tightens identification.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Outcome variable.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "x_base",
        "x_controls"
      ],
      "type": "object"
    }
  },
  {
    "description": "Lee (2009) bounds for ATE under sample selection, optionally Validation: validated evidence tier (known-truth, reference, external-parity, or Monte Carlo artifact).",
    "name": "selection_bounds",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "covariates": {
          "description": "Covariates to condition on for tighter (conditional) bounds.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "method": {
          "default": "conditional",
          "description": "- ``'conditional'``: compute Lee bounds within covariate strata and average (tighter). - ``'unconditional'``: standard Lee bounds ignoring covariates.",
          "type": "string"
        },
        "n_boot": {
          "default": 500,
          "description": "Number of bootstrap replications.",
          "type": "integer"
        },
        "random_state": {
          "default": 42,
          "description": "Random seed or RandomState for reproducible stochastic steps.",
          "type": "integer"
        },
        "selection": {
          "description": "Binary indicator: 1 = outcome observed, 0 = missing.",
          "type": "string"
        },
        "treatment": {
          "description": "Binary treatment (0/1).",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable (may have NaN when selection=0).",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "treatment",
        "selection"
      ],
      "type": "object"
    }
  },
  {
    "description": "Masten-Poirier (2021) breakdown frontier for qualitative conclusions. Validation: validated evidence tier (known-truth, reference, external-parity, or Monte Carlo artifact).",
    "name": "breakdown_frontier",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "assumption": {
          "default": "parallel_trends",
          "description": "Label for the identifying assumption being relaxed. Currently supports a generic linear violation model applicable to ``'parallel_trends'``, ``'exclusion_restriction'``, or ``'selection_on_observables'``.",
          "type": "string"
        },
        "estimate": {
          "description": "Point estimate of the treatment effect.",
          "type": "number"
        },
        "max_violation": {
          "default": 0.1,
          "description": "Maximum magnitude of the assumption violation to explore.",
          "type": "number"
        },
        "n_grid": {
          "default": 100,
          "description": "Grid resolution for the frontier.",
          "type": "integer"
        },
        "se": {
          "description": "Standard error of the estimate.",
          "type": "number"
        }
      },
      "required": [
        "estimate",
        "se"
      ],
      "type": "object"
    }
  },
  {
    "description": "Spillover / interference effects estimator.",
    "name": "SpilloverEstimator",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "cluster": {
          "description": "Cluster identifier column for clustered standard errors.",
          "type": "string"
        },
        "covariates": {
          "description": "Covariate matrix, DataFrame, or column names.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "exposure_fn": {
          "default": "fraction",
          "description": "exposure_fn parameter (str).",
          "type": "string"
        },
        "n_bootstrap": {
          "default": 500,
          "description": "Number of bootstrap replications.",
          "type": "integer"
        },
        "random_state": {
          "default": 42,
          "description": "Random seed or RandomState for reproducible stochastic steps.",
          "type": "integer"
        },
        "treat": {
          "description": "Treatment indicator or first-treatment-period column.",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "treat",
        "cluster"
      ],
      "type": "object"
    }
  },
  {
    "description": "G-estimation for a multi-stage dynamic treatment regime. Validation: certified parity evidence.",
    "name": "g_estimation",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "covariates_by_stage": {
          "description": "Covariates (tailoring variables) available at each stage. covariates_by_stage[k] are the variables available when deciding treatment k.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "data": {
          "description": "Input data.",
          "type": "string"
        },
        "n_bootstrap": {
          "default": 500,
          "description": "Number of bootstrap replications.",
          "type": "integer"
        },
        "propensity_covariates": {
          "description": "Covariates for propensity model at each stage. If None, uses covariates_by_stage.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "propensity_model": {
          "default": "logit",
          "description": "Model for ``E[A_k | propensity covariates]`` in the g-estimating equation ``sum (A_k - A_hat_k) (Y~ - X_k beta - psi_k A_k) = 0`` (solved jointly with ``sum X_k (Y~ - X_k beta - psi_k A_k) = 0``, ``X_k`` = ``[1, covariates_by_stage[k]]``). ``'logit'`` is the logistic MLE that R ``DTRreg(method = \"gest\")`` fits for a binary treatment. ``'linear'`` is a linear-probability OLS fit; with propensity covariates equal to the stage covariates it reduces ``psi_k`` to the OLS coefficient on ``A_k`` in ``Y~ ~ X_k + A_k``.",
          "enum": [
            "logit",
            "linear"
          ],
          "type": "string"
        },
        "random_state": {
          "default": 42,
          "description": "Random seed or RandomState for reproducible stochastic steps.",
          "type": "integer"
        },
        "treatments": {
          "description": "Treatment variables at each stage, in temporal order. E.g., ['A1', 'A2'] for a two-stage DTR.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Final outcome variable.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "treatments",
        "covariates_by_stage"
      ],
      "type": "object"
    }
  },
  {
    "description": "G-estimation for dynamic treatment regimes via SNMM.",
    "name": "GEstimation",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "covariates_by_stage": {
          "description": "covariates_by_stage parameter (List[List[str]]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "n_bootstrap": {
          "default": 500,
          "description": "Number of bootstrap replications.",
          "type": "integer"
        },
        "propensity_covariates": {
          "description": "propensity_covariates parameter (Optional[List[List[str]]]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "propensity_model": {
          "default": "logit",
          "description": "propensity_model parameter (str).",
          "type": "string"
        },
        "random_state": {
          "default": 42,
          "description": "Random seed or RandomState for reproducible stochastic steps.",
          "type": "integer"
        },
        "treatments": {
          "description": "treatments parameter (List[str]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "treatments",
        "covariates_by_stage"
      ],
      "type": "object"
    }
  },
  {
    "description": "Multi-valued treatment effects estimator.",
    "name": "MultiTreatment",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "covariates": {
          "description": "Covariate matrix, DataFrame, or column names.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "n_bootstrap": {
          "default": 500,
          "description": "Number of bootstrap replications.",
          "type": "integer"
        },
        "outcome_model": {
          "default": "gbm",
          "description": "outcome_model parameter (str).",
          "type": "string"
        },
        "random_state": {
          "default": 42,
          "description": "Random seed or RandomState for reproducible stochastic steps.",
          "type": "integer"
        },
        "reference": {
          "description": "reference parameter (Optional[int]).",
          "type": "integer"
        },
        "se_method": {
          "default": "bootstrap",
          "description": "se_method parameter (str).",
          "type": "string"
        },
        "treat": {
          "description": "Treatment indicator or first-treatment-period column.",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "treat",
        "covariates"
      ],
      "type": "object"
    }
  },
  {
    "description": "Holds all specification curve outputs.",
    "name": "SpecCurveResult",
    "parameters": {
      "properties": {
        "choice_dims": {
          "description": "choice_dims parameter (List[str]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "median_estimate": {
          "description": "median_estimate parameter (float).",
          "type": "number"
        },
        "n_specs": {
          "description": "Number of specs.",
          "type": "integer"
        },
        "results_df": {
          "description": "results_df parameter (pd.DataFrame).",
          "type": "string"
        },
        "share_positive": {
          "description": "share_positive parameter (float).",
          "type": "number"
        },
        "share_significant": {
          "description": "share_significant parameter (float).",
          "type": "number"
        },
        "x": {
          "description": "Primary running variable, regressor, or feature input for this estimator.",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "results_df",
        "x",
        "y",
        "n_specs",
        "median_estimate",
        "share_significant",
        "share_positive",
        "choice_dims"
      ],
      "type": "object"
    }
  },
  {
    "description": "Run an automated battery of robustness checks.",
    "name": "robustness_report",
    "parameters": {
      "properties": {
        "cluster_var": {
          "description": "Column for clustered SE check.",
          "type": "string"
        },
        "data": {
          "description": "Analysis dataset.",
          "type": "string"
        },
        "drop_controls": {
          "description": "Baseline controls to drop (one-by-one) for sensitivity.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "extra_controls": {
          "description": "Additional controls to add (one-by-one) beyond baseline.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "formula": {
          "description": "Baseline regression formula, e.g. ``\"y ~ x1 + x2 + x3\"``.",
          "type": "string"
        },
        "subsets": {
          "description": "Named boolean masks for subsample checks.",
          "type": "object"
        },
        "trim_pct": {
          "description": "Drop observations beyond this percentile from both tails. E.g. ``0.01`` trims top and bottom 1 %.",
          "type": "number"
        },
        "winsor_levels": {
          "description": "Winsorization percentiles, e.g. ``[0.01, 0.05]``.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "x": {
          "description": "Key explanatory variable whose estimate stability is assessed.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "formula",
        "x"
      ],
      "type": "object"
    }
  },
  {
    "description": "Container for robustness report results.",
    "name": "RobustnessResult",
    "parameters": {
      "properties": {
        "baseline_estimate": {
          "description": "baseline_estimate parameter (float).",
          "type": "number"
        },
        "baseline_se": {
          "description": "baseline_se parameter (float).",
          "type": "number"
        },
        "n_checks": {
          "description": "Number of checks.",
          "type": "integer"
        },
        "results_df": {
          "description": "results_df parameter (pd.DataFrame).",
          "type": "string"
        },
        "x": {
          "description": "Primary running variable, regressor, or feature input for this estimator.",
          "type": "string"
        }
      },
      "required": [
        "results_df",
        "x",
        "baseline_estimate",
        "baseline_se",
        "n_checks"
      ],
      "type": "object"
    }
  },
  {
    "description": "Run subgroup heterogeneity analysis with forest plot. Validation: certified parity evidence.",
    "name": "subgroup_analysis",
    "parameters": {
      "properties": {
        "by": {
          "description": "Mapping of *display name* -> *column name* for grouping.",
          "type": "object"
        },
        "data": {
          "description": "Analysis dataset.",
          "type": "string"
        },
        "formula": {
          "description": "Regression formula, e.g. ``\"wage ~ education + experience\"``.",
          "type": "string"
        },
        "robust": {
          "default": "hc1",
          "description": "Standard error type for subgroup regressions.",
          "type": "string"
        },
        "x": {
          "description": "Key explanatory variable.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "formula",
        "x",
        "by"
      ],
      "type": "object"
    }
  },
  {
    "description": "Container for subgroup heterogeneity analysis.",
    "name": "SubgroupResult",
    "parameters": {
      "properties": {
        "het_tests": {
          "description": "het_tests parameter (Dict[str, Dict[str, float]]).",
          "type": "object"
        },
        "overall_estimate": {
          "description": "overall_estimate parameter (float).",
          "type": "number"
        },
        "overall_se": {
          "description": "overall_se parameter (float).",
          "type": "number"
        },
        "results_df": {
          "description": "results_df parameter (pd.DataFrame).",
          "type": "string"
        },
        "x": {
          "description": "Primary running variable, regressor, or feature input for this estimator.",
          "type": "string"
        }
      },
      "required": [
        "results_df",
        "x",
        "overall_estimate",
        "overall_se",
        "het_tests"
      ],
      "type": "object"
    }
  },
  {
    "description": "Declare a complex survey design.",
    "name": "SurveyDesign",
    "parameters": {
      "properties": {
        "cluster": {
          "description": "Primary sampling unit (PSU) variable (column name).",
          "type": "string"
        },
        "data": {
          "description": "Survey microdata.",
          "type": "string"
        },
        "fpc": {
          "description": "Finite population correction, as in R ``survey::svydesign(fpc=)`` and Stata ``svyset, fpc()``: either the population number of PSUs in the stratum (all values >= 1, at least one > 1) or the first-stage sampling fraction (all values <= 1). For an element-sampled design (``cluster=None``) the PSUs are the rows, so the count form is the population number of elements. Mixing the two forms, or a count smaller than the number of sampled PSUs in the stratum, raises ``ValueError``.",
          "type": "string"
        },
        "lonely_psu": {
          "description": "How a stratum with a single sampled PSU enters the variance, with the semantics of R's ``options(survey.lonely.psu=)`` for a single-stage design: * ``\"remove\"`` (default) / ``\"certainty\"`` -- the stratum contributes zero (Stata ``singleunit(certainty)``); * ``\"adjust\"`` -- the single PSU total is centred at the grand mean of PSU totals instead of its stratum mean (Stata ``singleunit(centered)``); * ``\"average\"`` -- the variance summed over strata with >1 PSU is scaled by #strata / #strata-with->1-PSU (Stata ``singleunit(scaled)``); * ``\"fail\"`` -- raise ``ValueError`` (R's default). Unless ``lonely_psu`` is given explicitly, a lonely stratum emits a ``UserWarning`` (Stata's default reports a missing SE instead).",
          "enum": [
            "remove",
            "certainty",
            "adjust",
            "average",
            "fail"
          ],
          "type": "string"
        },
        "nest": {
          "default": false,
          "description": "If True, PSU ids are relabelled to be unique within strata. PSUs are always identified *within* their stratum for variance and degrees-of-freedom purposes (Stata ``svyset`` convention), so with ``nest=False`` ids that repeat across strata only trigger a warning (R's ``svydesign`` refuses them without ``nest=TRUE``).",
          "type": "boolean"
        },
        "strata": {
          "description": "Stratification variable (column name).",
          "type": "string"
        },
        "weights": {
          "description": "Sampling weights (inverse probability). If *str*, column name in *data*.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "weights"
      ],
      "type": "object"
    }
  },
  {
    "description": "Survey-weighted mean with design-corrected standard errors. Validation: certified parity evidence.",
    "name": "svymean",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "CIs use the t distribution with the design df (#PSU - #strata), as Stata ``svy:`` and R ``confint(..., df = degf(design))``; R's ``confint.svystat`` default is the normal quantile.",
          "type": "number"
        },
        "deff": {
          "default": "wor",
          "description": "Denominator of the design effect: SRS without replacement with N = sum of weights (R ``deff=TRUE``; Stata ``estat effects`` when an fpc is declared) or with replacement (R ``deff=\"replace\"``; Stata ``estat effects`` without fpc).",
          "enum": [
            "wor",
            "replace"
          ],
          "type": "string"
        },
        "design": {
          "description": "design parameter ('SurveyDesign').",
          "type": "string"
        },
        "variables": {
          "description": "Column name(s) in the design's data.",
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "required": [
        "variables",
        "design"
      ],
      "type": "object"
    }
  },
  {
    "description": "Survey-weighted total with design-corrected standard errors. Validation: certified parity evidence.",
    "name": "svytotal",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "CIs use the t distribution with the design df (#PSU - #strata), as Stata ``svy:`` and R ``confint(..., df = degf(design))``; R's ``confint.svystat`` default is the normal quantile.",
          "type": "number"
        },
        "deff": {
          "default": "wor",
          "description": "Denominator of the design effect: SRS without replacement with N = sum of weights (R ``deff=TRUE``; Stata ``estat effects`` when an fpc is declared) or with replacement (R ``deff=\"replace\"``; Stata ``estat effects`` without fpc).",
          "enum": [
            "wor",
            "replace"
          ],
          "type": "string"
        },
        "design": {
          "description": "design parameter ('SurveyDesign').",
          "type": "string"
        },
        "variables": {
          "description": "variables parameter (Union[str, List[str]]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "required": [
        "variables",
        "design"
      ],
      "type": "object"
    }
  },
  {
    "description": "Survey-weighted generalised linear model. Validation: certified parity evidence.",
    "name": "svyglm",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "design": {
          "description": "design parameter ('SurveyDesign').",
          "type": "string"
        },
        "dof": {
          "default": "design",
          "description": "Degrees of freedom for t statistics, p-values and CIs. ``\"design\"`` (default): #PSU - #strata, as Stata ``svy:``. ``\"residual\"``: #PSU - #strata + 1 - #coefficients, as R ``summary.svyglm`` / ``confint.svyglm``.",
          "enum": [
            "design",
            "residual"
          ],
          "type": "string"
        },
        "family": {
          "default": "gaussian",
          "description": "``\"gaussian\"``, ``\"binomial\"`` (logistic), or ``\"poisson\"``. Canonical links; the sandwich is ``A^{-1} B A^{-1}`` with ``A = X' diag(w V(mu)) X`` and ``B`` the design covariance of the score totals ``w (y - mu) x`` (R ``survey:::svy.varcoef``).",
          "type": "string"
        },
        "formula": {
          "description": "``\"y ~ x1 + x2\"`` style formula.",
          "type": "string"
        }
      },
      "required": [
        "formula",
        "design"
      ],
      "type": "object"
    }
  },
  {
    "description": "A directed acyclic graph for causal reasoning.",
    "name": "DAG",
    "parameters": {
      "properties": {
        "spec": {
          "default": "",
          "description": "Edge specification. Supported formats: - ``\"X -> Y; Z -> X; Z -> Y\"`` (semicolon-separated) - ``\"X -> Y\\n Z -> X\\n Z -> Y\"`` (newline-separated) - ``\"X -> Y, Z -> X, Z -> Y\"`` (comma-separated) - Bidirected (latent common cause): ``\"X <-> Y\"`` adds a latent node ``_L_X_Y`` with edges to both X and Y.",
          "type": "string"
        }
      },
      "required": [],
      "type": "object"
    }
  },
  {
    "description": "List available classic DAG examples.",
    "name": "dag_examples",
    "parameters": {
      "properties": {},
      "required": [],
      "type": "object"
    }
  },
  {
    "description": "Return hand-tuned node positions for a named example DAG.",
    "name": "dag_example_positions",
    "parameters": {
      "properties": {
        "name": {
          "description": "name parameter (str).",
          "type": "string"
        }
      },
      "required": [
        "name"
      ],
      "type": "object"
    }
  },
  {
    "description": "Run a classic DAG simulation from Cunningham (2021, ch. 3).",
    "name": "dag_simulate",
    "parameters": {
      "properties": {
        "n": {
          "default": 10000,
          "description": "Number of observations (default 10000).",
          "type": "integer"
        },
        "name": {
          "description": "``'discrimination'`` or ``'movie_star'``.",
          "type": "string"
        },
        "seed": {
          "default": 42,
          "description": "Random seed for reproducibility.",
          "type": "integer"
        }
      },
      "required": [
        "name"
      ],
      "type": "object"
    }
  },
  {
    "description": "Compute statistical power (or solve for sample size) for a causal",
    "name": "power",
    "parameters": {
      "properties": {
        "design": {
          "description": "One of ``'rct'``, ``'did'``, ``'rd'``, ``'iv'``, ``'cluster_rct'``, ``'ols'``.",
          "type": "string"
        },
        "effect_size": {
          "description": "Standardised effect size. Pass *None* to solve for MDE (use :func:`mde` for a cleaner interface).",
          "type": "string"
        },
        "n": {
          "description": "Sample size. For cluster_rct this is *n_clusters*. Pass *None* to solve for the minimum n that achieves *power_target*.",
          "type": "string"
        },
        "power_target": {
          "description": "Target power (e.g. 0.80). When *n* is None, the function performs a binary search for the minimum n that achieves this power.",
          "type": "number"
        }
      },
      "required": [
        "design"
      ],
      "type": "object"
    }
  },
  {
    "description": "Container for power analysis results.",
    "name": "PowerResult",
    "parameters": {
      "properties": {
        "design": {
          "description": "design parameter (str).",
          "type": "string"
        },
        "effect_size": {
          "description": "effect_size parameter.",
          "type": "string"
        },
        "n": {
          "description": "n parameter.",
          "type": "string"
        },
        "params": {
          "description": "params parameter (Dict[str, Any]).",
          "type": "object"
        },
        "power_val": {
          "description": "power_val parameter.",
          "type": "string"
        }
      },
      "required": [
        "power_val",
        "n",
        "effect_size",
        "design",
        "params"
      ],
      "type": "object"
    }
  },
  {
    "description": "Power for a two-arm Randomised Controlled Trial. Validation: certified parity evidence.",
    "name": "power_rct",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level (two-sided).",
          "type": "number"
        },
        "effect_size": {
          "description": "Standardised effect size (delta / sigma).",
          "type": "string"
        },
        "n": {
          "description": "Total sample size (treatment + control).",
          "type": "string"
        },
        "ratio": {
          "default": 1.0,
          "description": "Treatment / control allocation ratio (1 = equal allocation).",
          "type": "number"
        },
        "sigma": {
          "default": 1.0,
          "description": "Outcome standard deviation (default 1 for standardised effect).",
          "type": "number"
        }
      },
      "required": [
        "n",
        "effect_size"
      ],
      "type": "object"
    }
  },
  {
    "description": "Power for Difference-in-Differences.",
    "name": "power_did",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level (two-sided).",
          "type": "number"
        },
        "effect_size": {
          "description": "Standardised effect size.",
          "type": "string"
        },
        "n": {
          "description": "Total number of units (treated + control).",
          "type": "string"
        },
        "n_periods": {
          "description": "Total number of time periods.",
          "type": "integer"
        },
        "n_treated_periods": {
          "description": "Number of post-treatment periods.",
          "type": "integer"
        },
        "rho": {
          "default": 0.5,
          "description": "First-order autocorrelation of errors (0-1).",
          "type": "number"
        },
        "sigma": {
          "default": 1.0,
          "description": "Error standard deviation.",
          "type": "number"
        }
      },
      "required": [
        "n",
        "effect_size",
        "n_periods",
        "n_treated_periods"
      ],
      "type": "object"
    }
  },
  {
    "description": "Power for Regression Discontinuity designs.",
    "name": "power_rd",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level.",
          "type": "number"
        },
        "bandwidth": {
          "description": "Bandwidth around the cutoff. If *None*, defaults to 0.5 (half the running-variable range on each side).",
          "type": "number"
        },
        "density_at_cutoff": {
          "default": 1.0,
          "description": "Estimated density of the running variable at the cutoff (default 1.0 for a uniform running variable on [0,1]).",
          "type": "number"
        },
        "effect_size": {
          "description": "Standardised effect size at the cutoff.",
          "type": "string"
        },
        "kernel": {
          "default": "triangular",
          "description": "Kernel used for local weighting.",
          "enum": [
            "triangular",
            "uniform",
            "epanechnikov"
          ],
          "type": "string"
        },
        "n": {
          "description": "Total sample size in the data.",
          "type": "string"
        },
        "sigma": {
          "default": 1.0,
          "description": "Conditional outcome std dev near the cutoff.",
          "type": "number"
        }
      },
      "required": [
        "n",
        "effect_size"
      ],
      "type": "object"
    }
  },
  {
    "description": "Power for Instrumental Variables / 2SLS estimation.",
    "name": "power_iv",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level.",
          "type": "number"
        },
        "effect_size": {
          "description": "Standardised effect size of the endogenous variable.",
          "type": "string"
        },
        "first_stage_f": {
          "description": "First-stage F-statistic. If provided, power is adjusted for instrument weakness: effective_power ~ power_ols * F / (F + 1).",
          "type": "number"
        },
        "n": {
          "description": "Sample size.",
          "type": "string"
        },
        "r2_z": {
          "description": "R-squared of the first-stage regression. Alternative to *first_stage_f*; if both are given, *first_stage_f* takes precedence.",
          "type": "number"
        },
        "sigma": {
          "default": 1.0,
          "description": "Error standard deviation.",
          "type": "number"
        }
      },
      "required": [
        "n",
        "effect_size"
      ],
      "type": "object"
    }
  },
  {
    "description": "Power for a Cluster-Randomised Controlled Trial. Validation: certified parity evidence.",
    "name": "power_cluster_rct",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level.",
          "type": "number"
        },
        "cluster_size": {
          "description": "Average number of individuals per cluster.",
          "type": "number"
        },
        "effect_size": {
          "description": "Standardised effect size.",
          "type": "string"
        },
        "icc": {
          "description": "Intra-cluster correlation coefficient.",
          "type": "number"
        },
        "n_clusters": {
          "description": "Total number of clusters (treatment + control).",
          "type": "string"
        },
        "sigma": {
          "default": 1.0,
          "description": "Individual-level outcome standard deviation.",
          "type": "number"
        }
      },
      "required": [
        "n_clusters",
        "cluster_size",
        "effect_size",
        "icc"
      ],
      "type": "object"
    }
  },
  {
    "description": "Power for OLS regression (single coefficient of interest). Validation: validated evidence tier (known-truth, reference, external-parity, or Monte Carlo artifact).",
    "name": "power_ols",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level.",
          "type": "number"
        },
        "effect_size": {
          "description": "Standardised effect of the variable of interest.",
          "type": "string"
        },
        "n": {
          "description": "Sample size.",
          "type": "string"
        },
        "n_covariates": {
          "default": 0,
          "description": "Number of other covariates in the model.",
          "type": "integer"
        },
        "r2_other": {
          "default": 0.0,
          "description": "R-squared attributable to other covariates (reduces residual variance and thus improves power).",
          "type": "number"
        },
        "sigma": {
          "default": 1.0,
          "description": "Outcome standard deviation.",
          "type": "number"
        }
      },
      "required": [
        "n",
        "effect_size"
      ],
      "type": "object"
    }
  },
  {
    "description": "Compute the Minimum Detectable Effect (MDE) for a given design. Validation: certified parity evidence.",
    "name": "mde",
    "parameters": {
      "properties": {
        "design": {
          "description": "Research design (see :func:`power`).",
          "type": "string"
        },
        "n": {
          "description": "Sample size. For ``'cluster_rct'`` this is *n_clusters*.",
          "type": "integer"
        },
        "power_target": {
          "default": 0.8,
          "description": "Desired power (default 0.80).",
          "type": "number"
        }
      },
      "required": [
        "design"
      ],
      "type": "object"
    }
  },
  {
    "description": "Power (or sample size) to detect a difference between two proportions. Validation: certified parity evidence.",
    "name": "power_two_proportions",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level.",
          "type": "number"
        },
        "alternative": {
          "default": "two-sided",
          "description": "Test sidedness.",
          "enum": [
            "two-sided",
            "one-sided"
          ],
          "type": "string"
        },
        "n": {
          "description": "Total sample size (both groups). Pass ``None`` together with ``power_target`` to solve for the smallest ``n`` achieving that power.",
          "type": "string"
        },
        "p1": {
          "default": 0.5,
          "description": "Outcome probabilities in group 1 (reference) and group 2.",
          "type": "number"
        },
        "p2": {
          "default": 0.5,
          "description": "Outcome probabilities in group 1 (reference) and group 2.",
          "type": "number"
        },
        "power_target": {
          "description": "Desired power; when supplied with ``n=None`` the function returns the required total sample size.",
          "type": "number"
        },
        "ratio": {
          "default": 1.0,
          "description": "Allocation ratio ``n2 / n1`` (1.0 = equal allocation).",
          "type": "number"
        }
      },
      "required": [],
      "type": "object"
    }
  },
  {
    "description": "Power (or sample size) for a two-arm log-rank / survival comparison. Validation: certified parity evidence.",
    "name": "power_logrank",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level.",
          "type": "number"
        },
        "alternative": {
          "default": "two-sided",
          "description": "alternative parameter (str).",
          "enum": [
            "two-sided",
            "one-sided"
          ],
          "type": "string"
        },
        "hazard_ratio": {
          "default": 0.5,
          "description": "Hazard ratio between the two arms (must be > 0, != 1).",
          "type": "number"
        },
        "n": {
          "description": "Total sample size. ``None`` + ``power_target`` solves for ``n``.",
          "type": "string"
        },
        "power_target": {
          "description": "Desired power; solve for ``n`` when supplied with ``n=None``.",
          "type": "number"
        },
        "prob_event": {
          "default": 1.0,
          "description": "Probability that a randomly chosen subject is observed to have the event during follow-up (the overall event rate). ``D = n*prob_event``.",
          "type": "number"
        },
        "ratio": {
          "default": 1.0,
          "description": "Allocation ratio ``n2 / n1``.",
          "type": "number"
        }
      },
      "required": [],
      "type": "object"
    }
  },
  {
    "description": "Power (or number of cases) for an unmatched case-control study. Validation: certified parity evidence.",
    "name": "power_case_control",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level.",
          "type": "number"
        },
        "alternative": {
          "default": "two-sided",
          "description": "alternative parameter (str).",
          "enum": [
            "two-sided",
            "one-sided"
          ],
          "type": "string"
        },
        "exposure_prevalence": {
          "default": 0.3,
          "description": "Exposure prevalence among controls (the source-population exposure probability), in (0, 1).",
          "type": "number"
        },
        "n_cases": {
          "description": "Number of cases. ``None`` + ``power_target`` solves for the number of cases.",
          "type": "string"
        },
        "odds_ratio": {
          "default": 2.0,
          "description": "Exposure odds ratio to detect (must be > 0, != 1).",
          "type": "number"
        },
        "power_target": {
          "description": "Desired power; solve for the number of cases when ``n_cases=None``.",
          "type": "number"
        },
        "ratio": {
          "default": 1.0,
          "description": "Number of controls per case.",
          "type": "number"
        },
        "test": {
          "default": "chi2",
          "description": "``\"wald\"``: the unpooled two-proportion z test, ``Phi(|p1 - p0| / se1 - z)`` with ``se1`` the unpooled standard error (upper tail only). ``\"chi2\"``: the Pearson chi-squared test, whose null standard error uses the pooled proportion, with both tails counted when two-sided -- Stata ``power twoproportions`` (controls as group 1, cases as group 2). .. versionchanged:: 1.30.0 The default moved from ``\"wald\"`` (no package reference) to ``\"chi2\"``, the test of Stata ``power twoproportions`` and R ``power.prop.test``. Pass ``test=\"wald\"`` for the old power.",
          "enum": [
            "chi2",
            "wald"
          ],
          "type": "string"
        }
      },
      "required": [],
      "type": "object"
    }
  },
  {
    "description": "Oaxaca-Blinder decomposition of mean outcome gaps. Validation: certified parity evidence.",
    "name": "oaxaca",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for p-values.",
          "type": "number"
        },
        "data": {
          "description": "Input dataset.",
          "type": "string"
        },
        "detail": {
          "default": true,
          "description": "If True, compute variable-level contributions to the explained component.",
          "type": "boolean"
        },
        "group": {
          "description": "Binary group indicator (0 = Group A, 1 = Group B).",
          "type": "string"
        },
        "reference": {
          "default": 0,
          "description": "Reference coefficient vector beta*: - ``0`` -- Group A coefficients (beta_A). The \"explained\" part uses Group A's returns as the benchmark. - ``1`` -- Group B coefficients (beta_B). - ``'pooled'`` -- Pooled OLS (Neumark 1988). - ``'cotton'`` -- Sample-size weighted average (Cotton 1988). - ``'reimers'`` -- Equal-weighted average (Reimers 1983).",
          "enum": [
            "pooled",
            "cotton",
            "reimers"
          ],
          "type": "integer"
        },
        "x": {
          "description": "Covariate names.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Outcome variable name.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "group",
        "x"
      ],
      "type": "object"
    }
  },
  {
    "description": "Gelbach (2016) decomposition of omitted variable bias. Validation: certified parity evidence.",
    "name": "gelbach",
    "parameters": {
      "properties": {
        "added_x": {
          "description": "Variables added to obtain the full (long) specification.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "alpha": {
          "default": 0.05,
          "description": "Significance level.",
          "type": "number"
        },
        "base_x": {
          "description": "Variables in the base (short) specification.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "data": {
          "description": "Input dataset.",
          "type": "string"
        },
        "var_of_interest": {
          "description": "Which base variable's coefficient change to decompose. Defaults to the first element of ``base_x``.",
          "type": "string"
        },
        "vce": {
          "default": "robust",
          "description": "Heteroskedasticity-robust or homoskedastic covariance, as ``b1x2 ..., robust`` and plain ``b1x2``. Standard errors were a two-term delta method that treated the auxiliary and long-regression estimates as independent; ``b1x2`` keeps their covariance. On ``cps_wage`` that moved the SEs by 0.03%, but the old formula has no guarantee of being that close. Point estimates are unchanged.",
          "enum": [
            "robust",
            "nonrobust"
          ],
          "type": "string"
        },
        "y": {
          "description": "Outcome variable name.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "base_x",
        "added_x"
      ],
      "type": "object"
    }
  },
  {
    "description": "Result container for Oaxaca-Blinder decomposition.",
    "name": "OaxacaResult",
    "parameters": {
      "properties": {
        "detailed": {
          "description": "detailed parameter (pd.DataFrame).",
          "type": "string"
        },
        "group_stats": {
          "description": "group_stats parameter (Dict[str, Any]).",
          "type": "object"
        },
        "overall": {
          "description": "overall parameter (Dict[str, float]).",
          "type": "object"
        },
        "reference": {
          "description": "reference parameter (Union[str, int]).",
          "type": "integer"
        },
        "var_names": {
          "description": "var_names parameter (List[str]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "required": [
        "overall",
        "detailed",
        "group_stats",
        "reference",
        "var_names"
      ],
      "type": "object"
    }
  },
  {
    "description": "Result container for Gelbach (2016) decomposition.",
    "name": "GelbachResult",
    "parameters": {
      "properties": {
        "base_coef": {
          "description": "base_coef parameter (float).",
          "type": "number"
        },
        "base_var": {
          "description": "base_var parameter (str).",
          "type": "string"
        },
        "decomposition": {
          "description": "decomposition parameter (pd.DataFrame).",
          "type": "string"
        },
        "full_coef": {
          "description": "full_coef parameter (float).",
          "type": "number"
        },
        "total_change": {
          "description": "total_change parameter (float).",
          "type": "number"
        },
        "total_se": {
          "description": "total_se parameter (Optional[float]).",
          "type": "number"
        },
        "vcov": {
          "description": "Variance-covariance estimator option.",
          "type": "string"
        }
      },
      "required": [
        "total_change",
        "decomposition",
        "base_coef",
        "full_coef",
        "base_var"
      ],
      "type": "object"
    }
  },
  {
    "description": "Stepwise variable selection for OLS regression. Validation: validated evidence tier (known-truth, reference, external-parity, or Monte Carlo artifact).",
    "name": "stepwise",
    "parameters": {
      "properties": {
        "alpha_in": {
          "default": 0.05,
          "description": "p-value threshold for variable entry (when ``criterion=\"pvalue\"``).",
          "type": "number"
        },
        "alpha_out": {
          "default": 0.1,
          "description": "p-value threshold for variable removal (when ``criterion=\"pvalue\"``).",
          "type": "number"
        },
        "criterion": {
          "default": "bic",
          "description": "Optimisation criterion. Default ``\"bic\"``.",
          "enum": [
            "aic",
            "bic",
            "adjr2",
            "pvalue"
          ],
          "type": "string"
        },
        "data": {
          "description": "Dataset containing all variables.",
          "type": "string"
        },
        "method": {
          "default": "both",
          "description": "Selection strategy. Default ``\"both\"`` (bidirectional).",
          "enum": [
            "forward",
            "backward",
            "both"
          ],
          "type": "string"
        },
        "verbose": {
          "default": true,
          "description": "Print step-by-step progress.",
          "type": "boolean"
        },
        "x": {
          "description": "Candidate independent variable column names.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Name of the dependent variable column.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "x"
      ],
      "type": "object"
    }
  },
  {
    "description": "LASSO-based variable selection. Validation: validated evidence tier (known-truth, reference, external-parity, or Monte Carlo artifact).",
    "name": "lasso_select",
    "parameters": {
      "properties": {
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "eps": {
          "default": 0.001,
          "description": "Ratio of lambda_min / lambda_max.",
          "type": "number"
        },
        "max_iter": {
          "default": 1000,
          "description": "Maximum coordinate descent iterations per lambda.",
          "type": "integer"
        },
        "method": {
          "default": "cv",
          "description": "How to choose the regularisation parameter lambda.",
          "enum": [
            "cv",
            "bic",
            "aic"
          ],
          "type": "string"
        },
        "n_folds": {
          "default": 10,
          "description": "Number of cross-validation folds (only for ``method=\"cv\"``).",
          "type": "integer"
        },
        "n_lambda": {
          "default": 100,
          "description": "Number of lambda values in the grid.",
          "type": "integer"
        },
        "seed": {
          "default": 42,
          "description": "Random seed for CV fold assignment.",
          "type": "integer"
        },
        "tol": {
          "default": 1e-06,
          "description": "Convergence tolerance.",
          "type": "number"
        },
        "verbose": {
          "default": true,
          "description": "Print progress.",
          "type": "boolean"
        },
        "x": {
          "description": "Candidate independent variables.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Dependent variable column.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "x"
      ],
      "type": "object"
    }
  },
  {
    "description": "Result container for variable selection procedures.",
    "name": "SelectionResult",
    "parameters": {
      "properties": {
        "coefficients": {
          "description": "coefficients parameter (Optional[Dict[str, float]]).",
          "type": "object"
        },
        "dropped": {
          "description": "dropped parameter (List[str]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "final_model": {
          "description": "final_model parameter (Dict[str, Any]).",
          "type": "object"
        },
        "history": {
          "description": "history parameter (pd.DataFrame).",
          "type": "string"
        },
        "lasso_path": {
          "description": "Filesystem path for lasso.",
          "type": "object"
        },
        "method": {
          "default": "",
          "description": "Estimator or algorithm variant to use.",
          "type": "string"
        },
        "selected": {
          "description": "selected parameter (List[str]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "required": [
        "selected",
        "dropped",
        "history",
        "final_model"
      ],
      "type": "object"
    }
  },
  {
    "description": "Container for quantile treatment effect estimates.",
    "name": "QTEResult",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "ate": {
          "description": "ate parameter (float).",
          "type": "number"
        },
        "ci_lower": {
          "description": "ci_lower parameter (np.ndarray).",
          "type": "string"
        },
        "ci_lower_uniform": {
          "description": "ci_lower_uniform parameter (Optional[np.ndarray]).",
          "type": "string"
        },
        "ci_upper": {
          "description": "ci_upper parameter (np.ndarray).",
          "type": "string"
        },
        "ci_upper_uniform": {
          "description": "ci_upper_uniform parameter (Optional[np.ndarray]).",
          "type": "string"
        },
        "effects": {
          "description": "effects parameter (np.ndarray).",
          "type": "string"
        },
        "method": {
          "description": "Estimator or algorithm variant to use.",
          "type": "string"
        },
        "model_info": {
          "description": "model_info parameter (Optional[dict]).",
          "type": "object"
        },
        "n_obs": {
          "description": "Number of obs.",
          "type": "integer"
        },
        "quantiles": {
          "description": "quantiles parameter (np.ndarray).",
          "type": "string"
        },
        "se": {
          "description": "se parameter (np.ndarray).",
          "type": "string"
        },
        "uniform_crit": {
          "default": NaN,
          "description": "uniform_crit parameter (float).",
          "type": "number"
        }
      },
      "required": [
        "quantiles",
        "effects",
        "se",
        "ci_lower",
        "ci_upper",
        "ate",
        "method",
        "n_obs"
      ],
      "type": "object"
    }
  },
  {
    "description": "Romano-Wolf stepdown adjusted p-values for multiple outcomes. Validation: validated evidence tier (known-truth, reference, external-parity, or Monte Carlo artifact).",
    "name": "romano_wolf",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Nominal significance level (used only for display).",
          "type": "number"
        },
        "cluster": {
          "description": "Column name for cluster-robust standard errors and cluster bootstrap resampling.",
          "type": "string"
        },
        "controls": {
          "description": "Additional control variables included in every regression.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "data": {
          "description": "Analysis data (complete cases used; rows with NaN in any relevant column are dropped).",
          "type": "string"
        },
        "n_boot": {
          "default": 1000,
          "description": "Number of bootstrap replications.",
          "type": "integer"
        },
        "seed": {
          "description": "Random seed for reproducibility (uses ``np.random.default_rng``).",
          "type": "integer"
        },
        "x": {
          "description": "Treatment / regressor(s) of interest. The adjusted p-value corresponds to the *first* element of ``x``.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Outcome variable names (one regression per outcome).",
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "required": [
        "data",
        "y",
        "x"
      ],
      "type": "object"
    }
  },
  {
    "description": "Container for Romano-Wolf multiple hypothesis testing results.",
    "name": "RomanoWolfResult",
    "parameters": {
      "properties": {
        "n_boot": {
          "description": "Number of bootstrap replications.",
          "type": "integer"
        },
        "n_obs": {
          "description": "Number of obs.",
          "type": "integer"
        },
        "n_outcomes": {
          "description": "Number of outcomes.",
          "type": "integer"
        },
        "table": {
          "description": "table parameter (pd.DataFrame).",
          "type": "string"
        }
      },
      "required": [
        "table",
        "n_outcomes",
        "n_boot",
        "n_obs"
      ],
      "type": "object"
    }
  },
  {
    "description": "Adjust p-values for multiple comparisons. Validation: certified parity evidence.",
    "name": "adjust_pvalues",
    "parameters": {
      "properties": {
        "method": {
          "default": "holm",
          "description": "Adjustment method. One of: - ``'bonferroni'`` -- Bonferroni correction. - ``'holm'`` -- Holm (1979) step-down. - ``'bh'`` or ``'fdr'`` -- Benjamini-Hochberg FDR. For Romano-Wolf or Westfall-Young adjustments (which require the original data and bootstrap), use :func:`romano_wolf` directly.",
          "type": "string"
        },
        "pvalues": {
          "description": "Unadjusted p-values.",
          "type": "string"
        }
      },
      "required": [
        "pvalues"
      ],
      "type": "object"
    }
  },
  {
    "description": "Bonferroni correction: ``p_adj = min(p * S, 1)``. Validation: certified parity evidence.",
    "name": "bonferroni",
    "parameters": {
      "properties": {
        "pvalues": {
          "description": "Unadjusted p-values.",
          "type": "string"
        }
      },
      "required": [
        "pvalues"
      ],
      "type": "object"
    }
  },
  {
    "description": "Holm (1979) step-down correction. Validation: certified parity evidence.",
    "name": "holm",
    "parameters": {
      "properties": {
        "pvalues": {
          "description": "Unadjusted p-values.",
          "type": "string"
        }
      },
      "required": [
        "pvalues"
      ],
      "type": "object"
    }
  },
  {
    "description": "Benjamini-Hochberg (1995) FDR correction. Validation: certified parity evidence.",
    "name": "benjamini_hochberg",
    "parameters": {
      "properties": {
        "pvalues": {
          "description": "Unadjusted p-values.",
          "type": "string"
        }
      },
      "required": [
        "pvalues"
      ],
      "type": "object"
    }
  },
  {
    "description": "List all registered StatsPAI functions, optionally filtered.",
    "name": "list_functions",
    "parameters": {
      "properties": {
        "category": {
          "description": "Limit to one category (``\"causal\"``, ``\"panel\"`` ...).",
          "type": "string"
        },
        "core": {
          "default": false,
          "description": "Return only :data:`CORE_FUNCTIONS`, the everyday estimation and post-estimation verbs, in their curated order (the other filters still apply).",
          "type": "boolean"
        },
        "stability": {
          "description": "Limit to one tier -- one of :data:`STABILITY_TIERS` (``\"stable\"`` / ``\"experimental\"`` / ``\"deprecated\"``). This is the API-lifecycle filter.",
          "type": "string"
        },
        "validation_status": {
          "description": "Limit to one evidence tier -- one of :data:`VALIDATION_STATUSES` (``\"certified\"`` / ``\"validated\"`` / ``\"api_stable\"`` / ``\"experimental\"`` / ``\"deprecated\"``). Use ``validation_status='certified'`` for parity-backed tool catalogs.",
          "type": "string"
        }
      },
      "required": [],
      "type": "object"
    }
  },
  {
    "description": "Return the full specification for a function as a dictionary.",
    "name": "describe_function",
    "parameters": {
      "properties": {
        "name": {
          "description": "name parameter (str).",
          "type": "string"
        }
      },
      "required": [
        "name"
      ],
      "type": "object"
    }
  },
  {
    "description": "Return an OpenAI function-calling compatible JSON schema.",
    "name": "function_schema",
    "parameters": {
      "properties": {
        "agent_native": {
          "default": false,
          "description": "When True, attach a top-level ``x_statspai`` extension block carrying identifying assumptions, data pre-conditions, failure modes, ranked alternatives, stability / validation tier, ``typical_n_min``, and the negative-guidance pair ``not_recommended_when`` / ``cost_profile`` -- so an LLM planner can reason about *whether* to call the tool inside the same schema it already reads, with no extra :func:`agent_card` round-trip. (The negative guidance is also folded into the plain ``description`` as ``Do NOT use when: ...`` / ``Cost: ...``, so callers that read nothing but ``description`` still see it.) The base ``name`` / ``description`` / ``parameters`` shape is unchanged, so the schema still works with strict tool-calling APIs.",
          "type": "boolean"
        },
        "name": {
          "description": "Registered function name.",
          "type": "string"
        }
      },
      "required": [
        "name"
      ],
      "type": "object"
    }
  },
  {
    "description": "Return the agent-native tool schema for a function.",
    "name": "agent_schema",
    "parameters": {
      "properties": {
        "name": {
          "description": "name parameter (str).",
          "type": "string"
        }
      },
      "required": [
        "name"
      ],
      "type": "object"
    }
  },
  {
    "description": "Keyword search across function names, descriptions, and tags.",
    "name": "search_functions",
    "parameters": {
      "properties": {
        "query": {
          "description": "query parameter (str).",
          "type": "string"
        }
      },
      "required": [
        "query"
      ],
      "type": "object"
    }
  },
  {
    "description": "Export all function schemas at once (for bulk agent tool registration).",
    "name": "all_schemas",
    "parameters": {
      "properties": {
        "agent_native": {
          "default": false,
          "description": "When True, each schema carries the ``x_statspai`` agent-native block (see :func:`function_schema`).",
          "type": "boolean"
        }
      },
      "required": [],
      "type": "object"
    }
  },
  {
    "description": "Return the agent-native metadata card for a function.",
    "name": "agent_card",
    "parameters": {
      "properties": {
        "name": {
          "description": "name parameter (str).",
          "type": "string"
        }
      },
      "required": [
        "name"
      ],
      "type": "object"
    }
  },
  {
    "description": "Bulk export of agent cards, optionally filtered.",
    "name": "agent_cards",
    "parameters": {
      "properties": {
        "category": {
          "description": "Limit to one category.",
          "type": "string"
        },
        "stability": {
          "description": "Limit to one stability tier (see :data:`STABILITY_TIERS`).",
          "type": "string"
        },
        "validation_status": {
          "description": "Limit to one validation tier. ``validation_status='certified'`` is the standard cross-language parity-backed filter for agent tool catalogs.",
          "type": "string"
        }
      },
      "required": [],
      "type": "object"
    }
  },
  {
    "description": "One failure mode for agent-native recovery.",
    "name": "FailureMode",
    "parameters": {
      "properties": {
        "alternative": {
          "default": "",
          "description": "``sp.xxx`` to try next when this failure mode triggers.",
          "type": "string"
        },
        "exception": {
          "description": "Fully-qualified exception name (``\"statspai.AssumptionViolation\"`` or ``\"ValueError\"``). Agents should ``except`` on this.",
          "type": "string"
        },
        "remedy": {
          "description": "One-sentence, actionable recovery hint.",
          "type": "string"
        },
        "symptom": {
          "description": "What the agent observes (exception class, warning text, pattern).",
          "type": "string"
        }
      },
      "required": [
        "symptom",
        "exception",
        "remedy"
      ],
      "type": "object"
    }
  },
  {
    "description": "Render the ``## For Agents`` markdown block for one function.",
    "name": "render_agent_block",
    "parameters": {
      "properties": {
        "header": {
          "default": true,
          "description": "If False, omit the ``## For Agents`` header (useful when embedding inside another section).",
          "type": "boolean"
        },
        "name": {
          "description": "Registered function name (``\"did\"``, ``\"iv\"``, ``\"rd\"``, ...).",
          "type": "string"
        }
      },
      "required": [
        "name"
      ],
      "type": "object"
    }
  },
  {
    "description": "Render the combined ``## For Agents`` section for several functions.",
    "name": "render_agent_blocks",
    "parameters": {
      "properties": {
        "category": {
          "description": "Restrict to one registry category.",
          "type": "string"
        },
        "names": {
          "description": "Explicit list of function names. If ``None`` and ``category`` is set, pulls every function in that category that has agent-native metadata. If both are ``None``, returns ``\"\"``.",
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "required": [],
      "type": "object"
    }
  },
  {
    "description": "Structured result returned by :func:`reproduce_jss_tables`.",
    "name": "ReproductionResult",
    "parameters": {
      "properties": {
        "artifacts": {
          "description": "artifacts parameter (Dict[str, Any]).",
          "type": "object"
        },
        "dry_run": {
          "description": "dry_run parameter (bool).",
          "type": "boolean"
        },
        "generated_at": {
          "description": "generated_at parameter (str).",
          "type": "string"
        },
        "repo_root": {
          "description": "repo_root parameter (str).",
          "type": "string"
        },
        "steps": {
          "description": "steps parameter (List[ReproductionStep]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "success": {
          "description": "success parameter (bool).",
          "type": "boolean"
        },
        "targets": {
          "description": "targets parameter (List[str]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "warnings": {
          "default": null,
          "description": "warnings parameter (List[str]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "required": [
        "generated_at",
        "repo_root",
        "targets",
        "dry_run",
        "success",
        "steps",
        "artifacts"
      ],
      "type": "object"
    }
  },
  {
    "description": "One command or file-copy step in a JSS table reproduction run.",
    "name": "ReproductionStep",
    "parameters": {
      "properties": {
        "action": {
          "description": "action parameter (str).",
          "type": "string"
        },
        "command": {
          "description": "command parameter (List[str]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "cwd": {
          "description": "cwd parameter (str).",
          "type": "string"
        },
        "elapsed_s": {
          "default": 0.0,
          "description": "elapsed_s parameter (float).",
          "type": "number"
        },
        "name": {
          "description": "name parameter (str).",
          "type": "string"
        },
        "returncode": {
          "description": "returncode parameter (Optional[int]).",
          "type": "integer"
        },
        "skipped": {
          "default": false,
          "description": "skipped parameter (bool).",
          "type": "boolean"
        },
        "stderr_tail": {
          "default": "",
          "description": "stderr_tail parameter (str).",
          "type": "string"
        },
        "stdout_tail": {
          "default": "",
          "description": "stdout_tail parameter (str).",
          "type": "string"
        }
      },
      "required": [
        "name",
        "action",
        "command",
        "cwd"
      ],
      "type": "object"
    }
  },
  {
    "description": "Structured validation evidence for the current StatsPAI checkout.",
    "name": "ValidationReport",
    "parameters": {
      "properties": {
        "artifacts": {
          "default": null,
          "description": "artifacts parameter (Dict[str, Any]).",
          "type": "object"
        },
        "evidence": {
          "description": "evidence parameter (Dict[str, Any]).",
          "type": "object"
        },
        "generated_at": {
          "description": "generated_at parameter (str).",
          "type": "string"
        },
        "registry": {
          "description": "registry parameter (Dict[str, Any]).",
          "type": "object"
        },
        "repo_root": {
          "description": "repo_root parameter (Optional[str]).",
          "type": "string"
        },
        "version": {
          "description": "version parameter (str).",
          "type": "string"
        },
        "warnings": {
          "default": null,
          "description": "warnings parameter (List[str]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "required": [
        "generated_at",
        "version",
        "repo_root",
        "registry",
        "evidence"
      ],
      "type": "object"
    }
  },
  {
    "description": "Return a reproducible coverage matrix for the paper narrative.",
    "name": "coverage_matrix",
    "parameters": {
      "properties": {
        "fmt": {
          "default": "dataframe",
          "description": "Output format.",
          "enum": [
            "dataframe",
            "records",
            "markdown"
          ],
          "type": "string"
        },
        "level": {
          "default": "category",
          "description": "``\"category\"`` aggregates live registry coverage by registry category and joins Track A evidence counts. ``\"parity\"`` returns the 36-module Track A matrix parsed from ``tests/r_parity/README.md``.",
          "enum": [
            "category",
            "parity"
          ],
          "type": "string"
        },
        "repo_root": {
          "description": "StatsPAI source tree. Needed for parity-artifact columns.",
          "type": "string"
        }
      },
      "required": [],
      "type": "object"
    }
  },
  {
    "description": "Return the open parity/convention gaps from Track A artifacts.",
    "name": "parity_gap_report",
    "parameters": {
      "properties": {
        "fmt": {
          "default": "dataframe",
          "description": "fmt parameter (str).",
          "type": "string"
        },
        "repo_root": {
          "description": "repo_root parameter (Optional[Union[str, Path]]).",
          "type": "string"
        }
      },
      "required": [],
      "type": "object"
    }
  },
  {
    "description": "Regenerate the JSS-facing table artifacts from source scripts.",
    "name": "reproduce_jss_tables",
    "parameters": {
      "properties": {
        "dry_run": {
          "default": false,
          "description": "Return the planned steps without executing or copying files.",
          "type": "boolean"
        },
        "python_executable": {
          "description": "Python executable used for scripts. Defaults to ``sys.executable``.",
          "type": "string"
        },
        "repo_root": {
          "description": "StatsPAI repository root. Required when the source tree cannot be auto-detected.",
          "type": "string"
        },
        "strict": {
          "default": false,
          "description": "Raise ``RuntimeError`` if any step fails.",
          "type": "boolean"
        },
        "targets": {
          "default": "all",
          "description": "``\"all\"`` expands to worked examples, parity, appendices, and inventory. ``\"core\"`` skips worked examples. Individual targets: ``\"worked_examples\"``, ``\"parity\"``, ``\"appendices\"``, ``\"inventory\"``, ``\"figures\"``, and ``\"verify\"``.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "timeout": {
          "default": 600.0,
          "description": "Per-command timeout in seconds. ``None`` disables the timeout.",
          "type": "number"
        }
      },
      "required": [],
      "type": "object"
    }
  },
  {
    "description": "Summarize registry and validation evidence for the JSS narrative.",
    "name": "validation_report",
    "parameters": {
      "properties": {
        "collect_tests": {
          "default": false,
          "description": "Run ``pytest --collect-only`` on the reference-parity, external-parity, and coverage Monte Carlo directories and record the authoritative collected-test counts (the integers the JSS manuscript headlines, e.g. 124 / 52 / 12). Off by default because it spawns pytest collection (seconds); the cheap metadata-only path reports test *file* counts instead.",
          "type": "boolean"
        },
        "fmt": {
          "default": "object",
          "description": "Return a :class:`ValidationReport`, a JSON-serializable dict, or a Markdown string.",
          "enum": [
            "object",
            "dict",
            "markdown"
          ],
          "type": "string"
        },
        "include_files": {
          "default": true,
          "description": "Include artifact file existence and size metadata.",
          "type": "boolean"
        },
        "repo_root": {
          "description": "StatsPAI repository root. If omitted, the function searches ``STATSPAI_REPO_ROOT``, the current working directory, and the installed package's parents.",
          "type": "string"
        }
      },
      "required": [],
      "type": "object"
    }
  },
  {
    "description": "Dict-compatible parity record with a human-readable rendering.",
    "name": "ParityStatus",
    "parameters": {
      "properties": {},
      "required": [],
      "type": "object"
    }
  },
  {
    "description": "Return the full parity matrix over every registered function.",
    "name": "parity_matrix",
    "parameters": {
      "properties": {
        "fmt": {
          "default": "records",
          "description": "Output format. ``\"dataframe\"`` / ``\"markdown\"`` need pandas.",
          "enum": [
            "records",
            "dataframe",
            "markdown"
          ],
          "type": "string"
        },
        "source": {
          "description": "Filter by evidence source (``\"track_a\"``, ``\"reference_parity\"``, ``\"external_parity\"``, or ``\"none\"``).",
          "type": "string"
        },
        "status": {
          "description": "Filter to one taxonomy grade (see :data:`TAXONOMY`).",
          "type": "string"
        }
      },
      "required": [],
      "type": "object"
    }
  },
  {
    "description": "Return the cross-language parity record for one function.",
    "name": "parity_status",
    "parameters": {
      "properties": {
        "name": {
          "description": "A registered ``sp.*`` function name (e.g. ``\"feols\"``).",
          "type": "string"
        }
      },
      "required": [
        "name"
      ],
      "type": "object"
    }
  },
  {
    "description": "Headline counts of the parity matrix -- the honest coverage snapshot.",
    "name": "parity_summary",
    "parameters": {
      "properties": {},
      "required": [],
      "type": "object"
    }
  },
  {
    "description": "Which validation artifacts cover the configuration that was run? Validation: validated evidence tier (known-truth, reference, external-parity, or Monte Carlo artifact).",
    "name": "validation_scope",
    "parameters": {
      "properties": {
        "function": {
          "description": "Name of the estimator (``\"dml\"``, ``\"callaway_santanna\"``, ...), required when ``result`` is omitted and otherwise inferred.",
          "type": "string"
        },
        "result": {
          "description": "A result from one of the validation-suite estimators. Its configuration (estimator variant, inference option, treatment type, code path, ...) is read from the fitted object.",
          "type": "string"
        }
      },
      "required": [],
      "type": "object"
    }
  },
  {
    "description": "Evidence for one fitted configuration (a ``dict``; prints as a table).",
    "name": "ValidationScope",
    "parameters": {
      "properties": {},
      "required": [],
      "type": "object"
    }
  },
  {
    "description": "Unified help entry point for StatsPAI.",
    "name": "help",
    "parameters": {
      "properties": {
        "format": {
          "default": "text",
          "description": "``'text'`` returns a ``HelpResult`` that prints nicely. ``'dict'`` returns the underlying data mapping, suitable for programmatic consumers.",
          "enum": [
            "text",
            "dict"
          ],
          "type": "string"
        },
        "search": {
          "description": "Keyword search across names, descriptions, and tags.",
          "type": "string"
        },
        "topic": {
          "description": "- ``None`` -> top-level overview. - ``'<function_name>'`` -> function detail (from registry + docstring). - ``'<category>'`` -> list functions in category. - ``'<category>.<function_name>'`` -> scoped function detail. - any callable (e.g. ``sp.regress``) -> docstring fallback.",
          "type": "string"
        },
        "verbose": {
          "default": false,
          "description": "If True, append live docstring after registry metadata.",
          "type": "boolean"
        }
      },
      "required": [],
      "type": "object"
    }
  },
  {
    "description": "Rich-ish help payload: prints as text, exposes data as dict.",
    "name": "HelpResult",
    "parameters": {
      "properties": {
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "object"
        },
        "text": {
          "description": "text parameter (str).",
          "type": "string"
        }
      },
      "required": [
        "text"
      ],
      "type": "object"
    }
  },
  {
    "description": "Generate Difference-in-Differences panel data.",
    "name": "dgp_did",
    "parameters": {
      "properties": {
        "effect": {
          "default": 0.5,
          "description": "Average treatment effect on the treated.",
          "type": "number"
        },
        "heterogeneous": {
          "default": false,
          "description": "If True, the treatment effect varies by unit.",
          "type": "boolean"
        },
        "n_groups": {
          "default": 4,
          "description": "Number of treatment-timing groups (used when ``staggered=True``).",
          "type": "integer"
        },
        "n_periods": {
          "default": 10,
          "description": "Number of time periods.",
          "type": "integer"
        },
        "n_units": {
          "default": 100,
          "description": "Number of cross-sectional units.",
          "type": "integer"
        },
        "seed": {
          "description": "Random seed for reproducibility.",
          "type": "integer"
        },
        "staggered": {
          "default": false,
          "description": "If True, treatment adoption is staggered across groups.",
          "type": "boolean"
        }
      },
      "required": [],
      "type": "object"
    }
  },
  {
    "description": "Generate Regression Discontinuity data (sharp or fuzzy).",
    "name": "dgp_rd",
    "parameters": {
      "properties": {
        "bandwidth_relevant": {
          "default": 0.5,
          "description": "Standard deviation of the running variable (controls spread).",
          "type": "number"
        },
        "cutoff": {
          "default": 0.0,
          "description": "RD cutoff value.",
          "type": "number"
        },
        "effect": {
          "default": 0.3,
          "description": "Treatment effect at the cutoff.",
          "type": "number"
        },
        "fuzzy": {
          "default": false,
          "description": "If True, generate a fuzzy RD design.",
          "type": "boolean"
        },
        "n": {
          "default": 1000,
          "description": "Sample size.",
          "type": "integer"
        },
        "seed": {
          "description": "Random seed.",
          "type": "integer"
        }
      },
      "required": [],
      "type": "object"
    }
  },
  {
    "description": "Generate Instrumental Variables data with endogeneity.",
    "name": "dgp_iv",
    "parameters": {
      "properties": {
        "effect": {
          "default": 0.5,
          "description": "True causal effect of treatment on outcome.",
          "type": "number"
        },
        "first_stage": {
          "default": 0.4,
          "description": "Strength of the instrument in the first stage.",
          "type": "number"
        },
        "n": {
          "default": 500,
          "description": "Sample size.",
          "type": "integer"
        },
        "n_instruments": {
          "default": 1,
          "description": "Number of instruments.",
          "type": "integer"
        },
        "seed": {
          "description": "Random seed.",
          "type": "integer"
        }
      },
      "required": [],
      "type": "object"
    }
  },
  {
    "description": "Generate Randomised Controlled Trial data.",
    "name": "dgp_rct",
    "parameters": {
      "properties": {
        "effect": {
          "default": 0.3,
          "description": "Average treatment effect.",
          "type": "number"
        },
        "heterogeneous": {
          "default": false,
          "description": "If True, the treatment effect varies with the first covariate.",
          "type": "boolean"
        },
        "n": {
          "default": 500,
          "description": "Sample size.",
          "type": "integer"
        },
        "n_covariates": {
          "default": 3,
          "description": "Number of pre-treatment covariates.",
          "type": "integer"
        },
        "p_treat": {
          "default": 0.5,
          "description": "Probability of assignment to treatment.",
          "type": "number"
        },
        "seed": {
          "description": "Random seed.",
          "type": "integer"
        }
      },
      "required": [],
      "type": "object"
    }
  },
  {
    "description": "Generate panel data with unit/time fixed effects and AR(1) covariate.",
    "name": "dgp_panel",
    "parameters": {
      "properties": {
        "ar1_coef": {
          "default": 0.5,
          "description": "AR(1) coefficient for the covariate process.",
          "type": "number"
        },
        "fe_sd": {
          "default": 1.0,
          "description": "Standard deviation of unit fixed effects.",
          "type": "number"
        },
        "n_periods": {
          "default": 20,
          "description": "Number of time periods.",
          "type": "integer"
        },
        "n_units": {
          "default": 100,
          "description": "Number of units.",
          "type": "integer"
        },
        "seed": {
          "description": "Random seed.",
          "type": "integer"
        },
        "te_sd": {
          "default": 0.5,
          "description": "Standard deviation of time fixed effects.",
          "type": "number"
        }
      },
      "required": [],
      "type": "object"
    }
  },
  {
    "description": "Generate observational data with selection on observables.",
    "name": "dgp_observational",
    "parameters": {
      "properties": {
        "confounding": {
          "default": 0.3,
          "description": "Strength of confounding (higher = more selection bias).",
          "type": "number"
        },
        "effect": {
          "default": 0.5,
          "description": "True treatment effect.",
          "type": "number"
        },
        "n": {
          "default": 1000,
          "description": "Sample size.",
          "type": "integer"
        },
        "seed": {
          "description": "Random seed.",
          "type": "integer"
        }
      },
      "required": [],
      "type": "object"
    }
  },
  {
    "description": "Generate cluster-randomised trial data.",
    "name": "dgp_cluster_rct",
    "parameters": {
      "properties": {
        "cluster_size": {
          "default": 20,
          "description": "Number of individuals per cluster.",
          "type": "integer"
        },
        "effect": {
          "default": 0.3,
          "description": "Treatment effect.",
          "type": "number"
        },
        "icc": {
          "default": 0.1,
          "description": "Intra-cluster correlation coefficient (0-1).",
          "type": "number"
        },
        "n_clusters": {
          "default": 50,
          "description": "Number of clusters.",
          "type": "integer"
        },
        "seed": {
          "description": "Random seed.",
          "type": "integer"
        }
      },
      "required": [],
      "type": "object"
    }
  },
  {
    "description": "Generate bunching data around a kink point.",
    "name": "dgp_bunching",
    "parameters": {
      "properties": {
        "elasticity": {
          "default": 0.3,
          "description": "Behavioral elasticity governing bunching intensity.",
          "type": "number"
        },
        "kink_point": {
          "default": 50000.0,
          "description": "Location of the kink (e.g., tax threshold).",
          "type": "number"
        },
        "n": {
          "default": 10000,
          "description": "Sample size.",
          "type": "integer"
        },
        "seed": {
          "description": "Random seed.",
          "type": "integer"
        }
      },
      "required": [],
      "type": "object"
    }
  },
  {
    "description": "Generate synthetic control data with a factor model.",
    "name": "dgp_synth",
    "parameters": {
      "properties": {
        "effect": {
          "default": 0.5,
          "description": "Treatment effect (additive, constant post-treatment).",
          "type": "number"
        },
        "n_periods": {
          "default": 30,
          "description": "Number of time periods.",
          "type": "integer"
        },
        "n_units": {
          "default": 20,
          "description": "Number of units (including the treated unit).",
          "type": "integer"
        },
        "seed": {
          "description": "Random seed.",
          "type": "integer"
        },
        "treated_unit": {
          "default": 0,
          "description": "Index of the treated unit.",
          "type": "integer"
        },
        "treatment_time": {
          "default": 20,
          "description": "Period when treatment begins.",
          "type": "integer"
        }
      },
      "required": [],
      "type": "object"
    }
  },
  {
    "description": "Generate shift-share (Bartik) instrument data.",
    "name": "dgp_bartik",
    "parameters": {
      "properties": {
        "effect": {
          "default": 1.0,
          "description": "True effect of the Bartik instrument on the outcome.",
          "type": "number"
        },
        "n_industries": {
          "default": 10,
          "description": "Number of industries.",
          "type": "integer"
        },
        "n_regions": {
          "default": 50,
          "description": "Number of regions.",
          "type": "integer"
        },
        "seed": {
          "description": "Random seed.",
          "type": "integer"
        }
      },
      "required": [],
      "type": "object"
    }
  },
  {
    "description": "Multinomial logit for J > 2 unordered categories via MLE. Validation: certified parity evidence.",
    "name": "mlogit",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "base": {
          "default": 0,
          "description": "Base / reference category (index into sorted unique values).",
          "type": "integer"
        },
        "cluster": {
          "description": "Cluster variable for clustered SE.",
          "type": "string"
        },
        "data": {
          "description": "Data.",
          "type": "string"
        },
        "formula": {
          "description": "Formula ``\"y ~ x1 + x2\"``.",
          "type": "string"
        },
        "maxiter": {
          "default": 100,
          "description": "maxiter parameter (int).",
          "type": "integer"
        },
        "robust": {
          "default": "nonrobust",
          "description": "``\"robust\"`` / ``\"HC1\"`` for Huber-White sandwich SE.",
          "type": "string"
        },
        "rrr": {
          "default": false,
          "description": "Report Relative Risk Ratios (exp(beta)) instead of coefficients.",
          "type": "boolean"
        },
        "tol": {
          "default": 1e-08,
          "description": "Numerical convergence tolerance.",
          "type": "number"
        },
        "x": {
          "description": "Regressors.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Dependent variable (categorical, integer-coded).",
          "type": "string"
        }
      },
      "required": [],
      "type": "object"
    }
  },
  {
    "description": "Ordered logit (proportional odds) model via MLE. Validation: certified parity evidence.",
    "name": "ologit",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "cluster": {
          "description": "Cluster identifier column for clustered standard errors.",
          "type": "string"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "formula": {
          "description": "Formula ``\"y ~ x1 + x2\"``.",
          "type": "string"
        },
        "maxiter": {
          "default": 100,
          "description": "maxiter parameter (int).",
          "type": "integer"
        },
        "robust": {
          "default": "nonrobust",
          "description": "Robust standard-error or covariance estimator option.",
          "type": "string"
        },
        "tol": {
          "default": 1e-08,
          "description": "Numerical convergence tolerance.",
          "type": "number"
        },
        "x": {
          "description": "Primary running variable, regressor, or feature input for this estimator.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Ordered categorical dependent variable.",
          "type": "string"
        }
      },
      "required": [],
      "type": "object"
    }
  },
  {
    "description": "Ordered probit model via MLE. Validation: certified parity evidence.",
    "name": "oprobit",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "cluster": {
          "description": "Cluster identifier column for clustered standard errors.",
          "type": "string"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "formula": {
          "description": "Formula ``\"y ~ x1 + x2\"``.",
          "type": "string"
        },
        "maxiter": {
          "default": 100,
          "description": "maxiter parameter (int).",
          "type": "integer"
        },
        "robust": {
          "default": "nonrobust",
          "description": "Robust standard-error or covariance estimator option.",
          "type": "string"
        },
        "tol": {
          "default": 1e-08,
          "description": "Numerical convergence tolerance.",
          "type": "number"
        },
        "x": {
          "description": "Primary running variable, regressor, or feature input for this estimator.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Ordered categorical dependent variable.",
          "type": "string"
        }
      },
      "required": [],
      "type": "object"
    }
  },
  {
    "description": "McFadden's conditional (fixed-effect) logit for choice data. Validation: certified parity evidence.",
    "name": "clogit",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "cluster": {
          "description": "Cluster identifier column for clustered standard errors.",
          "type": "string"
        },
        "data": {
          "description": "Long-format data with one row per alternative per choice set.",
          "type": "string"
        },
        "formula": {
          "description": "Formula ``\"chosen ~ price + quality\"``.",
          "type": "string"
        },
        "group": {
          "description": "Variable identifying the choice set / decision-maker.",
          "type": "string"
        },
        "maxiter": {
          "default": 100,
          "description": "maxiter parameter (int).",
          "type": "integer"
        },
        "robust": {
          "default": "nonrobust",
          "description": "Robust standard-error or covariance estimator option.",
          "type": "string"
        },
        "tol": {
          "default": 1e-08,
          "description": "Numerical convergence tolerance.",
          "type": "number"
        },
        "x": {
          "description": "Alternative-specific (and/or individual-specific interacted with alternative dummies) covariates.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Binary indicator: 1 = chosen, 0 = not chosen.",
          "type": "string"
        }
      },
      "required": [],
      "type": "object"
    }
  },
  {
    "description": "Mixed Logit (random-coefficient MNL) via simulated maximum likelihood. Validation: certified parity evidence.",
    "name": "mixlogit",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level.",
          "type": "number"
        },
        "alt": {
          "description": "Alternative identifier -- accepted for API compatibility with ``statsmodels.MNLogit`` / Stata conventions, but the ordering of alternatives is taken directly from the DataFrame's row order within each ``chid`` group.",
          "type": "string"
        },
        "chid": {
          "description": "Choice-situation identifier. All rows with the same ``chid`` form one choice set.",
          "type": "string"
        },
        "correlated": {
          "default": false,
          "description": "If True, estimate a full Cholesky factor of ``cov(beta_random)``; otherwise only diagonal standard deviations.",
          "type": "boolean"
        },
        "data": {
          "description": "Long-format choice data: one row per (choice-situation, alternative).",
          "type": "string"
        },
        "halton_burn": {
          "default": 10,
          "description": "Leading Halton points discarded per dimension.",
          "type": "integer"
        },
        "halton_seed": {
          "default": 1234,
          "description": "halton_seed parameter (int).",
          "type": "integer"
        },
        "halton_shift": {
          "default": true,
          "description": "Randomised-start Halton (one uniform shift per dimension, seeded by ``halton_seed``). ``halton_burn=15, halton_shift=False`` with ``n_draws=50`` reproduces the draws of Stata ``mixlogit``'s defaults (``nrep(50) burn(15)``), so the simulated likelihood is the same function and the estimates agree to optimiser precision.",
          "type": "boolean"
        },
        "maxiter": {
          "default": 200,
          "description": "maxiter parameter (int).",
          "type": "integer"
        },
        "n_draws": {
          "default": 500,
          "description": "Number of Halton draws per individual. Rule-of-thumb: use ``>= 1000`` for correlated models or precise inference.",
          "type": "integer"
        },
        "panel_id": {
          "description": "Individual identifier. When provided, the SAME draws of the random coefficients are used for every choice of the individual (Train 2009 Section 6.5). Omit for cross-sectional data.",
          "type": "string"
        },
        "random_dist": {
          "description": "Per-random-variable distribution -- one of ``'normal'`` (default), ``'lognormal'``, ``'triangular'``.",
          "type": "object"
        },
        "robust": {
          "default": true,
          "description": "Huber sandwich ``H-1 B H-1`` with ``B`` the outer product of the per-individual scores (Stata ``mixlogit, robust``). ``False`` -> inverse observed Hessian (Stata ``mixlogit``'s default ``oim``).",
          "type": "boolean"
        },
        "small_sample": {
          "default": true,
          "description": "Multiply the robust variance by ``N/(N - 1)``, N = number of individuals, as Stata's ``robust`` does. Ignored when ``robust=False``.",
          "type": "boolean"
        },
        "tol": {
          "default": 1e-06,
          "description": "Numerical convergence tolerance.",
          "type": "number"
        },
        "verbose": {
          "default": false,
          "description": "verbose parameter (bool).",
          "type": "boolean"
        },
        "x_fixed": {
          "description": "Columns entering with fixed (non-random) coefficients.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "x_random": {
          "description": "Columns entering with random coefficients (at least one required).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Column name with the 0/1 chosen indicator.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "chid"
      ],
      "type": "object"
    }
  },
  {
    "description": "Instrumental-variable quantile regression (Chernozhukov-Hansen). Validation: validated evidence tier (known-truth, reference, external-parity, or Monte Carlo artifact).",
    "name": "ivqreg",
    "parameters": {
      "properties": {
        "add_constant": {
          "default": true,
          "description": "add_constant parameter (bool).",
          "type": "boolean"
        },
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals.",
          "type": "number"
        },
        "bootstrap": {
          "default": 0,
          "description": "Number of pairs-bootstrap replications for standard errors. ``0`` disables bootstrap; asymptotic rank-test inversion is not implemented in this MVP.",
          "type": "integer"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "endog": {
          "description": "Endogenous regressor(s) ``D``.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "exog": {
          "description": "Exogenous controls ``X`` (may be empty).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "instruments": {
          "description": "Instrument(s) ``Z``. Must be at least as many as ``endog``.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "n_grid": {
          "default": 41,
          "description": "Grid resolution for the profile search over ``alpha`` (scalar case) -- ignored when ``endog`` is multi-dimensional.",
          "type": "integer"
        },
        "random_state": {
          "default": 1234,
          "description": "Random seed or RandomState for reproducible stochastic steps.",
          "type": "integer"
        },
        "refine": {
          "default": true,
          "description": "After the grid search, refine ``alpha`` with a local optimizer.",
          "type": "boolean"
        },
        "tau": {
          "default": 0.5,
          "description": "Quantile(s) of interest in ``(0, 1)``.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "verbose": {
          "default": false,
          "description": "verbose parameter (bool).",
          "type": "boolean"
        },
        "y": {
          "description": "Outcome variable.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "endog",
        "instruments"
      ],
      "type": "object"
    }
  },
  {
    "description": "Zero-Inflated Poisson (ZIP) regression via MLE. Validation: certified parity evidence.",
    "name": "zip_model",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals.",
          "type": "number"
        },
        "cluster": {
          "description": "Cluster variable name for clustered standard errors.",
          "type": "string"
        },
        "data": {
          "description": "Dataset.",
          "type": "string"
        },
        "formula": {
          "description": "Patsy-style formula for the count equation, e.g. \"y ~ x1 + x2\".",
          "type": "string"
        },
        "inflate": {
          "description": "Inflation-equation regressors. Default: same as count regressors.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "maxiter": {
          "default": 200,
          "description": "Maximum iterations for optimizer.",
          "type": "integer"
        },
        "robust": {
          "default": "nonrobust",
          "description": "\"nonrobust\", \"HC0\", \"HC1\", etc.",
          "type": "string"
        },
        "tol": {
          "default": 1e-08,
          "description": "Convergence tolerance.",
          "type": "number"
        },
        "x": {
          "description": "Count-equation regressors (alternative to formula).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Dependent variable name (alternative to formula).",
          "type": "string"
        }
      },
      "required": [],
      "type": "object"
    }
  },
  {
    "description": "Zero-Inflated Negative Binomial (ZINB) regression via MLE. Validation: certified parity evidence.",
    "name": "zinb",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "cluster": {
          "description": "Cluster variable name.",
          "type": "string"
        },
        "data": {
          "description": "Dataset.",
          "type": "string"
        },
        "formula": {
          "description": "Patsy-style formula for the count equation.",
          "type": "string"
        },
        "inflate": {
          "description": "Inflation-equation regressors. Default: same as count regressors.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "maxiter": {
          "default": 200,
          "description": "maxiter parameter (int).",
          "type": "integer"
        },
        "robust": {
          "default": "nonrobust",
          "description": "Standard error type.",
          "type": "string"
        },
        "tol": {
          "default": 1e-08,
          "description": "Numerical convergence tolerance.",
          "type": "number"
        },
        "x": {
          "description": "Count-equation regressors.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Dependent variable name.",
          "type": "string"
        }
      },
      "required": [],
      "type": "object"
    }
  },
  {
    "description": "Hurdle (two-part) model for count data. Validation: certified parity evidence.",
    "name": "hurdle",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "cluster": {
          "description": "Cluster identifier column for clustered standard errors.",
          "type": "string"
        },
        "count_model": {
          "default": "poisson",
          "description": "Count distribution: \"poisson\" or \"negbin\".",
          "type": "string"
        },
        "data": {
          "description": "Dataset.",
          "type": "string"
        },
        "formula": {
          "description": "Patsy-style formula.",
          "type": "string"
        },
        "maxiter": {
          "default": 200,
          "description": "maxiter parameter (int).",
          "type": "integer"
        },
        "robust": {
          "default": "nonrobust",
          "description": "Robust standard-error or covariance estimator option.",
          "type": "string"
        },
        "tol": {
          "default": 1e-08,
          "description": "Numerical convergence tolerance.",
          "type": "number"
        },
        "x": {
          "description": "Regressors (used for both hurdle and count parts).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Dependent variable name.",
          "type": "string"
        }
      },
      "required": [],
      "type": "object"
    }
  },
  {
    "description": "Limited Information Maximum Likelihood (LIML) estimator. Validation: certified parity evidence.",
    "name": "liml",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "cluster": {
          "description": "Cluster identifier column for clustered standard errors.",
          "type": "string"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "formula": {
          "description": "Model formula using patsy/R-style syntax.",
          "type": "string"
        },
        "fuller": {
          "description": "Fuller's constant (typically 1 or 4). If None, pure LIML.",
          "type": "number"
        },
        "robust": {
          "default": "nonrobust",
          "description": "Robust standard-error or covariance estimator option.",
          "type": "string"
        },
        "x_endog": {
          "description": "Endogenous regressors.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "x_exog": {
          "description": "Exogenous regressors (included instruments).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Outcome variable.",
          "type": "string"
        },
        "z": {
          "description": "Excluded instruments.",
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "required": [],
      "type": "object"
    }
  },
  {
    "description": "Jackknife Instrumental Variables Estimation (JIVE). Validation: certified parity evidence.",
    "name": "jive",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "cluster": {
          "description": "Cluster identifier column for clustered standard errors.",
          "type": "string"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "robust": {
          "default": "nonrobust",
          "description": "Robust standard-error or covariance estimator option.",
          "type": "string"
        },
        "variant": {
          "default": "jive1",
          "description": "Angrist, Imbens & Krueger (1999) jackknife instrument, used as the instrument in an IV second stage (Stata ``jive``'s ``ujive1`` / ``ujive2``): - ``'jive1'``: ``(Z_i pi - h_i x_i) / (1 - h_i)`` -- the leave-one-out first-stage fitted value. - ``'jive2'``: ``(Z_i pi - h_i x_i) / (1 - 1/n)``. ``h_i`` is the leverage of the first-stage regression on the included exogenous regressors and the excluded instruments.",
          "type": "string"
        },
        "x_endog": {
          "description": "x_endog parameter (List[str]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "x_exog": {
          "description": "x_exog parameter (Optional[List[str]]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        },
        "z": {
          "description": "Instrument, proxy, or auxiliary variable used by this estimator.",
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "required": [
        "data",
        "y",
        "x_endog"
      ],
      "type": "object"
    }
  },
  {
    "description": "LASSO-selected instrumental variables (information-criterion penalty). Validation: validated evidence tier (known-truth, reference, external-parity, or Monte Carlo artifact).",
    "name": "lasso_iv",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "cluster": {
          "description": "Cluster identifier column for clustered standard errors.",
          "type": "string"
        },
        "d": {
          "description": "d parameter (Optional[Any]).",
          "type": "string"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "penalty": {
          "default": "bic",
          "description": "Instrument selection criterion: 'bic', 'aic', 'cv'.",
          "type": "string"
        },
        "robust": {
          "default": "robust",
          "description": "Robust standard-error or covariance estimator option.",
          "type": "string"
        },
        "x_endog": {
          "description": "x_endog parameter (Optional[List[str]]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "x_exog": {
          "description": "x_exog parameter (Optional[List[str]]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        },
        "z": {
          "description": "Full set of candidate instruments.",
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "required": [
        "data",
        "y"
      ],
      "type": "object"
    }
  },
  {
    "description": "Rigorous Lasso / post-Lasso -- a faithful port of ``hdm::rlasso``. Validation: certified parity evidence.",
    "name": "rlasso",
    "parameters": {
      "properties": {
        "X": {
          "description": "Design matrix of candidate covariates (``p`` may exceed ``n``).",
          "type": "string"
        },
        "colnames": {
          "description": "Names for the columns of ``X`` (default ``V1..Vp``).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "control": {
          "description": "Overrides for ``numIter`` (default 15), ``tol`` (default 1e-5) and ``threshold`` (default ``None``).",
          "type": "object"
        },
        "intercept": {
          "default": true,
          "description": "Center ``X`` and ``y`` and report an intercept on the original scale.",
          "type": "boolean"
        },
        "penalty": {
          "description": "Overrides for ``homoscedastic`` (``True`` / ``False`` / ``\"none\"``), ``X.dependent.lambda`` (bool), ``c`` (slack, default 1.1), ``gamma`` (default ``0.1/log(n)``), ``lambda.start`` and ``numSim``. Defaults reproduce hdm exactly.",
          "type": "object"
        },
        "post": {
          "default": true,
          "description": "If ``True``, re-estimate the selected support by OLS (post-Lasso).",
          "type": "boolean"
        },
        "rng": {
          "description": "Only used when ``X.dependent.lambda`` simulation is requested.",
          "type": "string"
        },
        "y": {
          "description": "Response.",
          "type": "string"
        }
      },
      "required": [
        "X",
        "y"
      ],
      "type": "object"
    }
  },
  {
    "description": "Effect of ``d`` on ``y`` after Lasso-selecting controls ``x``. Validation: certified parity evidence.",
    "name": "rlasso_effect",
    "parameters": {
      "properties": {
        "I3": {
          "description": "Amelioration set forced into the control set (double-selection only) -- hdm's ``I3`` argument.",
          "type": "string"
        },
        "control": {
          "description": "Forwarded to :func:`statspai.rlasso.rlasso`.",
          "type": "object"
        },
        "d": {
          "description": "d parameter (Union[np.ndarray, pd.Series, str]).",
          "type": "string"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "method": {
          "default": "partialling out",
          "description": "See the module docstring.",
          "type": "string"
        },
        "penalty": {
          "description": "Forwarded to :func:`statspai.rlasso.rlasso`.",
          "type": "object"
        },
        "post": {
          "default": true,
          "description": "Post-Lasso inside the selection steps.",
          "type": "boolean"
        },
        "x": {
          "description": "Primary running variable, regressor, or feature input for this estimator.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "x",
        "y",
        "d"
      ],
      "type": "object"
    }
  },
  {
    "description": "Estimate the effect of each targeted column of ``X`` on ``y``. Validation: certified parity evidence.",
    "name": "rlasso_effects",
    "parameters": {
      "properties": {
        "X": {
          "description": "Feature matrix or covariate DataFrame.",
          "type": "string"
        },
        "control": {
          "description": "control parameter (Optional[Dict[str, Any]]).",
          "type": "object"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "index": {
          "description": "index parameter (Optional[Sequence[int]]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "method": {
          "default": "partialling out",
          "description": "Estimator or algorithm variant to use.",
          "type": "string"
        },
        "penalty": {
          "description": "penalty parameter (Optional[Dict[str, Any]]).",
          "type": "object"
        },
        "post": {
          "default": true,
          "description": "post parameter (bool).",
          "type": "boolean"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "X",
        "y"
      ],
      "type": "object"
    }
  },
  {
    "description": "Instrumental-variables estimation with rigorous-Lasso selection. Validation: certified parity evidence.",
    "name": "rlasso_iv",
    "parameters": {
      "properties": {
        "control": {
          "description": "Forwarded to :func:`statspai.rlasso.rlasso` (penalty level, loadings, iteration controls).",
          "type": "object"
        },
        "d": {
          "description": "name).",
          "type": "string"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "intercept": {
          "default": true,
          "description": "Passed to the underlying ``rlasso`` first stages.",
          "type": "boolean"
        },
        "penalty": {
          "description": "Forwarded to :func:`statspai.rlasso.rlasso` (penalty level, loadings, iteration controls).",
          "type": "object"
        },
        "post": {
          "default": true,
          "description": "Post-Lasso (OLS refit) inside every selection step.",
          "type": "boolean"
        },
        "select_X": {
          "default": true,
          "description": "Lasso-select among the controls (partialling-out).",
          "type": "boolean"
        },
        "select_Z": {
          "default": true,
          "description": "Lasso-select among the instruments.",
          "type": "boolean"
        },
        "x": {
          "description": "Primary running variable, regressor, or feature input for this estimator.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "name).",
          "type": "string"
        },
        "z": {
          "description": "Instrument, proxy, or auxiliary variable used by this estimator.",
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "required": [
        "y",
        "d",
        "z"
      ],
      "type": "object"
    }
  },
  {
    "description": "Logistic rigorous (post-)Lasso -- a faithful port of ``hdm::rlassologit``. Validation: certified parity evidence.",
    "name": "rlassologit",
    "parameters": {
      "properties": {
        "X": {
          "description": "Feature matrix or covariate DataFrame.",
          "type": "string"
        },
        "colnames": {
          "description": "Column names (default ``V1..Vp``).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "control": {
          "description": "``threshold`` -- coefficients below it are zeroed (default None).",
          "type": "object"
        },
        "intercept": {
          "default": true,
          "description": "Include an intercept.",
          "type": "boolean"
        },
        "penalty": {
          "description": "Overrides for ``c`` (slack; default 1.1 for ``post=True``, else 0.5), ``gamma`` (default ``0.1/log n``) and ``lambda`` (raw penalty; bypasses the data-driven level).",
          "type": "object"
        },
        "post": {
          "default": true,
          "description": "If ``True``, refit the selected support by *unpenalized* logistic regression (post-Lasso); else keep the glmnet-penalized fit.",
          "type": "boolean"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "enum": [
            "0",
            "1"
          ],
          "type": "string"
        }
      },
      "required": [
        "X",
        "y"
      ],
      "type": "object"
    }
  },
  {
    "description": "Effect of ``d`` on a binary ``y`` after Lasso-selecting controls ``x``. Validation: certified parity evidence.",
    "name": "rlassologit_effect",
    "parameters": {
      "properties": {
        "I3": {
          "description": "Amelioration set forced into the control set (hdm's ``I3``).",
          "type": "string"
        },
        "d": {
          "description": "d parameter (Union[np.ndarray, pd.Series, str]).",
          "type": "string"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "post": {
          "default": true,
          "description": "Post-Lasso inside the two selection steps.",
          "type": "boolean"
        },
        "x": {
          "description": "Primary running variable, regressor, or feature input for this estimator.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "x",
        "y",
        "d"
      ],
      "type": "object"
    }
  },
  {
    "description": "Logistic high-dimensional effect of each targeted column of ``X``. Validation: certified parity evidence.",
    "name": "rlassologit_effects",
    "parameters": {
      "properties": {
        "I3": {
          "description": "I3 parameter (Optional[np.ndarray]).",
          "type": "string"
        },
        "X": {
          "description": "Feature matrix or covariate DataFrame.",
          "type": "string"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "index": {
          "description": "index parameter (Optional[Sequence[int]]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "post": {
          "default": true,
          "description": "post parameter (bool).",
          "type": "boolean"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "X",
        "y"
      ],
      "type": "object"
    }
  },
  {
    "description": "Rigorous (post-)Lasso as a scikit-learn regressor.",
    "name": "RlassoRegressor",
    "parameters": {
      "properties": {
        "c": {
          "default": 1.1,
          "description": "c parameter (float).",
          "type": "number"
        },
        "gamma": {
          "description": "gamma parameter (Optional[float]).",
          "type": "number"
        },
        "homoscedastic": {
          "default": false,
          "description": "homoscedastic parameter (bool).",
          "type": "boolean"
        },
        "intercept": {
          "default": true,
          "description": "intercept parameter (bool).",
          "type": "boolean"
        },
        "lambda_start": {
          "description": "lambda_start parameter (Optional[float]).",
          "type": "number"
        },
        "num_iter": {
          "default": 15,
          "description": "num_iter parameter (int).",
          "type": "integer"
        },
        "post": {
          "default": true,
          "description": "post parameter (bool).",
          "type": "boolean"
        },
        "tol": {
          "default": 1e-05,
          "description": "Numerical convergence tolerance.",
          "type": "number"
        },
        "x_dependent": {
          "default": false,
          "description": "x_dependent parameter (bool).",
          "type": "boolean"
        }
      },
      "required": [],
      "type": "object"
    }
  },
  {
    "description": "Linear-probability classifier backed by the rigorous Lasso.",
    "name": "RlassoClassifier",
    "parameters": {
      "properties": {
        "c": {
          "default": 1.1,
          "description": "c parameter (float).",
          "type": "number"
        },
        "clip": {
          "default": 0.001,
          "description": "clip parameter (float).",
          "type": "number"
        },
        "gamma": {
          "description": "gamma parameter (Optional[float]).",
          "type": "number"
        },
        "homoscedastic": {
          "default": false,
          "description": "homoscedastic parameter (bool).",
          "type": "boolean"
        },
        "intercept": {
          "default": true,
          "description": "intercept parameter (bool).",
          "type": "boolean"
        },
        "lambda_start": {
          "description": "lambda_start parameter (Optional[float]).",
          "type": "number"
        },
        "num_iter": {
          "default": 15,
          "description": "num_iter parameter (int).",
          "type": "integer"
        },
        "post": {
          "default": true,
          "description": "post parameter (bool).",
          "type": "boolean"
        },
        "tol": {
          "default": 1e-05,
          "description": "Numerical convergence tolerance.",
          "type": "number"
        },
        "x_dependent": {
          "default": false,
          "description": "x_dependent parameter (bool).",
          "type": "boolean"
        }
      },
      "required": [],
      "type": "object"
    }
  },
  {
    "description": "Logistic rigorous-Lasso classifier -- a genuine (calibrated) propensity.",
    "name": "RlassologitClassifier",
    "parameters": {
      "properties": {
        "c": {
          "description": "c parameter (Optional[float]).",
          "type": "number"
        },
        "clip": {
          "default": 1e-05,
          "description": "clip parameter (float).",
          "type": "number"
        },
        "gamma": {
          "description": "gamma parameter (Optional[float]).",
          "type": "number"
        },
        "intercept": {
          "default": true,
          "description": "intercept parameter (bool).",
          "type": "boolean"
        },
        "lambda_": {
          "description": "lambda_ parameter (Optional[float]).",
          "type": "number"
        },
        "post": {
          "default": true,
          "description": "post parameter (bool).",
          "type": "boolean"
        }
      },
      "required": [],
      "type": "object"
    }
  },
  {
    "description": "Estimate OLS / IV with high-dimensional fixed effects via pyfixest. Validation: certified parity evidence. Do NOT use when: vce='conley' on more than ~20,000 rows -- that path is dense O(n^2) (see cost); use sp.conley on the fitted result, which is sparse and scales. Cost: Default (OLS / HC / CRV1) is linear in n. vce='conley' is the exception: it calls conley_vcov_matrix, which materialises several dense n x n float64 arrays (lat/lon differences, distances, the uniform kernel) -- ~0.8 GB at n=10,000, ~80 GB at n=100,000, ~157 GB at n=140,000. Prefer sp.conley (sparse cKDTree) above ~20,000 rows.",
    "name": "feols",
    "parameters": {
      "properties": {
        "cluster": {
          "description": "Cluster id column for the extended ``vce=`` menu; also a shorthand for one-way ``{\"CRV1\": cluster}``.",
          "type": "string"
        },
        "collin_tol": {
          "default": 1e-06,
          "description": "Collinearity tolerance.",
          "type": "number"
        },
        "conley_cutoff": {
          "description": "Conley distance cutoff in km for ``vce=\"conley\"``.",
          "type": "number"
        },
        "conley_lat": {
          "description": "Coordinate columns (decimal degrees) for ``vce=\"conley\"``.",
          "type": "string"
        },
        "conley_lon": {
          "description": "Coordinate columns (decimal degrees) for ``vce=\"conley\"``.",
          "type": "string"
        },
        "data": {
          "description": "Input dataset.",
          "type": "string"
        },
        "fixef_rm": {
          "default": "none",
          "description": "How to handle singleton fixed effects: ``\"none\"`` (keep) or ``\"singleton\"`` (drop).",
          "type": "string"
        },
        "fml": {
          "description": "A pyfixest-style formula. Examples: - ``\"Y ~ X1 + X2\"`` -- plain OLS - ``\"Y ~ X1 | firm + year\"`` -- two-way fixed effects - ``\"Y ~ 1 | firm | X1 ~ Z1\"`` -- IV with fixed effects - ``\"Y ~ X1 | csw0(firm, year)\"`` -- multiple estimations",
          "type": "string"
        },
        "lean": {
          "default": false,
          "description": "If True, drop large intermediate arrays to save memory.",
          "type": "boolean"
        },
        "seed": {
          "description": "RNG seed for ``vce=\"wild\"``.",
          "type": "integer"
        },
        "ssc": {
          "description": "Small-sample correction via ``pyfixest.ssc()``.",
          "type": "string"
        },
        "vcov": {
          "description": "Variance-covariance estimator (``vce=`` is the canonical alias). - ``\"iid\"`` -- classical - ``\"HC1\"``, ``\"HC2\"``, ``\"HC3\"`` -- heteroskedasticity-robust - ``{\"CRV1\": \"firm\"}`` -- cluster-robust - ``{\"CRV1\": \"firm + year\"}`` -- two-way clustering - ``vce=\"CR2\"`` / ``\"CR3\"`` / ``\"jackknife\"`` (with ``cluster=``) -- Pustejovsky-Tipton bias-reduced cluster-robust on the FE-absorbed within design; matches R ``clubSandwich::vcovCR(plm)``. - ``vce=\"wild\"`` (with ``cluster=``) -- WCR wild cluster bootstrap (Cameron-Gelbach-Miller 2008); validated against Stata ``boottest``. - ``vce=\"conley\"`` (with ``conley_lat=/conley_lon=/conley_cutoff=``) -- Conley spatial HAC (Stata ``acreg`` planar-distance convention).",
          "type": "object"
        },
        "weights": {
          "description": "Column name for regression weights.",
          "type": "string"
        },
        "wild_reps": {
          "default": 999,
          "description": "Bootstrap replications for ``vce=\"wild\"``.",
          "type": "integer"
        },
        "wild_weight_type": {
          "default": "rademacher",
          "description": "Wild weight distribution (``\"rademacher\"``, ``\"webb\"``, ``\"mammen\"``).",
          "type": "string"
        }
      },
      "required": [
        "fml",
        "data"
      ],
      "type": "object"
    }
  },
  {
    "description": "Estimate Poisson regression with high-dimensional fixed effects via pyfixest. Validation: certified parity evidence.",
    "name": "fepois",
    "parameters": {
      "properties": {
        "cluster": {
          "description": "Cluster id column for the extended ``vce=`` menu; also a shorthand for one-way ``{\"CRV1\": cluster}``.",
          "type": "string"
        },
        "collin_tol": {
          "default": 1e-06,
          "description": "Collinearity tolerance.",
          "type": "number"
        },
        "conley_cutoff": {
          "description": "conley_cutoff parameter (Optional[float]).",
          "type": "number"
        },
        "conley_lat": {
          "description": "conley_lat parameter (Optional[str]).",
          "type": "string"
        },
        "conley_lon": {
          "description": "conley_lon parameter (Optional[str]).",
          "type": "string"
        },
        "data": {
          "description": "Input dataset.",
          "type": "string"
        },
        "fixef_rm": {
          "default": "none",
          "description": "Singleton fixed effect handling.",
          "type": "string"
        },
        "fml": {
          "description": "pyfixest formula. E.g. ``\"Y ~ X1 | firm\"``.",
          "type": "string"
        },
        "iwls_maxiter": {
          "default": 25,
          "description": "Max IWLS iterations.",
          "type": "integer"
        },
        "iwls_tol": {
          "default": 1e-08,
          "description": "IWLS convergence tolerance.",
          "type": "number"
        },
        "seed": {
          "description": "RNG seed for sampled (non-enumerated) ``vce=\"wild\"`` draws.",
          "type": "integer"
        },
        "ssc": {
          "description": "Small-sample correction.",
          "type": "string"
        },
        "vcov": {
          "description": "Variance-covariance estimator (``vce=`` is the canonical alias). Besides the pyfixest values (``\"iid\"``, ``\"HC1\"``, ``{\"CRV1\": \"firm\"}``, ...), accepts the extended menu: - ``vce=\"CR2\"`` / ``\"CR3\"`` / ``\"jackknife\"`` (with ``cluster=``) -- clubSandwich glm bias-reduced cluster-robust SEs on the FE-as-dummies design; matches R ``clubSandwich::vcovCR(glm)``. - ``vce=\"wild\"`` (with ``cluster=``) -- restricted score wild cluster bootstrap (Kline-Santos 2012) with Stata ``boottest``'s exact studentization; bit-exact vs ``boottest`` in the enumerated regime.",
          "type": "object"
        },
        "weights": {
          "description": "Column name for regression weights (not supported with the extended ``vce=`` menu).",
          "type": "string"
        },
        "wild_reps": {
          "default": 9999,
          "description": "Replications for ``vce=\"wild\"``. When ``2**G <= wild_reps`` the full Rademacher grid is enumerated (deterministic).",
          "type": "integer"
        },
        "wild_weight_type": {
          "default": "rademacher",
          "description": "Wild weight distribution (``\"rademacher\"`` or ``\"webb\"``).",
          "type": "string"
        }
      },
      "required": [
        "fml",
        "data"
      ],
      "type": "object"
    }
  },
  {
    "description": "Estimate GLM (logit, probit, Gaussian) with high-dimensional fixed effects. Validation: certified parity evidence.",
    "name": "feglm",
    "parameters": {
      "properties": {
        "cluster": {
          "description": "Cluster id column for the extended ``vce=`` menu (also a shorthand for one-way ``{\"CRV1\": cluster}``).",
          "type": "string"
        },
        "conley_cutoff": {
          "description": "conley_cutoff parameter (Optional[float]).",
          "type": "number"
        },
        "conley_lat": {
          "description": "conley_lat parameter (Optional[str]).",
          "type": "string"
        },
        "conley_lon": {
          "description": "conley_lon parameter (Optional[str]).",
          "type": "string"
        },
        "data": {
          "description": "Input dataset.",
          "type": "string"
        },
        "family": {
          "default": "gaussian",
          "description": "GLM family: ``\"gaussian\"``, ``\"logit\"``, ``\"probit\"``.",
          "type": "string"
        },
        "fml": {
          "description": "pyfixest formula.",
          "type": "string"
        },
        "seed": {
          "description": "RNG seed for sampled (non-enumerated) ``vce=\"wild\"`` draws.",
          "type": "integer"
        },
        "vcov": {
          "description": "Variance-covariance estimator (``vce=`` is the canonical alias). Also accepts ``vce=\"CR2\"``/``\"CR3\"``/``\"jackknife\"`` (with ``cluster=``) for the clubSandwich bias-reduced cluster-robust SEs, and ``vce=\"wild\"`` (with ``cluster=``) for the restricted score wild cluster bootstrap (Kline-Santos 2012; bit-exact vs Stata ``boottest`` in the enumerated regime).",
          "type": "object"
        },
        "wild_reps": {
          "default": 9999,
          "description": "Replications for ``vce=\"wild\"``. When ``2**G <= wild_reps`` the full Rademacher grid is enumerated (deterministic).",
          "type": "integer"
        },
        "wild_weight_type": {
          "default": "rademacher",
          "description": "Wild weight distribution (``\"rademacher\"`` or ``\"webb\"``).",
          "type": "string"
        }
      },
      "required": [
        "fml",
        "data"
      ],
      "type": "object"
    }
  },
  {
    "description": "Display a pyfixest-style regression table for StatsPAI results.",
    "name": "etable",
    "parameters": {
      "properties": {
        "kwargs": {
          "description": "Passed to ``pyfixest.etable()``.",
          "type": "string"
        },
        "results": {
          "description": "One or more fitted results.",
          "type": "string"
        }
      },
      "required": [],
      "type": "object"
    }
  },
  {
    "description": "Cox Proportional Hazards model via partial likelihood. Validation: certified parity evidence.",
    "name": "cox",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals.",
          "type": "number"
        },
        "cluster": {
          "description": "Column name for cluster-robust SE.",
          "type": "string"
        },
        "data": {
          "description": "Input data.",
          "type": "string"
        },
        "duration": {
          "description": "Column name for follow-up time (overrides formula LHS).",
          "type": "string"
        },
        "event": {
          "description": "Column name for event indicator (1 = event, 0 = censored).",
          "type": "string"
        },
        "formula": {
          "description": "Formula of the form ``'duration ~ x1 + x2'``. If given, ``duration`` is inferred from the LHS.",
          "type": "string"
        },
        "hazard_ratio": {
          "default": true,
          "description": "If True, report hazard ratios in the summary alongside coefficients.",
          "type": "boolean"
        },
        "robust": {
          "default": "nonrobust",
          "description": "``'hc0'`` for sandwich SE.",
          "type": "string"
        },
        "strata": {
          "description": "Column name for stratification variable.",
          "type": "string"
        },
        "ties": {
          "default": "efron",
          "description": "Tie-handling method: ``'efron'`` or ``'breslow'``.",
          "type": "string"
        },
        "x": {
          "description": "Covariate column names (overrides formula RHS).",
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "required": [],
      "type": "object"
    }
  },
  {
    "description": "Kaplan-Meier non-parametric survival function estimator. Validation: certified parity evidence.",
    "name": "kaplan_meier",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals (Greenwood formula).",
          "type": "number"
        },
        "data": {
          "description": "Input data.",
          "type": "string"
        },
        "duration": {
          "description": "Column name for duration / follow-up time.",
          "type": "string"
        },
        "event": {
          "description": "Column name for event indicator (1 = event, 0 = censored).",
          "type": "string"
        },
        "group": {
          "description": "Column name for group variable (stratification).",
          "type": "string"
        }
      },
      "required": [
        "data",
        "duration",
        "event"
      ],
      "type": "object"
    }
  },
  {
    "description": "Parametric survival model (AFT parameterization). Validation: certified parity evidence.",
    "name": "survreg",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "cluster": {
          "description": "Cluster identifier column for clustered standard errors.",
          "type": "string"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "dist": {
          "default": "weibull",
          "description": "dist parameter (str).",
          "type": "string"
        },
        "duration": {
          "description": "Follow-up time column (or formula LHS).",
          "type": "string"
        },
        "event": {
          "description": "Event indicator column.",
          "type": "string"
        },
        "formula": {
          "description": "Formula ``'duration ~ x1 + x2'``.",
          "type": "string"
        },
        "robust": {
          "default": "nonrobust",
          "description": "Robust standard-error or covariance estimator option.",
          "type": "string"
        },
        "x": {
          "description": "Covariate columns (or formula RHS).",
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "required": [],
      "type": "object"
    }
  },
  {
    "description": "Result from Cox proportional hazards estimation.",
    "name": "CoxResult",
    "parameters": {
      "properties": {
        "_X": {
          "description": "_X parameter.",
          "type": "string"
        },
        "_baseline_hazard_df": {
          "description": "_baseline_hazard_df parameter (Optional[pandas.DataFrame]).",
          "type": "string"
        },
        "_concordance": {
          "description": "_concordance parameter (Optional[float]).",
          "type": "number"
        },
        "_durations": {
          "description": "_durations parameter.",
          "type": "string"
        },
        "_events": {
          "description": "_events parameter.",
          "type": "string"
        },
        "_hazard_ratios": {
          "description": "_hazard_ratios parameter.",
          "type": "string"
        },
        "_hr_ci": {
          "description": "_hr_ci parameter.",
          "type": "string"
        },
        "_schoenfeld_resid": {
          "description": "_schoenfeld_resid parameter.",
          "type": "string"
        },
        "_strata": {
          "description": "_strata parameter.",
          "type": "string"
        },
        "data_info": {
          "description": "data_info parameter (Optional[Dict[str, Any]]).",
          "type": "object"
        },
        "diagnostics": {
          "description": "diagnostics parameter (Optional[Dict[str, Any]]).",
          "type": "object"
        },
        "model_info": {
          "description": "model_info parameter.",
          "type": "string"
        },
        "params": {
          "description": "params parameter.",
          "type": "string"
        },
        "std_errors": {
          "description": "std_errors parameter.",
          "type": "string"
        }
      },
      "required": [
        "params",
        "std_errors",
        "model_info"
      ],
      "type": "object"
    }
  },
  {
    "description": "Kaplan-Meier survival analysis result.",
    "name": "KMResult",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "tables": {
          "description": "tables parameter (Dict[str, pandas.DataFrame]).",
          "type": "object"
        }
      },
      "required": [
        "tables"
      ],
      "type": "object"
    }
  },
  {
    "description": "Log-rank test for equality of survival distributions across groups. Validation: certified parity evidence.",
    "name": "logrank_test",
    "parameters": {
      "properties": {
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "duration": {
          "description": "Column names.",
          "type": "string"
        },
        "event": {
          "description": "Column names.",
          "type": "string"
        },
        "group": {
          "description": "Column names.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "duration",
        "event",
        "group"
      ],
      "type": "object"
    }
  },
  {
    "description": "Cumulative incidence functions for competing risks (Aalen-Johansen). Validation: certified parity evidence.",
    "name": "cuminc",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for the confidence bands.",
          "type": "number"
        },
        "conf_type": {
          "default": "linear",
          "description": "``\"linear\"`` is ``F +/- z se`` clipped to [0, 1]; ``\"log-log\"`` is ``F^exp(+/- z se / (F log F))``, stcompet's bounds.",
          "enum": [
            "linear",
            "log-log"
          ],
          "type": "string"
        },
        "data": {
          "description": "Input data.",
          "type": "string"
        },
        "duration": {
          "description": "Column name for the follow-up time.",
          "type": "string"
        },
        "event": {
          "description": "Column name for the event indicator. ``0`` = censored; ``1, 2, ...`` = competing causes.",
          "type": "string"
        },
        "group": {
          "description": "Column name for a grouping variable. When supplied, CIFs are estimated per group and Gray's K-sample test is reported per cause.",
          "type": "string"
        },
        "rho": {
          "default": 0.0,
          "description": "Power of the ``(1 - F(t-))^rho`` weight in Gray's test (cmprsk's ``rho``).",
          "type": "number"
        },
        "variance": {
          "default": "delta",
          "description": "Pointwise variance of the CIF. ``\"delta\"`` is the Marubini-Valsecchi delta-method estimator (the one Stata's ``stcompet`` reports); ``\"gray\"`` is Gray's asymptotic variance, the ``var`` component of R ``cmprsk::cuminc``.",
          "enum": [
            "delta",
            "gray"
          ],
          "type": "string"
        }
      },
      "required": [
        "data",
        "duration",
        "event"
      ],
      "type": "object"
    }
  },
  {
    "description": "Fine & Gray (1999) proportional subdistribution hazards model. Validation: certified parity evidence.",
    "name": "finegray",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals.",
          "type": "number"
        },
        "cause": {
          "default": 1,
          "description": "Cause of interest (default ``1``).",
          "type": "integer"
        },
        "data": {
          "description": "Input data.",
          "type": "string"
        },
        "duration": {
          "description": "Follow-up-time column.",
          "type": "string"
        },
        "event": {
          "description": "Event indicator: ``0`` = censored, ``1, 2, ...`` = causes.",
          "type": "string"
        },
        "max_iter": {
          "default": 50,
          "description": "Newton-Raphson controls (``tol`` is on the largest coefficient step).",
          "type": "integer"
        },
        "small_sample": {
          "default": false,
          "description": "Multiply the robust variance by ``n / (n - 1)``, as Stata's ``stcrreg`` does.",
          "type": "boolean"
        },
        "tol": {
          "default": 1e-10,
          "description": "Newton-Raphson controls (``tol`` is on the largest coefficient step).",
          "type": "number"
        },
        "vce": {
          "default": "robust",
          "description": "``\"robust\"`` is Fine & Gray's sandwich variance, whose score residuals include the term for estimating the censoring",
          "enum": [
            "robust",
            "model"
          ],
          "type": "string"
        },
        "x": {
          "description": "Covariate column names.",
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "required": [
        "data",
        "duration",
        "event",
        "x"
      ],
      "type": "object"
    }
  },
  {
    "description": "Cumulative incidence functions for competing risks.",
    "name": "CumIncResult",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "causes": {
          "description": "causes parameter (List[int]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "cif_table": {
          "description": "cif_table parameter (pd.DataFrame).",
          "type": "string"
        },
        "gray_test": {
          "description": "gray_test parameter (Optional[Dict[int, Dict[str, float]]]).",
          "type": "object"
        }
      },
      "required": [
        "cif_table",
        "causes"
      ],
      "type": "object"
    }
  },
  {
    "description": "Fine-Gray proportional subdistribution hazards model result.",
    "name": "FineGrayResult",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "bse": {
          "description": "bse parameter (np.ndarray).",
          "type": "string"
        },
        "cause": {
          "description": "cause parameter (int).",
          "type": "integer"
        },
        "covariates": {
          "description": "Covariate matrix, DataFrame, or column names.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "loglik": {
          "description": "loglik parameter (float).",
          "type": "number"
        },
        "n_events": {
          "description": "Number of events.",
          "type": "integer"
        },
        "n_obs": {
          "description": "Number of obs.",
          "type": "integer"
        },
        "params": {
          "description": "params parameter (np.ndarray).",
          "type": "string"
        },
        "vce": {
          "default": "robust",
          "description": "vce parameter (str).",
          "type": "string"
        },
        "vcov": {
          "description": "Variance-covariance estimator option.",
          "type": "string"
        }
      },
      "required": [
        "params",
        "bse",
        "covariates",
        "cause",
        "n_obs",
        "n_events",
        "loglik"
      ],
      "type": "object"
    }
  },
  {
    "description": "Local polynomial regression. Validation: certified parity evidence.",
    "name": "lpoly",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for CI.",
          "type": "number"
        },
        "bandwidth": {
          "description": "Kernel bandwidth. If None, uses Silverman's rule-of-thumb.",
          "type": "number"
        },
        "ci": {
          "default": true,
          "description": "Compute confidence intervals.",
          "type": "boolean"
        },
        "data": {
          "description": "Input data.",
          "type": "string"
        },
        "degree": {
          "default": 1,
          "description": "Polynomial degree (0=Nadaraya-Watson, 1=local linear, 2=local quadratic).",
          "type": "integer"
        },
        "grid": {
          "description": "Custom evaluation grid. Overrides n_grid.",
          "type": "string"
        },
        "kernel": {
          "default": "epanechnikov",
          "description": "Kernel function: 'epanechnikov', 'gaussian', 'uniform', 'triangular', 'biweight'.",
          "type": "string"
        },
        "n_grid": {
          "default": 100,
          "description": "Number of evaluation grid points.",
          "type": "integer"
        },
        "pwidth": {
          "description": "Pilot bandwidth for ``se_method=\"stata\"`` (required; Stata's default of 1.5 x its rule-of-thumb bandwidth is not implemented).",
          "type": "number"
        },
        "se_method": {
          "default": "robust",
          "description": "``\"robust\"``: heteroskedasticity-robust sandwich ``(X'WX)^-1 X'W diag(e^2) W X (X'WX)^-1`` times ``m / (m - p - 1)``. ``\"stata\"``: Stata ``lpoly``'s standard error, which plugs a local residual variance from a degree ``p + 2`` fit with pilot bandwidth ``pwidth`` into ``(X'WX)^-1 X'W^2X (X'WX)^-1``.",
          "enum": [
            "robust",
            "stata"
          ],
          "type": "string"
        },
        "x": {
          "description": "Independent variable name.",
          "type": "string"
        },
        "y": {
          "description": "Dependent variable name.",
          "type": "string"
        }
      },
      "required": [],
      "type": "object"
    }
  },
  {
    "description": "Results from local polynomial regression.",
    "name": "LPolyResult",
    "parameters": {
      "properties": {
        "bandwidth": {
          "description": "Bandwidth used for local smoothing or kernel weighting.",
          "type": "number"
        },
        "ci_lower": {
          "description": "ci_lower parameter (ndarray).",
          "type": "string"
        },
        "ci_upper": {
          "description": "ci_upper parameter (ndarray).",
          "type": "string"
        },
        "data_x": {
          "description": "data_x parameter (ndarray).",
          "type": "string"
        },
        "data_y": {
          "description": "data_y parameter (ndarray).",
          "type": "string"
        },
        "degree": {
          "description": "degree parameter (int).",
          "type": "integer"
        },
        "fitted": {
          "description": "fitted parameter (ndarray).",
          "type": "string"
        },
        "grid": {
          "description": "grid parameter (ndarray).",
          "type": "string"
        },
        "kernel": {
          "description": "Kernel function used for weighting or smoothing.",
          "type": "string"
        },
        "n": {
          "description": "n parameter (int).",
          "type": "integer"
        },
        "se": {
          "description": "se parameter (ndarray).",
          "type": "string"
        }
      },
      "required": [
        "grid",
        "fitted",
        "se",
        "ci_lower",
        "ci_upper",
        "bandwidth",
        "degree",
        "kernel",
        "n",
        "data_x",
        "data_y"
      ],
      "type": "object"
    }
  },
  {
    "description": "Kernel density estimation. Validation: certified parity evidence.",
    "name": "kdensity",
    "parameters": {
      "properties": {
        "bandwidth": {
          "description": "Kernel bandwidth. If None, selected automatically.",
          "type": "number"
        },
        "bw_method": {
          "default": "silverman",
          "description": "Bandwidth selection when ``bandwidth`` is None: * ``'silverman'`` (alias ``'nrd0'``): R ``bw.nrd0``, ``0.9 min(sd, IQR/1.34) n^(-1/5)``. * ``'stata'``: Stata ``kdensity``'s default, ``0.9 min(sd, (p75-p25)/1.349) n^(-1/5)`` with Stata's percentiles (unweighted only). * ``'sheather-jones'``: Sheather-Jones solve-the-equation (R ``bw.SJ``, computed without binning). Each rule is applied as-is whatever the kernel. Note that the ``'epanechnikov'`` kernel here has support [-1, 1] (Stata's ``epan2``), not the unit-variance form of R ``density`` and Stata's default ``epanechnikov``.",
          "type": "string"
        },
        "data": {
          "description": "Input data.",
          "type": "string"
        },
        "grid": {
          "description": "Custom evaluation grid.",
          "type": "string"
        },
        "kernel": {
          "default": "gaussian",
          "description": "Kernel function: 'gaussian', 'epanechnikov', 'uniform', 'triangular', 'biweight', 'cosine'.",
          "type": "string"
        },
        "n_grid": {
          "default": 512,
          "description": "Number of evaluation grid points.",
          "type": "integer"
        },
        "weights": {
          "description": "Column name for observation weights.",
          "type": "string"
        },
        "x": {
          "description": "Variable name for density estimation.",
          "type": "string"
        }
      },
      "required": [],
      "type": "object"
    }
  },
  {
    "description": "Results from kernel density estimation.",
    "name": "KDensityResult",
    "parameters": {
      "properties": {
        "bandwidth": {
          "description": "Bandwidth used for local smoothing or kernel weighting.",
          "type": "number"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "density": {
          "description": "density parameter (ndarray).",
          "type": "string"
        },
        "grid": {
          "description": "grid parameter (ndarray).",
          "type": "string"
        },
        "kernel": {
          "description": "Kernel function used for weighting or smoothing.",
          "type": "string"
        },
        "n": {
          "description": "n parameter (int).",
          "type": "integer"
        }
      },
      "required": [
        "grid",
        "density",
        "bandwidth",
        "kernel",
        "n",
        "data"
      ],
      "type": "object"
    }
  },
  {
    "description": "Local polynomial fit at ``eval_point`` with robust bias correction. Validation: validated evidence tier (known-truth, reference, external-parity, or Monte Carlo artifact).",
    "name": "lprobust_at_point",
    "parameters": {
      "properties": {
        "b": {
          "description": "Bias bandwidth, for the order-``p+1`` fit that estimates the leading bias. Defaults to ``h`` (``rho = 1``), which is ``lprobust``'s behaviour when only ``h`` is supplied.",
          "type": "number"
        },
        "deriv": {
          "default": 0,
          "description": "Order of the derivative to report; 0 is the level.",
          "type": "integer"
        },
        "eval_point": {
          "description": "Where to evaluate the regression function.",
          "type": "number"
        },
        "h": {
          "description": "Main bandwidth, for the order-``p`` fit.",
          "type": "number"
        },
        "kernel": {
          "default": "epanechnikov",
          "description": "Default epanechnikov, matching ``lprobust``.",
          "enum": [
            "epanechnikov",
            "triangular",
            "uniform",
            "gaussian"
          ],
          "type": "string"
        },
        "n_neighbors": {
          "default": 3,
          "description": "Neighbours for the nearest-neighbour variance, matching ``lprobust``'s ``vce(nn)`` default of 3.",
          "type": "integer"
        },
        "p": {
          "default": 1,
          "description": "Polynomial order -- 1 is local linear.",
          "type": "integer"
        },
        "x": {
          "description": "Running variable and outcome, same length.",
          "type": "string"
        },
        "y": {
          "description": "Running variable and outcome, same length.",
          "type": "string"
        }
      },
      "required": [
        "x",
        "y",
        "eval_point",
        "h"
      ],
      "type": "object"
    }
  },
  {
    "description": "One evaluation point of :func:`lprobust_at_point`.",
    "name": "LProbustPoint",
    "parameters": {
      "properties": {
        "b": {
          "description": "b parameter (float).",
          "type": "number"
        },
        "eval_point": {
          "description": "eval_point parameter (float).",
          "type": "number"
        },
        "h": {
          "description": "Bandwidth used for local smoothing or kernel weighting.",
          "type": "number"
        },
        "n_eff": {
          "description": "Number of eff.",
          "type": "integer"
        },
        "se_rb": {
          "description": "se_rb parameter (float).",
          "type": "number"
        },
        "se_us": {
          "description": "se_us parameter (float).",
          "type": "number"
        },
        "tau_bc": {
          "description": "tau_bc parameter (float).",
          "type": "number"
        },
        "tau_us": {
          "description": "tau_us parameter (float).",
          "type": "number"
        }
      },
      "required": [
        "eval_point",
        "h",
        "b",
        "n_eff",
        "tau_us",
        "tau_bc",
        "se_us",
        "se_rb"
      ],
      "type": "object"
    }
  },
  {
    "description": "MSE-optimal direct-plug-in bandwidths, as ``bwselect('mse-dpi')``. Validation: validated evidence tier (known-truth, reference, external-parity, or Monte Carlo artifact).",
    "name": "lpbwselect_mse_dpi",
    "parameters": {
      "properties": {
        "bwcheck": {
          "default": 21,
          "description": "bwcheck parameter (int).",
          "type": "integer"
        },
        "bwregul": {
          "default": 1.0,
          "description": "bwregul parameter (float).",
          "type": "number"
        },
        "deriv": {
          "default": 0,
          "description": "deriv parameter (int).",
          "type": "integer"
        },
        "eval_point": {
          "description": "eval_point parameter (float).",
          "type": "number"
        },
        "kernel": {
          "default": "epanechnikov",
          "description": "Kernel function used for weighting or smoothing.",
          "type": "string"
        },
        "n_neighbors": {
          "default": 3,
          "description": "Number of neighbors.",
          "type": "integer"
        },
        "p": {
          "default": 1,
          "description": "p parameter (int).",
          "type": "integer"
        },
        "x": {
          "description": "Primary running variable, regressor, or feature input for this estimator.",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "x",
        "y",
        "eval_point"
      ],
      "type": "object"
    }
  },
  {
    "description": "Estimate a Vector Autoregression (VAR) model. Validation: certified parity evidence.",
    "name": "var",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level.",
          "type": "number"
        },
        "data": {
          "description": "Time series data.",
          "type": "string"
        },
        "lags": {
          "default": 1,
          "description": "Number of lags.",
          "type": "integer"
        },
        "se_df": {
          "default": "stata",
          "description": "Residual-variance denominator for coefficient standard errors. ``'stata'``/``'ml'`` uses ``T`` and matches Stata ``var`` default conditional-MLE standard errors. ``'r'``/``'unbiased'`` uses ``T - k_params`` and matches the equation-by-equation ``lm()`` standard errors returned inside R ``vars::VAR()``.",
          "enum": [
            "stata",
            "ml",
            "r",
            "unbiased"
          ],
          "type": "string"
        },
        "trend": {
          "default": "c",
          "description": "trend parameter (str).",
          "type": "string"
        },
        "variables": {
          "description": "Variable names. If None, uses all numeric columns.",
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "required": [
        "data"
      ],
      "type": "object"
    }
  },
  {
    "description": "Results from VAR estimation.",
    "name": "VARResult",
    "parameters": {
      "properties": {
        "aic": {
          "description": "aic parameter (float).",
          "type": "number"
        },
        "bic": {
          "description": "bic parameter (float).",
          "type": "number"
        },
        "coefs": {
          "description": "coefs parameter (Dict[str, pandas.DataFrame]).",
          "type": "object"
        },
        "det_sigma": {
          "description": "det_sigma parameter (float).",
          "type": "number"
        },
        "hqic": {
          "description": "hqic parameter (float).",
          "type": "number"
        },
        "lags": {
          "description": "lags parameter (int).",
          "type": "integer"
        },
        "log_likelihood": {
          "description": "log_likelihood parameter (float).",
          "type": "number"
        },
        "n_obs": {
          "description": "Number of obs.",
          "type": "integer"
        },
        "residuals": {
          "description": "residuals parameter (DataFrame).",
          "type": "string"
        },
        "se": {
          "description": "se parameter (Dict[str, numpy.ndarray]).",
          "type": "object"
        },
        "se_df": {
          "default": "stata",
          "description": "se_df parameter (str).",
          "type": "string"
        },
        "sigma_u": {
          "description": "sigma_u parameter (DataFrame).",
          "type": "string"
        },
        "var_names": {
          "description": "var_names parameter (List[str]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "required": [
        "coefs",
        "se",
        "residuals",
        "sigma_u",
        "var_names",
        "lags",
        "n_obs",
        "aic",
        "bic",
        "hqic",
        "det_sigma",
        "log_likelihood"
      ],
      "type": "object"
    }
  },
  {
    "description": "Granger causality test. Validation: certified parity evidence.",
    "name": "granger_causality",
    "parameters": {
      "properties": {
        "caused": {
          "description": "Variable being tested for causation.",
          "type": "string"
        },
        "causing": {
          "description": "Variable(s) hypothesised to cause; a list tests all their lags jointly (Stata ``vargranger``'s ``ALL`` row when it lists every other variable).",
          "type": "string"
        },
        "data": {
          "description": "Data (if var_result not provided).",
          "type": "string"
        },
        "lags": {
          "description": "Number of lags (if fitting new VAR).",
          "type": "integer"
        },
        "var_result": {
          "description": "Pre-estimated VAR model.",
          "type": "string"
        }
      },
      "required": [],
      "type": "object"
    }
  },
  {
    "description": "Compute impulse response functions from VAR. Validation: certified parity evidence.",
    "name": "irf",
    "parameters": {
      "properties": {
        "cumulative": {
          "default": false,
          "description": "Return cumulative responses ``sum_{j<=s}`` (Stata ``cirf`` / ``coirf``; ``vars::irf(cumulative = TRUE)``).",
          "type": "boolean"
        },
        "impulse": {
          "description": "Impulse variable (if None, all).",
          "type": "string"
        },
        "orthogonal": {
          "default": true,
          "description": "Orthogonalized (Cholesky) IRF.",
          "type": "boolean"
        },
        "periods": {
          "default": 20,
          "description": "Number of periods for IRF.",
          "type": "integer"
        },
        "response": {
          "description": "Response variable (if None, all).",
          "type": "string"
        },
        "sigma_df": {
          "description": "Divisor of the residual covariance that the orthogonalisation uses: ``'ml'`` = ``T`` (Stata ``irf create`` after ``var``), ``'unbiased'`` = ``T - m`` with ``m`` regressors per equation (``vars::Psi``, and Stata after ``var, dfk``). Default follows the fitted model's ``se_df`` (``'stata'``/``'ml'`` -> ``'ml'``, ``'r'``/``'unbiased'`` -> ``'unbiased'``). Irrelevant when ``orthogonal=False``.",
          "enum": [
            "ml",
            "unbiased"
          ],
          "type": "string"
        },
        "var_result": {
          "description": "Estimated VAR model.",
          "type": "string"
        }
      },
      "required": [
        "var_result"
      ],
      "type": "object"
    }
  },
  {
    "description": "Structural break detection. Validation: certified parity evidence.",
    "name": "structural_break",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level.",
          "type": "number"
        },
        "data": {
          "description": "Time series data (assumed ordered by time).",
          "type": "string"
        },
        "max_breaks": {
          "default": 5,
          "description": "Maximum number of breaks to test.",
          "type": "integer"
        },
        "method": {
          "default": "bai-perron",
          "description": "Estimator or algorithm variant to use.",
          "type": "string"
        },
        "min_segment": {
          "default": 0.15,
          "description": "Minimum segment length as fraction of sample.",
          "type": "number"
        },
        "pvalue_method": {
          "default": "hansen",
          "description": "p-value of the sup-F statistic ('sup-f' / 'chow'). ``'hansen'`` is Hansen's (1997) response-surface approximation of the Andrews (1993) limit law, the one ``strucchange::sctest(type = \"supF\")`` and Stata's ``estat sbsingle`` report, evaluated at ``lambda = ((n - from) to) / (from (n - to))`` of the candidate range (a single candidate date is an ordinary Chow F test, as in strucchange). ``'simulate'`` is the earlier Monte-Carlo draw from the same limit law (5,000 fixed-seed Brownian-bridge replications on a grid tied to ``n``).",
          "enum": [
            "hansen",
            "simulate"
          ],
          "type": "string"
        },
        "x": {
          "description": "Regressors. If None, uses constant only (mean shift).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Dependent variable.",
          "type": "string"
        }
      },
      "required": [],
      "type": "object"
    }
  },
  {
    "description": "Results from structural break tests.",
    "name": "StructuralBreakResult",
    "parameters": {
      "properties": {
        "bic": {
          "description": "bic parameter (Optional[float]).",
          "type": "number"
        },
        "break_dates": {
          "description": "break_dates parameter (list[int]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "f_stats": {
          "description": "f_stats parameter.",
          "type": "string"
        },
        "n_breaks": {
          "description": "Number of breaks.",
          "type": "integer"
        },
        "n_obs": {
          "description": "Number of obs.",
          "type": "integer"
        },
        "p_values": {
          "description": "p_values parameter.",
          "type": "string"
        },
        "rss_full": {
          "description": "rss_full parameter (float).",
          "type": "number"
        },
        "rss_segments": {
          "description": "rss_segments parameter (Optional[float]).",
          "type": "number"
        },
        "test_type": {
          "description": "test_type parameter (str).",
          "type": "string"
        }
      },
      "required": [
        "test_type",
        "break_dates",
        "f_stats",
        "p_values",
        "n_breaks",
        "rss_full",
        "rss_segments",
        "bic",
        "n_obs"
      ],
      "type": "object"
    }
  },
  {
    "description": "CUSUM test for parameter stability. Validation: certified parity evidence.",
    "name": "cusum_test",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "x": {
          "description": "Regressors.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Dependent variable.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y"
      ],
      "type": "object"
    }
  },
  {
    "description": "Randomize units to treatment and control.",
    "name": "randomize",
    "parameters": {
      "properties": {
        "balance_vars": {
          "description": "balance_vars parameter (Optional[List[str]]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "cluster": {
          "description": "Cluster variable for cluster randomization.",
          "type": "string"
        },
        "data": {
          "description": "Data with units to randomize.",
          "type": "string"
        },
        "method": {
          "default": "simple",
          "description": "'simple' (independent Bernoulli draws; ``randomizr::simple_ra``), 'complete' (fixed arm counts; ``complete_ra``), 'stratified' (complete randomization within each stratum; ``block_ra``) or 'cluster' (complete randomization of clusters; ``cluster_ra``). Passing ``strata=`` / ``cluster=`` selects the block / cluster design whatever ``method`` says.",
          "type": "string"
        },
        "n_arms": {
          "default": 2,
          "description": "Number of treatment arms.",
          "type": "integer"
        },
        "n_rerand": {
          "default": 0,
          "description": "Number of rerand.",
          "type": "integer"
        },
        "prob": {
          "description": "Probability of each arm. Default: equal.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "rerand_threshold": {
          "default": 0.001,
          "description": "rerand_threshold parameter (float).",
          "type": "number"
        },
        "seed": {
          "description": "Random seed for reproducible stochastic steps.",
          "type": "integer"
        },
        "strata": {
          "description": "Stratification variable for block randomization.",
          "type": "string"
        },
        "treatment_col": {
          "default": "treatment",
          "description": "Treatment variable column name.",
          "type": "string"
        }
      },
      "required": [
        "data"
      ],
      "type": "object"
    }
  },
  {
    "description": "Results from randomization.",
    "name": "RandomizationResult",
    "parameters": {
      "properties": {
        "balance": {
          "description": "balance parameter (Optional[Dict[str, Any]]).",
          "type": "object"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "method": {
          "description": "Estimator or algorithm variant to use.",
          "type": "string"
        },
        "n_control": {
          "description": "Number of control.",
          "type": "integer"
        },
        "n_treated": {
          "description": "Number of treated.",
          "type": "integer"
        },
        "seed": {
          "description": "Random seed for reproducible stochastic steps.",
          "type": "integer"
        },
        "strata_col": {
          "description": "Column name for strata.",
          "type": "string"
        },
        "treatment_col": {
          "description": "Treatment variable column name.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "treatment_col",
        "n_treated",
        "n_control",
        "strata_col",
        "method",
        "balance",
        "seed"
      ],
      "type": "object"
    }
  },
  {
    "description": "Check covariate balance between treatment and control. Validation: certified parity evidence.",
    "name": "balance_check",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "covariates": {
          "description": "Covariates to check balance on.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "equal_var": {
          "default": false,
          "description": "Per-covariate difference test. ``False`` is Welch's unequal-variance t-test. ``True`` is the pooled-variance t-test -- the OLS t-test of the covariate on the treatment dummy -- which is what Stata ``iebaltab`` reports (its pair test regresses the variable on the group dummy; its difference is control minus treatment, the opposite sign of ``diff`` here).",
          "type": "boolean"
        },
        "treatment": {
          "description": "Binary treatment variable (0/1).",
          "type": "string"
        }
      },
      "required": [
        "data",
        "treatment",
        "covariates"
      ],
      "type": "object"
    }
  },
  {
    "description": "Results from balance check.",
    "name": "BalanceResult",
    "parameters": {
      "properties": {
        "n_control": {
          "description": "Number of control.",
          "type": "integer"
        },
        "n_treat": {
          "description": "Number of treat.",
          "type": "integer"
        },
        "normalized_diffs": {
          "description": "normalized_diffs parameter (Dict[str, float]).",
          "type": "object"
        },
        "omnibus_f": {
          "description": "omnibus_f parameter (float).",
          "type": "number"
        },
        "omnibus_p": {
          "description": "omnibus_p parameter (float).",
          "type": "number"
        },
        "table": {
          "description": "table parameter (DataFrame).",
          "type": "string"
        }
      },
      "required": [
        "table",
        "omnibus_f",
        "omnibus_p",
        "normalized_diffs",
        "n_treat",
        "n_control"
      ],
      "type": "object"
    }
  },
  {
    "description": "Test for differential attrition in an RCT. Validation: certified parity evidence.",
    "name": "attrition_test",
    "parameters": {
      "properties": {
        "correction": {
          "default": true,
          "description": "Yates' continuity correction for the 2x2 differential-attrition test (R ``chisq.test``'s default). ``False`` gives Pearson's chi2, which is what Stata ``tabulate treat observed, chi2`` reports.",
          "type": "boolean"
        },
        "covariates": {
          "description": "Baseline covariates to test as predictors of attrition.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "observed": {
          "description": "Indicator for whether outcome is observed (1) or missing (0).",
          "type": "string"
        },
        "treatment": {
          "description": "Treatment indicator (0/1).",
          "type": "string"
        }
      },
      "required": [
        "data",
        "treatment",
        "observed"
      ],
      "type": "object"
    }
  },
  {
    "description": "Compute bounds on treatment effects under attrition. Validation: certified parity evidence.",
    "name": "attrition_bounds",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "method": {
          "default": "lee",
          "description": "Bounding method: 'lee' (Lee 2009), 'manski' (worst-case).",
          "type": "string"
        },
        "observed": {
          "description": "Indicator for observed outcome. If None, uses non-missing y.",
          "type": "string"
        },
        "treatment": {
          "description": "Treatment indicator (0/1).",
          "type": "string"
        },
        "trimming": {
          "default": "quantile",
          "description": "Lee trimming rule, as in :func:`sp.lee_bounds`: Lee's sample-quantile estimator, or fractional trimming of the observations tied at the quantile (what Stata ``leebounds`` is written to compute). The Lee bounds equal ``sp.lee_bounds``' exactly.",
          "enum": [
            "quantile",
            "exact"
          ],
          "type": "string"
        },
        "y": {
          "description": "Outcome variable.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "treatment"
      ],
      "type": "object"
    }
  },
  {
    "description": "Results from attrition analysis.",
    "name": "AttritionResult",
    "parameters": {
      "properties": {
        "control_rate": {
          "description": "control_rate parameter.",
          "type": "string"
        },
        "covariate_tests": {
          "description": "covariate_tests parameter.",
          "type": "string"
        },
        "diff_p_value": {
          "description": "diff_p_value parameter.",
          "type": "string"
        },
        "diff_test_stat": {
          "description": "diff_test_stat parameter.",
          "type": "string"
        },
        "n_attrit": {
          "description": "Number of attrit.",
          "type": "string"
        },
        "n_total": {
          "description": "Number of total.",
          "type": "string"
        },
        "overall_rate": {
          "description": "overall_rate parameter.",
          "type": "string"
        },
        "treat_rate": {
          "description": "treat_rate parameter.",
          "type": "string"
        }
      },
      "required": [
        "overall_rate",
        "treat_rate",
        "control_rate",
        "diff_test_stat",
        "diff_p_value",
        "covariate_tests",
        "n_total",
        "n_attrit"
      ],
      "type": "object"
    }
  },
  {
    "description": "Compute optimal sample size and design parameters.",
    "name": "optimal_design",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level.",
          "type": "number"
        },
        "baseline_mean": {
          "default": 0.0,
          "description": "baseline_mean parameter (float).",
          "type": "number"
        },
        "cluster_size": {
          "description": "Average cluster size.",
          "type": "integer"
        },
        "cost_per_cluster": {
          "description": "Cost of adding a cluster (for optimal allocation).",
          "type": "number"
        },
        "cost_per_unit": {
          "description": "Cost per individual unit.",
          "type": "number"
        },
        "design": {
          "default": "individual",
          "description": "'individual', 'cluster', 'stratified'.",
          "type": "string"
        },
        "icc": {
          "default": 0.0,
          "description": "Intra-cluster correlation (for cluster designs).",
          "type": "number"
        },
        "mde": {
          "description": "Minimum detectable effect. If None, compute MDE given n.",
          "type": "number"
        },
        "n_arms": {
          "default": 2,
          "description": "Number of treatment arms.",
          "type": "integer"
        },
        "n_clusters": {
          "description": "Number of clusters (if fixed).",
          "type": "integer"
        },
        "power": {
          "default": 0.8,
          "description": "Statistical power (1 - Type II error).",
          "type": "number"
        },
        "prop_treat": {
          "default": 0.5,
          "description": "Proportion assigned to treatment.",
          "type": "number"
        },
        "r2": {
          "default": 0.0,
          "description": "R-squared from baseline covariates (variance reduction).",
          "type": "number"
        },
        "sigma": {
          "default": 1.0,
          "description": "Standard deviation of the outcome.",
          "type": "number"
        }
      },
      "required": [],
      "type": "object"
    }
  },
  {
    "description": "Results from optimal design calculation.",
    "name": "OptimalDesignResult",
    "parameters": {
      "properties": {
        "alpha": {
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "cluster_size": {
          "description": "cluster_size parameter (Optional[int]).",
          "type": "integer"
        },
        "design_type": {
          "description": "design_type parameter (str).",
          "type": "string"
        },
        "icc": {
          "description": "icc parameter (float).",
          "type": "number"
        },
        "mde": {
          "description": "mde parameter (Optional[float]).",
          "type": "number"
        },
        "n_clusters": {
          "description": "Number of clusters.",
          "type": "integer"
        },
        "n_per_arm": {
          "description": "Number of per arm.",
          "type": "integer"
        },
        "n_total": {
          "description": "Number of total.",
          "type": "integer"
        },
        "power": {
          "description": "power parameter (float).",
          "type": "number"
        }
      },
      "required": [
        "n_total",
        "n_per_arm",
        "n_clusters",
        "cluster_size",
        "icc",
        "mde",
        "power",
        "alpha",
        "design_type"
      ],
      "type": "object"
    }
  },
  {
    "description": "Multiple Imputation by Chained Equations (MICE). Validation: validated evidence tier (known-truth, reference, external-parity, or Monte Carlo artifact).",
    "name": "mice",
    "parameters": {
      "properties": {
        "data": {
          "description": "Data with missing values (NaN).",
          "type": "string"
        },
        "m": {
          "default": 5,
          "description": "Number of imputations.",
          "type": "integer"
        },
        "max_iter": {
          "default": 10,
          "description": "Number of MICE iterations per imputation.",
          "type": "integer"
        },
        "method": {
          "default": "pmm",
          "description": "Imputation method per variable: 'pmm' (predictive mean matching), 'norm' (Bayesian linear), 'logreg' (logistic for binary), 'sample' (random sample). Dict maps variable names to methods.",
          "type": "object"
        },
        "predictors": {
          "description": "Dict mapping variable -> list of predictor variables. If None, uses all other variables.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "print_progress": {
          "default": false,
          "description": "print_progress parameter (bool).",
          "type": "boolean"
        },
        "seed": {
          "description": "Random seed.",
          "type": "integer"
        }
      },
      "required": [
        "data"
      ],
      "type": "object"
    }
  },
  {
    "description": "Results from MICE imputation.",
    "name": "MICEResult",
    "parameters": {
      "properties": {
        "convergence": {
          "description": "convergence parameter.",
          "type": "string"
        },
        "imputed_datasets": {
          "description": "imputed_datasets parameter.",
          "type": "string"
        },
        "methods": {
          "description": "methods parameter.",
          "type": "string"
        },
        "n_imputations": {
          "description": "Number of imputations.",
          "type": "string"
        },
        "n_missing": {
          "description": "Number of missing.",
          "type": "string"
        },
        "n_obs": {
          "description": "Number of obs.",
          "type": "string"
        },
        "variables_imputed": {
          "description": "variables_imputed parameter.",
          "type": "string"
        }
      },
      "required": [
        "imputed_datasets",
        "n_imputations",
        "n_obs",
        "n_missing",
        "variables_imputed",
        "methods",
        "convergence"
      ],
      "type": "object"
    }
  },
  {
    "description": "Run an estimator on each imputed dataset and combine using Rubin's rules. Validation: certified parity evidence.",
    "name": "mi_estimate",
    "parameters": {
      "properties": {
        "estimator": {
          "description": "Estimation function that returns an EconometricResults object.",
          "type": "string"
        },
        "mice_result": {
          "description": "Result from mice().",
          "type": "string"
        }
      },
      "required": [
        "mice_result",
        "estimator"
      ],
      "type": "object"
    }
  },
  {
    "description": "Mendelian Randomization analysis using summary statistics. Validation: certified parity evidence.",
    "name": "mendelian_randomization",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "beta_exposure": {
          "description": "Column name for SNP-exposure association beta.",
          "type": "string"
        },
        "beta_outcome": {
          "description": "Column name for SNP-outcome association beta.",
          "type": "string"
        },
        "data": {
          "description": "Summary statistics with one row per SNP/instrument.",
          "type": "string"
        },
        "exposure_name": {
          "default": "Exposure",
          "description": "exposure_name parameter (str).",
          "type": "string"
        },
        "methods": {
          "description": "MR methods to use. Default: ['ivw', 'egger', 'weighted_median'].",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "outcome_name": {
          "default": "Outcome",
          "description": "outcome_name parameter (str).",
          "type": "string"
        },
        "se_exposure": {
          "description": "Column name for SNP-exposure SE.",
          "type": "string"
        },
        "se_outcome": {
          "description": "Column name for SNP-outcome SE.",
          "type": "string"
        },
        "seed": {
          "description": "Random seed for reproducible stochastic steps.",
          "type": "integer"
        }
      },
      "required": [],
      "type": "object"
    }
  },
  {
    "description": "Results from Mendelian Randomization analysis.",
    "name": "MRResult",
    "parameters": {
      "properties": {
        "estimates": {
          "description": "estimates parameter (DataFrame).",
          "type": "string"
        },
        "exposure": {
          "description": "Exposure term or exposure column.",
          "type": "string"
        },
        "heterogeneity": {
          "description": "heterogeneity parameter (Dict[str, Any]).",
          "type": "object"
        },
        "n_snps": {
          "description": "Number of snps.",
          "type": "integer"
        },
        "outcome": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        },
        "pleiotropy": {
          "description": "pleiotropy parameter (Optional[Dict[str, float]]).",
          "type": "object"
        }
      },
      "required": [
        "estimates",
        "heterogeneity",
        "pleiotropy",
        "n_snps",
        "exposure",
        "outcome"
      ],
      "type": "object"
    }
  },
  {
    "description": "MR-Egger regression. Validation: certified parity evidence.",
    "name": "mr_egger",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "beta_exposure": {
          "description": "beta_exposure parameter (ndarray).",
          "type": "string"
        },
        "beta_outcome": {
          "description": "beta_outcome parameter (ndarray).",
          "type": "string"
        },
        "se_exposure": {
          "description": "se_exposure parameter (ndarray).",
          "type": "string"
        },
        "se_outcome": {
          "description": "se_outcome parameter (ndarray).",
          "type": "string"
        }
      },
      "required": [
        "beta_exposure",
        "beta_outcome",
        "se_exposure",
        "se_outcome"
      ],
      "type": "object"
    }
  },
  {
    "description": "Inverse-Variance Weighted (IVW) MR estimator. Validation: certified parity evidence.",
    "name": "mr_ivw",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "beta_exposure": {
          "description": "beta_exposure parameter (ndarray).",
          "type": "string"
        },
        "beta_outcome": {
          "description": "beta_outcome parameter (ndarray).",
          "type": "string"
        },
        "model": {
          "default": "default",
          "description": "Model variant or parameterisation to fit.",
          "type": "string"
        },
        "se_exposure": {
          "description": "se_exposure parameter (ndarray).",
          "type": "string"
        },
        "se_outcome": {
          "description": "se_outcome parameter (ndarray).",
          "type": "string"
        }
      },
      "required": [
        "beta_exposure",
        "beta_outcome",
        "se_exposure",
        "se_outcome"
      ],
      "type": "object"
    }
  },
  {
    "description": "Weighted median MR estimator. Validation: certified parity evidence.",
    "name": "mr_median",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "beta_exposure": {
          "description": "beta_exposure parameter (ndarray).",
          "type": "string"
        },
        "beta_outcome": {
          "description": "beta_outcome parameter (ndarray).",
          "type": "string"
        },
        "n_boot": {
          "default": 1000,
          "description": "Parametric bootstrap for the standard error: draw ``bx`` and ``by`` from their normal sampling distributions and take the weighted median of the resampled ratios with the ORIGINAL weights, as the reference does. The standard error is stochastic; the estimate is not.",
          "type": "integer"
        },
        "penalized": {
          "default": false,
          "description": "Kept for backward compatibility; equivalent to ``weighting=\"penalized\"``.",
          "type": "boolean"
        },
        "se_exposure": {
          "description": "se_exposure parameter (ndarray).",
          "type": "string"
        },
        "se_outcome": {
          "description": "se_outcome parameter (ndarray).",
          "type": "string"
        },
        "seed": {
          "description": "Parametric bootstrap for the standard error: draw ``bx`` and ``by`` from their normal sampling distributions and take the weighted median of the resampled ratios with the ORIGINAL weights, as the reference does. The standard error is stochastic; the estimate is not.",
          "type": "integer"
        },
        "weighting": {
          "description": "``\"weighted\"`` uses ``(bx / se_y)^2``; ``\"simple\"`` equal weights; ``\"penalized\"`` multiplies the weighted weights by ``min(1, 20 q_j)``, where ``q_j`` is the upper-tail chi-square(1) p-value of variant j's heterogeneity about the weighted median. Defaults to ``\"penalized\"`` if ``penalized=True`` else ``\"weighted\"``.",
          "enum": [
            "weighted",
            "simple",
            "penalized"
          ],
          "type": "string"
        }
      },
      "required": [
        "beta_exposure",
        "beta_outcome",
        "se_exposure",
        "se_outcome"
      ],
      "type": "object"
    }
  },
  {
    "description": "Multi-cutoff RD design. Validation: certified parity evidence.",
    "name": "rdmc",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "bandwidth": {
          "description": "Bandwidth for local polynomial. If None, uses Silverman rule.",
          "type": "number"
        },
        "cutoff_var": {
          "description": "cutoff_var parameter (Optional[str]).",
          "type": "string"
        },
        "cutoffs": {
          "description": "Cutoff values.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "kernel": {
          "default": "triangular",
          "description": "Kernel function used for weighting or smoothing.",
          "type": "string"
        },
        "pooling": {
          "default": "ivw",
          "description": "Pooling method: 'ivw' (inverse-variance weighted) or 'equal'.",
          "type": "string"
        },
        "x": {
          "description": "Running variable.",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "x"
      ],
      "type": "object"
    }
  },
  {
    "description": "Multi-score / Geographic RD design at a single boundary point. Validation: certified parity evidence.",
    "name": "rdms",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "bandwidth": {
          "description": "Fixed bandwidth on the signed-distance score. When omitted the CCT MSE-optimal cascade selects it, as the reference does.",
          "type": "number"
        },
        "cutoff1": {
          "default": 0,
          "description": "Cutoff for x1.",
          "type": "number"
        },
        "cutoff2": {
          "default": 0,
          "description": "Cutoff for x2.",
          "type": "number"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "kernel": {
          "default": "triangular",
          "description": "Kernel function used for weighting or smoothing.",
          "type": "string"
        },
        "treat": {
          "description": "Column holding the 0/1 treatment indicator (R's ``zvar``). Strongly",
          "type": "string"
        },
        "x1": {
          "description": "First running variable (e.g., latitude distance to boundary).",
          "type": "string"
        },
        "x2": {
          "description": "Second running variable (e.g., longitude distance to boundary).",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "x1",
        "x2"
      ],
      "type": "object"
    }
  },
  {
    "description": "Results from multi-cutoff/multi-score RD.",
    "name": "RDMultiResult",
    "parameters": {
      "properties": {
        "cutoff_results": {
          "description": "cutoff_results parameter (List[Dict[str, Any]]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "method": {
          "description": "Estimator or algorithm variant to use.",
          "type": "string"
        },
        "n_cutoffs": {
          "description": "Number of cutoffs.",
          "type": "integer"
        },
        "n_total": {
          "description": "Number of total.",
          "type": "integer"
        },
        "pooled_ci": {
          "description": "pooled_ci parameter (Tuple[float, float]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "pooled_estimate": {
          "description": "pooled_estimate parameter (float).",
          "type": "number"
        },
        "pooled_se": {
          "description": "pooled_se parameter (float).",
          "type": "number"
        }
      },
      "required": [
        "cutoff_results",
        "pooled_estimate",
        "pooled_se",
        "pooled_ci",
        "n_cutoffs",
        "n_total",
        "method"
      ],
      "type": "object"
    }
  },
  {
    "description": "User-friendly alias for :func:`sp.rdmc` (multi-cutoff RD). Validation: certified parity evidence.",
    "name": "multi_cutoff_rd",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "bandwidth": {
          "description": "Bandwidth used for local smoothing or kernel weighting.",
          "type": "number"
        },
        "cutoff_var": {
          "description": "cutoff_var parameter (Optional[str]).",
          "type": "string"
        },
        "cutoffs": {
          "description": "cutoffs parameter (Optional[List[float]]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "kernel": {
          "default": "triangular",
          "description": "Kernel function used for weighting or smoothing.",
          "type": "string"
        },
        "pooling": {
          "default": "ivw",
          "description": "pooling parameter (str).",
          "type": "string"
        },
        "x": {
          "description": "Primary running variable, regressor, or feature input for this estimator.",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "x"
      ],
      "type": "object"
    }
  },
  {
    "description": "User-friendly alias for :func:`sp.rdms` (multi-score RD). Validation: certified parity evidence.",
    "name": "geographic_rd",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "bandwidth": {
          "description": "Bandwidth used for local smoothing or kernel weighting.",
          "type": "number"
        },
        "cutoff1": {
          "default": 0,
          "description": "cutoff1 parameter (float).",
          "type": "number"
        },
        "cutoff2": {
          "default": 0,
          "description": "cutoff2 parameter (float).",
          "type": "number"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "kernel": {
          "default": "triangular",
          "description": "Kernel function used for weighting or smoothing.",
          "type": "string"
        },
        "treat": {
          "description": "Treatment indicator or first-treatment-period column.",
          "type": "string"
        },
        "x1": {
          "description": "x1 parameter (str).",
          "type": "string"
        },
        "x2": {
          "description": "x2 parameter (str).",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "x1",
        "x2"
      ],
      "type": "object"
    }
  },
  {
    "description": "User-friendly alias for :func:`sp.rd2d` (boundary discontinuity design). Validation: validated evidence tier (known-truth, reference, external-parity, or Monte Carlo artifact).",
    "name": "boundary_rd",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "approach": {
          "default": "location",
          "description": "approach parameter (str).",
          "type": "string"
        },
        "boundary": {
          "description": "boundary parameter (Optional[Callable]).",
          "type": "string"
        },
        "bwcheck": {
          "default": "auto",
          "description": "bwcheck parameter (Union[int, str, None]).",
          "type": "integer"
        },
        "bwparam": {
          "default": "main",
          "description": "bwparam parameter (str).",
          "type": "string"
        },
        "bwselect": {
          "default": "mserd",
          "description": "bwselect parameter (str).",
          "type": "string"
        },
        "cluster": {
          "description": "Cluster identifier column for clustered standard errors.",
          "type": "string"
        },
        "cqt": {
          "default": 0.5,
          "description": "cqt parameter (float).",
          "type": "number"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "deriv": {
          "default": [
            0,
            0
          ],
          "description": "deriv parameter (Tuple[int, int]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "distance": {
          "description": "distance parameter (Optional[Union[np.ndarray, List[str]]]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "eval_points": {
          "description": "eval_points parameter (Optional[np.ndarray]).",
          "type": "string"
        },
        "fitmethod": {
          "default": "joint",
          "description": "fitmethod parameter (str).",
          "type": "string"
        },
        "fuzzy": {
          "description": "fuzzy parameter (Optional[str]).",
          "type": "string"
        },
        "h": {
          "description": "Bandwidth used for local smoothing or kernel weighting.",
          "type": "number"
        },
        "kernel": {
          "default": "triangular",
          "description": "Kernel function used for weighting or smoothing.",
          "type": "string"
        },
        "kernel_type": {
          "default": "prod",
          "description": "kernel_type parameter (str).",
          "type": "string"
        },
        "kink_position": {
          "description": "kink_position parameter (Optional[Sequence]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "kink_unknown": {
          "default": false,
          "description": "kink_unknown parameter (Union[bool, Tuple[bool, bool]]).",
          "type": "boolean"
        },
        "masspoints": {
          "default": "check",
          "description": "masspoints parameter (str).",
          "type": "string"
        },
        "method": {
          "default": "dpi",
          "description": "Estimator or algorithm variant to use.",
          "type": "string"
        },
        "n_eval": {
          "default": 1,
          "description": "Number of eval.",
          "type": "integer"
        },
        "p": {
          "default": 1,
          "description": "p parameter (int).",
          "type": "integer"
        },
        "q": {
          "description": "Quantile level.",
          "type": "integer"
        },
        "scalebiascrct": {
          "default": 1.0,
          "description": "scalebiascrct parameter (float).",
          "type": "number"
        },
        "scaleregul": {
          "description": "scaleregul parameter (Optional[float]).",
          "type": "number"
        },
        "side": {
          "default": "two",
          "description": "side parameter (str).",
          "type": "string"
        },
        "stdvars": {
          "default": true,
          "description": "stdvars parameter (bool).",
          "type": "boolean"
        },
        "tangvec": {
          "description": "tangvec parameter (Optional[np.ndarray]).",
          "type": "string"
        },
        "treatment": {
          "description": "Treatment indicator, treatment variable, or treatment array.",
          "type": "string"
        },
        "vce": {
          "default": "hc1",
          "description": "vce parameter (str).",
          "type": "string"
        },
        "weights": {
          "description": "Observation weights.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "x1": {
          "description": "x1 parameter (str).",
          "type": "string"
        },
        "x2": {
          "description": "x2 parameter (str).",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "x1",
        "x2",
        "treatment"
      ],
      "type": "object"
    }
  },
  {
    "description": "User-friendly alias for :func:`sp.rd_multi_score`.",
    "name": "multi_score_rd",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "bandwidth": {
          "description": "Bandwidth used for local smoothing or kernel weighting.",
          "type": "number"
        },
        "cutoffs": {
          "description": "cutoffs parameter (List[float]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "kernel": {
          "default": "triangular",
          "description": "Kernel function used for weighting or smoothing.",
          "type": "string"
        },
        "running_vars": {
          "description": "running_vars parameter (List[str]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "running_vars",
        "cutoffs"
      ],
      "type": "object"
    }
  },
  {
    "description": "Heterogeneous-adoption DiD using quasi-untreateded groups. Validation: validated evidence tier (known-truth, reference, external-parity, or Monte Carlo artifact).",
    "name": "did_had",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "1 - alpha confidence level.",
          "type": "number"
        },
        "bandwidth": {
          "default": "mse-dpi",
          "description": "Bandwidth for the local polynomial fit at dose zero -- one value, or one per reported horizon (placebos first, then effects). **Required for now.** Stata's default ``bw_method('mse-dpi')`` selector is not yet implemented; see Notes.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "data": {
          "description": "Long panel, one row per group-period.",
          "type": "string"
        },
        "dynamic": {
          "default": false,
          "description": "Scale effect l by the average **cumulative** dose from F to ``F-1+l`` instead of the dose at ``F-1+l``. The current-dose normalization is right under a static model, the cumulative one under a dynamic model where past treatment still matters.",
          "type": "boolean"
        },
        "effects": {
          "default": 1,
          "description": "Number of event-study effects. Effect l is the effect at period ``F-1+l``, i.e. l periods after adoption.",
          "type": "integer"
        },
        "group": {
          "description": "Column names. ``treat`` is the dose, not an indicator.",
          "type": "string"
        },
        "kernel": {
          "default": "epanechnikov",
          "description": "Default epanechnikov, matching ``did_had``.",
          "enum": [
            "epanechnikov",
            "triangular",
            "uniform",
            "gaussian"
          ],
          "type": "string"
        },
        "placebo": {
          "default": 0,
          "description": "Number of placebo estimates, built symmetrically: the ``F-1`` to ``F-1+l`` evolution is replaced by ``F-1`` to ``F-1-l``, with the dose taken from the matching post period.",
          "type": "integer"
        },
        "time": {
          "description": "Column names. ``treat`` is the dose, not an indicator.",
          "type": "string"
        },
        "treat": {
          "description": "Column names. ``treat`` is the dose, not an indicator.",
          "type": "string"
        },
        "trends_lin": {
          "default": false,
          "description": "Allow group-specific linear trends, estimated from each group's ``F-2`` to ``F-1`` evolution and subtracted. Costs one placebo, and needs at least three pre-treat periods.",
          "type": "boolean"
        },
        "y": {
          "description": "Column names. ``treat`` is the dose, not an indicator.",
          "type": "string"
        },
        "yatchew": {
          "default": false,
          "description": "Report the Yatchew differencing test alongside each horizon. Effects are tested for **linearity** in the dose (order 1), placebos for **mean independence** of the pre-period evolution from the future dose (order 0). Theorem 5 of the paper: with (quasi-)untreated groups, plain OLS of the evolution on the dose is unbiased for the same estimand *iff* that conditional expectation is linear. Failing to reject therefore licenses the far simpler estimator; rejecting says the nonparametric machinery is doing real work.",
          "type": "boolean"
        }
      },
      "required": [
        "data",
        "y",
        "group",
        "time",
        "treat"
      ],
      "type": "object"
    }
  },
  {
    "description": "Test that quasi-untreateded groups exist (dCDH et al., Section 3.3). Validation: validated evidence tier (known-truth, reference, external-parity, or Monte Carlo artifact).",
    "name": "quasi_untreated_test",
    "parameters": {
      "properties": {
        "dose": {
          "description": "Doses at the horizon being tested. Non-positive values are",
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "required": [
        "dose"
      ],
      "type": "object"
    }
  },
  {
    "description": "Yatchew differencing test that E[y | x] is a polynomial of ``order``. Validation: validated evidence tier (known-truth, reference, external-parity, or Monte Carlo artifact).",
    "name": "yatchew_linearity_test",
    "parameters": {
      "properties": {
        "het_robust": {
          "default": true,
          "description": "Use the heteroskedasticity-robust statistic of the paper's Appendix E. ``did_had`` always does.",
          "type": "boolean"
        },
        "order": {
          "default": 1,
          "description": "Polynomial order under the null. 1 = linear, 0 = constant.",
          "type": "integer"
        },
        "x": {
          "description": "Running variable and outcome, same length.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Running variable and outcome, same length.",
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "required": [
        "x",
        "y"
      ],
      "type": "object"
    }
  },
  {
    "description": "Interactive fixed effects estimator (Bai 2009). Validation: certified parity evidence.",
    "name": "interactive_fe",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "data": {
          "description": "Balanced panel data.",
          "type": "string"
        },
        "dof": {
          "default": "exact",
          "description": "Parameters absorbed by the factor structure. ``'exact'`` counts ``r(N + T - r)`` (``F'`` is identified only up to an rxr rotation); ``'regife'`` counts ``r(N + T)``, as Stata ``regife`` does through ``reghdfe`` (which flags that it could not detect the redundant ones). Enters ``sigma2`` and ``K`` in the cluster factor.",
          "enum": [
            "exact",
            "regife"
          ],
          "type": "string"
        },
        "id": {
          "default": "id",
          "description": "Unit identifier.",
          "type": "string"
        },
        "maxiter": {
          "default": 10000,
          "description": "maxiter parameter (int).",
          "type": "integer"
        },
        "method": {
          "default": "iterative",
          "description": "Bai's (2009) iterated least squares: alternate principal components of ``Y - Xbeta`` (factors) and least squares of ``Y M_F`` on ``X M_F`` (slopes) until the slopes move by less than ``tol``. ``'pca'`` is accepted for backward compatibility and runs the same algorithm (it always did); it is deprecated.",
          "type": "string"
        },
        "n_factors": {
          "default": 1,
          "description": "Number of interactive factors (r).",
          "type": "integer"
        },
        "robust": {
          "default": true,
          "description": "``True``: cluster-robust by unit (Bai's ``D0-1 D_Z D0-1`` with the Stata CR1 factor ``G/(G-1)*(NT-1)/(NT-K)``); ``False``: Bai's homoskedastic ``sigma2 D0-1``. In both, the regressors enter through ``Z_j = M_ X_j M_F`` -- projected off *both* the factor space and the loading space.",
          "type": "boolean"
        },
        "time": {
          "default": "time",
          "description": "Time identifier.",
          "type": "string"
        },
        "tol": {
          "default": 1e-10,
          "description": "Convergence criterion on max |Deltabeta|. The iteration converges linearly, so the default is tight (``regife``'s own default is 1e-9).",
          "type": "number"
        },
        "x": {
          "description": "Regressors.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Dependent variable.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "x"
      ],
      "type": "object"
    }
  },
  {
    "description": "Panel unit root test. Validation: certified parity evidence.",
    "name": "panel_unitroot",
    "parameters": {
      "properties": {
        "convention": {
          "default": "stata",
          "description": "Where Stata ``xtunitroot`` and ``plm::purtest`` differ: * IPS: the IPS Table-3 moments are looked up at T = observations per panel (Stata) or T - lags - 1 (plm); * LLC: the LLC mean/std adjustments at T~ = T - lags - 1 (Stata) or T (plm); long-run-variance bandwidth ``int(3.21 T^(1/3))`` (Stata) or ``round(3.21 T^(1/3))`` (plm); Stata normalises by the short-run variance rss/n and uses the se with divisor N (plm with ``dfcor = FALSE`` is identical on those two).",
          "enum": [
            "stata",
            "plm"
          ],
          "type": "string"
        },
        "data": {
          "description": "Panel data.",
          "type": "string"
        },
        "dfcor": {
          "description": "Residual variance of the per-unit ADF regressions (IPS, Fisher, plm-LLC) and of the Hadri residuals: rss/(n - K) if True, rss/n if False. Default True for ``convention='stata'`` (OLS, as Stata's ``regress``) and False for ``'plm'`` (plm's default).",
          "type": "boolean"
        },
        "id": {
          "default": "id",
          "description": "Unit identifier.",
          "type": "string"
        },
        "lags": {
          "description": "Number of lagged differences in every ADF regression (Stata ``lags()``, plm ``lags = <int>``). If None, uses ``int(4 * (T / 100) ** 0.25)`` per unit (not an information criterion). Ignored by 'hadri'.",
          "type": "integer"
        },
        "robust": {
          "default": true,
          "description": "Hadri only: heteroskedasticity-consistent version (unit-specific variances; Stata ``hadri, robust``, plm ``Hcons = TRUE``). False pools the variance (Stata default, plm ``Hcons = FALSE``).",
          "type": "boolean"
        },
        "test": {
          "default": "ips",
          "description": "Test type: 'llc' (Levin-Lin-Chu), 'ips' (Im-Pesaran-Shin W-t-bar), 'fisher' (Maddala-Wu inverse chi-squared of the per-unit ADF p-values; the inverse-normal, inverse-logit and modified statistics are in ``details``), 'hadri' (stationarity LM test).",
          "type": "string"
        },
        "time": {
          "default": "time",
          "description": "Time identifier.",
          "type": "string"
        },
        "trend": {
          "default": "c",
          "description": "'n' (none), 'c' (constant), 'ct' (constant + trend). IPS and Hadri require 'c' or 'ct'.",
          "type": "string"
        },
        "variable": {
          "description": "Variable to test.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "variable"
      ],
      "type": "object"
    }
  },
  {
    "description": "Results from panel unit root test.",
    "name": "PanelUnitRootResult",
    "parameters": {
      "properties": {
        "details": {
          "description": "details parameter (Optional[Dict[str, Any]]).",
          "type": "object"
        },
        "individual_stats": {
          "description": "individual_stats parameter.",
          "type": "string"
        },
        "lags": {
          "description": "lags parameter.",
          "type": "string"
        },
        "n_periods": {
          "description": "Number of periods.",
          "type": "integer"
        },
        "n_units": {
          "description": "Number of units.",
          "type": "integer"
        },
        "p_value": {
          "description": "p_value parameter (float).",
          "type": "number"
        },
        "statistic": {
          "description": "statistic parameter (float).",
          "type": "number"
        },
        "test_type": {
          "description": "test_type parameter (str).",
          "type": "string"
        }
      },
      "required": [
        "test_type",
        "statistic",
        "p_value",
        "n_units",
        "n_periods",
        "individual_stats",
        "lags"
      ],
      "type": "object"
    }
  },
  {
    "description": "Engle-Granger (1987) two-step cointegration test. Validation: certified parity evidence.",
    "name": "engle_granger",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Level at which ``rank`` (1 = cointegrated) is decided.",
          "enum": [
            "0.01",
            "0.05",
            "0.10"
          ],
          "type": "number"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "lags": {
          "description": "Number of lagged differences ``L`` in the step-2 regression (Stata ``egranger, lags()``). If None, uses the fixed rule ``int(4 * (n / 100) ** 0.25)``.",
          "type": "integer"
        },
        "trend": {
          "default": "c",
          "description": "Deterministic terms in step 1: constant; constant + linear trend (``egranger, trend``); constant + linear + quadratic trend (``egranger, qtrend``). The trend is ``t = 0, 1, ...``.",
          "enum": [
            "c",
            "ct",
            "ctt"
          ],
          "type": "string"
        },
        "variables": {
          "description": "Variables to test (first is dependent).",
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "required": [
        "data"
      ],
      "type": "object"
    }
  },
  {
    "description": "Johansen (1991) cointegration test. Validation: certified parity evidence.",
    "name": "johansen",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Level of the Osterwald-Lenum critical values (the table Stata's ``vecrank`` uses) that decide ``rank``.",
          "enum": [
            "0.05",
            "0.01"
          ],
          "type": "number"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "lags": {
          "default": 1,
          "description": "Number of lagged **differences** in the VECM. This is Stata's ``vecrank, lags(p)`` minus one and ``ca.jo(K = lags + 1)``.",
          "type": "integer"
        },
        "test": {
          "default": "trace",
          "description": "test parameter (str).",
          "enum": [
            "trace",
            "maxeig"
          ],
          "type": "string"
        },
        "trend": {
          "default": "c",
          "description": "Deterministic specification (Stata ``vecrank, trend()`` names in parentheses): ``'n'`` (``none``); ``'rc'`` (``rconstant``, constant restricted to the cointegrating space = ``ca.jo(ecdet=\"const\")``); ``'c'`` (``constant``, unrestricted constant = ``ca.jo(ecdet=\"none\")``); ``'rt'`` (``rtrend``, trend restricted to the cointegrating space, unrestricted constant = ``ca.jo(ecdet=\"trend\")``); ``'ct'`` (``trend``, unrestricted constant and trend). Stata's long names are accepted as aliases.",
          "type": "string"
        },
        "variables": {
          "description": "Variables to test.",
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "required": [
        "data"
      ],
      "type": "object"
    }
  },
  {
    "description": "Results from cointegration test.",
    "name": "CointegrationResult",
    "parameters": {
      "properties": {
        "critical_values": {
          "description": "critical_values parameter (list[float]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "eigenvalues": {
          "description": "eigenvalues parameter (Optional[numpy.ndarray]).",
          "type": "string"
        },
        "eigenvectors": {
          "description": "eigenvectors parameter (Optional[numpy.ndarray]).",
          "type": "string"
        },
        "lags": {
          "description": "lags parameter (int).",
          "type": "integer"
        },
        "n_obs": {
          "description": "Number of obs.",
          "type": "integer"
        },
        "n_vars": {
          "description": "Number of vars.",
          "type": "integer"
        },
        "rank": {
          "description": "rank parameter (int).",
          "type": "integer"
        },
        "test_stats": {
          "description": "test_stats parameter.",
          "type": "string"
        },
        "test_type": {
          "description": "test_type parameter (str).",
          "type": "string"
        }
      },
      "required": [
        "test_type",
        "test_stats",
        "critical_values",
        "rank",
        "eigenvalues",
        "eigenvectors",
        "n_obs",
        "n_vars",
        "lags"
      ],
      "type": "object"
    }
  },
  {
    "description": "Fractional response model (Papke & Wooldridge 1996). Validation: certified parity evidence.",
    "name": "fracreg",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "cluster": {
          "description": "Cluster variable for clustered SE.",
          "type": "string"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "link": {
          "default": "logit",
          "description": "Link function: 'logit' or 'probit'.",
          "type": "string"
        },
        "maxiter": {
          "default": 100,
          "description": "maxiter parameter (int).",
          "type": "integer"
        },
        "robust": {
          "default": "robust",
          "description": "Always use robust SE (quasi-MLE).",
          "type": "string"
        },
        "tol": {
          "default": 1e-08,
          "description": "Numerical convergence tolerance.",
          "type": "number"
        },
        "x": {
          "description": "Regressors.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Outcome variable in [0, 1].",
          "type": "string"
        }
      },
      "required": [],
      "type": "object"
    }
  },
  {
    "description": "Beta regression (Ferrari & Cribari-Neto 2004). Validation: certified parity evidence.",
    "name": "betareg",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "cluster": {
          "description": "Cluster identifier column for clustered standard errors.",
          "type": "string"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "link": {
          "default": "logit",
          "description": "Link for mean: 'logit', 'probit', 'cloglog'.",
          "type": "string"
        },
        "maxiter": {
          "default": 200,
          "description": "maxiter parameter (int).",
          "type": "integer"
        },
        "robust": {
          "default": "nonrobust",
          "description": "Robust standard-error or covariance estimator option.",
          "type": "string"
        },
        "tol": {
          "default": 1e-08,
          "description": "Numerical convergence tolerance.",
          "type": "number"
        },
        "x": {
          "description": "Regressors for the mean equation.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Outcome in (0, 1).",
          "type": "string"
        },
        "z": {
          "description": "Regressors for the precision equation. If None, constant precision.",
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "required": [],
      "type": "object"
    }
  },
  {
    "description": "Bivariate probit model. Validation: certified parity evidence.",
    "name": "biprobit",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "cluster": {
          "description": "Cluster identifier column for clustered standard errors.",
          "type": "string"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "maxiter": {
          "default": 200,
          "description": "maxiter parameter (int).",
          "type": "integer"
        },
        "robust": {
          "default": "nonrobust",
          "description": "Robust standard-error or covariance estimator option.",
          "type": "string"
        },
        "tol": {
          "default": 1e-08,
          "description": "Numerical convergence tolerance.",
          "type": "number"
        },
        "x1": {
          "description": "Regressors for equation 1.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "x2": {
          "description": "Regressors for equation 2. If None, same as x1.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y1": {
          "description": "First binary outcome.",
          "type": "string"
        },
        "y2": {
          "description": "Second binary outcome.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y1",
        "y2",
        "x1"
      ],
      "type": "object"
    }
  },
  {
    "description": "Endogenous treatment effects model. Validation: certified parity evidence.",
    "name": "etregress",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "cluster": {
          "description": "Cluster column. Uses Stata's ML cluster factor ``g/(g-1)``.",
          "type": "string"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "maxiter": {
          "default": 200,
          "description": "maxiter parameter (int).",
          "type": "integer"
        },
        "method": {
          "default": "mle",
          "description": "``'mle'`` for full-information maximum likelihood (Stata's default) or ``'twostep'`` for the control-function estimator (Stata's ``twostep`` option). They are different estimators, not two routes to the same numbers.",
          "type": "string"
        },
        "robust": {
          "default": "nonrobust",
          "description": "``'nonrobust'`` (observed information), ``'robust'`` (sandwich, with Stata's ``N/(N-1)`` factor) or ``'cluster'``. Passing ``cluster=`` implies ``'cluster'``. ``vce=`` is accepted as an alias.",
          "type": "string"
        },
        "tol": {
          "default": 1e-08,
          "description": "Numerical convergence tolerance.",
          "type": "number"
        },
        "treatment": {
          "description": "Binary treatment variable (0/1, both values present).",
          "type": "string"
        },
        "x": {
          "description": "Exogenous regressors.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Outcome variable.",
          "type": "string"
        },
        "z": {
          "description": "Instruments for the selection equation.",
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "required": [
        "data",
        "y",
        "x",
        "treatment",
        "z"
      ],
      "type": "object"
    }
  },
  {
    "description": "Estimate distributional treatment effects. Validation: validated evidence tier (known-truth, reference, external-parity, or Monte Carlo artifact).",
    "name": "distributional_te",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "method": {
          "default": "ipw",
          "description": "Estimator or algorithm variant to use.",
          "enum": [
            "ipw",
            "dr",
            "cic"
          ],
          "type": "string"
        },
        "n_boot": {
          "default": 500,
          "description": "Number of bootstrap replications.",
          "type": "integer"
        },
        "n_grid": {
          "default": 100,
          "description": "Number of grid.",
          "type": "integer"
        },
        "quantiles": {
          "description": "quantiles parameter (Optional[List[float]]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "seed": {
          "description": "Random seed for reproducible stochastic steps.",
          "type": "integer"
        },
        "treatment": {
          "description": "0-3 group encoding for CiC).",
          "type": "string"
        },
        "x": {
          "description": "Primary running variable, regressor, or feature input for this estimator.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "treatment"
      ],
      "type": "object"
    }
  },
  {
    "description": "Container for distributional treatment effect estimates.",
    "name": "DTEResult",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "cdf_counterfactual": {
          "description": "cdf_counterfactual parameter.",
          "type": "string"
        },
        "cdf_treated": {
          "description": "cdf_treated parameter.",
          "type": "string"
        },
        "cvm_pvalue": {
          "default": NaN,
          "description": "cvm_pvalue parameter.",
          "type": "string"
        },
        "cvm_stat": {
          "default": NaN,
          "description": "cvm_stat parameter.",
          "type": "string"
        },
        "dte": {
          "description": "dte parameter.",
          "type": "string"
        },
        "dte_se": {
          "description": "dte_se parameter.",
          "type": "string"
        },
        "grid": {
          "description": "grid parameter.",
          "type": "string"
        },
        "ks_pvalue": {
          "description": "ks_pvalue parameter.",
          "type": "string"
        },
        "ks_stat": {
          "description": "ks_stat parameter.",
          "type": "string"
        },
        "method": {
          "default": "ipw",
          "description": "Estimator or algorithm variant to use.",
          "type": "string"
        },
        "n_boot_failed": {
          "default": 0,
          "description": "Number of boot failed.",
          "type": "integer"
        },
        "n_obs": {
          "description": "Number of obs.",
          "type": "string"
        },
        "qte_effects": {
          "description": "qte_effects parameter.",
          "type": "string"
        },
        "qte_se": {
          "description": "qte_se parameter.",
          "type": "string"
        },
        "qte_taus": {
          "description": "qte_taus parameter.",
          "type": "string"
        }
      },
      "required": [
        "grid",
        "dte",
        "dte_se",
        "qte_taus",
        "qte_effects",
        "qte_se",
        "cdf_treated",
        "cdf_counterfactual",
        "ks_stat",
        "ks_pvalue",
        "n_obs"
      ],
      "type": "object"
    }
  },
  {
    "description": "Estimate a BLP random-coefficients logit demand model. Validation: certified parity evidence.",
    "name": "blp",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level (used in summary output).",
          "type": "number"
        },
        "data": {
          "description": "Panel of product-market observations.",
          "type": "string"
        },
        "instruments": {
          "description": "Excluded instrument columns. If None, standard BLP instruments (functions of own and rival characteristics) are constructed.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "market_id": {
          "default": "market_id",
          "description": "Column identifying markets.",
          "type": "string"
        },
        "maxiter": {
          "default": 200,
          "description": "Maximum outer-loop iterations.",
          "type": "integer"
        },
        "method": {
          "default": "contraction",
          "description": "Inner-loop method. Currently only 'contraction' (NFP) is supported.",
          "type": "string"
        },
        "n_draws": {
          "default": 200,
          "description": "Number of quasi-Monte Carlo draws for integration.",
          "type": "integer"
        },
        "prices": {
          "description": "Column name for product prices.",
          "type": "string"
        },
        "product_id": {
          "default": "product_id",
          "description": "Column identifying products.",
          "type": "string"
        },
        "seed": {
          "description": "Random seed for reproducibility of Monte Carlo draws.",
          "type": "integer"
        },
        "shares": {
          "description": "Column name for observed market shares (0 < s < 1).",
          "type": "string"
        },
        "tol_inner": {
          "default": 1e-12,
          "description": "Contraction mapping tolerance.",
          "type": "number"
        },
        "tol_outer": {
          "default": 1e-06,
          "description": "Outer-loop GMM tolerance.",
          "type": "number"
        },
        "x_linear": {
          "description": "Product characteristics entering mean utility linearly. These will be included alongside price in the linear part.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "x_random": {
          "description": "Subset of x_linear that also have random coefficients. If None, defaults to all of x_linear.",
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "required": [
        "data",
        "shares",
        "prices",
        "x_linear"
      ],
      "type": "object"
    }
  },
  {
    "description": "Results from BLP demand estimation.",
    "name": "BLPResult",
    "parameters": {
      "properties": {
        "_data_index": {
          "description": "_data_index parameter (pd.Index).",
          "type": "string"
        },
        "_elasticity_matrices": {
          "description": "_elasticity_matrices parameter (dict).",
          "type": "object"
        },
        "_market_ids": {
          "description": "_market_ids parameter (np.ndarray).",
          "type": "string"
        },
        "_product_ids": {
          "description": "_product_ids parameter (np.ndarray).",
          "type": "string"
        },
        "alpha": {
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "converged": {
          "description": "converged parameter (bool).",
          "type": "boolean"
        },
        "gmm_objective": {
          "description": "gmm_objective parameter (float).",
          "type": "number"
        },
        "linear_params": {
          "description": "linear_params parameter (pd.Series).",
          "type": "string"
        },
        "mean_utility": {
          "description": "mean_utility parameter (pd.Series).",
          "type": "string"
        },
        "n_markets": {
          "description": "Number of markets.",
          "type": "integer"
        },
        "n_products": {
          "description": "Number of products.",
          "type": "integer"
        },
        "nonlinear_params": {
          "description": "nonlinear_params parameter (pd.Series).",
          "type": "string"
        },
        "own_elasticities": {
          "description": "own_elasticities parameter (pd.Series).",
          "type": "string"
        },
        "se_linear": {
          "description": "se_linear parameter (pd.Series).",
          "type": "string"
        },
        "se_nonlinear": {
          "description": "se_nonlinear parameter (pd.Series).",
          "type": "string"
        }
      },
      "required": [
        "linear_params",
        "nonlinear_params",
        "se_linear",
        "se_nonlinear",
        "mean_utility",
        "own_elasticities",
        "n_markets",
        "n_products",
        "gmm_objective",
        "converged",
        "_elasticity_matrices",
        "_market_ids",
        "_product_ids",
        "_data_index",
        "alpha"
      ],
      "type": "object"
    }
  },
  {
    "description": "Result object for production function estimation.",
    "name": "ProductionResult",
    "parameters": {
      "properties": {
        "coef": {
          "description": "coef parameter (Dict[str, float]).",
          "type": "object"
        },
        "cov": {
          "description": "cov parameter (Optional[np.ndarray]).",
          "type": "string"
        },
        "diagnostics": {
          "description": "diagnostics parameter (Optional[Dict[str, Any]]).",
          "type": "object"
        },
        "method": {
          "description": "Estimator or algorithm variant to use.",
          "type": "string"
        },
        "model_info": {
          "description": "model_info parameter (Optional[Dict[str, Any]]).",
          "type": "object"
        },
        "params": {
          "description": "params parameter (pd.Series).",
          "type": "string"
        },
        "productivity_process": {
          "description": "productivity_process parameter (Dict[str, float]).",
          "type": "object"
        },
        "residuals": {
          "description": "residuals parameter (np.ndarray).",
          "type": "string"
        },
        "sample": {
          "description": "sample parameter (pd.DataFrame).",
          "type": "string"
        },
        "std_errors": {
          "description": "std_errors parameter (pd.Series).",
          "type": "string"
        },
        "tfp": {
          "description": "tfp parameter (np.ndarray).",
          "type": "string"
        }
      },
      "required": [
        "method",
        "params",
        "std_errors",
        "coef",
        "tfp",
        "residuals",
        "productivity_process",
        "sample"
      ],
      "type": "object"
    }
  },
  {
    "description": "Recommend the appropriate estimator(s) for your research question.",
    "name": "recommend",
    "parameters": {
      "properties": {
        "allow_experimental": {
          "default": false,
          "description": "Whether to include estimators registered as ``stability='experimental'`` (or ``'deprecated'``) in the ranked output. The default ``False`` is the agent-safe choice -- an LLM agent or pipeline that asks for an estimator recommendation should not silently land on a frontier MVP. Set ``True`` when you are explicitly exploring frontier methods (e.g. ``causal_text``, ``did_multiplegt_dyn``); dropped names are listed in ``RecommendationResult.warnings`` either way. See ``docs/guides/stability.md`` for the full contract.",
          "type": "boolean"
        },
        "covariates": {
          "description": "Control variables.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "cutoff": {
          "description": "RD cutoff value.",
          "type": "number"
        },
        "dag": {
          "description": "Causal DAG for identification analysis.",
          "type": "string"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "design": {
          "description": "Override design detection: 'rct', 'did', 'rd', 'iv', 'observational', 'panel', 'cross-section'.",
          "type": "string"
        },
        "id": {
          "description": "Unit identifier (for panel data).",
          "type": "string"
        },
        "instrument": {
          "description": "Instrumental variable.",
          "type": "string"
        },
        "mediator": {
          "description": "Mediator variable name. Triggers mediation / front-door recommendations (Imai-Keele-Tingley 2010, VanderWeele et al. 2014, Pearl 1995).",
          "type": "string"
        },
        "post_treat_strata": {
          "description": "Binary post-treatment variable defining principal strata (take-up, survival, employment, ...). Triggers `sp.principal_strat` (Frangakis & Rubin 2002).",
          "type": "string"
        },
        "proxy_w": {
          "description": "Outcome-side proxy variables (see `proxy_z`).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "proxy_z": {
          "description": "Treatment-side proxy variables. Triggers `sp.proximal` when `proxy_w` is also supplied (Tchetgen Tchetgen et al. 2020).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "running_var": {
          "description": "Running variable (for RD designs).",
          "type": "string"
        },
        "time": {
          "description": "Time variable (for panel/DID).",
          "type": "string"
        },
        "treatment": {
          "description": "Treatment/exposure variable.",
          "type": "string"
        },
        "tv_confounders": {
          "description": "Time-varying confounders (must be pre-treatment at each period). Triggers `sp.msm` -- Marginal Structural Model via IPTW (Robins, Hernan & Brumback 2000).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "verify": {
          "default": false,
          "description": "If True, run *resampling-stability* checks on the top-k recommendations (bootstrap CV, permutation placebo, 50%-subsample sign agreement) and attach a composite ``score`` to each recommendation's ``verify`` dict. The score is used to re-rank the top-k. Opt-in because it costs extra compute.",
          "type": "boolean"
        },
        "verify_B": {
          "default": 50,
          "description": "Bootstrap replications per recommendation (auto-capped by budget).",
          "type": "integer"
        },
        "verify_budget_s": {
          "default": 30.0,
          "description": "Wall-clock budget (seconds) per verified recommendation.",
          "type": "number"
        },
        "verify_top_k": {
          "default": 3,
          "description": "Number of top recommendations to verify.",
          "type": "integer"
        },
        "y": {
          "description": "Outcome variable.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y"
      ],
      "type": "object"
    }
  },
  {
    "description": "Result from the estimator recommendation engine.",
    "name": "RecommendationResult",
    "parameters": {
      "properties": {
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "data_profile": {
          "description": "data_profile parameter (Dict[str, Any]).",
          "type": "object"
        },
        "design": {
          "description": "design parameter (str).",
          "type": "string"
        },
        "recommendations": {
          "description": "recommendations parameter (List[Dict[str, Any]]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "treatment": {
          "description": "Treatment indicator, treatment variable, or treatment array.",
          "type": "string"
        },
        "warnings": {
          "description": "warnings parameter (List[str]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "recommendations",
        "data_profile",
        "design",
        "warnings",
        "data",
        "y",
        "treatment"
      ],
      "type": "object"
    }
  },
  {
    "description": "Route design facts to a method-selection status.",
    "name": "design_intake",
    "parameters": {
      "properties": {
        "assignment": {
          "description": "assignment parameter (Optional[str]).",
          "type": "string"
        },
        "controls": {
          "description": "Control-variable column names.",
          "type": "string"
        },
        "data_topology": {
          "description": "data_topology parameter (Optional[str]).",
          "type": "string"
        },
        "estimand": {
          "description": "estimand parameter (Optional[str]).",
          "type": "string"
        },
        "identification_support": {
          "description": "identification_support parameter (Optional[str]).",
          "type": "string"
        },
        "needs": {
          "description": "needs parameter (Optional[str]).",
          "type": "string"
        }
      },
      "required": [],
      "type": "object"
    }
  },
  {
    "description": "Structured routing outcome before estimator selection.",
    "name": "IntakeResult",
    "parameters": {
      "properties": {
        "assumptions": {
          "default": null,
          "description": "assumptions parameter (List[str]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "candidate_designs": {
          "default": null,
          "description": "candidate_designs parameter (List[str]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "deciding_question": {
          "description": "deciding_question parameter (Optional[str]).",
          "type": "string"
        },
        "next_step": {
          "default": "",
          "description": "next_step parameter (str).",
          "type": "string"
        },
        "outcome": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        },
        "recommended_design": {
          "description": "recommended_design parameter (Optional[str]).",
          "type": "string"
        },
        "required_data": {
          "default": null,
          "description": "required_data parameter (List[str]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "risks": {
          "default": null,
          "description": "risks parameter (List[str]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "why": {
          "default": "",
          "description": "why parameter (str).",
          "type": "string"
        }
      },
      "required": [
        "outcome"
      ],
      "type": "object"
    }
  },
  {
    "description": "Run design-level identification diagnostics before fitting an estimator.",
    "name": "check_identification",
    "parameters": {
      "properties": {
        "cluster": {
          "description": "Clustering column for inference.",
          "type": "string"
        },
        "cohort": {
          "description": "First-treatment-period column (for staggered DID).",
          "type": "string"
        },
        "covariates": {
          "description": "Candidate control variables.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "cutoff": {
          "description": "RD cutoff value.",
          "type": "number"
        },
        "dag": {
          "description": "Causal DAG. If supplied, runs Cinelli-Forney-Pearl (2022) bad-control detection (mediator, descendant, collider, M-bias) and verifies the covariate set satisfies a valid adjustment criterion. Upgrades correlation heuristic to a principled check.",
          "type": "string"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "design": {
          "description": "Override auto-detected design: one of 'rct', 'did', 'rd', 'iv', 'observational', 'panel'.",
          "type": "string"
        },
        "id": {
          "description": "Panel identifiers.",
          "type": "string"
        },
        "instrument": {
          "description": "IV instrument.",
          "type": "string"
        },
        "running_var": {
          "description": "RD running variable.",
          "type": "string"
        },
        "strict": {
          "default": false,
          "description": "If True, raise :class:`IdentificationError` when the report's verdict is ``'BLOCKERS'``. Use in CI / automated pipelines where you want a hard failure when the design is broken. The exception carries ``.report`` for post-mortem inspection.",
          "type": "boolean"
        },
        "time": {
          "description": "Panel identifiers.",
          "type": "string"
        },
        "treatment": {
          "description": "Binary or continuous treatment column.",
          "type": "string"
        },
        "y": {
          "description": "Outcome column.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y"
      ],
      "type": "object"
    }
  },
  {
    "description": "Report from ``check_identification``.",
    "name": "IdentificationReport",
    "parameters": {
      "properties": {
        "design": {
          "default": "",
          "description": "design parameter (str).",
          "type": "string"
        },
        "findings": {
          "default": null,
          "description": "findings parameter (List[DiagnosticFinding]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "n_obs": {
          "default": 0,
          "description": "Number of obs.",
          "type": "integer"
        },
        "n_units": {
          "description": "Number of units.",
          "type": "integer"
        }
      },
      "required": [],
      "type": "object"
    }
  },
  {
    "description": "A single design-level finding.",
    "name": "DiagnosticFinding",
    "parameters": {
      "properties": {
        "category": {
          "description": "category parameter (str).",
          "type": "string"
        },
        "evidence": {
          "default": null,
          "description": "evidence parameter (Dict[str, Any]).",
          "type": "object"
        },
        "message": {
          "description": "message parameter (str).",
          "type": "string"
        },
        "severity": {
          "description": "severity parameter (str).",
          "type": "string"
        },
        "suggestion": {
          "description": "suggestion parameter (Optional[str]).",
          "type": "string"
        }
      },
      "required": [
        "severity",
        "category",
        "message"
      ],
      "type": "object"
    }
  },
  {
    "description": "Raised by ``check_identification(strict=True)`` when a blocker is found.",
    "name": "IdentificationError",
    "parameters": {
      "properties": {
        "report": {
          "description": "report parameter ('IdentificationReport').",
          "type": "string"
        }
      },
      "required": [
        "report"
      ],
      "type": "object"
    }
  },
  {
    "description": "Run multiple estimators on the same data and compare.",
    "name": "compare_estimators",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "covariates": {
          "description": "Covariate matrix, DataFrame, or column names.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "id": {
          "description": "Panel unit ID.",
          "type": "string"
        },
        "instrument": {
          "description": "instrument parameter (Optional[str]).",
          "type": "string"
        },
        "method_hints": {
          "description": "Per-method keyword overrides, merged with the shared kwargs when dispatching each estimator. Structure:: {'proximal': {'proxy_z': ['z'], 'proxy_w': ['w']}, 'msm': {'time_varying': ['L_lag']}, 'principal_strat': {'strata': 's'}} **Collision rule** (docs/ROADMAP.md Section 6): per-method hints take precedence over the shared kwargs for the method they name. If the top-level ``covariates=['age']`` disagrees with ``method_hints={'proximal': {'covariates': ['age', 'educ']}}``, proximal uses the hint and every other method uses the shared arg. A ``UserWarning`` fires on conflict so the override is visible in the log.",
          "type": "object"
        },
        "methods": {
          "description": "Estimators to compare. Default auto-selects based on data. Classical options: ``'ols'``, ``'matching'``, ``'ipw'``, ``'aipw'``, ``'dml'``, ``'g_computation'``, ``'causal_forest'``, ``'did'``, ``'panel_fe'``. Hint-driven Sprint-B options (require ``method_hints``): ``'proximal'``, ``'msm'``, ``'principal_strat'``, ``'mediate'``, ``'mediate_interventional'``, ``'front_door'``. Each needs method-specific kwargs the shared signature does not expose (proxy_z/proxy_w, time_varying, strata, mediator, etc.) -- pass them through ``method_hints``.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "time": {
          "description": "Time variable.",
          "type": "string"
        },
        "treatment": {
          "description": "Treatment variable (binary).",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "treatment"
      ],
      "type": "object"
    }
  },
  {
    "description": "Results from multi-estimator comparison.",
    "name": "ComparisonResult",
    "parameters": {
      "properties": {
        "agreement": {
          "description": "agreement parameter (Dict[str, float]).",
          "type": "object"
        },
        "estimates_table": {
          "description": "estimates_table parameter (pd.DataFrame).",
          "type": "string"
        },
        "n_obs": {
          "description": "Number of obs.",
          "type": "integer"
        },
        "results": {
          "description": "results parameter (Dict[str, Any]).",
          "type": "object"
        }
      },
      "required": [
        "results",
        "estimates_table",
        "agreement",
        "n_obs"
      ],
      "type": "object"
    }
  },
  {
    "description": "Outcome of cross-validating one estimand across several engines.",
    "name": "CrossValidationResult",
    "parameters": {
      "properties": {
        "agreement": {
          "description": "agreement parameter (AgreementReport).",
          "type": "string"
        },
        "degradations": {
          "description": "degradations parameter (Optional[List[Dict[str, Any]]]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "estimand": {
          "description": "estimand parameter (str).",
          "type": "string"
        },
        "estimates": {
          "description": "estimates parameter (List[EngineEstimate]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "provenance": {
          "description": "provenance parameter (Optional[Dict[str, Any]]).",
          "type": "object"
        },
        "spec": {
          "description": "spec parameter (Dict[str, Any]).",
          "type": "object"
        },
        "term": {
          "description": "term parameter (str).",
          "type": "string"
        }
      },
      "required": [
        "estimand",
        "term",
        "estimates",
        "agreement",
        "spec"
      ],
      "type": "object"
    }
  },
  {
    "description": "Comprehensive assumption audit for any estimated model.",
    "name": "assumption_audit",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for tests.",
          "type": "number"
        },
        "data": {
          "description": "Original data (needed for some tests). Auto-extracted if available.",
          "type": "string"
        },
        "result": {
          "description": "Estimated model result.",
          "type": "string"
        },
        "verbose": {
          "default": true,
          "description": "Print summary automatically.",
          "type": "boolean"
        }
      },
      "required": [
        "result"
      ],
      "type": "object"
    }
  },
  {
    "description": "Results from comprehensive assumption audit.",
    "name": "AssumptionResult",
    "parameters": {
      "properties": {
        "checks": {
          "description": "checks parameter (List[statspai.smart.assumptions.AssumptionCheck]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "critical_failures": {
          "description": "critical_failures parameter (List[str]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "method": {
          "description": "Estimator or algorithm variant to use.",
          "type": "string"
        },
        "n_fail": {
          "description": "Number of fail.",
          "type": "integer"
        },
        "n_inconclusive": {
          "description": "Number of inconclusive.",
          "type": "integer"
        },
        "n_pass": {
          "description": "Number of pass.",
          "type": "integer"
        },
        "overall_grade": {
          "description": "overall_grade parameter (str).",
          "type": "string"
        }
      },
      "required": [
        "method",
        "checks",
        "overall_grade",
        "n_pass",
        "n_fail",
        "n_inconclusive",
        "critical_failures"
      ],
      "type": "object"
    }
  },
  {
    "description": "Reviewer-checklist audit of a fitted StatsPAI result.",
    "name": "audit",
    "parameters": {
      "properties": {
        "result": {
          "description": "Any fitted StatsPAI result with ``model_info`` attached.",
          "type": "string"
        },
        "treatment": {
          "description": "Name of the treatment variable when ``result`` is a plain regression used for causal adjustment on a selection-on-observables design. When supplied, the audit additionally asks for overlap / common-support, post-adjustment balance, and omitted-variable sensitivity -- the checks a referee demands on an observational design but that a bare OLS would otherwise escape. Has no effect on designs whose family already carries these checks (matching / DML / IV / ...). Descriptive regressions (no treatment declared) are never flagged.",
          "type": "string"
        }
      },
      "required": [
        "result"
      ],
      "type": "object"
    }
  },
  {
    "description": "Top-level structured citation for a fitted result.",
    "name": "bib_for",
    "parameters": {
      "properties": {
        "result": {
          "description": "Any fitted result object exposing a ``.cite()`` method.",
          "type": "string"
        }
      },
      "required": [
        "result"
      ],
      "type": "object"
    }
  },
  {
    "description": "Generate a referee-grade *Methods and Formulas* appendix for results.",
    "name": "methods_appendix",
    "parameters": {
      "properties": {
        "format": {
          "default": "latex",
          "description": "Output format. ``\"latex\"`` emits ``\\subsection*`` blocks with display math; ``\"markdown\"`` emits ``###`` headings with ``$$`` math; ``\"text\"`` emits a plain-text rendering.",
          "enum": [
            "latex",
            "markdown",
            "text"
          ],
          "type": "string"
        },
        "include_assumptions": {
          "default": true,
          "description": "Include the identifying-assumptions list.",
          "type": "boolean"
        },
        "include_citation": {
          "default": true,
          "description": "Append the APA-style reference from ``result.cite()``.",
          "type": "boolean"
        },
        "include_diagnostics": {
          "default": true,
          "description": "Include the inference block (SE method, clustering, bandwidth, F, CI).",
          "type": "boolean"
        },
        "include_provenance": {
          "default": true,
          "description": "Append a one-line provenance trace (StatsPAI version + estimator identity + methods-spec key) -- the \"exact code path\" leg of the formula / citation / code-path traceability triple.",
          "type": "boolean"
        },
        "results": {
          "description": "One or more fitted result objects exposing ``method`` / ``model_info`` / ``cite``.",
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "required": [
        "results"
      ],
      "type": "object"
    }
  },
  {
    "description": "Resolve verified BibTeX entries from ``paper.bib`` by citation key.",
    "name": "bibtex",
    "parameters": {
      "properties": {
        "keys": {
          "description": "One or more BibTeX keys, e.g. ``\"chernozhukov2016hdm\"`` or ``[\"callaway2021difference\", \"rambachan2023more\"]``.",
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "required": [
        "keys"
      ],
      "type": "object"
    }
  },
  {
    "description": "Render a one-line status summary of a fitted result.",
    "name": "brief",
    "parameters": {
      "properties": {
        "result": {
          "description": "exposing ``method`` / ``estimate`` / ``se`` / ``pvalue`` / ``ci`` / ``n_obs`` attributes).",
          "type": "string"
        }
      },
      "required": [
        "result"
      ],
      "type": "object"
    }
  },
  {
    "description": "Detect the most plausible study design from a DataFrame.",
    "name": "detect_design",
    "parameters": {
      "properties": {
        "cutoff": {
          "description": "RD cutoff value (the heuristic does NOT auto-discover this).",
          "type": "number"
        },
        "data": {
          "description": "The dataset to inspect. Must have >= 1 row.",
          "type": "string"
        },
        "running_var": {
          "description": "Force this numeric column to be evaluated as an RD running variable.",
          "type": "string"
        },
        "time": {
          "description": "Column the caller has already identified as the time dimension.",
          "type": "string"
        },
        "unit": {
          "description": "Column the caller has already identified as the unit ID. Skips unit-detection and pins this column.",
          "type": "string"
        }
      },
      "required": [
        "data"
      ],
      "type": "object"
    }
  },
  {
    "description": "Return runnable code examples + registry metadata for a function.",
    "name": "examples",
    "parameters": {
      "properties": {
        "name": {
          "description": "Canonical StatsPAI function name (e.g. ``\"did\"``, ``\"regress\"``, ``\"callaway_santanna\"``). Lower-cased and stripped before lookup.",
          "type": "string"
        }
      },
      "required": [
        "name"
      ],
      "type": "object"
    }
  },
  {
    "description": "Method-specific pre-estimation diagnostics.",
    "name": "preflight",
    "parameters": {
      "properties": {
        "data": {
          "description": "Same DataFrame the agent plans to pass to ``sp.{method}(...)``.",
          "type": "string"
        },
        "method": {
          "description": "Name of the StatsPAI estimator to pre-flight (e.g. ``\"did\"``, ``\"rdrobust\"``, ``\"ivreg\"``, ``\"callaway_santanna\"``, ``\"ebalance\"``). Unknown methods get only the universal DataFrame-shape sanity checks.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "method"
      ],
      "type": "object"
    }
  },
  {
    "description": "Set every reachable RNG to a known seed for the duration of the",
    "name": "session",
    "parameters": {
      "properties": {
        "jax": {
          "default": true,
          "description": "Yield a fresh JAX ``PRNGKey(seed)`` to the caller via the ``jax_key`` attribute on the yielded session object, when JAX is already imported. Never imports jax on its own. (JAX has no global state so we can't seed it -- agents must thread the key explicitly.)",
          "type": "boolean"
        },
        "pythonhashseed": {
          "default": false,
          "description": "Set ``PYTHONHASHSEED`` for the duration of the block. Most causal-inference numerics don't depend on dict iteration order, but spec-curve enumerators and graph-based DAG search sometimes do. Off by default to avoid surprising downstream callers.",
          "type": "boolean"
        },
        "seed": {
          "description": "Seed value. ``None`` (the default) means \"snapshot current state but don't reseed\" -- useful for opportunistic save / restore around code that you don't want to leak RNG drift.",
          "type": "integer"
        },
        "torch": {
          "default": true,
          "description": "Seed PyTorch (CPU + CUDA) when the library is already imported. Never imports torch on its own.",
          "type": "boolean"
        }
      },
      "required": [],
      "type": "object"
    }
  },
  {
    "description": "Comprehensive multi-dimensional sensitivity analysis.",
    "name": "sensitivity_dashboard",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "data": {
          "description": "Original data (auto-extracted if possible).",
          "type": "string"
        },
        "dimensions": {
          "description": "Which dimensions to test. Default: all applicable.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "result": {
          "description": "Baseline estimated result.",
          "type": "string"
        },
        "verbose": {
          "default": true,
          "description": "verbose parameter (bool).",
          "type": "boolean"
        }
      },
      "required": [
        "result"
      ],
      "type": "object"
    }
  },
  {
    "description": "Multi-dimensional sensitivity analysis results.",
    "name": "SensitivityDashboard",
    "parameters": {
      "properties": {
        "baseline": {
          "description": "baseline parameter (Dict[str, Any]).",
          "type": "object"
        },
        "dimensions": {
          "description": "dimensions parameter (List[Dict[str, Any]]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "method": {
          "description": "Estimator or algorithm variant to use.",
          "type": "string"
        },
        "overall_stability": {
          "description": "overall_stability parameter (str).",
          "type": "string"
        }
      },
      "required": [
        "baseline",
        "dimensions",
        "overall_stability",
        "method"
      ],
      "type": "object"
    }
  },
  {
    "description": "Publication readiness checklist.",
    "name": "pub_ready",
    "parameters": {
      "properties": {
        "design": {
          "description": "Research design: 'rct', 'did', 'rd', 'iv', 'observational'.",
          "type": "string"
        },
        "has_balance": {
          "default": false,
          "description": "Already have balance table.",
          "type": "boolean"
        },
        "has_heterogeneity": {
          "default": false,
          "description": "Already have subgroup analysis.",
          "type": "boolean"
        },
        "has_mht": {
          "default": false,
          "description": "Already have MHT correction.",
          "type": "boolean"
        },
        "has_placebo": {
          "default": false,
          "description": "Already have placebo tests.",
          "type": "boolean"
        },
        "has_pretrends": {
          "default": false,
          "description": "Already have pre-trend tests.",
          "type": "boolean"
        },
        "has_robustness": {
          "default": false,
          "description": "Already have robustness checks.",
          "type": "boolean"
        },
        "has_sensitivity": {
          "default": false,
          "description": "Already have sensitivity analysis.",
          "type": "boolean"
        },
        "results": {
          "description": "List of estimated result objects.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "venue": {
          "default": "top5_econ",
          "description": "Target venue: 'top5_econ', 'aej_applied', 'rct'.",
          "type": "string"
        }
      },
      "required": [],
      "type": "object"
    }
  },
  {
    "description": "Publication readiness checklist results.",
    "name": "PubReadyResult",
    "parameters": {
      "properties": {
        "checks": {
          "description": "checks parameter (List[Dict[str, Any]]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "missing": {
          "description": "missing parameter (List[str]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "present": {
          "description": "present parameter (List[str]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "score": {
          "description": "score parameter (int).",
          "type": "integer"
        },
        "venue": {
          "description": "venue parameter (str).",
          "type": "string"
        }
      },
      "required": [
        "venue",
        "checks",
        "score",
        "missing",
        "present"
      ],
      "type": "object"
    }
  },
  {
    "description": "Load a famous dataset and a step-by-step replication guide.",
    "name": "replicate",
    "parameters": {
      "properties": {
        "key": {
          "description": "Replication key (see ``sp.list_replications()``).",
          "type": "string"
        },
        "simulated": {
          "description": "Override the entry's default data source. ``True`` forces a simulated replica; ``False`` forces the bundled real CSV (only valid for entries where ``has_real_data`` is True). Default ``None`` uses whatever the entry declares (currently real for ``card_1995`` and ``abadie_2010``).",
          "type": "boolean"
        }
      },
      "required": [
        "key"
      ],
      "type": "object"
    }
  },
  {
    "description": "List all available replication datasets and guides.",
    "name": "list_replications",
    "parameters": {
      "properties": {},
      "required": [],
      "type": "object"
    }
  },
  {
    "description": "Empirically verify a single recommendation.",
    "name": "verify",
    "parameters": {
      "properties": {
        "B": {
          "default": 50,
          "description": "Number of bootstrap replications (auto-reduced if over budget).",
          "type": "integer"
        },
        "K_subsample": {
          "default": 5,
          "description": "Number of 50% subsample splits.",
          "type": "integer"
        },
        "budget_s": {
          "default": 30.0,
          "description": "Wall-clock budget per recommendation (seconds).",
          "type": "number"
        },
        "data": {
          "description": "The original dataset.",
          "type": "string"
        },
        "n_placebo": {
          "default": 5,
          "description": "Number of permutation placebo runs.",
          "type": "integer"
        },
        "rec": {
          "description": "A single entry from ``RecommendationResult.recommendations``.",
          "type": "object"
        },
        "seed": {
          "default": 42,
          "description": "RNG seed for reproducibility.",
          "type": "integer"
        }
      },
      "required": [
        "rec",
        "data"
      ],
      "type": "object"
    }
  },
  {
    "description": "Empirically verify a single recommendation.",
    "name": "verify_recommendation",
    "parameters": {
      "properties": {
        "B": {
          "default": 50,
          "description": "Number of bootstrap replications (auto-reduced if over budget).",
          "type": "integer"
        },
        "K_subsample": {
          "default": 5,
          "description": "Number of 50% subsample splits.",
          "type": "integer"
        },
        "budget_s": {
          "default": 30.0,
          "description": "Wall-clock budget per recommendation (seconds).",
          "type": "number"
        },
        "data": {
          "description": "The original dataset.",
          "type": "string"
        },
        "n_placebo": {
          "default": 5,
          "description": "Number of permutation placebo runs.",
          "type": "integer"
        },
        "rec": {
          "description": "A single entry from ``RecommendationResult.recommendations``.",
          "type": "object"
        },
        "seed": {
          "default": 42,
          "description": "RNG seed for reproducibility.",
          "type": "integer"
        }
      },
      "required": [
        "rec",
        "data"
      ],
      "type": "object"
    }
  },
  {
    "description": "Run verify against built-in DGPs with known true effects.",
    "name": "verify_benchmark",
    "parameters": {
      "properties": {
        "n_reps": {
          "default": 3,
          "description": "Number of independent DGP draws per scenario. Each uses a different seed to average out Monte Carlo noise.",
          "type": "integer"
        },
        "scenarios": {
          "description": "Subset of {'rct', 'did', 'staggered_did', 'rd', 'iv', 'observational'}. Defaults to all six.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "seed": {
          "default": 0,
          "description": "Base seed. Per-rep seeds are ``seed + offset``.",
          "type": "integer"
        },
        "verbose": {
          "default": true,
          "description": "Print per-scenario progress.",
          "type": "boolean"
        },
        "verify_B": {
          "default": 30,
          "description": "Bootstrap replications per verification run.",
          "type": "integer"
        },
        "verify_budget_s": {
          "default": 15.0,
          "description": "Wall-clock budget per verification run.",
          "type": "number"
        }
      },
      "required": [],
      "type": "object"
    }
  },
  {
    "description": "Score ``sp.recommend`` / ``sp.audit`` against the ground-truth corpus.",
    "name": "recommend_benchmark",
    "parameters": {
      "properties": {
        "corpus_path": {
          "description": "Path to an alternative corpus YAML. Defaults to the bundled corpus.",
          "type": "string"
        },
        "fit": {
          "default": true,
          "description": "Also run the dynamic audit pass (fit each top-1 estimator and run ``sp.audit`` on the result). Set ``False`` for a faster recommend-only run.",
          "type": "boolean"
        }
      },
      "required": [],
      "type": "object"
    }
  },
  {
    "description": "Truncated regression (MLE). Validation: certified parity evidence.",
    "name": "truncreg",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "cluster": {
          "description": "Cluster identifier column for clustered standard errors.",
          "type": "string"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "ll": {
          "description": "Lower truncation point. None = no lower truncation.",
          "type": "number"
        },
        "maxiter": {
          "default": 200,
          "description": "maxiter parameter (int).",
          "type": "integer"
        },
        "robust": {
          "default": "nonrobust",
          "description": "Robust standard-error or covariance estimator option.",
          "type": "string"
        },
        "tol": {
          "default": 1e-08,
          "description": "Numerical convergence tolerance.",
          "type": "number"
        },
        "ul": {
          "description": "Upper truncation point. None = no upper truncation.",
          "type": "number"
        },
        "x": {
          "description": "Regressors.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Outcome variable.",
          "type": "string"
        }
      },
      "required": [],
      "type": "object"
    }
  },
  {
    "description": "Seemingly Unrelated Regression (SUR). Validation: certified parity evidence.",
    "name": "sureg",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "equations": {
          "description": "Mapping from equation name to (dep_var, list_of_regressors).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "maxiter": {
          "default": 100,
          "description": "maxiter parameter (int).",
          "type": "integer"
        },
        "method": {
          "default": "fgls",
          "description": "'ols' (equation-by-equation), 'fgls' (feasible GLS / SUR), 'iterative' (iterated SUR).",
          "type": "string"
        },
        "tol": {
          "default": 1e-08,
          "description": "Numerical convergence tolerance.",
          "type": "number"
        }
      },
      "required": [
        "equations",
        "data"
      ],
      "type": "object"
    }
  },
  {
    "description": "Results from SUR estimation.",
    "name": "SURResult",
    "parameters": {
      "properties": {
        "breusch_pagan": {
          "description": "breusch_pagan parameter (Optional[Dict[str, Any]]).",
          "type": "object"
        },
        "equations": {
          "description": "equations parameter (Dict[str, Dict[str, Any]]).",
          "type": "object"
        },
        "method": {
          "description": "Estimator or algorithm variant to use.",
          "type": "string"
        },
        "n_equations": {
          "description": "Number of equations.",
          "type": "integer"
        },
        "n_obs": {
          "description": "Number of obs.",
          "type": "integer"
        },
        "params_all": {
          "description": "params_all parameter (ndarray).",
          "type": "string"
        },
        "se_all": {
          "description": "se_all parameter (ndarray).",
          "type": "string"
        },
        "sigma": {
          "description": "sigma parameter (DataFrame).",
          "type": "string"
        }
      },
      "required": [
        "equations",
        "sigma",
        "params_all",
        "se_all",
        "n_obs",
        "n_equations",
        "method",
        "breusch_pagan"
      ],
      "type": "object"
    }
  },
  {
    "description": "Three-Stage Least Squares (3SLS). Validation: certified parity evidence.",
    "name": "three_sls",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "equations": {
          "description": "equations parameter (Dict[str, Tuple[str, List[str], List[str]]]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "instruments": {
          "description": "Full set of instruments (all exogenous variables in the system).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "maxiter": {
          "default": 100,
          "description": "maxiter parameter (int).",
          "type": "integer"
        }
      },
      "required": [
        "equations",
        "data"
      ],
      "type": "object"
    }
  },
  {
    "description": "Panel logit model. Validation: certified parity evidence.",
    "name": "panel_logit",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals.",
          "type": "number"
        },
        "cluster": {
          "description": "Column for cluster-robust SEs.",
          "type": "string"
        },
        "data": {
          "description": "Panel data in long format.",
          "type": "string"
        },
        "id": {
          "default": "id",
          "description": "Unit and time identifier columns.",
          "type": "string"
        },
        "maxiter": {
          "default": 200,
          "description": "Maximum optimizer iterations.",
          "type": "integer"
        },
        "method": {
          "default": "fe",
          "description": "'fe' (conditional FE logit), 're' (random effects), 'cre' (Mundlak).",
          "type": "string"
        },
        "n_quadrature": {
          "default": 12,
          "description": "Gauss-Hermite quadrature points (RE/CRE only).",
          "type": "integer"
        },
        "robust": {
          "default": "nonrobust",
          "description": "'nonrobust' or 'robust'.",
          "type": "string"
        },
        "time": {
          "default": "time",
          "description": "Unit and time identifier columns.",
          "type": "string"
        },
        "tol": {
          "default": 1e-08,
          "description": "Gradient tolerance.",
          "type": "number"
        },
        "x": {
          "description": "Regressors.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Binary dependent variable (0/1).",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "x"
      ],
      "type": "object"
    }
  },
  {
    "description": "Panel probit model. Validation: certified parity evidence.",
    "name": "panel_probit",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals.",
          "type": "number"
        },
        "cluster": {
          "description": "Column for cluster-robust SEs.",
          "type": "string"
        },
        "data": {
          "description": "Panel data in long format.",
          "type": "string"
        },
        "id": {
          "default": "id",
          "description": "Unit and time identifier columns.",
          "type": "string"
        },
        "maxiter": {
          "default": 200,
          "description": "Maximum optimizer iterations.",
          "type": "integer"
        },
        "method": {
          "default": "re",
          "description": "'re' (random effects) or 'cre' (Mundlak). FE probit not supported (incidental parameters problem).",
          "type": "string"
        },
        "n_quadrature": {
          "default": 12,
          "description": "Gauss-Hermite quadrature points.",
          "type": "integer"
        },
        "robust": {
          "default": "nonrobust",
          "description": "'nonrobust' or 'robust'.",
          "type": "string"
        },
        "time": {
          "default": "time",
          "description": "Unit and time identifier columns.",
          "type": "string"
        },
        "tol": {
          "default": 1e-08,
          "description": "Gradient tolerance.",
          "type": "number"
        },
        "x": {
          "description": "Regressors.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Binary dependent variable (0/1).",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "x"
      ],
      "type": "object"
    }
  },
  {
    "description": "Panel FGLS (Feasible Generalized Least Squares). Validation: certified parity evidence.",
    "name": "panel_fgls",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "corr": {
          "default": "independent",
          "description": "Within-panel correlation: 'independent', 'ar1' (panel-specific AR(1)), 'psar1' (common AR(1)).",
          "type": "string"
        },
        "data": {
          "description": "Panel data.",
          "type": "string"
        },
        "id": {
          "default": "id",
          "description": "Panel identifier.",
          "type": "string"
        },
        "igls": {
          "default": false,
          "description": "Iterate the variance estimates to convergence (Stata's ``igls``). The default is the two-step estimator Stata's ``xtgls`` reports without that option. .. versionchanged:: 1.27.0 This function iterated unconditionally, so its default was Stata's ``igls`` while the docstring claimed equivalence to the plain command. On a balanced N=60, T=12 panel under ``panels(hetero)`` the two differ by 2.8% on the slope. Pass ``igls=True`` for the previous behaviour.",
          "type": "boolean"
        },
        "maxiter": {
          "default": 100,
          "description": "maxiter parameter (int).",
          "type": "integer"
        },
        "panels": {
          "default": "heteroskedastic",
          "description": "Error structure across panels: 'homoskedastic', 'heteroskedastic', 'correlated' (cross-sectional).",
          "type": "string"
        },
        "time": {
          "default": "time",
          "description": "Time identifier.",
          "type": "string"
        },
        "tol": {
          "default": 1e-06,
          "description": "Numerical convergence tolerance.",
          "type": "number"
        },
        "x": {
          "description": "Regressors.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Dependent variable.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "x"
      ],
      "type": "object"
    }
  },
  {
    "description": "Fit a linear mixed-effects model. Validation: certified parity evidence.",
    "name": "mixed",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Optimiser controls and inference significance level. The defaults use a tight likelihood tolerance so REML variance components and ICC agree with R ``lme4`` / Stata ``mixed`` on parity fixtures.",
          "type": "number"
        },
        "cov_type": {
          "default": "unstructured",
          "description": "Parameterisation of the random-effect covariance matrix *G*: ``'unstructured'`` (default), ``'diagonal'``, or ``'identity'``.",
          "type": "string"
        },
        "data": {
          "description": "Long-format panel / grouped data.",
          "type": "string"
        },
        "group": {
          "description": "Grouping variable. Pass a list like ``[\"school\", \"class\"]`` to estimate a three-level nested model -- the innermost level is used as the cluster for the random slopes/intercept; the outer levels enter as additional random-intercept blocks.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "maxiter": {
          "default": 1000,
          "description": "Optimiser controls and inference significance level. The defaults use a tight likelihood tolerance so REML variance components and ICC agree with R ``lme4`` / Stata ``mixed`` on parity fixtures.",
          "type": "integer"
        },
        "method": {
          "default": "reml",
          "description": "``'reml'`` (default) or ``'ml'``.",
          "type": "string"
        },
        "tol": {
          "default": 1e-09,
          "description": "Optimiser controls and inference significance level. The defaults use a tight likelihood tolerance so REML variance components and ICC agree with R ``lme4`` / Stata ``mixed`` on parity fixtures.",
          "type": "number"
        },
        "x_fixed": {
          "description": "Fixed-effect regressors (intercept is added automatically).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "x_random": {
          "description": "Random-slope variables. ``None`` => random intercept only.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Dependent variable column.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "x_fixed",
        "group"
      ],
      "type": "object"
    }
  },
  {
    "description": "Container for ``sp.mixed()`` estimation results.",
    "name": "MixedResult",
    "parameters": {
      "properties": {
        "_G": {
          "default": null,
          "description": "_G parameter (np.ndarray).",
          "type": "string"
        },
        "_alpha": {
          "default": 0.05,
          "description": "_alpha parameter (float).",
          "type": "number"
        },
        "_blocks": {
          "default": null,
          "description": "_blocks parameter (List[_GroupBlock]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "_converged": {
          "default": true,
          "description": "_converged parameter (bool).",
          "type": "boolean"
        },
        "_cov_fixed": {
          "default": null,
          "description": "_cov_fixed parameter (np.ndarray).",
          "type": "string"
        },
        "_cov_type": {
          "default": "unstructured",
          "description": "_cov_type parameter (str).",
          "type": "string"
        },
        "_fixed_names": {
          "default": null,
          "description": "_fixed_names parameter (List[str]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "_group_cols": {
          "default": null,
          "description": "_group_cols parameter (List[str]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "_lr_test": {
          "description": "_lr_test parameter (Optional[Dict[str, float]]).",
          "type": "object"
        },
        "_method": {
          "default": "reml",
          "description": "_method parameter (str).",
          "type": "string"
        },
        "_n_cov_params": {
          "default": 0,
          "description": "_n_cov_params parameter (int).",
          "type": "integer"
        },
        "_nakagawa": {
          "description": "_nakagawa parameter (Optional[Dict[str, float]]).",
          "type": "object"
        },
        "_random_names": {
          "default": null,
          "description": "_random_names parameter (List[str]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "_ranef_se": {
          "description": "_ranef_se parameter (Optional[pd.DataFrame]).",
          "type": "string"
        },
        "_se_fixed": {
          "description": "_se_fixed parameter (pd.Series).",
          "type": "string"
        },
        "_sigma2": {
          "default": NaN,
          "description": "_sigma2 parameter (float).",
          "type": "number"
        },
        "_x_fixed": {
          "default": null,
          "description": "_x_fixed parameter (List[str]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "_x_random": {
          "default": null,
          "description": "_x_random parameter (List[str]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "_y_name": {
          "default": "",
          "description": "_y_name parameter (str).",
          "type": "string"
        },
        "blups": {
          "description": "blups parameter (Dict[Any, np.ndarray]).",
          "type": "object"
        },
        "fixed_effects": {
          "description": "fixed_effects parameter (pd.Series).",
          "type": "string"
        },
        "icc": {
          "description": "icc parameter (float).",
          "type": "number"
        },
        "log_likelihood": {
          "description": "log_likelihood parameter (float).",
          "type": "number"
        },
        "n_groups": {
          "description": "Number of groups.",
          "type": "integer"
        },
        "n_obs": {
          "description": "Number of obs.",
          "type": "integer"
        },
        "random_effects": {
          "description": "random_effects parameter (pd.DataFrame).",
          "type": "string"
        },
        "variance_components": {
          "description": "variance_components parameter (Dict[str, float]).",
          "type": "object"
        }
      },
      "required": [
        "fixed_effects",
        "random_effects",
        "variance_components",
        "blups",
        "n_obs",
        "n_groups",
        "icc",
        "log_likelihood"
      ],
      "type": "object"
    }
  },
  {
    "description": "Fit a generalised linear mixed model. Validation: certified parity evidence.",
    "name": "meglm",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Optimisation controls / CI width. ``maxiter`` / ``tol`` drive the L-BFGS-B stage; for AGHQ (``nAGQ > 1``) the default budget is internally tightened to ``maxiter=5000`` and ``tol=1e-12`` (explicit user-supplied controls are respected). Either way the optimum is then finished with full Newton steps on the numerical gradient and Hessian, so the reported estimates sit at the optimum to ~1e-9 rather than at L-BFGS-B's relative-function-change stopping point (which on these likelihoods is ~1e-4 away).",
          "type": "number"
        },
        "cov_type": {
          "default": "unstructured",
          "description": "Random-effect covariance: ``'unstructured'`` (default), ``'diagonal'``, ``'identity'``.",
          "type": "string"
        },
        "curvature": {
          "default": "observed",
          "description": "Curvature of the log integrand used by the Laplace approximation and to scale the AGHQ nodes. ``'observed'`` (default) is the second derivative of the conditional log-likelihood at the mode -- the Laplace approximation proper, and what Stata ``me*`` (``intmethod(laplace)`` / ``mcaghermite``), ``glmmTMB`` and ``ordinal::clmm`` compute. ``'expected'`` substitutes the Fisher information (the PIRLS weights of ``lme4::glmer`` / ``glmer.nb``). The two coincide for canonical links (Gaussian, binomial-logit, Poisson-log) and differ for gamma-log and NB-2-log, where they are two different approximations of the same integral.",
          "type": "string"
        },
        "data": {
          "description": "Long-format dataframe.",
          "type": "string"
        },
        "family": {
          "default": "gaussian",
          "description": "``'gaussian'``, ``'binomial'``, ``'poisson'``, ``'gamma'``, or ``'nbinomial'`` (alias ``'negbin'``).",
          "type": "string"
        },
        "group": {
          "description": "Grouping variable for random effects.",
          "type": "string"
        },
        "maxiter": {
          "default": 300,
          "description": "Optimisation controls / CI width. ``maxiter`` / ``tol`` drive the L-BFGS-B stage; for AGHQ (``nAGQ > 1``) the default budget is internally tightened to ``maxiter=5000`` and ``tol=1e-12`` (explicit user-supplied controls are respected). Either way the optimum is then finished with full Newton steps on the numerical gradient and Hessian, so the reported estimates sit at the optimum to ~1e-9 rather than at L-BFGS-B's relative-function-change stopping point (which on these likelihoods is ~1e-4 away).",
          "type": "integer"
        },
        "nAGQ": {
          "default": 1,
          "description": "Number of adaptive Gauss-Hermite quadrature points per scalar random effect. ``1`` (default) == Laplace approximation. Use ``nAGQ=7`` to match Stata ``meglm intpoints(7)``; values ``> 1`` require a single scalar random effect (no random slopes).",
          "type": "integer"
        },
        "offset": {
          "description": "Column of fixed offsets added to the linear predictor (e.g. ``log(exposure)`` for Poisson rate models).",
          "type": "string"
        },
        "tol": {
          "default": 1e-08,
          "description": "Optimisation controls / CI width. ``maxiter`` / ``tol`` drive the L-BFGS-B stage; for AGHQ (``nAGQ > 1``) the default budget is internally tightened to ``maxiter=5000`` and ``tol=1e-12`` (explicit user-supplied controls are respected). Either way the optimum is then finished with full Newton steps on the numerical gradient and Hessian, so the reported estimates sit at the optimum to ~1e-9 rather than at L-BFGS-B's relative-function-change stopping point (which on these likelihoods is ~1e-4 away).",
          "type": "number"
        },
        "trials": {
          "description": "Column of trial counts for binomial responses. Defaults to 1 (Bernoulli).",
          "type": "string"
        },
        "x_fixed": {
          "description": "Fixed-effect regressors (intercept added automatically).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "x_random": {
          "description": "Random-slope variables; defaults to random intercept only.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Outcome column. For binomial models this is the number of successes; pair it with ``trials=`` to model proportions.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "x_fixed",
        "group"
      ],
      "type": "object"
    }
  },
  {
    "description": "Random-effects logistic regression (Stata ``melogit``). Validation: certified parity evidence.",
    "name": "melogit",
    "parameters": {
      "properties": {
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "group": {
          "description": "Group or cohort identifier.",
          "type": "string"
        },
        "nAGQ": {
          "default": 1,
          "description": "nAGQ parameter (int).",
          "type": "integer"
        },
        "trials": {
          "description": "trials parameter (Optional[str]).",
          "type": "string"
        },
        "x_fixed": {
          "description": "x_fixed parameter (Sequence[str]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "x_random": {
          "description": "x_random parameter (Optional[Sequence[str]]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "x_fixed",
        "group"
      ],
      "type": "object"
    }
  },
  {
    "description": "Random-effects Poisson regression (Stata ``mepoisson``). Validation: certified parity evidence.",
    "name": "mepoisson",
    "parameters": {
      "properties": {
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "group": {
          "description": "Group or cohort identifier.",
          "type": "string"
        },
        "nAGQ": {
          "default": 1,
          "description": "nAGQ parameter (int).",
          "type": "integer"
        },
        "offset": {
          "description": "Offset term or offset column.",
          "type": "string"
        },
        "x_fixed": {
          "description": "x_fixed parameter (Sequence[str]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "x_random": {
          "description": "x_random parameter (Optional[Sequence[str]]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "x_fixed",
        "group"
      ],
      "type": "object"
    }
  },
  {
    "description": "Random-effects negative-binomial regression (Stata ``menbreg``). Validation: certified parity evidence.",
    "name": "menbreg",
    "parameters": {
      "properties": {
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "group": {
          "description": "Group or cohort identifier.",
          "type": "string"
        },
        "nAGQ": {
          "default": 1,
          "description": "nAGQ parameter (int).",
          "type": "integer"
        },
        "offset": {
          "description": "Offset term or offset column.",
          "type": "string"
        },
        "x_fixed": {
          "description": "x_fixed parameter (Sequence[str]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "x_random": {
          "description": "x_random parameter (Optional[Sequence[str]]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "x_fixed",
        "group"
      ],
      "type": "object"
    }
  },
  {
    "description": "Random-effects Gamma GLMM with log link (Stata ``meglm`` ``family(gamma)``). Validation: certified parity evidence.",
    "name": "megamma",
    "parameters": {
      "properties": {
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "group": {
          "description": "Group or cohort identifier.",
          "type": "string"
        },
        "nAGQ": {
          "default": 1,
          "description": "nAGQ parameter (int).",
          "type": "integer"
        },
        "offset": {
          "description": "Offset term or offset column.",
          "type": "string"
        },
        "x_fixed": {
          "description": "x_fixed parameter (Sequence[str]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "x_random": {
          "description": "x_random parameter (Optional[Sequence[str]]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "x_fixed",
        "group"
      ],
      "type": "object"
    }
  },
  {
    "description": "Random-effects ordinal logit (Stata ``meologit``, R ``ordinal::clmm``). Validation: certified parity evidence.",
    "name": "meologit",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "cov_type": {
          "default": "unstructured",
          "description": "Covariance estimator type.",
          "type": "string"
        },
        "curvature": {
          "default": "observed",
          "description": "curvature parameter (str).",
          "type": "string"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "group": {
          "description": "Group or cohort identifier.",
          "type": "string"
        },
        "maxiter": {
          "default": 300,
          "description": "maxiter parameter (int).",
          "type": "integer"
        },
        "nAGQ": {
          "default": 1,
          "description": "nAGQ parameter (int).",
          "type": "integer"
        },
        "offset": {
          "description": "Offset term or offset column.",
          "type": "string"
        },
        "tol": {
          "default": 1e-06,
          "description": "Numerical convergence tolerance.",
          "type": "number"
        },
        "x_fixed": {
          "description": "x_fixed parameter (Sequence[str]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "x_random": {
          "description": "x_random parameter (Optional[Sequence[str]]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "x_fixed",
        "group"
      ],
      "type": "object"
    }
  },
  {
    "description": "Container for GLMM fits (``meglm``, ``melogit``, ``mepoisson``,",
    "name": "MEGLMResult",
    "parameters": {
      "properties": {
        "_G": {
          "description": "_G parameter (Optional[np.ndarray]).",
          "type": "string"
        },
        "_alpha": {
          "default": 0.05,
          "description": "_alpha parameter (float).",
          "type": "number"
        },
        "_converged": {
          "default": true,
          "description": "_converged parameter (bool).",
          "type": "boolean"
        },
        "_cov_fixed": {
          "description": "_cov_fixed parameter (Optional[np.ndarray]).",
          "type": "string"
        },
        "_cov_fixed_conditional": {
          "description": "_cov_fixed_conditional parameter (Optional[np.ndarray]).",
          "type": "string"
        },
        "_cov_full": {
          "description": "_cov_full parameter (Optional[np.ndarray]).",
          "type": "string"
        },
        "_cov_type": {
          "default": "unstructured",
          "description": "_cov_type parameter (str).",
          "type": "string"
        },
        "_dispersion": {
          "description": "_dispersion parameter (Optional[float]).",
          "type": "number"
        },
        "_fixed_names": {
          "default": null,
          "description": "_fixed_names parameter (List[str]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "_group_col": {
          "default": "",
          "description": "Column name for group.",
          "type": "string"
        },
        "_method": {
          "default": "laplace",
          "description": "_method parameter (str).",
          "type": "string"
        },
        "_n_cov_params": {
          "default": 0,
          "description": "_n_cov_params parameter (int).",
          "type": "integer"
        },
        "_offset_name": {
          "description": "_offset_name parameter (Optional[str]).",
          "type": "string"
        },
        "_random_names": {
          "default": null,
          "description": "_random_names parameter (List[str]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "_se_fixed": {
          "description": "_se_fixed parameter (pd.Series).",
          "type": "string"
        },
        "_vce_method": {
          "default": "oim",
          "description": "_vce_method parameter (str).",
          "type": "string"
        },
        "_x_fixed": {
          "default": null,
          "description": "_x_fixed parameter (List[str]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "_x_random": {
          "default": null,
          "description": "_x_random parameter (List[str]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "_y_name": {
          "default": "",
          "description": "_y_name parameter (str).",
          "type": "string"
        },
        "blups": {
          "description": "blups parameter (Dict[Any, np.ndarray]).",
          "type": "object"
        },
        "family": {
          "default": "gaussian",
          "description": "family parameter (str).",
          "type": "string"
        },
        "fixed_effects": {
          "description": "fixed_effects parameter (pd.Series).",
          "type": "string"
        },
        "link": {
          "default": "identity",
          "description": "link parameter (str).",
          "type": "string"
        },
        "log_likelihood": {
          "description": "log_likelihood parameter (float).",
          "type": "number"
        },
        "n_groups": {
          "description": "Number of groups.",
          "type": "integer"
        },
        "n_obs": {
          "description": "Number of obs.",
          "type": "integer"
        },
        "random_effects": {
          "description": "random_effects parameter (pd.DataFrame).",
          "type": "string"
        },
        "thresholds": {
          "description": "thresholds parameter (Optional[pd.Series]).",
          "type": "string"
        },
        "thresholds_se": {
          "description": "thresholds_se parameter (Optional[pd.Series]).",
          "type": "string"
        },
        "variance_components": {
          "description": "variance_components parameter (Dict[str, float]).",
          "type": "object"
        }
      },
      "required": [
        "fixed_effects",
        "random_effects",
        "variance_components",
        "blups",
        "n_obs",
        "n_groups",
        "log_likelihood"
      ],
      "type": "object"
    }
  },
  {
    "description": "Intra-class correlation for a fitted random-intercept model. Validation: certified parity evidence.",
    "name": "icc",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for the confidence interval. Default 0.05.",
          "type": "number"
        },
        "component": {
          "default": "_cons",
          "description": "Random-intercept name (``\"_cons\"``); for a three-level fit the grouping column of the level whose ICC is wanted.",
          "type": "string"
        },
        "n_boot": {
          "default": 0,
          "description": "Parametric-bootstrap intervals are not implemented; must be 0.",
          "type": "integer"
        },
        "result": {
          "description": "A ``MixedResult`` from :func:`statspai.mixed` or an ``MEGLMResult`` from ``melogit`` / ``meologit`` / ``meglm(family='gaussian')``, with a random intercept only.",
          "type": "string"
        },
        "seed": {
          "description": "Unused (reserved for the bootstrap).",
          "type": "integer"
        }
      },
      "required": [
        "result"
      ],
      "type": "object"
    }
  },
  {
    "description": "Likelihood-ratio test comparing a *restricted* and a *full* model. Validation: certified parity evidence.",
    "name": "lrtest",
    "parameters": {
      "properties": {
        "boundary": {
          "description": "Whether to apply the chi2 boundary correction. When ``None`` (default) we infer it from whether the restriction touches a variance component -- the only parameters that live on the boundary of their support. ``boundary=False`` reproduces Stata's ``lrtest`` and R's ``anova()`` (naive chi2(df) tail; Stata prints a note that the test is conservative when the null is on the boundary). The degrees of freedom are the difference in the number of estimated parameters (Stata ``e(k)``), which counts every variance and covariance parameter.",
          "type": "boolean"
        },
        "full": {
          "description": "Two fitted mixed models. ``full`` should strictly nest ``restricted`` -- i.e. the parameter space of the restricted model is a subset of the full model's.",
          "type": "string"
        },
        "restricted": {
          "description": "Two fitted mixed models. ``full`` should strictly nest ``restricted`` -- i.e. the parameter space of the restricted model is a subset of the full model's.",
          "type": "string"
        }
      },
      "required": [
        "restricted",
        "full"
      ],
      "type": "object"
    }
  },
  {
    "description": "Estimate a cross-sectional stochastic frontier model by ML. Validation: certified parity evidence.",
    "name": "frontier",
    "parameters": {
      "properties": {
        "B": {
          "default": 400,
          "description": "B parameter (int).",
          "type": "integer"
        },
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "cluster": {
          "description": "Cluster variable for cluster-robust SE (Liang-Zeger 1986). When specified, implies ``vce='robust'`` aggregated over clusters.",
          "type": "string"
        },
        "cost": {
          "default": false,
          "description": "If True, estimate a cost frontier (composed error ``v + u``).",
          "type": "boolean"
        },
        "data": {
          "description": "Cross-sectional data. Rows with missing values in any referenced column are dropped.",
          "type": "string"
        },
        "dist": {
          "default": "half-normal",
          "description": "Distribution of the inefficiency term ``u``.",
          "enum": [
            "half-normal",
            "exponential",
            "truncated-normal"
          ],
          "type": "string"
        },
        "emean": {
          "description": "Columns parameterizing ``mu_i = delta' [1, z_i]`` for the truncated normal (Battese-Coelli 1995; Kumbhakar-Ghosh-McGuckin 1991). Requires ``dist='truncated-normal'``.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "maxiter": {
          "default": 2000,
          "description": "maxiter parameter (int).",
          "type": "integer"
        },
        "seed": {
          "description": "Random seed for reproducible stochastic steps.",
          "type": "integer"
        },
        "start": {
          "description": "User-supplied starting values for the full parameter vector.",
          "type": "string"
        },
        "te_method": {
          "default": "bc",
          "description": "Default technical-efficiency formula accessed via ``.efficiency()``.",
          "enum": [
            "bc",
            "jlms"
          ],
          "type": "string"
        },
        "tol": {
          "default": 1e-10,
          "description": "Numerical convergence tolerance.",
          "type": "number"
        },
        "usigma": {
          "description": "Columns parameterizing ``ln sigma_u_i = gamma_u' [1, w_i]`` (Caudill-Ford-Gropper 1995).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "vce": {
          "default": "oim",
          "description": "Variance-covariance estimator: ``'oim'`` -- observed information matrix (inverse numerical Hessian). ``'opg'`` -- outer product of gradients (Berndt-Hall-Hall-Hausman). ``'robust'`` -- sandwich ``H^{-1} (S' S) H^{-1}`` (White 1982).",
          "enum": [
            "oim",
            "opg",
            "robust"
          ],
          "type": "string"
        },
        "vsigma": {
          "description": "Columns parameterizing ``ln sigma_v_i = gamma_v' [1, r_i]`` (Wang 2002).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "x": {
          "description": "Frontier regressors (a constant is added automatically).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Dependent variable (output for production, cost for cost frontier).",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "x"
      ],
      "type": "object"
    }
  },
  {
    "description": "Panel stochastic frontier estimator. Validation: certified parity evidence.",
    "name": "xtfrontier",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "bias_correct": {
          "default": false,
          "description": "TFE-only. If True, applies Dhaene-Jochmans (2015) split-panel jackknife to reduce the O(1/T) incidental-parameters bias on ``beta`` and ``sigma_u``.",
          "type": "boolean"
        },
        "cluster": {
          "description": "Column name for cluster-robust SEs. Defaults to ``id`` whenever ``vce != 'oim'`` (the natural grouping for panels).",
          "type": "string"
        },
        "cost": {
          "default": false,
          "description": "cost parameter (bool).",
          "type": "boolean"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "dist": {
          "default": "half-normal",
          "description": "For ``ti``, ``tvd``, ``tfe``, ``tre``. BC95 always uses TN.",
          "enum": [
            "half-normal",
            "truncated-normal"
          ],
          "type": "string"
        },
        "emean": {
          "description": "Required for ``model='bc95'``; inefficiency determinants ``z_it``.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "id": {
          "description": "Panel unit identifier.",
          "type": "string"
        },
        "maxiter": {
          "default": 500,
          "description": "maxiter parameter (int).",
          "type": "integer"
        },
        "model": {
          "default": "ti",
          "description": "``'ti'`` Pitt-Lee (1981) time-invariant inefficiency. ``'tvd'`` Battese-Coelli (1992) time-varying decay. ``'bc95'`` Battese-Coelli (1995) inefficiency effects model (requires ``emean``). ``'tfe'`` Greene (2005) True Fixed Effects: unit dummies + cross-sectional composed error. Recommended for T >= ~10. ``'tre'`` Greene (2005) True Random Effects: ``alpha_i ~ N(0, sigma_alpha^2)`` integrated out by Gauss-Hermite quadrature.",
          "enum": [
            "ti",
            "tvd",
            "bc95",
            "tfe",
            "tre"
          ],
          "type": "string"
        },
        "n_quad": {
          "default": 24,
          "description": "TRE-only. Number of Gauss-Hermite nodes used to integrate out ``alpha_i``. Increase to 48 or 64 when ``sigma_alpha`` is large relative to ``sigma_v`` (large between-firm heterogeneity) so that the quadrature tails are not truncated. A warning is emitted when the fitted ``sigma_alpha`` suggests insufficient tail coverage at the chosen ``n_quad``.",
          "type": "integer"
        },
        "time": {
          "description": "Time variable (required for ``model='tvd'`` and recommended for all panel models; falls back to observation order otherwise).",
          "type": "string"
        },
        "tol": {
          "default": 1e-08,
          "description": "Numerical convergence tolerance.",
          "type": "number"
        },
        "vce": {
          "default": "oim",
          "description": "Variance-covariance estimator. ``'oim'`` uses the inverse observed information. ``'opg'`` is the outer product of gradients (BHHH). ``'robust'`` is the sandwich ``H^-1 (S'S) H^-1``. Passing ``cluster=`` implies cluster-robust SEs (Liang-Zeger 1986). Note: ``vce='bootstrap'`` is only available on the cross-sectional :func:`frontier`; for panel models use ``'robust'`` with ``cluster=id`` instead.",
          "enum": [
            "oim",
            "opg",
            "robust",
            "cluster"
          ],
          "type": "string"
        },
        "x": {
          "description": "Primary running variable, regressor, or feature input for this estimator.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "x",
        "id"
      ],
      "type": "object"
    }
  },
  {
    "description": "Result object returned by :func:`frontier` and :func:`xtfrontier`.",
    "name": "FrontierResult",
    "parameters": {
      "properties": {
        "data_info": {
          "description": "data_info parameter (Optional[Dict[str, Any]]).",
          "type": "object"
        },
        "diagnostics": {
          "description": "diagnostics parameter (Optional[Dict[str, Any]]).",
          "type": "object"
        },
        "model_info": {
          "description": "model_info parameter (Dict[str, Any]).",
          "type": "object"
        },
        "params": {
          "description": "params parameter (Series).",
          "type": "string"
        },
        "std_errors": {
          "description": "std_errors parameter (Series).",
          "type": "string"
        }
      },
      "required": [
        "params",
        "std_errors",
        "model_info"
      ],
      "type": "object"
    }
  },
  {
    "description": "Estimate a metafrontier across ``K`` groups. Validation: validated evidence tier (known-truth, reference, external-parity, or Monte Carlo artifact).",
    "name": "metafrontier",
    "parameters": {
      "properties": {
        "cost": {
          "default": false,
          "description": "cost parameter (bool).",
          "type": "boolean"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "dist": {
          "default": "half-normal",
          "description": "dist parameter (str).",
          "type": "string"
        },
        "envelope": {
          "default": "all",
          "description": "Which envelopment constraints the LP imposes. * ``'all'`` -- ``x_i' beta_meta >= x_i' beta^k`` at every pooled observation ``i`` for every group ``k``: the metafrontier lies on or above each group frontier wherever any firm operates. * ``'own'`` -- ``x_i' beta_meta >= x_i' beta^{k(i)}`` only against observation ``i``'s own group frontier. This is the constraint set of R ``metafrontier::metafrontier(objective = \"lp\")``, which its documentation attributes to O'Donnell, Rao & Battese (2008). The two coincide when the group frontiers do not cross inside the data; when they cross, ``'all'`` gives a (weakly) higher metafrontier and hence (weakly) lower technology-gap ratios.",
          "enum": [
            "all",
            "own"
          ],
          "type": "string"
        },
        "group": {
          "description": "Group or cohort identifier.",
          "type": "string"
        },
        "lp_tol": {
          "default": 1e-07,
          "description": "Primal / dual feasibility tolerance forwarded to HiGHS. With thousands of constraints and floating-point group betas, HiGHS can declare numerical infeasibility on mathematically feasible problems; loosening ``lp_tol`` (e.g., ``1e-6``) typically recovers these cases.",
          "type": "number"
        },
        "te_method": {
          "default": "bc",
          "description": "te_method parameter (str).",
          "enum": [
            "bc",
            "jlms"
          ],
          "type": "string"
        },
        "x": {
          "description": "Primary running variable, regressor, or feature input for this estimator.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "x",
        "group"
      ],
      "type": "object"
    }
  },
  {
    "description": "Container for a metafrontier fit.",
    "name": "MetafrontierResult",
    "parameters": {
      "properties": {
        "beta_groups": {
          "description": "beta_groups parameter (Dict[Any, pd.Series]).",
          "type": "object"
        },
        "beta_meta": {
          "description": "beta_meta parameter (pd.Series).",
          "type": "string"
        },
        "data_info": {
          "description": "data_info parameter (Dict[str, Any]).",
          "type": "object"
        },
        "group_frontiers": {
          "description": "group_frontiers parameter (Dict[Any, FrontierResult]).",
          "type": "object"
        },
        "lp_status": {
          "description": "lp_status parameter (str).",
          "type": "string"
        },
        "te_group": {
          "description": "te_group parameter (pd.Series).",
          "type": "string"
        },
        "te_meta": {
          "description": "te_meta parameter (pd.Series).",
          "type": "string"
        },
        "tgr": {
          "description": "tgr parameter (pd.Series).",
          "type": "string"
        }
      },
      "required": [
        "beta_meta",
        "beta_groups",
        "group_frontiers",
        "tgr",
        "te_meta",
        "te_group",
        "data_info",
        "lp_status"
      ],
      "type": "object"
    }
  },
  {
    "description": "Compute the Malmquist productivity index via period-by-period SFA. Validation: certified parity evidence.",
    "name": "malmquist",
    "parameters": {
      "properties": {
        "cost": {
          "default": false,
          "description": "cost parameter (bool).",
          "type": "boolean"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "dist": {
          "default": "half-normal",
          "description": "dist parameter (str).",
          "type": "string"
        },
        "efficiency": {
          "default": "bc",
          "description": "How the efficiency-change component is measured. ``'bc'`` / ``'jlms'`` use the ratio of each period's predicted technical efficiencies (Battese-Coelli ``E[exp(-u)|eps]`` / JLMS ``exp(-E[u|eps])``), which filters out the noise ``v``. ``'residual'`` uses the composed residual ``exp(y - x'beta_s)`` as the distance, so EC and M include the change in noise (the deterministic-frontier convention; the pre-1.29.0 default). TC is the same under all three.",
          "enum": [
            "bc",
            "jlms",
            "residual"
          ],
          "type": "string"
        },
        "id": {
          "description": "Unit, subject, or panel identifier column.",
          "type": "string"
        },
        "overflow_threshold": {
          "default": 1000000.0,
          "description": "Any firm-level ``m_index`` / ``ec`` / ``tc`` whose absolute value exceeds this is replaced with NaN and a UserWarning is emitted. Protects summary statistics from contamination by degenerate per-period frontier fits.",
          "type": "number"
        },
        "time": {
          "description": "Time period column.",
          "type": "string"
        },
        "x": {
          "description": "Primary running variable, regressor, or feature input for this estimator.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "x",
        "id",
        "time"
      ],
      "type": "object"
    }
  },
  {
    "description": "Container for Malmquist productivity index decomposition.",
    "name": "MalmquistResult",
    "parameters": {
      "properties": {
        "data_info": {
          "description": "data_info parameter (Dict[str, Any]).",
          "type": "object"
        },
        "index_table": {
          "description": "index_table parameter (pd.DataFrame).",
          "type": "string"
        },
        "period_frontiers": {
          "description": "period_frontiers parameter (Dict[Any, FrontierResult]).",
          "type": "object"
        },
        "summary_by_period": {
          "description": "summary_by_period parameter (pd.DataFrame).",
          "type": "string"
        }
      },
      "required": [
        "index_table",
        "period_frontiers",
        "summary_by_period",
        "data_info"
      ],
      "type": "object"
    }
  },
  {
    "description": "Build a translog design matrix from Cobb-Douglas inputs. Validation: validated evidence tier (known-truth, reference, external-parity, or Monte Carlo artifact).",
    "name": "translog_design",
    "parameters": {
      "properties": {
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "include_interactions": {
          "default": true,
          "description": "If True, adds ``x_k * x_l`` for k < l.",
          "type": "boolean"
        },
        "include_squares": {
          "default": true,
          "description": "If True, adds ``0.5 * x_k^2`` terms (translog convention).",
          "type": "boolean"
        },
        "inputs": {
          "description": "Columns containing ``log x_k`` terms (already log-transformed).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "interaction_prefix": {
          "default": "",
          "description": "Optional prefix for the generated columns (e.g., ``\"tl_\"``).",
          "type": "string"
        }
      },
      "required": [
        "data",
        "inputs"
      ],
      "type": "object"
    }
  },
  {
    "description": "Zero-Inefficiency Stochastic Frontier (Kumbhakar-Parmeter-Tsionas 2013). Validation: certified parity evidence.",
    "name": "zisf",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "cluster": {
          "description": "Cluster identifier column for clustered standard errors.",
          "type": "string"
        },
        "cost": {
          "default": false,
          "description": "cost parameter (bool).",
          "type": "boolean"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "dist": {
          "default": "half-normal",
          "description": "Distribution of ``u`` in the inefficient regime. Currently only half-normal is supported (KPT 2013 baseline).",
          "type": "string"
        },
        "maxiter": {
          "default": 500,
          "description": "maxiter parameter (int).",
          "type": "integer"
        },
        "tol": {
          "default": 1e-08,
          "description": "Numerical convergence tolerance.",
          "type": "number"
        },
        "vce": {
          "default": "oim",
          "description": "vce parameter (str).",
          "type": "string"
        },
        "x": {
          "description": "Primary running variable, regressor, or feature input for this estimator.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        },
        "zprob": {
          "description": "Covariates for the mixing probability; a constant is added.",
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "required": [
        "data",
        "y",
        "x"
      ],
      "type": "object"
    }
  },
  {
    "description": "Two-class Latent-Class SFA (Orea-Kumbhakar 2004; Greene 2005). Validation: certified parity evidence.",
    "name": "lcsf",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "cluster": {
          "description": "Cluster identifier column for clustered standard errors.",
          "type": "string"
        },
        "cost": {
          "default": false,
          "description": "cost parameter (bool).",
          "type": "boolean"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "dist": {
          "default": "half-normal",
          "description": "dist parameter (str).",
          "type": "string"
        },
        "maxiter": {
          "default": 500,
          "description": "maxiter parameter (int).",
          "type": "integer"
        },
        "tol": {
          "default": 1e-08,
          "description": "Numerical convergence tolerance.",
          "type": "number"
        },
        "vce": {
          "default": "oim",
          "description": "vce parameter (str).",
          "type": "string"
        },
        "x": {
          "description": "Primary running variable, regressor, or feature input for this estimator.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        },
        "z_class": {
          "description": "Covariates shifting the class-1 logit probability.",
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "required": [
        "data",
        "y",
        "x"
      ],
      "type": "object"
    }
  },
  {
    "description": "Return a small descriptive DataFrame of TE scores (summary stats only).",
    "name": "te_summary",
    "parameters": {
      "properties": {
        "method": {
          "description": "Estimator or algorithm variant to use.",
          "type": "string"
        },
        "result": {
          "description": "result parameter.",
          "type": "string"
        }
      },
      "required": [
        "result"
      ],
      "type": "object"
    }
  },
  {
    "description": "Return efficiency scores sorted descending, with rank column.",
    "name": "te_rank",
    "parameters": {
      "properties": {
        "B": {
          "default": 500,
          "description": "B parameter (int).",
          "type": "integer"
        },
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "method": {
          "description": "Estimator or algorithm variant to use.",
          "type": "string"
        },
        "result": {
          "description": "result parameter.",
          "type": "string"
        },
        "seed": {
          "default": 0,
          "description": "Random seed for reproducible stochastic steps.",
          "type": "integer"
        },
        "with_ci": {
          "default": false,
          "description": "with_ci parameter (bool).",
          "type": "boolean"
        }
      },
      "required": [
        "result"
      ],
      "type": "object"
    }
  },
  {
    "description": "General GMM estimator for arbitrary moment conditions. Validation: certified parity evidence.",
    "name": "gmm",
    "parameters": {
      "properties": {
        "W": {
          "description": "Weighting matrix ``(q, q)`` for the first step. Defaults to the identity. Supplying one and asking for ``se='unadjusted'`` warns unless it happens to be efficient.",
          "type": "string"
        },
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "center": {
          "default": false,
          "description": "Centre the moments before forming ``S``. ``False`` matches Stata, ``True`` matches R's ``gmm``.",
          "type": "boolean"
        },
        "cluster": {
          "description": "Group labels, one per moment row. Required when ``vcov='cluster'``.",
          "type": "string"
        },
        "data": {
          "description": "Passed through to ``moment_fn``.",
          "type": "string"
        },
        "hac_bandwidth": {
          "description": "Bartlett bandwidth for ``vcov='hac'``; the kernel vanishes at ``lag == hac_bandwidth``.",
          "type": "integer"
        },
        "jacobian": {
          "description": "``D(theta, data) -> ndarray`` of shape ``(q, k)``, the derivative of the *average* moment. Supplying it removes finite-difference error from the standard errors and from the affine test.",
          "type": "string"
        },
        "maxiter": {
          "default": 200,
          "description": "maxiter parameter (int).",
          "type": "integer"
        },
        "method": {
          "default": "twostep",
          "description": "Estimator or algorithm variant to use.",
          "enum": [
            "onestep",
            "twostep",
            "iterative",
            "cue"
          ],
          "type": "string"
        },
        "moment_fn": {
          "description": "``g(theta, data) -> ndarray`` of shape ``(n, q)``: the moment contribution of each observation.",
          "type": "string"
        },
        "param_names": {
          "description": "param_names parameter (Optional[List[str]]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "sandwich_weight": {
          "default": "reestimated",
          "description": "Weight inside the robust variance of the two-step / iterated estimators. ``'reestimated'`` uses ``S-1`` re-evaluated at the final estimate, so the sandwich collapses to ``(D'S-1D)-1/n`` (R ``gmm::gmm``). ``'estimation'`` uses the weight the final round was *minimised* with -- computed from the penultimate estimate -- with ``S`` at the final estimate: ``(D'WD)-1 D'WSWD (D'WD)-1 / n``, as Stata's ``gmm`` documents in its Methods and formulas. The two coincide for the iterated estimator at convergence and for the one-step estimator; for two-step they differ at order 1/n.",
          "enum": [
            "reestimated",
            "estimation"
          ],
          "type": "string"
        },
        "se": {
          "default": "robust",
          "description": "``'robust'`` returns the sandwich, valid for any ``W``. ``'unadjusted'`` returns the efficient-GMM variance ``(D'WD)^{-1}/n``, which describes the estimator *only* at the efficient weight; otherwise it warns.",
          "enum": [
            "robust",
            "unadjusted"
          ],
          "type": "string"
        },
        "theta0": {
          "description": "Starting values. Also the expansion point for the closed form when the moments are affine.",
          "type": "string"
        },
        "tol": {
          "default": 1e-08,
          "description": "Numerical convergence tolerance.",
          "type": "number"
        },
        "vcov": {
          "default": "mds",
          "description": "Estimator for the moment covariance ``S``.",
          "enum": [
            "mds",
            "iid",
            "hac",
            "cluster"
          ],
          "type": "string"
        }
      },
      "required": [
        "moment_fn",
        "theta0"
      ],
      "type": "object"
    }
  },
  {
    "description": "Causal Impact visualization (Google-style 3-panel plot).",
    "name": "impactplot",
    "parameters": {
      "properties": {
        "ax": {
          "description": "Only for single-panel types.",
          "type": "string"
        },
        "figsize": {
          "default": [
            12,
            9
          ],
          "description": "figsize parameter (tuple[float, float]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "result": {
          "description": "Result from ``causal_impact()``.",
          "type": "string"
        },
        "title": {
          "description": "title parameter (Optional[str]).",
          "type": "string"
        },
        "type": {
          "default": "all",
          "description": "'all': 3-panel (original + pointwise + cumulative). 'original': actual vs counterfactual. 'pointwise': pointwise causal effect. 'cumulative': cumulative effect.",
          "type": "string"
        }
      },
      "required": [
        "result"
      ],
      "type": "object"
    }
  },
  {
    "description": "Sensitivity analysis for causal mediation (linear models). Validation: certified parity evidence.",
    "name": "mediate_sensitivity",
    "parameters": {
      "properties": {
        "conf_level": {
          "default": 0.95,
          "description": "Level of the normal-approximation band ``ACME +/- z * SE``.",
          "type": "number"
        },
        "covariates": {
          "description": "Pre-treatment covariates entering both models.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "eps": {
          "description": "Stopping rule of the feasible-GLS iteration: stop when the squared change of the coefficient vector is at most ``eps``. The default iterates to the fixed point (machine precision). ``eps=np.sqrt(np.finfo(float).eps)`` is ``medsens``' default and reproduces its output exactly; that rule stops a few iterations early at large |rho| (ACME off by up to ~1e-4 relative).",
          "type": "number"
        },
        "max_iter": {
          "default": 10000,
          "description": "Iteration cap; a warning is raised when it binds.",
          "type": "integer"
        },
        "mediator": {
          "description": "Outcome, treatment (binary or continuous) and mediator columns.",
          "type": "string"
        },
        "n_grid": {
          "default": 41,
          "description": "Number of grid points (``medsens``' default ``rho.by = 0.1`` is ``rho_range=(-0.9, 0.9), n_grid=19``).",
          "type": "integer"
        },
        "rho_range": {
          "default": [
            -0.9,
            0.9
          ],
          "description": "Grid end points; must lie strictly inside (-1, 1).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "treat": {
          "description": "Outcome, treatment (binary or continuous) and mediator columns.",
          "type": "string"
        },
        "y": {
          "description": "Outcome, treatment (binary or continuous) and mediator columns.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "treat",
        "mediator"
      ],
      "type": "object"
    }
  },
  {
    "description": "Unified plot function for all Synthetic Control variants.",
    "name": "synthplot",
    "parameters": {
      "properties": {
        "ax": {
          "description": "Pre-existing axes for single-panel plots.",
          "type": "string"
        },
        "figsize": {
          "description": "Figure size. Auto-selected if None.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "labels": {
          "description": "Labels for ``type='compare'``.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "result": {
          "description": "Output of any ``synth()`` variant. Pass a list for ``type='compare'``.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "title": {
          "description": "Override the auto-generated title.",
          "type": "string"
        },
        "top_n": {
          "default": 15,
          "description": "Number of donors to show in weight plots.",
          "type": "integer"
        },
        "type": {
          "default": "trajectory",
          "description": "Plot type: * ``'trajectory'`` -- treated vs synthetic over time. * ``'gap'`` -- effect (gap) over time. * ``'both'`` -- two-panel: trajectory + gap. * ``'weights'`` -- donor weight bar chart. * ``'placebo'`` -- placebo ATT distribution. * ``'placebo_gap'`` -- placebo gap spaghetti plot (Abadie et al. 2010). * ``'rmspe'`` -- post/pre RMSPE ratio histogram (Abadie et al. 2010). * ``'conformal'`` -- period-level effects + conformal CIs. * ``'staggered'`` -- cohort-level ATT comparison. * ``'factors'`` -- latent factor loadings (gsynth only). * ``'compare'`` -- overlay multiple results.",
          "type": "string"
        }
      },
      "required": [
        "result"
      ],
      "type": "object"
    }
  },
  {
    "description": "Multiple Outcomes Synthetic Control Method (Sun, Ben-Michael & Feller). Validation: certified parity evidence.",
    "name": "multi_outcome_synth",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and joint test.",
          "type": "number"
        },
        "data": {
          "description": "Long-format panel data containing all outcome columns.",
          "type": "string"
        },
        "method": {
          "default": "concatenated",
          "description": "Weight-estimation strategy. * ``'concatenated'`` -- stack all K standardised outcome panels vertically and solve one quadratic programme. * ``'averaged'`` -- standardise each outcome, average across K, then solve SCM on the mean series.",
          "enum": [
            "concatenated",
            "averaged"
          ],
          "type": "string"
        },
        "outcomes": {
          "description": "Column names for the K outcome variables.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "penalization": {
          "default": 0.0,
          "description": "Ridge-type penalty added to the diagonal of the donor cross-product matrix (``penalization * I``). Helps when donors are collinear.",
          "type": "number"
        },
        "placebo": {
          "default": true,
          "description": "Run in-space placebo permutations for inference (each donor is pretended to be treated in turn).",
          "type": "boolean"
        },
        "standardize": {
          "default": true,
          "description": "Standardise each outcome to zero mean / unit variance before stacking or averaging (strongly recommended when outcome scales differ).",
          "type": "boolean"
        },
        "time": {
          "description": "Time period column.",
          "type": "string"
        },
        "treated_unit": {
          "description": "Value identifying the treated unit.",
          "type": "string"
        },
        "treatment_time": {
          "description": "First treatment period (inclusive).",
          "type": "string"
        },
        "unit": {
          "description": "Unit identifier column.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "outcomes",
        "unit",
        "time",
        "treated_unit",
        "treatment_time"
      ],
      "type": "object"
    }
  },
  {
    "description": "Prediction intervals for synthetic control methods (R ``scpi::scpi``). Validation: certified parity evidence. Cost: Prediction intervals come from a simulation step on top of the point fit, so runtime is dominated by the number of simulations rather than n. cores= is accepted for API compatibility with R scpi but the simulation currently runs serially.",
    "name": "scpi",
    "parameters": {
      "properties": {
        "Q": {
          "description": "Constraint bounds; see :func:`scest`.",
          "type": "number"
        },
        "Q2": {
          "description": "Constraint bounds; see :func:`scest`.",
          "type": "number"
        },
        "alpha": {
          "default": 0.05,
          "description": "Default for both ``u_alpha`` and ``e_alpha``. As in R, the combined interval has nominal coverage at least ``1 - (u_alpha + e_alpha)`` (0.90 with the defaults).",
          "type": "number"
        },
        "cores": {
          "default": 1,
          "description": "Accepted for API compatibility; the simulation runs serially.",
          "type": "integer"
        },
        "data": {
          "description": "Long-format panel data.",
          "type": "string"
        },
        "draws": {
          "description": "``(J, sims)`` matrix of standard-normal draws used for the in-sample simulation instead of generating them; passing the matrix R's ``rnorm`` produced reproduces R's simulated bounds.",
          "type": "string"
        },
        "e_alpha": {
          "description": "Levels of the in-sample / out-of-sample bounds (default ``alpha``).",
          "type": "number"
        },
        "e_method": {
          "default": "gaussian",
          "description": "Out-of-sample bound: sub-Gaussian, location-scale or quantile regression. All three are always computed (``model_info``).",
          "enum": [
            "gaussian",
            "ls",
            "qreg"
          ],
          "type": "string"
        },
        "e_order": {
          "default": 1,
          "description": "Order of the out-of-sample moment models (R ``e.order``; ``e.lags`` is fixed at 0).",
          "enum": [
            "0",
            "1"
          ],
          "type": "integer"
        },
        "lasso_lambda": {
          "description": "Deprecated and ignored; see :func:`scest`.",
          "type": "number"
        },
        "outcome": {
          "description": "Column names.",
          "type": "string"
        },
        "pi_type": {
          "default": "both",
          "description": "Which interval is reported in ``period_results`` / ``ci``: ``'both'`` = R ``CI.all.<e_method>`` (in-sample + out-of-sample bounds), ``'in_sample'`` = R ``CI.in.sample``, ``'out_of_sample'`` = synthetic fit + out-of-sample bounds only (not an R table).",
          "enum": [
            "both",
            "in_sample",
            "out_of_sample"
          ],
          "type": "string"
        },
        "rho": {
          "description": "Regularisation of the local geometry (default ``'type-2'``).",
          "enum": [
            "type-1",
            "type-2"
          ],
          "type": "number"
        },
        "rho_max": {
          "default": 0.2,
          "description": "Upper bound on ``rho``.",
          "type": "number"
        },
        "ridge_lambda": {
          "description": "Deprecated and ignored; see :func:`scest`.",
          "type": "number"
        },
        "seed": {
          "description": "Seed of the ``numpy`` generator for the in-sample draws (R uses its own ``rnorm`` stream, so simulated bounds agree with R only up to Monte Carlo error unless ``draws`` is supplied).",
          "type": "integer"
        },
        "sims": {
          "default": 200,
          "description": "Number of in-sample simulation draws (R ``sims``; >= 10).",
          "type": "integer"
        },
        "time": {
          "description": "Column names.",
          "type": "string"
        },
        "treated_unit": {
          "description": "Identifier of the treated unit.",
          "type": "string"
        },
        "treatment_time": {
          "description": "First treatment period.",
          "type": "string"
        },
        "u_alpha": {
          "description": "Levels of the in-sample / out-of-sample bounds (default ``alpha``).",
          "type": "number"
        },
        "u_missp": {
          "default": true,
          "description": "Allow a misspecified conditional mean of the pseudo-residuals (R ``u.missp``).",
          "type": "boolean"
        },
        "u_order": {
          "default": 1,
          "description": "Order of the pseudo-residual mean model (R ``u.order``; ``u.lags`` is fixed at 0).",
          "enum": [
            "0",
            "1"
          ],
          "type": "integer"
        },
        "u_sigma": {
          "default": "HC1",
          "description": "Variance estimator of the pseudo-residuals.",
          "enum": [
            "HC0",
            "HC1",
            "HC2",
            "HC3",
            "HC4"
          ],
          "type": "string"
        },
        "unit": {
          "description": "Column names.",
          "type": "string"
        },
        "w_constr": {
          "default": "simplex",
          "description": "Weight constraint; see :func:`scest`.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "outcome",
        "unit",
        "time",
        "treated_unit",
        "treatment_time"
      ],
      "type": "object"
    }
  },
  {
    "description": "Estimate synthetic control weights (R ``scpi::scest``). Validation: certified parity evidence.",
    "name": "scest",
    "parameters": {
      "properties": {
        "Q": {
          "description": "Norm bound of the constraint (sum of weights for ``simplex``).",
          "type": "number"
        },
        "Q2": {
          "description": "L2 bound for ``'L1-L2'``. Default: R's ridge rule.",
          "type": "number"
        },
        "data": {
          "description": "Long-format panel data.",
          "type": "string"
        },
        "lasso_lambda": {
          "description": "Deprecated and ignored (emit ``DeprecationWarning``). Before 1.28.x these set L1 / L2 *penalties* of a different estimator; R ``scpi`` constrains the norm of ``w`` instead -- use ``Q`` / ``Q2``.",
          "type": "number"
        },
        "outcome": {
          "description": "Outcome variable column name.",
          "type": "string"
        },
        "ridge_lambda": {
          "description": "Deprecated and ignored (emit ``DeprecationWarning``). Before 1.28.x these set L1 / L2 *penalties* of a different estimator; R ``scpi`` constrains the norm of ``w`` instead -- use ``Q`` / ``Q2``.",
          "type": "number"
        },
        "time": {
          "description": "Time period column name.",
          "type": "string"
        },
        "treated_unit": {
          "description": "Identifier of the treated unit.",
          "type": "string"
        },
        "treatment_time": {
          "description": "First treatment period.",
          "type": "string"
        },
        "unit": {
          "description": "Unit identifier column name.",
          "type": "string"
        },
        "w_constr": {
          "default": "simplex",
          "description": "Weight constraint, as in R ``w.constr = list(name = ...)``: - ``'simplex'`` : w >= 0, sum(w) = Q (Q = 1) - ``'lasso'`` : ||w||_1 <= Q (Q = 1), weights may be negative - ``'ridge'`` : ||w||_2 <= Q, Q from R's ``shrinkage.EST`` rule (``max(Q_hat, 0.5)`` with Q_hat = ||b_ols|| / (1 + lambda), lambda = sigma^2 J / ||b_ols||^2) - ``'L1-L2'`` : w >= 0, sum(w) = 1 and ||w||_2 <= Q2 (Q2 as ridge Q) - ``'ols'`` : unconstrained least squares (``'ls'`` is an alias)",
          "type": "string"
        }
      },
      "required": [
        "data",
        "outcome",
        "unit",
        "time",
        "treated_unit",
        "treatment_time"
      ],
      "type": "object"
    }
  },
  {
    "description": "Prepare data matrices for synthetic control estimation. Validation: certified parity evidence.",
    "name": "scdata",
    "parameters": {
      "properties": {
        "data": {
          "description": "Long-format panel data (one row per unit-period).",
          "type": "string"
        },
        "outcome": {
          "description": "Outcome variable column name.",
          "type": "string"
        },
        "time": {
          "description": "Time period column name.",
          "type": "string"
        },
        "treated_unit": {
          "description": "Identifier of the treated unit.",
          "type": "string"
        },
        "treatment_time": {
          "description": "First treatment period (periods ``< treatment_time`` are pre-treatment).",
          "type": "string"
        },
        "unit": {
          "description": "Unit identifier column name.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "outcome",
        "unit",
        "time",
        "treated_unit",
        "treatment_time"
      ],
      "type": "object"
    }
  },
  {
    "description": "Distributional Synthetic Controls (Gunsilius 2023). Validation: certified parity evidence.",
    "name": "discos",
    "parameters": {
      "properties": {
        "M": {
          "default": 1000,
          "description": "Individual-level data, ``method='quantile'``: number of quantile nodes in each weight regression (R ``M``). Default nodes are the mid-points ``(m - 0.5) / M``.",
          "type": "integer"
        },
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals.",
          "type": "number"
        },
        "cdf_grid": {
          "description": "``method='mixture'``: outcome grid per period (all periods, in time order) on which the CDFs are compared and inverted (R's ``runif(G)`` grid). Default: ``G`` equispaced points on ``[floor(10 min)/10 - 0.25, ceil(10 max)/10 + 0.25]``.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "data": {
          "description": "Panel data in long format with columns for unit, time, and outcome.",
          "type": "string"
        },
        "method": {
          "description": "Individual-level data: ``'quantile'`` (default; R ``mixture = FALSE``) or ``'mixture'`` (R ``mixture = TRUE``) -- see the module docstring. Aggregate panels: ``'mixture'`` (default) or ``'quantile'`` select the fallback heuristic.",
          "enum": [
            "quantile",
            "mixture"
          ],
          "type": "string"
        },
        "n_quantiles": {
          "description": "Individual-level data: ``G`` -- the counterfactual quantile function is reported on ``0, 1/G, ..., 1`` and the mixture CDF grid has ``G`` points (default 1000, as R). Aggregate panels: number of quantile grid points on (0, 1) (default 100).",
          "type": "integer"
        },
        "outcome": {
          "description": "Outcome variable column name.",
          "type": "string"
        },
        "placebo": {
          "default": true,
          "description": "Run in-space placebo permutation tests for inference.",
          "type": "boolean"
        },
        "q_nodes": {
          "description": "Quantile nodes for the weight regressions: one array reused for every pre-period, or one array per pre-period (e.g. R's ``runif(M)`` draws, to reproduce ``DiSCo`` exactly).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "seed": {
          "description": "Unused (all node sets are deterministic unless supplied).",
          "type": "integer"
        },
        "simplex": {
          "default": false,
          "description": "Individual-level data: restrict weights to be non-negative (R ``simplex``). ``False`` (R default) allows negative weights.",
          "type": "boolean"
        },
        "time": {
          "description": "Time period column name.",
          "type": "string"
        },
        "treated_unit": {
          "description": "Value in *unit* that identifies the treated unit.",
          "type": "string"
        },
        "treatment_time": {
          "description": "First period of treatment (inclusive).",
          "type": "string"
        },
        "unit": {
          "description": "Unit identifier column name.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "outcome",
        "unit",
        "time",
        "treated_unit",
        "treatment_time"
      ],
      "type": "object"
    }
  },
  {
    "description": "Test for distributional treatment effects.",
    "name": "discos_test",
    "parameters": {
      "properties": {
        "result": {
          "description": "Output from ``discos()`` or ``qqsynth()``.",
          "type": "string"
        },
        "test": {
          "default": "ks",
          "description": "``'ks'``: two-sample Kolmogorov-Smirnov test comparing treated and counterfactual quantile functions. ``'cvm'``: Cramer-von Mises test statistic (permutation-based). ``'stochastic_dominance'``: first-order stochastic dominance test.",
          "enum": [
            "ks",
            "cvm",
            "stochastic_dominance"
          ],
          "type": "string"
        }
      },
      "required": [
        "result"
      ],
      "type": "object"
    }
  },
  {
    "description": "Visualise distributional synthetic control results.",
    "name": "discos_plot",
    "parameters": {
      "properties": {
        "ax": {
          "description": "Pre-existing axes for the plot.",
          "type": "string"
        },
        "ci_alpha": {
          "default": 0.2,
          "description": "Transparency for CI band.",
          "type": "number"
        },
        "color": {
          "default": "#2C3E50",
          "description": "Primary plot colour.",
          "type": "string"
        },
        "figsize": {
          "default": [
            10,
            6
          ],
          "description": "Figure size.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "result": {
          "description": "Output from ``discos()`` or ``qqsynth()``.",
          "type": "string"
        },
        "title": {
          "description": "Plot title override.",
          "type": "string"
        },
        "type": {
          "default": "quantile_effect",
          "description": "default 'quantile_effect' ``'quantile_effect'``: treatment effect Delta(tau) across quantiles with CIs. ``'quantile_comparison'``: overlay treated vs. counterfactual quantile functions. ``'gap'``: gap plot (treated - synthetic) over time. ``'weights'``: horizontal bar chart of donor weights.",
          "enum": [
            "quantile_effect",
            "quantile_comparison",
            "gap",
            "weights"
          ],
          "type": "string"
        }
      },
      "required": [
        "result"
      ],
      "type": "object"
    }
  },
  {
    "description": "Quantile Synthetic Control: exactly ``sp.discos(..., method='quantile')``.",
    "name": "qqsynth",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level.",
          "type": "number"
        },
        "data": {
          "description": "Panel data in long format.",
          "type": "string"
        },
        "n_quantiles": {
          "default": 100,
          "description": "Number of quantile grid points.",
          "type": "integer"
        },
        "outcome": {
          "description": "Outcome variable column.",
          "type": "string"
        },
        "placebo": {
          "default": true,
          "description": "Run placebo permutation inference.",
          "type": "boolean"
        },
        "seed": {
          "description": "Random seed.",
          "type": "integer"
        },
        "time": {
          "description": "Time period column.",
          "type": "string"
        },
        "treated_unit": {
          "description": "Identifier of the treated unit.",
          "type": "string"
        },
        "treatment_time": {
          "description": "First treatment period (inclusive).",
          "type": "string"
        },
        "unit": {
          "description": "Unit identifier column.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "outcome",
        "unit",
        "time",
        "treated_unit",
        "treatment_time"
      ],
      "type": "object"
    }
  },
  {
    "description": "Test for stochastic dominance of the treated distribution over the Validation: validated evidence tier (known-truth, reference, external-parity, or Monte Carlo artifact).",
    "name": "stochastic_dominance",
    "parameters": {
      "properties": {
        "order": {
          "default": 1,
          "description": "Order of stochastic dominance. 1 = first-order (CDF dominance). 2 = second-order (integrated CDF dominance).",
          "enum": [
            "1",
            "2"
          ],
          "type": "integer"
        },
        "result": {
          "description": "Output from ``discos()`` or ``qqsynth()``.",
          "type": "string"
        }
      },
      "required": [
        "result"
      ],
      "type": "object"
    }
  },
  {
    "description": "Leave-one-out donor sensitivity for Synthetic Control. Validation: certified parity evidence.",
    "name": "synth_loo",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Accepted for API symmetry with :func:`synth_sensitivity`; it does not affect any returned column.",
          "type": "number"
        },
        "data": {
          "description": "Long-format panel.",
          "type": "string"
        },
        "outcome": {
          "description": "Outcome variable.",
          "type": "string"
        },
        "penalization": {
          "default": 0.0,
          "description": "Ridge penalty forwarded to SCM.",
          "type": "number"
        },
        "time": {
          "description": "Time column.",
          "type": "string"
        },
        "treated_unit": {
          "description": "Identifier of the treated unit.",
          "type": "string"
        },
        "treatment_time": {
          "description": "First treatment period.",
          "type": "string"
        },
        "unit": {
          "description": "Unit identifier column.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "outcome",
        "unit",
        "time",
        "treated_unit",
        "treatment_time"
      ],
      "type": "object"
    }
  },
  {
    "description": "Time-placebo (\"backdating\") test for Synthetic Control. Validation: certified parity evidence.",
    "name": "synth_time_placebo",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Accepted for API symmetry with :func:`synth_sensitivity`; it does not affect any returned column.",
          "type": "number"
        },
        "data": {
          "description": "Long-format panel.",
          "type": "string"
        },
        "n_placebo_times": {
          "description": "Max number of placebo treatment times to try (a subset drawn with a fixed seed). Default is all feasible pre-treatment times: every pre-period from the third on, so each placebo fit keeps >= 2 pre-periods. Real post-treatment periods are discarded before the placebo fits.",
          "type": "integer"
        },
        "outcome": {
          "description": "Outcome variable.",
          "type": "string"
        },
        "penalization": {
          "default": 0.0,
          "description": "Ridge penalty forwarded to SCM.",
          "type": "number"
        },
        "time": {
          "description": "Time column.",
          "type": "string"
        },
        "treated_unit": {
          "description": "Identifier of the treated unit.",
          "type": "string"
        },
        "treatment_time": {
          "description": "Real first treatment period.",
          "type": "string"
        },
        "unit": {
          "description": "Unit identifier column.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "outcome",
        "unit",
        "time",
        "treated_unit",
        "treatment_time"
      ],
      "type": "object"
    }
  },
  {
    "description": "Donor-pool bootstrap sensitivity for Synthetic Control. Validation: certified parity evidence.",
    "name": "synth_donor_sensitivity",
    "parameters": {
      "properties": {
        "data": {
          "description": "Long-format panel.",
          "type": "string"
        },
        "k": {
          "description": "Donor subset size. Default is ``floor(J * 0.75)`` where *J* is the total number of donors.",
          "type": "integer"
        },
        "n_samples": {
          "default": 100,
          "description": "Number of random donor subsets to draw.",
          "type": "integer"
        },
        "outcome": {
          "description": "Outcome variable.",
          "type": "string"
        },
        "penalization": {
          "default": 0.0,
          "description": "Ridge penalty forwarded to SCM.",
          "type": "number"
        },
        "seed": {
          "description": "Random seed for reproducibility.",
          "type": "integer"
        },
        "time": {
          "description": "Time column.",
          "type": "string"
        },
        "treated_unit": {
          "description": "Identifier of the treated unit.",
          "type": "string"
        },
        "treatment_time": {
          "description": "First treatment period.",
          "type": "string"
        },
        "unit": {
          "description": "Unit identifier column.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "outcome",
        "unit",
        "time",
        "treated_unit",
        "treatment_time"
      ],
      "type": "object"
    }
  },
  {
    "description": "Pre-fit-filtered placebo p-values (Abadie et al. 2010). Validation: certified parity evidence.",
    "name": "synth_rmspe_filter",
    "parameters": {
      "properties": {
        "data": {
          "description": "Long-format panel.",
          "type": "string"
        },
        "metric": {
          "default": "rmspe",
          "description": "Scale on which ``thresholds`` act. ``'rmspe'`` (default) compares pre-period RMSPEs, the convention of Stata ``synth_runner``'s ``pre_limit_mult()``. ``'mspe'`` compares pre-period MSPEs, the convention of Abadie et al. (2010), who drop placebos with a pre-period MSPE above 20 / 5 / 2 times California's, and of R ``SCtools::mspe.test(discard.extreme = TRUE, mspe.limit = ...)``. A threshold ``c`` on the MSPE scale equals ``sqrt(c)`` on the RMSPE scale.",
          "enum": [
            "rmspe",
            "mspe"
          ],
          "type": "string"
        },
        "outcome": {
          "description": "Outcome variable.",
          "type": "string"
        },
        "penalization": {
          "default": 0.0,
          "description": "Ridge penalty.",
          "type": "number"
        },
        "placebo_pool": {
          "default": "include_treated",
          "description": "Donor pool of each placebo fit. ``'include_treated'`` (default) uses every other unit, including the actually-treated one, which is the exact permutation design under the sharp null of no effect and matches ``sp.synth``'s own placebo loop. ``'exclude_treated'`` drops the treated unit from every placebo's donor pool, as R ``SCtools::generate.placebos`` and Stata ``synth_runner`` do.",
          "enum": [
            "include_treated",
            "exclude_treated"
          ],
          "type": "string"
        },
        "thresholds": {
          "description": "Cut-off multiples of the treated unit's pre-treatment fit; a placebo is kept when its fit is ``<= threshold x`` the treated unit's. Default ``[1, 2, 5, 10, 20, np.inf]``.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "time": {
          "description": "Time column.",
          "type": "string"
        },
        "treated_unit": {
          "description": "Identifier of the treated unit.",
          "type": "string"
        },
        "treatment_time": {
          "description": "First treatment period.",
          "type": "string"
        },
        "unit": {
          "description": "Unit identifier column.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "outcome",
        "unit",
        "time",
        "treated_unit",
        "treatment_time"
      ],
      "type": "object"
    }
  },
  {
    "description": "Run all SCM sensitivity diagnostics in a single call.",
    "name": "synth_sensitivity",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level.",
          "type": "number"
        },
        "data": {
          "description": "Long-format panel.",
          "type": "string"
        },
        "n_donor_samples": {
          "default": 100,
          "description": "Number of random donor subsets for donor sensitivity.",
          "type": "integer"
        },
        "outcome": {
          "description": "Outcome variable.",
          "type": "string"
        },
        "penalization": {
          "default": 0.0,
          "description": "Ridge penalty.",
          "type": "number"
        },
        "seed": {
          "description": "Random seed.",
          "type": "integer"
        },
        "time": {
          "description": "Time column.",
          "type": "string"
        },
        "treated_unit": {
          "description": "Identifier of the treated unit.",
          "type": "string"
        },
        "treatment_time": {
          "description": "First treatment period.",
          "type": "string"
        },
        "unit": {
          "description": "Unit identifier column.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "outcome",
        "unit",
        "time",
        "treated_unit",
        "treatment_time"
      ],
      "type": "object"
    }
  },
  {
    "description": "Multi-panel sensitivity diagnostic plot.",
    "name": "synth_sensitivity_plot",
    "parameters": {
      "properties": {
        "figsize": {
          "default": [
            14,
            10
          ],
          "description": "Figure size in inches.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "sensitivity_result": {
          "description": "Output from :func:`synth_sensitivity`.",
          "type": "object"
        },
        "title": {
          "description": "Super-title for the figure.",
          "type": "string"
        }
      },
      "required": [
        "sensitivity_result"
      ],
      "type": "object"
    }
  },
  {
    "description": "Power analysis for Synthetic Control designs. Validation: validated evidence tier (known-truth, reference, external-parity, or Monte Carlo artifact).",
    "name": "synth_power",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for the placebo test.",
          "type": "number"
        },
        "data": {
          "description": "Long-format panel data.",
          "type": "string"
        },
        "effect_sizes": {
          "description": "Grid of hypothetical additive effect sizes to evaluate. If ``None``, auto-generates 10 steps from 0 to 3 * pre-treatment SD of the outcome.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "n_simulations": {
          "default": 200,
          "description": "Number of Monte-Carlo simulations per effect size.",
          "type": "integer"
        },
        "outcome": {
          "description": "Outcome variable name.",
          "type": "string"
        },
        "seed": {
          "description": "Random seed for reproducibility.",
          "type": "integer"
        },
        "time": {
          "description": "Time period column.",
          "type": "string"
        },
        "treated_unit": {
          "description": "Identifier of the treated unit.",
          "type": "string"
        },
        "treatment_time": {
          "description": "First treatment period (inclusive).",
          "type": "string"
        },
        "unit": {
          "description": "Unit identifier column.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "outcome",
        "unit",
        "time",
        "treated_unit",
        "treatment_time"
      ],
      "type": "object"
    }
  },
  {
    "description": "Minimum Detectable Effect for a Synthetic Control design.",
    "name": "synth_mde",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for the placebo test.",
          "type": "number"
        },
        "data": {
          "description": "Long-format panel data.",
          "type": "string"
        },
        "n_simulations": {
          "default": 200,
          "description": "Number of simulations per effect size.",
          "type": "integer"
        },
        "outcome": {
          "description": "Outcome variable name.",
          "type": "string"
        },
        "power_target": {
          "default": 0.8,
          "description": "Desired power level.",
          "type": "number"
        },
        "seed": {
          "description": "Random seed for reproducibility.",
          "type": "integer"
        },
        "time": {
          "description": "Time period column.",
          "type": "string"
        },
        "treated_unit": {
          "description": "Identifier of the treated unit.",
          "type": "string"
        },
        "treatment_time": {
          "description": "First treatment period.",
          "type": "string"
        },
        "unit": {
          "description": "Unit identifier column.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "outcome",
        "unit",
        "time",
        "treated_unit",
        "treatment_time"
      ],
      "type": "object"
    }
  },
  {
    "description": "Plot the power curve from :func:`synth_power`.",
    "name": "synth_power_plot",
    "parameters": {
      "properties": {
        "ax": {
          "description": "Axes to plot on. If ``None``, a new figure is created.",
          "type": "string"
        },
        "figsize": {
          "default": [
            9,
            6
          ],
          "description": "Figure size (width, height) in inches.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "power_result": {
          "description": "Output of :func:`synth_power`. Must contain columns ``effect_size``, ``power``, and ``mde_flag``.",
          "type": "string"
        },
        "title": {
          "description": "Custom plot title. Defaults to ``\"SCM Power Curve -- Minimum Detectable Effect\"``.",
          "type": "string"
        }
      },
      "required": [
        "power_result"
      ],
      "type": "object"
    }
  },
  {
    "description": "Run multiple SCM variants and compare them side by side. Cost: Runs every estimator in methods= end to end, so cost is the sum of the individual fits -- and each placebo-enabled member internally re-runs once per donor. Expect it to be the slowest call in a synthetic-control workflow; narrow methods= once you have shortlisted.",
    "name": "synth_compare",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals.",
          "type": "number"
        },
        "data": {
          "description": "Long-format panel data.",
          "type": "string"
        },
        "methods": {
          "description": "SCM variants to compare. If ``None`` (default), all 20 registered methods are attempted, in ascending complexity order: ``classic, penalized, demeaned, detrended, unconstrained, elastic_net, augmented, sdid, gsynth, mc, discos, scpi, penscm, fdid, sparse, cluster, kernel, kernel_ridge, bayesian, bsts``. Pass an explicit subset to reduce runtime.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "outcome": {
          "description": "Outcome variable column name.",
          "type": "string"
        },
        "placebo": {
          "default": true,
          "description": "Whether to run placebo inference for each method.",
          "type": "boolean"
        },
        "time": {
          "description": "Time period column.",
          "type": "string"
        },
        "treated_unit": {
          "description": "Identifier of the treated unit.",
          "type": "string"
        },
        "treatment_time": {
          "description": "First treatment period (inclusive).",
          "type": "string"
        },
        "unit": {
          "description": "Unit identifier column.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "outcome",
        "unit",
        "time"
      ],
      "type": "object"
    }
  },
  {
    "description": "Quickly recommend the best SCM method for the given data.",
    "name": "synth_recommend",
    "parameters": {
      "properties": {
        "data": {
          "description": "Long-format panel data.",
          "type": "string"
        },
        "outcome": {
          "description": "Outcome variable column name.",
          "type": "string"
        },
        "time": {
          "description": "Time period column.",
          "type": "string"
        },
        "treated_unit": {
          "description": "Identifier of the treated unit.",
          "type": "string"
        },
        "treatment_time": {
          "description": "First treatment period (inclusive).",
          "type": "string"
        },
        "unit": {
          "description": "Unit identifier column.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "outcome",
        "unit",
        "time"
      ],
      "type": "object"
    }
  },
  {
    "description": "Structured container for multi-method SCM comparison results.",
    "name": "SynthComparison",
    "parameters": {
      "properties": {
        "comparison_table": {
          "description": "comparison_table parameter (pd.DataFrame).",
          "type": "string"
        },
        "recommendation_reason": {
          "description": "recommendation_reason parameter (str).",
          "type": "string"
        },
        "recommended": {
          "description": "recommended parameter (str).",
          "type": "string"
        },
        "results": {
          "description": "results parameter (Dict[str, CausalResult]).",
          "type": "object"
        }
      },
      "required": [
        "results",
        "comparison_table",
        "recommended",
        "recommendation_reason"
      ],
      "type": "object"
    }
  },
  {
    "description": "Generate a comprehensive Synthetic Control analysis report.",
    "name": "synth_report",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for CIs and hypothesis tests.",
          "type": "number"
        },
        "data": {
          "description": "Long-format panel data.",
          "type": "string"
        },
        "method": {
          "default": "classic",
          "description": "SCM variant passed to ``synth()``.",
          "type": "string"
        },
        "outcome": {
          "description": "Outcome variable name.",
          "type": "string"
        },
        "output": {
          "default": "text",
          "description": "Output format: ``'text'``, ``'markdown'``, or ``'latex'``.",
          "type": "string"
        },
        "sensitivity": {
          "default": true,
          "description": "Whether to include the sensitivity analysis section.",
          "type": "boolean"
        },
        "time": {
          "description": "Time period column.",
          "type": "string"
        },
        "treated_unit": {
          "description": "Identifier of the treated unit.",
          "type": "string"
        },
        "treatment_time": {
          "description": "First treatment period (inclusive).",
          "type": "string"
        },
        "unit": {
          "description": "Unit identifier column.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "outcome",
        "unit",
        "time"
      ],
      "type": "object"
    }
  },
  {
    "description": "Generate an SCM report and write it directly to a file.",
    "name": "synth_report_to_file",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level.",
          "type": "number"
        },
        "data": {
          "description": "Long-format panel data.",
          "type": "string"
        },
        "filename": {
          "default": "report.md",
          "description": "Output file path.",
          "type": "string"
        },
        "method": {
          "default": "classic",
          "description": "SCM variant passed to ``synth()``.",
          "type": "string"
        },
        "outcome": {
          "description": "Outcome variable name.",
          "type": "string"
        },
        "output": {
          "default": "markdown",
          "description": "Output format: ``'text'``, ``'markdown'``, or ``'latex'``.",
          "type": "string"
        },
        "sensitivity": {
          "default": true,
          "description": "Whether to include the sensitivity analysis section.",
          "type": "boolean"
        },
        "time": {
          "description": "Time period column.",
          "type": "string"
        },
        "treated_unit": {
          "description": "Identifier of the treated unit.",
          "type": "string"
        },
        "treatment_time": {
          "description": "First treatment period (inclusive).",
          "type": "string"
        },
        "unit": {
          "description": "Unit identifier column.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "outcome",
        "unit",
        "time"
      ],
      "type": "object"
    }
  },
  {
    "description": "Formatted LaTeX table for synthetic-control results.",
    "name": "synth_to_latex",
    "parameters": {
      "properties": {
        "booktabs": {
          "default": true,
          "description": "If True, use ``\\toprule`` / ``\\midrule`` / ``\\bottomrule`` (requires ``\\usepackage{booktabs}``). Falls back to ``\\hline`` if False.",
          "type": "boolean"
        },
        "caption": {
          "description": "Table caption. Defaults to a sensible auto-generated string.",
          "type": "string"
        },
        "digits": {
          "description": "Number of decimal places.",
          "type": "integer"
        },
        "label": {
          "description": "LaTeX label for cross-referencing. Defaults to ``\"tab:synth\"`` (single) or ``\"tab:synth_compare\"`` (multi).",
          "type": "string"
        },
        "method_names": {
          "description": "Override column labels in comparison mode.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "obj": {
          "description": "Object to render. ``SynthComparison`` and lists trigger the side-by-side multi-method layout.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "show_ci": {
          "default": true,
          "description": "Include the confidence-interval row.",
          "type": "boolean"
        },
        "show_weights": {
          "default": false,
          "description": "Append a panel listing the top-N donor weights.",
          "type": "boolean"
        },
        "top_n_weights": {
          "default": 5,
          "description": "How many donors to show per method when ``show_weights=True``.",
          "type": "integer"
        }
      },
      "required": [
        "obj"
      ],
      "type": "object"
    }
  },
  {
    "description": "GitHub-flavoured Markdown table for synthetic-control results.",
    "name": "synth_to_markdown",
    "parameters": {
      "properties": {
        "digits": {
          "description": "digits parameter (Optional[int]).",
          "type": "integer"
        },
        "method_names": {
          "description": "method_names parameter (Optional[Sequence[str]]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "obj": {
          "description": "obj parameter (Union[CausalResult, 'SynthComparison', List[CausalResult]]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "show_ci": {
          "default": true,
          "description": "show_ci parameter (bool).",
          "type": "boolean"
        },
        "show_weights": {
          "default": false,
          "description": "show_weights parameter (bool).",
          "type": "boolean"
        },
        "title": {
          "description": "title parameter (Optional[str]).",
          "type": "string"
        },
        "top_n_weights": {
          "default": 5,
          "description": "top_n_weights parameter (int).",
          "type": "integer"
        }
      },
      "required": [
        "obj"
      ],
      "type": "object"
    }
  },
  {
    "description": "Multi-sheet Excel workbook for synthetic-control results.",
    "name": "synth_to_excel",
    "parameters": {
      "properties": {
        "digits": {
          "default": 6,
          "description": "Rounding for floating-point values.",
          "type": "integer"
        },
        "method_names": {
          "description": "Override sheet / column labels.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "obj": {
          "description": "Object to export.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "path": {
          "description": "Destination ``.xlsx`` file path.",
          "type": "string"
        }
      },
      "required": [
        "obj",
        "path"
      ],
      "type": "object"
    }
  },
  {
    "description": "German reunification dataset (simulated).",
    "name": "german_reunification",
    "parameters": {
      "properties": {},
      "required": [],
      "type": "object"
    }
  },
  {
    "description": "Basque Country terrorism dataset (simulated).",
    "name": "basque_terrorism",
    "parameters": {
      "properties": {},
      "required": [],
      "type": "object"
    }
  },
  {
    "description": "California Proposition 99 tobacco dataset (simulated, extended).",
    "name": "california_tobacco",
    "parameters": {
      "properties": {},
      "required": [],
      "type": "object"
    }
  },
  {
    "description": "Spatial weights matrix with CSR-sparse backing. Validation: validated evidence tier (known-truth, reference, external-parity, or Monte Carlo artifact).",
    "name": "W",
    "parameters": {
      "properties": {
        "id_order": {
          "description": "Explicit ordering of observation ids. Defaults to ``sorted(neighbors)``.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "neighbors": {
          "description": "Mapping observation id -> list of neighbour ids.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "weights": {
          "description": "Matching weight values. If ``None``, binary (1.0) weights are used.",
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "required": [
        "neighbors"
      ],
      "type": "object"
    }
  },
  {
    "description": "Global Moran's I -- test for global spatial autocorrelation. Validation: certified parity evidence.",
    "name": "moran",
    "parameters": {
      "properties": {
        "permutations": {
          "default": 999,
          "description": "Number of conditional random permutations used for the pseudo p-value. Set to ``0`` to skip the permutation test and rely on the analytic z-score only.",
          "type": "integer"
        },
        "seed": {
          "description": "Seed for the permutation RNG (``numpy.random.default_rng``) for reproducible pseudo p-values.",
          "type": "integer"
        },
        "two_tailed": {
          "default": true,
          "description": "If True the analytic ``p_norm`` is two-tailed; otherwise it is the upper-tail (positive autocorrelation) p-value.",
          "type": "boolean"
        },
        "w": {
          "description": "Spatial weights object. ``w.sparse`` must be an ``n x n`` matrix whose total sum is non-zero. Row-standardised or binary weights both work.",
          "type": "string"
        },
        "y": {
          "description": "Variable of interest, length ``n`` (one value per spatial unit). Flattened to 1-D internally.",
          "type": "string"
        }
      },
      "required": [
        "y",
        "w"
      ],
      "type": "object"
    }
  },
  {
    "description": "Local Moran's I (LISA) -- per-location spatial association. Validation: certified parity evidence.",
    "name": "moran_local",
    "parameters": {
      "properties": {
        "permutations": {
          "default": 999,
          "description": "Number of conditional permutations for the per-location pseudo p-values. ``0`` skips the permutation test.",
          "type": "integer"
        },
        "seed": {
          "description": "Seed for the permutation RNG for reproducibility.",
          "type": "integer"
        },
        "w": {
          "description": "Spatial weights object; ``w.sparse`` is the ``n x n`` weights matrix.",
          "type": "string"
        },
        "y": {
          "description": "Variable of interest, length ``n``.",
          "type": "string"
        }
      },
      "required": [
        "y",
        "w"
      ],
      "type": "object"
    }
  },
  {
    "description": "Moran scatter: z vs spatial lag Wz. Slope of OLS line = Moran's I.",
    "name": "moran_plot",
    "parameters": {
      "properties": {
        "ax": {
          "description": "ax parameter (Optional[Any]).",
          "type": "string"
        },
        "w": {
          "description": "Weights, spatial weights, or balancing-weight input for this estimator.",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "y",
        "w"
      ],
      "type": "object"
    }
  },
  {
    "description": "Moran's I applied to regression residuals (quick LM-err companion). Validation: certified parity evidence.",
    "name": "moran_residuals",
    "parameters": {
      "properties": {
        "W": {
          "description": "Covariates, proxy variables, or weights used by this estimator.",
          "type": "string"
        },
        "X": {
          "description": "The regression's design matrix, **including its constant**. When supplied, the p-value uses Cliff and Ord's null for *regression residuals* -- the one ``spdep::lm.morantest`` uses -- which depends on ``X`` through the hat matrix. Without it the function falls back to the raw-variable null, which is not the residuals' null. .. versionchanged:: 1.27.0 The statistic was always exact (5e-16 against ``lm.morantest``). The p-value was computed from the observed-variable null, which is the wrong reference distribution for a projection of ``y``; on a 10x10 lattice it read 3.64e-06 where ``lm.morantest`` reports 1.56e-06 (two-sided 3.13e-06). Pass ``X`` for the correct null.",
          "type": "string"
        },
        "residuals": {
          "description": "residuals parameter (np.ndarray).",
          "type": "string"
        },
        "row_normalize": {
          "default": true,
          "description": "row_normalize parameter (bool).",
          "type": "boolean"
        }
      },
      "required": [
        "residuals",
        "W"
      ],
      "type": "object"
    }
  },
  {
    "description": "Global Geary's C -- squared-difference measure of spatial autocorrelation. Validation: certified parity evidence.",
    "name": "geary",
    "parameters": {
      "properties": {
        "assumption": {
          "default": "randomisation",
          "description": "Null used for the closed-form variance, following Cliff and Ord (1981) and matching ``spdep::geary.test``'s ``randomisation=`` argument. The randomisation null carries the fourth-moment term ``k = m4 / m2^2``; the normality null drops it. .. versionchanged:: 1.27.0 ``variance``, ``z_score`` and ``p_norm`` used to be ``NaN`` whenever ``permutations=0``, even though Geary's C has a closed form for all of them -- ``moran`` reported its own analytic null in the same situation. The reported ``z_score`` also changes sign: it is now ``(E[C] - C) / sd``, so a positive z means positive spatial autocorrelation, which is ``spdep``'s convention and the one ``sp.moran`` already used. Two-sided p-values are unaffected.",
          "enum": [
            "randomisation",
            "normality"
          ],
          "type": "string"
        },
        "permutations": {
          "default": 999,
          "description": "Number of random permutations for the pseudo p-value. ``0`` skips the permutation test; the analytic ``variance`` / ``z_score`` / ``p_norm`` are reported either way.",
          "type": "integer"
        },
        "seed": {
          "description": "Seed for the permutation RNG for reproducibility.",
          "type": "integer"
        },
        "w": {
          "description": "Spatial weights object; ``w.sparse`` must have non-zero total.",
          "type": "string"
        },
        "y": {
          "description": "Variable of interest, length ``n``.",
          "type": "string"
        }
      },
      "required": [
        "y",
        "w"
      ],
      "type": "object"
    }
  },
  {
    "description": "Global Getis-Ord G -- overall concentration of high (or low) values. Validation: certified parity evidence.",
    "name": "getis_ord_g",
    "parameters": {
      "properties": {
        "permutations": {
          "default": 999,
          "description": "Number of random permutations for the pseudo p-value, empirical variance and z-score. ``0`` skips the permutation test.",
          "type": "integer"
        },
        "seed": {
          "description": "Seed for the permutation RNG for reproducibility.",
          "type": "integer"
        },
        "w": {
          "description": "Spatial weights object defining the neighbourhood structure.",
          "type": "string"
        },
        "y": {
          "description": "Non-negative variable of interest, length ``n``.",
          "type": "string"
        }
      },
      "required": [
        "y",
        "w"
      ],
      "type": "object"
    }
  },
  {
    "description": "Local Getis-Ord Gi / Gi* -- hotspot and coldspot detection. Validation: certified parity evidence.",
    "name": "getis_ord_local",
    "parameters": {
      "properties": {
        "permutations": {
          "default": 999,
          "description": "Accepted for API symmetry; the current implementation returns the analytic standardised statistic.",
          "type": "integer"
        },
        "seed": {
          "description": "Seed placeholder for API symmetry.",
          "type": "integer"
        },
        "star": {
          "default": true,
          "description": "If True compute Gi* (self-included); otherwise Gi (self-excluded).",
          "type": "boolean"
        },
        "w": {
          "description": "Spatial weights object; densified internally to an ``n x n`` matrix.",
          "type": "string"
        },
        "y": {
          "description": "Variable of interest, length ``n``.",
          "type": "string"
        }
      },
      "required": [
        "y",
        "w"
      ],
      "type": "object"
    }
  },
  {
    "description": "Join-count statistics for a binary spatial variable. Validation: certified parity evidence.",
    "name": "join_counts",
    "parameters": {
      "properties": {
        "permutations": {
          "default": 999,
          "description": "Number of random permutations for the BB pseudo p-value. ``0`` skips the test.",
          "type": "integer"
        },
        "seed": {
          "description": "Seed for the permutation RNG for reproducibility.",
          "type": "integer"
        },
        "w": {
          "description": "Spatial weights object defining the join structure.",
          "type": "string"
        },
        "y": {
          "description": "Binary variable, values in ``{0, 1}``, length ``n``.",
          "type": "string"
        }
      },
      "required": [
        "y",
        "w"
      ],
      "type": "object"
    }
  },
  {
    "description": "Classify each observation HH/LL/HL/LH/Not significant and colour on a GDF.",
    "name": "lisa_cluster_map",
    "parameters": {
      "properties": {
        "ax": {
          "description": "ax parameter (Optional[Any]).",
          "type": "string"
        },
        "gdf": {
          "description": "gdf parameter.",
          "type": "string"
        },
        "p_threshold": {
          "default": 0.05,
          "description": "p_threshold parameter (float).",
          "type": "number"
        },
        "w": {
          "description": "Weights, spatial weights, or balancing-weight input for this estimator.",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "y",
        "w",
        "gdf"
      ],
      "type": "object"
    }
  },
  {
    "description": "Anselin (1988) Lagrange multiplier battery. Validation: certified parity evidence.",
    "name": "lm_tests",
    "parameters": {
      "properties": {
        "W": {
          "description": "Covariates, proxy variables, or weights used by this estimator.",
          "type": "string"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "formula": {
          "description": "``\"y ~ x1 + x2\"`` style formula (constant added automatically).",
          "type": "string"
        },
        "row_normalize": {
          "default": true,
          "description": "row_normalize parameter (bool).",
          "type": "boolean"
        }
      },
      "required": [
        "formula",
        "data",
        "W"
      ],
      "type": "object"
    }
  },
  {
    "description": "Compute direct / indirect / total impacts + simulated SEs. Validation: certified parity evidence.",
    "name": "impacts",
    "parameters": {
      "properties": {
        "n_sim": {
          "default": 1000,
          "description": "Number of Monte-Carlo draws for simulated SEs.",
          "type": "integer"
        },
        "result": {
          "description": "Output of ``sp.sar``, ``sp.sdm``, or ``sp.sac``.",
          "type": "string"
        },
        "seed": {
          "description": "RNG seed.",
          "type": "integer"
        }
      },
      "required": [
        "result"
      ],
      "type": "object"
    }
  },
  {
    "description": "Length of linear features falling inside each polygon.",
    "name": "line_length_in_polygon",
    "parameters": {
      "properties": {
        "allow_geographic": {
          "default": false,
          "description": "Opt out of the projected-CRS requirement. Only set this if you have verified the CRS units are metres.",
          "type": "boolean"
        },
        "lines_gdf": {
          "description": "Linear features. Must be ``LineString`` / ``MultiLineString``.",
          "type": "string"
        },
        "polygon_id": {
          "description": "Column in ``polygons_gdf`` identifying each unit. Defaults to the index of ``polygons_gdf``.",
          "type": "string"
        },
        "polygons_gdf": {
          "description": "Areal units. Must be ``Polygon`` / ``MultiPolygon``.",
          "type": "string"
        },
        "unit": {
          "default": "km",
          "description": "Output unit.",
          "enum": [
            "km",
            "m"
          ],
          "type": "string"
        }
      },
      "required": [
        "lines_gdf",
        "polygons_gdf"
      ],
      "type": "object"
    }
  },
  {
    "description": "Share of point features lying within a buffer of the linear features.",
    "name": "share_within_buffer",
    "parameters": {
      "properties": {
        "allow_geographic": {
          "default": false,
          "description": "Opt out of the projected-CRS requirement.",
          "type": "boolean"
        },
        "buffer_km": {
          "default": 10.0,
          "description": "Buffer radius in kilometres. Must be strictly positive.",
          "type": "number"
        },
        "group_col": {
          "description": "Column in ``points_gdf`` to group by (e.g. ``'county_id'``). If omitted the share is computed over all points.",
          "type": "string"
        },
        "lines_gdf": {
          "description": "Linear features to buffer (e.g. the canal).",
          "type": "string"
        },
        "points_gdf": {
          "description": "Point features (e.g. market towns).",
          "type": "string"
        }
      },
      "required": [
        "points_gdf",
        "lines_gdf"
      ],
      "type": "object"
    }
  },
  {
    "description": "Distance from each point to the nearest target feature.",
    "name": "distance_to_feature",
    "parameters": {
      "properties": {
        "allow_geographic": {
          "default": false,
          "description": "Opt out of the projected-CRS requirement.",
          "type": "boolean"
        },
        "points_gdf": {
          "description": "Point features.",
          "type": "string"
        },
        "target_gdf": {
          "description": "Target layer of any geometry type; the nearest distance to the union of its features is returned. Points inside a target polygon get ``0``.",
          "type": "string"
        },
        "unit": {
          "default": "km",
          "description": "Output unit.",
          "enum": [
            "km",
            "m"
          ],
          "type": "string"
        }
      },
      "required": [
        "points_gdf",
        "target_gdf"
      ],
      "type": "object"
    }
  },
  {
    "description": "Queen-contiguity spatial weights from polygon geometries. Validation: certified parity evidence.",
    "name": "queen_weights",
    "parameters": {
      "properties": {
        "gdf": {
          "description": "Layer of polygon geometries (accessed via ``gdf.geometry`` and the spatial index ``gdf.sindex``).",
          "type": "string"
        }
      },
      "required": [
        "gdf"
      ],
      "type": "object"
    }
  },
  {
    "description": "Rook-contiguity spatial weights from polygon geometries. Validation: certified parity evidence.",
    "name": "rook_weights",
    "parameters": {
      "properties": {
        "gdf": {
          "description": "Layer of polygon geometries.",
          "type": "string"
        }
      },
      "required": [
        "gdf"
      ],
      "type": "object"
    }
  },
  {
    "description": "k-nearest-neighbour spatial weights from point coordinates. Validation: certified parity evidence.",
    "name": "knn_weights",
    "parameters": {
      "properties": {
        "coords": {
          "description": "``(n, d)`` array of coordinates (typically ``d=2``).",
          "type": "string"
        },
        "k": {
          "default": 5,
          "description": "Number of nearest neighbours per unit. Must satisfy ``0 < k < n``.",
          "type": "integer"
        }
      },
      "required": [
        "coords"
      ],
      "type": "object"
    }
  },
  {
    "description": "Distance-band spatial weights -- connect units within a fixed radius. Validation: certified parity evidence.",
    "name": "distance_band",
    "parameters": {
      "properties": {
        "binary": {
          "default": true,
          "description": "If True all neighbour weights are ``1.0``; if False they are inverse Euclidean distance ``1/d`` (coincident points are dropped via an infinite distance).",
          "type": "boolean"
        },
        "coords": {
          "description": "``(n, d)`` array of coordinates.",
          "type": "string"
        },
        "threshold": {
          "description": "Distance band radius. Units within this distance become neighbours. Choose at least the maximum nearest-neighbour distance to avoid islands (units with no neighbours).",
          "type": "number"
        }
      },
      "required": [
        "coords",
        "threshold"
      ],
      "type": "object"
    }
  },
  {
    "description": "Kernel (distance-decay) spatial weights from point coordinates. Validation: certified parity evidence.",
    "name": "kernel_weights",
    "parameters": {
      "properties": {
        "bandwidth": {
          "description": "With ``fixed=True``, the bandwidth distance ``h`` (for the bisquare and triangular kernels, neighbours beyond ``h`` get zero weight; the gaussian kernel has infinite support so all units are included). With ``fixed=False``, the integer number ``k`` of nearest",
          "type": "number"
        },
        "coords": {
          "description": "``(n, d)`` array of coordinates.",
          "type": "string"
        },
        "fixed": {
          "default": true,
          "description": "Fixed (True) versus adaptive (False) bandwidth.",
          "type": "boolean"
        },
        "kernel": {
          "default": "gaussian",
          "description": "Kernel shape. ``gaussian`` ``exp(-u^2/2)``; ``bisquare`` ``(1-u^2)^2`` for ``u<1``; ``triangular`` ``1-u`` for ``u<1``, where ``u = d / bandwidth``.",
          "enum": [
            "gaussian",
            "bisquare",
            "triangular"
          ],
          "type": "string"
        }
      },
      "required": [
        "coords",
        "bandwidth"
      ],
      "type": "object"
    }
  },
  {
    "description": "Block (regime) spatial weights -- full connectivity within each group. Validation: certified parity evidence.",
    "name": "block_weights",
    "parameters": {
      "properties": {
        "regimes": {
          "description": "Length-``n`` array of group labels (any hashable dtype). Units with equal labels become mutual neighbours.",
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "required": [
        "regimes"
      ],
      "type": "object"
    }
  },
  {
    "description": "Fit Geographically Weighted Regression. Validation: certified parity evidence.",
    "name": "gwr",
    "parameters": {
      "properties": {
        "X": {
          "description": "Design matrix WITHOUT constant unless ``add_constant=False``.",
          "type": "string"
        },
        "add_constant": {
          "default": true,
          "description": "Prepend a column of ones to ``X``.",
          "type": "boolean"
        },
        "bw": {
          "description": "Bandwidth. Interpretation depends on ``fixed``: - ``fixed=True``: metric distance. - ``fixed=False`` (default, \"adaptive\"): number of nearest neighbours, counting the regression point itself. The kernel scale is the distance to the ``floor(bw)``-th nearest point (``GWmodel::gw.weight`` and ``mgwr`` both floor); ``bw > n`` extrapolates the scale to ``(bw / n) * max distance`` as ``GWmodel`` does.",
          "type": "number"
        },
        "coords": {
          "description": "Point coordinates (projected -- use UTM or equivalent; lat/lon will bias distances in most regions).",
          "type": "string"
        },
        "fixed": {
          "default": false,
          "description": "fixed parameter (bool).",
          "type": "boolean"
        },
        "kernel": {
          "default": "bisquare",
          "description": "Bisquare is mgwr's default. Gaussian and exponential have infinite support under both fixed and adaptive bandwidths; only the bisquare is truncated at the bandwidth.",
          "enum": [
            "bisquare",
            "gaussian",
            "exponential"
          ],
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "coords",
        "y",
        "X",
        "bw"
      ],
      "type": "object"
    }
  },
  {
    "description": "Multiscale GWR by back-fitting, as PySAL ``mgwr`` computes it. Validation: certified parity evidence.",
    "name": "mgwr",
    "parameters": {
      "properties": {
        "X": {
          "description": "Regressors, without a constant unless ``add_constant=False``. ``mgwr``'s documentation standardises ``y`` and ``X`` (mean 0, variance 1) before fitting, so that the bandwidths are comparable; this function does not standardise.",
          "type": "string"
        },
        "add_constant": {
          "default": true,
          "description": "add_constant parameter (bool).",
          "type": "boolean"
        },
        "bw_init": {
          "description": "Bandwidth of the initialising all-covariate GWR (``mgwr``'s ``init_multi``); searched when omitted.",
          "type": "number"
        },
        "bw_max": {
          "description": "Per-covariate search bounds (``mgwr``'s ``multi_bw_min`` / ``multi_bw_max``); a scalar applies to every covariate.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "bw_min": {
          "description": "Per-covariate search bounds (``mgwr``'s ``multi_bw_min`` / ``multi_bw_max``); a scalar applies to every covariate.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "bws": {
          "description": "Fixed covariate-specific bandwidths (length ``k``, intercept first when ``add_constant=True``). No bandwidth is searched; the result is the additive-model fit at these bandwidths.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "bws_same_times": {
          "default": 5,
          "description": "Freeze the bandwidths once they have been unchanged for this many consecutive sweeps.",
          "type": "integer"
        },
        "coords": {
          "description": "Projected point coordinates.",
          "type": "string"
        },
        "criterion": {
          "default": "AICc",
          "description": "Bandwidth criterion, with ``mgwr``'s definitions (its CV is the leave-one-out sum of squares divided by ``n``).",
          "enum": [
            "AICc",
            "AIC",
            "BIC",
            "CV"
          ],
          "type": "string"
        },
        "fixed": {
          "default": false,
          "description": "fixed parameter (bool).",
          "type": "boolean"
        },
        "kernel": {
          "default": "bisquare",
          "description": "Kernel function used for weighting or smoothing.",
          "enum": [
            "bisquare",
            "gaussian",
            "exponential"
          ],
          "type": "string"
        },
        "kernel_eps": {
          "default": 1.0000001,
          "description": "Multiplier of the adaptive nearest-neighbour distance, as in ``mgwr.kernels.Kernel``. ``1.0`` gives ``GWmodel``'s (and :func:`sp.gwr`'s) kernel, under which the ``int(bw)``-th neighbour gets zero bisquare weight.",
          "type": "number"
        },
        "max_iter": {
          "default": 200,
          "description": "Maximum back-fitting sweeps (``mgwr``'s ``max_iter_multi``).",
          "type": "integer"
        },
        "rss_score": {
          "default": false,
          "description": "Stop on the relative change of the RSS instead of the SOC.",
          "type": "boolean"
        },
        "search_max_iter": {
          "default": 200,
          "description": "Golden-section stop (``mgwr``'s ``tol`` / ``max_iter``).",
          "type": "integer"
        },
        "search_tol": {
          "default": 1e-06,
          "description": "Golden-section stop (``mgwr``'s ``tol`` / ``max_iter``).",
          "type": "number"
        },
        "tol": {
          "default": 1e-05,
          "description": "Back-fitting stop on the score of change (``mgwr``'s ``tol_multi``); see the module docstring. Until StatsPAI 1.28 this was a threshold on ``max |f_new - f_old|``.",
          "type": "number"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "coords",
        "y",
        "X"
      ],
      "type": "object"
    }
  },
  {
    "description": "Select a GWR bandwidth by golden-section search (``GWmodel::bw.gwr``). Validation: certified parity evidence.",
    "name": "gwr_bandwidth",
    "parameters": {
      "properties": {
        "X": {
          "description": "Regressors, without a constant unless ``add_constant=False``.",
          "type": "string"
        },
        "add_constant": {
          "default": true,
          "description": "add_constant parameter (bool).",
          "type": "boolean"
        },
        "bw_max": {
          "description": "Search bounds. Defaults are GWmodel's: ``[20, n]`` for an adaptive bandwidth (``[k + 2, n]`` when ``n <= 20``, where GWmodel's bounds would be empty) and ``[D / 5000, D]`` for a fixed one, ``D`` being the largest inter-point distance.",
          "type": "number"
        },
        "bw_min": {
          "description": "Search bounds. Defaults are GWmodel's: ``[20, n]`` for an adaptive bandwidth (``[k + 2, n]`` when ``n <= 20``, where GWmodel's bounds would be empty) and ``[D / 5000, D]`` for a fixed one, ``D`` being the largest inter-point distance.",
          "type": "number"
        },
        "coords": {
          "description": "Projected point coordinates.",
          "type": "string"
        },
        "criterion": {
          "default": "AICc",
          "description": "``\"AICc\"`` is ``bw.gwr(approach = \"AICc\")`` (and mgwr's default); ``\"CV\"`` is ``bw.gwr(approach = \"CV\")``, GWmodel's default.",
          "enum": [
            "AICc",
            "AIC",
            "BIC",
            "CV"
          ],
          "type": "string"
        },
        "fixed": {
          "default": false,
          "description": "fixed parameter (bool).",
          "type": "boolean"
        },
        "kernel": {
          "default": "bisquare",
          "description": "Kernel function used for weighting or smoothing.",
          "enum": [
            "bisquare",
            "gaussian",
            "exponential"
          ],
          "type": "string"
        },
        "tol": {
          "default": 0.0001,
          "description": "Stopping threshold of the golden-section search, applied (as in GWmodel, where it is hard-coded to ``1e-4``) both to the bracket step and to the difference of the two probed criterion values.",
          "type": "number"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "coords",
        "y",
        "X"
      ],
      "type": "object"
    }
  },
  {
    "description": "SAC / SARAR: combined spatial lag + spatial error model. Validation: certified parity evidence.",
    "name": "sac",
    "parameters": {
      "properties": {
        "W": {
          "description": "Covariates, proxy variables, or weights used by this estimator.",
          "type": "string"
        },
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "formula": {
          "description": "Model formula using patsy/R-style syntax.",
          "type": "string"
        },
        "row_normalize": {
          "default": true,
          "description": "row_normalize parameter (bool).",
          "type": "boolean"
        }
      },
      "required": [
        "W",
        "data",
        "formula"
      ],
      "type": "object"
    }
  },
  {
    "description": "Spatial Lag of X (SLX) model: ``Y = Xbeta + WXtheta + epsilon``. Validation: certified parity evidence.",
    "name": "slx",
    "parameters": {
      "properties": {
        "W": {
          "description": "Covariates, proxy variables, or weights used by this estimator.",
          "type": "string"
        },
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "formula": {
          "description": "Model formula using patsy/R-style syntax.",
          "type": "string"
        },
        "row_normalize": {
          "default": true,
          "description": "row_normalize parameter (bool).",
          "type": "boolean"
        }
      },
      "required": [
        "W",
        "data",
        "formula"
      ],
      "type": "object"
    }
  },
  {
    "description": "Kelejian-Prucha (1998) 2SLS for SAR with spatial-lag instruments. Validation: certified parity evidence.",
    "name": "sar_gmm",
    "parameters": {
      "properties": {
        "W": {
          "description": "Covariates, proxy variables, or weights used by this estimator.",
          "type": "string"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "formula": {
          "description": "Model formula using patsy/R-style syntax.",
          "type": "string"
        },
        "robust": {
          "description": "Robust standard-error or covariance estimator option.",
          "type": "string"
        },
        "row_normalize": {
          "default": true,
          "description": "row_normalize parameter (bool).",
          "type": "boolean"
        },
        "w_lags": {
          "default": 1,
          "description": "w_lags parameter (int).",
          "type": "integer"
        }
      },
      "required": [
        "W",
        "data",
        "formula"
      ],
      "type": "object"
    }
  },
  {
    "description": "SARAR (spatial lag + spatial error) by generalized spatial 2SLS. Validation: certified parity evidence.",
    "name": "sarar_gmm",
    "parameters": {
      "properties": {
        "W": {
          "description": "Spatial weights.",
          "type": "string"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "formula": {
          "description": "``\"y ~ x1 + x2\"``; a constant is always added.",
          "type": "string"
        },
        "robust": {
          "description": "``\"het\"``: HC0 sandwich in both 2SLS steps (``gstsls(robust = TRUE)``). The ``lambda`` moments are still the homoskedastic Kelejian-Prucha (1999) ones; this is *not* the Arraiz et al. (2010) heteroskedastic GM estimator of ``sphet::spreg(het = TRUE)``.",
          "type": "string"
        },
        "row_normalize": {
          "default": true,
          "description": "Row-standardise ``W`` (``spdep`` style ``\"W\"``).",
          "type": "boolean"
        },
        "sig2n_k": {
          "default": false,
          "description": "Divide the residual sum of squares by ``n - k`` instead of ``n`` in the homoskedastic variance (``gstsls(sig2n_k = TRUE)``).",
          "type": "boolean"
        },
        "w_lags": {
          "default": 2,
          "description": "Spatial lags of ``X`` used as instruments: ``[W X, ..., W^w_lags X]``. ``2`` is Kelejian-Prucha's ``H = [X, WX, W^2 X]`` and the only choice ``gstsls`` offers (its ``W2X`` argument is not used by the code); ``1`` is PySAL ``spreg.GM_Combo``'s default.",
          "type": "integer"
        }
      },
      "required": [
        "W",
        "data",
        "formula"
      ],
      "type": "object"
    }
  },
  {
    "description": "Kelejian-Prucha (1999) GMM for the spatial-error parameter lambda. Validation: certified parity evidence.",
    "name": "sem_gmm",
    "parameters": {
      "properties": {
        "W": {
          "description": "Covariates, proxy variables, or weights used by this estimator.",
          "type": "string"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "formula": {
          "description": "Model formula using patsy/R-style syntax.",
          "type": "string"
        },
        "robust": {
          "description": "When ``\"het\"``, the final beta covariance uses the heteroscedasticity- robust sandwich (Arraiz et al. 2010 / ``spreg.GM_Error_Het``).",
          "type": "string"
        },
        "row_normalize": {
          "default": true,
          "description": "row_normalize parameter (bool).",
          "type": "boolean"
        }
      },
      "required": [
        "W",
        "data",
        "formula"
      ],
      "type": "object"
    }
  },
  {
    "description": "Fit a spatial panel model by concentrated ML (Elhorst 2014). Validation: certified parity evidence.",
    "name": "spatial_panel",
    "parameters": {
      "properties": {
        "W": {
          "description": "N x N spatial weights matrix aligned with ``sorted(data[entity].unique())``.",
          "type": "string"
        },
        "data": {
          "description": "Long-format panel; must be balanced.",
          "type": "string"
        },
        "effects": {
          "default": "fe",
          "description": "``\"fe\"`` = entity FE only. ``\"twoways\"`` = entity + time demeaning.",
          "enum": [
            "fe",
            "twoways"
          ],
          "type": "string"
        },
        "entity": {
          "description": "Column names identifying entity and time dimensions.",
          "type": "string"
        },
        "formula": {
          "description": "``\"y ~ x1 + x2\"``. Constant is dropped (absorbed by the entity FE).",
          "type": "string"
        },
        "model": {
          "default": "sar",
          "description": "Model variant or parameterisation to fit.",
          "enum": [
            "sar",
            "sem",
            "sdm"
          ],
          "type": "string"
        },
        "row_normalize": {
          "default": true,
          "description": "Row-standardise ``W``.",
          "type": "boolean"
        },
        "time": {
          "description": "Column names identifying entity and time dimensions.",
          "type": "string"
        },
        "twoways_lag": {
          "default": "splm",
          "description": "Spatial lag regressor of SAR / SDM under ``effects=\"twoways\"`` (ignored otherwise: under entity effects the two coincide). ``\"splm\"``: ``W (Q y)``, the lag of the two-way demeaned ``y``, as ``splm::spml`` computes it (and as ``xsmle``'s log-likelihood evaluator does). ``\"within\"``: ``Q (W y)``, the two-way within transform of the lag, which is what the concentrated likelihood of the model with unit and time dummies (Lee & Yu 2010, eq. 21, the \"direct approach\") contains. For a row-standardised ``W`` the two differ by a per-period constant ``(1/N) 1' W Q y_t`` unless ``W`` is also column-stochastic, so ``\"splm\"``'s SSE carries an extra ``rho^2 N sum_t m_t^2`` term. Both are direct-approach estimators with an O(1/N) bias in ``rho`` that does not vanish as ``T`` grows (see ``docs/dev/campaign_phase3``). ``\"within\"`` supports ``vce=\"oim\"`` only.",
          "enum": [
            "splm",
            "within"
          ],
          "type": "string"
        },
        "vce": {
          "default": "information",
          "description": "``\"information\"``: inverse of the analytic information matrix of ``(sigma^2, rho, beta)`` evaluated at the estimates, as ``splm::spml(model = \"within\")`` reports. ``\"oim\"``: inverse of the observed (analytic) Hessian of the full log-likelihood -- the ``vce(oim)`` default of Stata's ``xsmle``. The two differ in the ``rho``-``rho`` term (expected ``tr(WA'WA)`` + ``beta'X'(WA)'WAX beta`` against observed ``(Wy)'(Wy)``) and so in every SE.",
          "enum": [
            "information",
            "oim"
          ],
          "type": "string"
        }
      },
      "required": [
        "data",
        "formula",
        "entity",
        "time",
        "W"
      ],
      "type": "object"
    }
  },
  {
    "description": "Boundary discontinuity design: pointwise effects along a 2-D boundary. Validation: certified parity evidence.",
    "name": "rd2d",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "``1 - alpha`` is R's ``level / 100``.",
          "type": "number"
        },
        "approach": {
          "default": "location",
          "description": "See the module docstring.",
          "enum": [
            "location",
            "distance",
            "pooled"
          ],
          "type": "string"
        },
        "boundary": {
          "description": "``f(x1) -> x2`` describing the boundary; used only to place default evaluation points (``n_eval`` of them) and by ``approach=\"pooled\"``. ``None`` means the line ``x1 = 0``.",
          "type": "string"
        },
        "bwcheck": {
          "default": "auto",
          "description": "Minimum number of observations (or mass points) inside every bandwidth; ``'auto'`` is R's default ``50 + p + 1``.",
          "type": "integer"
        },
        "bwparam": {
          "default": "main",
          "description": "Fuzzy designs: select the bandwidth for the ratio or the ITT.",
          "enum": [
            "main",
            "itt"
          ],
          "type": "string"
        },
        "bwselect": {
          "default": "mserd",
          "description": "``mserd``, ``cerrd``, ``imserd``, ``icerrd``, ``msetwo``, ``certwo``, ``imsetwo`` or ``icertwo``.",
          "type": "string"
        },
        "cluster": {
          "description": "Cluster identifier (vce must then be hc0 or hc1).",
          "type": "string"
        },
        "cqt": {
          "default": 0.5,
          "description": "Distance approach: quantile of the distance below which the pilot polynomial for the bias constant is fitted.",
          "type": "number"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "deriv": {
          "default": [
            0,
            0
          ],
          "description": "Location approach: partial derivative of the effect to estimate.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "distance": {
          "description": "Distance approach: signed distances (non-negative on the treated side) to each evaluation point. Default: Euclidean distance to ``eval_points`` signed by ``treatment``.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "eval_points": {
          "description": "Boundary points (R's ``b``). Default: ``n_eval`` points along ``boundary`` between the 10th and 90th percentiles of the data.",
          "type": "string"
        },
        "fitmethod": {
          "default": "joint",
          "description": "``joint`` applies the degrees-of-freedom correction to both sides together (R's default).",
          "enum": [
            "joint",
            "separate"
          ],
          "type": "string"
        },
        "fuzzy": {
          "description": "Treatment take-up for a fuzzy design; the effect is the ratio of the outcome and take-up jumps.",
          "type": "string"
        },
        "h": {
          "description": "Bandwidth. ``location``: scalar or ``(k, 4)`` array ``(h01, h02, h11, h12)``; ``distance``: scalar or ``(k, 2)`` array ``(h0, h1)``. ``None`` selects it (R ``rdbw2d`` / ``rdbw2d.distance``).",
          "type": "number"
        },
        "kernel": {
          "default": "triangular",
          "description": "Kernel function used for weighting or smoothing.",
          "enum": [
            "triangular",
            "epanechnikov",
            "uniform",
            "gaussian"
          ],
          "type": "string"
        },
        "kernel_type": {
          "default": "prod",
          "description": "Location approach: product or radial kernel.",
          "enum": [
            "prod",
            "rad"
          ],
          "type": "string"
        },
        "kink_position": {
          "description": "Distance approach: 0-based indices (or a boolean mask) of evaluation points that are known kinks of the boundary.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "kink_unknown": {
          "default": false,
          "description": "Distance approach: boundary may have kinks at unknown places; undersmooth the bandwidth (first) and the bias-correction bandwidth (second).",
          "type": "boolean"
        },
        "masspoints": {
          "default": "check",
          "description": "masspoints parameter (str).",
          "enum": [
            "check",
            "adjust",
            "off"
          ],
          "type": "string"
        },
        "method": {
          "default": "dpi",
          "description": "Location approach: pilot bandwidth rule for the bias constants.",
          "enum": [
            "dpi",
            "rot"
          ],
          "type": "string"
        },
        "n_eval": {
          "default": 1,
          "description": "Number of eval.",
          "type": "integer"
        },
        "p": {
          "default": 1,
          "description": "Polynomial order of the point estimator.",
          "type": "integer"
        },
        "q": {
          "description": "Order of the bias-correction fit; default ``p + 1`` (``p`` for the distance approach with ``kink_unknown``).",
          "type": "integer"
        },
        "scalebiascrct": {
          "default": 1.0,
          "description": "Location approach: weight of the higher-order bias correction in the bandwidth selector.",
          "type": "number"
        },
        "scaleregul": {
          "description": "Regularisation weight of the bandwidth selector; default is R's (3 for location, 1 for distance).",
          "type": "number"
        },
        "side": {
          "default": "two",
          "description": "Two-sided or one-sided intervals.",
          "enum": [
            "two",
            "left",
            "right"
          ],
          "type": "string"
        },
        "stdvars": {
          "default": true,
          "description": "Location approach: standardise x1, x2 before selecting bandwidths.",
          "type": "boolean"
        },
        "tangvec": {
          "description": "Location approach: tangential direction for a directional derivative (overrides ``deriv``).",
          "type": "string"
        },
        "treatment": {
          "description": "0/1 assignment (R's ``assignment``); defines the two sides of the boundary.",
          "type": "string"
        },
        "vce": {
          "default": "hc1",
          "description": "vce parameter (str).",
          "enum": [
            "hc1",
            "hc0",
            "hc2",
            "hc3"
          ],
          "type": "string"
        },
        "weights": {
          "description": "WBATE weights over the evaluation points (R ``summary(..., WBATE=)``); normalised to sum to one. Default: equal weights.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "x1": {
          "description": "The two running variables.",
          "type": "string"
        },
        "x2": {
          "description": "The two running variables.",
          "type": "string"
        },
        "y": {
          "description": "Outcome.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "x1",
        "x2",
        "treatment"
      ],
      "type": "object"
    }
  },
  {
    "description": "Bandwidth selection for boundary discontinuity designs. Validation: certified parity evidence.",
    "name": "rd2d_bw",
    "parameters": {
      "properties": {
        "approach": {
          "default": "location",
          "description": "Bivariate fit at boundary points, or a univariate fit on the signed distance. ``sp.rd2d``'s ``'pooled'`` has no bandwidth selector of its own -- call :func:`sp.rdbwselect` on the signed distance instead.",
          "enum": [
            "location",
            "distance"
          ],
          "type": "string"
        },
        "boundary": {
          "description": "``f(x1) -> x2`` tracing the boundary; ``None`` uses ``x1 = 0``.",
          "type": "string"
        },
        "bwcheck": {
          "default": "auto",
          "description": "bwcheck parameter (Union[int, str, None]).",
          "type": "integer"
        },
        "bwparam": {
          "default": "main",
          "description": "bwparam parameter (str).",
          "type": "string"
        },
        "bwselect": {
          "default": "mserd",
          "description": "bwselect parameter (str).",
          "type": "string"
        },
        "cluster": {
          "description": "Cluster identifier column for clustered standard errors.",
          "type": "string"
        },
        "cqt": {
          "default": 0.5,
          "description": "cqt parameter (float).",
          "type": "number"
        },
        "data": {
          "description": "Long frame holding the outcome, both running variables and the treatment indicator.",
          "type": "string"
        },
        "deriv": {
          "default": [
            0,
            0
          ],
          "description": "deriv parameter (Tuple[int, int]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "distance": {
          "description": "distance parameter (Optional[Union[np.ndarray, List[str]]]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "eval_points": {
          "description": "eval_points parameter (Optional[np.ndarray]).",
          "type": "string"
        },
        "fitmethod": {
          "default": "joint",
          "description": "fitmethod parameter (str).",
          "type": "string"
        },
        "fuzzy": {
          "description": "fuzzy parameter (Optional[str]).",
          "type": "string"
        },
        "kernel": {
          "default": "triangular",
          "description": "``'triangular'``, ``'uniform'`` or ``'epanechnikov'``.",
          "type": "string"
        },
        "kernel_type": {
          "default": "prod",
          "description": "kernel_type parameter (str).",
          "type": "string"
        },
        "kink_position": {
          "description": "kink_position parameter (Optional[Sequence]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "kink_unknown": {
          "default": false,
          "description": "kink_unknown parameter (Union[bool, Tuple[bool, bool]]).",
          "type": "boolean"
        },
        "masspoints": {
          "default": "check",
          "description": "masspoints parameter (str).",
          "type": "string"
        },
        "method": {
          "default": "dpi",
          "description": "Estimator or algorithm variant to use.",
          "type": "string"
        },
        "n_eval": {
          "default": 1,
          "description": "Number of eval.",
          "type": "integer"
        },
        "p": {
          "default": 1,
          "description": "Polynomial order of the point estimator.",
          "type": "integer"
        },
        "scalebiascrct": {
          "default": 1.0,
          "description": "scalebiascrct parameter (float).",
          "type": "number"
        },
        "scaleregul": {
          "default": 1.0,
          "description": "scaleregul parameter (float).",
          "type": "number"
        },
        "stdvars": {
          "default": true,
          "description": "stdvars parameter (bool).",
          "type": "boolean"
        },
        "tangvec": {
          "description": "tangvec parameter (Optional[np.ndarray]).",
          "type": "string"
        },
        "treatment": {
          "description": "Column names, as in :func:`sp.rd2d`.",
          "type": "string"
        },
        "vce": {
          "default": "hc1",
          "description": "vce parameter (str).",
          "type": "string"
        },
        "x1": {
          "description": "Column names, as in :func:`sp.rd2d`.",
          "type": "string"
        },
        "x2": {
          "description": "Column names, as in :func:`sp.rd2d`.",
          "type": "string"
        },
        "y": {
          "description": "Column names, as in :func:`sp.rd2d`.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "x1",
        "x2",
        "treatment"
      ],
      "type": "object"
    }
  },
  {
    "description": "2D boundary RD visualization.",
    "name": "rd2d_plot",
    "parameters": {
      "properties": {
        "ax": {
          "description": "ax parameter (Optional[Any]).",
          "type": "string"
        },
        "boundary": {
          "description": "Boundary function ``f(x1) -> x2``; ``None`` draws the line ``x1 = 0``.",
          "type": "string"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "figsize": {
          "default": [
            10,
            8
          ],
          "description": "figsize parameter (Tuple[float, float]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "plot_type": {
          "default": "scatter",
          "description": "plot_type parameter (str).",
          "enum": [
            "scatter",
            "heatmap",
            "boundary_effects"
          ],
          "type": "string"
        },
        "result": {
          "description": "Result of :func:`sp.rd2d`; its evaluation points are marked on the scatter, and ``plot_type='boundary_effects'`` plots its pointwise bias-corrected effects with robust intervals.",
          "type": "string"
        },
        "treatment": {
          "description": "Binary assignment indicator.",
          "type": "string"
        },
        "x1": {
          "description": "Running variable names.",
          "type": "string"
        },
        "x2": {
          "description": "Running variable names.",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable name.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "x1",
        "x2",
        "treatment"
      ],
      "type": "object"
    }
  },
  {
    "description": "Power of an RD design given sample size and effect size. Validation: certified parity evidence.",
    "name": "rdpower",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level.",
          "type": "number"
        },
        "c": {
          "default": 0.0,
          "description": "Cutoff, for data mode.",
          "type": "number"
        },
        "data": {
          "description": "Enables data mode. Requires ``y`` and ``x``.",
          "type": "string"
        },
        "h_left": {
          "default": 0.5,
          "description": "Bandwidth fractions (proportion of running-variable support used). Design mode only.",
          "type": "number"
        },
        "h_right": {
          "default": 0.5,
          "description": "Bandwidth fractions (proportion of running-variable support used). Design mode only.",
          "type": "number"
        },
        "n_left": {
          "default": 500,
          "description": "Available sample size on each side. Design mode only.",
          "type": "integer"
        },
        "n_right": {
          "default": 500,
          "description": "Available sample size on each side. Design mode only.",
          "type": "integer"
        },
        "target_power": {
          "description": "If set, compute MDE for this target power instead.",
          "type": "number"
        },
        "tau": {
          "description": "Hypothesised treatment effect at the cutoff.",
          "type": "number"
        },
        "var_left": {
          "default": 1.0,
          "description": "Outcome variance on each side of the cutoff. Design mode only.",
          "type": "number"
        },
        "var_right": {
          "default": 1.0,
          "description": "Outcome variance on each side of the cutoff. Design mode only.",
          "type": "number"
        },
        "x": {
          "description": "Outcome and running-variable column names, for data mode.",
          "type": "string"
        },
        "y": {
          "description": "Outcome and running-variable column names, for data mode.",
          "type": "string"
        }
      },
      "required": [
        "tau"
      ],
      "type": "object"
    }
  },
  {
    "description": "Minimum sample size for a given power in an RD design. Validation: certified parity evidence.",
    "name": "rdsampsi",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "c": {
          "default": 0.0,
          "description": "c parameter (float).",
          "type": "number"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "h_left": {
          "default": 0.5,
          "description": "h_left parameter (float).",
          "type": "number"
        },
        "h_right": {
          "default": 0.5,
          "description": "h_right parameter (float).",
          "type": "number"
        },
        "ratio": {
          "default": 1.0,
          "description": "``n_right / n_left``. Default 1.0 assumes equal allocation.",
          "type": "number"
        },
        "target_power": {
          "default": 0.8,
          "description": "target_power parameter (float).",
          "type": "number"
        },
        "tau": {
          "description": "Quantile level or target treatment-effect index.",
          "type": "number"
        },
        "var_left": {
          "default": 1.0,
          "description": "var_left parameter (float).",
          "type": "number"
        },
        "var_right": {
          "default": 1.0,
          "description": "var_right parameter (float).",
          "type": "number"
        },
        "x": {
          "description": "Primary running variable, regressor, or feature input for this estimator.",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "tau"
      ],
      "type": "object"
    }
  },
  {
    "description": "Generate 2D boundary RD data.",
    "name": "dgp_rd_2d",
    "parameters": {
      "properties": {
        "effect": {
          "default": 2.0,
          "description": "Treatment effect at boundary.",
          "type": "number"
        },
        "n": {
          "default": 2000,
          "description": "Sample size.",
          "type": "integer"
        },
        "seed": {
          "description": "Random seed.",
          "type": "integer"
        }
      },
      "required": [],
      "type": "object"
    }
  },
  {
    "description": "Generate RD data with heterogeneous treatment effects.",
    "name": "dgp_rd_hte",
    "parameters": {
      "properties": {
        "ate": {
          "default": 2.0,
          "description": "Average treatment effect at cutoff.",
          "type": "number"
        },
        "cutoff": {
          "default": 0.0,
          "description": "RD cutoff.",
          "type": "number"
        },
        "hte_coef": {
          "default": 1.5,
          "description": "Slope of heterogeneity w.r.t. covariate z.",
          "type": "number"
        },
        "n": {
          "default": 3000,
          "description": "Sample size.",
          "type": "integer"
        },
        "seed": {
          "description": "Random seed.",
          "type": "integer"
        }
      },
      "required": [],
      "type": "object"
    }
  },
  {
    "description": "Generate Regression Kink Design data (Card et al. 2015).",
    "name": "dgp_rd_kink",
    "parameters": {
      "properties": {
        "cutoff": {
          "default": 0.0,
          "description": "Kink point location.",
          "type": "number"
        },
        "kink": {
          "default": 0.8,
          "description": "True kink in slope at cutoff.",
          "type": "number"
        },
        "n": {
          "default": 2000,
          "description": "Sample size.",
          "type": "integer"
        },
        "seed": {
          "description": "Random seed.",
          "type": "integer"
        }
      },
      "required": [],
      "type": "object"
    }
  },
  {
    "description": "Generate multi-cutoff RD data.",
    "name": "dgp_rd_multi",
    "parameters": {
      "properties": {
        "cutoffs": {
          "description": "Cutoff values. Default [0.0, 1.0].",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "effects": {
          "description": "Treatment effects at each cutoff. Default [2.0, 3.0].",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "n": {
          "default": 3000,
          "description": "Sample size.",
          "type": "integer"
        },
        "seed": {
          "description": "Random seed.",
          "type": "integer"
        }
      },
      "required": [],
      "type": "object"
    }
  },
  {
    "description": "Generate Regression Discontinuity in Time data (Hausman-Rapson 2018).",
    "name": "dgp_rdit",
    "parameters": {
      "properties": {
        "cutoff_period": {
          "default": 100,
          "description": "Period of the policy change.",
          "type": "integer"
        },
        "effect": {
          "default": 2.0,
          "description": "Treatment effect at the policy change date.",
          "type": "number"
        },
        "n_periods": {
          "default": 200,
          "description": "Number of time periods.",
          "type": "integer"
        },
        "seasonality": {
          "default": true,
          "description": "Include monthly seasonality.",
          "type": "boolean"
        },
        "seed": {
          "description": "Random seed.",
          "type": "integer"
        }
      },
      "required": [],
      "type": "object"
    }
  },
  {
    "description": "Unified entry point for all decomposition methods. Validation: certified parity evidence.",
    "name": "decompose",
    "parameters": {
      "properties": {
        "method": {
          "description": "One of the methods listed in ``available_methods()``. Aliases are supported (e.g. 'mm' -> 'machado_mata').",
          "type": "string"
        }
      },
      "required": [
        "method"
      ],
      "type": "object"
    }
  },
  {
    "description": "DFL (1996) reweighting decomposition at a chosen distributional statistic. Validation: certified parity evidence.",
    "name": "dfl_decompose",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "group": {
          "description": "Group or cohort identifier.",
          "type": "string"
        },
        "inference": {
          "default": "analytical",
          "description": "inference parameter (str).",
          "enum": [
            "bootstrap",
            "analytical"
          ],
          "type": "string"
        },
        "n_boot": {
          "default": 299,
          "description": "Number of bootstrap replications.",
          "type": "integer"
        },
        "quantile_grid": {
          "description": "If provided, also compute quantile-process decomposition on this grid.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "reference": {
          "default": 0,
          "description": "- 0: reweight Group B to look like A's X (default). The counterfactual is F_{Y<1|0>} -- A's X distribution with B's outcome structure. - 1: reweight Group A to look like B's X. The counterfactual is F_{Y<0|1>} -- B's X distribution with A's outcome structure. .. warning:: ``reference`` has different economic semantics across method families. In DFL, ``reference=0`` yields cf = *A's X, B's beta* (reweighting approach). In ``machado_mata`` / ``melly`` / ``cfm``, ``reference=0`` yields cf = *A's beta, B's X* (coefficient-substitution approach). These are **opposite** counterfactual constructions. Within each method labels are internally consistent (DFL structure = A - cf; MM composition = A - cf). When comparing estimates across methods, read the per-method docstrings carefully.",
          "enum": [
            "0",
            "1"
          ],
          "type": "integer"
        },
        "seed": {
          "default": 12345,
          "description": "Random seed for reproducible stochastic steps.",
          "type": "integer"
        },
        "stat": {
          "default": "mean",
          "description": "stat parameter (str).",
          "enum": [
            "mean",
            "variance",
            "std",
            "quantile",
            "iqr",
            "gini",
            "log_var"
          ],
          "type": "string"
        },
        "stat_convention": {
          "default": "statspai",
          "description": "Weighted variance / quantile definition for the reweighted counterfactual (see ``_common.statistic_value``). ``'hmisc'`` reproduces ``ddecompose::dfl_decompose``, which uses ``Hmisc::wtd.var`` and ``Hmisc::wtd.quantile``; the reweighting itself is identical under both. ``stat='gini'`` is the exact plug-in Gini either way; ``ddecompose`` integrates the Lorenz curve numerically and differs from it in the fourth significant digit.",
          "enum": [
            "statspai",
            "hmisc"
          ],
          "type": "string"
        },
        "tau": {
          "default": 0.5,
          "description": "Quantile level or target treatment-effect index.",
          "type": "number"
        },
        "trim": {
          "default": 0.001,
          "description": "trim parameter (float).",
          "type": "number"
        },
        "weights": {
          "description": "Observation weights.",
          "type": "string"
        },
        "x": {
          "description": "Primary running variable, regressor, or feature input for this estimator.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "group",
        "x"
      ],
      "type": "object"
    }
  },
  {
    "description": "Machado-Mata (2005) quantile decomposition. Validation: validated evidence tier (known-truth, reference, external-parity, or Monte Carlo artifact).",
    "name": "machado_mata",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "group": {
          "description": "Group or cohort identifier.",
          "type": "string"
        },
        "inference": {
          "default": "none",
          "description": "inference parameter (str).",
          "type": "string"
        },
        "n_boot": {
          "default": 199,
          "description": "Number of bootstrap replications.",
          "type": "integer"
        },
        "n_sim": {
          "default": 500,
          "description": "Number of sim.",
          "type": "integer"
        },
        "n_tau_qr": {
          "default": 100,
          "description": "Number ``J`` of quantile regressions, fitted at the midpoints ``(j - 0.5)/J``; each simulated draw picks one of them uniformly, so the draws sample the uniform on (0, 1) at resolution ``1/J`` (Machado & Mata draw ``u ~ U(0, 1)`` directly; with this grid, as ``n_sim -> inf`` the decomposition converges to :func:`melly_decompose` on the same ``J``). Changed in 1.29.0 from 99 regressions on ``linspace(0.01, 0.99, 99)``.",
          "type": "integer"
        },
        "reference": {
          "default": 0,
          "description": "0: use Group A's coefficients with Group B's X. The counterfactual is F_{Y<0|1>} -- A's beta on B's X. 1: use Group B's coefficients with Group A's X. .. warning:: Opposite convention to ``dfl_decompose``. In DFL, ``reference=0`` means *A's X, B's beta* (reweighting). Here, ``reference=0`` means *A's beta, B's X* (coefficient swap). See ``dfl_decompose`` docstring for the full convention map.",
          "enum": [
            "0",
            "1"
          ],
          "type": "integer"
        },
        "seed": {
          "default": 12345,
          "description": "Random seed for reproducible stochastic steps.",
          "type": "integer"
        },
        "tau_grid": {
          "description": "tau grid for reporting (default: deciles 0.1..0.9)",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "x": {
          "description": "Primary running variable, regressor, or feature input for this estimator.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "group",
        "x"
      ],
      "type": "object"
    }
  },
  {
    "description": "Melly (2005) quantile decomposition. Validation: certified parity evidence.",
    "name": "melly_decompose",
    "parameters": {
      "properties": {
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "group": {
          "description": "Group or cohort identifier.",
          "type": "string"
        },
        "n_tau_qr": {
          "default": 100,
          "description": "Number ``J`` of quantile regressions in the process, fitted at the midpoints ``(j - 0.5)/J`` (the grid of Stata ``cdeco``). Changed in 1.29.0 from 99 regressions on ``linspace(0.01, 0.99, 99)``, which dropped the outer 0.5% of each conditional tail.",
          "type": "integer"
        },
        "reference": {
          "default": 0,
          "description": "Same convention as ``machado_mata``: ``reference=0`` uses A's beta on B's X (coefficient-swap counterfactual F_{Y<0|1>}), opposite to ``dfl_decompose`` whose ``reference=0`` uses A's X with B's beta.",
          "enum": [
            "0",
            "1"
          ],
          "type": "integer"
        },
        "tau_grid": {
          "description": "Grid of tau values to evaluate.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "x": {
          "description": "Primary running variable, regressor, or feature input for this estimator.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "group",
        "x"
      ],
      "type": "object"
    }
  },
  {
    "description": "RIF regression (Firpo, Fortin & Lemieux 2009). Validation: certified parity evidence.",
    "name": "rifreg",
    "parameters": {
      "properties": {
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "formula": {
          "description": "``\"y ~ x1 + x2\"`` style.",
          "type": "string"
        },
        "quantile_convention": {
          "default": "statspai",
          "description": "Quantile RIF convention for ``statistic=\"quantile\"``.",
          "enum": [
            "statspai",
            "dineq",
            "rifreg"
          ],
          "type": "string"
        },
        "statistic": {
          "default": "quantile",
          "description": "statistic parameter (StatisticKind).",
          "enum": [
            "quantile",
            "variance",
            "gini"
          ],
          "type": "string"
        },
        "tau": {
          "default": 0.5,
          "description": "Quantile level (default 0.5 = median UQPE).",
          "type": "number"
        }
      },
      "required": [
        "formula",
        "data"
      ],
      "type": "object"
    }
  },
  {
    "description": "RIF Oaxaca-Blinder decomposition (FFL 2009, Section 5). Validation: certified parity evidence.",
    "name": "rif_decomposition",
    "parameters": {
      "properties": {
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "formula": {
          "description": "Model formula using patsy/R-style syntax.",
          "type": "string"
        },
        "group": {
          "description": "Binary (0/1) group indicator column.",
          "type": "string"
        },
        "quantile_convention": {
          "default": "statspai",
          "description": "Quantile RIF convention for ``statistic=\"quantile\"``. Use ``\"dineq\"`` for R ``dineq::rif`` parity.",
          "enum": [
            "statspai",
            "dineq",
            "rifreg"
          ],
          "type": "string"
        },
        "reference": {
          "default": 0,
          "description": "Which group's coefficients to use as the reference (0 or 1).",
          "type": "integer"
        },
        "statistic": {
          "default": "quantile",
          "description": "statistic parameter (StatisticKind).",
          "type": "string"
        },
        "tau": {
          "default": 0.5,
          "description": "Quantile level or target treatment-effect index.",
          "type": "number"
        }
      },
      "required": [
        "formula",
        "data",
        "group"
      ],
      "type": "object"
    }
  },
  {
    "description": "Firpo-Fortin-Lemieux two-step detailed distributional decomposition. Validation: certified parity evidence.",
    "name": "ffl_decompose",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "group": {
          "description": "Group or cohort identifier.",
          "enum": [
            "0",
            "1"
          ],
          "type": "string"
        },
        "inference": {
          "default": "analytical",
          "description": "inference parameter (str).",
          "enum": [
            "analytical",
            "bootstrap"
          ],
          "type": "string"
        },
        "n_boot": {
          "default": 299,
          "description": "Number of bootstrap replications.",
          "type": "integer"
        },
        "quantile_convention": {
          "default": "statspai",
          "description": "Quantile RIF convention (``stat='quantile'`` / ``'iqr'``). ``'rifreg'`` reproduces ``ddecompose`` / ``rifreg``.",
          "enum": [
            "statspai",
            "dineq",
            "rifreg"
          ],
          "type": "string"
        },
        "reference": {
          "default": 0,
          "description": "0: B reweighted to look like A's X (composition = effect of A's X on B's outcomes relative to observed B)",
          "enum": [
            "0",
            "1"
          ],
          "type": "integer"
        },
        "seed": {
          "default": 12345,
          "description": "Random seed for reproducible stochastic steps.",
          "type": "integer"
        },
        "stat": {
          "default": "quantile",
          "description": "'log_var', 'theil_t', 'theil_l', 'atkinson'}",
          "type": "string"
        },
        "tau": {
          "default": 0.5,
          "description": "Quantile level or target treatment-effect index.",
          "type": "number"
        },
        "trim": {
          "default": 0.001,
          "description": "trim parameter (float).",
          "type": "number"
        },
        "weights": {
          "description": "Observation weights.",
          "type": "string"
        },
        "x": {
          "description": "Primary running variable, regressor, or feature input for this estimator.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "group",
        "x"
      ],
      "type": "object"
    }
  },
  {
    "description": "Fairlie (2005) nonlinear decomposition for binary outcomes. Validation: certified parity evidence.",
    "name": "fairlie",
    "parameters": {
      "properties": {
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "group": {
          "description": "Group or cohort identifier.",
          "type": "string"
        },
        "model": {
          "default": "logit",
          "description": "Model variant or parameterisation to fit.",
          "enum": [
            "logit",
            "probit"
          ],
          "type": "string"
        },
        "n_sim": {
          "default": 500,
          "description": "Number of sim.",
          "type": "integer"
        },
        "reference": {
          "default": 0,
          "description": "``reference()``). The switching always runs from A's covariates to B's, so the contributions share the sign of the explained gap.",
          "enum": [
            "0",
            "1"
          ],
          "type": "integer"
        },
        "seed": {
          "default": 12345,
          "description": "Random seed for reproducible stochastic steps.",
          "type": "integer"
        },
        "x": {
          "description": "Primary running variable, regressor, or feature input for this estimator.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "enum": [
            "0",
            "1"
          ],
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "group",
        "x"
      ],
      "type": "object"
    }
  },
  {
    "description": "Bauer-Sinning (2008) nonlinear Oaxaca-Blinder decomposition with",
    "name": "yun_nonlinear",
    "parameters": {
      "properties": {
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "group": {
          "description": "Group or cohort identifier.",
          "type": "string"
        },
        "model": {
          "default": "logit",
          "description": "Model variant or parameterisation to fit.",
          "enum": [
            "logit",
            "probit"
          ],
          "type": "string"
        },
        "reference": {
          "default": 0,
          "description": "reference parameter (int).",
          "enum": [
            "0",
            "1"
          ],
          "type": "integer"
        },
        "variant": {
          "default": "yun",
          "description": "variant parameter (str).",
          "type": "string"
        },
        "x": {
          "description": "Primary running variable, regressor, or feature input for this estimator.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "group",
        "x"
      ],
      "type": "object"
    }
  },
  {
    "description": "Chernozhukov-Fernandez-Val-Melly (2013) counterfactual decomposition. Validation: certified parity evidence.",
    "name": "cfm_decompose",
    "parameters": {
      "properties": {
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "group": {
          "description": "Group or cohort identifier.",
          "type": "string"
        },
        "inversion": {
          "default": "interpolate",
          "description": "How the estimated CDF, known only at the thresholds, is inverted. ``'interpolate'`` interpolates linearly between thresholds (a smoothed quantile). ``'step'`` returns the first threshold at which the CDF exceeds ``tau`` -- the step-function inverse of Chernozhukov, Fernandez-Val & Melly's Stata ``cdeco``; with the same thresholds it reproduces ``cdeco, method(logit)``.",
          "enum": [
            "interpolate",
            "step"
          ],
          "type": "string"
        },
        "ks_test": {
          "default": true,
          "description": "ks_test parameter (bool).",
          "type": "boolean"
        },
        "n_thresh": {
          "default": 40,
          "description": "(ignored when ``thresholds`` is given).",
          "type": "integer"
        },
        "reference": {
          "default": 0,
          "description": "Same convention as ``machado_mata`` / ``melly_decompose``: ``reference=0`` builds the counterfactual from A's distribution regression coefficients applied to B's X (F_{Y<0|1>}), opposite to the reweighting convention in ``dfl_decompose``.",
          "enum": [
            "0",
            "1"
          ],
          "type": "integer"
        },
        "tau_grid": {
          "description": "Grid of tau values to evaluate.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "thresholds": {
          "description": "Explicit distribution-regression thresholds, shared by both groups.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "x": {
          "description": "Primary running variable, regressor, or feature input for this estimator.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "group",
        "x"
      ],
      "type": "object"
    }
  },
  {
    "description": "Bauer-Sinning (2008) nonlinear Oaxaca-Blinder decomposition with Validation: certified parity evidence.",
    "name": "bauer_sinning",
    "parameters": {
      "properties": {
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "group": {
          "description": "Group or cohort identifier.",
          "type": "string"
        },
        "model": {
          "default": "logit",
          "description": "Model variant or parameterisation to fit.",
          "enum": [
            "logit",
            "probit"
          ],
          "type": "string"
        },
        "reference": {
          "default": 0,
          "description": "reference parameter (int).",
          "enum": [
            "0",
            "1"
          ],
          "type": "integer"
        },
        "variant": {
          "default": "yun",
          "description": "variant parameter (str).",
          "type": "string"
        },
        "x": {
          "description": "Primary running variable, regressor, or feature input for this estimator.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "group",
        "x"
      ],
      "type": "object"
    }
  },
  {
    "description": "Das Gupta (1993) multi-factor decomposition. Validation: certified parity evidence.",
    "name": "das_gupta",
    "parameters": {
      "properties": {
        "by": {
          "description": "Column(s) identifying the stratum of each row, used to pair the rows of ``data_a`` and ``data_b``. Both frames must contain the same strata. Without ``by`` the rows are paired by position, which requires equal lengths.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "data_a": {
          "description": "One row per stratum (e.g. age group). The aggregate for each population is ``R = sum_i prod_f factor_{f,i}``; with a single row it is simply ``prod_f factor_f``.",
          "type": "string"
        },
        "data_b": {
          "description": "One row per stratum (e.g. age group). The aggregate for each population is ``R = sum_i prod_f factor_{f,i}``; with a single row it is simply ``prod_f factor_f``.",
          "type": "string"
        },
        "factor_names": {
          "description": "factor_names parameter (Sequence[str]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "required": [
        "data_a",
        "data_b",
        "factor_names"
      ],
      "type": "object"
    }
  },
  {
    "description": "Counterfactual gap after equalising covariate distributions. Validation: certified parity evidence.",
    "name": "gap_closing",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "group": {
          "description": "Group or cohort identifier.",
          "type": "string"
        },
        "inference": {
          "default": "analytical",
          "description": "inference parameter (str).",
          "enum": [
            "analytical",
            "bootstrap"
          ],
          "type": "string"
        },
        "method": {
          "default": "aipw",
          "description": "AIPW is doubly robust (recommended).",
          "enum": [
            "regression",
            "ipw",
            "aipw"
          ],
          "type": "string"
        },
        "n_boot": {
          "default": 299,
          "description": "Number of bootstrap replications.",
          "type": "integer"
        },
        "seed": {
          "default": 12345,
          "description": "Random seed for reproducible stochastic steps.",
          "type": "integer"
        },
        "target_dist": {
          "default": 1,
          "description": "- 1: shift Group A's covariate distribution to match Group B's - 0: shift Group B's to match Group A's",
          "enum": [
            "0",
            "1"
          ],
          "type": "integer"
        },
        "trim": {
          "default": 0.001,
          "description": "trim parameter (float).",
          "type": "number"
        },
        "x": {
          "description": "Primary running variable, regressor, or feature input for this estimator.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "group",
        "x"
      ],
      "type": "object"
    }
  },
  {
    "description": "Kitagawa (1955) two-factor rate decomposition. Validation: certified parity evidence.",
    "name": "kitagawa_decompose",
    "parameters": {
      "properties": {
        "by": {
          "description": "Category variable(s) defining cells.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "data": {
          "description": "Tidy data. Either individual-level (aggregated internally) or pre-aggregated cell-level with columns: `group`, `by`, `rate`, optional `weights` (population size in each cell).",
          "type": "string"
        },
        "group": {
          "description": "Binary group indicator.",
          "type": "string"
        },
        "normalize": {
          "default": "symmetric",
          "description": "- 'a': rate effect evaluated at A's composition - 'b': rate effect evaluated at B's composition - 'symmetric': average (default)",
          "enum": [
            "symmetric",
            "a",
            "b"
          ],
          "type": "string"
        },
        "rate": {
          "description": "Column holding the category-specific rate (or 0/1 outcome at the individual level).",
          "type": "string"
        },
        "weights": {
          "description": "Cell population weights. If None, each row treated as individual-level data (weight = 1).",
          "type": "string"
        }
      },
      "required": [
        "data",
        "rate",
        "group",
        "by"
      ],
      "type": "object"
    }
  },
  {
    "description": "Lerman-Yitzhaki (1985) Gini source decomposition. Validation: certified parity evidence.",
    "name": "source_decompose",
    "parameters": {
      "properties": {
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "gini": {
          "default": "corrected",
          "description": "``\"corrected\"`` uses the ``n/(n-1)`` bias-corrected Gini, the default of :func:`inequality_index`. ``\"population\"`` uses the plug-in Gini, as Stata's ``descogini`` and Lerman & Yitzhaki's covariance formula do; shares ``S_k``, Gini correlations ``R_k`` and each source's percentage of the total are identical under both, since the factor cancels.",
          "enum": [
            "corrected",
            "population"
          ],
          "type": "string"
        },
        "sources": {
          "description": "Income-source columns; total income is their row sum.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "weights": {
          "description": "Observation weights.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "sources"
      ],
      "type": "object"
    }
  },
  {
    "description": "Subgroup decomposition (between / within) of an inequality index. Validation: certified parity evidence.",
    "name": "subgroup_decompose",
    "parameters": {
      "properties": {
        "alpha": {
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "by": {
          "description": "by parameter (str).",
          "type": "string"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "eps": {
          "default": 1.0,
          "description": "eps parameter (float).",
          "type": "number"
        },
        "index": {
          "default": "theil_t",
          "description": "index parameter (str).",
          "type": "string"
        },
        "weights": {
          "description": "Observation weights.",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "by"
      ],
      "type": "object"
    }
  },
  {
    "description": "Jackson & VanderWeele (2018) causal disparity decomposition.",
    "name": "disparity_decompose",
    "parameters": {
      "properties": {
        "covariates": {
          "description": "Covariate matrix, DataFrame, or column names.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "group": {
          "description": "Group or cohort identifier.",
          "type": "string"
        },
        "mediator": {
          "description": "mediator parameter (str).",
          "type": "string"
        },
        "target_level": {
          "description": "Value at which to fix mediator for the \"initial\" counterfactual.",
          "type": "number"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "group",
        "mediator"
      ],
      "type": "object"
    }
  },
  {
    "description": "Synthetic disparity panel with treatment, mediator, outcome.",
    "name": "disparity_panel",
    "parameters": {
      "properties": {
        "n": {
          "default": 3000,
          "description": "n parameter (int).",
          "type": "integer"
        },
        "seed": {
          "default": 42,
          "description": "Random seed for reproducible stochastic steps.",
          "type": "integer"
        }
      },
      "required": [],
      "type": "object"
    }
  },
  {
    "description": "Linear nested-models mediation decomposition (VanderWeele 2014 Validation: certified parity evidence.",
    "name": "mediation_decompose",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "covariates": {
          "description": "Covariate matrix, DataFrame, or column names.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "inference": {
          "default": "analytical",
          "description": "``'none'``: point estimates only. ``'analytical'`` (default): delta-method standard errors from the two regressions' classical OLS covariances (``s^2 (X'X)^{-1}``, ``s^2`` on ``n - p``), block-diagonal across the two models -- Stata ``paramed`` / R ``CMAverse`` ``inference = \"delta\"``. Up to 1.28.0 this option computed nothing and ``se`` was ``None``.",
          "enum": [
            "analytical",
            "bootstrap"
          ],
          "type": "string"
        },
        "mediator": {
          "description": "mediator parameter (str).",
          "type": "string"
        },
        "n_boot": {
          "default": 299,
          "description": "Number of bootstrap replications.",
          "type": "integer"
        },
        "seed": {
          "default": 12345,
          "description": "Random seed for reproducible stochastic steps.",
          "type": "integer"
        },
        "treatment": {
          "description": "Treatment indicator, treatment variable, or treatment array.",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "treatment",
        "mediator"
      ],
      "type": "object"
    }
  },
  {
    "description": "Nonparametric causal decomposition of a group disparity. Validation: certified parity evidence.",
    "name": "yu_elwert_decompose",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Two-sided coverage level.",
          "type": "number"
        },
        "cluster": {
          "description": "Column name to use for cluster bootstrap.",
          "type": "string"
        },
        "data": {
          "description": "Long-format panel with one row per observation.",
          "type": "string"
        },
        "group": {
          "description": "Binary group indicator (0/1) -- ``1`` = advantaged / index group.",
          "type": "string"
        },
        "inference": {
          "default": "bootstrap",
          "description": "``\"bootstrap\"`` returns SEs and percentile CIs from the non-parametric (cluster-aware) bootstrap. ``\"analytic\"`` (only with ``method=\"efficient\"``) returns the efficient-influence-function SEs and Wald CIs of ``cdgd``. ``\"none\"`` skips inference.",
          "enum": [
            "bootstrap",
            "analytic"
          ],
          "type": "string"
        },
        "method": {
          "default": "plugin",
          "description": "``\"plugin\"`` uses within-cell OLS for outcomes and within-group logit for the propensity and computes plug-in expectations. ``\"efficient\"`` is the efficient-influence-function estimator of the authors' ``cdgd`` package (``cdgd0_manual``) on those same nuisance fits: Hajek-normalised doubly robust potential outcomes, with selection the residual so the components add up exactly -- recommended when nuisance functions might be misspecified.",
          "enum": [
            "plugin",
            "efficient"
          ],
          "type": "string"
        },
        "n_boot": {
          "default": 499,
          "description": "Number of bootstrap replications.",
          "type": "integer"
        },
        "seed": {
          "default": 12345,
          "description": "Random seed for reproducible stochastic steps.",
          "type": "integer"
        },
        "treatment": {
          "description": "Binary treatment indicator (0/1).",
          "type": "string"
        },
        "trim": {
          "default": 0.005,
          "description": "Lower/upper clip for fitted propensities (only used in ``method=\"efficient\"``).",
          "type": "number"
        },
        "x": {
          "description": "Adjustment covariates (used to identify within-group CATEs).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Name of the (continuous) outcome column.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "treatment",
        "group",
        "x"
      ],
      "type": "object"
    }
  },
  {
    "description": "Yu-Elwert (2025) causal decomposition of a group disparity.",
    "name": "YuElwertResult",
    "parameters": {
      "properties": {
        "baseline": {
          "default": 0.0,
          "description": "baseline parameter (float).",
          "type": "number"
        },
        "ci": {
          "description": "ci parameter (Optional[Dict[str, Tuple[float, float]]]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "detailed": {
          "default": null,
          "description": "detailed parameter (pd.DataFrame).",
          "type": "string"
        },
        "disparity": {
          "default": 0.0,
          "description": "disparity parameter (float).",
          "type": "number"
        },
        "effect": {
          "default": 0.0,
          "description": "effect parameter (float).",
          "type": "number"
        },
        "method": {
          "default": "plugin",
          "description": "Estimator or algorithm variant to use.",
          "type": "string"
        },
        "nuisance": {
          "default": null,
          "description": "nuisance parameter (Dict[str, Any]).",
          "type": "object"
        },
        "prevalence": {
          "default": 0.0,
          "description": "prevalence parameter (float).",
          "type": "number"
        },
        "se": {
          "description": "se parameter (Optional[Dict[str, float]]).",
          "type": "object"
        },
        "selection": {
          "default": 0.0,
          "description": "selection parameter (float).",
          "type": "number"
        }
      },
      "required": [],
      "type": "object"
    }
  },
  {
    "description": "Compute a single inequality index. Validation: certified parity evidence.",
    "name": "inequality_index",
    "parameters": {
      "properties": {
        "alpha": {
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "eps": {
          "default": 1.0,
          "description": "eps parameter (float).",
          "type": "number"
        },
        "index": {
          "default": "theil_t",
          "description": "atkinson, gini, cv2",
          "type": "string"
        },
        "weights": {
          "description": "Observation weights.",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "y"
      ],
      "type": "object"
    }
  },
  {
    "description": "Shorrocks-Shapley decomposition of an inequality index across Validation: certified parity evidence.",
    "name": "shapley_inequality",
    "parameters": {
      "properties": {
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "index": {
          "default": "theil_t",
          "description": "index parameter (str).",
          "type": "string"
        },
        "weights": {
          "description": "Observation weights.",
          "type": "string"
        },
        "x": {
          "description": "Primary running variable, regressor, or feature input for this estimator.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "x"
      ],
      "type": "object"
    }
  },
  {
    "description": "Plot an ``aggte()`` result, mirroring R :func:`did::ggdid`.",
    "name": "ggdid",
    "parameters": {
      "properties": {
        "ax": {
          "description": "ax parameter.",
          "type": "string"
        },
        "band_color": {
          "default": "#F18F01",
          "description": "Colours for the pointwise estimate and the uniform band.",
          "type": "string"
        },
        "figsize": {
          "default": [
            10,
            6
          ],
          "description": "figsize parameter (Tuple[float, float]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "point_color": {
          "default": "#2E86AB",
          "description": "Colours for the pointwise estimate and the uniform band.",
          "type": "string"
        },
        "result": {
          "description": "Output of :func:`aggte`.",
          "type": "string"
        },
        "show_pointwise": {
          "default": true,
          "description": "Draw pointwise CI lines.",
          "type": "boolean"
        },
        "show_uniform": {
          "default": true,
          "description": "Draw uniform band (shaded region).",
          "type": "boolean"
        },
        "title": {
          "description": "title parameter (Optional[str]).",
          "type": "string"
        }
      },
      "required": [
        "result"
      ],
      "type": "object"
    }
  },
  {
    "description": "Cluster-bootstrap joint Wald test for BJS pre-treatment coefficients.",
    "name": "bjs_pretrend_joint",
    "parameters": {
      "properties": {
        "cluster": {
          "description": "Cluster identifier column for clustered standard errors.",
          "type": "string"
        },
        "controls": {
          "description": "Control-variable column names.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "first_treat": {
          "description": "first_treat parameter (str).",
          "type": "string"
        },
        "group": {
          "description": "Group or cohort identifier.",
          "type": "string"
        },
        "horizon": {
          "description": "If omitted, inferred from ``result.model_info['event_study']``.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "n_boot": {
          "default": 300,
          "description": "Cluster-bootstrap replications. Clusters are sampled with replacement; unit ids are reassigned in the resampled frame so BJS refits cleanly.",
          "type": "integer"
        },
        "result": {
          "description": "Output of :func:`did_imputation` on ``data`` with a non-trivial ``horizon`` that covers negative values. Only its ``model_info['event_study']`` frame is consulted, to look up the observed pre-period point estimates that we re-test with a covariance-aware statistic. Same arguments you passed to the original :func:`did_imputation` call. Needed to re-run BJS on each cluster-bootstrap resample.",
          "type": "string"
        },
        "seed": {
          "description": "RNG seed for reproducibility.",
          "type": "integer"
        },
        "time": {
          "description": "Time period column.",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "result",
        "data",
        "y",
        "group",
        "time",
        "first_treat"
      ],
      "type": "object"
    }
  },
  {
    "description": "One-call staggered-DID workflow following Baker et al. (2026).",
    "name": "cs_report",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "anticipation": {
          "default": 0,
          "description": "anticipation parameter (int).",
          "type": "integer"
        },
        "balance": {
          "default": true,
          "description": "balance parameter (bool).",
          "type": "boolean"
        },
        "control_group": {
          "default": "nevertreated",
          "description": "control_group parameter (str).",
          "enum": [
            "nevertreated",
            "notyettreated"
          ],
          "type": "string"
        },
        "data_or_result": {
          "description": "Either a long-format panel (then ``y, g, t, i`` are required and :func:`callaway_santanna` is run first), or an already-fitted :func:`callaway_santanna` result.",
          "type": "string"
        },
        "estimator": {
          "default": "dr",
          "description": "estimator parameter (str).",
          "enum": [
            "dr",
            "ipw",
            "reg"
          ],
          "type": "string"
        },
        "functional_form": {
          "default": true,
          "description": "functional_form parameter (bool).",
          "type": "boolean"
        },
        "g": {
          "description": "Outcome / cohort / time / unit id columns (required when ``data_or_result`` is a DataFrame).",
          "type": "string"
        },
        "i": {
          "description": "Outcome / cohort / time / unit id columns (required when ``data_or_result`` is a DataFrame).",
          "type": "string"
        },
        "max_e": {
          "default": Infinity,
          "description": "Event-time window passed to the dynamic aggregation.",
          "type": "number"
        },
        "min_e": {
          "default": -Infinity,
          "description": "Event-time window passed to the dynamic aggregation.",
          "type": "number"
        },
        "n_boot": {
          "default": 1000,
          "description": "Multiplier-bootstrap replications for :func:`aggte`.",
          "type": "integer"
        },
        "random_state": {
          "default": 0,
          "description": "Seed for the bootstrap (set to ``None`` for non-reproducibility).",
          "type": "integer"
        },
        "rr_method": {
          "default": "smoothness",
          "description": "Sensitivity restriction handed to :func:`breakdown_m`.",
          "enum": [
            "smoothness",
            "relative_magnitude"
          ],
          "type": "string"
        },
        "save_to": {
          "description": "When set, treats the value as a *path prefix* and writes the report in every supported format in one call: - ``<prefix>.txt`` -- fixed-width plain-text report - ``<prefix>.md`` -- GitHub-flavoured Markdown - ``<prefix>.tex`` -- booktabs LaTeX fragment - ``<prefix>.xlsx`` -- multi-sheet workbook - ``<prefix>.png`` -- 2x2 summary figure (only if matplotlib is installed; silently skipped otherwise) Missing parent directories are created on the fly.",
          "type": "string"
        },
        "t": {
          "description": "Outcome / cohort / time / unit id columns (required when ``data_or_result`` is a DataFrame).",
          "type": "string"
        },
        "triangulate": {
          "default": true,
          "description": "Also fit the other covariate strategies (regression adjustment, IPW, doubly robust) and report them side by side, as in the paper's Table 7 / Figure 4. Only meaningful with covariates.",
          "type": "boolean"
        },
        "verbose": {
          "default": true,
          "description": "If ``True``, print the report before returning.",
          "type": "boolean"
        },
        "weights": {
          "description": "Unit weights omega. Changes the target parameter, not just precision -- see :func:`statspai.did.callaway_santanna`.",
          "type": "string"
        },
        "x": {
          "description": "Covariates for conditional parallel trends.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Outcome / cohort / time / unit id columns (required when ``data_or_result`` is a DataFrame).",
          "type": "string"
        }
      },
      "required": [
        "data_or_result"
      ],
      "type": "object"
    }
  },
  {
    "description": "Structured output of :func:`cs_report`.",
    "name": "CSReport",
    "parameters": {
      "properties": {
        "balance": {
          "description": "balance parameter (Optional[Any]).",
          "type": "string"
        },
        "breakdown": {
          "description": "breakdown parameter (pd.DataFrame).",
          "type": "string"
        },
        "calendar": {
          "description": "calendar parameter (pd.DataFrame).",
          "type": "string"
        },
        "degradations": {
          "default": null,
          "description": "degradations parameter (List[Dict[str, Any]]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "dynamic": {
          "description": "dynamic parameter (pd.DataFrame).",
          "type": "string"
        },
        "estimator_comparison": {
          "default": null,
          "description": "estimator_comparison parameter (pd.DataFrame).",
          "type": "string"
        },
        "functional_form": {
          "default": null,
          "description": "functional_form parameter (Dict[str, Any]).",
          "type": "object"
        },
        "group": {
          "description": "Group or cohort identifier.",
          "type": "string"
        },
        "meta": {
          "default": null,
          "description": "meta parameter (Dict[str, Any]).",
          "type": "object"
        },
        "overall": {
          "description": "overall parameter (Dict[str, float]).",
          "type": "object"
        },
        "overlap": {
          "default": null,
          "description": "overlap parameter (Dict[str, Any]).",
          "type": "object"
        },
        "pretrend": {
          "description": "pretrend parameter (Dict[str, Any]).",
          "type": "object"
        },
        "simple": {
          "description": "simple parameter (pd.DataFrame).",
          "type": "string"
        }
      },
      "required": [
        "overall",
        "simple",
        "dynamic",
        "group",
        "calendar",
        "pretrend",
        "breakdown"
      ],
      "type": "object"
    }
  },
  {
    "description": "Estimate impulse responses via Jorda (2005) local projections. Validation: certified parity evidence.",
    "name": "local_projections",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for the confidence band.",
          "type": "number"
        },
        "auto_lag": {
          "default": true,
          "description": "If ``True`` (the legacy default), also adds ``y_{t-1}`` and ``shock_{t-1}`` as automatic regressors. Set ``False`` for a bare ``y_{t+h} ~ const + shock_t + controls`` specification. These two auto-controls were silent in the pre-1.16 docstring.",
          "type": "boolean"
        },
        "controls": {
          "description": "Additional regressors taken **verbatim** from ``data``: the column values at time t are used directly, without re-lagging. If you want the lag of a control, lag it yourself before passing it in (e.g. ``df[\"unemp_lag\"] = df[\"unemp\"].shift(1)`` and then ``controls=[\"unemp_lag\"]``). The pre-1.16 behaviour silently re-lagged controls a second time on top of an auto- added ``y_{t-1}``, producing collinear columns and surprising impulse responses; see ``MIGRATION.md`` for context.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "cumulative": {
          "default": false,
          "description": "If ``True``, return the cumulative response ``y_{t+h} - y_{t-1}``. Default (False) returns ``y_{t+h}`` directly.",
          "type": "boolean"
        },
        "data": {
          "description": "Time-ordered panel (single series for now -- panel extension TBD).",
          "type": "string"
        },
        "endog_order": {
          "description": "Endogenous variable order used only when ``identification='lpirfs_cholesky'``. Defaults to ``[outcome, shock]``.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "horizons": {
          "default": 20,
          "description": "Number of horizons h = 0, 1, ..., H to estimate.",
          "type": "integer"
        },
        "identification": {
          "default": "direct",
          "description": "Shock-identification convention. ``'direct'`` uses the coefficient on the observed ``shock`` variable in each horizon regression. ``'lpirfs_cholesky'`` reproduces ``lpirfs::lp_lin`` with ``lags_endog_lin=1`` and ``shock_type=1``: the variables in ``endog_order`` define the Cholesky ordering, and the reported response is the unit structural shock for ``shock``.",
          "enum": [
            "direct",
            "lpirfs_cholesky"
          ],
          "type": "string"
        },
        "nw_lags": {
          "description": "Newey-West truncation lag. Defaults to ``round(1.5 * horizons)`` per Kilian & Kim (2011) recommendation.",
          "type": "integer"
        },
        "outcome": {
          "description": "Column name of the outcome variable y.",
          "type": "string"
        },
        "shock": {
          "description": "Column name of the shock / treatment variable.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "outcome",
        "shock"
      ],
      "type": "object"
    }
  },
  {
    "description": "Impulse-response container returned by :func:`local_projections`.",
    "name": "LocalProjectionsResult",
    "parameters": {
      "properties": {
        "alpha": {
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "ci_lower": {
          "description": "ci_lower parameter (np.ndarray).",
          "type": "string"
        },
        "ci_upper": {
          "description": "ci_upper parameter (np.ndarray).",
          "type": "string"
        },
        "horizons": {
          "description": "horizons parameter (np.ndarray).",
          "type": "string"
        },
        "irf": {
          "description": "irf parameter (np.ndarray).",
          "type": "string"
        },
        "n_obs_per_horizon": {
          "description": "Number of obs per horizon.",
          "type": "string"
        },
        "outcome_name": {
          "description": "outcome_name parameter (str).",
          "type": "string"
        },
        "se": {
          "description": "se parameter (np.ndarray).",
          "type": "string"
        },
        "shock_name": {
          "description": "shock_name parameter (str).",
          "type": "string"
        }
      },
      "required": [
        "horizons",
        "irf",
        "se",
        "ci_lower",
        "ci_upper",
        "alpha",
        "shock_name",
        "outcome_name",
        "n_obs_per_horizon"
      ],
      "type": "object"
    }
  },
  {
    "description": "Cardinality matching -- maximise the number of matched pairs subject Validation: validated evidence tier (known-truth, reference, external-parity, or Monte Carlo artifact).",
    "name": "cardinality_match",
    "parameters": {
      "properties": {
        "covariates": {
          "description": "Covariate matrix, DataFrame, or column names.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "outcome": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        },
        "smd_tolerance": {
          "default": 0.1,
          "description": "smd_tolerance parameter (float).",
          "type": "number"
        },
        "time_limit": {
          "default": 30.0,
          "description": "time_limit parameter (float).",
          "type": "number"
        },
        "treatment": {
          "description": "Treatment indicator, treatment variable, or treatment array.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "treatment",
        "outcome",
        "covariates"
      ],
      "type": "object"
    }
  },
  {
    "description": "Result of :func:`cardinality_match` (Zubizarreta cardinality matching).",
    "name": "CardinalityMatchResult",
    "parameters": {
      "properties": {
        "ate": {
          "description": "ate parameter (float).",
          "type": "number"
        },
        "balance": {
          "description": "balance parameter (pd.DataFrame).",
          "type": "string"
        },
        "control_matched": {
          "description": "control_matched parameter (np.ndarray).",
          "type": "string"
        },
        "n_matched_pairs": {
          "description": "Number of matched pairs.",
          "type": "integer"
        },
        "se": {
          "description": "se parameter (float).",
          "type": "number"
        },
        "treated_matched": {
          "description": "treated_matched parameter (np.ndarray).",
          "type": "string"
        }
      },
      "required": [
        "treated_matched",
        "control_matched",
        "ate",
        "se",
        "n_matched_pairs",
        "balance"
      ],
      "type": "object"
    }
  },
  {
    "description": "Optimal 1:1 matching via the Hungarian algorithm. Validation: certified parity evidence. Do NOT use when: either arm has more than ~10,000 units -- the assignment problem is superquadratic (see cost); use sp.psm or sp.match (greedy nearest-neighbour) at that scale. Cost: Materialises the dense n_treated x n_control distance matrix, then solves a linear sum assignment (Hungarian, ~O(n^3) worst case). Both memory and time degrade sharply past a few thousand units per arm.",
    "name": "optimal_match",
    "parameters": {
      "properties": {
        "caliper": {
          "description": "Drop any pair with distance greater than ``caliper``.",
          "type": "number"
        },
        "covariates": {
          "description": "Covariate matrix, DataFrame, or column names.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "metric": {
          "default": "mahalanobis",
          "description": "metric parameter (str).",
          "type": "string"
        },
        "outcome": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        },
        "treatment": {
          "description": "Treatment indicator, treatment variable, or treatment array.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "treatment",
        "outcome",
        "covariates"
      ],
      "type": "object"
    }
  },
  {
    "description": "Result of :func:`optimal_match` (optimal 1:1 Hungarian matching).",
    "name": "OptimalMatchResult",
    "parameters": {
      "properties": {
        "ate": {
          "description": "ate parameter (float).",
          "type": "number"
        },
        "distances": {
          "description": "distances parameter (np.ndarray).",
          "type": "string"
        },
        "n_matched": {
          "description": "Number of matched.",
          "type": "integer"
        },
        "n_treated": {
          "description": "Number of treated.",
          "type": "integer"
        },
        "pairs": {
          "description": "pairs parameter (pd.DataFrame).",
          "type": "string"
        },
        "se": {
          "description": "se parameter (float).",
          "type": "number"
        }
      },
      "required": [
        "pairs",
        "distances",
        "ate",
        "se",
        "n_treated",
        "n_matched"
      ],
      "type": "object"
    }
  },
  {
    "description": "Deville-Sarndal (1992) linear calibration. Validation: certified parity evidence.",
    "name": "linear_calibration",
    "parameters": {
      "properties": {
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "totals": {
          "description": "``{column_name: population_total}`` for continuous auxiliary variables.",
          "type": "object"
        },
        "weight": {
          "description": "Design weight column. If ``None``, uses equal weights.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "totals"
      ],
      "type": "object"
    }
  },
  {
    "description": "Raking (iterative proportional fitting). Validation: certified parity evidence.",
    "name": "rake",
    "parameters": {
      "properties": {
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "margins": {
          "description": "``{column_name: {category: target}}``. Targets may be proportions or population counts; each margin is normalised to shares. Every category observed in ``data[column_name]`` must have a target and every target category must occur in the data (R ``rake`` also refuses both).",
          "type": "object"
        },
        "max_iter": {
          "default": 100,
          "description": "Maximum number of full sweeps over the margins.",
          "type": "integer"
        },
        "tol": {
          "default": 1e-10,
          "description": "Numerical convergence tolerance.",
          "type": "number"
        },
        "weight": {
          "description": "Existing design weight column. If ``None``, starts with equal weights.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "margins"
      ],
      "type": "object"
    }
  },
  {
    "description": "Fit an Accelerated Failure Time model by MLE. Validation: certified parity evidence.",
    "name": "aft",
    "parameters": {
      "properties": {
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "family": {
          "default": "weibull",
          "description": "family parameter (AFTFamily).",
          "enum": [
            "exponential",
            "weibull",
            "lognormal",
            "loglogistic"
          ],
          "type": "string"
        },
        "formula": {
          "description": "``\"duration + event ~ x1 + x2\"``.",
          "type": "string"
        }
      },
      "required": [
        "formula",
        "data"
      ],
      "type": "object"
    }
  },
  {
    "description": "Cox proportional hazards with shared gamma frailty. Validation: certified parity evidence.",
    "name": "cox_frailty",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level (kept for API symmetry; intervals are built by the result's accessors).",
          "type": "number"
        },
        "cluster": {
          "description": "Column identifying clusters (e.g. hospital, site).",
          "type": "string"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "formula": {
          "description": "``\"duration + event ~ x1 + x2\"`` (like R's ``Surv(time, event) ~ x``).",
          "type": "string"
        },
        "maxiter": {
          "default": 50,
          "description": "Newton-Raphson controls for the penalised fit at each theta.",
          "type": "integer"
        },
        "theta": {
          "description": "Fix the frailty variance instead of estimating it (R ``frailty(id, theta=)``). ``0`` fits the ordinary Cox model.",
          "type": "number"
        },
        "ties": {
          "default": "efron",
          "description": "Tie handling in the partial likelihood (R's default is Efron, Stata's ``stcox`` default is Breslow; identical without ties).",
          "enum": [
            "efron",
            "breslow"
          ],
          "type": "string"
        },
        "tol": {
          "default": 1e-10,
          "description": "Newton-Raphson controls for the penalised fit at each theta.",
          "type": "number"
        }
      },
      "required": [
        "formula",
        "data",
        "cluster"
      ],
      "type": "object"
    }
  },
  {
    "description": "Fit DirectLiNGAM (Shimizu 2011). Validation: validated evidence tier (known-truth, reference, external-parity, or Monte Carlo artifact).",
    "name": "lingam",
    "parameters": {
      "properties": {
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "standardize": {
          "default": true,
          "description": "Zero-mean / unit-variance each variable before the algorithm.",
          "type": "boolean"
        }
      },
      "required": [
        "data"
      ],
      "type": "object"
    }
  },
  {
    "description": "Result of a :func:`lingam` (DirectLiNGAM) fit.",
    "name": "LiNGAMResult",
    "parameters": {
      "properties": {
        "adjacency": {
          "description": "adjacency parameter (np.ndarray).",
          "type": "string"
        },
        "names": {
          "description": "names parameter (List[str]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "order": {
          "description": "order parameter (List[int]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "residuals": {
          "description": "residuals parameter (np.ndarray).",
          "type": "string"
        }
      },
      "required": [
        "order",
        "adjacency",
        "names",
        "residuals"
      ],
      "type": "object"
    }
  },
  {
    "description": "Greedy Equivalence Search. Validation: validated evidence tier (known-truth, reference, external-parity, or Monte Carlo artifact).",
    "name": "ges",
    "parameters": {
      "properties": {
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "max_iter": {
          "default": 500,
          "description": "Maximum total edge additions + removals.",
          "type": "integer"
        }
      },
      "required": [
        "data"
      ],
      "type": "object"
    }
  },
  {
    "description": "Result of :func:`ges` -- a CPDAG (Markov equivalence class).",
    "name": "GESResult",
    "parameters": {
      "properties": {
        "adjacency": {
          "description": "adjacency parameter (np.ndarray).",
          "type": "string"
        },
        "bic": {
          "description": "bic parameter (float).",
          "type": "number"
        },
        "names": {
          "description": "names parameter (List[str]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "required": [
        "adjacency",
        "names",
        "bic"
      ],
      "type": "object"
    }
  },
  {
    "description": "Run FCI. Returns a :class:`FCIResult` with the learned PAG. Validation: validated evidence tier (known-truth, reference, external-parity, or Monte Carlo artifact).",
    "name": "fci",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for CI tests.",
          "type": "number"
        },
        "ci_test": {
          "default": "fisherz",
          "description": "Only Fisher-Z partial-correlation test is supported; extensions (kernel / chi-square) can be added later.",
          "type": "string"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "max_cond_size": {
          "description": "Max size of conditioning set.",
          "type": "integer"
        },
        "variables": {
          "description": "Columns to use; defaults to all numeric columns.",
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "required": [
        "data"
      ],
      "type": "object"
    }
  },
  {
    "description": "Partial Ancestral Graph (PAG) learned by :func:`fci`.",
    "name": "FCIResult",
    "parameters": {
      "properties": {
        "alpha": {
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "ci_test": {
          "description": "ci_test parameter (str).",
          "type": "string"
        },
        "edges": {
          "description": "edges parameter (List[Tuple[str, str, str]]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "n_obs": {
          "description": "Number of obs.",
          "type": "integer"
        },
        "pag_left": {
          "description": "pag_left parameter (pd.DataFrame).",
          "type": "string"
        },
        "pag_right": {
          "description": "pag_right parameter (pd.DataFrame).",
          "type": "string"
        },
        "separating_sets": {
          "description": "separating_sets parameter (Dict[Tuple[str, str], Set[str]]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "skeleton": {
          "description": "skeleton parameter (pd.DataFrame).",
          "type": "string"
        },
        "variables": {
          "description": "variables parameter (List[str]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "required": [
        "variables",
        "skeleton",
        "pag_left",
        "pag_right",
        "edges",
        "separating_sets",
        "n_obs",
        "alpha",
        "ci_test"
      ],
      "type": "object"
    }
  },
  {
    "description": "Fit ARIMA(p,d,q) or SARIMAX. Validation: certified parity evidence.",
    "name": "arima",
    "parameters": {
      "properties": {
        "auto": {
          "default": false,
          "description": "If True, select (p, d, q) by AICc grid search (ignores ``order``).",
          "type": "boolean"
        },
        "exog": {
          "description": "Exogenous regressors (ARIMAX).",
          "type": "string"
        },
        "max_d": {
          "default": 2,
          "description": "Bounds for the auto search.",
          "type": "integer"
        },
        "max_p": {
          "default": 5,
          "description": "Bounds for the auto search.",
          "type": "integer"
        },
        "max_q": {
          "default": 5,
          "description": "Bounds for the auto search.",
          "type": "integer"
        },
        "method": {
          "default": "statespace",
          "description": "Estimation convention. ``'statespace'`` keeps the default exact Kalman/SARIMAX likelihood. ``'css_ml'`` is retained as a compatibility alias for ``'innovations_mle'``. The innovations-MLE path uses statsmodels' stationary/invertible exact-MLE parameterization, matching ``stats::arima(method='ML')`` and tightly converged Stata ``arima`` coefficient conventions for pure ARMA models.",
          "enum": [
            "statespace",
            "css_ml",
            "innovations_mle"
          ],
          "type": "string"
        },
        "order": {
          "default": [
            1,
            0,
            0
          ],
          "description": "order parameter (Tuple[int, int, int]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "seasonal_order": {
          "description": "seasonal_order parameter (Optional[Tuple[int, int, int, int]]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "y"
      ],
      "type": "object"
    }
  },
  {
    "description": "Fitted ARIMA(p,d,q) / SARIMAX model returned by :func:`statspai.arima`.",
    "name": "ARIMAResult",
    "parameters": {
      "properties": {
        "_model": {
          "description": "_model parameter.",
          "type": "string"
        },
        "aic": {
          "description": "aic parameter (float).",
          "type": "number"
        },
        "aicc": {
          "description": "aicc parameter (float).",
          "type": "number"
        },
        "bic": {
          "description": "bic parameter (float).",
          "type": "number"
        },
        "fitted_values": {
          "description": "fitted_values parameter (np.ndarray).",
          "type": "string"
        },
        "log_likelihood": {
          "description": "log_likelihood parameter (float).",
          "type": "number"
        },
        "n": {
          "description": "n parameter (int).",
          "type": "integer"
        },
        "order": {
          "description": "order parameter (Tuple[int, int, int]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "params": {
          "description": "params parameter (pd.Series).",
          "type": "string"
        },
        "residuals": {
          "description": "residuals parameter (np.ndarray).",
          "type": "string"
        },
        "se": {
          "description": "se parameter (pd.Series).",
          "type": "string"
        },
        "seasonal_order": {
          "description": "seasonal_order parameter (Optional[Tuple[int, int, int, int]]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "required": [
        "order",
        "seasonal_order",
        "params",
        "se",
        "aic",
        "bic",
        "aicc",
        "log_likelihood",
        "residuals",
        "fitted_values",
        "n",
        "_model"
      ],
      "type": "object"
    }
  },
  {
    "description": "Bayesian VAR with the original Minnesota (Litterman) prior. Validation: validated evidence tier (known-truth, reference, external-parity, or Monte Carlo artifact).",
    "name": "bvar",
    "parameters": {
      "properties": {
        "data": {
          "description": "Columns are the endogenous variables.",
          "type": "string"
        },
        "lags": {
          "default": 4,
          "description": "lags parameter (int).",
          "type": "integer"
        },
        "lambda1": {
          "default": 0.1,
          "description": "Overall (self) tightness (smaller = stronger shrinkage toward RW).",
          "type": "number"
        },
        "lambda2": {
          "default": 0.5,
          "description": "Cross-variable tightness relative to own lags.",
          "type": "number"
        },
        "lambda3": {
          "default": 1.0,
          "description": "Lag decay.",
          "type": "number"
        },
        "lambda4": {
          "default": 100.0,
          "description": "Tightness of the constant (exogenous) terms.",
          "type": "number"
        },
        "sigma": {
          "default": "ar",
          "description": "``Sigma0``: diagonal of the univariate AR(lags) residual variances (Litterman's original; Stata ``arcov``), or the diagonal of the OLS VAR residual variances ``diag(U'U) / n`` (Stata ``varcov``). The prior-variance scales ``s_i^2`` follow the same choice.",
          "enum": [
            "ar",
            "var"
          ],
          "type": "string"
        }
      },
      "required": [
        "data"
      ],
      "type": "object"
    }
  },
  {
    "description": "Posterior summary of a Bayesian VAR with Minnesota prior.",
    "name": "BVARResult",
    "parameters": {
      "properties": {
        "coef": {
          "description": "coef parameter (np.ndarray).",
          "type": "string"
        },
        "coef_sd": {
          "description": "coef_sd parameter (Optional[np.ndarray]).",
          "type": "string"
        },
        "fitted": {
          "description": "fitted parameter (np.ndarray).",
          "type": "string"
        },
        "lags": {
          "description": "lags parameter (int).",
          "type": "integer"
        },
        "lambda1": {
          "description": "lambda1 parameter (float).",
          "type": "number"
        },
        "lambda2": {
          "description": "lambda2 parameter (float).",
          "type": "number"
        },
        "n": {
          "description": "n parameter (int).",
          "type": "integer"
        },
        "residuals": {
          "description": "residuals parameter (np.ndarray).",
          "type": "string"
        },
        "sigma": {
          "description": "sigma parameter (np.ndarray).",
          "type": "string"
        },
        "var_names": {
          "description": "var_names parameter (list[str]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "required": [
        "coef",
        "sigma",
        "fitted",
        "residuals",
        "var_names",
        "lags",
        "n",
        "lambda1",
        "lambda2"
      ],
      "type": "object"
    }
  },
  {
    "description": "Fit GARCH(p,q) by conditional Gaussian MLE. Validation: certified parity evidence.",
    "name": "garch",
    "parameters": {
      "properties": {
        "mean": {
          "default": true,
          "description": "Estimate a constant mean mu; if False, mu = 0.",
          "type": "boolean"
        },
        "p": {
          "default": 1,
          "description": "Number of GARCH (lagged sigma2) terms (Stata ``garch(p)``).",
          "type": "integer"
        },
        "presample": {
          "default": "stata",
          "description": "How the recursion is started; both use ``m = mean(epsilon2)`` at the current mu. ``'stata'``: pre-sample epsilon2 and sigma2 equal ``m`` and every observation's sigma2 follows the recursion (Stata ``arch``, default ``arch0(xb)``). ``'rugarch'``: sigma2_t = ``m`` for the first ``max(p, q)`` observations (``rugarch::ugarchfit``, sGARCH). The log-likelihood sums over all observations in both cases.",
          "enum": [
            "stata",
            "rugarch"
          ],
          "type": "string"
        },
        "q": {
          "default": 1,
          "description": "Number of ARCH (lagged epsilon2) terms (Stata ``arch(q)``).",
          "type": "integer"
        },
        "vce": {
          "default": "oim",
          "description": "Covariance of the estimates: inverse observed information (Hessian of the log-likelihood, by central differences of the analytic score), outer product of the scores (Stata's ``arch`` default), or the Bollerslev-Wooldridge sandwich ``H^{-1} (S'S) H^{-1}``.",
          "enum": [
            "oim",
            "opg",
            "robust"
          ],
          "type": "string"
        },
        "y": {
          "description": "Return series (or log-return, etc.).",
          "type": "string"
        }
      },
      "required": [
        "y"
      ],
      "type": "object"
    }
  },
  {
    "description": "Fitted GARCH(p,q) model returned by :func:`garch`.",
    "name": "GARCHResult",
    "parameters": {
      "properties": {
        "aic": {
          "description": "aic parameter (float).",
          "type": "number"
        },
        "alpha": {
          "description": "Significance level for confidence intervals and tests.",
          "type": "string"
        },
        "beta": {
          "description": "beta parameter (np.ndarray).",
          "type": "string"
        },
        "bic": {
          "description": "bic parameter (float).",
          "type": "number"
        },
        "coef": {
          "description": "coef parameter (Optional[np.ndarray]).",
          "type": "string"
        },
        "log_likelihood": {
          "description": "log_likelihood parameter (float).",
          "type": "number"
        },
        "mu": {
          "description": "mu parameter (float).",
          "type": "number"
        },
        "n": {
          "description": "n parameter (int).",
          "type": "integer"
        },
        "omega": {
          "description": "omega parameter (float).",
          "type": "number"
        },
        "p": {
          "description": "p parameter (int).",
          "type": "integer"
        },
        "param_names": {
          "description": "param_names parameter (Optional[List[str]]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "q": {
          "description": "Quantile level.",
          "type": "integer"
        },
        "residuals": {
          "description": "residuals parameter (np.ndarray).",
          "type": "string"
        },
        "se_vec": {
          "description": "se_vec parameter (Optional[np.ndarray]).",
          "type": "string"
        },
        "sigma2": {
          "description": "sigma2 parameter (np.ndarray).",
          "type": "string"
        },
        "std_residuals": {
          "description": "std_residuals parameter (np.ndarray).",
          "type": "string"
        }
      },
      "required": [
        "omega",
        "alpha",
        "beta",
        "mu",
        "sigma2",
        "residuals",
        "std_residuals",
        "log_likelihood",
        "aic",
        "bic",
        "n",
        "p",
        "q"
      ],
      "type": "object"
    }
  },
  {
    "description": "CPS-style wage data with a gender gap.",
    "name": "cps_wage",
    "parameters": {
      "properties": {
        "n": {
          "default": 3000,
          "description": "n parameter (int).",
          "type": "integer"
        },
        "seed": {
          "default": 42,
          "description": "Random seed for reproducible stochastic steps.",
          "type": "integer"
        }
      },
      "required": [],
      "type": "object"
    }
  },
  {
    "description": "Two-period Mincer wage distribution with a structural shift.",
    "name": "mincer_wage_panel",
    "parameters": {
      "properties": {
        "n": {
          "default": 5000,
          "description": "n parameter (int).",
          "type": "integer"
        },
        "seed": {
          "default": 42,
          "description": "Random seed for reproducible stochastic steps.",
          "type": "integer"
        }
      },
      "required": [],
      "type": "object"
    }
  },
  {
    "description": "Chilean-style household income with urban/rural gap.",
    "name": "chilean_households",
    "parameters": {
      "properties": {
        "n": {
          "default": 2500,
          "description": "n parameter (int).",
          "type": "integer"
        },
        "seed": {
          "default": 42,
          "description": "Random seed for reproducible stochastic steps.",
          "type": "integer"
        }
      },
      "required": [],
      "type": "object"
    }
  },
  {
    "description": "Output of :func:`zero_first_stage`.",
    "name": "ZeroFirstStageResult",
    "parameters": {
      "properties": {
        "alpha": {
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "beta_iv": {
          "description": "beta_iv parameter (float).",
          "type": "number"
        },
        "beta_iv_se": {
          "description": "beta_iv_se parameter (float).",
          "type": "number"
        },
        "beta_zfs_corrected": {
          "description": "beta_zfs_corrected parameter (float).",
          "type": "number"
        },
        "beta_zfs_corrected_ci": {
          "description": "beta_zfs_corrected_ci parameter (tuple).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "beta_zfs_corrected_se": {
          "description": "beta_zfs_corrected_se parameter (float).",
          "type": "number"
        },
        "diagnostics": {
          "default": null,
          "description": "diagnostics parameter (Dict[str, Any]).",
          "type": "object"
        },
        "first_stage_main": {
          "description": "first_stage_main parameter (float).",
          "type": "number"
        },
        "first_stage_main_se": {
          "description": "first_stage_main_se parameter (float).",
          "type": "number"
        },
        "first_stage_zfs": {
          "description": "first_stage_zfs parameter (float).",
          "type": "number"
        },
        "first_stage_zfs_ci": {
          "description": "first_stage_zfs_ci parameter (tuple).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "first_stage_zfs_pvalue": {
          "description": "first_stage_zfs_pvalue parameter (float).",
          "type": "number"
        },
        "first_stage_zfs_se": {
          "description": "first_stage_zfs_se parameter (float).",
          "type": "number"
        },
        "implied_bias": {
          "description": "implied_bias parameter (float).",
          "type": "number"
        },
        "instrument": {
          "description": "instrument parameter (str).",
          "type": "string"
        },
        "n_boot": {
          "description": "Number of bootstrap replications.",
          "type": "integer"
        },
        "n_main": {
          "description": "Number of main.",
          "type": "integer"
        },
        "n_zfs": {
          "description": "Number of zfs.",
          "type": "integer"
        },
        "reduced_form_zfs": {
          "description": "reduced_form_zfs parameter (float).",
          "type": "number"
        },
        "reduced_form_zfs_ci": {
          "description": "reduced_form_zfs_ci parameter (tuple).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "reduced_form_zfs_pvalue": {
          "description": "reduced_form_zfs_pvalue parameter (float).",
          "type": "number"
        },
        "reduced_form_zfs_se": {
          "description": "reduced_form_zfs_se parameter (float).",
          "type": "number"
        }
      },
      "required": [
        "first_stage_zfs",
        "first_stage_zfs_se",
        "first_stage_zfs_ci",
        "first_stage_zfs_pvalue",
        "reduced_form_zfs",
        "reduced_form_zfs_se",
        "reduced_form_zfs_ci",
        "reduced_form_zfs_pvalue",
        "first_stage_main",
        "first_stage_main_se",
        "beta_iv",
        "beta_iv_se",
        "beta_zfs_corrected",
        "beta_zfs_corrected_se",
        "beta_zfs_corrected_ci",
        "implied_bias",
        "n_main",
        "n_zfs",
        "n_boot",
        "instrument",
        "alpha"
      ],
      "type": "object"
    }
  },
  {
    "description": "Output of kernel IV regression.",
    "name": "KernelIVResult",
    "parameters": {
      "properties": {
        "bandwidth": {
          "description": "Bandwidth used for local smoothing or kernel weighting.",
          "type": "number"
        },
        "ci_high": {
          "description": "ci_high parameter (np.ndarray).",
          "type": "string"
        },
        "ci_low": {
          "description": "ci_low parameter (np.ndarray).",
          "type": "string"
        },
        "grid": {
          "description": "grid parameter (np.ndarray).",
          "type": "string"
        },
        "h_hat": {
          "description": "h_hat parameter (np.ndarray).",
          "type": "string"
        },
        "n_obs": {
          "description": "Number of obs.",
          "type": "integer"
        }
      },
      "required": [
        "grid",
        "h_hat",
        "ci_low",
        "ci_high",
        "bandwidth",
        "n_obs"
      ],
      "type": "object"
    }
  },
  {
    "description": "Continuous-instrument LATE on the maximal complier class.",
    "name": "ContinuousLATEResult",
    "parameters": {
      "properties": {
        "ci": {
          "description": "ci parameter (tuple).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "complier_share": {
          "description": "complier_share parameter (float).",
          "type": "number"
        },
        "estimate": {
          "description": "estimate parameter (float).",
          "type": "number"
        },
        "n_obs": {
          "description": "Number of obs.",
          "type": "integer"
        },
        "se": {
          "description": "se parameter (float).",
          "type": "number"
        }
      },
      "required": [
        "estimate",
        "se",
        "ci",
        "complier_share",
        "n_obs"
      ],
      "type": "object"
    }
  },
  {
    "description": "Container for :func:`iv_diag` output.",
    "name": "IVDiagResult",
    "parameters": {
      "properties": {
        "alpha": {
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "ar_ci": {
          "description": "ar_ci parameter (Tuple[float, float]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "ar_pvalue": {
          "description": "ar_pvalue parameter (float).",
          "type": "number"
        },
        "ar_stat": {
          "description": "ar_stat parameter (float).",
          "type": "number"
        },
        "beta_2sls": {
          "description": "beta_2sls parameter (float).",
          "type": "number"
        },
        "beta_ols": {
          "description": "beta_ols parameter (float).",
          "type": "number"
        },
        "bootstrap_ci_pairs": {
          "description": "bootstrap_ci_pairs parameter (Optional[Tuple[float, float]]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "bootstrap_ci_wild": {
          "description": "bootstrap_ci_wild parameter (Optional[Tuple[float, float]]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "bootstrap_n": {
          "default": 0,
          "description": "bootstrap_n parameter (int).",
          "type": "integer"
        },
        "bootstrap_se_pairs": {
          "description": "bootstrap_se_pairs parameter (Optional[float]).",
          "type": "number"
        },
        "bootstrap_se_wild": {
          "description": "bootstrap_se_wild parameter (Optional[float]).",
          "type": "number"
        },
        "ci_analytic_2sls": {
          "description": "ci_analytic_2sls parameter (Tuple[float, float]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "ci_ols": {
          "description": "ci_ols parameter (Tuple[float, float]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "clr_ci": {
          "description": "clr_ci parameter (Optional[Tuple[float, float]]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "diagnostics": {
          "default": null,
          "description": "diagnostics parameter (Dict[str, Any]).",
          "type": "object"
        },
        "effective_F": {
          "description": "effective_F parameter (float).",
          "type": "number"
        },
        "endog": {
          "description": "endog parameter (str).",
          "type": "string"
        },
        "exog": {
          "description": "exog parameter (List[str]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "first_stage_F": {
          "description": "first_stage_F parameter (float).",
          "type": "number"
        },
        "instruments": {
          "description": "instruments parameter (List[str]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "k_ci": {
          "description": "k_ci parameter (Optional[Tuple[float, float]]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "kp_rk_f": {
          "description": "kp_rk_f parameter (Optional[float]).",
          "type": "number"
        },
        "kp_rk_lm": {
          "description": "kp_rk_lm parameter (Optional[float]).",
          "type": "number"
        },
        "kp_rk_lm_pvalue": {
          "description": "kp_rk_lm_pvalue parameter (Optional[float]).",
          "type": "number"
        },
        "ltz_ci": {
          "description": "ltz_ci parameter (Optional[Tuple[float, float]]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "ltz_warning": {
          "description": "ltz_warning parameter (Optional[str]).",
          "type": "string"
        },
        "n": {
          "description": "n parameter (int).",
          "type": "integer"
        },
        "n_endog": {
          "description": "Number of endog.",
          "type": "integer"
        },
        "n_exog": {
          "description": "Number of exog.",
          "type": "integer"
        },
        "n_instruments": {
          "description": "Number of instruments.",
          "type": "integer"
        },
        "p_2sls": {
          "description": "p_2sls parameter (float).",
          "type": "number"
        },
        "p_ols": {
          "description": "p_ols parameter (float).",
          "type": "number"
        },
        "raw": {
          "default": null,
          "description": "raw parameter (Dict[str, Any]).",
          "type": "object"
        },
        "se_2sls": {
          "description": "se_2sls parameter (float).",
          "type": "number"
        },
        "se_ols": {
          "description": "se_ols parameter (float).",
          "type": "number"
        },
        "tF_adjusted_ci": {
          "description": "tF_adjusted_ci parameter (Tuple[float, float]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "tF_critical_value": {
          "description": "tF_critical_value parameter (float).",
          "type": "number"
        },
        "t_2sls": {
          "description": "t_2sls parameter (float).",
          "type": "number"
        },
        "t_ols": {
          "description": "t_ols parameter (float).",
          "type": "number"
        },
        "tsls_late_caveat": {
          "description": "tsls_late_caveat parameter (Optional[str]).",
          "type": "string"
        }
      },
      "required": [
        "n",
        "n_endog",
        "n_instruments",
        "n_exog",
        "endog",
        "instruments",
        "exog",
        "alpha",
        "beta_2sls",
        "se_2sls",
        "t_2sls",
        "p_2sls",
        "ci_analytic_2sls",
        "beta_ols",
        "se_ols",
        "t_ols",
        "p_ols",
        "ci_ols",
        "first_stage_F",
        "effective_F",
        "tF_critical_value",
        "tF_adjusted_ci",
        "ar_stat",
        "ar_pvalue",
        "ar_ci",
        "kp_rk_lm",
        "kp_rk_lm_pvalue",
        "kp_rk_f"
      ],
      "type": "object"
    }
  },
  {
    "description": "Return list of all registered decomposition method names.",
    "name": "available_methods",
    "parameters": {
      "properties": {},
      "required": [],
      "type": "object"
    }
  },
  {
    "description": "Sharp / fuzzy RD -- article-friendly alias for :func:`rdrobust`. Validation: certified parity evidence.",
    "name": "rdd",
    "parameters": {
      "properties": {
        "cutoff": {
          "default": 0.0,
          "description": "cutoff parameter (float).",
          "type": "number"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "fuzzy": {
          "description": "fuzzy parameter (Optional[str]).",
          "type": "string"
        },
        "running": {
          "description": "running parameter (str).",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "running"
      ],
      "type": "object"
    }
  },
  {
    "description": "Front-door adjustment -- article-friendly alias for Validation: validated evidence tier (known-truth, reference, external-parity, or Monte Carlo artifact).",
    "name": "frontdoor",
    "parameters": {
      "properties": {
        "X": {
          "description": "Feature matrix or covariate DataFrame.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "d": {
          "description": "d parameter (str).",
          "type": "string"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "m": {
          "description": "m parameter (str).",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "d",
        "m"
      ],
      "type": "object"
    }
  },
  {
    "description": "X-Learner CATE -- article alias for :func:`metalearner(learner='x')`. Validation: validated evidence tier (known-truth, reference, external-parity, or Monte Carlo artifact).",
    "name": "xlearner",
    "parameters": {
      "properties": {
        "X": {
          "description": "Feature matrix or covariate DataFrame.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "d": {
          "description": "d parameter (str).",
          "type": "string"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "d",
        "X"
      ],
      "type": "object"
    }
  },
  {
    "description": "Conformal ITE -- article alias for :func:`conformal_cate`. Validation: validated evidence tier (known-truth, reference, external-parity, or Monte Carlo artifact).",
    "name": "conformal_ite",
    "parameters": {
      "properties": {
        "X": {
          "description": "Feature matrix or covariate DataFrame.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "d": {
          "description": "d parameter (str).",
          "type": "string"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "d",
        "X"
      ],
      "type": "object"
    }
  },
  {
    "description": "Propensity-score matching -- article alias for :func:`match` Validation: certified parity evidence.",
    "name": "psm",
    "parameters": {
      "properties": {
        "X": {
          "description": "Feature matrix or covariate DataFrame.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "d": {
          "description": "d parameter (str).",
          "type": "string"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "method": {
          "default": "nn",
          "description": "Estimator or algorithm variant to use.",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "d",
        "X"
      ],
      "type": "object"
    }
  },
  {
    "description": "Partial identification of ATE -- article alias for the ``bounds`` module.",
    "name": "partial_identification",
    "parameters": {
      "properties": {
        "X": {
          "description": "Feature matrix or covariate DataFrame.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "assumptions": {
          "description": "assumptions parameter (Optional[List[str]]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "d": {
          "description": "d parameter (str).",
          "type": "string"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "instrument": {
          "description": "instrument parameter (Optional[str]).",
          "type": "string"
        },
        "method": {
          "default": "manski",
          "description": "Estimator or algorithm variant to use.",
          "type": "string"
        },
        "selection": {
          "description": "selection parameter (Optional[str]).",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "d"
      ],
      "type": "object"
    }
  },
  {
    "description": "Anderson-Rubin confidence set -- re-export of Validation: certified parity evidence.",
    "name": "anderson_rubin_ci",
    "parameters": {
      "properties": {
        "add_const": {
          "default": true,
          "description": "add_const parameter (bool).",
          "type": "boolean"
        },
        "beta_grid": {
          "description": "Grid of beta values to evaluate.",
          "type": "string"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "endog": {
          "description": "endog parameter (Union[np.ndarray, pd.Series, str]).",
          "type": "string"
        },
        "exog": {
          "description": "exog parameter (Optional[Union[np.ndarray, pd.DataFrame, List[str]]]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "instruments": {
          "description": "instruments parameter (Union[np.ndarray, pd.DataFrame, List[str]]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "level": {
          "default": 0.95,
          "description": "Confidence level or reporting level.",
          "type": "number"
        },
        "n_grid": {
          "default": 401,
          "description": "Number of grid.",
          "type": "integer"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "y",
        "endog",
        "instruments"
      ],
      "type": "object"
    }
  },
  {
    "description": "Moreira (2003) CLR confidence set -- re-export of Validation: certified parity evidence.",
    "name": "conditional_lr_ci",
    "parameters": {
      "properties": {
        "add_const": {
          "default": true,
          "description": "add_const parameter (bool).",
          "type": "boolean"
        },
        "beta_grid": {
          "description": "Grid of beta values to evaluate.",
          "type": "string"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "endog": {
          "description": "endog parameter (Union[np.ndarray, pd.Series, str]).",
          "type": "string"
        },
        "exog": {
          "description": "exog parameter (Optional[Union[np.ndarray, pd.DataFrame, List[str]]]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "instruments": {
          "description": "instruments parameter (Union[np.ndarray, pd.DataFrame, List[str]]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "level": {
          "default": 0.95,
          "description": "Confidence level or reporting level.",
          "type": "number"
        },
        "method": {
          "default": "exact",
          "description": "Estimator or algorithm variant to use.",
          "type": "string"
        },
        "n_grid": {
          "default": 201,
          "description": "Number of grid.",
          "type": "integer"
        },
        "n_sim": {
          "default": 5000,
          "description": "Number of sim.",
          "type": "integer"
        },
        "random_state": {
          "description": "Random seed or RandomState for reproducible stochastic steps.",
          "type": "integer"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "y",
        "endog",
        "instruments"
      ],
      "type": "object"
    }
  },
  {
    "description": "tF adjusted critical value (Lee, McCrary, Moreira & Porter 2022, AER). Validation: certified parity evidence.",
    "name": "tF_adjustment",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "first_stage_F": {
          "description": "first_stage_F parameter (float).",
          "type": "number"
        }
      },
      "required": [
        "first_stage_F"
      ],
      "type": "object"
    }
  },
  {
    "description": "Run several staggered-DiD estimators side by side.",
    "name": "auto_did",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for reported CIs.",
          "type": "number"
        },
        "data": {
          "description": "Panel data, long format.",
          "type": "string"
        },
        "g": {
          "description": "First-treatment-period column (0 or NaN for never-treated).",
          "type": "string"
        },
        "i": {
          "description": "Unit identifier column.",
          "type": "string"
        },
        "methods": {
          "description": "Subset of ``{'cs', 'sa', 'bjs'}``. Defaults to all three.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "select_by": {
          "default": "median",
          "description": "How to pick the winner. ``'median'`` returns the candidate with the median point estimate across successes. A method name returns that specific candidate (useful for forcing a baseline).",
          "enum": [
            "median",
            "first_success",
            "cs",
            "sa",
            "bjs"
          ],
          "type": "string"
        },
        "t": {
          "description": "Calendar time column.",
          "type": "string"
        },
        "x": {
          "description": "Covariates (used by CS / SA / BJS where supported).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Outcome column.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "g",
        "t",
        "i"
      ],
      "type": "object"
    }
  },
  {
    "description": "Leaderboard + winner from :func:`auto_did`.",
    "name": "AutoDIDResult",
    "parameters": {
      "properties": {
        "candidates": {
          "description": "candidates parameter (Dict[str, Any]).",
          "type": "object"
        },
        "leaderboard": {
          "description": "leaderboard parameter (pd.DataFrame).",
          "type": "string"
        },
        "selection_rule": {
          "default": "median",
          "description": "selection_rule parameter (str).",
          "type": "string"
        },
        "winner": {
          "description": "winner parameter.",
          "type": "string"
        }
      },
      "required": [
        "leaderboard",
        "winner",
        "candidates"
      ],
      "type": "object"
    }
  },
  {
    "description": "Race 2SLS, LIML, and JIVE on a single-endogenous IV spec.",
    "name": "auto_iv",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "cluster": {
          "description": "Forwarded to each estimator where supported.",
          "type": "string"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "endog": {
          "description": "Single endogenous regressor column.",
          "type": "string"
        },
        "exog": {
          "description": "Exogenous controls (included in all requested IV estimators).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "instruments": {
          "description": "Instrument(s). A scalar is promoted to a one-element list.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "methods": {
          "description": "Subset of ``{'2sls', 'liml', 'jive'}``.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "robust": {
          "default": "nonrobust",
          "description": "Forwarded to each estimator where supported.",
          "type": "string"
        },
        "select_by": {
          "default": "median",
          "description": "select_by parameter (str).",
          "enum": [
            "median",
            "first_success",
            "2sls",
            "liml",
            "jive"
          ],
          "type": "string"
        },
        "y": {
          "description": "Outcome column.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "endog",
        "instruments"
      ],
      "type": "object"
    }
  },
  {
    "description": "Leaderboard + winner from :func:`auto_iv`.",
    "name": "AutoIVResult",
    "parameters": {
      "properties": {
        "candidates": {
          "description": "candidates parameter (Dict[str, Any]).",
          "type": "object"
        },
        "leaderboard": {
          "description": "leaderboard parameter (pd.DataFrame).",
          "type": "string"
        },
        "selection_rule": {
          "default": "median",
          "description": "selection_rule parameter (str).",
          "type": "string"
        },
        "winner": {
          "description": "winner parameter.",
          "type": "string"
        }
      },
      "required": [
        "leaderboard",
        "winner",
        "candidates"
      ],
      "type": "object"
    }
  },
  {
    "description": "Matrix-completion causal panel estimator (Athey et al., 2021). Validation: validated evidence tier (known-truth, reference, external-parity, or Monte Carlo artifact).",
    "name": "matrix_completion",
    "parameters": {
      "properties": {
        "d": {
          "description": "d parameter (str).",
          "type": "string"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "time": {
          "description": "Time period column.",
          "type": "string"
        },
        "unit": {
          "description": "Unit identifier column.",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "d",
        "unit",
        "time"
      ],
      "type": "object"
    }
  },
  {
    "description": "Causal-discovery dispatcher -- article-facing alias.",
    "name": "causal_discovery",
    "parameters": {
      "properties": {
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "method": {
          "default": "notears",
          "description": "Estimator or algorithm variant to use.",
          "type": "string"
        },
        "variables": {
          "description": "variables parameter (Optional[List[str]]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "required": [
        "data"
      ],
      "type": "object"
    }
  },
  {
    "description": "Causal-mediation analysis -- article-facing alias for Validation: certified parity evidence.",
    "name": "mediation",
    "parameters": {
      "properties": {
        "X": {
          "description": "Feature matrix or covariate DataFrame.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "d": {
          "description": "d parameter (str).",
          "type": "string"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "m": {
          "description": "m parameter (str).",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "d",
        "m"
      ],
      "type": "object"
    }
  },
  {
    "description": "E-value computed directly from a risk ratio and its CI bounds. Validation: certified parity evidence.",
    "name": "evalue_rr",
    "parameters": {
      "properties": {
        "rare_outcome": {
          "default": false,
          "description": "Passed through to :func:`evalue` for rare-outcome OR->RR correction.",
          "type": "boolean"
        },
        "rr": {
          "description": "Point-estimate risk ratio.",
          "type": "number"
        },
        "rr_lower": {
          "description": "Optional confidence-interval bounds on the risk ratio scale.",
          "type": "number"
        },
        "rr_upper": {
          "description": "Optional confidence-interval bounds on the risk ratio scale.",
          "type": "number"
        }
      },
      "required": [
        "rr"
      ],
      "type": "object"
    }
  },
  {
    "description": "Result of an IPCW fit.",
    "name": "IPCWResult",
    "parameters": {
      "properties": {
        "fitted_hazards": {
          "default": null,
          "description": "fitted_hazards parameter (np.ndarray).",
          "type": "string"
        },
        "method": {
          "description": "Estimator or algorithm variant to use.",
          "type": "string"
        },
        "stabilized": {
          "description": "stabilized parameter (bool).",
          "type": "boolean"
        },
        "summary_stats": {
          "description": "summary_stats parameter (dict).",
          "type": "object"
        },
        "weights": {
          "description": "Observation weights.",
          "type": "string"
        }
      },
      "required": [
        "weights",
        "stabilized",
        "summary_stats",
        "method"
      ],
      "type": "object"
    }
  },
  {
    "description": "Alias for ``icp(..., method='nonlinear')`` -- Heinze-Deml et al. 2018.",
    "name": "nonlinear_icp",
    "parameters": {
      "properties": {
        "X": {
          "description": "Feature matrix or covariate DataFrame.",
          "type": "string"
        },
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "environment": {
          "description": "environment parameter (np.ndarray).",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "X",
        "y",
        "environment"
      ],
      "type": "object"
    }
  },
  {
    "description": "Result of an Invariant Causal Prediction run (:func:`icp`).",
    "name": "ICPResult",
    "parameters": {
      "properties": {
        "accepted_subsets": {
          "description": "accepted_subsets parameter (list[frozenset]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "alpha": {
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "coefficients": {
          "description": "coefficients parameter (dict[str, tuple[float, float]]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "method": {
          "description": "Estimator or algorithm variant to use.",
          "type": "string"
        },
        "parents": {
          "description": "parents parameter (set[str]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "rejection_reason": {
          "description": "rejection_reason parameter (dict[frozenset, str]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "required": [
        "parents",
        "accepted_subsets",
        "rejection_reason",
        "alpha",
        "coefficients",
        "method"
      ],
      "type": "object"
    }
  },
  {
    "description": "Outcome of an identification query.",
    "name": "IdentificationResult",
    "parameters": {
      "properties": {
        "c_components": {
          "description": "c_components parameter (list[NodeSet]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "estimand": {
          "description": "estimand parameter (str).",
          "type": "string"
        },
        "explanation": {
          "description": "explanation parameter (str).",
          "type": "string"
        },
        "hedge": {
          "description": "hedge parameter (tuple[frozenset[str], frozenset[str]] | None).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "identifiable": {
          "description": "identifiable parameter (bool).",
          "type": "boolean"
        }
      },
      "required": [
        "identifiable",
        "estimand",
        "c_components",
        "hedge",
        "explanation"
      ],
      "type": "object"
    }
  },
  {
    "description": "Check Rule 1: can we *insert or delete observation* of Z?",
    "name": "do_rule1",
    "parameters": {
      "properties": {
        "W": {
          "description": "Covariates, proxy variables, or weights used by this estimator.",
          "type": "string"
        },
        "X": {
          "description": "Feature matrix or covariate DataFrame.",
          "type": "string"
        },
        "Y": {
          "description": "Y parameter (NodeInput).",
          "type": "string"
        },
        "Z": {
          "description": "Instrument matrix or auxiliary covariate matrix.",
          "type": "string"
        },
        "dag": {
          "description": "dag parameter.",
          "type": "string"
        }
      },
      "required": [
        "dag",
        "Y",
        "X",
        "Z"
      ],
      "type": "object"
    }
  },
  {
    "description": "Check Rule 2: can do(Z) be swapped for observing Z?",
    "name": "do_rule2",
    "parameters": {
      "properties": {
        "W": {
          "description": "Covariates, proxy variables, or weights used by this estimator.",
          "type": "string"
        },
        "X": {
          "description": "Feature matrix or covariate DataFrame.",
          "type": "string"
        },
        "Y": {
          "description": "Y parameter (NodeInput).",
          "type": "string"
        },
        "Z": {
          "description": "Instrument matrix or auxiliary covariate matrix.",
          "type": "string"
        },
        "dag": {
          "description": "dag parameter.",
          "type": "string"
        }
      },
      "required": [
        "dag",
        "Y",
        "X",
        "Z"
      ],
      "type": "object"
    }
  },
  {
    "description": "Check Rule 3: can we delete do(Z)?",
    "name": "do_rule3",
    "parameters": {
      "properties": {
        "W": {
          "description": "Covariates, proxy variables, or weights used by this estimator.",
          "type": "string"
        },
        "X": {
          "description": "Feature matrix or covariate DataFrame.",
          "type": "string"
        },
        "Y": {
          "description": "Y parameter (NodeInput).",
          "type": "string"
        },
        "Z": {
          "description": "Instrument matrix or auxiliary covariate matrix.",
          "type": "string"
        },
        "dag": {
          "description": "dag parameter.",
          "type": "string"
        }
      },
      "required": [
        "dag",
        "Y",
        "X",
        "Z"
      ],
      "type": "object"
    }
  },
  {
    "description": "Try all three rules and return every applicable simplification.",
    "name": "do_calculus_apply",
    "parameters": {
      "properties": {
        "W": {
          "description": "Covariates, proxy variables, or weights used by this estimator.",
          "type": "string"
        },
        "X": {
          "description": "Feature matrix or covariate DataFrame.",
          "type": "string"
        },
        "Y": {
          "description": "Y parameter (NodeInput).",
          "type": "string"
        },
        "Z": {
          "description": "Instrument matrix or auxiliary covariate matrix.",
          "type": "string"
        },
        "dag": {
          "description": "dag parameter.",
          "type": "string"
        }
      },
      "required": [
        "dag",
        "Y",
        "X",
        "Z"
      ],
      "type": "object"
    }
  },
  {
    "description": "Result of checking one do-calculus rule on a DAG.",
    "name": "RuleCheck",
    "parameters": {
      "properties": {
        "applicable": {
          "description": "applicable parameter (bool).",
          "type": "boolean"
        },
        "reason": {
          "description": "reason parameter (str).",
          "type": "string"
        },
        "rule": {
          "description": "rule parameter (int).",
          "type": "integer"
        },
        "transformed": {
          "description": "transformed parameter (str).",
          "type": "string"
        }
      },
      "required": [
        "applicable",
        "rule",
        "reason",
        "transformed"
      ],
      "type": "object"
    }
  },
  {
    "description": "Single World Intervention Graph for a DAG under ``do(X=x)``.",
    "name": "SWIGGraph",
    "parameters": {
      "properties": {
        "intervention": {
          "description": "intervention parameter (Mapping[str, str]).",
          "type": "object"
        },
        "parent": {
          "description": "parent parameter.",
          "type": "string"
        }
      },
      "required": [
        "parent",
        "intervention"
      ],
      "type": "object"
    }
  },
  {
    "description": "Structural Causal Model.",
    "name": "SCM",
    "parameters": {
      "properties": {
        "equations": {
          "default": null,
          "description": "equations parameter (dict[str, dict[str, Any]]).",
          "type": "object"
        }
      },
      "required": [],
      "type": "object"
    }
  },
  {
    "description": "Minimal CEVAE. Uses PyTorch if available, else a light numpy",
    "name": "CEVAE",
    "parameters": {
      "properties": {
        "hidden": {
          "default": 32,
          "description": "hidden parameter (int).",
          "type": "integer"
        },
        "lr": {
          "default": 0.01,
          "description": "lr parameter (float).",
          "type": "number"
        },
        "n_epochs": {
          "default": 200,
          "description": "Number of epochs.",
          "type": "integer"
        },
        "seed": {
          "default": 0,
          "description": "Random seed for reproducible stochastic steps.",
          "type": "integer"
        },
        "z_dim": {
          "default": 4,
          "description": "z_dim parameter (int).",
          "type": "integer"
        }
      },
      "required": [],
      "type": "object"
    }
  },
  {
    "description": "Container for CEVAE estimates (ATE, unit-level ITE, loss history).",
    "name": "CEVAEResult",
    "parameters": {
      "properties": {
        "ate": {
          "description": "ate parameter (float).",
          "type": "number"
        },
        "backend": {
          "description": "backend parameter (str).",
          "type": "string"
        },
        "ite": {
          "description": "ite parameter (np.ndarray).",
          "type": "string"
        },
        "loss_history": {
          "description": "loss_history parameter (list[float]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "required": [
        "ate",
        "ite",
        "loss_history",
        "backend"
      ],
      "type": "object"
    }
  },
  {
    "description": "Formal 7-component target trial protocol.",
    "name": "TargetTrialProtocol",
    "parameters": {
      "properties": {
        "analysis_plan": {
          "description": "Which estimator will recover the target estimand (e.g. ``\"IPW + Cox\"``, ``\"g-formula + pooled logistic\"``).",
          "type": "string"
        },
        "assignment": {
          "description": "\"randomization\" (for RCT) or \"observational emulation\" (conditional exchangeability assumed given baseline covariates).",
          "type": "string"
        },
        "baseline_covariates": {
          "default": null,
          "description": "Covariates measured at or before time zero that render treatment assignment conditionally exchangeable.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "causal_contrast": {
          "description": "One of ``\"ITT\"``, ``\"per-protocol\"``, ``\"as-treated\"``, ``\"observational-analogue\"``.",
          "type": "string"
        },
        "eligibility": {
          "description": "Entry criteria at time zero. May be a SQL-like string filter, a list of human-readable conditions, or a predicate that takes a DataFrame row and returns bool.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "followup_end": {
          "description": "Administrative censoring rule (e.g. ``\"min(event, loss, 2026-01-01)\"``).",
          "type": "string"
        },
        "notes": {
          "default": "",
          "description": "Free-form notes (e.g. known sources of confounding).",
          "type": "string"
        },
        "outcome": {
          "description": "Primary outcome definition.",
          "type": "string"
        },
        "time_varying_covariates": {
          "default": null,
          "description": "Post-baseline covariates affected by prior treatment; trigger g-methods (MSM / parametric g-formula / LTMLE).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "time_zero": {
          "description": "Explicit rule defining time zero -- usually the moment of eligibility + treatment assignment alignment. This is the single most important field for preventing immortal time bias.",
          "type": "string"
        },
        "treatment_strategies": {
          "description": "Named arms being contrasted, e.g. ``[\"initiate statin at t0\", \"no statin\"]``. At least two strategies required.",
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "required": [
        "eligibility",
        "treatment_strategies",
        "assignment",
        "time_zero",
        "followup_end",
        "outcome",
        "causal_contrast",
        "analysis_plan"
      ],
      "type": "object"
    }
  },
  {
    "description": "Result of target trial emulation.",
    "name": "TargetTrialResult",
    "parameters": {
      "properties": {
        "ci": {
          "description": "ci parameter (tuple[float, float]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "estimate": {
          "description": "estimate parameter (float).",
          "type": "number"
        },
        "method": {
          "description": "Estimator or algorithm variant to use.",
          "type": "string"
        },
        "n_eligible": {
          "description": "Number of eligible.",
          "type": "integer"
        },
        "n_excluded_immortal": {
          "description": "Number of excluded immortal.",
          "type": "integer"
        },
        "protocol": {
          "description": "protocol parameter (TargetTrialProtocol).",
          "type": "string"
        },
        "se": {
          "description": "se parameter (float).",
          "type": "number"
        },
        "weights": {
          "description": "Observation weights.",
          "type": "string"
        }
      },
      "required": [
        "protocol",
        "estimate",
        "se",
        "ci",
        "n_eligible",
        "n_excluded_immortal",
        "weights",
        "method"
      ],
      "type": "object"
    }
  },
  {
    "description": "Cloned, censored and IP-of-censoring-weighted target-trial data.",
    "name": "CloneCensorWeightResult",
    "parameters": {
      "properties": {
        "cloned_data": {
          "description": "cloned_data parameter (pd.DataFrame).",
          "type": "string"
        },
        "n_clones": {
          "description": "Number of clones.",
          "type": "integer"
        },
        "n_originals": {
          "description": "Number of originals.",
          "type": "integer"
        },
        "strategies": {
          "description": "strategies parameter (list[str]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "weights_summary": {
          "description": "weights_summary parameter (dict).",
          "type": "object"
        }
      },
      "required": [
        "cloned_data",
        "strategies",
        "n_originals",
        "n_clones",
        "weights_summary"
      ],
      "type": "object"
    }
  },
  {
    "description": "Emulate the target trial defined by ``protocol``. Validation: validated evidence tier (known-truth, reference, external-parity, or Monte Carlo artifact).",
    "name": "target_trial_emulate",
    "parameters": {
      "properties": {
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "outcome_col": {
          "description": "Outcome variable column name.",
          "type": "string"
        },
        "protocol": {
          "description": "protocol parameter (TargetTrialProtocol).",
          "type": "string"
        },
        "time_zero_filter": {
          "description": "time_zero_filter parameter (Callable[[pd.DataFrame], pd.Series] | None).",
          "type": "string"
        },
        "treatment_col": {
          "description": "Treatment variable column name.",
          "type": "string"
        },
        "weights": {
          "description": "Observation weights.",
          "type": "string"
        }
      },
      "required": [
        "protocol",
        "data",
        "outcome_col",
        "treatment_col"
      ],
      "type": "object"
    }
  },
  {
    "description": "Flag subjects whose follow-up begins *before* treatment Validation: validated evidence tier (known-truth, reference, external-parity, or Monte Carlo artifact).",
    "name": "immortal_time_check",
    "parameters": {
      "properties": {
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "eligibility_time_col": {
          "description": "The protocol's defined time zero.",
          "type": "string"
        },
        "id_col": {
          "description": "Column name for id.",
          "type": "string"
        },
        "time_col": {
          "description": "Follow-up time column.",
          "type": "string"
        },
        "treatment_start_col": {
          "description": "Time of treatment initiation (NaN / ``-inf`` if never treated).",
          "type": "string"
        }
      },
      "required": [
        "data",
        "id_col",
        "time_col",
        "treatment_start_col",
        "eligibility_time_col"
      ],
      "type": "object"
    }
  },
  {
    "description": "Result of density-ratio (inverse-odds-of-sampling) transport weighting.",
    "name": "TransportWeightResult",
    "parameters": {
      "properties": {
        "effect_source": {
          "description": "effect_source parameter (float).",
          "type": "number"
        },
        "effect_transported": {
          "description": "effect_transported parameter (float).",
          "type": "number"
        },
        "ess": {
          "description": "ess parameter (float).",
          "type": "number"
        },
        "max_weight": {
          "description": "max_weight parameter (float).",
          "type": "number"
        },
        "se_transported": {
          "description": "se_transported parameter (float).",
          "type": "number"
        },
        "weights": {
          "description": "Observation weights.",
          "type": "string"
        }
      },
      "required": [
        "weights",
        "ess",
        "max_weight",
        "effect_source",
        "effect_transported",
        "se_transported"
      ],
      "type": "object"
    }
  },
  {
    "description": "Result of a Pearl-Bareinboim transportability identification check.",
    "name": "TransportIdentificationResult",
    "parameters": {
      "properties": {
        "admissible_set": {
          "description": "admissible_set parameter (frozenset).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "formula": {
          "description": "Model formula using patsy/R-style syntax.",
          "type": "string"
        },
        "reason": {
          "description": "reason parameter (str).",
          "type": "string"
        },
        "transportable": {
          "description": "transportable parameter (bool).",
          "type": "boolean"
        }
      },
      "required": [
        "transportable",
        "formula",
        "admissible_set",
        "reason"
      ],
      "type": "object"
    }
  },
  {
    "description": "Transport an RCT effect to ``target_population``. Validation: validated evidence tier (known-truth, reference, external-parity, or Monte Carlo artifact).",
    "name": "transport_generalize",
    "parameters": {
      "properties": {
        "features": {
          "description": "Feature matrix or feature column names.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "outcome": {
          "default": "y",
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        },
        "rct": {
          "description": "rct parameter (pd.DataFrame).",
          "type": "string"
        },
        "target_population": {
          "description": "target_population parameter (pd.DataFrame).",
          "type": "string"
        },
        "treatment": {
          "default": "treat",
          "description": "Treatment indicator, treatment variable, or treatment array.",
          "type": "string"
        }
      },
      "required": [
        "rct",
        "target_population",
        "features"
      ],
      "type": "object"
    }
  },
  {
    "description": "Result of the iterative conditional expectation (ICE) g-formula.",
    "name": "ICEResult",
    "parameters": {
      "properties": {
        "ci": {
          "description": "ci parameter (tuple[float, float]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "method": {
          "default": "parametric-g-formula-ICE",
          "description": "Estimator or algorithm variant to use.",
          "type": "string"
        },
        "per_timepoint_means": {
          "description": "per_timepoint_means parameter (list[float] | None).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "se": {
          "description": "se parameter (float).",
          "type": "number"
        },
        "strategy": {
          "description": "strategy parameter (list).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "value": {
          "description": "value parameter (float).",
          "type": "number"
        }
      },
      "required": [
        "strategy",
        "value",
        "se",
        "ci"
      ],
      "type": "object"
    }
  },
  {
    "description": "Monte-Carlo parametric g-formula estimate of :math:`E[Y(g)]`. Validation: validated evidence tier (known-truth, reference, external-parity, or Monte Carlo artifact).",
    "name": "gformula_mc",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "bootstrap": {
          "default": 200,
          "description": "Non-parametric bootstrap replicates for SE / CI. Set to 0 to skip inference (point estimate only).",
          "type": "integer"
        },
        "confounder_cols": {
          "description": "Either a flat list (same confounders at every timepoint) or a list-of-lists with per-timepoint confounder sets. A confounder whose name ends in an index / time tag can be supplied as a flat list; for true time-varying confounders supply the nested form so the g-formula respects the correct temporal ordering.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "control_strategy": {
          "description": "If provided, a second arm is simulated under the control strategy and a contrast (treat - control) is reported.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "data": {
          "description": "One row per subject. Must contain all treatment, confounder and outcome columns.",
          "type": "string"
        },
        "id_col": {
          "description": "Unused when ``data`` is already wide; kept for API symmetry",
          "type": "string"
        },
        "n_simulations": {
          "default": 10000,
          "description": "Monte-Carlo trajectories per arm.",
          "type": "integer"
        },
        "outcome_col": {
          "description": "Terminal outcome column.",
          "type": "string"
        },
        "return_trajectories": {
          "default": false,
          "description": "Attach the simulated trajectories DataFrame to the result.",
          "type": "boolean"
        },
        "seed": {
          "description": "Random seed for reproducible stochastic steps.",
          "type": "integer"
        },
        "strategy": {
          "default": [
            1,
            1,
            1
          ],
          "description": "Either a static sequence of treatment values (e.g. ``[1]*K`` = always-treat), or a callable with signature ``strategy(t: int, history: dict) -> np.ndarray of length n_sim`` for dynamic regimes that depend on simulated state.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "time_col": {
          "description": "Unused when ``data`` is already wide; kept for API symmetry",
          "type": "string"
        },
        "treatment_cols": {
          "description": "Treatment column names, chronologically ordered.",
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "required": [
        "data",
        "treatment_cols",
        "confounder_cols",
        "outcome_col"
      ],
      "type": "object"
    }
  },
  {
    "description": "Result of one or two Monte-Carlo g-formula arms.",
    "name": "MCGFormulaResult",
    "parameters": {
      "properties": {
        "ci": {
          "description": "ci parameter (tuple).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "contrast_ci": {
          "description": "contrast_ci parameter (Optional[tuple]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "contrast_se": {
          "description": "contrast_se parameter (Optional[float]).",
          "type": "number"
        },
        "contrast_value": {
          "description": "contrast_value parameter (Optional[float]).",
          "type": "number"
        },
        "method": {
          "default": "MC-parametric-g-formula",
          "description": "Estimator or algorithm variant to use.",
          "type": "string"
        },
        "n_simulations": {
          "description": "Number of simulations.",
          "type": "integer"
        },
        "se": {
          "description": "se parameter (float).",
          "type": "number"
        },
        "strategies": {
          "description": "strategies parameter (Optional[dict]).",
          "type": "object"
        },
        "trajectories": {
          "description": "trajectories parameter (Optional[pd.DataFrame]).",
          "type": "string"
        },
        "value": {
          "description": "value parameter (float).",
          "type": "number"
        }
      },
      "required": [
        "value",
        "se",
        "ci",
        "n_simulations"
      ],
      "type": "object"
    }
  },
  {
    "description": "Number needed to treat (or harm), defined as |1 / RD|. Validation: certified parity evidence.",
    "name": "number_needed_to_treat",
    "parameters": {
      "properties": {
        "a": {
          "description": "a parameter.",
          "type": "string"
        },
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "b": {
          "description": "b parameter (Optional[float]).",
          "type": "number"
        },
        "c": {
          "description": "c parameter (Optional[float]).",
          "type": "number"
        },
        "d": {
          "description": "d parameter (Optional[float]).",
          "type": "number"
        }
      },
      "required": [
        "a"
      ],
      "type": "object"
    }
  },
  {
    "description": "Prevalence ratio (cross-sectional RR); mathematically identical Validation: certified parity evidence.",
    "name": "prevalence_ratio",
    "parameters": {
      "properties": {
        "a": {
          "description": "a parameter.",
          "type": "string"
        },
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "b": {
          "description": "b parameter (Optional[float]).",
          "type": "number"
        },
        "c": {
          "description": "c parameter (Optional[float]).",
          "type": "number"
        },
        "d": {
          "description": "d parameter (Optional[float]).",
          "type": "number"
        }
      },
      "required": [
        "a"
      ],
      "type": "object"
    }
  },
  {
    "description": "Alias for :func:`sensitivity_specificity`. Validation: certified parity evidence.",
    "name": "diagnostic_test",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "ci_method": {
          "default": "wilson",
          "description": "ci_method parameter (str).",
          "type": "string"
        },
        "fn": {
          "description": "fn parameter (Optional[int]).",
          "type": "integer"
        },
        "fp": {
          "description": "fp parameter (Optional[int]).",
          "type": "integer"
        },
        "tn": {
          "description": "tn parameter (Optional[int]).",
          "type": "integer"
        },
        "tp": {
          "description": "tp parameter (Optional[int]).",
          "type": "integer"
        },
        "y_pred": {
          "description": "y_pred parameter.",
          "type": "string"
        },
        "y_true": {
          "description": "y_true parameter.",
          "type": "string"
        }
      },
      "required": [],
      "type": "object"
    }
  },
  {
    "description": "Shortcut: just return the AUC. Validation: certified parity evidence.",
    "name": "auc",
    "parameters": {
      "properties": {
        "scores": {
          "description": "scores parameter.",
          "type": "string"
        },
        "y_true": {
          "description": "y_true parameter.",
          "type": "string"
        }
      },
      "required": [
        "y_true",
        "scores"
      ],
      "type": "object"
    }
  },
  {
    "description": "Container for binary diagnostic-test performance metrics.",
    "name": "DiagnosticTestResult",
    "parameters": {
      "properties": {
        "fn": {
          "description": "fn parameter (int).",
          "type": "integer"
        },
        "fp": {
          "description": "fp parameter (int).",
          "type": "integer"
        },
        "lr_neg": {
          "description": "lr_neg parameter (float).",
          "type": "number"
        },
        "lr_pos": {
          "description": "lr_pos parameter (float).",
          "type": "number"
        },
        "npv": {
          "description": "npv parameter (float).",
          "type": "number"
        },
        "ppv": {
          "description": "ppv parameter (float).",
          "type": "number"
        },
        "prevalence": {
          "description": "prevalence parameter (float).",
          "type": "number"
        },
        "sensitivity": {
          "description": "sensitivity parameter (float).",
          "type": "number"
        },
        "sensitivity_ci": {
          "description": "sensitivity_ci parameter (tuple[float, float]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "specificity": {
          "description": "specificity parameter (float).",
          "type": "number"
        },
        "specificity_ci": {
          "description": "specificity_ci parameter (tuple[float, float]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "tn": {
          "description": "tn parameter (int).",
          "type": "integer"
        },
        "tp": {
          "description": "tp parameter (int).",
          "type": "integer"
        }
      },
      "required": [
        "sensitivity",
        "sensitivity_ci",
        "specificity",
        "specificity_ci",
        "ppv",
        "npv",
        "lr_pos",
        "lr_neg",
        "prevalence",
        "tp",
        "fp",
        "fn",
        "tn"
      ],
      "type": "object"
    }
  },
  {
    "description": "Container for ROC-curve coordinates and AUC inference.",
    "name": "ROCResult",
    "parameters": {
      "properties": {
        "auc": {
          "description": "auc parameter (float).",
          "type": "number"
        },
        "auc_ci": {
          "description": "auc_ci parameter (tuple[float, float]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "auc_se": {
          "description": "auc_se parameter (float).",
          "type": "number"
        },
        "fpr": {
          "description": "fpr parameter (np.ndarray).",
          "type": "string"
        },
        "se_method": {
          "default": "delong",
          "description": "se_method parameter (str).",
          "type": "string"
        },
        "thresholds": {
          "description": "thresholds parameter (np.ndarray).",
          "type": "string"
        },
        "tpr": {
          "description": "tpr parameter (np.ndarray).",
          "type": "string"
        }
      },
      "required": [
        "thresholds",
        "tpr",
        "fpr",
        "auc",
        "auc_se",
        "auc_ci"
      ],
      "type": "object"
    }
  },
  {
    "description": "Container for Cohen's kappa inter-rater agreement.",
    "name": "KappaResult",
    "parameters": {
      "properties": {
        "ci": {
          "description": "ci parameter (tuple[float, float]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "expected_agreement": {
          "description": "expected_agreement parameter (float).",
          "type": "number"
        },
        "kappa": {
          "description": "kappa parameter (float).",
          "type": "number"
        },
        "n_categories": {
          "description": "Number of categories.",
          "type": "integer"
        },
        "observed_agreement": {
          "description": "observed_agreement parameter (float).",
          "type": "number"
        },
        "p_value": {
          "description": "p_value parameter (float).",
          "type": "number"
        },
        "se": {
          "description": "se parameter (float).",
          "type": "number"
        },
        "weights": {
          "description": "Observation weights.",
          "type": "string"
        },
        "z": {
          "description": "Instrument, proxy, or auxiliary variable used by this estimator.",
          "type": "number"
        }
      },
      "required": [
        "kappa",
        "se",
        "ci",
        "observed_agreement",
        "expected_agreement",
        "n_categories",
        "weights",
        "z",
        "p_value"
      ],
      "type": "object"
    }
  },
  {
    "description": "Container for Cochran's Q / Rucker's Q' heterogeneity diagnostics.",
    "name": "HeterogeneityResult",
    "parameters": {
      "properties": {
        "I2": {
          "description": "I2 parameter (float).",
          "type": "number"
        },
        "Q": {
          "description": "Q parameter (float).",
          "type": "number"
        },
        "Q_df": {
          "description": "Q_df parameter (int).",
          "type": "integer"
        },
        "Q_p": {
          "description": "Q_p parameter (float).",
          "type": "number"
        },
        "method": {
          "description": "Estimator or algorithm variant to use.",
          "type": "string"
        }
      },
      "required": [
        "Q",
        "Q_df",
        "Q_p",
        "I2",
        "method"
      ],
      "type": "object"
    }
  },
  {
    "description": "Container for the MR-Egger intercept (directional-pleiotropy) test.",
    "name": "PleiotropyResult",
    "parameters": {
      "properties": {
        "intercept": {
          "description": "intercept parameter (float).",
          "type": "number"
        },
        "p_value": {
          "description": "p_value parameter (float).",
          "type": "number"
        },
        "se": {
          "description": "se parameter (float).",
          "type": "number"
        }
      },
      "required": [
        "intercept",
        "se",
        "p_value"
      ],
      "type": "object"
    }
  },
  {
    "description": "Container for leave-one-out IVW estimates.",
    "name": "LeaveOneOutResult",
    "parameters": {
      "properties": {
        "table": {
          "description": "table parameter (pd.DataFrame).",
          "type": "string"
        }
      },
      "required": [
        "table"
      ],
      "type": "object"
    }
  },
  {
    "description": "Container for the Steiger directionality test.",
    "name": "SteigerResult",
    "parameters": {
      "properties": {
        "correct_direction": {
          "description": "correct_direction parameter (bool).",
          "type": "boolean"
        },
        "r2_exposure": {
          "description": "r2_exposure parameter (float).",
          "type": "number"
        },
        "r2_outcome": {
          "description": "r2_outcome parameter (float).",
          "type": "number"
        },
        "sample_size_exposure": {
          "description": "sample_size_exposure parameter (int).",
          "type": "integer"
        },
        "sample_size_outcome": {
          "description": "sample_size_outcome parameter (int).",
          "type": "integer"
        },
        "steiger_pvalue": {
          "description": "steiger_pvalue parameter (float).",
          "type": "number"
        }
      },
      "required": [
        "correct_direction",
        "steiger_pvalue",
        "r2_exposure",
        "r2_outcome",
        "sample_size_exposure",
        "sample_size_outcome"
      ],
      "type": "object"
    }
  },
  {
    "description": "Container for MR-PRESSO global test and outlier-corrected estimates.",
    "name": "MRPressoResult",
    "parameters": {
      "properties": {
        "distortion_coefficient": {
          "description": "distortion_coefficient parameter (Optional[float]).",
          "type": "number"
        },
        "distortion_p": {
          "description": "distortion_p parameter (Optional[float]).",
          "type": "number"
        },
        "global_test_pvalue": {
          "default": 1.0,
          "description": "global_test_pvalue parameter (float).",
          "type": "number"
        },
        "global_test_rss_obs": {
          "default": 0.0,
          "description": "global_test_rss_obs parameter (float).",
          "type": "number"
        },
        "outlier_corrected_estimate": {
          "description": "outlier_corrected_estimate parameter (Optional[float]).",
          "type": "number"
        },
        "outlier_corrected_p": {
          "description": "outlier_corrected_p parameter (Optional[float]).",
          "type": "number"
        },
        "outlier_corrected_se": {
          "description": "outlier_corrected_se parameter (Optional[float]).",
          "type": "number"
        },
        "outlier_pvalues": {
          "description": "outlier_pvalues parameter (Optional[List[float]]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "outliers": {
          "default": null,
          "description": "outliers parameter (List[int]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "raw_estimate": {
          "description": "raw_estimate parameter (float).",
          "type": "number"
        },
        "raw_p": {
          "description": "raw_p parameter (float).",
          "type": "number"
        },
        "raw_se": {
          "description": "raw_se parameter (float).",
          "type": "number"
        }
      },
      "required": [
        "raw_estimate",
        "raw_se",
        "raw_p",
        "outlier_corrected_estimate",
        "outlier_corrected_se",
        "outlier_corrected_p"
      ],
      "type": "object"
    }
  },
  {
    "description": "Container for radial-MR diagnostics.",
    "name": "RadialResult",
    "parameters": {
      "properties": {
        "Q_pvalue": {
          "description": "Q_pvalue parameter (float).",
          "type": "number"
        },
        "outliers": {
          "description": "outliers parameter (list[int]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "table": {
          "description": "table parameter (pd.DataFrame).",
          "type": "string"
        },
        "total_Q": {
          "description": "total_Q parameter (float).",
          "type": "number"
        }
      },
      "required": [
        "table",
        "total_Q",
        "Q_pvalue",
        "outliers"
      ],
      "type": "object"
    }
  },
  {
    "description": "Funnel plot of SNP-specific Wald ratios vs. precision.",
    "name": "mr_funnel_plot",
    "parameters": {
      "properties": {
        "ax": {
          "description": "ax parameter.",
          "type": "string"
        },
        "beta_exposure": {
          "description": "beta_exposure parameter (np.ndarray).",
          "type": "string"
        },
        "beta_outcome": {
          "description": "beta_outcome parameter (np.ndarray).",
          "type": "string"
        },
        "se_outcome": {
          "description": "se_outcome parameter (np.ndarray).",
          "type": "string"
        },
        "snp_ids": {
          "description": "snp_ids parameter (Optional[List[str]]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "required": [
        "beta_exposure",
        "beta_outcome",
        "se_outcome"
      ],
      "type": "object"
    }
  },
  {
    "description": "Classic MR scatter plot with IVW and MR-Egger lines.",
    "name": "mr_scatter_plot",
    "parameters": {
      "properties": {
        "ax": {
          "description": "ax parameter.",
          "type": "string"
        },
        "beta_exposure": {
          "description": "beta_exposure parameter (np.ndarray).",
          "type": "string"
        },
        "beta_outcome": {
          "description": "beta_outcome parameter (np.ndarray).",
          "type": "string"
        },
        "se_exposure": {
          "description": "se_exposure parameter (np.ndarray).",
          "type": "string"
        },
        "se_outcome": {
          "description": "se_outcome parameter (np.ndarray).",
          "type": "string"
        }
      },
      "required": [
        "beta_exposure",
        "beta_outcome",
        "se_exposure",
        "se_outcome"
      ],
      "type": "object"
    }
  },
  {
    "description": "Result container returned by :func:`mr_mode`.",
    "name": "ModeBasedResult",
    "parameters": {
      "properties": {
        "bandwidth": {
          "description": "Bandwidth used for local smoothing or kernel weighting.",
          "type": "number"
        },
        "ci": {
          "description": "ci parameter (tuple[float, float]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "estimate": {
          "description": "estimate parameter (float).",
          "type": "number"
        },
        "method": {
          "default": "weighted-mode",
          "description": "Estimator or algorithm variant to use.",
          "type": "string"
        },
        "n_snps": {
          "description": "Number of snps.",
          "type": "integer"
        },
        "p_value": {
          "description": "p_value parameter (float).",
          "type": "number"
        },
        "se": {
          "description": "se parameter (float).",
          "type": "number"
        }
      },
      "required": [
        "estimate",
        "se",
        "ci",
        "p_value",
        "n_snps",
        "bandwidth"
      ],
      "type": "object"
    }
  },
  {
    "description": "Result container returned by :func:`mr_f_statistic`.",
    "name": "FStatisticResult",
    "parameters": {
      "properties": {
        "f_max": {
          "description": "f_max parameter (float).",
          "type": "number"
        },
        "f_mean": {
          "description": "f_mean parameter (float).",
          "type": "number"
        },
        "f_min": {
          "description": "f_min parameter (float).",
          "type": "number"
        },
        "per_snp_F": {
          "description": "per_snp_F parameter (np.ndarray).",
          "type": "string"
        },
        "r2_mean": {
          "description": "r2_mean parameter (float).",
          "type": "number"
        },
        "weak_instrument_risk": {
          "description": "weak_instrument_risk parameter (bool).",
          "type": "boolean"
        }
      },
      "required": [
        "f_mean",
        "f_min",
        "f_max",
        "weak_instrument_risk",
        "r2_mean",
        "per_snp_F"
      ],
      "type": "object"
    }
  },
  {
    "description": "Convenience: the always-treat regime over K periods. Validation: validated evidence tier (known-truth, reference, external-parity, or Monte Carlo artifact).",
    "name": "always_treat",
    "parameters": {
      "properties": {
        "K": {
          "default": 1,
          "description": "K parameter (int).",
          "type": "integer"
        }
      },
      "required": [],
      "type": "object"
    }
  },
  {
    "description": "Convenience: the never-treat regime over K periods. Validation: validated evidence tier (known-truth, reference, external-parity, or Monte Carlo artifact).",
    "name": "never_treat",
    "parameters": {
      "properties": {
        "K": {
          "default": 1,
          "description": "K parameter (int).",
          "type": "integer"
        }
      },
      "required": [],
      "type": "object"
    }
  },
  {
    "description": "Result of a unified longitudinal analysis.",
    "name": "LongitudinalResult",
    "parameters": {
      "properties": {
        "ci": {
          "description": "ci parameter (tuple[float, float]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "diagnostics": {
          "default": null,
          "description": "diagnostics parameter (dict).",
          "type": "object"
        },
        "estimate": {
          "description": "estimate parameter (float).",
          "type": "number"
        },
        "method": {
          "description": "Estimator or algorithm variant to use.",
          "type": "string"
        },
        "n": {
          "description": "n parameter (int).",
          "type": "integer"
        },
        "n_periods": {
          "description": "Number of periods.",
          "type": "integer"
        },
        "regime_name": {
          "description": "regime_name parameter (str).",
          "type": "string"
        },
        "se": {
          "description": "se parameter (float).",
          "type": "number"
        },
        "underlying_result": {
          "description": "underlying_result parameter.",
          "type": "string"
        }
      },
      "required": [
        "method",
        "regime_name",
        "estimate",
        "se",
        "ci",
        "n",
        "n_periods"
      ],
      "type": "object"
    }
  },
  {
    "description": "A dynamic treatment regime.",
    "name": "Regime",
    "parameters": {
      "properties": {
        "kind": {
          "description": "kind parameter (str).",
          "type": "string"
        },
        "name": {
          "description": "name parameter (str).",
          "type": "string"
        },
        "rule": {
          "description": "rule parameter (Union[list, Callable]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "required": [
        "kind",
        "name",
        "rule"
      ],
      "type": "object"
    }
  },
  {
    "description": "Pre-registered causal question declaration.",
    "name": "CausalQuestion",
    "parameters": {
      "properties": {
        "_plan": {
          "description": "_plan parameter (Optional[IdentificationPlan]).",
          "type": "string"
        },
        "_result": {
          "description": "_result parameter (Optional[EstimationResult]).",
          "type": "string"
        },
        "cohort": {
          "description": "Treatment cohort or group identifier.",
          "type": "string"
        },
        "covariates": {
          "default": null,
          "description": "Covariate matrix, DataFrame, or column names.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "cutoff": {
          "description": "cutoff parameter (Optional[float]).",
          "type": "number"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "design": {
          "default": "auto",
          "description": "design parameter (str).",
          "type": "string"
        },
        "engine": {
          "default": "ols",
          "description": "engine parameter (str).",
          "type": "string"
        },
        "estimand": {
          "default": "ATE",
          "description": "estimand parameter (str).",
          "type": "string"
        },
        "id": {
          "description": "Unit, subject, or panel identifier column.",
          "type": "string"
        },
        "instruments": {
          "default": null,
          "description": "instruments parameter (List[str]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "notes": {
          "default": "",
          "description": "notes parameter (str).",
          "type": "string"
        },
        "outcome": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        },
        "population": {
          "default": "",
          "description": "population parameter (str).",
          "type": "string"
        },
        "running_variable": {
          "description": "running_variable parameter (Optional[str]).",
          "type": "string"
        },
        "time": {
          "description": "Time period column.",
          "type": "string"
        },
        "time_structure": {
          "default": "cross_section",
          "description": "time_structure parameter (str).",
          "type": "string"
        },
        "treatment": {
          "description": "Treatment indicator, treatment variable, or treatment array.",
          "type": "string"
        }
      },
      "required": [
        "treatment",
        "outcome"
      ],
      "type": "object"
    }
  },
  {
    "description": "Output of :meth:`CausalQuestion.identify`.",
    "name": "IdentificationPlan",
    "parameters": {
      "properties": {
        "assumptions": {
          "description": "assumptions parameter (List[str]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "estimand": {
          "description": "estimand parameter (str).",
          "type": "string"
        },
        "estimator": {
          "description": "estimator parameter (str).",
          "type": "string"
        },
        "fallback_estimators": {
          "default": null,
          "description": "fallback_estimators parameter (List[str]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "identification_story": {
          "description": "identification_story parameter (str).",
          "type": "string"
        },
        "warnings": {
          "default": null,
          "description": "warnings parameter (List[str]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "required": [
        "estimator",
        "estimand",
        "identification_story",
        "assumptions"
      ],
      "type": "object"
    }
  },
  {
    "description": "Unified view of a causal-question estimate.",
    "name": "EstimationResult",
    "parameters": {
      "properties": {
        "ci": {
          "description": "ci parameter (tuple[float, float]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "estimand": {
          "description": "estimand parameter (str).",
          "type": "string"
        },
        "estimate": {
          "description": "estimate parameter (float).",
          "type": "number"
        },
        "estimator": {
          "description": "estimator parameter (str).",
          "type": "string"
        },
        "n": {
          "description": "n parameter (int).",
          "type": "integer"
        },
        "plan": {
          "description": "plan parameter (IdentificationPlan).",
          "type": "string"
        },
        "se": {
          "description": "se parameter (float).",
          "type": "number"
        },
        "underlying": {
          "description": "underlying parameter.",
          "type": "string"
        }
      },
      "required": [
        "estimand",
        "estimator",
        "estimate",
        "se",
        "ci",
        "n",
        "underlying",
        "plan"
      ],
      "type": "object"
    }
  },
  {
    "description": "Result of a bridging-theorem comparison.",
    "name": "BridgeResult",
    "parameters": {
      "properties": {
        "detail": {
          "default": null,
          "description": "Amount of result detail to return.",
          "type": "object"
        },
        "diff": {
          "description": "diff parameter (float).",
          "type": "number"
        },
        "diff_p": {
          "description": "diff_p parameter (float).",
          "type": "number"
        },
        "diff_se": {
          "description": "diff_se parameter (float).",
          "type": "number"
        },
        "estimate_a": {
          "description": "estimate_a parameter (float).",
          "type": "number"
        },
        "estimate_b": {
          "description": "estimate_b parameter (float).",
          "type": "number"
        },
        "estimate_dr": {
          "description": "estimate_dr parameter (float).",
          "type": "number"
        },
        "kind": {
          "description": "kind parameter (str).",
          "type": "string"
        },
        "n_obs": {
          "description": "Number of obs.",
          "type": "integer"
        },
        "path_a_name": {
          "description": "path_a_name parameter (str).",
          "type": "string"
        },
        "path_b_name": {
          "description": "path_b_name parameter (str).",
          "type": "string"
        },
        "reference": {
          "default": "",
          "description": "reference parameter (str).",
          "type": "string"
        },
        "se_a": {
          "description": "se_a parameter (float).",
          "type": "number"
        },
        "se_b": {
          "description": "se_b parameter (float).",
          "type": "number"
        },
        "se_dr": {
          "description": "se_dr parameter (float).",
          "type": "number"
        }
      },
      "required": [
        "kind",
        "path_a_name",
        "path_b_name",
        "estimate_a",
        "estimate_b",
        "se_a",
        "se_b",
        "diff",
        "diff_se",
        "diff_p",
        "estimate_dr",
        "se_dr",
        "n_obs"
      ],
      "type": "object"
    }
  },
  {
    "description": "Result of :func:`did_forest`.",
    "name": "DIDForestResult",
    "parameters": {
      "properties": {
        "att_gt": {
          "description": "att_gt parameter (pd.DataFrame).",
          "type": "string"
        },
        "dropped_cells": {
          "description": "dropped_cells parameter (pd.DataFrame).",
          "type": "string"
        },
        "event_study": {
          "description": "event_study parameter (pd.DataFrame).",
          "type": "string"
        },
        "group_effects": {
          "description": "group_effects parameter (pd.DataFrame).",
          "type": "string"
        },
        "n_clusters": {
          "description": "Number of clusters.",
          "type": "integer"
        },
        "n_units": {
          "description": "Number of units.",
          "type": "integer"
        },
        "overall": {
          "description": "overall parameter (Dict[str, float]).",
          "type": "object"
        },
        "pretrend_test": {
          "description": "pretrend_test parameter (Dict[str, Any]).",
          "type": "object"
        },
        "settings": {
          "default": null,
          "description": "settings parameter (Dict[str, Any]).",
          "type": "object"
        },
        "unit_cate": {
          "description": "unit_cate parameter (pd.DataFrame).",
          "type": "string"
        }
      },
      "required": [
        "att_gt",
        "event_study",
        "overall",
        "group_effects",
        "pretrend_test",
        "unit_cate",
        "dropped_cells",
        "n_units",
        "n_clusters"
      ],
      "type": "object"
    }
  },
  {
    "description": "Debiased ML conformal counterfactual intervals.",
    "name": "conformal_debiased_ml",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.1,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "covariates": {
          "description": "Covariate matrix, DataFrame, or column names.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "n_folds": {
          "default": 5,
          "description": "Cross-fitting folds (debiased step).",
          "type": "integer"
        },
        "seed": {
          "default": 0,
          "description": "Random seed for reproducible stochastic steps.",
          "type": "integer"
        },
        "test_data": {
          "description": "test_data parameter (Optional[pd.DataFrame]).",
          "type": "string"
        },
        "treat": {
          "description": "Treatment indicator or first-treatment-period column.",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "treat",
        "covariates"
      ],
      "type": "object"
    }
  },
  {
    "description": "Debiased ML conformal counterfactual intervals.",
    "name": "DebiasedConformalResult",
    "parameters": {
      "properties": {
        "coverage_target": {
          "description": "coverage_target parameter (float).",
          "type": "number"
        },
        "intervals": {
          "description": "intervals parameter (np.ndarray).",
          "type": "string"
        },
        "n_calibration": {
          "description": "Number of calibration.",
          "type": "integer"
        },
        "n_test": {
          "description": "Number of test.",
          "type": "integer"
        },
        "point_estimate": {
          "description": "point_estimate parameter (np.ndarray).",
          "type": "string"
        }
      },
      "required": [
        "intervals",
        "point_estimate",
        "coverage_target",
        "n_calibration",
        "n_test"
      ],
      "type": "object"
    }
  },
  {
    "description": "Conditional-density conformal ITE intervals.",
    "name": "conformal_density_ite",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.1,
          "description": "Miscoverage; intervals target 1 - alpha coverage.",
          "type": "number"
        },
        "bandwidth": {
          "description": "KDE bandwidth; defaults to Silverman's rule on calibration Y.",
          "type": "number"
        },
        "covariates": {
          "description": "Covariate matrix, DataFrame, or column names.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "data": {
          "description": "Training data with both treated and control units.",
          "type": "string"
        },
        "seed": {
          "default": 0,
          "description": "Random seed for reproducible stochastic steps.",
          "type": "integer"
        },
        "test_data": {
          "description": "Test set; defaults to ``data`` (in-sample intervals).",
          "type": "string"
        },
        "treat": {
          "description": "Treatment indicator or first-treatment-period column.",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "treat",
        "covariates"
      ],
      "type": "object"
    }
  },
  {
    "description": "Conditional-density conformal ITE intervals.",
    "name": "ConformalDensityResult",
    "parameters": {
      "properties": {
        "coverage_target": {
          "description": "coverage_target parameter (float).",
          "type": "number"
        },
        "intervals": {
          "description": "intervals parameter (np.ndarray).",
          "type": "string"
        },
        "n_calibration": {
          "description": "Number of calibration.",
          "type": "integer"
        },
        "n_test": {
          "description": "Number of test.",
          "type": "integer"
        },
        "point_estimate": {
          "description": "point_estimate parameter (np.ndarray).",
          "type": "string"
        }
      },
      "required": [
        "intervals",
        "point_estimate",
        "coverage_target",
        "n_calibration",
        "n_test"
      ],
      "type": "object"
    }
  },
  {
    "description": "Fairness-aware conformal ITE intervals.",
    "name": "FairConformalResult",
    "parameters": {
      "properties": {
        "coverage_target": {
          "description": "coverage_target parameter (float).",
          "type": "number"
        },
        "group_assignment": {
          "description": "group_assignment parameter (np.ndarray).",
          "type": "string"
        },
        "group_coverage_targets": {
          "description": "group_coverage_targets parameter (Dict[str, float]).",
          "type": "object"
        },
        "group_widths": {
          "description": "group_widths parameter (Dict[str, float]).",
          "type": "object"
        },
        "intervals": {
          "description": "intervals parameter (np.ndarray).",
          "type": "string"
        },
        "point_estimate": {
          "description": "point_estimate parameter (np.ndarray).",
          "type": "string"
        }
      },
      "required": [
        "intervals",
        "point_estimate",
        "group_assignment",
        "group_widths",
        "group_coverage_targets",
        "coverage_target"
      ],
      "type": "object"
    }
  },
  {
    "description": "Multi-decision-point conformal ITE.",
    "name": "conformal_ite_multidp",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.1,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "data": {
          "description": "Long-format wide data: each row = subject; columns include stage-specific outcomes / treatments / histories.",
          "type": "string"
        },
        "history_per_stage": {
          "description": "History (covariates available at decision time k).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "seed": {
          "default": 0,
          "description": "Random seed for reproducible stochastic steps.",
          "type": "integer"
        },
        "test_data": {
          "description": "test_data parameter (Optional[pd.DataFrame]).",
          "type": "string"
        },
        "treat_per_stage": {
          "description": "Binary treatment at each stage.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y_per_stage": {
          "description": "Outcome column at each stage k = 1, ..., K.",
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "required": [
        "data",
        "y_per_stage",
        "treat_per_stage",
        "history_per_stage"
      ],
      "type": "object"
    }
  },
  {
    "description": "Multi-decision-point conformal ITE intervals.",
    "name": "MultiDPConformalResult",
    "parameters": {
      "properties": {
        "coverage_target": {
          "description": "coverage_target parameter (float).",
          "type": "number"
        },
        "cumulative_interval": {
          "description": "cumulative_interval parameter (np.ndarray).",
          "type": "string"
        },
        "intervals_per_stage": {
          "description": "intervals_per_stage parameter (List[np.ndarray]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "n_stages": {
          "description": "Number of stages.",
          "type": "integer"
        },
        "n_test": {
          "description": "Number of test.",
          "type": "integer"
        }
      },
      "required": [
        "intervals_per_stage",
        "cumulative_interval",
        "coverage_target",
        "n_stages",
        "n_test"
      ],
      "type": "object"
    }
  },
  {
    "description": "Score and rank candidate proxies for PCI.",
    "name": "select_pci_proxies",
    "parameters": {
      "properties": {
        "candidates": {
          "description": "All variables that could plausibly serve as proxies.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "covariates": {
          "description": "Covariate matrix, DataFrame, or column names.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "top_k": {
          "default": 2,
          "description": "Number of top candidates to recommend per side.",
          "type": "integer"
        },
        "treat": {
          "description": "Treatment indicator or first-treatment-period column.",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "treat",
        "candidates"
      ],
      "type": "object"
    }
  },
  {
    "description": "Per-candidate proxy score for PCI.",
    "name": "ProxyScoreResult",
    "parameters": {
      "properties": {
        "recommended_w": {
          "description": "recommended_w parameter (List[str]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "recommended_z": {
          "description": "recommended_z parameter (List[str]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "w_candidates": {
          "description": "w_candidates parameter (pd.DataFrame).",
          "type": "string"
        },
        "z_candidates": {
          "description": "z_candidates parameter (pd.DataFrame).",
          "type": "string"
        }
      },
      "required": [
        "z_candidates",
        "w_candidates",
        "recommended_z",
        "recommended_w"
      ],
      "type": "object"
    }
  },
  {
    "description": "Distributional LATE on compliers.",
    "name": "BeyondAverageResult",
    "parameters": {
      "properties": {
        "ci_high": {
          "description": "ci_high parameter (np.ndarray).",
          "type": "string"
        },
        "ci_low": {
          "description": "ci_low parameter (np.ndarray).",
          "type": "string"
        },
        "complier_share": {
          "description": "complier_share parameter (float).",
          "type": "number"
        },
        "late_q": {
          "description": "late_q parameter (np.ndarray).",
          "type": "string"
        },
        "n_obs": {
          "description": "Number of obs.",
          "type": "integer"
        },
        "quantiles": {
          "description": "quantiles parameter (np.ndarray).",
          "type": "string"
        },
        "se_q": {
          "description": "se_q parameter (np.ndarray).",
          "type": "string"
        }
      },
      "required": [
        "quantiles",
        "late_q",
        "se_q",
        "ci_low",
        "ci_high",
        "complier_share",
        "n_obs"
      ],
      "type": "object"
    }
  },
  {
    "description": "Panel quantile treatment effects with high-dimensional controls. Validation: validated evidence tier (known-truth, reference, external-parity, or Monte Carlo artifact).",
    "name": "qte_hd_panel",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "covariates": {
          "description": "Candidate control set; narrowed by double-selection LASSO.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "data": {
          "description": "Long-format panel.",
          "type": "string"
        },
        "lasso_alpha": {
          "description": "Penalty on standardised covariates. ``None`` uses the Belloni-Chernozhukov-Hansen plug-in penalty.",
          "type": "number"
        },
        "method": {
          "default": "canay",
          "description": "See the module docstring. ``'canay'`` assumes the individual effect is a pure location shift and needs a reasonably long panel.",
          "enum": [
            "canay",
            "dummy_fe",
            "pooled"
          ],
          "type": "string"
        },
        "n_boot": {
          "default": 200,
          "description": "Number of bootstrap replications.",
          "type": "integer"
        },
        "quantiles": {
          "description": "Defaults to ``(0.1, 0.25, 0.5, 0.75, 0.9)``.",
          "type": "string"
        },
        "se": {
          "default": "bootstrap",
          "description": "``'bootstrap'`` resamples **units**, preserving within-unit dependence. There is no analytic option: Canay's two-step variance depends on the first step, and a naive quantile-regression SE would understate it.",
          "type": "string"
        },
        "seed": {
          "default": 0,
          "description": "Random seed for reproducible stochastic steps.",
          "type": "integer"
        },
        "time": {
          "description": "Time period column.",
          "type": "string"
        },
        "treat": {
          "description": "Treatment indicator or first-treatment-period column.",
          "type": "string"
        },
        "unit": {
          "description": "Unit identifier column.",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "treat",
        "unit",
        "time",
        "covariates"
      ],
      "type": "object"
    }
  },
  {
    "description": "Panel QTE at multiple quantiles with high-dimensional control selection.",
    "name": "HDPanelQTEResult",
    "parameters": {
      "properties": {
        "ci_high": {
          "description": "ci_high parameter (np.ndarray).",
          "type": "string"
        },
        "ci_low": {
          "description": "ci_low parameter (np.ndarray).",
          "type": "string"
        },
        "diagnostics": {
          "default": null,
          "description": "diagnostics parameter (Dict[str, Any]).",
          "type": "object"
        },
        "method": {
          "default": "canay",
          "description": "Estimator or algorithm variant to use.",
          "type": "string"
        },
        "n_obs": {
          "description": "Number of obs.",
          "type": "integer"
        },
        "n_periods": {
          "default": 0,
          "description": "Number of periods.",
          "type": "integer"
        },
        "n_units": {
          "default": 0,
          "description": "Number of units.",
          "type": "integer"
        },
        "qte": {
          "description": "qte parameter (np.ndarray).",
          "type": "string"
        },
        "quantiles": {
          "description": "quantiles parameter (np.ndarray).",
          "type": "string"
        },
        "se": {
          "description": "se parameter (np.ndarray).",
          "type": "string"
        },
        "se_method": {
          "default": "bootstrap",
          "description": "se_method parameter (str).",
          "type": "string"
        },
        "selected_controls": {
          "description": "selected_controls parameter (List[str]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "required": [
        "quantiles",
        "qte",
        "se",
        "ci_low",
        "ci_high",
        "selected_controls",
        "n_obs"
      ],
      "type": "object"
    }
  },
  {
    "description": "Distribution-valued sharp RDD.",
    "name": "rd_distribution",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "bandwidth": {
          "description": "Bandwidth used for local smoothing or kernel weighting.",
          "type": "number"
        },
        "cutoff": {
          "default": 0.0,
          "description": "cutoff parameter (float).",
          "type": "number"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "kernel": {
          "default": "triangular",
          "description": "Kernel function used for weighting or smoothing.",
          "type": "string"
        },
        "quantiles": {
          "description": "Defaults to (0.1, 0.25, 0.5, 0.75, 0.9).",
          "type": "string"
        },
        "running": {
          "description": "running parameter (str).",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "running"
      ],
      "type": "object"
    }
  },
  {
    "description": "RDD effect on each quantile.",
    "name": "DistRDResult",
    "parameters": {
      "properties": {
        "bandwidth": {
          "description": "Bandwidth used for local smoothing or kernel weighting.",
          "type": "number"
        },
        "n_obs": {
          "description": "Number of obs.",
          "type": "integer"
        },
        "qte": {
          "description": "qte parameter (np.ndarray).",
          "type": "string"
        },
        "quantiles": {
          "description": "quantiles parameter (np.ndarray).",
          "type": "string"
        },
        "se": {
          "description": "se parameter (np.ndarray).",
          "type": "string"
        }
      },
      "required": [
        "quantiles",
        "qte",
        "se",
        "bandwidth",
        "n_obs"
      ],
      "type": "object"
    }
  },
  {
    "description": "Sharp RDD with network interference (Cabrelli-Marconi 2024).",
    "name": "rd_interference",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "bandwidth": {
          "description": "Defaults to IQR of own running variable.",
          "type": "number"
        },
        "cutoff": {
          "default": 0.0,
          "description": "cutoff parameter (float).",
          "type": "number"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "kernel": {
          "default": "triangular",
          "description": "Kernel function used for weighting or smoothing.",
          "type": "string"
        },
        "neighbour_running": {
          "description": "Average running variable across neighbours (precomputed).",
          "type": "string"
        },
        "running": {
          "description": "Own running variable.",
          "type": "string"
        },
        "y": {
          "description": "Outcome.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "running",
        "neighbour_running"
      ],
      "type": "object"
    }
  },
  {
    "description": "Direct + spillover RDD effects under network interference.",
    "name": "RDInterferenceResult",
    "parameters": {
      "properties": {
        "bandwidth": {
          "description": "Bandwidth used for local smoothing or kernel weighting.",
          "type": "number"
        },
        "direct_effect": {
          "description": "direct_effect parameter (float).",
          "type": "number"
        },
        "direct_se": {
          "description": "direct_se parameter (float).",
          "type": "number"
        },
        "n_obs": {
          "description": "Number of obs.",
          "type": "integer"
        },
        "spillover_effect": {
          "description": "spillover_effect parameter (float).",
          "type": "number"
        },
        "spillover_se": {
          "description": "spillover_se parameter (float).",
          "type": "number"
        }
      },
      "required": [
        "direct_effect",
        "direct_se",
        "spillover_effect",
        "spillover_se",
        "n_obs",
        "bandwidth"
      ],
      "type": "object"
    }
  },
  {
    "description": "Multi-score RDD: treatment if all running variables exceed cutoffs.",
    "name": "rd_multi_score",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "bandwidth": {
          "description": "Defaults to median IQR across running vars.",
          "type": "number"
        },
        "cutoffs": {
          "description": "One cutoff per running variable (same length).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "kernel": {
          "default": "triangular",
          "description": "Kernel function used for weighting or smoothing.",
          "type": "string"
        },
        "running_vars": {
          "description": "Multiple running variables.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Outcome.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "running_vars",
        "cutoffs"
      ],
      "type": "object"
    }
  },
  {
    "description": "Multi-score RDD effect on the eligibility boundary.",
    "name": "MultiScoreRDResult",
    "parameters": {
      "properties": {
        "bandwidth": {
          "description": "Bandwidth used for local smoothing or kernel weighting.",
          "type": "number"
        },
        "boundary_effect": {
          "description": "boundary_effect parameter (float).",
          "type": "number"
        },
        "boundary_share": {
          "description": "boundary_share parameter (float).",
          "type": "number"
        },
        "n_obs": {
          "description": "Number of obs.",
          "type": "integer"
        },
        "se": {
          "description": "se parameter (float).",
          "type": "number"
        }
      },
      "required": [
        "boundary_effect",
        "se",
        "n_obs",
        "boundary_share",
        "bandwidth"
      ],
      "type": "object"
    }
  },
  {
    "description": "PCMCI causal discovery for stationary time-series.",
    "name": "pcmci",
    "parameters": {
      "properties": {
        "ci_test": {
          "description": "Custom CI test ``(x, y, Z) -> p_value``. Defaults to :func:`partial_corr_pvalue`.",
          "type": "number"
        },
        "data": {
          "description": "Time-series DataFrame with one row per time step. Must be strictly ordered (rows are consecutive time points).",
          "type": "string"
        },
        "max_conds_dim": {
          "description": "Hard cap on the conditioning-set size during PC1. ``None`` means no cap (the algorithm stops automatically when no predictors remain).",
          "type": "integer"
        },
        "mci_alpha": {
          "description": "Significance threshold for the final MCI adjacency. Defaults to ``pc_alpha``.",
          "type": "number"
        },
        "pc_alpha": {
          "default": 0.05,
          "description": "Significance threshold used during the PC1 selection stage.",
          "type": "number"
        },
        "tau_max": {
          "default": 3,
          "description": "Maximum lag to consider for parent candidates.",
          "type": "integer"
        },
        "variables": {
          "description": "Columns to use. Defaults to all columns.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "verbose": {
          "default": false,
          "description": "verbose parameter (bool).",
          "type": "boolean"
        }
      },
      "required": [
        "data"
      ],
      "type": "object"
    }
  },
  {
    "description": "PCMCI output -- lag-specific adjacency + discovered links.",
    "name": "PCMCIResult",
    "parameters": {
      "properties": {
        "adjacency": {
          "description": "adjacency parameter (np.ndarray).",
          "type": "string"
        },
        "alpha": {
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "method": {
          "default": "PCMCI (Runge et al. 2019)",
          "description": "Estimator or algorithm variant to use.",
          "type": "string"
        },
        "n_effective": {
          "description": "Number of effective.",
          "type": "integer"
        },
        "p_matrix": {
          "description": "p_matrix parameter (np.ndarray).",
          "type": "string"
        },
        "tau_max": {
          "description": "tau_max parameter (int).",
          "type": "integer"
        },
        "val_matrix": {
          "description": "val_matrix parameter (np.ndarray).",
          "type": "string"
        },
        "variables": {
          "description": "variables parameter (List[str]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "required": [
        "variables",
        "tau_max",
        "alpha",
        "p_matrix",
        "val_matrix",
        "adjacency",
        "n_effective"
      ],
      "type": "object"
    }
  },
  {
    "description": "Output of :func:`lpcmci`.",
    "name": "LPCMCIResult",
    "parameters": {
      "properties": {
        "alpha": {
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "edge_types": {
          "description": "edge_types parameter (np.ndarray).",
          "type": "string"
        },
        "p_values": {
          "description": "p_values parameter (np.ndarray).",
          "type": "string"
        },
        "parents": {
          "description": "parents parameter (Dict[str, List[tuple]]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "tau_max": {
          "description": "tau_max parameter (int).",
          "type": "integer"
        },
        "variables": {
          "description": "variables parameter (List[str]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "required": [
        "variables",
        "tau_max",
        "alpha",
        "edge_types",
        "p_values",
        "parents"
      ],
      "type": "object"
    }
  },
  {
    "description": "Output of :func:`dynotears`.",
    "name": "DYNOTEARSResult",
    "parameters": {
      "properties": {
        "A": {
          "description": "A parameter (np.ndarray).",
          "type": "string"
        },
        "W": {
          "description": "Covariates, proxy variables, or weights used by this estimator.",
          "type": "string"
        },
        "lag": {
          "description": "lag parameter (int).",
          "type": "integer"
        },
        "loss": {
          "description": "loss parameter (float).",
          "type": "number"
        },
        "threshold": {
          "description": "threshold parameter (float).",
          "type": "number"
        },
        "variables": {
          "description": "variables parameter (List[str]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "required": [
        "variables",
        "W",
        "A",
        "lag",
        "threshold",
        "loss"
      ],
      "type": "object"
    }
  },
  {
    "description": "Partial-correlation p-value for H0: ``X Y | Z``.",
    "name": "partial_corr_pvalue",
    "parameters": {
      "properties": {
        "Z": {
          "description": "Instrument matrix or auxiliary covariate matrix.",
          "type": "string"
        },
        "x": {
          "description": "Primary running variable, regressor, or feature input for this estimator.",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "x",
        "y"
      ],
      "type": "object"
    }
  },
  {
    "description": "LTMLE for a discrete-time survival outcome under dynamic regimes. Validation: validated evidence tier (known-truth, reference, external-parity, or Monte Carlo artifact).",
    "name": "ltmle_survival",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "baseline": {
          "description": "Time-invariant baseline covariates.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "censoring": {
          "description": "``1`` if the subject is observed *through* interval k, ``0`` if right-censored at or before k. If omitted, no censoring.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "covariates_time": {
          "description": "Time-varying covariates at each interval.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "data": {
          "description": "Wide-format, one row per subject.",
          "type": "string"
        },
        "event_indicators": {
          "description": "Column names for the per-interval event indicator ``T_k`` (``1`` if the event occurs *in* interval k, ``0`` otherwise).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "propensity_bounds": {
          "default": [
            0.01,
            0.99
          ],
          "description": "propensity_bounds parameter (Tuple[float, float]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "regime_control": {
          "description": "Treatment regimes, same semantics as :func:`ltmle`.",
          "type": "string"
        },
        "regime_treated": {
          "description": "Treatment regimes, same semantics as :func:`ltmle`.",
          "type": "string"
        },
        "treatments": {
          "description": "Treatment column per interval.",
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "required": [
        "data",
        "event_indicators",
        "treatments",
        "covariates_time"
      ],
      "type": "object"
    }
  },
  {
    "description": "Counterfactual survival curves and contrasts.",
    "name": "LTMLESurvivalResult",
    "parameters": {
      "properties": {
        "K": {
          "description": "K parameter (int).",
          "type": "integer"
        },
        "detail": {
          "default": null,
          "description": "Amount of result detail to return.",
          "type": "object"
        },
        "n_obs": {
          "description": "Number of obs.",
          "type": "integer"
        },
        "risk_difference_final": {
          "description": "risk_difference_final parameter (float).",
          "type": "number"
        },
        "risk_difference_final_se": {
          "description": "risk_difference_final_se parameter (float).",
          "type": "number"
        },
        "rmst_ci": {
          "description": "rmst_ci parameter (tuple).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "rmst_control": {
          "description": "rmst_control parameter (float).",
          "type": "number"
        },
        "rmst_difference": {
          "description": "rmst_difference parameter (float).",
          "type": "number"
        },
        "rmst_pvalue": {
          "description": "rmst_pvalue parameter (float).",
          "type": "number"
        },
        "rmst_se": {
          "description": "rmst_se parameter (float).",
          "type": "number"
        },
        "rmst_treated": {
          "description": "rmst_treated parameter (float).",
          "type": "number"
        },
        "survival_control": {
          "description": "survival_control parameter (np.ndarray).",
          "type": "string"
        },
        "survival_treated": {
          "description": "survival_treated parameter (np.ndarray).",
          "type": "string"
        },
        "times": {
          "description": "times parameter (np.ndarray).",
          "type": "string"
        }
      },
      "required": [
        "times",
        "survival_treated",
        "survival_control",
        "rmst_treated",
        "rmst_control",
        "rmst_difference",
        "rmst_se",
        "rmst_ci",
        "rmst_pvalue",
        "risk_difference_final",
        "risk_difference_final_se",
        "K",
        "n_obs"
      ],
      "type": "object"
    }
  },
  {
    "description": "Per-cohort and aggregated output of :func:`sequential_sdid`.",
    "name": "SequentialSDIDResult",
    "parameters": {
      "properties": {
        "aggregate_att": {
          "description": "aggregate_att parameter (float).",
          "type": "number"
        },
        "aggregate_ci": {
          "description": "aggregate_ci parameter (tuple).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "aggregate_se": {
          "description": "aggregate_se parameter (float).",
          "type": "number"
        },
        "model_info": {
          "default": null,
          "description": "model_info parameter (Dict[str, Any]).",
          "type": "object"
        },
        "per_cohort": {
          "description": "per_cohort parameter (pd.DataFrame).",
          "type": "string"
        }
      },
      "required": [
        "aggregate_att",
        "aggregate_se",
        "aggregate_ci",
        "per_cohort"
      ],
      "type": "object"
    }
  },
  {
    "description": "Output of :func:`synth_survival`.",
    "name": "SyntheticSurvivalResult",
    "parameters": {
      "properties": {
        "alpha": {
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "ci_high": {
          "description": "ci_high parameter (Optional[np.ndarray]).",
          "type": "string"
        },
        "ci_low": {
          "description": "ci_low parameter (Optional[np.ndarray]).",
          "type": "string"
        },
        "gap": {
          "description": "gap parameter (np.ndarray).",
          "type": "string"
        },
        "placebo_gaps": {
          "description": "placebo_gaps parameter (Optional[np.ndarray]).",
          "type": "string"
        },
        "pre_rmse": {
          "description": "pre_rmse parameter (Optional[float]).",
          "type": "number"
        },
        "s_synth": {
          "description": "s_synth parameter (np.ndarray).",
          "type": "string"
        },
        "s_treated": {
          "description": "s_treated parameter (np.ndarray).",
          "type": "string"
        },
        "time_grid": {
          "description": "Grid of time values to evaluate.",
          "type": "string"
        },
        "treat_time": {
          "description": "treat_time parameter (float).",
          "type": "number"
        },
        "treated_unit": {
          "description": "treated_unit parameter (str).",
          "type": "string"
        },
        "weights": {
          "description": "Observation weights.",
          "type": "object"
        }
      },
      "required": [
        "treated_unit",
        "time_grid",
        "s_treated",
        "s_synth",
        "gap",
        "weights",
        "treat_time",
        "alpha"
      ],
      "type": "object"
    }
  },
  {
    "description": "ML-enhanced partial-identification bounds on the ATE.",
    "name": "ml_bounds",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level of the band.",
          "type": "number"
        },
        "covariates": {
          "description": "Covariates X used for the outcome / propensity regressions.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "custom_learner": {
          "description": "Any ``.fit()`` / ``.predict()``-compatible regressor.",
          "type": "string"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "learner": {
          "default": "random_forest",
          "description": "\"random_forest\" Outcome-regression learner. Ignored if ``custom_learner`` is set.",
          "enum": [
            "random_forest",
            "gradient_boosting"
          ],
          "type": "string"
        },
        "n_bootstrap": {
          "default": 200,
          "description": "Non-parametric bootstrap replicates for the 2-sided frequentist band. Set to 0 to return the raw plug-in bounds only.",
          "type": "integer"
        },
        "n_splits": {
          "default": 5,
          "description": "Number of cross-fitting folds.",
          "type": "integer"
        },
        "random_state": {
          "description": "Random seed or RandomState for reproducible stochastic steps.",
          "type": "integer"
        },
        "treat": {
          "description": "Outcome and 0/1 treatment column names.",
          "type": "string"
        },
        "y": {
          "description": "Outcome and 0/1 treatment column names.",
          "type": "string"
        },
        "y_max": {
          "description": "A priori bounds on Y. Defaults to the empirical min/max. **Tighter** external bounds (e.g. if Y is a probability, use ``[0, 1]``) give tighter ML bounds.",
          "type": "number"
        },
        "y_min": {
          "description": "A priori bounds on Y. Defaults to the empirical min/max. **Tighter** external bounds (e.g. if Y is a probability, use ``[0, 1]``) give tighter ML bounds.",
          "type": "number"
        }
      },
      "required": [
        "data",
        "y",
        "treat",
        "covariates"
      ],
      "type": "object"
    }
  },
  {
    "description": "Gaussian-copula sensitivity to unobserved confounding.",
    "name": "copula_sensitivity",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "estimate": {
          "description": "estimate parameter (float).",
          "type": "number"
        },
        "rho_grid": {
          "description": "Correlation grid. Defaults to ``np.linspace(-0.5, 0.5, 21)``.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "se": {
          "description": "se parameter (float).",
          "type": "number"
        },
        "sigma_u": {
          "default": 1.0,
          "description": "Standard deviations of the latent confounder and the outcome. With default values the bias coefficient is numerically equal to ``rho``, matching Chernozhukov-Cinelli-Hazlett's \"percentile scaling.\"",
          "type": "number"
        },
        "sigma_y": {
          "default": 1.0,
          "description": "Standard deviations of the latent confounder and the outcome. With default values the bias coefficient is numerically equal to ``rho``, matching Chernozhukov-Cinelli-Hazlett's \"percentile scaling.\"",
          "type": "number"
        }
      },
      "required": [
        "estimate",
        "se"
      ],
      "type": "object"
    }
  },
  {
    "description": "Nonparametric sensitivity for survival / hazard-ratio outcomes. Validation: validated evidence tier (known-truth, reference, external-parity, or Monte Carlo artifact).",
    "name": "survival_sensitivity",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "baseline_survival_t": {
          "default": 0.5,
          "description": "Baseline S_0(t) used to report Delta survival at time t.",
          "type": "number"
        },
        "gamma_grid": {
          "description": "Gamma (>= 1) values. Defaults to ``np.linspace(1.0, 3.0, 21)``.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "log_hr": {
          "description": "log_hr parameter (float).",
          "type": "number"
        },
        "se_log_hr": {
          "description": "se_log_hr parameter (float).",
          "type": "number"
        }
      },
      "required": [
        "log_hr",
        "se_log_hr"
      ],
      "type": "object"
    }
  },
  {
    "description": "Calibrate the strength of an unobserved confounder required to Validation: certified parity evidence.",
    "name": "calibrate_confounding_strength",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Level of the adjusted confidence interval.",
          "type": "number"
        },
        "dof": {
          "description": "Residual degrees of freedom of the regression. Required: the bias bound scales with ``sqrtdof``.",
          "type": "number"
        },
        "estimate": {
          "description": "OLS coefficient of the treatment and its (classical) standard error.",
          "type": "number"
        },
        "multipliers": {
          "description": "Values of ``k``; default ``np.linspace(0.5, 5.0, 19)``. A ``k`` that implies ``R2_{D~Z|X} >= 1`` is impossible and its row is NaN with ``feasible = False`` (sensemakr stops with an error instead).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "observed_r2_outcome": {
          "description": "Partial R2 of the benchmark covariate with the outcome, given the treatment and the other covariates (sensemakr ``r2yxj.dx``).",
          "type": "number"
        },
        "observed_r2_treatment": {
          "description": "Partial R2 of the benchmark covariate with the treatment, given the other covariates (sensemakr ``r2dxj.x``).",
          "type": "number"
        },
        "se": {
          "description": "OLS coefficient of the treatment and its (classical) standard error.",
          "type": "number"
        },
        "target_estimate": {
          "default": 0.0,
          "description": "Effect value to explain away.",
          "type": "number"
        }
      },
      "required": [
        "estimate",
        "se",
        "observed_r2_outcome",
        "observed_r2_treatment"
      ],
      "type": "object"
    }
  },
  {
    "description": "Container for frontier sensitivity analysis.",
    "name": "FrontierSensitivityResult",
    "parameters": {
      "properties": {
        "breakpoint": {
          "description": "breakpoint parameter (Optional[float]).",
          "type": "number"
        },
        "curve": {
          "description": "curve parameter (pd.DataFrame).",
          "type": "string"
        },
        "estimate": {
          "description": "estimate parameter (float).",
          "type": "number"
        },
        "interpretation": {
          "description": "interpretation parameter (str).",
          "type": "string"
        },
        "method": {
          "description": "Estimator or algorithm variant to use.",
          "type": "string"
        },
        "se": {
          "description": "se parameter (float).",
          "type": "number"
        }
      },
      "required": [
        "method",
        "estimate",
        "se",
        "curve",
        "breakpoint",
        "interpretation"
      ],
      "type": "object"
    }
  },
  {
    "description": "Distributional IV: the complier LATE at each quantile of ``y``. Validation: validated evidence tier (known-truth, reference, external-parity, or Monte Carlo artifact).",
    "name": "dist_iv",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "covariates": {
          "description": "Conditioning set for ``P(Z=1|X)``. Unlike versions <= 1.20.0 (which accepted and silently ignored this argument) it now changes the estimate.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "instrument": {
          "description": "Outcome, binary treatment, binary instrument.",
          "type": "string"
        },
        "n_boot": {
          "default": 200,
          "description": "Bootstrap replications when ``se='bootstrap'``.",
          "type": "integer"
        },
        "quantiles": {
          "description": "Defaults to ``(0.1, 0.25, 0.5, 0.75, 0.9)``.",
          "type": "string"
        },
        "se": {
          "default": "auto",
          "description": "``'auto'`` picks ``'analytic'`` without covariates and ``'bootstrap'`` with them (the analytic influence function treats ``pi(X)`` as known).",
          "enum": [
            "auto",
            "analytic",
            "bootstrap"
          ],
          "type": "string"
        },
        "seed": {
          "default": 0,
          "description": "Random seed for reproducible stochastic steps.",
          "type": "integer"
        },
        "treat": {
          "description": "Outcome, binary treatment, binary instrument.",
          "type": "string"
        },
        "y": {
          "description": "Outcome, binary treatment, binary instrument.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "treat",
        "instrument"
      ],
      "type": "object"
    }
  },
  {
    "description": "Deprecated alias for :func:`dist_iv`. Validation: validated evidence tier (known-truth, reference, external-parity, or Monte Carlo artifact).",
    "name": "kan_dlate",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "covariates": {
          "description": "Covariate matrix, DataFrame, or column names.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "instrument": {
          "description": "instrument parameter (str).",
          "type": "string"
        },
        "n_boot": {
          "default": 200,
          "description": "Number of bootstrap replications.",
          "type": "integer"
        },
        "quantiles": {
          "description": "quantiles parameter (Optional[np.ndarray]).",
          "type": "string"
        },
        "seed": {
          "default": 0,
          "description": "Random seed for reproducible stochastic steps.",
          "type": "integer"
        },
        "treat": {
          "description": "Treatment indicator or first-treatment-period column.",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "treat",
        "instrument"
      ],
      "type": "object"
    }
  },
  {
    "description": "Distributional IV: complier LATE per quantile.",
    "name": "DistIVResult",
    "parameters": {
      "properties": {
        "ci_high": {
          "description": "ci_high parameter (np.ndarray).",
          "type": "string"
        },
        "ci_low": {
          "description": "ci_low parameter (np.ndarray).",
          "type": "string"
        },
        "complier_share": {
          "default": NaN,
          "description": "complier_share parameter (float).",
          "type": "number"
        },
        "late_q": {
          "description": "late_q parameter (np.ndarray).",
          "type": "string"
        },
        "method": {
          "default": "Distributional IV (Abadie kappa-weighted complier QTE)",
          "description": "Estimator or algorithm variant to use.",
          "type": "string"
        },
        "model_info": {
          "default": null,
          "description": "model_info parameter (dict).",
          "type": "object"
        },
        "n_obs": {
          "description": "Number of obs.",
          "type": "integer"
        },
        "quantiles": {
          "description": "quantiles parameter (np.ndarray).",
          "type": "string"
        },
        "se_method": {
          "default": "analytic",
          "description": "se_method parameter (str).",
          "type": "string"
        },
        "se_q": {
          "description": "se_q parameter (np.ndarray).",
          "type": "string"
        }
      },
      "required": [
        "quantiles",
        "late_q",
        "se_q",
        "ci_low",
        "ci_high",
        "n_obs"
      ],
      "type": "object"
    }
  },
  {
    "description": "Bayesian RDD allowing CATE to depend on covariates.",
    "name": "rd_bayes_hte",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "bandwidth": {
          "description": "Bandwidth used for local smoothing or kernel weighting.",
          "type": "number"
        },
        "covariates": {
          "description": "Covariate matrix, DataFrame, or column names.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "cutoff": {
          "default": 0.0,
          "description": "cutoff parameter (float).",
          "type": "number"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "kernel": {
          "default": "triangular",
          "description": "Kernel function used for weighting or smoothing.",
          "type": "string"
        },
        "n_draws": {
          "default": 2000,
          "description": "Number of draws.",
          "type": "integer"
        },
        "running": {
          "description": "running parameter (str).",
          "type": "string"
        },
        "seed": {
          "default": 0,
          "description": "Random seed for reproducible stochastic steps.",
          "type": "integer"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "running",
        "covariates"
      ],
      "type": "object"
    }
  },
  {
    "description": "Bayesian RDD with HTE posterior summaries.",
    "name": "BayesRDHTEResult",
    "parameters": {
      "properties": {
        "bandwidth": {
          "description": "Bandwidth used for local smoothing or kernel weighting.",
          "type": "number"
        },
        "cate": {
          "description": "cate parameter (np.ndarray).",
          "type": "string"
        },
        "cate_sd": {
          "description": "cate_sd parameter (np.ndarray).",
          "type": "string"
        },
        "n_obs": {
          "description": "Number of obs.",
          "type": "integer"
        },
        "posterior_ci": {
          "description": "posterior_ci parameter (tuple).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "posterior_mean": {
          "description": "posterior_mean parameter (float).",
          "type": "number"
        },
        "posterior_sd": {
          "description": "posterior_sd parameter (float).",
          "type": "number"
        }
      },
      "required": [
        "posterior_mean",
        "posterior_sd",
        "posterior_ci",
        "cate",
        "cate_sd",
        "bandwidth",
        "n_obs"
      ],
      "type": "object"
    }
  },
  {
    "description": "Joint RDD + RKD on the conditional distribution of Y.",
    "name": "rd_distributional_design",
    "parameters": {
      "properties": {
        "bandwidth": {
          "description": "Bandwidth used for local smoothing or kernel weighting.",
          "type": "number"
        },
        "cutoff": {
          "default": 0.0,
          "description": "cutoff parameter (float).",
          "type": "number"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "kernel": {
          "default": "triangular",
          "description": "Kernel function used for weighting or smoothing.",
          "type": "string"
        },
        "quantiles": {
          "description": "quantiles parameter (Optional[np.ndarray]).",
          "type": "string"
        },
        "running": {
          "description": "running parameter (str).",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "running"
      ],
      "type": "object"
    }
  },
  {
    "description": "Distributional discontinuity design output.",
    "name": "DDDResult",
    "parameters": {
      "properties": {
        "bandwidth": {
          "description": "Bandwidth used for local smoothing or kernel weighting.",
          "type": "number"
        },
        "n_obs": {
          "description": "Number of obs.",
          "type": "integer"
        },
        "quantiles": {
          "description": "quantiles parameter (np.ndarray).",
          "type": "string"
        },
        "rdd_effect": {
          "description": "rdd_effect parameter (np.ndarray).",
          "type": "string"
        },
        "rkd_effect": {
          "description": "rkd_effect parameter (np.ndarray).",
          "type": "string"
        }
      },
      "required": [
        "quantiles",
        "rdd_effect",
        "rkd_effect",
        "bandwidth",
        "n_obs"
      ],
      "type": "object"
    }
  },
  {
    "description": "Four-panel RD diagnostic dashboard.",
    "name": "rd_dashboard",
    "parameters": {
      "properties": {
        "bw_grid": {
          "description": "Bandwidths to evaluate in the sensitivity panel. If None, uses ``[0.5, 0.75, 1.0, 1.25, 1.5, 2.0] x h_mse``.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "c": {
          "default": 0.0,
          "description": "Cutoff.",
          "type": "number"
        },
        "covs": {
          "description": "Covariates for the balance panel. If None, the panel shows the density's binomial near-cutoff test instead.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "figsize": {
          "default": [
            12,
            9
          ],
          "description": "figsize parameter (Tuple[float, float]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "fuzzy": {
          "description": "Fuzzy treatment column (passed through to RD plot/sensitivity).",
          "type": "string"
        },
        "h": {
          "description": "Reference bandwidth used for plotting and as the basis for ``bw_grid``. If None, MSE-optimal bandwidth from rdrobust.",
          "type": "number"
        },
        "save": {
          "description": "If a path is given, also save the figure (extension determines format).",
          "type": "string"
        },
        "title": {
          "description": "Suptitle.",
          "type": "string"
        },
        "x": {
          "description": "Primary running variable, regressor, or feature input for this estimator.",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "x"
      ],
      "type": "object"
    }
  },
  {
    "description": "Compare multiple RD estimators on the same data.",
    "name": "rd_compare",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Confidence level forwarded to each estimator that accepts ``alpha``.",
          "type": "number"
        },
        "c": {
          "default": 0.0,
          "description": "c parameter (float).",
          "type": "number"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "fuzzy": {
          "description": "Fuzzy treatment column passed through to all methods that accept it.",
          "type": "string"
        },
        "method_kwargs": {
          "description": "Per-method extra kwargs, e.g. ``{'rdrobust': {'kernel': 'uniform'}}``.",
          "type": "object"
        },
        "methods": {
          "default": [
            "rdrobust",
            "honest",
            "randinf"
          ],
          "description": "Method aliases recognised by the :data:`sp.rd._RD_METHOD_ALIASES` dispatcher. Default: ``('rdrobust', 'honest', 'randinf')``.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "x": {
          "description": "Primary running variable, regressor, or feature input for this estimator.",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "x"
      ],
      "type": "object"
    }
  },
  {
    "description": "Sweep over (kernel, bwselect, polynomial, donut) and return a",
    "name": "rd_robustness_table",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "bwselects": {
          "default": [
            "mserd",
            "cerrd",
            "msetwo"
          ],
          "description": "Specification grid.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "c": {
          "default": 0.0,
          "description": "c parameter (float).",
          "type": "number"
        },
        "cluster": {
          "description": "Cluster identifier column for clustered standard errors.",
          "type": "string"
        },
        "covs": {
          "description": "covs parameter (Optional[List[str]]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "donuts": {
          "default": [
            0.0
          ],
          "description": "Specification grid.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "fuzzy": {
          "description": "fuzzy parameter (Optional[str]).",
          "type": "string"
        },
        "kernels": {
          "default": [
            "triangular",
            "epanechnikov",
            "uniform"
          ],
          "description": "Specification grid.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "polynomials": {
          "default": [
            1,
            2
          ],
          "description": "Specification grid.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "x": {
          "description": "Primary running variable, regressor, or feature input for this estimator.",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "x"
      ],
      "type": "object"
    }
  },
  {
    "description": "Propose a candidate DAG from variable names + domain description.",
    "name": "llm_dag_propose",
    "parameters": {
      "properties": {
        "client": {
          "description": "An LLM client implementing ``.complete(prompt: str) -> str``. If ``None``, use the deterministic heuristic backend.",
          "type": "string"
        },
        "domain": {
          "default": "",
          "description": "Free-text domain description (e.g. \"labor economics, education and earnings\"). Helps the LLM but ignored by the heuristic backend.",
          "type": "string"
        },
        "seed": {
          "default": 0,
          "description": "Random seed for reproducible stochastic steps.",
          "type": "integer"
        },
        "variables": {
          "description": "Names of variables in the dataset.",
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "required": [
        "variables"
      ],
      "type": "object"
    }
  },
  {
    "description": "Result of an LLM (or heuristic) DAG proposal.",
    "name": "LLMDAGProposal",
    "parameters": {
      "properties": {
        "backend": {
          "default": "heuristic",
          "description": "backend parameter (str).",
          "type": "string"
        },
        "confidence": {
          "default": 0.5,
          "description": "confidence parameter (float).",
          "type": "number"
        },
        "edges": {
          "description": "edges parameter (List[tuple]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "rationale": {
          "description": "rationale parameter (List[str]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "roles": {
          "description": "roles parameter (dict).",
          "type": "object"
        }
      },
      "required": [
        "edges",
        "roles",
        "rationale"
      ],
      "type": "object"
    }
  },
  {
    "description": "Enumerate plausible unobserved confounders for a study.",
    "name": "llm_unobserved_confounders",
    "parameters": {
      "properties": {
        "client": {
          "description": "LLM client with ``.complete(prompt: str) -> str``.",
          "type": "string"
        },
        "domain": {
          "default": "health",
          "description": "domain parameter (str).",
          "enum": [
            "health",
            "education",
            "labor",
            "policy",
            "marketing"
          ],
          "type": "string"
        },
        "outcome": {
          "description": "Free-text descriptions (used by LLM, ignored by heuristic).",
          "type": "string"
        },
        "point_estimate_rr": {
          "default": 1.5,
          "description": "Observed risk ratio; suggested E-values are scaled relative to this so the user can read \"to nullify a RR of X you'd need an unobserved RR of Y\".",
          "type": "number"
        },
        "treatment": {
          "description": "Free-text descriptions (used by LLM, ignored by heuristic).",
          "type": "string"
        }
      },
      "required": [
        "treatment",
        "outcome"
      ],
      "type": "object"
    }
  },
  {
    "description": "List of plausible unobserved confounders + suggested E-values.",
    "name": "UnobservedConfounderProposal",
    "parameters": {
      "properties": {
        "backend": {
          "default": "heuristic",
          "description": "backend parameter (str).",
          "type": "string"
        },
        "candidates": {
          "description": "candidates parameter (List[str]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "domain": {
          "description": "domain parameter (str).",
          "type": "string"
        },
        "suggested_evalue_thresholds": {
          "description": "suggested_evalue_thresholds parameter (List[float]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "required": [
        "candidates",
        "suggested_evalue_thresholds",
        "domain"
      ],
      "type": "object"
    }
  },
  {
    "description": "Propose sensitivity-analysis priors for the substantive setting.",
    "name": "llm_sensitivity_priors",
    "parameters": {
      "properties": {
        "client": {
          "description": "LLM client with ``.complete(prompt: str) -> str``.",
          "type": "string"
        },
        "domain": {
          "default": "health",
          "description": "domain parameter (str).",
          "enum": [
            "health",
            "education",
            "labor",
            "policy",
            "marketing"
          ],
          "type": "string"
        },
        "outcome": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        },
        "treatment": {
          "description": "Treatment indicator, treatment variable, or treatment array.",
          "type": "string"
        }
      },
      "required": [
        "treatment",
        "outcome"
      ],
      "type": "object"
    }
  },
  {
    "description": "Suggested sensitivity parameter priors for sensemakr-style analysis.",
    "name": "SensitivityPriorProposal",
    "parameters": {
      "properties": {
        "backend": {
          "default": "heuristic",
          "description": "backend parameter (str).",
          "type": "string"
        },
        "domain": {
          "description": "domain parameter (str).",
          "type": "string"
        },
        "r2": {
          "description": "r2 parameter (float).",
          "type": "number"
        },
        "rationale": {
          "description": "rationale parameter (str).",
          "type": "string"
        },
        "rho_max": {
          "description": "rho_max parameter (float).",
          "type": "number"
        }
      },
      "required": [
        "rho_max",
        "r2",
        "rationale",
        "domain"
      ],
      "type": "object"
    }
  },
  {
    "description": "Output of :func:`llm_dag_constrained`.",
    "name": "LLMConstrainedDAGResult",
    "parameters": {
      "properties": {
        "alpha": {
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "converged": {
          "description": "converged parameter (bool).",
          "type": "boolean"
        },
        "cpdag": {
          "description": "cpdag parameter (pd.DataFrame).",
          "type": "string"
        },
        "edge_confidence": {
          "description": "edge_confidence parameter (pd.DataFrame).",
          "type": "string"
        },
        "final_edges": {
          "description": "final_edges parameter (List[Tuple[str, str]]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "iteration_log": {
          "description": "iteration_log parameter (List[Dict[str, Any]]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "n_obs": {
          "description": "Number of obs.",
          "type": "integer"
        },
        "provenance": {
          "default": null,
          "description": "provenance parameter (Dict[str, Any]).",
          "type": "object"
        },
        "skeleton": {
          "description": "skeleton parameter (pd.DataFrame).",
          "type": "string"
        },
        "variables": {
          "description": "variables parameter (List[str]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "required": [
        "final_edges",
        "edge_confidence",
        "iteration_log",
        "skeleton",
        "cpdag",
        "variables",
        "n_obs",
        "alpha",
        "converged"
      ],
      "type": "object"
    }
  },
  {
    "description": "Output of :func:`llm_dag_validate`.",
    "name": "DAGValidationResult",
    "parameters": {
      "properties": {
        "alpha": {
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "edge_evidence": {
          "description": "edge_evidence parameter (pd.DataFrame).",
          "type": "string"
        },
        "n_supported": {
          "description": "Number of supported.",
          "type": "integer"
        },
        "n_unsupported": {
          "description": "Number of unsupported.",
          "type": "integer"
        }
      },
      "required": [
        "edge_evidence",
        "n_supported",
        "n_unsupported",
        "alpha"
      ],
      "type": "object"
    }
  },
  {
    "description": "ATE result for text-as-treatment estimation.",
    "name": "TextTreatmentResult",
    "parameters": {
      "properties": {
        "alpha": {
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "ci": {
          "description": "ci parameter (tuple).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "diagnostics": {
          "description": "diagnostics parameter (Optional[Dict[str, Any]]).",
          "type": "object"
        },
        "embedder_name": {
          "description": "embedder_name parameter (str).",
          "type": "string"
        },
        "embedding_dim": {
          "description": "embedding_dim parameter (int).",
          "type": "integer"
        },
        "estimand": {
          "description": "estimand parameter (str).",
          "type": "string"
        },
        "estimate": {
          "description": "estimate parameter (float).",
          "type": "number"
        },
        "method": {
          "description": "Estimator or algorithm variant to use.",
          "type": "string"
        },
        "model_info": {
          "description": "model_info parameter (Optional[Dict[str, Any]]).",
          "type": "object"
        },
        "n_obs": {
          "description": "Number of obs.",
          "type": "integer"
        },
        "pvalue": {
          "description": "pvalue parameter (float).",
          "type": "number"
        },
        "se": {
          "description": "se parameter (float).",
          "type": "number"
        }
      },
      "required": [
        "method",
        "estimand",
        "estimate",
        "se",
        "pvalue",
        "ci",
        "alpha",
        "n_obs",
        "embedding_dim",
        "embedder_name"
      ],
      "type": "object"
    }
  },
  {
    "description": "Output of :func:`llm_annotator_correct`.",
    "name": "LLMAnnotatorResult",
    "parameters": {
      "properties": {
        "alpha": {
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "annotator_diagnostics": {
          "description": "annotator_diagnostics parameter (Dict[str, Any]).",
          "type": "object"
        },
        "ci": {
          "description": "ci parameter (tuple).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "correction_factor": {
          "description": "correction_factor parameter (float).",
          "type": "number"
        },
        "detail": {
          "description": "Amount of result detail to return.",
          "type": "string"
        },
        "estimand": {
          "description": "estimand parameter (str).",
          "type": "string"
        },
        "estimate": {
          "description": "estimate parameter (float).",
          "type": "number"
        },
        "method": {
          "description": "Estimator or algorithm variant to use.",
          "type": "string"
        },
        "model_info": {
          "description": "model_info parameter (Optional[Dict[str, Any]]).",
          "type": "object"
        },
        "n_obs": {
          "description": "Number of obs.",
          "type": "integer"
        },
        "naive_estimate": {
          "description": "naive_estimate parameter (float).",
          "type": "number"
        },
        "naive_se": {
          "description": "naive_se parameter (float).",
          "type": "number"
        },
        "pvalue": {
          "description": "pvalue parameter (float).",
          "type": "number"
        },
        "se": {
          "description": "se parameter (float).",
          "type": "number"
        }
      },
      "required": [
        "method",
        "estimand",
        "estimate",
        "se",
        "pvalue",
        "ci",
        "alpha",
        "n_obs",
        "naive_estimate",
        "naive_se",
        "correction_factor",
        "annotator_diagnostics"
      ],
      "type": "object"
    }
  },
  {
    "description": "Output of confounding-robust Q-learning (:func:`causal_dqn`).",
    "name": "CausalDQNResult",
    "parameters": {
      "properties": {
        "final_bellman_error": {
          "description": "final_bellman_error parameter (float).",
          "type": "number"
        },
        "gamma_bound": {
          "description": "gamma_bound parameter (float).",
          "type": "number"
        },
        "n_iter": {
          "description": "Number of iter.",
          "type": "integer"
        },
        "policy": {
          "description": "policy parameter (np.ndarray).",
          "type": "string"
        },
        "q_table": {
          "description": "q_table parameter (np.ndarray).",
          "type": "string"
        }
      },
      "required": [
        "q_table",
        "policy",
        "gamma_bound",
        "n_iter",
        "final_bellman_error"
      ],
      "type": "object"
    }
  },
  {
    "description": "Generate a synthetic causal-RL benchmark dataset.",
    "name": "causal_rl_benchmark",
    "parameters": {
      "properties": {
        "confounding_strength": {
          "default": 0.5,
          "description": "Magnitude of unmeasured confounding U -> (action, reward).",
          "type": "number"
        },
        "n_episodes": {
          "default": 1000,
          "description": "Number of episodes.",
          "type": "integer"
        },
        "name": {
          "default": "confounded_bandit",
          "description": "'confounded_targeting', 'confounded_routing'}",
          "type": "string"
        },
        "seed": {
          "default": 0,
          "description": "Random seed for reproducible stochastic steps.",
          "type": "integer"
        }
      },
      "required": [],
      "type": "object"
    }
  },
  {
    "description": "Output from a causal-RL benchmark run.",
    "name": "BanditBenchmarkResult",
    "parameters": {
      "properties": {
        "benchmark": {
          "description": "benchmark parameter (str).",
          "type": "string"
        },
        "optimal_policy": {
          "description": "optimal_policy parameter (np.ndarray).",
          "type": "string"
        },
        "optimal_value": {
          "description": "optimal_value parameter (float).",
          "type": "number"
        },
        "suggested_evaluator": {
          "description": "suggested_evaluator parameter (str).",
          "type": "string"
        },
        "transitions": {
          "description": "transitions parameter (pd.DataFrame).",
          "type": "string"
        }
      },
      "required": [
        "benchmark",
        "transitions",
        "optimal_policy",
        "optimal_value",
        "suggested_evaluator"
      ],
      "type": "object"
    }
  },
  {
    "description": "Safe offline policy learning with a cost-constraint.",
    "name": "offline_safe_policy",
    "parameters": {
      "properties": {
        "action": {
          "description": "Column names. state and action must be discrete.",
          "type": "string"
        },
        "cost": {
          "description": "Column names. state and action must be discrete.",
          "type": "string"
        },
        "cost_threshold": {
          "default": 0.5,
          "description": "Max allowed expected cost per step.",
          "type": "number"
        },
        "data": {
          "description": "Transition data (s, a, r, cost).",
          "type": "string"
        },
        "discount": {
          "default": 0.95,
          "description": "discount parameter (float).",
          "type": "number"
        },
        "n_iter": {
          "default": 100,
          "description": "Number of iter.",
          "type": "integer"
        },
        "reward": {
          "description": "Column names. state and action must be discrete.",
          "type": "string"
        },
        "seed": {
          "default": 0,
          "description": "Random seed for reproducible stochastic steps.",
          "type": "integer"
        },
        "state": {
          "description": "Column names. state and action must be discrete.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "state",
        "action",
        "reward",
        "cost"
      ],
      "type": "object"
    }
  },
  {
    "description": "Output of safe offline policy learning.",
    "name": "OfflineSafeResult",
    "parameters": {
      "properties": {
        "cost_threshold": {
          "description": "cost_threshold parameter (float).",
          "type": "number"
        },
        "expected_cost": {
          "description": "expected_cost parameter (float).",
          "type": "number"
        },
        "expected_reward": {
          "description": "expected_reward parameter (float).",
          "type": "number"
        },
        "feasible": {
          "description": "feasible parameter (bool).",
          "type": "boolean"
        },
        "policy": {
          "description": "policy parameter (np.ndarray).",
          "type": "string"
        }
      },
      "required": [
        "policy",
        "expected_reward",
        "expected_cost",
        "cost_threshold",
        "feasible"
      ],
      "type": "object"
    }
  },
  {
    "description": "Matched-pair cluster RCT estimator (weighted DIM + Bai SE).",
    "name": "cluster_matched_pair",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "cluster": {
          "description": "Cluster identifier.",
          "type": "string"
        },
        "data": {
          "description": "Individual-level data.",
          "type": "string"
        },
        "pair": {
          "description": "Pair identifier (each pair contains exactly two clusters).",
          "type": "string"
        },
        "treat": {
          "description": "Cluster-level binary treatment.",
          "type": "string"
        },
        "y": {
          "description": "Outcome.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "cluster",
        "treat",
        "pair"
      ],
      "type": "object"
    }
  },
  {
    "description": "Output of matched-pair cluster RCT estimation.",
    "name": "MatchedPairResult",
    "parameters": {
      "properties": {
        "ci": {
          "description": "ci parameter (tuple).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "estimate": {
          "description": "estimate parameter (float).",
          "type": "number"
        },
        "method": {
          "default": "Matched-Pair Cluster RCT",
          "description": "Estimator or algorithm variant to use.",
          "type": "string"
        },
        "n_clusters": {
          "description": "Number of clusters.",
          "type": "integer"
        },
        "n_pairs": {
          "description": "Number of pairs.",
          "type": "integer"
        },
        "se": {
          "description": "se parameter (float).",
          "type": "number"
        }
      },
      "required": [
        "estimate",
        "se",
        "ci",
        "n_pairs",
        "n_clusters"
      ],
      "type": "object"
    }
  },
  {
    "description": "Output of cross-cluster RCT with interference correction.",
    "name": "CrossClusterRCTResult",
    "parameters": {
      "properties": {
        "direct_effect": {
          "description": "direct_effect parameter (float).",
          "type": "number"
        },
        "direct_se": {
          "description": "direct_se parameter (float).",
          "type": "number"
        },
        "method": {
          "default": "Cluster RCT with Cross-Cluster Interference",
          "description": "Estimator or algorithm variant to use.",
          "type": "string"
        },
        "n_clusters": {
          "description": "Number of clusters.",
          "type": "integer"
        },
        "spillover_effect": {
          "description": "spillover_effect parameter (float).",
          "type": "number"
        },
        "spillover_se": {
          "description": "spillover_se parameter (float).",
          "type": "number"
        }
      },
      "required": [
        "direct_effect",
        "direct_se",
        "spillover_effect",
        "spillover_se",
        "n_clusters"
      ],
      "type": "object"
    }
  },
  {
    "description": "Staggered-rollout cluster RCT estimator.",
    "name": "cluster_staggered_rollout",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "cluster": {
          "description": "first_treat = first calendar time the cluster is treated (0 / NaN for never-treated).",
          "type": "string"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "first_treat": {
          "description": "first_treat = first calendar time the cluster is treated (0 / NaN for never-treated).",
          "type": "string"
        },
        "lags": {
          "default": 4,
          "description": "lags parameter (int).",
          "type": "integer"
        },
        "leads": {
          "default": 2,
          "description": "leads parameter (int).",
          "type": "integer"
        },
        "time": {
          "description": "first_treat = first calendar time the cluster is treated (0 / NaN for never-treated).",
          "type": "string"
        },
        "y": {
          "description": "first_treat = first calendar time the cluster is treated (0 / NaN for never-treated).",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "cluster",
        "time",
        "first_treat"
      ],
      "type": "object"
    }
  },
  {
    "description": "Staggered-rollout cluster RCT output.",
    "name": "StaggeredClusterRCTResult",
    "parameters": {
      "properties": {
        "event_study": {
          "description": "event_study parameter (pd.DataFrame).",
          "type": "string"
        },
        "method": {
          "default": "Staggered-Rollout Cluster RCT",
          "description": "Estimator or algorithm variant to use.",
          "type": "string"
        },
        "n_clusters": {
          "description": "Number of clusters.",
          "type": "integer"
        },
        "overall_att": {
          "description": "overall_att parameter (float).",
          "type": "number"
        },
        "overall_se": {
          "description": "overall_se parameter (float).",
          "type": "number"
        }
      },
      "required": [
        "overall_att",
        "overall_se",
        "event_study",
        "n_clusters"
      ],
      "type": "object"
    }
  },
  {
    "description": "DiD with double negative controls + (optional) GNN embedding for",
    "name": "dnc_gnn_did",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "data": {
          "description": "Long-format panel.",
          "type": "string"
        },
        "embedding": {
          "description": "Pre-computed (n_units, k) GNN embedding. If None, use simple unit-level lagged outcome as a stand-in network feature.",
          "type": "string"
        },
        "id": {
          "description": "Unit, subject, or panel identifier column.",
          "type": "string"
        },
        "n_boot": {
          "default": 100,
          "description": "Number of bootstrap replications.",
          "type": "integer"
        },
        "nc_exposure": {
          "description": "Negative-control exposures -- pseudo-treatments expected NOT to cause the outcome.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "nc_outcome": {
          "description": "Negative-control outcomes -- variables expected NOT affected by the treatment but sharing the same confounding structure.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "seed": {
          "default": 0,
          "description": "Random seed for reproducible stochastic steps.",
          "type": "integer"
        },
        "time": {
          "description": "Time period column.",
          "type": "string"
        },
        "treat": {
          "description": "First-treatment-period column (0 = never-treated).",
          "type": "string"
        },
        "y": {
          "description": "Outcome.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "treat",
        "time",
        "id",
        "nc_outcome",
        "nc_exposure"
      ],
      "type": "object"
    }
  },
  {
    "description": "Output of DNC + GNN + DiD.",
    "name": "DNCGNNDiDResult",
    "parameters": {
      "properties": {
        "ci": {
          "description": "ci parameter (tuple).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "diagnostics": {
          "description": "diagnostics parameter (Optional[dict]).",
          "type": "object"
        },
        "estimate": {
          "description": "estimate parameter (float).",
          "type": "number"
        },
        "method": {
          "default": "DNC + GNN-aware DiD",
          "description": "Estimator or algorithm variant to use.",
          "type": "string"
        },
        "n_obs": {
          "description": "Number of obs.",
          "type": "integer"
        },
        "se": {
          "description": "se parameter (float).",
          "type": "number"
        }
      },
      "required": [
        "estimate",
        "se",
        "ci",
        "n_obs"
      ],
      "type": "object"
    }
  },
  {
    "description": "Functional doubly-robust CATE estimator. Validation: validated evidence tier (known-truth, reference, external-parity, or Monte Carlo artifact).",
    "name": "focal_cate",
    "parameters": {
      "properties": {
        "covariates": {
          "description": "Covariate matrix, DataFrame, or column names.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "data": {
          "description": "Training data with outcome columns y_columns (one per function evaluation point t).",
          "type": "string"
        },
        "seed": {
          "default": 0,
          "description": "Random seed for reproducible stochastic steps.",
          "type": "integer"
        },
        "test_data": {
          "description": "Defaults to ``data``.",
          "type": "string"
        },
        "treat": {
          "description": "Treatment indicator or first-treatment-period column.",
          "type": "string"
        },
        "y_columns": {
          "description": "Outcome columns; len = number of function points.",
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "required": [
        "data",
        "y_columns",
        "treat",
        "covariates"
      ],
      "type": "object"
    }
  },
  {
    "description": "Output of FOCaL functional CATE estimator.",
    "name": "FunctionalCATEResult",
    "parameters": {
      "properties": {
        "cate_grid": {
          "description": "Grid of cate values to evaluate.",
          "type": "string"
        },
        "function_grid": {
          "description": "Grid of function values to evaluate.",
          "type": "string"
        },
        "n_test": {
          "description": "Number of test.",
          "type": "integer"
        },
        "n_train": {
          "description": "Number of train.",
          "type": "integer"
        },
        "se_grid": {
          "description": "Grid of se values to evaluate.",
          "type": "string"
        }
      },
      "required": [
        "cate_grid",
        "function_grid",
        "se_grid",
        "n_train",
        "n_test"
      ],
      "type": "object"
    }
  },
  {
    "description": "Cluster-based CATE estimator. Validation: validated evidence tier (known-truth, reference, external-parity, or Monte Carlo artifact).",
    "name": "cluster_cate",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "covariates": {
          "description": "Covariate matrix, DataFrame, or column names.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "n_clusters": {
          "default": 4,
          "description": "Number of K-means clusters.",
          "type": "integer"
        },
        "seed": {
          "default": 0,
          "description": "Random seed for reproducible stochastic steps.",
          "type": "integer"
        },
        "treat": {
          "description": "Treatment indicator or first-treatment-period column.",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "treat",
        "covariates"
      ],
      "type": "object"
    }
  },
  {
    "description": "Per-cluster CATE table.",
    "name": "ClusterCATEResult",
    "parameters": {
      "properties": {
        "cluster_table": {
          "description": "cluster_table parameter (pd.DataFrame).",
          "type": "string"
        },
        "n_clusters": {
          "description": "Number of clusters.",
          "type": "integer"
        },
        "n_obs": {
          "description": "Number of obs.",
          "type": "integer"
        }
      },
      "required": [
        "cluster_table",
        "n_clusters",
        "n_obs"
      ],
      "type": "object"
    }
  },
  {
    "description": "Evaluate any CATE estimator via RATE / AUTOC / Qini (Yadlowsky 2025). Validation: certified parity evidence.",
    "name": "cate_eval",
    "parameters": {
      "properties": {
        "T": {
          "description": "Observed outcome and binary treatment.",
          "type": "string"
        },
        "X": {
          "description": "Required if any of ``e_hat / m_hat / mu1_hat / mu0_hat`` is None; cross-fit nuisances are estimated with GBM defaults.",
          "type": "string"
        },
        "Y": {
          "description": "Observed outcome and binary treatment.",
          "type": "string"
        },
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "cate": {
          "description": "Estimated CATE :math:`\\hat\\tau(X_i)` from any estimator.",
          "type": "string"
        },
        "clip": {
          "default": 0.02,
          "description": "Propensity clip.",
          "type": "number"
        },
        "e_hat": {
          "description": "Pre-computed nuisance predictions (e.g., from the same estimator that produced ``cate``). If provided, no internal cross-fitting runs.",
          "type": "string"
        },
        "m_hat": {
          "description": "Pre-computed nuisance predictions (e.g., from the same estimator that produced ``cate``). If provided, no internal cross-fitting runs.",
          "type": "string"
        },
        "mu0_hat": {
          "description": "Pre-computed nuisance predictions (e.g., from the same estimator that produced ``cate``). If provided, no internal cross-fitting runs.",
          "type": "string"
        },
        "mu1_hat": {
          "description": "Pre-computed nuisance predictions (e.g., from the same estimator that produced ``cate``). If provided, no internal cross-fitting runs.",
          "type": "string"
        },
        "n_folds": {
          "default": 5,
          "description": "Number of cross-fitting or cross-validation folds.",
          "type": "integer"
        },
        "q_grid": {
          "default": 100,
          "description": "Grid of q values to evaluate.",
          "type": "integer"
        },
        "random_state": {
          "default": 0,
          "description": "Random seed or RandomState for reproducible stochastic steps.",
          "type": "integer"
        },
        "target": {
          "default": "AUTOC",
          "description": "Which scalar headline to emit; both are computed and returned.",
          "enum": [
            "AUTOC",
            "QINI"
          ],
          "type": "string"
        }
      },
      "required": [
        "cate",
        "Y",
        "T"
      ],
      "type": "object"
    }
  },
  {
    "description": "Output of :func:`cate_eval`.",
    "name": "CATEEvalResult",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "autoc": {
          "description": "autoc parameter (float).",
          "type": "number"
        },
        "autoc_ci": {
          "description": "autoc_ci parameter (Tuple[float, float]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "autoc_se": {
          "description": "autoc_se parameter (float).",
          "type": "number"
        },
        "method": {
          "default": "Yadlowsky et al. 2025 (DR-RATE, IF-SE)",
          "description": "Estimator or algorithm variant to use.",
          "type": "string"
        },
        "n_obs": {
          "description": "Number of obs.",
          "type": "integer"
        },
        "qini": {
          "description": "qini parameter (float).",
          "type": "number"
        },
        "qini_ci": {
          "description": "qini_ci parameter (Tuple[float, float]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "qini_se": {
          "description": "qini_se parameter (float).",
          "type": "number"
        },
        "target": {
          "default": "AUTOC",
          "description": "target parameter (str).",
          "type": "string"
        },
        "toc_curve": {
          "description": "toc_curve parameter (pd.DataFrame).",
          "type": "string"
        }
      },
      "required": [
        "autoc",
        "autoc_se",
        "autoc_ci",
        "qini",
        "qini_se",
        "qini_ci",
        "toc_curve",
        "n_obs"
      ],
      "type": "object"
    }
  },
  {
    "description": "High-order bunching design with bias correction. Validation: validated evidence tier (known-truth, reference, external-parity, or Monte Carlo artifact).",
    "name": "general_bunching",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "bandwidth": {
          "default": 1.0,
          "description": "Bandwidth used for local smoothing or kernel weighting.",
          "type": "number"
        },
        "bin_width": {
          "description": "Defaults to bandwidth / 25.",
          "type": "number"
        },
        "cutoff": {
          "default": 0.0,
          "description": "cutoff parameter (float).",
          "type": "number"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "n_boot": {
          "default": 200,
          "description": "Number of bootstrap replications.",
          "type": "integer"
        },
        "polynomial_order": {
          "default": 4,
          "description": "Order of the counterfactual polynomial fit.",
          "type": "integer"
        },
        "running": {
          "description": "Running variable (e.g. earnings).",
          "type": "string"
        },
        "seed": {
          "default": 0,
          "description": "Random seed for reproducible stochastic steps.",
          "type": "integer"
        }
      },
      "required": [
        "data",
        "running"
      ],
      "type": "object"
    }
  },
  {
    "description": "Output of high-order bunching design.",
    "name": "GeneralBunchingResult",
    "parameters": {
      "properties": {
        "bias_corrected_elasticity": {
          "description": "bias_corrected_elasticity parameter (float).",
          "type": "number"
        },
        "ci": {
          "description": "ci parameter (tuple).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "n_obs": {
          "description": "Number of obs.",
          "type": "integer"
        },
        "naive_elasticity": {
          "description": "naive_elasticity parameter (float).",
          "type": "number"
        },
        "polynomial_order": {
          "description": "polynomial_order parameter (int).",
          "type": "integer"
        },
        "se": {
          "description": "se parameter (float).",
          "type": "number"
        }
      },
      "required": [
        "naive_elasticity",
        "bias_corrected_elasticity",
        "se",
        "ci",
        "polynomial_order",
        "n_obs"
      ],
      "type": "object"
    }
  },
  {
    "description": "Run RDD + RKD + Bunching on the same data.",
    "name": "kink_unified",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "bandwidth": {
          "description": "Bandwidth used for local smoothing or kernel weighting.",
          "type": "number"
        },
        "bin_width": {
          "description": "bin_width parameter (Optional[float]).",
          "type": "number"
        },
        "cutoff": {
          "default": 0.0,
          "description": "cutoff parameter (float).",
          "type": "number"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "polynomial_order": {
          "default": 2,
          "description": "polynomial_order parameter (int).",
          "type": "integer"
        },
        "running": {
          "description": "Running variable (also forms density for Bunching).",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable (used by RDD/RKD).",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "running"
      ],
      "type": "object"
    }
  },
  {
    "description": "Joint RDD + RKD + Bunching estimate at a common cutoff.",
    "name": "KinkUnifiedResult",
    "parameters": {
      "properties": {
        "bandwidth": {
          "description": "Bandwidth used for local smoothing or kernel weighting.",
          "type": "number"
        },
        "bunching_elasticity": {
          "description": "bunching_elasticity parameter (float).",
          "type": "number"
        },
        "bunching_se": {
          "description": "bunching_se parameter (float).",
          "type": "number"
        },
        "n_obs": {
          "description": "Number of obs.",
          "type": "integer"
        },
        "rdd_effect": {
          "description": "rdd_effect parameter (float).",
          "type": "number"
        },
        "rdd_se": {
          "description": "rdd_se parameter (float).",
          "type": "number"
        },
        "rkd_effect": {
          "description": "rkd_effect parameter (float).",
          "type": "number"
        },
        "rkd_se": {
          "description": "rkd_se parameter (float).",
          "type": "number"
        }
      },
      "required": [
        "rdd_effect",
        "rdd_se",
        "rkd_effect",
        "rkd_se",
        "bunching_elasticity",
        "bunching_se",
        "bandwidth",
        "n_obs"
      ],
      "type": "object"
    }
  },
  {
    "description": "Output of :func:`mr_lap`.",
    "name": "MRLapResult",
    "parameters": {
      "properties": {
        "bias_correction": {
          "description": "bias_correction parameter (float).",
          "type": "number"
        },
        "ci_lower": {
          "description": "ci_lower parameter (float).",
          "type": "number"
        },
        "ci_upper": {
          "description": "ci_upper parameter (float).",
          "type": "number"
        },
        "estimate": {
          "description": "estimate parameter (float).",
          "type": "number"
        },
        "estimate_ivw": {
          "description": "estimate_ivw parameter (float).",
          "type": "number"
        },
        "f_mean": {
          "description": "f_mean parameter (float).",
          "type": "number"
        },
        "n_snps": {
          "description": "Number of snps.",
          "type": "integer"
        },
        "overlap_fraction": {
          "description": "overlap_fraction parameter (float).",
          "type": "number"
        },
        "overlap_rho": {
          "description": "overlap_rho parameter (float).",
          "type": "number"
        },
        "p_value": {
          "description": "p_value parameter (float).",
          "type": "number"
        },
        "se": {
          "description": "se parameter (float).",
          "type": "number"
        }
      },
      "required": [
        "estimate",
        "se",
        "ci_lower",
        "ci_upper",
        "p_value",
        "estimate_ivw",
        "bias_correction",
        "overlap_fraction",
        "overlap_rho",
        "f_mean",
        "n_snps"
      ],
      "type": "object"
    }
  },
  {
    "description": "Output of :func:`mr_clust`.",
    "name": "MRClustResult",
    "parameters": {
      "properties": {
        "K": {
          "description": "K parameter (int).",
          "type": "integer"
        },
        "assignments": {
          "description": "assignments parameter (np.ndarray).",
          "type": "string"
        },
        "bic": {
          "description": "bic parameter (dict).",
          "type": "object"
        },
        "cluster_estimates": {
          "description": "cluster_estimates parameter (pd.DataFrame).",
          "type": "string"
        },
        "loglik": {
          "description": "loglik parameter (float).",
          "type": "number"
        },
        "n_snps": {
          "description": "Number of snps.",
          "type": "integer"
        },
        "responsibilities": {
          "description": "responsibilities parameter (np.ndarray).",
          "type": "string"
        },
        "wald_ratios": {
          "description": "wald_ratios parameter (np.ndarray).",
          "type": "string"
        },
        "wald_se": {
          "description": "wald_se parameter (np.ndarray).",
          "type": "string"
        }
      },
      "required": [
        "cluster_estimates",
        "assignments",
        "responsibilities",
        "K",
        "bic",
        "loglik",
        "wald_ratios",
        "wald_se",
        "n_snps"
      ],
      "type": "object"
    }
  },
  {
    "description": "Output of :func:`grapple`.",
    "name": "GrappleResult",
    "parameters": {
      "properties": {
        "ci_lower": {
          "description": "ci_lower parameter (float).",
          "type": "number"
        },
        "ci_upper": {
          "description": "ci_upper parameter (float).",
          "type": "number"
        },
        "converged": {
          "description": "converged parameter (bool).",
          "type": "boolean"
        },
        "estimate": {
          "description": "estimate parameter (float).",
          "type": "number"
        },
        "loglik": {
          "description": "loglik parameter (float).",
          "type": "number"
        },
        "loss": {
          "default": "tukey",
          "description": "loss parameter (str).",
          "type": "string"
        },
        "n_snps": {
          "description": "Number of snps.",
          "type": "integer"
        },
        "p_value": {
          "description": "p_value parameter (float).",
          "type": "number"
        },
        "se": {
          "description": "se parameter (float).",
          "type": "number"
        },
        "tau2": {
          "description": "tau2 parameter (float).",
          "type": "number"
        },
        "tau2_se": {
          "default": NaN,
          "description": "tau2_se parameter (float).",
          "type": "number"
        }
      },
      "required": [
        "estimate",
        "se",
        "ci_lower",
        "ci_upper",
        "p_value",
        "tau2",
        "loglik",
        "converged",
        "n_snps"
      ],
      "type": "object"
    }
  },
  {
    "description": "Output of :func:`mr_cml`.",
    "name": "MRcMLResult",
    "parameters": {
      "properties": {
        "K_selected": {
          "description": "K_selected parameter (int).",
          "type": "integer"
        },
        "ci_lower": {
          "description": "ci_lower parameter (float).",
          "type": "number"
        },
        "ci_upper": {
          "description": "ci_upper parameter (float).",
          "type": "number"
        },
        "estimate": {
          "description": "estimate parameter (float).",
          "type": "number"
        },
        "invalid_snps": {
          "description": "invalid_snps parameter (np.ndarray).",
          "type": "string"
        },
        "loglik": {
          "description": "loglik parameter (float).",
          "type": "number"
        },
        "model_average": {
          "default": false,
          "description": "model_average parameter (bool).",
          "type": "boolean"
        },
        "n_samples": {
          "description": "Number of samples.",
          "type": "integer"
        },
        "n_snps": {
          "description": "Number of snps.",
          "type": "integer"
        },
        "p_value": {
          "description": "p_value parameter (float).",
          "type": "number"
        },
        "path": {
          "description": "path parameter (pd.DataFrame).",
          "type": "string"
        },
        "se": {
          "description": "se parameter (float).",
          "type": "number"
        }
      },
      "required": [
        "estimate",
        "se",
        "ci_lower",
        "ci_upper",
        "p_value",
        "K_selected",
        "invalid_snps",
        "path",
        "loglik",
        "n_snps"
      ],
      "type": "object"
    }
  },
  {
    "description": "Output of :func:`mr_raps`.",
    "name": "MRRapsResult",
    "parameters": {
      "properties": {
        "ci_lower": {
          "description": "ci_lower parameter (float).",
          "type": "number"
        },
        "ci_upper": {
          "description": "ci_upper parameter (float).",
          "type": "number"
        },
        "converged": {
          "description": "converged parameter (bool).",
          "type": "boolean"
        },
        "estimate": {
          "description": "estimate parameter (float).",
          "type": "number"
        },
        "loglik_robust": {
          "description": "loglik_robust parameter (float).",
          "type": "number"
        },
        "loss": {
          "default": "huber",
          "description": "loss parameter (str).",
          "type": "string"
        },
        "n_pruned": {
          "default": 0,
          "description": "Number of pruned.",
          "type": "integer"
        },
        "n_snps": {
          "description": "Number of snps.",
          "type": "integer"
        },
        "over_dispersion": {
          "default": true,
          "description": "over_dispersion parameter (bool).",
          "type": "boolean"
        },
        "p_value": {
          "description": "p_value parameter (float).",
          "type": "number"
        },
        "se": {
          "description": "se parameter (float).",
          "type": "number"
        },
        "tau2": {
          "description": "tau2 parameter (float).",
          "type": "number"
        },
        "tau2_se": {
          "default": NaN,
          "description": "tau2_se parameter (float).",
          "type": "number"
        },
        "tuning_c": {
          "description": "tuning_c parameter (float).",
          "type": "number"
        }
      },
      "required": [
        "estimate",
        "se",
        "ci_lower",
        "ci_upper",
        "p_value",
        "tau2",
        "loglik_robust",
        "converged",
        "tuning_c",
        "n_snps"
      ],
      "type": "object"
    }
  },
  {
    "description": "Return the full list of registered MR method names (incl. aliases).",
    "name": "mr_available_methods",
    "parameters": {
      "properties": {},
      "required": [],
      "type": "object"
    }
  },
  {
    "description": "Return the full list of registered conformal ``kind`` names.",
    "name": "conformal_available_kinds",
    "parameters": {
      "properties": {},
      "required": [],
      "type": "object"
    }
  },
  {
    "description": "Return the full list of registered interference ``design`` names.",
    "name": "interference_available_designs",
    "parameters": {
      "properties": {},
      "required": [],
      "type": "object"
    }
  },
  {
    "description": "A social network as a dense adjacency matrix.",
    "name": "Graph",
    "parameters": {
      "properties": {
        "adjacency": {
          "description": "Square ``(n, n)`` adjacency. Entry ``A[i, j]`` is the weight of the tie from ``i`` to ``j`` (``1`` for an unweighted tie).",
          "type": "string"
        },
        "allow_self_loops": {
          "default": false,
          "description": "Keep the diagonal of ``adjacency``. Off by default (SNA convention).",
          "type": "boolean"
        },
        "directed": {
          "default": false,
          "description": "If ``False`` the graph is treated as undirected and stored symmetrically.",
          "type": "boolean"
        },
        "node_labels": {
          "description": "Human-readable node names (defaults to ``\"0\"..\"n-1\"``).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "weighted": {
          "description": "Whether to treat off-diagonal entries as weights. If ``None`` (default) it is inferred: the graph is *weighted* when any entry is not in ``{0, 1}``.",
          "type": "boolean"
        }
      },
      "required": [
        "adjacency"
      ],
      "type": "object"
    }
  },
  {
    "description": "Structural summary of a network (the ``sp.network_summary`` output).",
    "name": "NetworkSummaryResult",
    "parameters": {
      "properties": {
        "assortativity": {
          "description": "assortativity parameter (float).",
          "type": "number"
        },
        "average_clustering": {
          "description": "average_clustering parameter (float).",
          "type": "number"
        },
        "average_path_length": {
          "description": "average_path_length parameter (float).",
          "type": "number"
        },
        "density": {
          "description": "density parameter (float).",
          "type": "number"
        },
        "detail": {
          "default": null,
          "description": "Amount of result detail to return.",
          "type": "object"
        },
        "diameter": {
          "description": "diameter parameter (float).",
          "type": "number"
        },
        "directed": {
          "description": "directed parameter (bool).",
          "type": "boolean"
        },
        "is_connected": {
          "description": "is_connected parameter (bool).",
          "type": "boolean"
        },
        "largest_component_frac": {
          "description": "largest_component_frac parameter (float).",
          "type": "number"
        },
        "mean_degree": {
          "description": "mean_degree parameter (float).",
          "type": "number"
        },
        "n_components": {
          "description": "Number of generated components or embedding dimensions.",
          "type": "integer"
        },
        "n_edges": {
          "description": "Number of edges.",
          "type": "integer"
        },
        "n_nodes": {
          "description": "Number of nodes.",
          "type": "integer"
        },
        "reciprocity": {
          "description": "reciprocity parameter (float).",
          "type": "number"
        },
        "transitivity": {
          "description": "transitivity parameter (float).",
          "type": "number"
        },
        "weighted": {
          "description": "weighted parameter (bool).",
          "type": "boolean"
        }
      },
      "required": [
        "n_nodes",
        "n_edges",
        "directed",
        "weighted",
        "density",
        "n_components",
        "largest_component_frac",
        "is_connected",
        "diameter",
        "average_path_length",
        "mean_degree",
        "transitivity",
        "average_clustering",
        "reciprocity",
        "assortativity"
      ],
      "type": "object"
    }
  },
  {
    "description": "Global clustering coefficient (transitivity). Validation: certified parity evidence.",
    "name": "transitivity",
    "parameters": {
      "properties": {
        "graph": {
          "description": "graph parameter.",
          "type": "string"
        }
      },
      "required": [
        "graph"
      ],
      "type": "object"
    }
  },
  {
    "description": "Per-node local clustering coefficient (Watts-Strogatz). Validation: certified parity evidence.",
    "name": "clustering",
    "parameters": {
      "properties": {
        "graph": {
          "description": "Network whose local clustering coefficients should be computed.",
          "type": "string"
        }
      },
      "required": [
        "graph"
      ],
      "type": "object"
    }
  },
  {
    "description": "Directed reciprocity: share of arcs that are reciprocated. Validation: certified parity evidence.",
    "name": "reciprocity",
    "parameters": {
      "properties": {
        "graph": {
          "description": "Directed network. Undirected inputs return ``1.0`` by construction.",
          "type": "string"
        }
      },
      "required": [
        "graph"
      ],
      "type": "object"
    }
  },
  {
    "description": "Newman degree-assortativity coefficient. Validation: certified parity evidence.",
    "name": "assortativity",
    "parameters": {
      "properties": {
        "graph": {
          "description": "Network whose endpoint-degree correlation should be computed.",
          "type": "string"
        }
      },
      "required": [
        "graph"
      ],
      "type": "object"
    }
  },
  {
    "description": "Connected-component decomposition. Validation: certified parity evidence.",
    "name": "network_components",
    "parameters": {
      "properties": {
        "connection": {
          "default": "weak",
          "description": "For directed graphs, whether to use weak or strong connectivity. Ignored for undirected graphs.",
          "enum": [
            "weak",
            "strong"
          ],
          "type": "string"
        },
        "graph": {
          "description": "graph parameter.",
          "type": "string"
        }
      },
      "required": [
        "graph"
      ],
      "type": "object"
    }
  },
  {
    "description": "Connected-component decomposition of a graph.",
    "name": "ComponentsResult",
    "parameters": {
      "properties": {
        "connection": {
          "default": "weak",
          "description": "connection parameter (str).",
          "type": "string"
        },
        "largest_size": {
          "description": "largest_size parameter (int).",
          "type": "integer"
        },
        "membership": {
          "description": "membership parameter (pd.Series).",
          "type": "string"
        },
        "n_components": {
          "description": "Number of generated components or embedding dimensions.",
          "type": "integer"
        },
        "sizes": {
          "description": "sizes parameter (List[int]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "required": [
        "n_components",
        "membership",
        "sizes",
        "largest_size"
      ],
      "type": "object"
    }
  },
  {
    "description": "Per-node centrality table (the ``sp.centrality`` output).",
    "name": "CentralityResult",
    "parameters": {
      "properties": {
        "measures": {
          "description": "measures parameter (List[str]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "most_central": {
          "default": null,
          "description": "most_central parameter (Dict[str, Any]).",
          "type": "object"
        },
        "scores": {
          "description": "scores parameter (pd.DataFrame).",
          "type": "string"
        }
      },
      "required": [
        "scores",
        "measures"
      ],
      "type": "object"
    }
  },
  {
    "description": "Degree centrality. Validation: certified parity evidence.",
    "name": "degree_centrality",
    "parameters": {
      "properties": {
        "graph": {
          "description": "graph parameter.",
          "type": "string"
        },
        "mode": {
          "default": "all",
          "description": "mode parameter (str).",
          "enum": [
            "all",
            "in",
            "out"
          ],
          "type": "string"
        },
        "normalized": {
          "default": true,
          "description": "Divide by ``n - 1`` (the maximum possible degree).",
          "type": "boolean"
        }
      },
      "required": [
        "graph"
      ],
      "type": "object"
    }
  },
  {
    "description": "Closeness centrality with the Wasserman-Faust disconnected correction. Validation: certified parity evidence.",
    "name": "closeness_centrality",
    "parameters": {
      "properties": {
        "graph": {
          "description": "Network to score.",
          "type": "string"
        },
        "weighted": {
          "description": "Use tie weights as distances. Defaults to the graph's weighted flag.",
          "type": "boolean"
        }
      },
      "required": [
        "graph"
      ],
      "type": "object"
    }
  },
  {
    "description": "Shortest-path betweenness centrality (Brandes 2001). Validation: certified parity evidence.",
    "name": "betweenness_centrality",
    "parameters": {
      "properties": {
        "graph": {
          "description": "graph parameter.",
          "type": "string"
        },
        "normalized": {
          "default": true,
          "description": "Scale by ``1/((n-1)(n-2))`` (directed) or ``2/((n-1)(n-2))`` (undirected), so scores lie in ``[0, 1]``.",
          "type": "boolean"
        },
        "weighted": {
          "description": "Use tie weights as path lengths (Dijkstra). Defaults to the graph's own weighted flag.",
          "type": "boolean"
        }
      },
      "required": [
        "graph"
      ],
      "type": "object"
    }
  },
  {
    "description": "Eigenvector centrality (leading eigenvector of the adjacency matrix). Validation: certified parity evidence.",
    "name": "eigenvector_centrality",
    "parameters": {
      "properties": {
        "graph": {
          "description": "Network to score.",
          "type": "string"
        },
        "max_iter": {
          "default": 1000,
          "description": "Maximum power-iteration steps.",
          "type": "integer"
        },
        "tol": {
          "default": 1e-09,
          "description": "Convergence tolerance on the score vector.",
          "type": "number"
        },
        "weighted": {
          "description": "Use tie weights; defaults to the graph's weighted flag.",
          "type": "boolean"
        }
      },
      "required": [
        "graph"
      ],
      "type": "object"
    }
  },
  {
    "description": "Katz centrality. Validation: certified parity evidence.",
    "name": "katz_centrality",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.1,
          "description": "Attenuation parameter. Must be below ``1 / lambda_max(A)``.",
          "type": "number"
        },
        "beta": {
          "default": 1.0,
          "description": "Baseline score for each node.",
          "type": "number"
        },
        "graph": {
          "description": "Network to score.",
          "type": "string"
        },
        "normalized": {
          "default": true,
          "description": "L2-normalise the returned vector.",
          "type": "boolean"
        },
        "weighted": {
          "description": "Use tie weights; defaults to the graph's weighted flag.",
          "type": "boolean"
        }
      },
      "required": [
        "graph"
      ],
      "type": "object"
    }
  },
  {
    "description": "Google PageRank (Brin & Page 1998). Validation: certified parity evidence.",
    "name": "pagerank",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.85,
          "description": "Probability of following a tie rather than teleporting.",
          "type": "number"
        },
        "graph": {
          "description": "Directed or undirected network to score.",
          "type": "string"
        },
        "max_iter": {
          "default": 1000,
          "description": "Maximum power-iteration steps.",
          "type": "integer"
        },
        "tol": {
          "default": 1e-12,
          "description": "L1 convergence tolerance.",
          "type": "number"
        },
        "weighted": {
          "description": "Use tie weights; defaults to the graph's weighted flag.",
          "type": "boolean"
        }
      },
      "required": [
        "graph"
      ],
      "type": "object"
    }
  },
  {
    "description": "Bonacich (1987) power centrality. Validation: certified parity evidence.",
    "name": "bonacich_power",
    "parameters": {
      "properties": {
        "beta": {
          "default": 0.1,
          "description": "Bonacich attenuation parameter. Positive values reward ties to powerful nodes; negative values reward ties to weak nodes.",
          "type": "number"
        },
        "graph": {
          "description": "Network to score.",
          "type": "string"
        },
        "weighted": {
          "description": "Use tie weights; defaults to the graph's weighted flag.",
          "type": "boolean"
        }
      },
      "required": [
        "graph"
      ],
      "type": "object"
    }
  },
  {
    "description": "Kleinberg HITS hub and authority scores. Validation: certified parity evidence.",
    "name": "hits",
    "parameters": {
      "properties": {
        "graph": {
          "description": "Directed or undirected network to score.",
          "type": "string"
        },
        "max_iter": {
          "default": 1000,
          "description": "Maximum power-iteration steps.",
          "type": "integer"
        },
        "tol": {
          "default": 1e-12,
          "description": "L1 convergence tolerance for hub and authority vectors.",
          "type": "number"
        }
      },
      "required": [
        "graph"
      ],
      "type": "object"
    }
  },
  {
    "description": "Community-detection partition (the ``sp.community_detection`` output).",
    "name": "CommunityResult",
    "parameters": {
      "properties": {
        "membership": {
          "description": "membership parameter (pd.Series).",
          "type": "string"
        },
        "method": {
          "description": "Estimator or algorithm variant to use.",
          "type": "string"
        },
        "modularity": {
          "description": "modularity parameter (float).",
          "type": "number"
        },
        "n_communities": {
          "description": "Number of communities.",
          "type": "integer"
        },
        "sizes": {
          "default": null,
          "description": "sizes parameter (List[int]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "required": [
        "membership",
        "n_communities",
        "modularity",
        "method"
      ],
      "type": "object"
    }
  },
  {
    "description": "Newman-Girvan modularity ``Q`` of a partition. Validation: certified parity evidence.",
    "name": "network_modularity",
    "parameters": {
      "properties": {
        "graph": {
          "description": "graph parameter.",
          "type": "string"
        },
        "membership": {
          "description": "Community label per node (any hashable labels).",
          "type": "string"
        },
        "resolution": {
          "default": 1.0,
          "description": "resolution parameter (float).",
          "type": "number"
        }
      },
      "required": [
        "graph",
        "membership"
      ],
      "type": "object"
    }
  },
  {
    "description": "QAP / MRQAP network-regression result (``sp.netlm`` / ``sp.netlogit``).",
    "name": "QAPResult",
    "parameters": {
      "properties": {
        "coefficients": {
          "description": "coefficients parameter (pd.DataFrame).",
          "type": "string"
        },
        "detail": {
          "default": null,
          "description": "Amount of result detail to return.",
          "type": "object"
        },
        "method": {
          "description": "Estimator or algorithm variant to use.",
          "type": "string"
        },
        "n_dyads": {
          "description": "Number of dyads.",
          "type": "integer"
        },
        "nperm": {
          "description": "nperm parameter (int).",
          "type": "integer"
        },
        "p_qap": {
          "description": "p_qap parameter (Dict[str, float]).",
          "type": "object"
        },
        "permutation": {
          "default": "dsp",
          "description": "permutation parameter (str).",
          "type": "string"
        },
        "r_squared": {
          "description": "r_squared parameter (float).",
          "type": "number"
        }
      },
      "required": [
        "coefficients",
        "p_qap",
        "r_squared",
        "n_dyads",
        "nperm",
        "method"
      ],
      "type": "object"
    }
  },
  {
    "description": "Dyadic OLS with dyadic-cluster-robust standard errors.",
    "name": "DyadicRegressionResult",
    "parameters": {
      "properties": {
        "coefficients": {
          "description": "coefficients parameter (pd.DataFrame).",
          "type": "string"
        },
        "detail": {
          "default": null,
          "description": "Amount of result detail to return.",
          "type": "object"
        },
        "n_dyads": {
          "description": "Number of dyads.",
          "type": "integer"
        },
        "n_nodes": {
          "description": "Number of nodes.",
          "type": "integer"
        },
        "r_squared": {
          "description": "r_squared parameter (float).",
          "type": "number"
        }
      },
      "required": [
        "coefficients",
        "n_dyads",
        "n_nodes",
        "r_squared"
      ],
      "type": "object"
    }
  },
  {
    "description": "ERGM fit by maximum pseudo-likelihood (the ``sp.ergm`` output).",
    "name": "ERGMResult",
    "parameters": {
      "properties": {
        "coefficients": {
          "description": "coefficients parameter (pd.DataFrame).",
          "type": "string"
        },
        "detail": {
          "default": null,
          "description": "Amount of result detail to return.",
          "type": "object"
        },
        "directed": {
          "description": "directed parameter (bool).",
          "type": "boolean"
        },
        "dyad_independent": {
          "description": "dyad_independent parameter (bool).",
          "type": "boolean"
        },
        "log_pseudolikelihood": {
          "description": "log_pseudolikelihood parameter (float).",
          "type": "number"
        },
        "method": {
          "default": "MPLE",
          "description": "Estimator or algorithm variant to use.",
          "type": "string"
        },
        "n_dyads": {
          "description": "Number of dyads.",
          "type": "integer"
        },
        "terms": {
          "description": "terms parameter (List[str]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "required": [
        "coefficients",
        "terms",
        "log_pseudolikelihood",
        "n_dyads",
        "directed",
        "dyad_independent"
      ],
      "type": "object"
    }
  },
  {
    "description": "Zachary's karate club friendship network (undirected, 34 nodes). Validation: certified parity evidence.",
    "name": "karate_club",
    "parameters": {
      "properties": {},
      "required": [],
      "type": "object"
    }
  },
  {
    "description": "Padgett's Florentine marriage network (undirected, 15 families). Validation: certified parity evidence.",
    "name": "florentine_families",
    "parameters": {
      "properties": {},
      "required": [],
      "type": "object"
    }
  },
  {
    "description": "Draw a network as a node-link diagram.",
    "name": "network_plot",
    "parameters": {
      "properties": {
        "ax": {
          "description": "ax parameter.",
          "type": "string"
        },
        "cmap": {
          "default": "tab10",
          "description": "cmap parameter (str).",
          "type": "string"
        },
        "edge_alpha": {
          "default": 0.35,
          "description": "edge_alpha parameter (float).",
          "type": "number"
        },
        "graph": {
          "description": "graph parameter.",
          "type": "string"
        },
        "labels": {
          "default": false,
          "description": "Annotate nodes with their labels.",
          "type": "boolean"
        },
        "layout": {
          "default": "spring",
          "description": "layout parameter (str).",
          "enum": [
            "spring",
            "circular"
          ],
          "type": "string"
        },
        "node_color": {
          "description": "Per-node values (e.g. a community membership :class:`pandas.Series`) mapped through ``cmap``.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "node_size": {
          "description": "Per-node sizes (e.g. a centrality score); rescaled to a sensible point range.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "seed": {
          "default": 0,
          "description": "Layout seed (spring layout).",
          "type": "integer"
        },
        "title": {
          "description": "title parameter (Optional[str]).",
          "type": "string"
        }
      },
      "required": [
        "graph"
      ],
      "type": "object"
    }
  },
  {
    "description": "Container for :func:`network_exposure` Horvitz-Thompson estimates.",
    "name": "NetworkExposureResult",
    "parameters": {
      "properties": {
        "contrasts": {
          "description": "contrasts parameter (pd.DataFrame).",
          "type": "string"
        },
        "design": {
          "description": "design parameter (str).",
          "type": "string"
        },
        "detail": {
          "default": null,
          "description": "Amount of result detail to return.",
          "type": "object"
        },
        "estimates": {
          "description": "estimates parameter (pd.DataFrame).",
          "type": "string"
        },
        "exposure_levels": {
          "description": "exposure_levels parameter (List[str]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "mapping": {
          "description": "mapping parameter (str).",
          "type": "string"
        },
        "n_obs": {
          "description": "Number of obs.",
          "type": "integer"
        },
        "p_treat": {
          "description": "p_treat parameter (float).",
          "type": "number"
        }
      },
      "required": [
        "estimates",
        "contrasts",
        "exposure_levels",
        "n_obs",
        "p_treat",
        "design",
        "mapping"
      ],
      "type": "object"
    }
  },
  {
    "description": "Fit the linear-in-means peer-effects model via 2SLS. Validation: validated evidence tier (known-truth, reference, external-parity, or Monte Carlo artifact).",
    "name": "peer_effects",
    "parameters": {
      "properties": {
        "W": {
          "description": "Covariates, proxy variables, or weights used by this estimator.",
          "type": "string"
        },
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "covariates": {
          "description": "Covariate matrix, DataFrame, or column names.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "include_contextual": {
          "default": true,
          "description": "Include the contextual peer effect (W X) as regressors.",
          "type": "boolean"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "covariates",
        "W"
      ],
      "type": "object"
    }
  },
  {
    "description": "Container for :func:`peer_effects` linear-in-means estimates.",
    "name": "PeerEffectsResult",
    "parameters": {
      "properties": {
        "ci": {
          "description": "ci parameter (Dict[str, tuple]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "coefficients": {
          "description": "coefficients parameter (pd.DataFrame).",
          "type": "string"
        },
        "contextual_peer": {
          "description": "contextual_peer parameter (Dict[str, float]).",
          "type": "object"
        },
        "detail": {
          "default": null,
          "description": "Amount of result detail to return.",
          "type": "object"
        },
        "direct": {
          "description": "direct parameter (Dict[str, float]).",
          "type": "object"
        },
        "endogenous_peer": {
          "description": "endogenous_peer parameter (float).",
          "type": "number"
        },
        "n_obs": {
          "description": "Number of obs.",
          "type": "integer"
        },
        "se": {
          "description": "se parameter (Dict[str, float]).",
          "type": "object"
        }
      },
      "required": [
        "endogenous_peer",
        "contextual_peer",
        "direct",
        "se",
        "ci",
        "n_obs",
        "coefficients"
      ],
      "type": "object"
    }
  },
  {
    "description": "Split conformal prediction with per-calibration-point weights. Validation: validated evidence tier (known-truth, reference, external-parity, or Monte Carlo artifact).",
    "name": "weighted_conformal_prediction",
    "parameters": {
      "properties": {
        "X_calib": {
          "description": "Calibration fold used to compute non-conformity scores.",
          "type": "string"
        },
        "X_test": {
          "description": "Points at which to produce prediction intervals.",
          "type": "string"
        },
        "X_train": {
          "description": "Training fold used to fit the base regression model.",
          "type": "string"
        },
        "alpha": {
          "default": 0.1,
          "description": "Miscoverage level (interval targets ``1-alpha`` coverage).",
          "type": "number"
        },
        "model": {
          "description": "Defaults to ``RandomForestRegressor(n_estimators=200, min_samples_leaf=5, random_state=0)``.",
          "type": "string"
        },
        "weights_calib": {
          "description": "Per-calibration-point likelihood-ratio weights ``w_i = f_test(X_i) / f_train(X_i)`` for covariate-shift correction. If None, uniform weights.",
          "type": "string"
        },
        "y_calib": {
          "description": "Calibration fold used to compute non-conformity scores.",
          "type": "string"
        },
        "y_train": {
          "description": "Training fold used to fit the base regression model.",
          "type": "string"
        }
      },
      "required": [
        "X_train",
        "y_train",
        "X_calib",
        "y_calib",
        "X_test"
      ],
      "type": "object"
    }
  },
  {
    "description": "Prediction intervals for the counterfactual potential outcomes",
    "name": "conformal_counterfactual",
    "parameters": {
      "properties": {
        "X_test": {
          "description": "Points at which to return intervals. Defaults to ``data[covariates]``.",
          "type": "string"
        },
        "alpha": {
          "default": 0.1,
          "description": "Miscoverage level.",
          "type": "number"
        },
        "calib_frac": {
          "default": 0.3,
          "description": "Fraction of each arm used for calibration.",
          "type": "number"
        },
        "covariates": {
          "description": "Columns used as features for both the outcome and propensity models.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "model": {
          "description": "Model variant or parameterisation to fit.",
          "type": "string"
        },
        "propensity_model": {
          "description": "propensity_model parameter (Optional[BaseEstimator]).",
          "type": "string"
        },
        "random_state": {
          "description": "Random seed or RandomState for reproducible stochastic steps.",
          "type": "integer"
        },
        "treat": {
          "description": "Outcome and 0/1 treatment column names.",
          "type": "string"
        },
        "y": {
          "description": "Outcome and 0/1 treatment column names.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "treat",
        "covariates"
      ],
      "type": "object"
    }
  },
  {
    "description": "Counterfactual prediction intervals under each potential outcome.",
    "name": "ConformalCounterfactualResult",
    "parameters": {
      "properties": {
        "X": {
          "description": "Feature matrix or covariate DataFrame.",
          "type": "string"
        },
        "alpha": {
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "lower_Y0": {
          "description": "lower_Y0 parameter (np.ndarray).",
          "type": "string"
        },
        "lower_Y1": {
          "description": "lower_Y1 parameter (np.ndarray).",
          "type": "string"
        },
        "marginal_coverage_estimate": {
          "description": "marginal_coverage_estimate parameter (float).",
          "type": "number"
        },
        "method": {
          "default": "Lei-Candes-2021-split-CQR",
          "description": "Estimator or algorithm variant to use.",
          "type": "string"
        },
        "upper_Y0": {
          "description": "upper_Y0 parameter (np.ndarray).",
          "type": "string"
        },
        "upper_Y1": {
          "description": "upper_Y1 parameter (np.ndarray).",
          "type": "string"
        }
      },
      "required": [
        "X",
        "lower_Y1",
        "upper_Y1",
        "lower_Y0",
        "upper_Y0",
        "alpha",
        "marginal_coverage_estimate"
      ],
      "type": "object"
    }
  },
  {
    "description": "Conformal prediction intervals for the individual treatment Validation: validated evidence tier (known-truth, reference, external-parity, or Monte Carlo artifact).",
    "name": "conformal_ite_interval",
    "parameters": {
      "properties": {
        "X_test": {
          "description": "X_test parameter (Optional[np.ndarray]).",
          "type": "string"
        },
        "alpha": {
          "default": 0.1,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "calib_frac": {
          "default": 0.3,
          "description": "calib_frac parameter (float).",
          "type": "number"
        },
        "covariates": {
          "description": "Covariate matrix, DataFrame, or column names.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "model": {
          "description": "Model variant or parameterisation to fit.",
          "type": "string"
        },
        "propensity_model": {
          "description": "propensity_model parameter (Optional[BaseEstimator]).",
          "type": "string"
        },
        "random_state": {
          "description": "Random seed or RandomState for reproducible stochastic steps.",
          "type": "integer"
        },
        "treat": {
          "description": "Treatment indicator or first-treatment-period column.",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "treat",
        "covariates"
      ],
      "type": "object"
    }
  },
  {
    "description": "Prediction intervals for the individual treatment effect tau(x).",
    "name": "ConformalITEResult",
    "parameters": {
      "properties": {
        "X": {
          "description": "Feature matrix or covariate DataFrame.",
          "type": "string"
        },
        "alpha": {
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "lower": {
          "description": "lower parameter (np.ndarray).",
          "type": "string"
        },
        "method": {
          "default": "nested-counterfactual-bound (Lei-Candes 2021 Eq. 3.4)",
          "description": "Estimator or algorithm variant to use.",
          "type": "string"
        },
        "point": {
          "description": "point parameter (np.ndarray).",
          "type": "string"
        },
        "upper": {
          "description": "upper parameter (np.ndarray).",
          "type": "string"
        }
      },
      "required": [
        "X",
        "lower",
        "upper",
        "point",
        "alpha"
      ],
      "type": "object"
    }
  },
  {
    "description": "Instrumental forest: heterogeneous local average treatment effects.",
    "name": "instrumental_forest",
    "parameters": {
      "properties": {
        "W_hat": {
          "description": "Precomputed ``E[Y|X]``, ``E[W|X]``, ``E[Z|X]``; by default each is the out-of-bag prediction of a regression forest with ``max(50, n_estimators // 4)`` trees.",
          "type": "string"
        },
        "Y_hat": {
          "description": "Precomputed ``E[Y|X]``, ``E[W|X]``, ``E[Z|X]``; by default each is the out-of-bag prediction of a regression forest with ``max(50, n_estimators // 4)`` trees.",
          "type": "string"
        },
        "Z_hat": {
          "description": "Precomputed ``E[Y|X]``, ``E[W|X]``, ``E[Z|X]``; by default each is the out-of-bag prediction of a regression forest with ``max(50, n_estimators // 4)`` trees.",
          "type": "string"
        },
        "alpha": {
          "description": "Significance level of the reported interval.",
          "type": "number"
        },
        "ci_group_size": {
          "description": "Trees per little bag; 1 disables variance estimates.",
          "type": "integer"
        },
        "clusters": {
          "description": "Cluster ids: trees sample whole clusters and every standard error is cluster-robust.",
          "type": "string"
        },
        "compliance_score": {
          "description": "``Delta(X_i)`` for the average-effect scores; by default estimated by an auxiliary 500-tree causal forest of ``W`` on ``Z``.",
          "type": "string"
        },
        "covariates": {
          "description": "Effect modifiers ``X``; the nuisances are also regressed on them.",
          "type": "string"
        },
        "data": {
          "description": "Input data. When omitted, ``y``, ``treat``, ``instrument`` and ``covariates`` are arrays.",
          "type": "string"
        },
        "equalize_cluster_weights": {
          "description": "Give every cluster the same weight (incompatible with ``weights``).",
          "type": "boolean"
        },
        "honest": {
          "description": "grf honesty options.",
          "type": "string"
        },
        "honesty_fraction": {
          "description": "grf honesty options.",
          "type": "string"
        },
        "honesty_prune_leaves": {
          "description": "grf honesty options.",
          "type": "string"
        },
        "imbalance_penalty": {
          "description": "imbalance_penalty parameter (float).",
          "type": "number"
        },
        "instrument": {
          "description": "Outcome, treatment (binary or continuous) and instrument (binary or continuous).",
          "type": "string"
        },
        "max_depth": {
          "description": "Depth cap (no grf analogue).",
          "type": "integer"
        },
        "max_samples": {
          "description": "Fraction of clusters drawn per tree (grf ``sample.fraction``).",
          "type": "number"
        },
        "min_samples_leaf": {
          "description": "grf ``min.node.size``.",
          "type": "integer"
        },
        "mtry": {
          "description": "Candidate variables per split (grf default ``min(ceil(sqrt(p) + 20), p)``).",
          "type": "integer"
        },
        "n_bootstrap": {
          "description": "Ignored. Standard errors now come from the doubly-robust scores.",
          "type": "string"
        },
        "n_estimators": {
          "description": "Trees (grf ``num.trees``).",
          "type": "integer"
        },
        "n_jobs": {
          "description": "Number of jobs.",
          "type": "integer"
        },
        "random_state": {
          "description": "Random seed or RandomState for reproducible stochastic steps.",
          "type": "integer"
        },
        "reduced_form_weight": {
          "description": "Mix the IV split criterion with the causal-forest criterion that treats ``W`` as exogenous (grf ``reduced.form.weight``).",
          "type": "number"
        },
        "split_alpha": {
          "description": "grf ``alpha``: minimum share of the parent's instrument variation each child of a stabilised split keeps.",
          "type": "number"
        },
        "stabilize_splits": {
          "description": "Use the instrument in the split constraints.",
          "type": "boolean"
        },
        "treat": {
          "description": "Outcome, treatment (binary or continuous) and instrument (binary or continuous).",
          "type": "string"
        },
        "weights": {
          "description": "Sample weights (grf ``sample.weights``).",
          "type": "string"
        },
        "y": {
          "description": "Outcome, treatment (binary or continuous) and instrument (binary or continuous).",
          "type": "string"
        }
      },
      "required": [],
      "type": "object"
    }
  },
  {
    "description": "A fitted instrumental forest.",
    "name": "IVForestResult",
    "parameters": {
      "properties": {},
      "required": [],
      "type": "object"
    }
  },
  {
    "description": "A fitted multi-arm causal forest.",
    "name": "MultiArmForestResult",
    "parameters": {
      "properties": {},
      "required": [],
      "type": "object"
    }
  },
  {
    "description": "A fitted linear-model forest.",
    "name": "LMForestResult",
    "parameters": {
      "properties": {},
      "required": [],
      "type": "object"
    }
  },
  {
    "description": "Honest regression forest for ``E[Y | X]`` (``grf::regression_forest``).",
    "name": "regression_forest",
    "parameters": {
      "properties": {
        "ci_group_size": {
          "default": 2,
          "description": "Trees per little bag (variance estimates need >= 2).",
          "type": "integer"
        },
        "clusters": {
          "description": "Cluster ids (trees sample whole clusters).",
          "type": "string"
        },
        "covariates": {
          "description": "Covariates ``X``.",
          "type": "string"
        },
        "data": {
          "description": "Input data; when omitted, ``y`` and ``covariates`` are arrays.",
          "type": "string"
        },
        "equalize_cluster_weights": {
          "default": false,
          "description": "equalize_cluster_weights parameter (bool).",
          "type": "boolean"
        },
        "honest": {
          "default": true,
          "description": "grf honesty options.",
          "type": "boolean"
        },
        "honesty_fraction": {
          "default": 0.5,
          "description": "grf honesty options.",
          "type": "number"
        },
        "honesty_prune_leaves": {
          "default": true,
          "description": "grf honesty options.",
          "type": "boolean"
        },
        "imbalance_penalty": {
          "default": 0.0,
          "description": "imbalance_penalty parameter (float).",
          "type": "number"
        },
        "max_depth": {
          "description": "max_depth parameter (Optional[int]).",
          "type": "integer"
        },
        "max_samples": {
          "default": 0.5,
          "description": "grf ``sample.fraction``.",
          "type": "number"
        },
        "min_samples_leaf": {
          "default": 5,
          "description": "grf ``min.node.size``.",
          "type": "integer"
        },
        "mtry": {
          "description": "mtry parameter (Optional[int]).",
          "type": "integer"
        },
        "n_estimators": {
          "default": 2000,
          "description": "Trees (grf ``num.trees``).",
          "type": "integer"
        },
        "n_jobs": {
          "default": 1,
          "description": "Number of jobs.",
          "type": "integer"
        },
        "random_state": {
          "default": 42,
          "description": "Random seed or RandomState for reproducible stochastic steps.",
          "type": "integer"
        },
        "split_alpha": {
          "default": 0.05,
          "description": "grf ``alpha`` (minimum child share).",
          "type": "number"
        },
        "weights": {
          "description": "Sample weights (grf ``sample.weights``).",
          "type": "string"
        },
        "y": {
          "description": "Outcome.",
          "type": "string"
        }
      },
      "required": [],
      "type": "object"
    }
  },
  {
    "description": "Multi-task regression forest for ``E[Y_1, ..., Y_q | X]``",
    "name": "multi_regression_forest",
    "parameters": {
      "properties": {
        "ci_group_size": {
          "default": 1,
          "description": "Trees per little bag (variance estimates need >= 2).",
          "type": "integer"
        },
        "clusters": {
          "description": "Cluster ids (trees sample whole clusters).",
          "type": "string"
        },
        "covariates": {
          "description": "Covariates ``X``.",
          "type": "string"
        },
        "data": {
          "description": "Input data; when omitted, ``y`` and ``covariates`` are arrays.",
          "type": "string"
        },
        "equalize_cluster_weights": {
          "default": false,
          "description": "equalize_cluster_weights parameter (bool).",
          "type": "boolean"
        },
        "honest": {
          "default": true,
          "description": "grf honesty options.",
          "type": "boolean"
        },
        "honesty_fraction": {
          "default": 0.5,
          "description": "grf honesty options.",
          "type": "number"
        },
        "honesty_prune_leaves": {
          "default": true,
          "description": "grf honesty options.",
          "type": "boolean"
        },
        "imbalance_penalty": {
          "default": 0.0,
          "description": "imbalance_penalty parameter (float).",
          "type": "number"
        },
        "max_depth": {
          "description": "max_depth parameter (Optional[int]).",
          "type": "integer"
        },
        "max_samples": {
          "default": 0.5,
          "description": "grf ``sample.fraction``.",
          "type": "number"
        },
        "min_samples_leaf": {
          "default": 5,
          "description": "grf ``min.node.size``.",
          "type": "integer"
        },
        "mtry": {
          "description": "mtry parameter (Optional[int]).",
          "type": "integer"
        },
        "n_estimators": {
          "default": 2000,
          "description": "Trees (grf ``num.trees``).",
          "type": "integer"
        },
        "n_jobs": {
          "default": 1,
          "description": "Number of jobs.",
          "type": "integer"
        },
        "random_state": {
          "default": 42,
          "description": "Random seed or RandomState for reproducible stochastic steps.",
          "type": "integer"
        },
        "split_alpha": {
          "default": 0.05,
          "description": "grf ``alpha`` (minimum child share).",
          "type": "number"
        },
        "weights": {
          "description": "Sample weights (grf ``sample.weights``).",
          "type": "string"
        },
        "y": {
          "description": "Outcomes.",
          "type": "string"
        }
      },
      "required": [],
      "type": "object"
    }
  },
  {
    "description": "Probability forest for ``P[Y = k | X]`` (``grf::probability_forest``).",
    "name": "probability_forest",
    "parameters": {
      "properties": {
        "ci_group_size": {
          "default": 2,
          "description": "Trees per little bag (variance estimates need >= 2).",
          "type": "integer"
        },
        "clusters": {
          "description": "Cluster ids (trees sample whole clusters).",
          "type": "string"
        },
        "covariates": {
          "description": "Covariates ``X``.",
          "type": "string"
        },
        "data": {
          "description": "Input data; when omitted, ``y`` and ``covariates`` are arrays.",
          "type": "string"
        },
        "equalize_cluster_weights": {
          "default": false,
          "description": "equalize_cluster_weights parameter (bool).",
          "type": "boolean"
        },
        "honest": {
          "default": true,
          "description": "grf honesty options.",
          "type": "boolean"
        },
        "honesty_fraction": {
          "default": 0.5,
          "description": "grf honesty options.",
          "type": "number"
        },
        "honesty_prune_leaves": {
          "default": true,
          "description": "grf honesty options.",
          "type": "boolean"
        },
        "imbalance_penalty": {
          "default": 0.0,
          "description": "imbalance_penalty parameter (float).",
          "type": "number"
        },
        "max_depth": {
          "description": "max_depth parameter (Optional[int]).",
          "type": "integer"
        },
        "max_samples": {
          "default": 0.5,
          "description": "grf ``sample.fraction``.",
          "type": "number"
        },
        "min_samples_leaf": {
          "default": 5,
          "description": "grf ``min.node.size``.",
          "type": "integer"
        },
        "mtry": {
          "description": "mtry parameter (Optional[int]).",
          "type": "integer"
        },
        "n_estimators": {
          "default": 2000,
          "description": "Trees (grf ``num.trees``).",
          "type": "integer"
        },
        "n_jobs": {
          "default": 1,
          "description": "Number of jobs.",
          "type": "integer"
        },
        "random_state": {
          "default": 42,
          "description": "Random seed or RandomState for reproducible stochastic steps.",
          "type": "integer"
        },
        "split_alpha": {
          "default": 0.05,
          "description": "grf ``alpha`` (minimum child share).",
          "type": "number"
        },
        "weights": {
          "description": "Sample weights (grf ``sample.weights``).",
          "type": "string"
        },
        "y": {
          "description": "Class labels (any hashable values; classes are sorted).",
          "type": "string"
        }
      },
      "required": [],
      "type": "object"
    }
  },
  {
    "description": "Quantile forest for conditional quantiles of ``Y`` (``grf::quantile_forest``). Validation: validated evidence tier (known-truth, reference, external-parity, or Monte Carlo artifact).",
    "name": "quantile_forest",
    "parameters": {
      "properties": {
        "clusters": {
          "description": "Cluster ids (trees sample whole clusters).",
          "type": "string"
        },
        "covariates": {
          "description": "Covariates ``X``.",
          "type": "string"
        },
        "data": {
          "description": "Input data; when omitted, ``y`` and ``covariates`` are arrays.",
          "type": "string"
        },
        "equalize_cluster_weights": {
          "default": false,
          "description": "equalize_cluster_weights parameter (bool).",
          "type": "boolean"
        },
        "honest": {
          "default": true,
          "description": "grf honesty options.",
          "type": "boolean"
        },
        "honesty_fraction": {
          "default": 0.5,
          "description": "grf honesty options.",
          "type": "number"
        },
        "honesty_prune_leaves": {
          "default": true,
          "description": "grf honesty options.",
          "type": "boolean"
        },
        "imbalance_penalty": {
          "default": 0.0,
          "description": "imbalance_penalty parameter (float).",
          "type": "number"
        },
        "max_depth": {
          "description": "max_depth parameter (Optional[int]).",
          "type": "integer"
        },
        "max_samples": {
          "default": 0.5,
          "description": "grf ``sample.fraction``.",
          "type": "number"
        },
        "min_samples_leaf": {
          "default": 5,
          "description": "grf ``min.node.size``.",
          "type": "integer"
        },
        "mtry": {
          "description": "mtry parameter (Optional[int]).",
          "type": "integer"
        },
        "n_estimators": {
          "default": 2000,
          "description": "Trees (grf ``num.trees``).",
          "type": "integer"
        },
        "n_jobs": {
          "default": 1,
          "description": "Number of jobs.",
          "type": "integer"
        },
        "quantiles": {
          "default": [
            0.1,
            0.5,
            0.9
          ],
          "description": "Quantiles that calibrate the splits and are predicted by default.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "random_state": {
          "default": 42,
          "description": "Random seed or RandomState for reproducible stochastic steps.",
          "type": "integer"
        },
        "regression_splitting": {
          "default": false,
          "description": "Use ordinary regression splits instead (grf ``regression.splitting``; the quantile regression forest of Meinshausen 2006).",
          "type": "boolean"
        },
        "split_alpha": {
          "default": 0.05,
          "description": "grf ``alpha`` (minimum child share).",
          "type": "number"
        },
        "weights": {
          "description": "Sample weights (grf ``sample.weights``).",
          "type": "string"
        },
        "y": {
          "description": "Outcome.",
          "type": "string"
        }
      },
      "required": [],
      "type": "object"
    }
  },
  {
    "description": "A fitted regression / multi-regression / probability / quantile forest.",
    "name": "PredictionForest",
    "parameters": {
      "properties": {
        "kind_name": {
          "description": "kind_name parameter (str).",
          "type": "string"
        }
      },
      "required": [
        "kind_name"
      ],
      "type": "object"
    }
  },
  {
    "description": "Survival forest: conditional survival curves ``S(t | x)`` from Validation: validated evidence tier (known-truth, reference, external-parity, or Monte Carlo artifact).",
    "name": "survival_forest",
    "parameters": {
      "properties": {
        "clusters": {
          "description": "Cluster-sampled trees; sample weights enter the curve estimates (not the log-rank splits, as in grf).",
          "type": "string"
        },
        "covariates": {
          "description": "Covariates ``X``.",
          "type": "string"
        },
        "data": {
          "description": "Input data; when omitted, the other inputs are arrays.",
          "type": "string"
        },
        "equalize_cluster_weights": {
          "default": false,
          "description": "Cluster-sampled trees; sample weights enter the curve estimates (not the log-rank splits, as in grf).",
          "type": "boolean"
        },
        "event": {
          "description": "1 = event observed, 0 = censored.",
          "type": "string"
        },
        "failure_times": {
          "description": "Increasing grid of times for the curves; default all distinct event times.",
          "type": "string"
        },
        "honest": {
          "default": true,
          "description": "Forest options (grf names ``sample.fraction``, ...).",
          "type": "boolean"
        },
        "honesty_fraction": {
          "default": 0.5,
          "description": "Forest options (grf names ``sample.fraction``, ...).",
          "type": "number"
        },
        "honesty_prune_leaves": {
          "default": true,
          "description": "Forest options (grf names ``sample.fraction``, ...).",
          "type": "boolean"
        },
        "max_depth": {
          "description": "max_depth parameter (Optional[int]).",
          "type": "integer"
        },
        "max_samples": {
          "default": 0.5,
          "description": "Forest options (grf names ``sample.fraction``, ...).",
          "type": "number"
        },
        "min_samples_leaf": {
          "default": 15,
          "description": "min_samples_leaf parameter (int).",
          "type": "integer"
        },
        "mtry": {
          "description": "Forest options (grf names ``sample.fraction``, ...).",
          "type": "integer"
        },
        "n_estimators": {
          "default": 1000,
          "description": "Number of estimators.",
          "type": "integer"
        },
        "n_jobs": {
          "default": 1,
          "description": "Number of jobs.",
          "type": "integer"
        },
        "prediction_type": {
          "default": "Kaplan-Meier",
          "description": "prediction_type parameter (str).",
          "enum": [
            "Kaplan-Meier",
            "Nelson-Aalen"
          ],
          "type": "string"
        },
        "random_state": {
          "default": 42,
          "description": "Random seed or RandomState for reproducible stochastic steps.",
          "type": "integer"
        },
        "split_alpha": {
          "default": 0.05,
          "description": "Each child must hold at least ``max(1, split_alpha * n_parent)`` failures (grf ``alpha``).",
          "type": "number"
        },
        "time": {
          "description": "Observed time (event or censoring), non-negative.",
          "type": "string"
        },
        "weights": {
          "description": "Cluster-sampled trees; sample weights enter the curve estimates (not the log-rank splits, as in grf).",
          "type": "string"
        }
      },
      "required": [],
      "type": "object"
    }
  },
  {
    "description": "A fitted survival forest.",
    "name": "SurvivalForestResult",
    "parameters": {
      "properties": {},
      "required": [],
      "type": "object"
    }
  },
  {
    "description": "Causal survival forest: heterogeneous effects on censored survival.",
    "name": "causal_survival",
    "parameters": {
      "properties": {
        "W_hat": {
          "description": "Precomputed propensities; default out-of-bag regression forest.",
          "type": "string"
        },
        "alpha": {
          "default": 0.05,
          "description": "Significance level of the reported interval.",
          "type": "number"
        },
        "ci_group_size": {
          "default": 2,
          "description": "Forest options (grf names).",
          "type": "integer"
        },
        "clusters": {
          "description": "As in :func:`statspai.causal_forest`.",
          "type": "string"
        },
        "covariates": {
          "description": "Covariates ``X`` (confounders and effect modifiers).",
          "type": "string"
        },
        "data": {
          "description": "Input data; when omitted, the other inputs are arrays.",
          "type": "string"
        },
        "equalize_cluster_weights": {
          "default": false,
          "description": "As in :func:`statspai.causal_forest`.",
          "type": "boolean"
        },
        "event": {
          "description": "1 = event observed, 0 = censored.",
          "type": "string"
        },
        "failure_times": {
          "description": "Grid for the survival nuisance forests (default: observed times).",
          "type": "string"
        },
        "honest": {
          "default": true,
          "description": "honest parameter (bool).",
          "type": "boolean"
        },
        "honesty_fraction": {
          "default": 0.5,
          "description": "honesty_fraction parameter (float).",
          "type": "number"
        },
        "honesty_prune_leaves": {
          "default": true,
          "description": "honesty_prune_leaves parameter (bool).",
          "type": "boolean"
        },
        "horizon": {
          "description": "``h`` of the estimand. Defaults to the 80th percentile of observed event times (recorded in ``detail``); grf requires it -- choose it from the study design.",
          "type": "number"
        },
        "imbalance_penalty": {
          "default": 0.0,
          "description": "Forest options (grf names).",
          "type": "number"
        },
        "max_depth": {
          "description": "Forest options (grf names).",
          "type": "integer"
        },
        "max_samples": {
          "default": 0.5,
          "description": "max_samples parameter (float).",
          "type": "number"
        },
        "min_samples_leaf": {
          "default": 5,
          "description": "min_samples_leaf parameter (int).",
          "type": "integer"
        },
        "mtry": {
          "description": "mtry parameter (Optional[int]).",
          "type": "integer"
        },
        "n_estimators": {
          "default": 2000,
          "description": "Number of estimators.",
          "type": "integer"
        },
        "n_jobs": {
          "default": 1,
          "description": "Number of jobs.",
          "type": "integer"
        },
        "propensity_bounds": {
          "description": "Clip ``e`` in the average-effect score denominators (off by default, as in grf).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "random_state": {
          "default": 42,
          "description": "Random seed or RandomState for reproducible stochastic steps.",
          "type": "integer"
        },
        "split_alpha": {
          "default": 0.05,
          "description": "grf ``alpha``; each child must also hold ``split_alpha`` of the parent's size in failures.",
          "type": "number"
        },
        "stabilize_splits": {
          "default": true,
          "description": "Forest options (grf names).",
          "type": "boolean"
        },
        "target": {
          "default": "RMST",
          "description": "target parameter (str).",
          "enum": [
            "RMST",
            "survival_probability"
          ],
          "type": "string"
        },
        "time": {
          "description": "Observed time ``min(T, C)``, non-negative.",
          "type": "string"
        },
        "treat": {
          "description": "Binary treatment.",
          "type": "string"
        },
        "weights": {
          "description": "As in :func:`statspai.causal_forest`.",
          "type": "string"
        }
      },
      "required": [],
      "type": "object"
    }
  },
  {
    "description": "A fitted causal survival forest.",
    "name": "CausalSurvivalForestResult",
    "parameters": {
      "properties": {},
      "required": [],
      "type": "object"
    }
  },
  {
    "description": "Output of :func:`bcf_factor_exposure`.",
    "name": "BCFFactorExposureResult",
    "parameters": {
      "properties": {
        "diagnostics": {
          "default": null,
          "description": "diagnostics parameter (Dict[str, Any]).",
          "type": "object"
        },
        "factor_loadings": {
          "description": "factor_loadings parameter (pd.DataFrame).",
          "type": "string"
        },
        "factor_scores": {
          "description": "factor_scores parameter (pd.DataFrame).",
          "type": "string"
        },
        "method": {
          "default": "bcf_factor_exposure",
          "description": "Estimator or algorithm variant to use.",
          "type": "string"
        },
        "per_factor_ate": {
          "description": "per_factor_ate parameter (pd.DataFrame).",
          "type": "string"
        },
        "total_mixture_ate": {
          "description": "total_mixture_ate parameter (float).",
          "type": "number"
        },
        "total_mixture_ci": {
          "description": "total_mixture_ci parameter (tuple).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "total_mixture_se": {
          "description": "total_mixture_se parameter (float).",
          "type": "number"
        }
      },
      "required": [
        "factor_loadings",
        "factor_scores",
        "per_factor_ate",
        "total_mixture_ate",
        "total_mixture_se",
        "total_mixture_ci"
      ],
      "type": "object"
    }
  },
  {
    "description": "Longitudinal BCF output container.",
    "name": "BCFLongResult",
    "parameters": {
      "properties": {
        "average_ate": {
          "description": "average_ate parameter (float).",
          "type": "number"
        },
        "average_ci": {
          "description": "average_ci parameter (tuple).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "average_se": {
          "description": "average_se parameter (float).",
          "type": "number"
        },
        "individual_cate": {
          "description": "individual_cate parameter (pd.DataFrame).",
          "type": "string"
        },
        "model_info": {
          "default": null,
          "description": "model_info parameter (Dict).",
          "type": "object"
        },
        "per_time_ate": {
          "description": "per_time_ate parameter (pd.DataFrame).",
          "type": "string"
        }
      },
      "required": [
        "per_time_ate",
        "average_ate",
        "average_se",
        "average_ci",
        "individual_cate"
      ],
      "type": "object"
    }
  },
  {
    "description": "Output of :func:`bcf_ordinal`.",
    "name": "BCFOrdinalResult",
    "parameters": {
      "properties": {
        "ate": {
          "description": "ate parameter (pd.Series).",
          "type": "string"
        },
        "ate_ci": {
          "description": "ate_ci parameter (pd.DataFrame).",
          "type": "string"
        },
        "ate_se": {
          "description": "ate_se parameter (pd.Series).",
          "type": "string"
        },
        "cate": {
          "description": "cate parameter (pd.DataFrame).",
          "type": "string"
        },
        "cate_se": {
          "description": "cate_se parameter (pd.DataFrame).",
          "type": "string"
        },
        "diagnostics": {
          "default": null,
          "description": "diagnostics parameter (Dict[str, Any]).",
          "type": "object"
        },
        "levels": {
          "description": "levels parameter (List[Any]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "method": {
          "default": "bcf_ordinal",
          "description": "Estimator or algorithm variant to use.",
          "type": "string"
        }
      },
      "required": [
        "cate",
        "cate_se",
        "ate",
        "ate_se",
        "ate_ci",
        "levels"
      ],
      "type": "object"
    }
  },
  {
    "description": "Double negative control estimator (Miao et al. 2018; Shi et al. 2020).",
    "name": "double_negative_control",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "covariates": {
          "description": "Covariate matrix, DataFrame, or column names.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "nce": {
          "description": "nce parameter (str).",
          "type": "string"
        },
        "nco": {
          "description": "nco parameter (str).",
          "type": "string"
        },
        "treat": {
          "description": "Treatment indicator or first-treatment-period column.",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "treat",
        "nce",
        "nco"
      ],
      "type": "object"
    }
  },
  {
    "description": "Regress outcome on a *negative-control exposure*.",
    "name": "negative_control_exposure",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "covariates": {
          "description": "Covariate matrix, DataFrame, or column names.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "nce": {
          "description": "nce parameter (str).",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "nce"
      ],
      "type": "object"
    }
  },
  {
    "description": "Lipsitch-style NCO calibration.",
    "name": "negative_control_outcome",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "covariates": {
          "description": "Measured confounders to condition on.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "nco": {
          "description": "Negative-control outcome -- a variable plausibly unaffected by the true treatment but sharing confounders with the real Y.",
          "type": "string"
        },
        "treat": {
          "description": "Treatment indicator or exposure variable.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "nco",
        "treat"
      ],
      "type": "object"
    }
  },
  {
    "description": "Doubly-robust regression-based PCI estimator for the ATE.",
    "name": "proximal_regression",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "covariates": {
          "description": "Measured covariates X.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "propensity_bounds": {
          "default": [
            0.02,
            0.98
          ],
          "description": "propensity_bounds parameter (tuple).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "treat": {
          "description": "Binary treatment column.",
          "type": "string"
        },
        "w_proxy": {
          "description": "Outcome-inducing confounding proxy W.",
          "type": "string"
        },
        "y": {
          "description": "Outcome column.",
          "type": "string"
        },
        "z_proxy": {
          "description": "Treatment-inducing confounding proxy Z.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "treat",
        "z_proxy",
        "w_proxy"
      ],
      "type": "object"
    }
  },
  {
    "description": "Unified result for negative-control procedures.",
    "name": "NegativeControlResult",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "ci": {
          "description": "ci parameter (tuple).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "detail": {
          "default": null,
          "description": "Amount of result detail to return.",
          "type": "object"
        },
        "estimate": {
          "description": "estimate parameter (float).",
          "type": "number"
        },
        "interpretation": {
          "default": "",
          "description": "interpretation parameter (str).",
          "type": "string"
        },
        "method": {
          "description": "Estimator or algorithm variant to use.",
          "type": "string"
        },
        "n_obs": {
          "default": 0,
          "description": "Number of obs.",
          "type": "integer"
        },
        "pvalue": {
          "description": "pvalue parameter (float).",
          "type": "number"
        },
        "se": {
          "description": "se parameter (float).",
          "type": "number"
        }
      },
      "required": [
        "method",
        "estimate",
        "se",
        "pvalue",
        "ci"
      ],
      "type": "object"
    }
  },
  {
    "description": "Result of the regression-based proximal causal inference estimator.",
    "name": "ProximalRegResult",
    "parameters": {
      "properties": {
        "ate": {
          "description": "ate parameter (float).",
          "type": "number"
        },
        "bridge_coefs": {
          "description": "bridge_coefs parameter (Dict[str, float]).",
          "type": "object"
        },
        "ci": {
          "description": "ci parameter (tuple).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "detail": {
          "default": null,
          "description": "Amount of result detail to return.",
          "type": "object"
        },
        "n_obs": {
          "description": "Number of obs.",
          "type": "integer"
        },
        "propensity_coefs": {
          "description": "propensity_coefs parameter (Dict[str, float]).",
          "type": "object"
        },
        "pvalue": {
          "description": "pvalue parameter (float).",
          "type": "number"
        },
        "se": {
          "description": "se parameter (float).",
          "type": "number"
        }
      },
      "required": [
        "ate",
        "se",
        "ci",
        "pvalue",
        "bridge_coefs",
        "propensity_coefs",
        "n_obs"
      ],
      "type": "object"
    }
  },
  {
    "description": "Direct outcome regression (plug-in Q-model) OPE. Validation: certified parity evidence.",
    "name": "direct_method",
    "parameters": {
      "properties": {
        "A": {
          "description": "A parameter (np.ndarray).",
          "type": "string"
        },
        "R": {
          "description": "R parameter (np.ndarray).",
          "type": "string"
        },
        "X": {
          "description": "Feature matrix or covariate DataFrame.",
          "type": "string"
        },
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "n_actions": {
          "description": "Number of actions.",
          "type": "integer"
        },
        "pi_target": {
          "description": "pi_target parameter.",
          "type": "string"
        },
        "q_hat": {
          "description": "q_hat parameter (Optional[np.ndarray]).",
          "type": "string"
        }
      },
      "required": [
        "X",
        "A",
        "R",
        "pi_target"
      ],
      "type": "object"
    }
  },
  {
    "description": "Doubly-robust OPE (Dudik et al. 2011). Validation: certified parity evidence.",
    "name": "doubly_robust",
    "parameters": {
      "properties": {
        "A": {
          "description": "A parameter (np.ndarray).",
          "type": "string"
        },
        "R": {
          "description": "R parameter (np.ndarray).",
          "type": "string"
        },
        "X": {
          "description": "Feature matrix or covariate DataFrame.",
          "type": "string"
        },
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "clip": {
          "default": 50.0,
          "description": "clip parameter (float).",
          "type": "number"
        },
        "n_actions": {
          "description": "Number of actions.",
          "type": "integer"
        },
        "pi_behavior": {
          "description": "pi_behavior parameter (Optional[np.ndarray]).",
          "type": "string"
        },
        "pi_target": {
          "description": "pi_target parameter.",
          "type": "string"
        },
        "q_hat": {
          "description": "q_hat parameter (Optional[np.ndarray]).",
          "type": "string"
        }
      },
      "required": [
        "X",
        "A",
        "R",
        "pi_target"
      ],
      "type": "object"
    }
  },
  {
    "description": "Inverse propensity score OPE. Validation: certified parity evidence.",
    "name": "ips",
    "parameters": {
      "properties": {
        "A": {
          "description": "A parameter (np.ndarray).",
          "type": "string"
        },
        "R": {
          "description": "R parameter (np.ndarray).",
          "type": "string"
        },
        "X": {
          "description": "Feature matrix or covariate DataFrame.",
          "type": "string"
        },
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "clip": {
          "default": 50.0,
          "description": "clip parameter (float).",
          "type": "number"
        },
        "pi_behavior": {
          "description": "pi_behavior parameter (Optional[np.ndarray]).",
          "type": "string"
        },
        "pi_target": {
          "description": "pi_target parameter.",
          "type": "string"
        }
      },
      "required": [
        "X",
        "A",
        "R",
        "pi_target"
      ],
      "type": "object"
    }
  },
  {
    "description": "Self-normalised IPS (bias-reduction for large IS weights). Validation: certified parity evidence.",
    "name": "snips",
    "parameters": {
      "properties": {
        "A": {
          "description": "A parameter (np.ndarray).",
          "type": "string"
        },
        "R": {
          "description": "R parameter (np.ndarray).",
          "type": "string"
        },
        "X": {
          "description": "Feature matrix or covariate DataFrame.",
          "type": "string"
        },
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "clip": {
          "default": 50.0,
          "description": "clip parameter (float).",
          "type": "number"
        },
        "pi_behavior": {
          "description": "pi_behavior parameter (Optional[np.ndarray]).",
          "type": "string"
        },
        "pi_target": {
          "description": "pi_target parameter.",
          "type": "string"
        }
      },
      "required": [
        "X",
        "A",
        "R",
        "pi_target"
      ],
      "type": "object"
    }
  },
  {
    "description": "Parametric four-way decomposition of TE = CDE + INT_ref + INT_med + PIE. Validation: certified parity evidence.",
    "name": "four_way_decomposition",
    "parameters": {
      "properties": {
        "a0": {
          "default": 0.0,
          "description": "Reference and comparison levels of the treatment (default 0, 1).",
          "type": "number"
        },
        "a1": {
          "default": 1.0,
          "description": "Reference and comparison levels of the treatment (default 0, 1).",
          "type": "number"
        },
        "covariates": {
          "description": "Covariate matrix, DataFrame, or column names.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "m0": {
          "default": 0.0,
          "description": "Mediator reference level at which CDE is evaluated.",
          "type": "number"
        },
        "mediator": {
          "description": "mediator parameter (str).",
          "type": "string"
        },
        "treat": {
          "description": "Treatment indicator or first-treatment-period column.",
          "type": "string"
        },
        "vce": {
          "default": "ols",
          "description": "Covariance of the two regressions fed to the delta-method standard errors in ``result.se`` (covariates are held fixed at their sample means, as in the point estimates). ``'ols'`` uses ``s^2 (X'X)^{-1}`` with ``s^2 = RSS / (n - p)`` -- R ``CMAverse::cmest(estimation = \"paramfunc\", inference = \"delta\")``; ``'ml'`` uses ``RSS / n`` -- Stata ``med4way``, which fits both linear models by maximum likelihood.",
          "enum": [
            "ols",
            "ml"
          ],
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "treat",
        "mediator"
      ],
      "type": "object"
    }
  },
  {
    "description": "Result container for :func:`four_way_decomposition`.",
    "name": "FourWayResult",
    "parameters": {
      "properties": {
        "cde": {
          "description": "cde parameter (float).",
          "type": "number"
        },
        "detail": {
          "default": null,
          "description": "Amount of result detail to return.",
          "type": "object"
        },
        "int_med": {
          "description": "int_med parameter (float).",
          "type": "number"
        },
        "int_ref": {
          "description": "int_ref parameter (float).",
          "type": "number"
        },
        "n_obs": {
          "description": "Number of obs.",
          "type": "integer"
        },
        "pie": {
          "description": "pie parameter (float).",
          "type": "number"
        },
        "proportions": {
          "description": "proportions parameter (Dict[str, float]).",
          "type": "object"
        },
        "se": {
          "default": null,
          "description": "se parameter (Dict[str, float]).",
          "type": "object"
        },
        "total_effect": {
          "description": "total_effect parameter (float).",
          "type": "number"
        }
      },
      "required": [
        "cde",
        "int_ref",
        "int_med",
        "pie",
        "total_effect",
        "proportions",
        "n_obs"
      ],
      "type": "object"
    }
  },
  {
    "description": "Segmented regression for interrupted time series. Validation: certified parity evidence.",
    "name": "its",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "data": {
          "description": "Observed series, sorted in time.",
          "type": "string"
        },
        "hac_lag": {
          "default": 4,
          "description": "Newey-West truncation lag (Bartlett weights ``1 - j/(L+1)``).",
          "type": "integer"
        },
        "hac_small_sample": {
          "default": false,
          "description": "hac_small_sample parameter (bool).",
          "type": "boolean"
        },
        "intervention": {
          "description": "Time index (integer row position) at which the intervention begins. Required.",
          "type": "integer"
        },
        "seasonality_harmonics": {
          "default": 2,
          "description": "seasonality_harmonics parameter (int).",
          "type": "integer"
        },
        "seasonality_period": {
          "description": "Period P of Fourier seasonal terms (e.g. 12 for monthly data with annual cycle). If None, no seasonality is added.",
          "type": "integer"
        },
        "time": {
          "description": "Time column. If None, uses row index 0..n-1.",
          "type": "string"
        },
        "y": {
          "description": "Outcome column.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y"
      ],
      "type": "object"
    }
  },
  {
    "description": "Result container for :func:`its`.",
    "name": "ITSResult",
    "parameters": {
      "properties": {
        "ci_level": {
          "description": "ci_level parameter (tuple).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "ci_slope": {
          "description": "ci_slope parameter (tuple).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "coefficients": {
          "description": "coefficients parameter (pd.DataFrame).",
          "type": "string"
        },
        "detail": {
          "default": null,
          "description": "Amount of result detail to return.",
          "type": "object"
        },
        "intervention_time": {
          "description": "intervention_time parameter (int).",
          "type": "integer"
        },
        "level_change": {
          "description": "level_change parameter (float).",
          "type": "number"
        },
        "n_obs": {
          "description": "Number of obs.",
          "type": "integer"
        },
        "pvalue_level": {
          "description": "pvalue_level parameter (float).",
          "type": "number"
        },
        "pvalue_slope": {
          "description": "pvalue_slope parameter (float).",
          "type": "number"
        },
        "se_level": {
          "description": "se_level parameter (float).",
          "type": "number"
        },
        "se_slope": {
          "description": "se_slope parameter (float).",
          "type": "number"
        },
        "slope_change": {
          "description": "slope_change parameter (float).",
          "type": "number"
        }
      },
      "required": [
        "level_change",
        "slope_change",
        "se_level",
        "se_slope",
        "ci_level",
        "ci_slope",
        "pvalue_level",
        "pvalue_slope",
        "coefficients",
        "n_obs",
        "intervention_time"
      ],
      "type": "object"
    }
  },
  {
    "description": "Longitudinal TMLE for static regime contrasts. Validation: certified parity evidence.",
    "name": "ltmle",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "baseline": {
          "description": "Baseline time-invariant covariates.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "censoring": {
          "description": "Censoring indicator column per time point (``1=observed``, ``0=censored``). If None, no censoring is modeled.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "covariates_time": {
          "description": "``covariates_time[k]`` lists time-k covariate columns (may be empty). Length ``K``.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "data": {
          "description": "Wide-format panel: one row per unit.",
          "type": "string"
        },
        "outcome_type": {
          "default": "auto",
          "description": "``auto`` detects from unique values of ``y``.",
          "enum": [
            "auto",
            "binary",
            "continuous"
          ],
          "type": "string"
        },
        "propensity_bounds": {
          "default": [
            0.01,
            1.0
          ],
          "description": "Bounds on the *cumulative* probability of following the regime (and remaining uncensored) up to each time point, ``prod_j g_j`` -- the ``gbounds`` of R ``ltmle``, whose default this is.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "regime_control": {
          "description": "Regimes to contrast. Default: all-1 vs all-0. A regime may also be a **callable** ``regime(k, history)`` for *dynamic regimes* that depend on the simulated / observed history of baseline and time-varying covariates. The callable receives ``k`` (int 0..K-1) and ``history`` -- a dict mapping column name to the length-``n`` numpy array observed up to that timepoint -- and must return a length-``n`` numpy array of 0/1 treatment assignments.",
          "enum": [
            "0",
            "1"
          ],
          "type": "string"
        },
        "regime_treated": {
          "description": "Regimes to contrast. Default: all-1 vs all-0. A regime may also be a **callable** ``regime(k, history)`` for *dynamic regimes* that depend on the simulated / observed history of baseline and time-varying covariates. The callable receives ``k`` (int 0..K-1) and ``history`` -- a dict mapping column name to the length-``n`` numpy array observed up to that timepoint -- and must return a length-``n`` numpy array of 0/1 treatment assignments.",
          "enum": [
            "0",
            "1"
          ],
          "type": "string"
        },
        "treatments": {
          "description": "Treatment column per time point, length ``K``.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Final outcome column.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "treatments",
        "covariates_time"
      ],
      "type": "object"
    }
  },
  {
    "description": "Structured output of :func:`ltmle`.",
    "name": "LTMLEResult",
    "parameters": {
      "properties": {
        "K": {
          "description": "K parameter (int).",
          "type": "integer"
        },
        "ate": {
          "description": "ate parameter (float).",
          "type": "number"
        },
        "ci": {
          "description": "ci parameter (tuple[float, float]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "detail": {
          "default": null,
          "description": "Amount of result detail to return.",
          "type": "object"
        },
        "n_obs": {
          "description": "Number of obs.",
          "type": "integer"
        },
        "psi_control": {
          "description": "psi_control parameter (float).",
          "type": "number"
        },
        "psi_treated": {
          "description": "psi_treated parameter (float).",
          "type": "number"
        },
        "pvalue": {
          "description": "pvalue parameter (float).",
          "type": "number"
        },
        "regime_control": {
          "description": "regime_control parameter (Sequence[int]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "regime_treated": {
          "description": "regime_treated parameter (Sequence[int]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "se": {
          "description": "se parameter (float).",
          "type": "number"
        }
      },
      "required": [
        "psi_treated",
        "psi_control",
        "ate",
        "se",
        "ci",
        "pvalue",
        "K",
        "n_obs",
        "regime_treated",
        "regime_control"
      ],
      "type": "object"
    }
  },
  {
    "description": "Full diagnostic output of :func:`harvest_did`.",
    "name": "HarvestDIDResult",
    "parameters": {
      "properties": {
        "alpha": {
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "ci": {
          "description": "ci parameter (tuple).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "comparisons": {
          "description": "comparisons parameter (pd.DataFrame).",
          "type": "string"
        },
        "diagnostics": {
          "default": null,
          "description": "diagnostics parameter (Dict[str, Any]).",
          "type": "object"
        },
        "estimate": {
          "description": "estimate parameter (float).",
          "type": "number"
        },
        "event_study": {
          "description": "event_study parameter (pd.DataFrame).",
          "type": "string"
        },
        "method": {
          "default": "harvest_did",
          "description": "Estimator or algorithm variant to use.",
          "type": "string"
        },
        "n_comparisons": {
          "description": "Number of comparisons.",
          "type": "integer"
        },
        "pretrend_test": {
          "description": "pretrend_test parameter (Dict[str, float]).",
          "type": "object"
        },
        "se": {
          "description": "se parameter (float).",
          "type": "number"
        }
      },
      "required": [
        "estimate",
        "se",
        "ci",
        "alpha",
        "n_comparisons",
        "comparisons",
        "event_study",
        "pretrend_test"
      ],
      "type": "object"
    }
  },
  {
    "description": "Output of :func:`inward_outward_spillover`.",
    "name": "InwardOutwardResult",
    "parameters": {
      "properties": {
        "inward_effect": {
          "description": "inward_effect parameter (float).",
          "type": "number"
        },
        "inward_se": {
          "description": "inward_se parameter (float).",
          "type": "number"
        },
        "outward_effect": {
          "description": "outward_effect parameter (float).",
          "type": "number"
        },
        "outward_se": {
          "description": "outward_se parameter (float).",
          "type": "number"
        },
        "ratio_in_out": {
          "description": "ratio_in_out parameter (float).",
          "type": "number"
        }
      },
      "required": [
        "inward_effect",
        "outward_effect",
        "inward_se",
        "outward_se",
        "ratio_in_out"
      ],
      "type": "object"
    }
  },
  {
    "description": "Output of :func:`network_hte`.",
    "name": "NetworkHTEResult",
    "parameters": {
      "properties": {
        "ci_alpha": {
          "description": "ci_alpha parameter (float).",
          "type": "number"
        },
        "covariates": {
          "description": "Covariate matrix, DataFrame, or column names.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "direct_effect": {
          "description": "direct_effect parameter (float).",
          "type": "number"
        },
        "direct_se": {
          "description": "direct_se parameter (float).",
          "type": "number"
        },
        "individual_direct": {
          "description": "individual_direct parameter (np.ndarray).",
          "type": "string"
        },
        "individual_spillover": {
          "description": "individual_spillover parameter (np.ndarray).",
          "type": "string"
        },
        "spillover_effect": {
          "description": "spillover_effect parameter (float).",
          "type": "number"
        },
        "spillover_se": {
          "description": "spillover_se parameter (float).",
          "type": "number"
        }
      },
      "required": [
        "direct_effect",
        "direct_se",
        "spillover_effect",
        "spillover_se",
        "individual_direct",
        "individual_spillover",
        "covariates",
        "ci_alpha"
      ],
      "type": "object"
    }
  },
  {
    "description": "Structured output of :func:`shift_share_political`.",
    "name": "ShiftSharePoliticalResult",
    "parameters": {
      "properties": {
        "diagnostics": {
          "default": null,
          "description": "diagnostics parameter (Dict[str, Any]).",
          "type": "object"
        },
        "iv_result": {
          "description": "iv_result parameter (CausalResult).",
          "type": "string"
        },
        "method": {
          "default": "shift_share_political",
          "description": "Estimator or algorithm variant to use.",
          "type": "string"
        },
        "n_industries": {
          "description": "Number of industries.",
          "type": "integer"
        },
        "n_periods": {
          "description": "Number of periods.",
          "type": "integer"
        },
        "n_units": {
          "description": "Number of units.",
          "type": "integer"
        },
        "rotemberg_top": {
          "description": "rotemberg_top parameter (pd.DataFrame).",
          "type": "string"
        },
        "share_balance": {
          "description": "share_balance parameter (pd.DataFrame).",
          "type": "string"
        }
      },
      "required": [
        "iv_result",
        "rotemberg_top",
        "share_balance",
        "n_units",
        "n_periods",
        "n_industries"
      ],
      "type": "object"
    }
  },
  {
    "description": "Structured output of :func:`shift_share_political_panel`.",
    "name": "ShiftSharePoliticalPanelResult",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "ci": {
          "description": "ci parameter (tuple).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "diagnostics": {
          "default": null,
          "description": "diagnostics parameter (Dict[str, Any]).",
          "type": "object"
        },
        "estimate": {
          "description": "estimate parameter (float).",
          "type": "number"
        },
        "method": {
          "default": "shift_share_political_panel",
          "description": "Estimator or algorithm variant to use.",
          "type": "string"
        },
        "model_info": {
          "default": null,
          "description": "model_info parameter (Dict[str, Any]).",
          "type": "object"
        },
        "n_industries": {
          "description": "Number of industries.",
          "type": "integer"
        },
        "n_periods": {
          "description": "Number of periods.",
          "type": "integer"
        },
        "n_units": {
          "description": "Number of units.",
          "type": "integer"
        },
        "per_period": {
          "description": "per_period parameter (pd.DataFrame).",
          "type": "string"
        },
        "rotemberg_panel": {
          "description": "rotemberg_panel parameter (pd.DataFrame).",
          "type": "string"
        },
        "se": {
          "description": "se parameter (float).",
          "type": "number"
        },
        "share_balance": {
          "description": "share_balance parameter (pd.DataFrame).",
          "type": "string"
        }
      },
      "required": [
        "estimate",
        "se",
        "ci",
        "per_period",
        "rotemberg_panel",
        "share_balance",
        "n_units",
        "n_periods",
        "n_industries"
      ],
      "type": "object"
    }
  },
  {
    "description": "Adversarial dose-response estimator (Bica et al. 2020).",
    "name": "scigan",
    "parameters": {
      "properties": {
        "covariates": {
          "description": "Covariate matrix, DataFrame, or column names.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "propensity_weights": {
          "description": "propensity_weights parameter (Optional[np.ndarray]).",
          "type": "string"
        },
        "t_grid": {
          "description": "Grid of t values to evaluate.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "treatment": {
          "description": "Treatment indicator, treatment variable, or treatment array.",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "treatment",
        "covariates"
      ],
      "type": "object"
    }
  },
  {
    "description": "Varying-coefficient dose-response estimator.",
    "name": "vcnet",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "covariates": {
          "description": "Covariate matrix, DataFrame, or column names.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "n_basis": {
          "default": 6,
          "description": "Number of B-spline basis functions for the t-axis.",
          "type": "integer"
        },
        "n_bootstrap": {
          "default": 200,
          "description": "Number of bootstrap replications.",
          "type": "integer"
        },
        "random_state": {
          "default": 42,
          "description": "Random seed or RandomState for reproducible stochastic steps.",
          "type": "integer"
        },
        "ridge": {
          "default": 0.01,
          "description": "Tikhonov regularisation on the coefficient matrix.",
          "type": "number"
        },
        "spline_degree": {
          "default": 3,
          "description": "spline_degree parameter (int).",
          "type": "integer"
        },
        "t_grid": {
          "description": "Treatment values at which to evaluate the dose-response curve. Defaults to 40 equally-spaced points between the observed min/max.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "treatment": {
          "description": "Continuous treatment / dose column.",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "treatment",
        "covariates"
      ],
      "type": "object"
    }
  },
  {
    "description": "Dose-response curve returned by :func:`vcnet` / :func:`scigan`.",
    "name": "VCNetResult",
    "parameters": {
      "properties": {
        "ci_hi": {
          "description": "ci_hi parameter (np.ndarray).",
          "type": "string"
        },
        "ci_lo": {
          "description": "ci_lo parameter (np.ndarray).",
          "type": "string"
        },
        "coef_matrix": {
          "description": "coef_matrix parameter (np.ndarray).",
          "type": "string"
        },
        "detail": {
          "default": null,
          "description": "Amount of result detail to return.",
          "type": "object"
        },
        "mu_hat": {
          "description": "mu_hat parameter (np.ndarray).",
          "type": "string"
        },
        "n_basis": {
          "description": "Number of basis.",
          "type": "integer"
        },
        "n_obs": {
          "description": "Number of obs.",
          "type": "integer"
        },
        "se": {
          "description": "se parameter (np.ndarray).",
          "type": "string"
        },
        "t_grid": {
          "description": "Grid of t values to evaluate.",
          "type": "string"
        }
      },
      "required": [
        "t_grid",
        "mu_hat",
        "se",
        "ci_lo",
        "ci_hi",
        "coef_matrix",
        "n_obs",
        "n_basis"
      ],
      "type": "object"
    }
  },
  {
    "description": "Merge an LLM-proposed DAG with a data-driven CI-test skeleton.",
    "name": "llm_dag",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "ci_test": {
          "default": "fisherz",
          "description": "ci_test parameter (str).",
          "type": "string"
        },
        "data": {
          "description": "Observational data used for CI-test skeleton construction. Required for merge_strategy in {\"intersection\", \"union_with_ci\"}.",
          "type": "string"
        },
        "descriptions": {
          "description": "Human-readable description per variable (passed to the oracle).",
          "type": "object"
        },
        "merge_strategy": {
          "default": "intersection",
          "description": "How to combine oracle and CI-test results.",
          "enum": [
            "oracle_only",
            "intersection",
            "union_with_ci"
          ],
          "type": "string"
        },
        "oracle": {
          "description": "A callable ``f(variables, descriptions) -> list[(from, to)]``. If ``None``, this function runs a **data-only** pipeline.",
          "type": "string"
        },
        "variables": {
          "description": "Variable names.",
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "required": [
        "variables"
      ],
      "type": "object"
    }
  },
  {
    "description": "Merged LLM-oracle / CI-test DAG returned by :func:`llm_dag`.",
    "name": "LLMDAGResult",
    "parameters": {
      "properties": {
        "ci_asserts": {
          "description": "ci_asserts parameter (List[Edge]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "ci_rejects": {
          "description": "ci_rejects parameter (List[Edge]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "disagreements": {
          "description": "disagreements parameter (List[Tuple[str, str, str]]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "edges": {
          "description": "edges parameter (List[Edge]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "oracle_edges": {
          "description": "oracle_edges parameter (List[Edge]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "provenance": {
          "default": null,
          "description": "provenance parameter (Dict[str, Any]).",
          "type": "object"
        }
      },
      "required": [
        "edges",
        "oracle_edges",
        "ci_rejects",
        "ci_asserts",
        "disagreements"
      ],
      "type": "object"
    }
  },
  {
    "description": "Output of :func:`llm_causal_assess`.",
    "name": "LLMCausalAssessResult",
    "parameters": {
      "properties": {
        "level1_accuracy": {
          "description": "level1_accuracy parameter (Optional[float]).",
          "type": "number"
        },
        "level2_accuracy": {
          "description": "level2_accuracy parameter (Optional[float]).",
          "type": "number"
        },
        "llm_identifier": {
          "description": "llm_identifier parameter (str).",
          "type": "string"
        },
        "per_item": {
          "description": "per_item parameter (pd.DataFrame).",
          "type": "string"
        }
      },
      "required": [
        "level1_accuracy",
        "level2_accuracy",
        "per_item",
        "llm_identifier"
      ],
      "type": "object"
    }
  },
  {
    "description": "Holds state across the diagnose -> estimate -> report pipeline.",
    "name": "CausalWorkflow",
    "parameters": {
      "properties": {
        "allow_experimental": {
          "default": false,
          "description": "allow_experimental parameter (bool).",
          "type": "boolean"
        },
        "cate_summary_table": {
          "description": "cate_summary_table parameter (Optional[pd.DataFrame]).",
          "type": "string"
        },
        "cluster": {
          "description": "Cluster identifier column for clustered standard errors.",
          "type": "string"
        },
        "cohort": {
          "description": "Treatment cohort or group identifier.",
          "type": "string"
        },
        "covariates": {
          "description": "Covariate matrix, DataFrame, or column names.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "cutoff": {
          "description": "cutoff parameter (Optional[float]).",
          "type": "number"
        },
        "dag": {
          "description": "dag parameter (Optional[Any]).",
          "type": "string"
        },
        "data": {
          "description": "pandas DataFrame containing the variables used by the estimator.",
          "type": "string"
        },
        "degradations": {
          "default": null,
          "description": "degradations parameter (List[Dict[str, Any]]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "design": {
          "description": "design parameter (Optional[str]).",
          "type": "string"
        },
        "diagnostics": {
          "description": "diagnostics parameter (Optional[Any]).",
          "type": "string"
        },
        "estimator_comparison": {
          "description": "estimator_comparison parameter (Optional[pd.DataFrame]).",
          "type": "string"
        },
        "id": {
          "description": "Unit, subject, or panel identifier column.",
          "type": "string"
        },
        "instrument": {
          "description": "instrument parameter (Optional[str]).",
          "type": "string"
        },
        "mediator": {
          "description": "mediator parameter (Optional[str]).",
          "type": "string"
        },
        "pipeline_notes": {
          "default": null,
          "description": "pipeline_notes parameter (List[str]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "post_treat_strata": {
          "description": "post_treat_strata parameter (Optional[str]).",
          "type": "string"
        },
        "proxy_w": {
          "description": "proxy_w parameter (Optional[List[str]]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "proxy_z": {
          "description": "proxy_z parameter (Optional[List[str]]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "recommendation": {
          "description": "recommendation parameter (Optional[Any]).",
          "type": "string"
        },
        "result": {
          "description": "result parameter (Optional[Any]).",
          "type": "string"
        },
        "robustness_findings": {
          "default": null,
          "description": "robustness_findings parameter (Dict[str, Any]).",
          "type": "object"
        },
        "running_var": {
          "description": "running_var parameter (Optional[str]).",
          "type": "string"
        },
        "sensitivity_panel_result": {
          "description": "sensitivity_panel_result parameter (Optional[pd.DataFrame]).",
          "type": "string"
        },
        "stages_completed": {
          "default": null,
          "description": "stages_completed parameter (List[str]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "strict": {
          "description": "strict parameter (bool).",
          "type": "boolean"
        },
        "time": {
          "description": "Time period column.",
          "type": "string"
        },
        "treatment": {
          "description": "Treatment indicator, treatment variable, or treatment array.",
          "type": "string"
        },
        "tv_confounders": {
          "description": "tv_confounders parameter (Optional[List[str]]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "data",
        "y",
        "treatment",
        "covariates",
        "id",
        "time",
        "running_var",
        "instrument",
        "cutoff",
        "cohort",
        "cluster",
        "design",
        "dag",
        "strict"
      ],
      "type": "object"
    }
  },
  {
    "description": "Structured output of :func:`synth_experimental_design`.",
    "name": "SynthExperimentalDesignResult",
    "parameters": {
      "properties": {
        "baseline_variance": {
          "description": "baseline_variance parameter (float).",
          "type": "number"
        },
        "diagnostics": {
          "default": null,
          "description": "diagnostics parameter (Dict[str, Any]).",
          "type": "object"
        },
        "donor_units": {
          "description": "donor_units parameter (List[Any]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "expected_variance": {
          "description": "expected_variance parameter (float).",
          "type": "number"
        },
        "method": {
          "default": "loo_sc_fit_ranking",
          "description": "Estimator or algorithm variant to use.",
          "type": "string"
        },
        "ranking": {
          "description": "ranking parameter (pd.DataFrame).",
          "type": "string"
        },
        "selected": {
          "description": "selected parameter (List[Any]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "weights": {
          "description": "Observation weights.",
          "type": "object"
        }
      },
      "required": [
        "selected",
        "ranking",
        "weights",
        "donor_units",
        "expected_variance",
        "baseline_variance"
      ],
      "type": "object"
    }
  },
  {
    "description": "Output of :func:`pairwise_causal_benchmark`.",
    "name": "PairwiseBenchmarkResult",
    "parameters": {
      "properties": {
        "accuracy": {
          "description": "accuracy parameter (float).",
          "type": "number"
        },
        "per_pair": {
          "description": "per_pair parameter (pd.DataFrame).",
          "type": "string"
        },
        "precision_forward": {
          "description": "precision_forward parameter (float).",
          "type": "number"
        },
        "recall_forward": {
          "description": "recall_forward parameter (float).",
          "type": "number"
        }
      },
      "required": [
        "accuracy",
        "precision_forward",
        "recall_forward",
        "per_pair"
      ],
      "type": "object"
    }
  },
  {
    "description": "Draft causal-analysis report assembled by :func:`sp.paper`.",
    "name": "PaperDraft",
    "parameters": {
      "properties": {
        "citations": {
          "default": null,
          "description": "citations parameter (List[str]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "dag": {
          "description": "dag parameter.",
          "type": "string"
        },
        "dag_outcome": {
          "description": "dag_outcome parameter (Optional[str]).",
          "type": "string"
        },
        "dag_treatment": {
          "description": "dag_treatment parameter (Optional[str]).",
          "type": "string"
        },
        "degradations": {
          "default": null,
          "description": "degradations parameter (List[Dict[str, Any]]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "fmt": {
          "description": "fmt parameter (str).",
          "type": "string"
        },
        "parsed_hints": {
          "default": null,
          "description": "parsed_hints parameter (Dict[str, Any]).",
          "type": "object"
        },
        "question": {
          "description": "question parameter (str).",
          "type": "string"
        },
        "sections": {
          "description": "sections parameter (Dict[str, str]).",
          "type": "object"
        },
        "workflow": {
          "description": "workflow parameter.",
          "type": "string"
        }
      },
      "required": [
        "question",
        "sections",
        "workflow",
        "fmt"
      ],
      "type": "object"
    }
  },
  {
    "description": "Compute Rosenbaum bounds on a paired observational study. Validation: certified parity evidence.",
    "name": "rosenbaum_bounds",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level used to report ``gamma_critical``.",
          "type": "number"
        },
        "alternative": {
          "default": "greater",
          "description": "Direction of the alternative hypothesis for the treatment effect.",
          "enum": [
            "greater",
            "less",
            "two-sided"
          ],
          "type": "string"
        },
        "control": {
          "description": "Outcome in the treated / control unit of each matched pair (same length). Ignored if ``data`` is provided.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "data": {
          "description": "Long-format data. Must contain exactly two rows per ``pair_id``, one with ``treat=1`` and one with ``treat=0``.",
          "type": "string"
        },
        "gamma_grid": {
          "description": "Gamma values (>= 1) over which to compute bounding p-values.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "method": {
          "default": "wilcoxon",
          "description": "Wilcoxon signed-rank bound (continuous) or binomial sign test (robust / binary).",
          "enum": [
            "wilcoxon",
            "sign"
          ],
          "type": "string"
        },
        "pair_id": {
          "description": "pair_id parameter (Optional[str]).",
          "type": "string"
        },
        "treat": {
          "description": "Treatment indicator or first-treatment-period column.",
          "type": "string"
        },
        "treated": {
          "description": "Outcome in the treated / control unit of each matched pair (same length). Ignored if ``data`` is provided.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        },
        "zero_method": {
          "default": "pratt",
          "description": "Treatment of pairs with a zero difference in the Wilcoxon bound. ``\"pratt\"`` ranks them with the other pairs and gives them no weight (Rosenbaum's ``DOS2::senWilcox``, Stata ``rbounds``); ``\"wilcox\"`` drops them before ranking (``rbounds::psens``). Identical when no difference is zero. Ignored by the sign test, which always drops zeros.",
          "enum": [
            "pratt",
            "wilcox"
          ],
          "type": "string"
        }
      },
      "required": [],
      "type": "object"
    }
  },
  {
    "description": "Compute Rosenbaum bounds on a paired observational study. Validation: certified parity evidence.",
    "name": "rosenbaum_gamma",
    "parameters": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level used to report ``gamma_critical``.",
          "type": "number"
        },
        "alternative": {
          "default": "greater",
          "description": "Direction of the alternative hypothesis for the treatment effect.",
          "enum": [
            "greater",
            "less",
            "two-sided"
          ],
          "type": "string"
        },
        "control": {
          "description": "Outcome in the treated / control unit of each matched pair (same length). Ignored if ``data`` is provided.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "data": {
          "description": "Long-format data. Must contain exactly two rows per ``pair_id``, one with ``treat=1`` and one with ``treat=0``.",
          "type": "string"
        },
        "gamma_grid": {
          "description": "Gamma values (>= 1) over which to compute bounding p-values.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "method": {
          "default": "wilcoxon",
          "description": "Wilcoxon signed-rank bound (continuous) or binomial sign test (robust / binary).",
          "enum": [
            "wilcoxon",
            "sign"
          ],
          "type": "string"
        },
        "pair_id": {
          "description": "pair_id parameter (Optional[str]).",
          "type": "string"
        },
        "treat": {
          "description": "Treatment indicator or first-treatment-period column.",
          "type": "string"
        },
        "treated": {
          "description": "Outcome in the treated / control unit of each matched pair (same length). Ignored if ``data`` is provided.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        },
        "zero_method": {
          "default": "pratt",
          "description": "Treatment of pairs with a zero difference in the Wilcoxon bound. ``\"pratt\"`` ranks them with the other pairs and gives them no weight (Rosenbaum's ``DOS2::senWilcox``, Stata ``rbounds``); ``\"wilcox\"`` drops them before ranking (``rbounds::psens``). Identical when no difference is zero. Ignored by the sign test, which always drops zeros.",
          "enum": [
            "pratt",
            "wilcox"
          ],
          "type": "string"
        }
      },
      "required": [],
      "type": "object"
    }
  },
  {
    "description": "Result container for :func:`rosenbaum_bounds`.",
    "name": "RosenbaumResult",
    "parameters": {
      "properties": {
        "alpha": {
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "alternative": {
          "description": "alternative parameter (str).",
          "type": "string"
        },
        "detail": {
          "default": null,
          "description": "Amount of result detail to return.",
          "type": "string"
        },
        "gamma_critical": {
          "description": "gamma_critical parameter (float).",
          "type": "number"
        },
        "gamma_grid": {
          "description": "Grid of gamma values to evaluate.",
          "type": "string"
        },
        "method": {
          "description": "Estimator or algorithm variant to use.",
          "type": "string"
        },
        "n_pairs": {
          "description": "Number of pairs.",
          "type": "integer"
        },
        "pvalue_lower": {
          "description": "pvalue_lower parameter (np.ndarray).",
          "type": "string"
        },
        "pvalue_upper": {
          "description": "pvalue_upper parameter (np.ndarray).",
          "type": "string"
        },
        "statistic": {
          "description": "statistic parameter (float).",
          "type": "number"
        }
      },
      "required": [
        "gamma_grid",
        "pvalue_lower",
        "pvalue_upper",
        "gamma_critical",
        "method",
        "alpha",
        "n_pairs",
        "statistic",
        "alternative"
      ],
      "type": "object"
    }
  }
]
