[
  {
    "description": "OLS with absorbed high-dimensional fixed effects (reghdfe-style). Validation: certified parity evidence.",
    "input_schema": {
      "properties": {
        "X": {
          "description": "Regressors *excluding* the absorbed FEs and the constant (the constant is absorbed by any FE dimension).",
          "type": "string"
        },
        "cluster": {
          "description": "One-way or multi-way cluster variables for robust SEs. If provided, returns cluster-robust SEs (one-way: Liang-Zeger sandwich; multi-way: inclusion-exclusion Cameron-Gelbach-Miller).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "cluster_df": {
          "default": "per_term",
          "description": "Small-sample factor of the multi-way (inclusion-exclusion) cluster variance. ``\"per_term\"`` scales each term by its own ``G_S/(G_S - 1)`` (R ``sandwich::vcovCL``, :func:`multiway_cluster_vcov`); ``\"min\"`` scales every term by ``G_min/(G_min - 1)`` with ``G_min`` the smallest one-way cluster count -- Stata ``reghdfe``'s convention (and ``fixest``'s ``cluster.df = \"min\"``). Identical for one-way clustering.",
          "enum": [
            "per_term",
            "min"
          ],
          "type": "string"
        },
        "drop_singletons": {
          "default": true,
          "description": "drop_singletons parameter (bool).",
          "type": "boolean"
        },
        "fe": {
          "description": "Fixed-effect columns.",
          "type": "string"
        },
        "maxiter": {
          "default": 10000,
          "description": "Demean convergence controls.",
          "type": "integer"
        },
        "return_absorber": {
          "default": false,
          "description": "If True, also return the ``Absorber`` object for reuse.",
          "type": "boolean"
        },
        "slopes": {
          "description": "slopes parameter (Optional[Sequence[SlopeSpec]]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "solver": {
          "default": "map",
          "description": "Within-transformation backend. See :class:`Absorber`.",
          "enum": [
            "map",
            "lsmr",
            "lsqr"
          ],
          "type": "string"
        },
        "tol": {
          "default": 1e-08,
          "description": "Demean convergence controls.",
          "type": "number"
        },
        "weights": {
          "description": "Observation weights.",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "X",
        "fe",
        "y"
      ],
      "type": "object"
    },
    "name": "absorb_ols"
  },
  {
    "description": "Adjust p-values for multiple comparisons. Validation: certified parity evidence.",
    "input_schema": {
      "properties": {
        "method": {
          "default": "holm",
          "description": "Adjustment method. One of: - ``'bonferroni'`` -- Bonferroni correction. - ``'holm'`` -- Holm (1979) step-down. - ``'bh'`` or ``'fdr'`` -- Benjamini-Hochberg FDR. For Romano-Wolf or Westfall-Young adjustments (which require the original data and bootstrap), use :func:`romano_wolf` directly.",
          "type": "string"
        },
        "pvalues": {
          "description": "Unadjusted p-values.",
          "type": "string"
        }
      },
      "required": [
        "pvalues"
      ],
      "type": "object"
    },
    "name": "adjust_pvalues"
  },
  {
    "description": "Fit an Accelerated Failure Time model by MLE. Validation: certified parity evidence.",
    "input_schema": {
      "properties": {
        "family": {
          "default": "weibull",
          "description": "family parameter (AFTFamily).",
          "enum": [
            "exponential",
            "weibull",
            "lognormal",
            "loglogistic"
          ],
          "type": "string"
        },
        "formula": {
          "description": "``\"duration + event ~ x1 + x2\"``.",
          "type": "string"
        }
      },
      "required": [
        "formula"
      ],
      "type": "object"
    },
    "name": "aft"
  },
  {
    "description": "Aggregate Callaway-Sant'Anna group-time ATTs into interpretable summaries -- overall ATT, event-study by relative time, group-specific ATT(g), or calendar-time ATT(t). Inference uses the multiplier bootstrap on the pre-stored influence functions, so SEs are correct under clustering at the unit level. Validation: validated evidence tier (known-truth, reference, external-parity, or Monte Carlo artifact). Assumptions: Same identifying assumptions as the source estimator (parallel trends, no anticipation, SUTVA); For dynamic aggregation: balanced panel within the requested event-time window (use balance_e). Pre-conditions: result was produced by sp.callaway_santanna or sp.did with staggered=True; result.detail contains the per-(g, t) ATT estimates and their influence functions. Failure modes: result.detail is empty or missing influence functions -> Re-run sp.callaway_santanna; aggte requires the per-(g,t) influence functions; Empty event-time aggregation (no overlapping cohorts) -> Widen the (min_e, max_e) window or drop balance_e. Alternatives: sp.callaway_santanna, sp.sun_abraham, sp.did_imputation. Typical minimum N: 50.",
    "input_schema": {
      "properties": {
        "agg_weights": {
          "default": "did",
          "description": "Aggregation-weight convention: R did's cohort shares, or Stata csdid's per-cell treated-observation counts (repeated cross-sections only; simple and group)",
          "enum": [
            "did",
            "csdid"
          ],
          "type": "string"
        },
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "balance_e": {
          "description": "For dynamic: cap event time at +/-balance_e for balanced panel",
          "type": "integer"
        },
        "boot_type": {
          "default": "multiplier",
          "description": "Bootstrap variant",
          "enum": [
            "multiplier"
          ],
          "type": "string"
        },
        "bstrap": {
          "default": true,
          "description": "bstrap parameter (bool).",
          "type": "boolean"
        },
        "cband": {
          "default": true,
          "description": "Uniform confidence band",
          "type": "boolean"
        },
        "max_e": {
          "default": Infinity,
          "description": "max_e parameter (float).",
          "type": "number"
        },
        "min_e": {
          "default": -Infinity,
          "description": "min_e parameter (float).",
          "type": "number"
        },
        "n_boot": {
          "default": 1000,
          "description": "Number of bootstrap replications.",
          "type": "integer"
        },
        "na_rm": {
          "default": true,
          "description": "Drop ATT(g,t) cells with missing / infinite SE before aggregating",
          "type": "boolean"
        },
        "random_state": {
          "description": "Random seed or RandomState for reproducible stochastic steps.",
          "type": "integer"
        },
        "result": {
          "description": "Output of sp.callaway_santanna or sp.did with staggered=True",
          "type": "string"
        },
        "share_variance": {
          "default": true,
          "description": "Carry the estimated-cohort-share term (R did:::wif) into the aggregated variance; False holds the shares fixed as Stata csdid's estat group does",
          "type": "boolean"
        },
        "type": {
          "default": "simple",
          "description": "Aggregation type",
          "enum": [
            "simple",
            "dynamic",
            "group",
            "calendar"
          ],
          "type": "string"
        }
      },
      "required": [
        "result"
      ],
      "type": "object"
    },
    "name": "aggte"
  },
  {
    "description": "Aggregate Callaway-Sant'Anna group-time ATTs directly from an influence-function export (DataFrame or file path from sp.influence_functions) -- event-study, group, calendar, or overall summaries with multiplier-bootstrap inference, no refit and no original data required. The post-hoc half of the Stata csdid saverif() workflow. Validation: validated evidence tier (known-truth, reference, external-parity, or Monte Carlo artifact). Pre-conditions: source was produced by sp.influence_functions. Failure modes: influence frame is missing required columns -> Re-export with sp.influence_functions(result, path). Alternatives: sp.aggte, sp.influence_functions. Typical minimum N: 50.",
    "input_schema": {
      "properties": {
        "source": {
          "description": "Frame from sp.influence_functions, or path to one (.parquet or CSV)",
          "type": "string"
        },
        "type": {
          "default": "simple",
          "description": "Aggregation type",
          "enum": [
            "simple",
            "dynamic",
            "group",
            "calendar"
          ],
          "type": "string"
        }
      },
      "required": [
        "source"
      ],
      "type": "object"
    },
    "name": "aggte_from_influence"
  },
  {
    "description": "Augmented inverse-probability weighting (AIPW) -- the canonical doubly-robust ATE estimator. Cross-fits an outcome regression and a propensity model and combines them via the efficient-influence-function formula, so the estimate is consistent if either nuisance is correctly specified (Robins, Rotnitzky & Zhao 1994). Validation: certified parity evidence. Assumptions: Unconfoundedness conditional on covariates (Y(0), Y(1) perp D | X); Overlap / common support: 0 < e(X) < 1 for all X with positive density; SUTVA. Pre-conditions: binary treatment column with both arms present; covariates must contain all confounders for unconfoundedness; no perfect overlap violations (0 < propensity < 1 in support). Failure modes: Propensity scores cluster near 0 or 1 -> Trim to overlap region with sp.trimming() or switch to overlap-weighted ATE; Cross-fit estimate has very wide CI -> Increase n_folds or reduce covariate dimension; check for near-empty propensity strata. Alternatives: sp.ipw, sp.dml, sp.tmle, sp.matching. Typical minimum N: 200.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "covariates": {
          "description": "Confounders to adjust for",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "cross_fit": {
          "default": true,
          "description": "Cross-fit the nuisance models over ``n_folds`` random folds (Chernozhukov et al. 2018).",
          "type": "boolean"
        },
        "estimand": {
          "default": "ATE",
          "description": "Target estimand",
          "enum": [
            "ATE",
            "ATT",
            "ATC"
          ],
          "type": "string"
        },
        "n_folds": {
          "default": 5,
          "description": "Cross-fitting folds (>= 2)",
          "type": "integer"
        },
        "se_method": {
          "default": "influence",
          "description": "``'influence'`` reports ``sd(psi) / sqrt(n)`` from the estimated efficient influence function, treating the nuisance fits as known (the cross-fitting / DML convention; R ``AIPW`` reports the same quantity). ``'sandwich'`` stacks the logit score, the two OLS normal equations and the AIPW moment and reports the M-estimation sandwich (divisor ``n``), which accounts for the estimated nuisance parameters at finite ``n``.",
          "enum": [
            "influence",
            "sandwich"
          ],
          "type": "string"
        },
        "seed": {
          "description": "Random seed for reproducible stochastic steps.",
          "type": "integer"
        },
        "treat": {
          "description": "Binary treatment (0/1)",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable",
          "type": "string"
        }
      },
      "required": [
        "covariates",
        "treat",
        "y"
      ],
      "type": "object"
    },
    "name": "aipw"
  },
  {
    "description": "Anderson-Rubin confidence set -- re-export of Validation: certified parity evidence.",
    "input_schema": {
      "properties": {
        "add_const": {
          "default": true,
          "description": "add_const parameter (bool).",
          "type": "boolean"
        },
        "beta_grid": {
          "description": "Grid of beta values to evaluate.",
          "type": "string"
        },
        "endog": {
          "description": "endog parameter (Union[np.ndarray, pd.Series, str]).",
          "type": "string"
        },
        "exog": {
          "description": "exog parameter (Optional[Union[np.ndarray, pd.DataFrame, List[str]]]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "instruments": {
          "description": "instruments parameter (Union[np.ndarray, pd.DataFrame, List[str]]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "level": {
          "default": 0.95,
          "description": "Confidence level or reporting level.",
          "type": "number"
        },
        "n_grid": {
          "default": 401,
          "description": "Number of grid.",
          "type": "integer"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "endog",
        "instruments",
        "y"
      ],
      "type": "object"
    },
    "name": "anderson_rubin_ci"
  },
  {
    "description": "Anderson-Rubin (1949) test -- size-correct under weak instruments. Validation: certified parity evidence.",
    "input_schema": {
      "properties": {
        "absorb": {
          "description": "High-dimensional fixed effects to partial out of ``y``, the endogenous regressor, the instruments and the controls before the test -- the same residualisation ``sp.iv(absorb=...)`` performs.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "alpha": {
          "default": 0.05,
          "description": "Significance level.",
          "type": "number"
        },
        "cluster": {
          "description": "Cluster the AR statistic. Switches from the homoskedastic F-form to the (multiway) cluster-robust quadratic form ``(Z'e)' Omega^-1 (Z'e)`` with ``Omega`` the cluster-sum variance of the moment vector, referred to ``F(k_z, G-1)``. Without this, a panel AR test over-rejects exactly as a homoskedastic t-test",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "endog": {
          "description": "Endogenous regressor (single).",
          "type": "string"
        },
        "exog": {
          "description": "Included exogenous controls.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "h0": {
          "default": 0,
          "description": "Null hypothesis value for the endogenous coefficient.",
          "type": "number"
        },
        "instruments": {
          "description": "Excluded instruments.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "vcov": {
          "default": "HC1",
          "description": "Variance estimator used for the Olea-Pflueger effective F reported alongside AR.",
          "enum": [
            "HC0",
            "HC1",
            "classic"
          ],
          "type": "string"
        },
        "y": {
          "description": "Outcome variable.",
          "type": "string"
        }
      },
      "required": [
        "endog",
        "instruments",
        "y"
      ],
      "type": "object"
    },
    "name": "anderson_rubin_test"
  },
  {
    "description": "Fit ARIMA(p,d,q) or SARIMAX. Validation: certified parity evidence.",
    "input_schema": {
      "properties": {
        "auto": {
          "default": false,
          "description": "If True, select (p, d, q) by AICc grid search (ignores ``order``).",
          "type": "boolean"
        },
        "exog": {
          "description": "Exogenous regressors (ARIMAX).",
          "type": "string"
        },
        "max_d": {
          "default": 2,
          "description": "Bounds for the auto search.",
          "type": "integer"
        },
        "max_p": {
          "default": 5,
          "description": "Bounds for the auto search.",
          "type": "integer"
        },
        "max_q": {
          "default": 5,
          "description": "Bounds for the auto search.",
          "type": "integer"
        },
        "method": {
          "default": "statespace",
          "description": "Estimation convention. ``'statespace'`` keeps the default exact Kalman/SARIMAX likelihood. ``'css_ml'`` is retained as a compatibility alias for ``'innovations_mle'``. The innovations-MLE path uses statsmodels' stationary/invertible exact-MLE parameterization, matching ``stats::arima(method='ML')`` and tightly converged Stata ``arima`` coefficient conventions for pure ARMA models.",
          "enum": [
            "statespace",
            "css_ml",
            "innovations_mle"
          ],
          "type": "string"
        },
        "order": {
          "default": [
            1,
            0,
            0
          ],
          "description": "order parameter (Tuple[int, int, int]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "seasonal_order": {
          "description": "seasonal_order parameter (Optional[Tuple[int, int, int, int]]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "y"
      ],
      "type": "object"
    },
    "name": "arima"
  },
  {
    "description": "Comprehensive assumption audit for any estimated model.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for tests.",
          "type": "number"
        },
        "result": {
          "description": "Estimated model result.",
          "type": "string"
        },
        "verbose": {
          "default": true,
          "description": "Print summary automatically.",
          "type": "boolean"
        }
      },
      "required": [
        "result"
      ],
      "type": "object"
    },
    "name": "assumption_audit"
  },
  {
    "description": "Reviewer-grade audit on a result. Returns the literature checklist (parallel-trends test, honest-DID, Bacon decomposition, placebo, balance, ...) with status per item and the concrete suggest_function to call to fill any missing high-importance check.",
    "input_schema": {
      "properties": {
        "result_id": {
          "description": "Result handle. Required unless you also pass a fitted result via the result kwarg (programmatic use).",
          "type": "string"
        }
      },
      "required": [],
      "type": "object"
    },
    "name": "audit"
  },
  {
    "description": "Reviewer-grade audit on a previously-fitted result. Pass the result_id returned by an earlier tool call (with as_handle=true). Returns the same checklist sp.audit() produces -- every robustness check the literature expects for the design, each with status 'passed|failed|missing|not_applicable' and a concrete suggest_function for the missing ones. A not_applicable check (e.g. an over-identification test on a just-identified IV fit) carries a 'reason' and is excluded from the summary's n_total.",
    "input_schema": {
      "properties": {
        "result_id": {
          "description": "Handle returned by an earlier estimator call. Must be in the server result cache (LRU-evicted; refit if missing).",
          "type": "string"
        }
      },
      "required": [
        "result_id"
      ],
      "type": "object"
    },
    "name": "audit_result"
  },
  {
    "description": "Augmented Synthetic Control with ridge bias correction (Ben-Michael et al. 2021). Validation: certified parity evidence. Do NOT use when: pre-treatment fit from plain sp.synth is already good -- the ridge augmentation mainly buys bias correction for poor fit and adds a tuning parameter to justify; many treated units -- use sp.gsynth or sp.sdid. Assumptions: A convex (or regularized) combination of donor units reproduces the treated unit's pre-treatment outcome path; No interference: the treatment does not affect the donor units (SUTVA); No anticipation before the treatment date. Pre-conditions: Panel of one or more treated units plus an untreated donor pool, observed over time; Pre-treatment window long enough to fit donor weights (rule of thumb: more pre-periods than donors used); Outcome observed for every unit in every period. Failure modes: Large pre-treatment RMSPE -- the synthetic unit fails to track the treated unit before treatment -> Add donors / predictors, lengthen the pre-period, or use a bias-corrected estimator (sdid, augsynth); Placebo / permutation inference shows the estimate is not extreme relative to donors -> Report the placebo distribution honestly; the effect...",
    "input_schema": {
      "properties": {
        "backend": {
          "default": "native",
          "description": "Computation backend: native or augsynth/R bridge backend",
          "type": "string"
        },
        "outcome": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        },
        "time": {
          "description": "Time period column.",
          "type": "string"
        },
        "treated_unit": {
          "description": "treated_unit parameter (str).",
          "type": "string"
        },
        "treatment_time": {
          "description": "treatment_time parameter (int).",
          "type": "integer"
        },
        "unit": {
          "description": "Unit identifier column.",
          "type": "string"
        }
      },
      "required": [
        "outcome",
        "time",
        "treated_unit",
        "treatment_time",
        "unit"
      ],
      "type": "object"
    },
    "name": "augsynth"
  },
  {
    "description": "Race several meta-learners and return a scored leaderboard + winner. Validation: validated evidence tier (known-truth, reference, external-parity, or Monte Carlo artifact). Assumptions: Unconfoundedness given the covariates; Overlap / positivity across the covariate space; Nuisance functions are estimated consistently; cross-fitting controls overfitting bias. Pre-conditions: Covariates, a treatment indicator, and an outcome for each unit; Enough data to fit flexible nuisance models with sample-splitting / cross-fitting. Failure modes: CATE estimates are unstable or extrapolate beyond the covariate support -> Restrict to the overlap region, increase data, or use a doubly-robust learner (DR-/R-learner). Alternatives: sp.dml, sp.causal_forest, sp.tmle. Typical minimum N: 500.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for both learner confidence intervals and the BLP-beta1 acceptance region used by the selection rule.",
          "type": "number"
        },
        "cate_model": {
          "description": "Override the default gradient-boosting models used for nuisance and final CATE fitting.",
          "type": "string"
        },
        "covariates": {
          "description": "Effect-modifier columns used as features for every nuisance and CATE model.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "learners": {
          "default": [
            "s",
            "t",
            "x",
            "r",
            "dr"
          ],
          "description": "Short codes of the meta-learners to race. Duplicates are ignored.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "n_bootstrap": {
          "default": 200,
          "description": "Bootstrap iterations for ATE standard error on non-DR learners (passed through to ``metalearner``).",
          "type": "integer"
        },
        "n_folds": {
          "default": 5,
          "description": "Number of folds used for both the shared nuisance cross-fit and each learner's honest CATE prediction.",
          "type": "integer"
        },
        "outcome_model": {
          "description": "Override the default gradient-boosting models used for nuisance and final CATE fitting.",
          "type": "string"
        },
        "propensity_model": {
          "description": "Override the default gradient-boosting models used for nuisance and final CATE fitting.",
          "type": "string"
        },
        "random_state": {
          "default": 42,
          "description": "Seed for all K-fold splits.",
          "type": "integer"
        },
        "score": {
          "default": "r_loss",
          "description": "Currently only ``'r_loss'`` is implemented. Reserved for future expansion.",
          "type": "string"
        },
        "treat": {
          "description": "Binary treatment column name (values in {0, 1}).",
          "type": "string"
        },
        "y": {
          "description": "Outcome column name.",
          "type": "string"
        }
      },
      "required": [
        "covariates",
        "treat",
        "y"
      ],
      "type": "object"
    },
    "name": "auto_cate"
  },
  {
    "description": "Optuna-tuned CATE learner race -- nuisance, per-learner, or both. Validation: validated evidence tier (known-truth, reference, external-parity, or Monte Carlo artifact). Assumptions: Unconfoundedness given the covariates; Overlap / positivity across the covariate space; Nuisance functions are estimated consistently; cross-fitting controls overfitting bias. Pre-conditions: Covariates, a treatment indicator, and an outcome for each unit; Enough data to fit flexible nuisance models with sample-splitting / cross-fitting. Failure modes: CATE estimates are unstable or extrapolate beyond the covariate support -> Restrict to the overlap region, increase data, or use a doubly-robust learner (DR-/R-learner). Alternatives: sp.dml, sp.causal_forest, sp.tmle. Typical minimum N: 500.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "covariates": {
          "description": "Covariate matrix, DataFrame, or column names.",
          "type": "string"
        },
        "learners": {
          "default": [
            "s",
            "t",
            "x",
            "r",
            "dr"
          ],
          "description": "learners parameter (LearnersArg).",
          "type": "string"
        },
        "n_bootstrap": {
          "default": 200,
          "description": "Number of bootstrap replications.",
          "type": "integer"
        },
        "n_folds": {
          "default": 5,
          "description": "Number of cross-fitting or cross-validation folds.",
          "type": "integer"
        },
        "n_trials": {
          "default": 25,
          "description": "Budget for the nuisance-tuning study (ignored when ``tune == 'per_learner'``).",
          "type": "integer"
        },
        "n_trials_per_learner": {
          "description": "Budget for each per-learner study. Defaults to ``max(5, n_trials // 3)``.",
          "type": "integer"
        },
        "per_learner_search_space": {
          "description": "Override default spaces. See :data:`DEFAULT_SEARCH_SPACE` and :data:`DEFAULT_PER_LEARNER_SEARCH_SPACE`. Passed through / see :func:`auto_cate`.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "random_state": {
          "default": 42,
          "description": "Random seed or RandomState for reproducible stochastic steps.",
          "type": "integer"
        },
        "sampler": {
          "description": "sampler parameter (Optional[Any]).",
          "type": "string"
        },
        "search_space": {
          "description": "Override default spaces. See :data:`DEFAULT_SEARCH_SPACE` and :data:`DEFAULT_PER_LEARNER_SEARCH_SPACE`. Passed through / see :func:`auto_cate`.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "timeout": {
          "description": "Wall-clock limit per study (seconds).",
          "type": "number"
        },
        "treat": {
          "description": "Treatment indicator or first-treatment-period column.",
          "type": "string"
        },
        "tune": {
          "default": "nuisance",
          "description": "Tuning regime: - ``'nuisance'`` -- tune the shared outcome / propensity GBMs against held-out R-loss, then hand them to ``auto_cate``. (v0.9.5 behaviour.) - ``'per_learner'`` -- keep default nuisance models; for each learner, tune its final-stage CATE model against held-out R-loss. - ``'both'`` -- run ``'nuisance'`` first, then ``'per_learner'`` using the tuned nuisance. Most expensive; most thorough.",
          "enum": [
            "nuisance",
            "per_learner",
            "both"
          ],
          "type": "string"
        },
        "verbose": {
          "default": false,
          "description": "verbose parameter (bool).",
          "type": "boolean"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "covariates",
        "treat",
        "y"
      ],
      "type": "object"
    },
    "name": "auto_cate_tuned"
  },
  {
    "description": "Run several staggered-DiD estimators side by side.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for reported CIs.",
          "type": "number"
        },
        "g": {
          "description": "First-treatment-period column (0 or NaN for never-treated).",
          "type": "string"
        },
        "i": {
          "description": "Unit identifier column.",
          "type": "string"
        },
        "methods": {
          "description": "Subset of ``{'cs', 'sa', 'bjs'}``. Defaults to all three.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "select_by": {
          "default": "median",
          "description": "How to pick the winner. ``'median'`` returns the candidate with the median point estimate across successes. A method name returns that specific candidate (useful for forcing a baseline).",
          "enum": [
            "median",
            "first_success",
            "cs",
            "sa",
            "bjs"
          ],
          "type": "string"
        },
        "t": {
          "description": "Calendar time column.",
          "type": "string"
        },
        "x": {
          "description": "Covariates (used by CS / SA / BJS where supported).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Outcome column.",
          "type": "string"
        }
      },
      "required": [
        "g",
        "i",
        "t",
        "y"
      ],
      "type": "object"
    },
    "name": "auto_did"
  },
  {
    "description": "Race 2SLS, LIML, and JIVE on a single-endogenous IV spec.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "cluster": {
          "description": "Forwarded to each estimator where supported.",
          "type": "string"
        },
        "endog": {
          "description": "Single endogenous regressor column.",
          "type": "string"
        },
        "exog": {
          "description": "Exogenous controls (included in all requested IV estimators).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "instruments": {
          "description": "Instrument(s). A scalar is promoted to a one-element list.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "methods": {
          "description": "Subset of ``{'2sls', 'liml', 'jive'}``.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "robust": {
          "default": "nonrobust",
          "description": "Forwarded to each estimator where supported.",
          "type": "string"
        },
        "select_by": {
          "default": "median",
          "description": "select_by parameter (str).",
          "enum": [
            "median",
            "first_success",
            "2sls",
            "liml",
            "jive"
          ],
          "type": "string"
        },
        "y": {
          "description": "Outcome column.",
          "type": "string"
        }
      },
      "required": [
        "endog",
        "instruments",
        "y"
      ],
      "type": "object"
    },
    "name": "auto_iv"
  },
  {
    "description": "Return list of all registered decomposition method names.",
    "input_schema": {
      "properties": {},
      "required": [],
      "type": "object"
    },
    "name": "available_methods"
  },
  {
    "description": "Aggregate CATE predictions into ATE/ATT/ATC/ATO targets. Validation: certified parity evidence. Assumptions: Unconfoundedness given the covariates; Overlap / positivity; Honesty: separate subsamples are used to choose splits and to estimate effects. Pre-conditions: Covariates, treatment, and outcome with enough data to grow an honest forest. Failure modes: Calibration test rejects -- the forest's heterogeneity is not well calibrated -> Increase the sample / number of trees, or fall back to a doubly-robust learner. Alternatives: sp.dml, sp.auto_cate, sp.tmle. Typical minimum N: 1000.",
    "input_schema": {
      "properties": {
        "T": {
          "description": "Rows to aggregate over (default: the training sample).",
          "type": "string"
        },
        "X": {
          "description": "Rows to aggregate over (default: the training sample).",
          "type": "string"
        },
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "clip": {
          "default": 0.01,
          "description": "Propensity scores are clipped to ``[clip, 1-clip]`` before the inverse-propensity terms of the ATE / ATT / ATC scores. ``grf`` does not clip; ``clip=0`` reproduces it, and the clip is inert whenever all propensities already lie inside the band.",
          "type": "number"
        },
        "covariates": {
          "default": "none",
          "description": "Forests with fixed effects only: covariates in the untreated outcome model ``Y(0) = alpha_i + gamma_t + C' beta``. ``'none'`` is :func:`statspai.did_imputation` without covariates (same estimate; same standard error with ``variance='bjs'``). ``'auto'`` adds the effect modifiers and covariates that vary within units; use it when a time-varying covariate drives the untreated outcome and is not itself affected by the treatment.",
          "type": "string"
        },
        "forest": {
          "description": "forest parameter ('CausalForest').",
          "type": "string"
        },
        "target_sample": {
          "default": "all",
          "description": "target_sample parameter (str).",
          "enum": [
            "all",
            "treated",
            "control",
            "overlap"
          ],
          "type": "string"
        },
        "variance": {
          "default": "forest",
          "description": "Forests with fixed effects only (see above); ignored otherwise.",
          "enum": [
            "forest",
            "bjs"
          ],
          "type": "string"
        }
      },
      "required": [
        "forest"
      ],
      "type": "object"
    },
    "name": "average_treatment_effect"
  },
  {
    "description": "Goodman-Bacon (2021) decomposition: breaks the two-way fixed-effects DID estimator into its 2x2 comparison weights. Reveals whether treated-vs-treated comparisons (which can have negative weights) dominate the estimate. Run this before trusting a TWFE-DID point estimate. Validation: certified parity evidence.",
    "input_schema": {
      "properties": {
        "id": {
          "type": "string"
        },
        "time": {
          "type": "string"
        },
        "treat": {
          "type": "string"
        },
        "y": {
          "type": "string"
        }
      },
      "required": [
        "y",
        "treat",
        "time",
        "id"
      ],
      "type": "object"
    },
    "name": "bacon_decomposition"
  },
  {
    "description": "Scatter plot of Goodman-Bacon decomposition.",
    "input_schema": {
      "properties": {
        "ax": {
          "description": "ax parameter.",
          "type": "string"
        },
        "bacon_result": {
          "description": "Output from ``bacon_decomposition()``. Must contain ``'decomposition'`` DataFrame and ``'beta_twfe'``.",
          "type": "object"
        },
        "colors": {
          "description": "Map comparison type -> color. Defaults provided.",
          "type": "object"
        },
        "figsize": {
          "default": [
            10,
            6
          ],
          "description": "figsize parameter (Tuple[float, float]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "title": {
          "description": "title parameter (Optional[str]).",
          "type": "string"
        }
      },
      "required": [
        "bacon_result"
      ],
      "type": "object"
    },
    "name": "bacon_plot"
  },
  {
    "description": "Unified balance diagnostics for matching and weighting estimators.",
    "input_schema": {
      "properties": {
        "covariates": {
          "description": "Covariates to audit.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "method": {
          "default": "logit",
          "description": "Propensity-score model when ``ps`` is not supplied.",
          "enum": [
            "logit",
            "probit",
            "gbm"
          ],
          "type": "string"
        },
        "ps": {
          "description": "Propensity scores. If omitted, estimated with ``method``.",
          "type": "string"
        },
        "threshold": {
          "default": 0.1,
          "description": "Balance threshold for absolute standardized mean differences.",
          "type": "number"
        },
        "treatment": {
          "description": "Binary treatment indicator.",
          "type": "string"
        },
        "weights": {
          "description": "Observation weights after matching/weighting. If omitted, ATE inverse-propensity weights are computed from ``ps``.",
          "type": "string"
        }
      },
      "required": [
        "covariates",
        "treatment"
      ],
      "type": "object"
    },
    "name": "balance_diagnostics"
  },
  {
    "description": "Balance a panel by keeping only units observed in every time period. Validation: certified parity evidence.",
    "input_schema": {
      "properties": {
        "entity": {
          "description": "Entity (unit) identifier column.",
          "type": "string"
        },
        "time": {
          "description": "Time period column.",
          "type": "string"
        }
      },
      "required": [
        "entity",
        "time"
      ],
      "type": "object"
    },
    "name": "balance_panel"
  },
  {
    "description": "Love plot: covariate balance visualization (SMD dot plot).",
    "input_schema": {
      "properties": {
        "ax": {
          "description": "ax parameter.",
          "type": "string"
        },
        "figsize": {
          "default": [
            8
          ],
          "description": "Height auto-scales with number of covariates if None.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "result": {
          "description": "Result from ``match()`` or ``ebalance()``.",
          "type": "string"
        },
        "threshold": {
          "default": 0.1,
          "description": "SMD threshold lines.",
          "type": "number"
        },
        "title": {
          "description": "title parameter (Optional[str]).",
          "type": "string"
        }
      },
      "required": [
        "result"
      ],
      "type": "object"
    },
    "name": "balanceplot"
  },
  {
    "description": "Bartik / shift-share IV estimator (Adao-Kolesar-Morales 2019; Borusyak-Hull-Jaravel 2022). Uses pre-period industry / group shares x exogenous shocks as an instrument for local outcome exposure. Validation: certified parity evidence. Assumptions: Exogeneity of shocks conditional on pre-period exposure structure (Borusyak-Hull-Jaravel); Shock-level IV: shocks are independent of region-level unobserved trends; Asymptotic framework: many shocks (L -> inf) -- check via sp.ssaggregate Herfindahl. Pre-conditions: pre-period shares are pre-determined (measured strictly before the outcome window); shocks are as-good-as-random conditional on unit-level controls; >= 50 regions for AKM shift-share SE to be well-sized. Failure modes: Herfindahl of shares too concentrated (one industry dominates) -> Shift-share SE unreliable -- use Adao-Kolesar-Morales shock-level SE via sp.shift_share_se; First-stage F < 10 -> Shares don't predict exposure enough -- report weak-IV-robust CI (sp.anderson_rubin_ci); Shocks correlate with pre-trends -> Shock exogeneity fails -- drop the violating shock dimension or add trend controls. Alternatives: sp.iv, sp.shift_share_se, sp.shift_share_political, sp.shift_sha...",
    "input_schema": {
      "properties": {
        "covariates": {
          "description": "Covariate matrix, DataFrame, or column names.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "endog": {
          "description": "Endogenous local exposure being instrumented (e.g. employment growth)",
          "type": "string"
        },
        "shares": {
          "description": "Pre-period share column (e.g. industry share)",
          "type": "string"
        },
        "shocks": {
          "description": "Shock column (e.g. industry-level change)",
          "type": "string"
        },
        "y": {
          "description": "Outcome (e.g. local wage growth)",
          "type": "string"
        }
      },
      "required": [
        "endog",
        "shares",
        "shocks",
        "y"
      ],
      "type": "object"
    },
    "name": "bartik"
  },
  {
    "description": "Basque Country terrorism dataset (simulated).",
    "input_schema": {
      "properties": {},
      "required": [],
      "type": "object"
    },
    "name": "basque_terrorism"
  },
  {
    "description": "Bauer-Sinning (2008) nonlinear Oaxaca-Blinder decomposition with Validation: certified parity evidence.",
    "input_schema": {
      "properties": {
        "group": {
          "description": "Group or cohort identifier.",
          "type": "string"
        },
        "model": {
          "default": "logit",
          "description": "Model variant or parameterisation to fit.",
          "enum": [
            "logit",
            "probit"
          ],
          "type": "string"
        },
        "reference": {
          "default": 0,
          "description": "reference parameter (int).",
          "enum": [
            "0",
            "1"
          ],
          "type": "integer"
        },
        "variant": {
          "default": "yun",
          "description": "variant parameter (str).",
          "type": "string"
        },
        "x": {
          "description": "Primary running variable, regressor, or feature input for this estimator.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "group",
        "x",
        "y"
      ],
      "type": "object"
    },
    "name": "bauer_sinning"
  },
  {
    "description": "Estimate heterogeneous treatment effects using Bayesian Causal Forest. Validation: validated evidence tier (known-truth, reference, external-parity, or Monte Carlo artifact). Assumptions: Unconfoundedness and overlap; The BART/forest priors are appropriate for the outcome scale. Pre-conditions: Covariates, treatment, and outcome; a propensity model is fit internally to limit regularization-induced confounding. Failure modes: MCMC diagnostics fail to converge, or estimates are sensitive to the prior -> Increase draws/tuning, re-scale the outcome, and report posterior diagnostics. Alternatives: sp.dml, sp.auto_cate, sp.causal_forest. Typical minimum N: 250.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level.",
          "type": "number"
        },
        "covariates": {
          "description": "Covariate names.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "n_bootstrap": {
          "default": 200,
          "description": "Bootstrap iterations for uncertainty quantification.",
          "type": "integer"
        },
        "n_folds": {
          "default": 5,
          "description": "Cross-fitting folds for propensity estimation.",
          "type": "integer"
        },
        "n_trees_mu": {
          "default": 200,
          "description": "Number of trees for the prognostic function mu(X).",
          "type": "integer"
        },
        "n_trees_tau": {
          "default": 50,
          "description": "Number of trees for the treatment effect function tau(X). Fewer trees = stronger shrinkage toward homogeneous effects.",
          "type": "integer"
        },
        "random_state": {
          "default": 42,
          "description": "Random seed or RandomState for reproducible stochastic steps.",
          "type": "integer"
        },
        "treat": {
          "description": "Binary treatment variable (0/1).",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable.",
          "type": "string"
        }
      },
      "required": [
        "covariates",
        "treat",
        "y"
      ],
      "type": "object"
    },
    "name": "bcf"
  },
  {
    "description": "BCF on PCA-factor scores of a high-dimensional exposure vector (arXiv:2601.16595, 2026). Compresses exposures via SVD or user-supplied loadings, then fits one BCF per factor. Validation: validated evidence tier (known-truth, reference, external-parity, or Monte Carlo artifact). Assumptions: Unconfoundedness and overlap; The BART/forest priors are appropriate for the outcome scale. Pre-conditions: Covariates, treatment, and outcome; a propensity model is fit internally to limit regularization-induced confounding. Failure modes: MCMC diagnostics fail to converge, or estimates are sensitive to the prior -> Increase draws/tuning, re-scale the outcome, and report posterior diagnostics. Alternatives: sp.dml, sp.auto_cate, sp.causal_forest. Typical minimum N: 250.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "binarize": {
          "default": "median",
          "description": "binarize parameter (str).",
          "enum": [
            "median",
            "zero",
            "none"
          ],
          "type": "string"
        },
        "covariates": {
          "description": "Covariate matrix, DataFrame, or column names.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "exposures": {
          "description": "exposures parameter (list).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "loadings": {
          "description": "loadings parameter (DataFrame).",
          "type": "string"
        },
        "n_bootstrap": {
          "default": 100,
          "description": "Number of bootstrap replications.",
          "type": "integer"
        },
        "n_factors": {
          "default": 3,
          "description": "Number of factors.",
          "type": "integer"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "covariates",
        "exposures",
        "y"
      ],
      "type": "object"
    },
    "name": "bcf_factor_exposure"
  },
  {
    "description": "Hierarchical Bayesian Causal Forest for longitudinal data (BCFLong) -- allows mu_t(X), tau_t(X) to evolve across time with unit-level random intercepts. Assumptions: Unconfoundedness and overlap; The BART/forest priors are appropriate for the outcome scale. Pre-conditions: Covariates, treatment, and outcome; a propensity model is fit internally to limit regularization-induced confounding. Failure modes: MCMC diagnostics fail to converge, or estimates are sensitive to the prior -> Increase draws/tuning, re-scale the outcome, and report posterior diagnostics. Alternatives: sp.dml, sp.auto_cate, sp.causal_forest. Typical minimum N: 250.",
    "input_schema": {
      "properties": {
        "covariates": {
          "description": "Covariate matrix, DataFrame, or column names.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "n_bootstrap": {
          "default": 100,
          "description": "Number of bootstrap replications.",
          "type": "integer"
        },
        "n_trees_mu": {
          "default": 200,
          "description": "Number of trees mu.",
          "type": "integer"
        },
        "n_trees_tau": {
          "default": 50,
          "description": "Number of trees tau.",
          "type": "integer"
        },
        "outcome": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        },
        "time": {
          "description": "Time period column.",
          "type": "string"
        },
        "treatment": {
          "description": "Treatment indicator, treatment variable, or treatment array.",
          "type": "string"
        },
        "unit": {
          "description": "Unit identifier column.",
          "type": "string"
        }
      },
      "required": [
        "covariates",
        "outcome",
        "time",
        "treatment",
        "unit"
      ],
      "type": "object"
    },
    "name": "bcf_longitudinal"
  },
  {
    "description": "Bayesian Causal Forest for ordered / dose-level treatment (Zorzetto et al. 2026). Estimates cumulative dose-response curves via chained BCF between consecutive levels. Assumptions: Unconfoundedness and overlap; The BART/forest priors are appropriate for the outcome scale. Pre-conditions: Covariates, treatment, and outcome; a propensity model is fit internally to limit regularization-induced confounding. Failure modes: MCMC diagnostics fail to converge, or estimates are sensitive to the prior -> Increase draws/tuning, re-scale the outcome, and report posterior diagnostics. Alternatives: sp.dml, sp.auto_cate, sp.causal_forest. Typical minimum N: 250.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "baseline": {
          "description": "baseline parameter (str).",
          "type": "string"
        },
        "covariates": {
          "description": "Covariate matrix, DataFrame, or column names.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "n_bootstrap": {
          "default": 100,
          "description": "Number of bootstrap replications.",
          "type": "integer"
        },
        "n_folds": {
          "default": 5,
          "description": "Number of cross-fitting or cross-validation folds.",
          "type": "integer"
        },
        "n_trees_mu": {
          "default": 200,
          "description": "Number of trees mu.",
          "type": "integer"
        },
        "n_trees_tau": {
          "default": 50,
          "description": "Number of trees tau.",
          "type": "integer"
        },
        "random_state": {
          "default": 42,
          "description": "Random seed or RandomState for reproducible stochastic steps.",
          "type": "integer"
        },
        "treat": {
          "description": "Treatment indicator or first-treatment-period column.",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "covariates",
        "treat",
        "y"
      ],
      "type": "object"
    },
    "name": "bcf_ordinal"
  },
  {
    "description": "Benjamini-Hochberg (1995) FDR correction. Validation: certified parity evidence.",
    "input_schema": {
      "properties": {
        "pvalues": {
          "description": "Unadjusted p-values.",
          "type": "string"
        }
      },
      "required": [
        "pvalues"
      ],
      "type": "object"
    },
    "name": "benjamini_hochberg"
  },
  {
    "description": "Best linear projection of a forest's conditional effect on covariates (grf::best_linear_projection): OLS of the doubly-robust scores on (1, A) with HC0-HC3 (cluster-robust when the forest has clusters) standard errors -- valid inference on a low-dimensional summary of tau(x) (Semenova and Chernozhukov 2021). Works for causal, instrumental, multi-arm and causal-survival forests; matches grf to 1e-15 given the scores. Validation: validated evidence tier (known-truth, reference, external-parity, or Monte Carlo artifact). Assumptions: the forest's nuisances are consistent (doubly-robust scores). Pre-conditions: A has one row per training observation. Failure modes: MethodIncompatibility: A must have one row per training observation -> Align A with the rows the forest was fitted on. Alternatives: sp.calibration_test, sp.rate, sp.forest_group_effects.",
    "input_schema": {
      "properties": {
        "A": {
          "description": "Projection covariates, one row per training observation (after missing-value removal).",
          "type": "string"
        },
        "alpha": {
          "default": 0.05,
          "description": "Significance level for the intervals.",
          "type": "number"
        },
        "forest": {
          "description": "``sp.causal_forest`` (GRF engine), ``sp.iv_forest``, ``sp.multi_arm_forest`` (one projection per contrast, stacked), ``sp.causal_survival_forest``.",
          "type": "string"
        },
        "vce": {
          "default": "HC3",
          "description": "Covariance type HC0-HC3 (sandwich::vcovCL conventions; grf's vcov.type). vcov_type= is accepted as an alias.",
          "type": "string"
        }
      },
      "required": [
        "forest"
      ],
      "type": "object"
    },
    "name": "best_linear_projection"
  },
  {
    "description": "Beta regression (Ferrari & Cribari-Neto 2004). Validation: certified parity evidence.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "cluster": {
          "description": "Cluster identifier column for clustered standard errors.",
          "type": "string"
        },
        "link": {
          "default": "logit",
          "description": "Link for mean: 'logit', 'probit', 'cloglog'.",
          "type": "string"
        },
        "maxiter": {
          "default": 200,
          "description": "maxiter parameter (int).",
          "type": "integer"
        },
        "robust": {
          "default": "nonrobust",
          "description": "Robust standard-error or covariance estimator option.",
          "type": "string"
        },
        "tol": {
          "default": 1e-08,
          "description": "Numerical convergence tolerance.",
          "type": "number"
        },
        "x": {
          "description": "Regressors for the mean equation.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Outcome in (0, 1).",
          "type": "string"
        },
        "z": {
          "description": "Regressors for the precision equation. If None, constant precision.",
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "required": [],
      "type": "object"
    },
    "name": "betareg"
  },
  {
    "description": "Beyond-average LATE (Xie-Wu 2025). Identifies the entire treatment-effect distribution among compliers under incomplete compliance, not just its mean. Validation: validated evidence tier (known-truth, reference, external-parity, or Monte Carlo artifact). Assumptions: Selection-on-observables (unconfoundedness + overlap) or, for IV variants, instrument validity; For IV-QTE: rank invariance / rank similarity (monotonicity of the structural quantile function). Pre-conditions: Covariates, treatment, and outcome; for IV-quantile methods, a valid instrument; Enough data to estimate the outcome distribution across quantiles. Failure modes: Estimated conditional quantiles cross (non-monotone), or tail quantiles are unstable -> Use rearrangement / monotonization and avoid extreme quantiles where data are sparse. Alternatives: sp.qte, sp.iv, sp.dml. Typical minimum N: 500.",
    "input_schema": {
      "properties": {
        "instrument": {
          "description": "instrument parameter (str).",
          "type": "string"
        },
        "quantiles": {
          "description": "Quantiles tau at which to evaluate QTE (default 0.1..0.9 step 0.1)",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "treat": {
          "description": "Treatment indicator or first-treatment-period column.",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "instrument",
        "treat",
        "y"
      ],
      "type": "object"
    },
    "name": "beyond_average_late"
  },
  {
    "description": "Confounding bias factor ``B`` (Ding & VanderWeele 2016). Validation: certified parity evidence.",
    "input_schema": {
      "properties": {
        "rr_eu": {
          "description": "rr_eu parameter (float).",
          "type": "number"
        },
        "rr_ud": {
          "description": "rr_ud parameter (float).",
          "type": "number"
        }
      },
      "required": [
        "rr_eu",
        "rr_ud"
      ],
      "type": "object"
    },
    "name": "bias_factor"
  },
  {
    "description": "Top-level structured citation for a fitted result.",
    "input_schema": {
      "properties": {
        "result": {
          "description": "Any fitted result object exposing a ``.cite()`` method.",
          "type": "string"
        }
      },
      "required": [
        "result"
      ],
      "type": "object"
    },
    "name": "bib_for"
  },
  {
    "description": "Return verified BibTeX entries from paper.bib (StatsPAI's single source of truth for citations). Pass one or more bib keys (e.g. 'callaway2021difference'). NEVER invent citations -- call this tool instead. Unknown keys return an empty entry plus a list of close matches.",
    "input_schema": {
      "properties": {
        "keys": {
          "description": "Bib keys to look up. Most estimators advertise their key in agent_card.reference.",
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "required": [
        "keys"
      ],
      "type": "object"
    },
    "name": "bibtex"
  },
  {
    "description": "Bidirectional proximal causal inference (Min, Zhang & Luo 2025). Solves for both outcome and treatment bridges simultaneously in a single two-way regression system. Validation: validated evidence tier (known-truth, reference, external-parity, or Monte Carlo artifact). Assumptions: The proxies are valid negative controls (relevant to the confounder, excluded from the causal channel); A bridge function exists (completeness conditions hold). Pre-conditions: Treatment-inducing and outcome-inducing proxy variables (negative controls) for the unobserved confounder. Failure modes: Proxies are weak or invalid -- the bridge function is poorly identified -> Test proxy relevance, select stronger proxies, or fall back to sensitivity analysis. Alternatives: sp.select_pci_proxies, sp.dml. Typical minimum N: 500.",
    "input_schema": {
      "properties": {
        "covariates": {
          "description": "Covariate matrix, DataFrame, or column names.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "proxy_w": {
          "description": "proxy_w parameter (list).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "proxy_z": {
          "description": "proxy_z parameter (list).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "treat": {
          "description": "Treatment indicator or first-treatment-period column.",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "proxy_w",
        "proxy_z",
        "treat",
        "y"
      ],
      "type": "object"
    },
    "name": "bidirectional_pci"
  },
  {
    "description": "Bivariate probit model. Validation: certified parity evidence.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "cluster": {
          "description": "Cluster identifier column for clustered standard errors.",
          "type": "string"
        },
        "maxiter": {
          "default": 200,
          "description": "maxiter parameter (int).",
          "type": "integer"
        },
        "robust": {
          "default": "nonrobust",
          "description": "Robust standard-error or covariance estimator option.",
          "type": "string"
        },
        "tol": {
          "default": 1e-08,
          "description": "Numerical convergence tolerance.",
          "type": "number"
        },
        "x1": {
          "description": "Regressors for equation 1.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "x2": {
          "description": "Regressors for equation 2. If None, same as x1.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y1": {
          "description": "First binary outcome.",
          "type": "string"
        },
        "y2": {
          "description": "Second binary outcome.",
          "type": "string"
        }
      },
      "required": [
        "x1",
        "y1",
        "y2"
      ],
      "type": "object"
    },
    "name": "biprobit"
  },
  {
    "description": "Borusyak, Jaravel & Spiess (2024) imputation DID estimator. Validation: certified parity evidence. Do NOT use when: no never-treated or not-yet-treated observations remain -- the imputation model cannot be fit; the design is a simple 2x2 -- use sp.did(method='2x2'). Assumptions: Conditional parallel trends between treated and comparison groups absent treatment; No anticipation of treatment before its onset; Treatment effects may be heterogeneous across cohorts and time (no homogeneity required). Pre-conditions: Panel or repeated cross-section with a unit (or group) identifier and a time identifier; At least one never-treated or not-yet-treated comparison group; Pre-treatment periods to assess parallel trends. Failure modes: Pre-treatment event-study coefficients are jointly non-zero (pre-trend violation) -> Use honest DiD bounds to quantify robustness to trend violations, or condition on covariates; Two-way fixed-effects estimate is contaminated by 'forbidden' comparisons / negative weights -> Use a heterogeneity-robust estimator (Callaway-Sant'Anna, Borusyak et al., Gardner two-stage). Alternatives: sp.callaway_santanna, sp.did, sp.honest_did. Typical minimum N: 100.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals.",
          "type": "number"
        },
        "balanced": {
          "default": false,
          "description": "Stata ``did_imputation, hbalance``: keep only eventually-treated units observed at *every* requested horizon, so the event-study composition is stable across ``k`` (no cohort churn). Never-treated units are always kept. Requires ``horizon`` (or ``pretrends``). Warns with the number of units dropped.",
          "type": "boolean"
        },
        "boot_seed": {
          "default": 0,
          "description": "Seed for the cluster bootstrap (deterministic results).",
          "type": "integer"
        },
        "cluster": {
          "description": "Variable for cluster-robust standard errors. Defaults to ``group`` (unit-level clustering).",
          "type": "string"
        },
        "controls": {
          "description": "Continuous time-varying controls, entering the Y(0) model additively. Stata ``did_imputation``'s ``controls()``.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "fe": {
          "description": "Which fixed effects the Y(0) model carries, replacing the default two-way ``unit + time``. Mirrors Stata ``did_imputation``'s ``fe()``. Each entry is one fixed effect, written either as a bare column or Stata-style with ``#`` for the interacted cell:: fe=['time'] # period FE only fe=['unit', 'state#year'] # unit FE + state-by-year FE fe=[] # no fixed effects at all Levels are factorized over the whole panel, so a cell seen only among treated rows still gets a column and is imputed rather than silently absorbing the reference level.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "first_treat": {
          "description": "Column indicating the period of first treatment. Use ``np.inf``, ``np.nan``, or ``0`` for never-treated units.",
          "type": "string"
        },
        "group": {
          "description": "Unit identifier column.",
          "type": "string"
        },
        "hetby": {
          "description": "Stata ``did_imputation, hetby(varname)``: report heterogeneous overall ATTs by the levels of a **time-invariant** unit-level variable. Results land in ``model_info['hetby']`` (one row per",
          "type": "string"
        },
        "horizon": {
          "description": "Relative time periods for event study estimates, e.g. ``list(range(-5, 6))``. If ``None``, reports only the overall ATT (no event study disaggregation).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "min_n": {
          "description": "Stata ``did_imputation, minn(#)``: drop event-study horizons with fewer than ``min_n`` treated observations (they are noisy and dominated by a single cohort). Dropped horizons are listed in a warning and excluded from the pre-trend test.",
          "type": "integer"
        },
        "n_boot": {
          "default": 199,
          "description": "Number of cluster-bootstrap replications when ``vce='bootstrap'``.",
          "type": "integer"
        },
        "pretrend_method": {
          "default": "bjs",
          "description": "How the **pre-treatment** event-study coefficients are built. The post-treatment coefficients are imputation residuals either way; only the leads differ, and the difference is visible in the plot rather than in the ATT. - ``'bjs'`` -- the convention of Stata ``did_imputation, pretrends(k)``: an auxiliary dynamic TWFE regression on the untreated observations, with all relative times earlier than the requested leads pooled into the omitted category. Reproduces Stata's coefficients and standard errors. Not available with ``fe=``, ``unit_covariates=`` or ``time_covariates=``. - ``'in-sample'`` -- average the imputation residuals at pre-treatment relative times, as ``fect`` and ``did2s`` do. These are in-sample prediction errors: in a non-staggered design they equal the symmetric benchmark times the untreated unit share ``N0/N``, so they understate pre-trends, severely when most units are treated. This was StatsPAI's behaviour before v1.23.0. - ``'symmetric'`` -- Roth's (2026) repair, which uses the average of the pre-treatment periods as the reference for both halves of the path, so the plot matches a dynamic TWFE event study up to a common vertical shift and the usual visual heuristics apply. Non-staggered balanced designs without covariates only; raises otherwise rather than applying an unverified factor. The chosen convention and its caveat are recorded in ``model_info['pretrend_method']`` and ``model_info['event_study_convention']``.",
          "enum": [
            "bjs",
            "in-sample",
            "symmetric"
          ],
          "type": "string"
        },
        "pretrends": {
          "description": "Stata ``did_imputation, pretrends(k)``: estimate the ``k`` pre-treatment placebo coefficients (horizons ``-k .. -1``, added to ``horizon`` if not already requested) and report their joint Wald test in ``model_info['pretrend_test']``. Under the default ``pretrend_method='bjs'`` the test uses the full cluster-robust covariance of the auxiliary lead regression; under the other conventions it assumes the lead estimates are uncorrelated (valid but conservative), and :func:`statspai.bjs_pretrend_joint` gives the covariance-aware cluster-bootstrap version.",
          "type": "integer"
        },
        "project": {
          "description": "project parameter (Optional[List[str]]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "save_residuals": {
          "default": false,
          "description": "Stata ``did_imputation, saveresid()``: store the untreated-fit residuals ``y - y0`` in ``model_info['residuals']`` (aligned with the rows of ``data``; ``NaN`` on treated rows, whose ``y - y0`` is the treatment effect, not a residual).",
          "type": "boolean"
        },
        "save_weights": {
          "default": false,
          "description": "Stata ``did_imputation, saveweights()``: store the exact estimation weights ``w`` such that ``ATT = w'y`` in ``model_info['estimation_weights']`` (aligned with the rows of ``data``). Treated rows get ``1/N1``; untreated rows get the (negative) imputation weights implied by the FE projection -- useful for diagnosing which comparisons drive the estimate.",
          "type": "boolean"
        },
        "se_method": {
          "description": "Shared DiD spelling for ``vce=`` -- ``'analytic'``, ``'bootstrap'`` (also ``'cluster'``, ``'pairs'``) or ``'auto'``. Passing both raises. ``'auto'`` picks the cluster bootstrap when the design has at most 30 clusters (Cameron, Gelbach & Miller 2008) and the analytic BJS variance otherwise: with few clusters the cluster-score sum behind any sandwich is itself noisy, which is a small-cluster problem rather than a defect in the formula.",
          "type": "string"
        },
        "time": {
          "description": "Time period column.",
          "type": "string"
        },
        "time_covariates": {
          "description": "Controls interacted with the **period** fixed effects, i.e. one coefficient per period. Stata's ``timecontrols()``. Typically a time-invariant unit characteristic whose effect is allowed to move over calendar time.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "unit_covariates": {
          "description": "Controls interacted with the **unit** fixed effects, i.e. one slope per unit. Stata's ``unitcontrols()``. The canonical use is ``unit_covariates=[time_col]``, which makes Y(0) carry unit-specific linear trends. Identification bites hard here: a unit-specific slope needs at least two untreated periods for that unit, so early-treated cohorts can lose their imputation entirely.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "vce": {
          "default": "analytic",
          "description": "Standard-error mode for the overall ATT. ``'analytic'`` is the exact Borusyak--Jaravel--Spiess variance: the estimator is linear in the outcome, so its weights are computed rather than approximated, and the result reproduces Stata ``did_imputation`` and R ``didimputation`` to ~5e-8. Before v1.23.0 this path used an approximation that was materially anti-conservative (18-36% too small on the harness fixtures); see MIGRATION.md. Measured, not asserted. On a homogeneous-effect design with a never-treated half, 400-800 replications per cell: =========== ========== ==================== clusters coverage mean SE / sd(est) =========== ========== ==================== 30 0.925 0.985 60 0.938 0.936 120 0.948 0.978 240 0.935 1.007 480 **0.950** 0.983 =========== ========== ==================== Two things to read off it. The ratio sits at 1 throughout, so the variance formula carries no systematic bias -- the approximation it replaced ran 18-36% low. And coverage reaches nominal by 480 clusters, so the shortfall at 60 is a small-cluster effect rather than a missing term: there the standard error is simply *noisy* (dispersion 9.2% against 3.2% at 480), which fattens the studentised distribution (sd(t) = 1.04 against 1.007). A ``t(G-1)`` critical value barely helps (0.938 to 0.941 at G = 60) because the problem is the variability of the standard error, not the degrees of freedom. ``vce='bootstrap'`` is the remedy in small designs, and ``se_method='auto'`` selects it below 30 clusters. ``'bootstrap'`` resamples whole clusters and re-runs the full imputation estimator. Point estimates are identical either way; per-horizon event-study SEs are unaffected.",
          "enum": [
            "analytic",
            "bootstrap"
          ],
          "type": "string"
        },
        "weights": {
          "description": "Column of estimation weights ``omega_i`` (Stata ``[aw=]``, R ``didimputation(weights=)``). The untreated Y(0) model is fit by weighted least squares, every reported average is the ``omega``-weighted mean over the treated cells it covers, and the exact variance uses the weighted projection. Like ``weights=`` on the other staggered estimators this changes the *estimand*: the weighted ATT averages over the population the treated units represent, not over the units. Not combinable with ``project=``.",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable name.",
          "type": "string"
        }
      },
      "required": [
        "first_treat",
        "group",
        "time",
        "y"
      ],
      "type": "object"
    },
    "name": "bjs"
  },
  {
    "description": "Cluster-bootstrap joint Wald test for BJS pre-treatment coefficients.",
    "input_schema": {
      "properties": {
        "cluster": {
          "description": "Cluster identifier column for clustered standard errors.",
          "type": "string"
        },
        "controls": {
          "description": "Control-variable column names.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "first_treat": {
          "description": "first_treat parameter (str).",
          "type": "string"
        },
        "group": {
          "description": "Group or cohort identifier.",
          "type": "string"
        },
        "horizon": {
          "description": "If omitted, inferred from ``result.model_info['event_study']``.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "n_boot": {
          "default": 300,
          "description": "Cluster-bootstrap replications. Clusters are sampled with replacement; unit ids are reassigned in the resampled frame so BJS refits cleanly.",
          "type": "integer"
        },
        "result": {
          "description": "Output of :func:`did_imputation` on ``data`` with a non-trivial ``horizon`` that covers negative values. Only its ``model_info['event_study']`` frame is consulted, to look up the observed pre-period point estimates that we re-test with a covariance-aware statistic. Same arguments you passed to the original :func:`did_imputation` call. Needed to re-run BJS on each cluster-bootstrap resample.",
          "type": "string"
        },
        "seed": {
          "description": "RNG seed for reproducibility.",
          "type": "integer"
        },
        "time": {
          "description": "Time period column.",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "first_treat",
        "group",
        "result",
        "time",
        "y"
      ],
      "type": "object"
    },
    "name": "bjs_pretrend_joint"
  },
  {
    "description": "Best Linear Predictor (BLP) of the CATE, Chernozhukov-Demirer-Duflo-Fernandez-Val. Validation: certified parity evidence. Assumptions: Unconfoundedness given the covariates; Overlap / positivity across the covariate space; Nuisance functions are estimated consistently; cross-fitting controls overfitting bias. Pre-conditions: Covariates, a treatment indicator, and an outcome for each unit; Enough data to fit flexible nuisance models with sample-splitting / cross-fitting. Failure modes: CATE estimates are unstable or extrapolate beyond the covariate support -> Restrict to the overlap region, increase data, or use a doubly-robust learner (DR-/R-learner). Alternatives: sp.dml, sp.causal_forest, sp.tmle. Typical minimum N: 500.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "baseline": {
          "description": "Baseline proxy ``B(Z)`` added as a control.",
          "type": "string"
        },
        "covariates": {
          "description": "Covariates ``Z`` (for the propensity and the proxy refit).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "n_folds": {
          "default": 5,
          "description": "Folds for the propensity and proxy cross-fits.",
          "type": "integer"
        },
        "propensity": {
          "description": "Known or estimated ``p(Z)``. Default: 5-fold gradient-boosting classifier, clipped to [0.01, 0.99]. In a randomized experiment pass the design probabilities.",
          "type": "string"
        },
        "proxy": {
          "default": "cross_fit",
          "description": "Source of ``S(Z)``.",
          "enum": [
            "cross_fit",
            "in_sample"
          ],
          "type": "string"
        },
        "result": {
          "description": "A ``metalearner()`` result (its learner is refit out of fold), or per-unit CATE predictions used as supplied.",
          "type": "string"
        },
        "seed": {
          "default": 42,
          "description": "Fold seed for the proxy refit.",
          "type": "integer"
        },
        "treat": {
          "description": "Outcome and binary treatment columns.",
          "type": "string"
        },
        "vce": {
          "default": "HC1",
          "description": "Covariance of the WLS fit (``sandwich::vcovHC`` types; GenericML's default is ``'const'``).",
          "enum": [
            "HC1",
            "const",
            "HC0",
            "HC2",
            "HC3"
          ],
          "type": "string"
        },
        "y": {
          "description": "Outcome and binary treatment columns.",
          "type": "string"
        }
      },
      "required": [
        "covariates",
        "result",
        "treat",
        "y"
      ],
      "type": "object"
    },
    "name": "blp_test"
  },
  {
    "description": "Bonferroni correction: ``p_adj = min(p * S, 1)``. Validation: certified parity evidence.",
    "input_schema": {
      "properties": {
        "pvalues": {
          "description": "Unadjusted p-values.",
          "type": "string"
        }
      },
      "required": [
        "pvalues"
      ],
      "type": "object"
    },
    "name": "bonferroni"
  },
  {
    "description": "General bootstrap inference: nonparametric, cluster, block. Percentile/BCa/normal CIs. Validation: validated evidence tier (known-truth, reference, external-parity, or Monte Carlo artifact). Assumptions: Observations (or clusters/blocks) are exchangeable under the resampling scheme used; cluster= resamples whole clusters, block= preserves within-block ordering for serial dependence; The statistic callable is a smooth, well-defined functional of the data so its sampling distribution is approximable by resampling; BCa intervals further assume an estimable bias/acceleration; percentile/normal CIs assume approximate pivotality of the resampled distribution. Pre-conditions: statistic is a deterministic callable returning a finite scalar on any resampled DataFrame; Enough independent resampling units (rows, clusters, or blocks) to populate n_boot replicates; Specify cluster= or block= when data are clustered/serially dependent rather than iid. Failure modes: Few clusters yields anti-conservative CIs / wildly unstable bootstrap distribution -> Use a wild cluster bootstrap with Rademacher weights when the number of clusters is small (<~40); Statistic raises or returns NaN on a resample (e...",
    "input_schema": {
      "properties": {
        "ci_method": {
          "default": "percentile",
          "description": "CI method",
          "enum": [
            "percentile",
            "bca",
            "normal"
          ],
          "type": "string"
        },
        "cluster": {
          "description": "Cluster variable for cluster bootstrap",
          "type": "string"
        },
        "n_boot": {
          "default": 1000,
          "description": "Number of bootstrap replications.",
          "type": "integer"
        },
        "statistic": {
          "description": "Function f(df) -> float",
          "type": "string"
        }
      },
      "required": [
        "statistic"
      ],
      "type": "object"
    },
    "name": "bootstrap"
  },
  {
    "description": "Borusyak, Jaravel & Spiess (2024) imputation DID estimator. Validation: certified parity evidence. Do NOT use when: no never-treated or not-yet-treated observations remain -- the imputation model cannot be fit; the design is a simple 2x2 -- use sp.did(method='2x2'). Assumptions: Conditional parallel trends between treated and comparison groups absent treatment; No anticipation of treatment before its onset; Treatment effects may be heterogeneous across cohorts and time (no homogeneity required). Pre-conditions: Panel or repeated cross-section with a unit (or group) identifier and a time identifier; At least one never-treated or not-yet-treated comparison group; Pre-treatment periods to assess parallel trends. Failure modes: Pre-treatment event-study coefficients are jointly non-zero (pre-trend violation) -> Use honest DiD bounds to quantify robustness to trend violations, or condition on covariates; Two-way fixed-effects estimate is contaminated by 'forbidden' comparisons / negative weights -> Use a heterogeneity-robust estimator (Callaway-Sant'Anna, Borusyak et al., Gardner two-stage); Pre-trend joint test p < 0.05 (or underpowered at 0.10) -> Use sp.sensitivity_rr (Rambachan & R...",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals.",
          "type": "number"
        },
        "balanced": {
          "default": false,
          "description": "Stata ``did_imputation, hbalance``: keep only eventually-treated units observed at *every* requested horizon, so the event-study composition is stable across ``k`` (no cohort churn). Never-treated units are always kept. Requires ``horizon`` (or ``pretrends``). Warns with the number of units dropped.",
          "type": "boolean"
        },
        "boot_seed": {
          "default": 0,
          "description": "Seed for the cluster bootstrap (deterministic results).",
          "type": "integer"
        },
        "cluster": {
          "description": "Variable for cluster-robust standard errors. Defaults to ``group`` (unit-level clustering).",
          "type": "string"
        },
        "controls": {
          "description": "Continuous time-varying controls, entering the Y(0) model additively. Stata ``did_imputation``'s ``controls()``.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "fe": {
          "description": "Which fixed effects the Y(0) model carries, replacing the default two-way ``unit + time``. Mirrors Stata ``did_imputation``'s ``fe()``. Each entry is one fixed effect, written either as a bare column or Stata-style with ``#`` for the interacted cell:: fe=['time'] # period FE only fe=['unit', 'state#year'] # unit FE + state-by-year FE fe=[] # no fixed effects at all Levels are factorized over the whole panel, so a cell seen only among treated rows still gets a column and is imputed rather than silently absorbing the reference level.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "first_treat": {
          "description": "Column indicating the period of first treatment. Use ``np.inf``, ``np.nan``, or ``0`` for never-treated units.",
          "type": "string"
        },
        "group": {
          "description": "Unit identifier column.",
          "type": "string"
        },
        "hetby": {
          "description": "Stata ``did_imputation, hetby(varname)``: report heterogeneous overall ATTs by the levels of a **time-invariant** unit-level variable. Results land in ``model_info['hetby']`` (one row per",
          "type": "string"
        },
        "horizon": {
          "description": "Relative time periods for event study estimates, e.g. ``list(range(-5, 6))``. If ``None``, reports only the overall ATT (no event study disaggregation).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "min_n": {
          "description": "Stata ``did_imputation, minn(#)``: drop event-study horizons with fewer than ``min_n`` treated observations (they are noisy and dominated by a single cohort). Dropped horizons are listed in a warning and excluded from the pre-trend test.",
          "type": "integer"
        },
        "n_boot": {
          "default": 199,
          "description": "Number of cluster-bootstrap replications when ``vce='bootstrap'``.",
          "type": "integer"
        },
        "pretrend_method": {
          "default": "bjs",
          "description": "How the **pre-treatment** event-study coefficients are built. The post-treatment coefficients are imputation residuals either way; only the leads differ, and the difference is visible in the plot rather than in the ATT. - ``'bjs'`` -- the convention of Stata ``did_imputation, pretrends(k)``: an auxiliary dynamic TWFE regression on the untreated observations, with all relative times earlier than the requested leads pooled into the omitted category. Reproduces Stata's coefficients and standard errors. Not available with ``fe=``, ``unit_covariates=`` or ``time_covariates=``. - ``'in-sample'`` -- average the imputation residuals at pre-treatment relative times, as ``fect`` and ``did2s`` do. These are in-sample prediction errors: in a non-staggered design they equal the symmetric benchmark times the untreated unit share ``N0/N``, so they understate pre-trends, severely when most units are treated. This was StatsPAI's behaviour before v1.23.0. - ``'symmetric'`` -- Roth's (2026) repair, which uses the average of the pre-treatment periods as the reference for both halves of the path, so the plot matches a dynamic TWFE event study up to a common vertical shift and the usual visual heuristics apply. Non-staggered balanced designs without covariates only; raises otherwise rather than applying an unverified factor. The chosen convention and its caveat are recorded in ``model_info['pretrend_method']`` and ``model_info['event_study_convention']``.",
          "enum": [
            "bjs",
            "in-sample",
            "symmetric"
          ],
          "type": "string"
        },
        "pretrends": {
          "description": "Stata ``did_imputation, pretrends(k)``: estimate the ``k`` pre-treatment placebo coefficients (horizons ``-k .. -1``, added to ``horizon`` if not already requested) and report their joint Wald test in ``model_info['pretrend_test']``. Under the default ``pretrend_method='bjs'`` the test uses the full cluster-robust covariance of the auxiliary lead regression; under the other conventions it assumes the lead estimates are uncorrelated (valid but conservative), and :func:`statspai.bjs_pretrend_joint` gives the covariance-aware cluster-bootstrap version.",
          "type": "integer"
        },
        "project": {
          "description": "project parameter (Optional[List[str]]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "save_residuals": {
          "default": false,
          "description": "Stata ``did_imputation, saveresid()``: store the untreated-fit residuals ``y - y0`` in ``model_info['residuals']`` (aligned with the rows of ``data``; ``NaN`` on treated rows, whose ``y - y0`` is the treatment effect, not a residual).",
          "type": "boolean"
        },
        "save_weights": {
          "default": false,
          "description": "Stata ``did_imputation, saveweights()``: store the exact estimation weights ``w`` such that ``ATT = w'y`` in ``model_info['estimation_weights']`` (aligned with the rows of ``data``). Treated rows get ``1/N1``; untreated rows get the (negative) imputation weights implied by the FE projection -- useful for diagnosing which comparisons drive the estimate.",
          "type": "boolean"
        },
        "se_method": {
          "description": "Shared DiD spelling for ``vce=`` -- ``'analytic'``, ``'bootstrap'`` (also ``'cluster'``, ``'pairs'``) or ``'auto'``. Passing both raises. ``'auto'`` picks the cluster bootstrap when the design has at most 30 clusters (Cameron, Gelbach & Miller 2008) and the analytic BJS variance otherwise: with few clusters the cluster-score sum behind any sandwich is itself noisy, which is a small-cluster problem rather than a defect in the formula.",
          "type": "string"
        },
        "time": {
          "description": "Time period column.",
          "type": "string"
        },
        "time_covariates": {
          "description": "Controls interacted with the **period** fixed effects, i.e. one coefficient per period. Stata's ``timecontrols()``. Typically a time-invariant unit characteristic whose effect is allowed to move over calendar time.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "unit_covariates": {
          "description": "Controls interacted with the **unit** fixed effects, i.e. one slope per unit. Stata's ``unitcontrols()``. The canonical use is ``unit_covariates=[time_col]``, which makes Y(0) carry unit-specific linear trends. Identification bites hard here: a unit-specific slope needs at least two untreated periods for that unit, so early-treated cohorts can lose their imputation entirely.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "vce": {
          "default": "analytic",
          "description": "Standard-error mode for the overall ATT. ``'analytic'`` is the exact Borusyak--Jaravel--Spiess variance: the estimator is linear in the outcome, so its weights are computed rather than approximated, and the result reproduces Stata ``did_imputation`` and R ``didimputation`` to ~5e-8. Before v1.23.0 this path used an approximation that was materially anti-conservative (18-36% too small on the harness fixtures); see MIGRATION.md. Measured, not asserted. On a homogeneous-effect design with a never-treated half, 400-800 replications per cell: =========== ========== ==================== clusters coverage mean SE / sd(est) =========== ========== ==================== 30 0.925 0.985 60 0.938 0.936 120 0.948 0.978 240 0.935 1.007 480 **0.950** 0.983 =========== ========== ==================== Two things to read off it. The ratio sits at 1 throughout, so the variance formula carries no systematic bias -- the approximation it replaced ran 18-36% low. And coverage reaches nominal by 480 clusters, so the shortfall at 60 is a small-cluster effect rather than a missing term: there the standard error is simply *noisy* (dispersion 9.2% against 3.2% at 480), which fattens the studentised distribution (sd(t) = 1.04 against 1.007). A ``t(G-1)`` critical value barely helps (0.938 to 0.941 at G = 60) because the problem is the variability of the standard error, not the degrees of freedom. ``vce='bootstrap'`` is the remedy in small designs, and ``se_method='auto'`` selects it below 30 clusters. ``'bootstrap'`` resamples whole clusters and re-runs the full imputation estimator. Point estimates are identical either way; per-horizon event-study SEs are unaffected.",
          "enum": [
            "analytic",
            "bootstrap"
          ],
          "type": "string"
        },
        "weights": {
          "description": "Column of estimation weights ``omega_i`` (Stata ``[aw=]``, R ``didimputation(weights=)``). The untreated Y(0) model is fit by weighted least squares, every reported average is the ``omega``-weighted mean over the treated cells it covers, and the exact variance uses the weighted projection. Like ``weights=`` on the other staggered estimators this changes the *estimand*: the weighted ATT averages over the population the treated units represent, not over the units. Not combinable with ``project=``.",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable name.",
          "type": "string"
        }
      },
      "required": [
        "first_treat",
        "group",
        "time",
        "y"
      ],
      "type": "object"
    },
    "name": "borusyak_jaravel_spiess"
  },
  {
    "description": "User-friendly alias for :func:`sp.rd2d` (boundary discontinuity design). Validation: validated evidence tier (known-truth, reference, external-parity, or Monte Carlo artifact). Assumptions: Conditional expectations of potential outcomes are continuous at the cutoff; Units cannot precisely manipulate the running variable around the cutoff (no sorting); For fuzzy designs: monotonicity of treatment take-up at the cutoff. Pre-conditions: A continuous running/forcing variable with a known cutoff that (sharply or fuzzily) assigns treatment; Enough observations in a neighbourhood of the cutoff to fit a local polynomial. Failure modes: Density of the running variable jumps at the cutoff (manipulation / sorting) -> Run a McCrary / density test (rdplotdensity); if manipulation is present the design is invalid near the cutoff; Estimate swings with the bandwidth -- results are not robust -> Report a bandwidth-sensitivity curve and use a data-driven MSE-optimal bandwidth. Alternatives: sp.rdrobust, sp.rdrandinf, sp.rdbwselect. Typical minimum N: 500.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "approach": {
          "default": "location",
          "description": "approach parameter (str).",
          "type": "string"
        },
        "boundary": {
          "description": "boundary parameter (Optional[Callable]).",
          "type": "string"
        },
        "bwcheck": {
          "default": "auto",
          "description": "bwcheck parameter (Union[int, str, None]).",
          "type": "integer"
        },
        "bwparam": {
          "default": "main",
          "description": "bwparam parameter (str).",
          "type": "string"
        },
        "bwselect": {
          "default": "mserd",
          "description": "bwselect parameter (str).",
          "type": "string"
        },
        "cluster": {
          "description": "Cluster identifier column for clustered standard errors.",
          "type": "string"
        },
        "cqt": {
          "default": 0.5,
          "description": "cqt parameter (float).",
          "type": "number"
        },
        "deriv": {
          "default": [
            0,
            0
          ],
          "description": "deriv parameter (Tuple[int, int]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "distance": {
          "description": "distance parameter (Optional[Union[np.ndarray, List[str]]]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "eval_points": {
          "description": "eval_points parameter (Optional[np.ndarray]).",
          "type": "string"
        },
        "fitmethod": {
          "default": "joint",
          "description": "fitmethod parameter (str).",
          "type": "string"
        },
        "fuzzy": {
          "description": "fuzzy parameter (Optional[str]).",
          "type": "string"
        },
        "h": {
          "description": "Bandwidth used for local smoothing or kernel weighting.",
          "type": "number"
        },
        "kernel": {
          "default": "triangular",
          "description": "Kernel function used for weighting or smoothing.",
          "type": "string"
        },
        "kernel_type": {
          "default": "prod",
          "description": "kernel_type parameter (str).",
          "type": "string"
        },
        "kink_position": {
          "description": "kink_position parameter (Optional[Sequence]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "kink_unknown": {
          "default": false,
          "description": "kink_unknown parameter (Union[bool, Tuple[bool, bool]]).",
          "type": "boolean"
        },
        "masspoints": {
          "default": "check",
          "description": "masspoints parameter (str).",
          "type": "string"
        },
        "method": {
          "default": "dpi",
          "description": "Estimator or algorithm variant to use.",
          "type": "string"
        },
        "n_eval": {
          "default": 1,
          "description": "Number of eval.",
          "type": "integer"
        },
        "p": {
          "default": 1,
          "description": "p parameter (int).",
          "type": "integer"
        },
        "q": {
          "description": "Quantile level.",
          "type": "integer"
        },
        "scalebiascrct": {
          "default": 1.0,
          "description": "scalebiascrct parameter (float).",
          "type": "number"
        },
        "scaleregul": {
          "description": "scaleregul parameter (Optional[float]).",
          "type": "number"
        },
        "side": {
          "default": "two",
          "description": "side parameter (str).",
          "type": "string"
        },
        "stdvars": {
          "default": true,
          "description": "stdvars parameter (bool).",
          "type": "boolean"
        },
        "tangvec": {
          "description": "tangvec parameter (Optional[np.ndarray]).",
          "type": "string"
        },
        "treatment": {
          "description": "Treatment indicator, treatment variable, or treatment array.",
          "type": "string"
        },
        "vce": {
          "default": "hc1",
          "description": "vce parameter (str).",
          "type": "string"
        },
        "weights": {
          "description": "Observation weights.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "x1": {
          "description": "x1 parameter (str).",
          "type": "string"
        },
        "x2": {
          "description": "x2 parameter (str).",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "treatment",
        "x1",
        "x2",
        "y"
      ],
      "type": "object"
    },
    "name": "boundary_rd"
  },
  {
    "description": "Masten-Poirier (2021) breakdown frontier for qualitative conclusions. Validation: validated evidence tier (known-truth, reference, external-parity, or Monte Carlo artifact). Assumptions: Only weak (set-identifying) assumptions are imposed; the result is an interval, not a point; Lee bounds add monotonicity of selection; Oster's delta adds proportional selection on observed vs. unobserved. Pre-conditions: The data needed for the point-identifying analysis, plus the weakest credible identifying restriction; For Lee bounds: a binary selection/attrition indicator. Failure modes: Bounds are too wide to be informative -> Add a credible auxiliary restriction (monotone treatment response, instrument) to tighten the bounds. Alternatives: sp.oster_delta, sp.lee_bounds, sp.manski_bounds. Typical minimum N: 100.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "assumption": {
          "default": "parallel_trends",
          "description": "Label for the identifying assumption being relaxed. Currently supports a generic linear violation model applicable to ``'parallel_trends'``, ``'exclusion_restriction'``, or ``'selection_on_observables'``.",
          "type": "string"
        },
        "estimate": {
          "description": "Point estimate of the treatment effect.",
          "type": "number"
        },
        "max_violation": {
          "default": 0.1,
          "description": "Maximum magnitude of the assumption violation to explore.",
          "type": "number"
        },
        "n_grid": {
          "default": 100,
          "description": "Grid resolution for the frontier.",
          "type": "integer"
        },
        "se": {
          "description": "Standard error of the estimate.",
          "type": "number"
        }
      },
      "required": [
        "estimate",
        "se"
      ],
      "type": "object"
    },
    "name": "breakdown_frontier"
  },
  {
    "description": "Compute the breakdown value of M. Validation: certified parity evidence. Assumptions: Conditional parallel trends between treated and comparison groups absent treatment; No anticipation of treatment before its onset; Treatment effects may be heterogeneous across cohorts and time (no homogeneity required). Pre-conditions: Panel or repeated cross-section with a unit (or group) identifier and a time identifier; At least one never-treated or not-yet-treated comparison group; Pre-treatment periods to assess parallel trends. Failure modes: Pre-treatment event-study coefficients are jointly non-zero (pre-trend violation) -> Use honest DiD bounds to quantify robustness to trend violations, or condition on covariates; Two-way fixed-effects estimate is contaminated by 'forbidden' comparisons / negative weights -> Use a heterogeneity-robust estimator (Callaway-Sant'Anna, Borusyak et al., Gardner two-stage). Alternatives: sp.callaway_santanna, sp.did, sp.honest_did. Typical minimum N: 100.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "e": {
          "default": 0,
          "description": "Relative time period.",
          "type": "integer"
        },
        "method": {
          "default": "smoothness",
          "description": "The restriction whose confidence set is inverted; the same meaning as in :func:`honest_did`.",
          "enum": [
            "smoothness",
            "relative_magnitude"
          ],
          "type": "string"
        },
        "result": {
          "description": "DID result with event study.",
          "type": "string"
        }
      },
      "required": [
        "result"
      ],
      "type": "object"
    },
    "name": "breakdown_m"
  },
  {
    "description": "Unified dispatcher for six causal-inference bridging theorems (2025-2026): DiD==SC (Shi-Athey), EWM==CATE (Ferman), IPW==DR==CB (Zhao-Percival), Bunching==RDD (Lu-Wang-Xie), DR-via-Calibration (Zhang), Long-term-surrogate==PCI (Imbens-Kallus-Mao-Wang). Reports both path estimates + doubly-robust recommendation. Assumptions: The two estimation paths being bridged target the same causal estimand under the chosen bridging theorem (e.g., DID = synthetic control for kind='did_sc'); Each path's own identification holds (parallel trends / SC convex-hull / proximal completeness, depending on kind), so a non-rejected agreement test is meaningful; The doubly-robust combined estimate is consistent if at least one of the two bridged paths is correctly specified. Pre-conditions: kind is one of the supported bridges and the matching per-bridge kwargs are supplied; Panel/data shape matches the chosen bridge (e.g., did_sc needs unit, time, treated_unit, treatment_time); Both paths are estimable on the same sample so the agreement test is well-defined. Failure modes: Agreement test rejects: the two paths give materially different estimates -> Treat divergence as evidence one bridging assumption fa...",
    "input_schema": {
      "properties": {
        "kind": {
          "description": "Which bridge to invoke",
          "enum": [
            "did_sc",
            "ewm_cate",
            "cb_ipw",
            "kink_rdd",
            "dr_calib",
            "surrogate_pci"
          ],
          "type": "string"
        }
      },
      "required": [
        "kind"
      ],
      "type": "object"
    },
    "name": "bridge"
  },
  {
    "description": "One-line agent-friendly brief for a fitted result. Cheaper than calling brief_result if you already have the result object in scope.",
    "input_schema": {
      "properties": {
        "result_id": {
          "type": "string"
        }
      },
      "required": [],
      "type": "object"
    },
    "name": "brief"
  },
  {
    "description": "Return the one-line agent-friendly brief for a fitted result. Uses sp.brief(). Useful when an agent wants to summarise a chained workflow without paying for the full JSON payload again.",
    "input_schema": {
      "properties": {
        "result_id": {
          "description": "Handle to a previously-fitted result.",
          "type": "string"
        }
      },
      "required": [
        "result_id"
      ],
      "type": "object"
    },
    "name": "brief_result"
  },
  {
    "description": "Estimate bunching at a policy threshold. Validation: validated evidence tier (known-truth, reference, external-parity, or Monte Carlo artifact). Assumptions: The counterfactual density would be smooth through the threshold absent the policy; Excess mass at the threshold reflects the behavioural elasticity of interest; No other discontinuity coincides with the threshold. Pre-conditions: A behavioural choice variable (earnings, hours, ...) with a known kink or notch in the budget/choice set; A visible empirical density of the running variable around the threshold. Failure modes: Round-number heaping or a coincident policy contaminates the bunching mass -> Exclude heaping points, widen the excluded region, and test the counterfactual polynomial order. Alternatives: sp.rdrobust, sp.rkd. Typical minimum N: 500.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level.",
          "type": "number"
        },
        "bin_width": {
          "description": "Width of bins. If None, computed from data range / n_bins.",
          "type": "number"
        },
        "bunch_region": {
          "description": "(lower, upper) bounds of the bunching region. If None, uses [threshold - 2*bin_width, threshold + 2*bin_width].",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "design": {
          "default": "kink",
          "description": "'kink' or 'notch'.",
          "type": "string"
        },
        "dt": {
          "description": "Change in marginal tax rate at the kink (for elasticity). E.g., 0.10 for a 10pp increase.",
          "type": "number"
        },
        "exclude_region": {
          "description": "Same as bunch_region unless otherwise specified.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "n_bins": {
          "default": 50,
          "description": "Number of bins on each side of the threshold.",
          "type": "integer"
        },
        "n_bootstrap": {
          "default": 200,
          "description": "Bootstrap iterations for standard errors.",
          "type": "integer"
        },
        "poly_order": {
          "default": 7,
          "description": "Order of the counterfactual polynomial.",
          "type": "integer"
        },
        "random_state": {
          "default": 42,
          "description": "Random seed or RandomState for reproducible stochastic steps.",
          "type": "integer"
        },
        "running_var": {
          "description": "Name of the running variable (e.g., income).",
          "type": "string"
        },
        "threshold": {
          "description": "Policy threshold (kink/notch point).",
          "type": "number"
        }
      },
      "required": [
        "running_var",
        "threshold"
      ],
      "type": "object"
    },
    "name": "bunching"
  },
  {
    "description": "Bayesian VAR with the original Minnesota (Litterman) prior. Validation: validated evidence tier (known-truth, reference, external-parity, or Monte Carlo artifact).",
    "input_schema": {
      "properties": {
        "lags": {
          "default": 4,
          "description": "lags parameter (int).",
          "type": "integer"
        },
        "lambda1": {
          "default": 0.1,
          "description": "Overall (self) tightness (smaller = stronger shrinkage toward RW).",
          "type": "number"
        },
        "lambda2": {
          "default": 0.5,
          "description": "Cross-variable tightness relative to own lags.",
          "type": "number"
        },
        "lambda3": {
          "default": 1.0,
          "description": "Lag decay.",
          "type": "number"
        },
        "lambda4": {
          "default": 100.0,
          "description": "Tightness of the constant (exogenous) terms.",
          "type": "number"
        },
        "sigma": {
          "default": "ar",
          "description": "``Sigma0``: diagonal of the univariate AR(lags) residual variances (Litterman's original; Stata ``arcov``), or the diagonal of the OLS VAR residual variances ``diag(U'U) / n`` (Stata ``varcov``). The prior-variance scales ``s_i^2`` follow the same choice.",
          "enum": [
            "ar",
            "var"
          ],
          "type": "string"
        }
      },
      "required": [],
      "type": "object"
    },
    "name": "bvar"
  },
  {
    "description": "Rescale CATE predictions by their best-linear-predictor calibration. Validation: validated evidence tier (known-truth, reference, external-parity, or Monte Carlo artifact).",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Level for the reported slope confidence intervals.",
          "type": "number"
        },
        "covariates": {
          "default": "none",
          "description": "Imputation regression only; see :func:`average_treatment_effect`.",
          "type": "string"
        },
        "forest": {
          "description": "A forest fitted with the GRF engine (``split_rule=\"grf\"``).",
          "type": "string"
        },
        "method": {
          "default": "auto",
          "description": "Forests with fixed effects only; see :func:`calibration_test`.",
          "enum": [
            "auto",
            "imputation",
            "within"
          ],
          "type": "string"
        },
        "newdata": {
          "description": "Effect-modifier rows to calibrate. ``None`` calibrates the out-of-bag predictions of the training rows. ``X=`` is accepted as an alias.",
          "type": "string"
        }
      },
      "required": [
        "forest"
      ],
      "type": "object"
    },
    "name": "calibrate_cate"
  },
  {
    "description": "Calibrate the strength of an unobserved confounder required to Validation: certified parity evidence.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Level of the adjusted confidence interval.",
          "type": "number"
        },
        "dof": {
          "description": "Residual degrees of freedom of the regression. Required: the bias bound scales with ``sqrtdof``.",
          "type": "number"
        },
        "estimate": {
          "description": "OLS coefficient of the treatment and its (classical) standard error.",
          "type": "number"
        },
        "multipliers": {
          "description": "Values of ``k``; default ``np.linspace(0.5, 5.0, 19)``. A ``k`` that implies ``R2_{D~Z|X} >= 1`` is impossible and its row is NaN with ``feasible = False`` (sensemakr stops with an error instead).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "observed_r2_outcome": {
          "description": "Partial R2 of the benchmark covariate with the outcome, given the treatment and the other covariates (sensemakr ``r2yxj.dx``).",
          "type": "number"
        },
        "observed_r2_treatment": {
          "description": "Partial R2 of the benchmark covariate with the treatment, given the other covariates (sensemakr ``r2dxj.x``).",
          "type": "number"
        },
        "se": {
          "description": "OLS coefficient of the treatment and its (classical) standard error.",
          "type": "number"
        },
        "target_estimate": {
          "default": 0.0,
          "description": "Effect value to explain away.",
          "type": "number"
        }
      },
      "required": [
        "estimate",
        "observed_r2_outcome",
        "observed_r2_treatment",
        "se"
      ],
      "type": "object"
    },
    "name": "calibrate_confounding_strength"
  },
  {
    "description": "Best-linear-predictor calibration test of CATEs [@chernozhukov2025generic]. Validation: certified parity evidence. Assumptions: Unconfoundedness given the covariates; Overlap / positivity; Honesty: separate subsamples are used to choose splits and to estimate effects. Pre-conditions: Covariates, treatment, and outcome with enough data to grow an honest forest. Failure modes: Calibration test rejects -- the forest's heterogeneity is not well calibrated -> Increase the sample / number of trees, or fall back to a doubly-robust learner. Alternatives: sp.dml, sp.auto_cate, sp.tmle. Typical minimum N: 1000.",
    "input_schema": {
      "properties": {
        "T": {
          "description": "The training sample (defaults to the stored arrays).",
          "type": "string"
        },
        "X": {
          "description": "The training sample (defaults to the stored arrays).",
          "type": "string"
        },
        "Y": {
          "description": "The training sample (defaults to the stored arrays).",
          "type": "string"
        },
        "alpha": {
          "default": 0.05,
          "description": "Level for ``ci_low`` / ``ci_high``.",
          "type": "number"
        },
        "covariates": {
          "default": "none",
          "description": "Covariates in the untreated outcome model of the imputation regression (see :func:`average_treatment_effect`).",
          "type": "string"
        },
        "forest": {
          "description": "forest parameter ('CausalForest').",
          "type": "string"
        },
        "method": {
          "default": "auto",
          "description": "Forests with fixed effects only: ``'auto'`` is ``'imputation'`` for ``fe='twoway'`` with a binary treatment and ``'within'`` otherwise. Other forests accept only ``'auto'``.",
          "enum": [
            "auto",
            "imputation",
            "within"
          ],
          "type": "string"
        },
        "vce": {
          "default": "HC3",
          "description": "Heteroskedasticity-robust covariance (``grf``'s ``vcov.type``).",
          "enum": [
            "HC3",
            "HC2",
            "HC1",
            "HC0"
          ],
          "type": "string"
        }
      },
      "required": [
        "forest"
      ],
      "type": "object"
    },
    "name": "calibration_test"
  },
  {
    "description": "California Proposition 99 tobacco control dataset.",
    "input_schema": {
      "properties": {},
      "required": [],
      "type": "object"
    },
    "name": "california_prop99"
  },
  {
    "description": "California Proposition 99 tobacco dataset (simulated, extended).",
    "input_schema": {
      "properties": {},
      "required": [],
      "type": "object"
    },
    "name": "california_tobacco"
  },
  {
    "description": "Staggered DID (Callaway-Sant'Anna 2021): group-time ATT with doubly-robust, IPW, or regression-adjusted estimators. Robust to heterogeneous treatment effects where TWFE fails. Requires a cohort column g (first-treatment period; 0 = never-treated). Validation: certified evidence with scoped limitations. Known limitations: clustervars is not yet supported with bstrap=False; the analytic standard errors do not account for within-cluster dependence, so the multiplier bootstrap is required.",
    "input_schema": {
      "properties": {
        "control_group": {
          "default": "nevertreated",
          "enum": [
            "nevertreated",
            "notyettreated"
          ],
          "type": "string"
        },
        "estimator": {
          "default": "dr",
          "enum": [
            "dr",
            "ipw",
            "reg"
          ],
          "type": "string"
        },
        "g": {
          "description": "First-treatment-period cohort column (0 for never-treated).",
          "type": "string"
        },
        "i": {
          "description": "Unit ID column",
          "type": "string"
        },
        "t": {
          "description": "Time column",
          "type": "string"
        },
        "y": {
          "type": "string"
        }
      },
      "required": [
        "y",
        "g",
        "t",
        "i"
      ],
      "type": "object"
    },
    "name": "callaway_santanna"
  },
  {
    "description": "Cardinality matching -- maximise the number of matched pairs subject Validation: validated evidence tier (known-truth, reference, external-parity, or Monte Carlo artifact). Assumptions: Unconfoundedness: treatment is as-good-as-random given the measured covariates; Overlap / common support: every unit has a non-degenerate probability of each treatment; The covariate set blocks all back-door paths. Pre-conditions: Pre-treatment covariates measured for treated and control units; A binary (or low-cardinality) treatment indicator; Sufficient covariate overlap between treatment arms. Failure modes: Poor overlap -- extreme propensity scores or few acceptable matches -> Trim or restrict to the common-support region and report the discarded units; Covariate imbalance remains after matching/weighting -> Re-specify the balancing model (CBPS, entropy balancing) and re-check standardized mean differences. Alternatives: sp.propensity_score, sp.cbps, sp.ebalance, sp.dml. Typical minimum N: 200.",
    "input_schema": {
      "properties": {
        "covariates": {
          "description": "Covariate matrix, DataFrame, or column names.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "outcome": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        },
        "smd_tolerance": {
          "default": 0.1,
          "description": "smd_tolerance parameter (float).",
          "type": "number"
        },
        "time_limit": {
          "default": 30.0,
          "description": "time_limit parameter (float).",
          "type": "number"
        },
        "treatment": {
          "description": "Treatment indicator, treatment variable, or treatment array.",
          "type": "string"
        }
      },
      "required": [
        "covariates",
        "outcome",
        "treatment"
      ],
      "type": "object"
    },
    "name": "cardinality_match"
  },
  {
    "description": "Group-level average treatment effects. Assumptions: Unconfoundedness given the covariates; Overlap / positivity across the covariate space; Nuisance functions are estimated consistently; cross-fitting controls overfitting bias. Pre-conditions: Covariates, a treatment indicator, and an outcome for each unit; Enough data to fit flexible nuisance models with sample-splitting / cross-fitting. Failure modes: CATE estimates are unstable or extrapolate beyond the covariate support -> Restrict to the overlap region, increase data, or use a doubly-robust learner (DR-/R-learner). Alternatives: sp.dml, sp.causal_forest, sp.tmle. Typical minimum N: 500.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for CIs.",
          "type": "number"
        },
        "by": {
          "description": "Column name to group by, or 'cate' to group by CATE quartiles.",
          "type": "string"
        },
        "n_groups": {
          "default": 4,
          "description": "Number of quantile groups when ``by='cate'`` or when the grouping variable is continuous.",
          "type": "integer"
        },
        "result": {
          "description": "Result from ``metalearner()`` / ``tarnet()``, a fitted ``causal_forest()`` model, or a raw array of per-unit CATE estimates.",
          "type": "string"
        }
      },
      "required": [
        "by",
        "result"
      ],
      "type": "object"
    },
    "name": "cate_by_group"
  },
  {
    "description": "Evaluate any CATE estimator via RATE / AUTOC / Qini (Yadlowsky 2025). Validation: certified parity evidence. Assumptions: Unconfoundedness given the covariates; Overlap / positivity across the covariate space; Nuisance functions are estimated consistently; cross-fitting controls overfitting bias. Pre-conditions: Covariates, a treatment indicator, and an outcome for each unit; Enough data to fit flexible nuisance models with sample-splitting / cross-fitting. Failure modes: CATE estimates are unstable or extrapolate beyond the covariate support -> Restrict to the overlap region, increase data, or use a doubly-robust learner (DR-/R-learner). Alternatives: sp.dml, sp.causal_forest, sp.tmle. Typical minimum N: 500.",
    "input_schema": {
      "properties": {
        "T": {
          "description": "Observed outcome and binary treatment.",
          "type": "string"
        },
        "X": {
          "description": "Required if any of ``e_hat / m_hat / mu1_hat / mu0_hat`` is None; cross-fit nuisances are estimated with GBM defaults.",
          "type": "string"
        },
        "Y": {
          "description": "Observed outcome and binary treatment.",
          "type": "string"
        },
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "cate": {
          "description": "Estimated CATE :math:`\\hat\\tau(X_i)` from any estimator.",
          "type": "string"
        },
        "clip": {
          "default": 0.02,
          "description": "Propensity clip.",
          "type": "number"
        },
        "e_hat": {
          "description": "Pre-computed nuisance predictions (e.g., from the same estimator that produced ``cate``). If provided, no internal cross-fitting runs.",
          "type": "string"
        },
        "m_hat": {
          "description": "Pre-computed nuisance predictions (e.g., from the same estimator that produced ``cate``). If provided, no internal cross-fitting runs.",
          "type": "string"
        },
        "mu0_hat": {
          "description": "Pre-computed nuisance predictions (e.g., from the same estimator that produced ``cate``). If provided, no internal cross-fitting runs.",
          "type": "string"
        },
        "mu1_hat": {
          "description": "Pre-computed nuisance predictions (e.g., from the same estimator that produced ``cate``). If provided, no internal cross-fitting runs.",
          "type": "string"
        },
        "n_folds": {
          "default": 5,
          "description": "Number of cross-fitting or cross-validation folds.",
          "type": "integer"
        },
        "q_grid": {
          "default": 100,
          "description": "Grid of q values to evaluate.",
          "type": "integer"
        },
        "random_state": {
          "default": 0,
          "description": "Random seed or RandomState for reproducible stochastic steps.",
          "type": "integer"
        },
        "target": {
          "default": "AUTOC",
          "description": "Which scalar headline to emit; both are computed and returned.",
          "enum": [
            "AUTOC",
            "QINI"
          ],
          "type": "string"
        }
      },
      "required": [
        "T",
        "Y",
        "cate"
      ],
      "type": "object"
    },
    "name": "cate_eval"
  },
  {
    "description": "Plot group-level CATEs with confidence intervals.",
    "input_schema": {
      "properties": {
        "ax": {
          "description": "ax parameter.",
          "type": "string"
        },
        "color": {
          "default": "#2C3E50",
          "description": "color parameter (str).",
          "type": "string"
        },
        "figsize": {
          "default": [
            8,
            5
          ],
          "description": "figsize parameter (tuple).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "group_df": {
          "description": "Output from ``cate_by_group()``.",
          "type": "string"
        },
        "title": {
          "description": "title parameter (Optional[str]).",
          "type": "string"
        }
      },
      "required": [
        "group_df"
      ],
      "type": "object"
    },
    "name": "cate_group_plot"
  },
  {
    "description": "Plot the CATE distribution.",
    "input_schema": {
      "properties": {
        "ax": {
          "description": "ax parameter.",
          "type": "string"
        },
        "color": {
          "default": "#2C3E50",
          "description": "color parameter (str).",
          "type": "string"
        },
        "figsize": {
          "default": [
            8,
            5
          ],
          "description": "figsize parameter (tuple).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "kind": {
          "default": "hist",
          "description": "'hist' for histogram, 'kde' for kernel density, 'both'.",
          "type": "string"
        },
        "result": {
          "description": "Result from ``metalearner()``.",
          "type": "string"
        },
        "title": {
          "description": "title parameter (Optional[str]).",
          "type": "string"
        }
      },
      "required": [
        "result"
      ],
      "type": "object"
    },
    "name": "cate_plot"
  },
  {
    "description": "Pre-trend test by predicted-effect group for causal forests with fixed effects: units sorted by their out-of-bag CATE, untreated cells regressed on unit and period effects and group x lead indicators, cluster-robust Wald tests that the leads are zero and equal across groups. Assumptions: Clusters (default: unit) are independent. Pre-conditions: forest fitted with fe='twoway' (or 'unit') and a binary absorbing treatment; treated units observed for at least `leads` periods before adoption. Alternatives: sp.did_forest, sp.pretrends_test, sp.bjs_pretrend_joint.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "covariates": {
          "default": "none",
          "description": "Covariate matrix, DataFrame, or column names.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "forest": {
          "description": "Fitted fe= forest.",
          "type": "string"
        },
        "groups": {
          "description": "Unit-constant labels per row.",
          "type": "string"
        },
        "leads": {
          "description": "Default: up to 4.",
          "type": "integer"
        },
        "n_groups": {
          "default": 2,
          "description": "Number of groups.",
          "type": "integer"
        },
        "time_effects": {
          "default": "common",
          "description": "time_effects parameter (str).",
          "enum": [
            "common",
            "by_group"
          ],
          "type": "string"
        }
      },
      "required": [
        "forest"
      ],
      "type": "object"
    },
    "name": "cate_pretrend_test"
  },
  {
    "description": "Descriptive statistics of the CATE distribution.",
    "input_schema": {
      "properties": {
        "result": {
          "description": "Result from ``metalearner()`` containing ``model_info['cate']``.",
          "type": "string"
        }
      },
      "required": [
        "result"
      ],
      "type": "object"
    },
    "name": "cate_summary"
  },
  {
    "description": "End-to-end causal workflow: diagnose -> recommend estimator -> fit -> run robustness -> return result. The one-shot entry point that lets an agent analyse a dataset in a single call without orchestrating stages itself.",
    "input_schema": {
      "properties": {
        "cohort": {
          "type": "string"
        },
        "covariates": {
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "design": {
          "enum": [
            "did",
            "rd",
            "iv",
            "observational",
            "panel",
            "rct"
          ],
          "type": "string"
        },
        "id": {
          "type": "string"
        },
        "instrument": {
          "type": "string"
        },
        "running_var": {
          "type": "string"
        },
        "time": {
          "type": "string"
        },
        "treatment": {
          "type": "string"
        },
        "y": {
          "type": "string"
        }
      },
      "required": [
        "y"
      ],
      "type": "object"
    },
    "name": "causal"
  },
  {
    "description": "Causal-discovery dispatcher -- article-facing alias.",
    "input_schema": {
      "properties": {
        "method": {
          "default": "notears",
          "description": "Estimator or algorithm variant to use.",
          "type": "string"
        },
        "variables": {
          "description": "variables parameter (Optional[List[str]]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "required": [],
      "type": "object"
    },
    "name": "causal_discovery"
  },
  {
    "description": "Causal deep Q-network (Li, Zhang, Bareinboim 2025, arXiv:2510.21110) for offline policy learning under unobserved confounding. Learns a confounding-robust Q-function via bootstrap data augmentation. Assumptions: Sequential ignorability: no unobserved confounders of actions and outcomes; Positivity: the behaviour policy explores all evaluated actions; The environment satisfies the assumed (Markov) dynamics. Pre-conditions: Logged trajectories (states, actions, rewards) from a known or estimable behaviour policy. Failure modes: Poor behaviour-policy coverage -- the target policy queries unseen state-action regions -> Use offline-safe / pessimistic methods and report effective sample size of the importance weights. Alternatives: sp.offline_safe_policy, sp.policy_value. Typical minimum N: 1000.",
    "input_schema": {
      "properties": {
        "action": {
          "description": "action parameter (str).",
          "type": "string"
        },
        "discount": {
          "default": 0.95,
          "description": "Discount factor",
          "type": "number"
        },
        "n_iter": {
          "default": 100,
          "description": "Fitted-Q iterations",
          "type": "integer"
        },
        "next_state": {
          "description": "Next-state column(s)",
          "type": "string"
        },
        "reward": {
          "description": "reward parameter (str).",
          "type": "string"
        },
        "state": {
          "description": "State column(s)",
          "type": "string"
        }
      },
      "required": [
        "action",
        "next_state",
        "reward",
        "state"
      ],
      "type": "object"
    },
    "name": "causal_dqn"
  },
  {
    "description": "Honest generalized random forest for conditional average treatment effects (CATE). Default engine reimplements the GRF algorithm: splits on the causal gradient pseudo-outcome, out-of-bag nuisances and predictions, little-bag variances, cluster-aware sampling and inference. fe='twoway' gives a causal forest with fixed effects for panels (unit and period effects removed within every node and leaf). Validation: certified evidence with scoped limitations. Known limitations: The AIPW ATE/ATT are validated against grf on clean-overlap designs only; under severe propensity-overlap loss the AIPW influence function inflates the standard error (conservative, over-covering inference), so inspect the sp.audit overlap diagnostic before interpreting the ATE on that kind of sample; The forest is compared with grf only statistically (tier T3: RMSE, pointwise variances, coverage), not bit-for-bit; given a fitted forest, the inference operators match grf / sandwich to 1e-14; Doubly-robust averages (average_treatment_effect, best_linear_projection, rate) are not implemented for fe= forests, which have no propensity score; use sp.did_forest for group-time averages; For fe= forests the calibration slo...",
    "input_schema": {
      "properties": {
        "W_hat": {
          "description": "Precomputed out-of-sample E[T | X, W] (skips the treatment nuisance forest).",
          "type": "string"
        },
        "Y_hat": {
          "description": "Precomputed out-of-sample E[Y | X, W] (skips the outcome nuisance forest).",
          "type": "string"
        },
        "clusters": {
          "description": "Cluster ids or column name; sampling, honesty, cross-fitting and standard errors respect clusters. Always set for repeated observations of a unit.",
          "type": "string"
        },
        "d": {
          "description": "Treatment column.",
          "type": "string"
        },
        "fe": {
          "description": "Panel fixed effects removed within nodes and leaves.",
          "enum": [
            "unit",
            "twoway"
          ],
          "type": "string"
        },
        "formula": {
          "description": "'y ~ treatment | x1 + x2 [| w1 + w2]' (pipe separates effect modifiers and controls). Alternatively use y/d/x/w column names or Y/T/X/W arrays.",
          "type": "string"
        },
        "id": {
          "description": "Unit id column (fe=); unit= is an alias.",
          "type": "string"
        },
        "max_samples": {
          "default": 0.5,
          "description": "Share of clusters per tree.",
          "type": "number"
        },
        "min_samples_leaf": {
          "default": 5,
          "description": "GRF min.node.size.",
          "type": "integer"
        },
        "n_estimators": {
          "default": 2000,
          "description": "Number of trees (``n_trees`` is accepted as an alias).",
          "type": "integer"
        },
        "random_state": {
          "description": "Random seed or RandomState for reproducible stochastic steps.",
          "type": "integer"
        },
        "split_rule": {
          "default": "grf",
          "description": "'cffe' (fe= only) uses the tau-heterogeneity criterion of Kattenberg, Scheer and Thiel instead of the GRF gradient; 'legacy' reproduces the pre-1.29 estimator (deprecated).",
          "enum": [
            "grf",
            "cffe",
            "legacy"
          ],
          "type": "string"
        },
        "time": {
          "description": "Period column (fe='twoway').",
          "type": "string"
        },
        "w": {
          "description": "Control columns (nuisances only).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "x": {
          "description": "Effect-modifier columns.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Outcome column.",
          "type": "string"
        }
      },
      "required": [],
      "type": "object"
    },
    "name": "causal_forest"
  },
  {
    "description": "Causal impact of an intervention on a time series: pre-period OLS on the covariate series with an AR(1) latent state (Kalman filter), post-period counterfactual forecasts and frequentist prediction intervals for pointwise, average and cumulative effects. Not the Bayesian structural time-series model of R CausalImpact. Validation: validated evidence tier (known-truth, reference, external-parity, or Monte Carlo artifact). Assumptions: No simultaneous shocks affect treated and control series differently at intervention; Pre-period relationship extrapolates into the post-period absent treatment. Pre-conditions: Observed time series has a clearly defined intervention date; Pre-intervention period is long enough to fit the counterfactual model. Failure modes: Poor pre-period fit or unstable posterior predictive interval -> Add controls, lengthen the pre-period, or use synthetic control as a robustness check. Alternatives: sp.synth, sp.sequential_sdid, sp.local_projections. Typical minimum N: 30.",
    "input_schema": {
      "properties": {
        "intervention_time": {
          "description": "Date/index of intervention",
          "type": "string"
        },
        "time": {
          "description": "Time / date column",
          "type": "string"
        },
        "y": {
          "description": "Outcome time-series column",
          "type": "string"
        }
      },
      "required": [
        "intervention_time",
        "time",
        "y"
      ],
      "type": "object"
    },
    "name": "causal_impact"
  },
  {
    "description": "Generate a synthetic causal-RL benchmark dataset. Assumptions: Sequential ignorability: no unobserved confounders of actions and outcomes; Positivity: the behaviour policy explores all evaluated actions; The environment satisfies the assumed (Markov) dynamics. Pre-conditions: Logged trajectories (states, actions, rewards) from a known or estimable behaviour policy. Failure modes: Poor behaviour-policy coverage -- the target policy queries unseen state-action regions -> Use offline-safe / pessimistic methods and report effective sample size of the importance weights. Alternatives: sp.offline_safe_policy, sp.policy_value. Typical minimum N: 1000.",
    "input_schema": {
      "properties": {
        "confounding_strength": {
          "default": 0.5,
          "description": "Magnitude of unmeasured confounding U -> (action, reward).",
          "type": "number"
        },
        "n_episodes": {
          "default": 1000,
          "description": "Number of episodes.",
          "type": "integer"
        },
        "name": {
          "default": "confounded_bandit",
          "description": "'confounded_targeting', 'confounded_routing'}",
          "type": "string"
        },
        "seed": {
          "default": 0,
          "description": "Random seed for reproducible stochastic steps.",
          "type": "integer"
        }
      },
      "required": [],
      "type": "object"
    },
    "name": "causal_rl_benchmark"
  },
  {
    "description": "Causal survival forest: heterogeneous effects on censored survival.",
    "input_schema": {
      "properties": {
        "W_hat": {
          "description": "Precomputed propensities; default out-of-bag regression forest.",
          "type": "string"
        },
        "alpha": {
          "default": 0.05,
          "description": "Significance level of the reported interval.",
          "type": "number"
        },
        "ci_group_size": {
          "default": 2,
          "description": "Forest options (grf names).",
          "type": "integer"
        },
        "clusters": {
          "description": "As in :func:`statspai.causal_forest`.",
          "type": "string"
        },
        "covariates": {
          "description": "Covariates ``X`` (confounders and effect modifiers).",
          "type": "string"
        },
        "equalize_cluster_weights": {
          "default": false,
          "description": "As in :func:`statspai.causal_forest`.",
          "type": "boolean"
        },
        "event": {
          "description": "1 = event observed, 0 = censored.",
          "type": "string"
        },
        "failure_times": {
          "description": "Grid for the survival nuisance forests (default: observed times).",
          "type": "string"
        },
        "honest": {
          "default": true,
          "description": "honest parameter (bool).",
          "type": "boolean"
        },
        "honesty_fraction": {
          "default": 0.5,
          "description": "honesty_fraction parameter (float).",
          "type": "number"
        },
        "honesty_prune_leaves": {
          "default": true,
          "description": "honesty_prune_leaves parameter (bool).",
          "type": "boolean"
        },
        "horizon": {
          "description": "``h`` of the estimand. Defaults to the 80th percentile of observed event times (recorded in ``detail``); grf requires it -- choose it from the study design.",
          "type": "number"
        },
        "imbalance_penalty": {
          "default": 0.0,
          "description": "Forest options (grf names).",
          "type": "number"
        },
        "max_depth": {
          "description": "Forest options (grf names).",
          "type": "integer"
        },
        "max_samples": {
          "default": 0.5,
          "description": "max_samples parameter (float).",
          "type": "number"
        },
        "min_samples_leaf": {
          "default": 5,
          "description": "min_samples_leaf parameter (int).",
          "type": "integer"
        },
        "mtry": {
          "description": "mtry parameter (Optional[int]).",
          "type": "integer"
        },
        "n_estimators": {
          "default": 2000,
          "description": "Number of estimators.",
          "type": "integer"
        },
        "n_jobs": {
          "default": 1,
          "description": "Number of jobs.",
          "type": "integer"
        },
        "propensity_bounds": {
          "description": "Clip ``e`` in the average-effect score denominators (off by default, as in grf).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "random_state": {
          "default": 42,
          "description": "Random seed or RandomState for reproducible stochastic steps.",
          "type": "integer"
        },
        "split_alpha": {
          "default": 0.05,
          "description": "grf ``alpha``; each child must also hold ``split_alpha`` of the parent's size in failures.",
          "type": "number"
        },
        "stabilize_splits": {
          "default": true,
          "description": "Forest options (grf names).",
          "type": "boolean"
        },
        "target": {
          "default": "RMST",
          "description": "target parameter (str).",
          "enum": [
            "RMST",
            "survival_probability"
          ],
          "type": "string"
        },
        "time": {
          "description": "Observed time ``min(T, C)``, non-negative.",
          "type": "string"
        },
        "treat": {
          "description": "Binary treatment.",
          "type": "string"
        },
        "weights": {
          "description": "As in :func:`statspai.causal_forest`.",
          "type": "string"
        }
      },
      "required": [],
      "type": "object"
    },
    "name": "causal_survival"
  },
  {
    "description": "Causal survival forest (Cui et al. 2023; grf::causal_survival_forest): heterogeneous effects on right-censored outcomes -- RMST or survival-probability difference at a horizon -- under unconfoundedness and conditionally independent censoring. Nuisance survival and censoring curves come from out-of-bag survival forests on [X, W]; the censoring-robust scores follow grf's discretisation exactly (T2 on the score map), with doubly-robust averages and BLPs. Validation: validated evidence tier (known-truth, reference, external-parity, or Monte Carlo artifact). Assumptions: unconfoundedness given X; censoring independent of T given (X, W); positivity of treatment and of remaining uncensored up to the horizon. Pre-conditions: binary treatment; events before the horizon; censoring survival positive up to the horizon. Failure modes: DataInsufficient: censoring survival ... is zero -> Choose a shorter horizon; MethodIncompatibility: treat must be binary -> Encode a binary treatment. Alternatives: sp.survival_forest, sp.cox, sp.ltmle_survival. Typical minimum N: 1000.",
    "input_schema": {
      "properties": {
        "W_hat": {
          "description": "Precomputed propensities; default out-of-bag regression forest.",
          "type": "string"
        },
        "alpha": {
          "default": 0.05,
          "description": "Significance level of the reported interval.",
          "type": "number"
        },
        "ci_group_size": {
          "default": 2,
          "description": "Trees per little bag; 1 disables variance estimates.",
          "type": "integer"
        },
        "clusters": {
          "description": "Cluster ids: trees sample whole clusters; SEs are cluster-robust.",
          "type": "string"
        },
        "covariates": {
          "description": "Covariates ``X`` (confounders and effect modifiers).",
          "type": "string"
        },
        "equalize_cluster_weights": {
          "default": false,
          "description": "Give every cluster equal weight (incompatible with weights).",
          "type": "boolean"
        },
        "event": {
          "description": "1 = event observed, 0 = censored.",
          "type": "string"
        },
        "failure_times": {
          "description": "Grid for the survival nuisance forests (default: observed times).",
          "type": "string"
        },
        "honest": {
          "default": true,
          "description": "grf honesty: grow on one half of the drawn sample, estimate leaves on the other.",
          "type": "boolean"
        },
        "honesty_fraction": {
          "default": 0.5,
          "description": "Share of the drawn sample used to grow the tree.",
          "type": "number"
        },
        "honesty_prune_leaves": {
          "default": true,
          "description": "Prune leaves left empty by the estimation half.",
          "type": "boolean"
        },
        "horizon": {
          "description": "``h`` of the estimand. Defaults to the 80th percentile of observed event times (recorded in ``detail``); grf requires it -- choose it from the study design.",
          "type": "string"
        },
        "imbalance_penalty": {
          "default": 0.0,
          "description": "grf imbalance.penalty on unbalanced splits.",
          "type": "number"
        },
        "max_depth": {
          "description": "Optional depth cap (no grf analogue).",
          "type": "string"
        },
        "max_samples": {
          "default": 0.5,
          "description": "Fraction of clusters drawn per tree (grf sample.fraction); at most 0.5 when ci_group_size > 1.",
          "type": "number"
        },
        "min_samples_leaf": {
          "default": 5,
          "description": "grf min.node.size: nodes with at most this many growing samples are not split.",
          "type": "integer"
        },
        "mtry": {
          "description": "Candidate variables per split (grf default min(ceil(sqrt(p) + 20), p)).",
          "type": "string"
        },
        "n_estimators": {
          "default": 2000,
          "description": "Trees (grf num.trees).",
          "type": "integer"
        },
        "n_jobs": {
          "default": 1,
          "description": "Threads used to grow trees (-1 = all cores).",
          "type": "integer"
        },
        "propensity_bounds": {
          "description": "Clip ``e`` in the average-effect score denominators (off by default, as in grf).",
          "type": "string"
        },
        "random_state": {
          "default": 42,
          "description": "Seed; different seeds give independent forests.",
          "type": "integer"
        },
        "split_alpha": {
          "default": 0.05,
          "description": "grf ``alpha``; each child must also hold ``split_alpha`` of the parent's size in failures.",
          "type": "number"
        },
        "stabilize_splits": {
          "default": true,
          "description": "Apply the treatment/instrument split constraints (grf stabilize.splits).",
          "type": "boolean"
        },
        "target": {
          "default": "RMST",
          "description": "Estimand: 'RMST' or 'survival_probability'.",
          "type": "string"
        },
        "time": {
          "description": "Observed time ``min(T, C)``, non-negative.",
          "type": "string"
        },
        "treat": {
          "description": "Binary treatment.",
          "type": "string"
        },
        "weights": {
          "description": "Sample weights (grf sample.weights).",
          "type": "string"
        }
      },
      "required": [],
      "type": "object"
    },
    "name": "causal_survival_forest"
  },
  {
    "description": "Covariate-Balancing Propensity Score estimator (Imai-Ratkovic 2014). Validation: certified parity evidence. Assumptions: Unconfoundedness: treatment is as-good-as-random given the measured covariates; Overlap / common support: every unit has a non-degenerate probability of each treatment; The covariate set blocks all back-door paths. Pre-conditions: Pre-treatment covariates measured for treated and control units; A binary (or low-cardinality) treatment indicator; Sufficient covariate overlap between treatment arms. Failure modes: Poor overlap -- extreme propensity scores or few acceptable matches -> Trim or restrict to the common-support region and report the discarded units; Covariate imbalance remains after matching/weighting -> Re-specify the balancing model (CBPS, entropy balancing) and re-check standardized mean differences. Alternatives: sp.propensity_score, sp.cbps, sp.ebalance, sp.dml. Typical minimum N: 200.",
    "input_schema": {
      "properties": {
        "add_intercept": {
          "default": true,
          "description": "Prepend a constant to the covariate matrix.",
          "type": "boolean"
        },
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "covariates": {
          "description": "Covariates entering the logit score.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "estimand": {
          "default": "ATE",
          "description": "estimand parameter (Literal['ATE', 'ATT']).",
          "enum": [
            "ATE",
            "ATT"
          ],
          "type": "string"
        },
        "n_bootstrap": {
          "default": 500,
          "description": "Number of bootstrap replications.",
          "type": "integer"
        },
        "seed": {
          "description": "Random seed for reproducible stochastic steps.",
          "type": "integer"
        },
        "treat": {
          "description": "Binary 0/1 treatment column.",
          "type": "string"
        },
        "trim": {
          "default": 0.0,
          "description": "Optional pscore clip for stability.",
          "type": "number"
        },
        "variant": {
          "default": "over",
          "description": "'exact': just-identified CBPS (only balance moments). 'over': over-identified CBPS (MLE + balance, solved via two-step GMM).",
          "enum": [
            "exact",
            "over"
          ],
          "type": "string"
        },
        "y": {
          "description": "Outcome column.",
          "type": "string"
        }
      },
      "required": [
        "covariates",
        "treat",
        "y"
      ],
      "type": "object"
    },
    "name": "cbps"
  },
  {
    "description": "Chernozhukov-Fernandez-Val-Melly (2013) counterfactual decomposition. Validation: certified parity evidence.",
    "input_schema": {
      "properties": {
        "group": {
          "description": "Group or cohort identifier.",
          "type": "string"
        },
        "inversion": {
          "default": "interpolate",
          "description": "How the estimated CDF, known only at the thresholds, is inverted. ``'interpolate'`` interpolates linearly between thresholds (a smoothed quantile). ``'step'`` returns the first threshold at which the CDF exceeds ``tau`` -- the step-function inverse of Chernozhukov, Fernandez-Val & Melly's Stata ``cdeco``; with the same thresholds it reproduces ``cdeco, method(logit)``.",
          "enum": [
            "interpolate",
            "step"
          ],
          "type": "string"
        },
        "ks_test": {
          "default": true,
          "description": "ks_test parameter (bool).",
          "type": "boolean"
        },
        "n_thresh": {
          "default": 40,
          "description": "(ignored when ``thresholds`` is given).",
          "type": "integer"
        },
        "reference": {
          "default": 0,
          "description": "Same convention as ``machado_mata`` / ``melly_decompose``: ``reference=0`` builds the counterfactual from A's distribution regression coefficients applied to B's X (F_{Y<0|1>}), opposite to the reweighting convention in ``dfl_decompose``.",
          "enum": [
            "0",
            "1"
          ],
          "type": "integer"
        },
        "tau_grid": {
          "description": "Grid of tau values to evaluate.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "thresholds": {
          "description": "Explicit distribution-regression thresholds, shared by both groups.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "x": {
          "description": "Primary running variable, regressor, or feature input for this estimator.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "group",
        "x",
        "y"
      ],
      "type": "object"
    },
    "name": "cfm_decompose"
  },
  {
    "description": "Callaway, Goodman-Bacon & Sant'Anna (2024) DiD with a CONTINUOUS treatment. A dose has no single ATT: the TWFE coefficient averages the 0.2-dose and 0.8-dose comparisons with weights that can be negative. Reports ATT(d) and its derivative ACRT(d) -- the causal response at dose d, which is what a marginal-dose question asks -- from a B-spline regression of the outcome change on the dose. Validation: certified evidence with scoped limitations. Known limitations: standard errors come from the per-cell influence function; contdid routes its own through the pte aggregation layer, which is not implemented here; staggered designs aggregate cells with StatsPAI's own treated-count weights; only the per-cell estimator is pinned against the reference; the cck (nonparametric) dose estimator is not implemented. Assumptions: Parallel trends in the untreated potential outcome; Strong parallel trends for ATT(d) to be the effect of dose d; No anticipation. Pre-conditions: panel with a continuous dose and some zero-dose units; at least one period before each treated cohort. Failure modes: No zero-dose units in a cell -> ATT(d) is levelled against the zero-dose group. Use control_group='notyettreate...",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "cohort": {
          "description": "First-treatment period (0 = never treated)",
          "type": "string"
        },
        "control_group": {
          "default": "nevertreated",
          "description": "control_group parameter (str).",
          "enum": [
            "nevertreated",
            "notyettreated"
          ],
          "type": "string"
        },
        "curve_basis": {
          "default": "fitted",
          "description": "'fitted' evaluates the curves on the basis they were fitted on; 'reference' re-anchors to the dose grid to reproduce contdid 0.1.1's reported curves, which are a rescaled version of the fitted response",
          "enum": [
            "fitted",
            "reference"
          ],
          "type": "string"
        },
        "degree": {
          "default": 3,
          "description": "B-spline degree; degree=1 with no knots gives a constant ACRT, the 'effect per unit of dose' reading",
          "type": "integer"
        },
        "dose": {
          "description": "Treatment intensity (0 = untreated)",
          "type": "string"
        },
        "dose_grid": {
          "description": "Doses to report the curves at (default: 10th-99th pct)",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "knots": {
          "description": "Explicit interior knots",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "num_knots": {
          "default": 0,
          "description": "Interior knots at dose quantiles; more buys flexibility at the cost of variance and there is no auto-selector",
          "type": "integer"
        },
        "time": {
          "description": "Time period column.",
          "type": "string"
        },
        "unit": {
          "description": "Unit identifier column.",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "cohort",
        "dose",
        "time",
        "unit",
        "y"
      ],
      "type": "object"
    },
    "name": "cgs_continuous_did"
  },
  {
    "description": "Detect non-absorbing (reverting) treatment in a panel. Cohort-based DiD estimators (callaway_santanna, sun_abraham, did_imputation, etwfe, stacked_did) represent treatment by the first-treated period, which is lossless only when treatment never turns off. Under reversal they treat post-reversal periods as still-treated and are biased toward zero -- on a 150-unit panel with a third of units reverting, callaway_santanna returns 0.71 against a true ATT of 1.5, silently, because it never sees the time-varying indicator. Run this on the raw panel before picking an estimator. Validation: validated evidence tier (known-truth, reference, external-parity, or Monte Carlo artifact). Pre-conditions: long panel with unit x time x time-varying treatment; treatment column is numeric (0/1). Alternatives: sp.did_multiplegt, sp.lp_did, sp.did_multiplegt_dyn.",
    "input_schema": {
      "properties": {
        "strict": {
          "default": false,
          "description": "Raise MethodIncompatibility instead of returning when treatment reverts; use as a guard in front of a cohort-based estimator.",
          "type": "boolean"
        },
        "time": {
          "description": "Time column",
          "type": "string"
        },
        "treat": {
          "description": "Time-varying 0/1 treatment indicator. A cohort / first-treatment column cannot express reversal, so passing one makes the check meaningless.",
          "type": "string"
        },
        "unit": {
          "description": "Unit id column",
          "type": "string"
        }
      },
      "required": [
        "time",
        "treat",
        "unit"
      ],
      "type": "object"
    },
    "name": "check_absorbing"
  },
  {
    "description": "Design-level identification diagnostics: bad controls, overlap, cohort sizes, IV first-stage F, clustering. Run BEFORE fitting any estimator to surface design problems.",
    "input_schema": {
      "properties": {
        "cohort": {
          "type": "string"
        },
        "covariates": {
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "design": {
          "enum": [
            "did",
            "rd",
            "iv",
            "observational",
            "panel",
            "rct",
            "cross-section"
          ],
          "type": "string"
        },
        "id": {
          "type": "string"
        },
        "instrument": {
          "type": "string"
        },
        "running_var": {
          "type": "string"
        },
        "strict": {
          "default": false,
          "description": "Raise IdentificationError on BLOCKERS.",
          "type": "boolean"
        },
        "time": {
          "type": "string"
        },
        "treatment": {
          "type": "string"
        },
        "y": {
          "type": "string"
        }
      },
      "required": [
        "y"
      ],
      "type": "object"
    },
    "name": "check_identification"
  },
  {
    "description": "Chilean-style household income with urban/rural gap.",
    "input_schema": {
      "properties": {
        "n": {
          "default": 2500,
          "description": "n parameter (int).",
          "type": "integer"
        },
        "seed": {
          "default": 42,
          "description": "Random seed for reproducible stochastic steps.",
          "type": "integer"
        }
      },
      "required": [],
      "type": "object"
    },
    "name": "chilean_households"
  },
  {
    "description": "Changes-in-Changes (Athey & Imbens 2006). Nonparametric quantile DiD that identifies the full counterfactual outcome distribution for treated units, not just the mean. Reports quantile treatment effects (QTE) via empirical-CDF transformation; bootstrap SE. Validation: certified parity evidence. Assumptions: Rank-invariance of untreated potential outcomes across periods; Time-invariant group-level production technology (distributional DiD); SUTVA. Pre-conditions: continuous-ish outcome with sufficient support overlap between treated and control; 2 periods, 2 groups. Failure modes: Discrete outcome with few support points -> CIC quantile transformation degenerates; use sp.qte or sp.drdid for mean effects. Alternatives: sp.qte, sp.drdid, sp.did_2x2. Typical minimum N: 200.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "covariates": {
          "description": "Covariates for the Athey-Imbens (2006 p.466) two-step estimator; 'C(col)' / 'i.col' terms are absorbed as fixed effects",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "first_stage": {
          "default": "feols",
          "description": "First-stage residualizer (only 'feols' supported)",
          "type": "string"
        },
        "group": {
          "description": "Treatment-group indicator (0/1)",
          "type": "string"
        },
        "n_boot": {
          "default": 500,
          "description": "Number of bootstrap replications.",
          "type": "integer"
        },
        "n_grid": {
          "default": 200,
          "description": "Grid size for inverse-CDF mapping",
          "type": "integer"
        },
        "quantiles": {
          "description": "Quantile grid (default: deciles)",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "seed": {
          "description": "Random seed for reproducible stochastic steps.",
          "type": "integer"
        },
        "time": {
          "description": "Period indicator (0=pre, 1=post)",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "group",
        "time",
        "y"
      ],
      "type": "object"
    },
    "name": "cic"
  },
  {
    "description": "McFadden's conditional (fixed-effect) logit for choice data. Validation: certified parity evidence.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "cluster": {
          "description": "Cluster identifier column for clustered standard errors.",
          "type": "string"
        },
        "formula": {
          "description": "Formula ``\"chosen ~ price + quality\"``.",
          "type": "string"
        },
        "group": {
          "description": "Variable identifying the choice set / decision-maker.",
          "type": "string"
        },
        "maxiter": {
          "default": 100,
          "description": "maxiter parameter (int).",
          "type": "integer"
        },
        "robust": {
          "default": "nonrobust",
          "description": "Robust standard-error or covariance estimator option.",
          "type": "string"
        },
        "tol": {
          "default": 1e-08,
          "description": "Numerical convergence tolerance.",
          "type": "number"
        },
        "x": {
          "description": "Alternative-specific (and/or individual-specific interacted with alternative dummies) covariates.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Binary indicator: 1 = chosen, 0 = not chosen.",
          "type": "string"
        }
      },
      "required": [],
      "type": "object"
    },
    "name": "clogit"
  },
  {
    "description": "Complementary log-log regression via maximum likelihood. Validation: certified parity evidence.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals.",
          "type": "number"
        },
        "at_values": {
          "description": "Variable values for ``marginal_effects='at'``.",
          "type": "object"
        },
        "cluster": {
          "description": "Column name for clustered standard errors.",
          "type": "string"
        },
        "formula": {
          "description": "Formula like ``\"y ~ x1 + x2\"``.",
          "type": "string"
        },
        "marginal_effects": {
          "description": "``'average'`` (AME), ``'mean'`` (MEM), or ``'at'`` (MER).",
          "type": "string"
        },
        "maxiter": {
          "default": 100,
          "description": "Maximum Newton-Raphson iterations.",
          "type": "integer"
        },
        "robust": {
          "default": "nonrobust",
          "description": "``'nonrobust'`` for MLE SE, ``'hc1'`` / ``'robust'`` for sandwich SE.",
          "type": "string"
        },
        "tol": {
          "default": 1e-08,
          "description": "Convergence tolerance on log-likelihood change.",
          "type": "number"
        },
        "weights": {
          "description": "Column name for frequency/analytic weights.",
          "type": "string"
        },
        "x": {
          "description": "Regressor names (alternative to formula).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Dependent variable name (alternative to formula).",
          "type": "string"
        }
      },
      "required": [],
      "type": "object"
    },
    "name": "cloglog"
  },
  {
    "description": "Cluster-based CATE estimator. Validation: validated evidence tier (known-truth, reference, external-parity, or Monte Carlo artifact). Assumptions: Unconfoundedness given the covariates; Overlap / positivity across the covariate space; Nuisance functions are estimated consistently; cross-fitting controls overfitting bias. Pre-conditions: Covariates, a treatment indicator, and an outcome for each unit; Enough data to fit flexible nuisance models with sample-splitting / cross-fitting. Failure modes: CATE estimates are unstable or extrapolate beyond the covariate support -> Restrict to the overlap region, increase data, or use a doubly-robust learner (DR-/R-learner). Alternatives: sp.dml, sp.causal_forest, sp.tmle. Typical minimum N: 500.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "covariates": {
          "description": "Covariate matrix, DataFrame, or column names.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "n_clusters": {
          "default": 4,
          "description": "Number of K-means clusters.",
          "type": "integer"
        },
        "seed": {
          "default": 0,
          "description": "Random seed for reproducible stochastic steps.",
          "type": "integer"
        },
        "treat": {
          "description": "Treatment indicator or first-treatment-period column.",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "covariates",
        "treat",
        "y"
      ],
      "type": "object"
    },
    "name": "cluster_cate"
  },
  {
    "description": "Cluster-randomised trial under cross-cluster interference (Ding et al. 2025). Estimates direct + spillover effects when treatment of one cluster affects outcomes in adjacent clusters. Validation: validated evidence tier (known-truth, reference, external-parity, or Monte Carlo artifact). Assumptions: Partial interference: spillovers operate within clusters but not across cluster boundaries; Exposure mapping correctly captured by neighbour_treat_share (user-precomputed share of treated neighbours); Cluster-level treatment is binary and randomized (cluster RCT). Pre-conditions: Cluster identifier column plus individual-level outcome; Cluster-level binary treatment column; Precomputed neighbour_treat_share column from spatial/network adjacency. Failure modes: neighbour_treat_share missing or not a valid 0-1 share -> Precompute the treated-neighbour share per cluster from your adjacency matrix before calling; Too few clusters for cluster-robust inference -> Increase the number of clusters or use a design with weaker cluster requirements. Alternatives: sp.inward_outward_spillover, sp.network_hte, sp.interference, sp.spillover. Typical minimum N: 30.",
    "input_schema": {
      "properties": {
        "cluster": {
          "description": "Cluster identifier column for clustered standard errors.",
          "type": "string"
        },
        "neighbour_treat_share": {
          "description": "Column with neighbours' treatment share",
          "type": "string"
        },
        "treat": {
          "description": "Treatment indicator or first-treatment-period column.",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "cluster",
        "neighbour_treat_share",
        "treat",
        "y"
      ],
      "type": "object"
    },
    "name": "cluster_cross_interference"
  },
  {
    "description": "Return cluster-robust standard errors (diagonal sqrt of vcov). Validation: certified parity evidence.",
    "input_schema": {
      "properties": {
        "X": {
          "description": "Feature matrix or covariate DataFrame.",
          "type": "string"
        },
        "clusters": {
          "description": "Cluster labels for clustered inference.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "resid": {
          "description": "resid parameter (np.ndarray).",
          "type": "string"
        }
      },
      "required": [
        "X",
        "clusters",
        "resid"
      ],
      "type": "object"
    },
    "name": "cluster_robust_se"
  },
  {
    "description": "Cohort-anchored event study. Instead of averaging across cohorts at each relative-time bin (which can contaminate leads / lags with other cohorts' dynamics), estimates separate event-study paths per cohort and then aggregates with cohort weights. Standard errors are cluster-robust and carry no protection against parallel-trends violations: this is the cohort-anchored estimator Liu (2025) starts from, NOT that paper's block-bias robust-inference procedure, which is not implemented. For parallel-trends sensitivity use sp.honest_did. Assumptions: Parallel trends hold within each treatment cohort relative to never-treated units (cohort-anchored, not pooled TWFE); No anticipation: outcomes in pre-event periods unaffected by future treatment; Treatment is an absorbing first-treatment event (staggered adoption, treat encodes first period, 0 = never-treated). Pre-conditions: Long-format balanced/unbalanced panel with id and time columns; treat column gives first-treatment period per unit (0 = never-treated), with at least one never-treated cohort; Enough pre/post periods to fill the requested leads/lags event-time window. Failure modes: All units treated in same period / no never-treated...",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "cluster": {
          "description": "Cluster identifier column for clustered standard errors.",
          "type": "string"
        },
        "id": {
          "description": "Unit, subject, or panel identifier column.",
          "type": "string"
        },
        "lags": {
          "default": 4,
          "description": "lags parameter (int).",
          "type": "integer"
        },
        "leads": {
          "default": 4,
          "description": "leads parameter (int).",
          "type": "integer"
        },
        "time": {
          "description": "Time period column.",
          "type": "string"
        },
        "treat": {
          "description": "Treatment indicator or first-treatment-period column.",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "id",
        "time",
        "treat",
        "y"
      ],
      "type": "object"
    },
    "name": "cohort_anchored_event_study"
  },
  {
    "description": "Per-cohort event study plot (overlay).",
    "input_schema": {
      "properties": {
        "aggregate_color": {
          "default": "#2C3E50",
          "description": "Color for aggregate line.",
          "type": "string"
        },
        "ax": {
          "description": "ax parameter.",
          "type": "string"
        },
        "ci": {
          "default": true,
          "description": "Show confidence intervals for each cohort.",
          "type": "boolean"
        },
        "ci_alpha": {
          "default": 0.08,
          "description": "CI band transparency.",
          "type": "number"
        },
        "figsize": {
          "default": [
            12,
            7
          ],
          "description": "figsize parameter (Tuple[float, float]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "palette": {
          "description": "Colors for each cohort. Auto-generated if None.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "result": {
          "description": "Result from ``callaway_santanna()`` or ``did(method='cs')``. Must have ``detail`` with 'group', 'relative_time', 'att' columns, and ``model_info['event_study']`` for aggregate.",
          "type": "string"
        },
        "show_aggregate": {
          "default": true,
          "description": "Overlay the aggregate event study line.",
          "type": "boolean"
        },
        "title": {
          "description": "title parameter (Optional[str]).",
          "type": "string"
        }
      },
      "required": [
        "result"
      ],
      "type": "object"
    },
    "name": "cohort_event_study_plot"
  },
  {
    "description": "Run multiple estimators on the same data and compare.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "covariates": {
          "description": "Covariate matrix, DataFrame, or column names.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "id": {
          "description": "Panel unit ID.",
          "type": "string"
        },
        "instrument": {
          "description": "instrument parameter (Optional[str]).",
          "type": "string"
        },
        "method_hints": {
          "description": "Per-method keyword overrides, merged with the shared kwargs when dispatching each estimator. Structure:: {'proximal': {'proxy_z': ['z'], 'proxy_w': ['w']}, 'msm': {'time_varying': ['L_lag']}, 'principal_strat': {'strata': 's'}} **Collision rule** (docs/ROADMAP.md Section 6): per-method hints take precedence over the shared kwargs for the method they name. If the top-level ``covariates=['age']`` disagrees with ``method_hints={'proximal': {'covariates': ['age', 'educ']}}``, proximal uses the hint and every other method uses the shared arg. A ``UserWarning`` fires on conflict so the override is visible in the log.",
          "type": "object"
        },
        "methods": {
          "description": "Estimators to compare. Default auto-selects based on data. Classical options: ``'ols'``, ``'matching'``, ``'ipw'``, ``'aipw'``, ``'dml'``, ``'g_computation'``, ``'causal_forest'``, ``'did'``, ``'panel_fe'``. Hint-driven Sprint-B options (require ``method_hints``): ``'proximal'``, ``'msm'``, ``'principal_strat'``, ``'mediate'``, ``'mediate_interventional'``, ``'front_door'``. Each needs method-specific kwargs the shared signature does not expose (proxy_z/proxy_w, time_varying, strata, mediator, etc.) -- pass them through ``method_hints``.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "time": {
          "description": "Time variable.",
          "type": "string"
        },
        "treatment": {
          "description": "Treatment variable (binary).",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable.",
          "type": "string"
        }
      },
      "required": [
        "treatment",
        "y"
      ],
      "type": "object"
    },
    "name": "compare_estimators"
  },
  {
    "description": "Run several DiD estimators on one non-staggered panel and measure how far each event-study path departs from the dynamic TWFE benchmark. Splits the difference into a common vertical shift within each half of the path and a residual, so a symmetric estimator scores zero asymmetry while the kink (Callaway-Sant'Anna varying base period), the jump (BJS pre-trend convention) and the N0/N attenuation (fect / did2s in-sample residuals) each get their own signature. Warns when the recorded convention disagrees with what the data show. Assumptions: The comparison is descriptive: it measures construction differences, not which estimator is correct. Pre-conditions: panel with unit x time x outcome; exactly one treated cohort (non-staggered design); at least one never-treated unit. Failure modes: Staggered adoption (more than one treated cohort) -> Restrict to one cohort plus never-treated units; with staggered timing a gap against TWFE mixes the reference convention with forbidden comparisons; No never-treated units -> The TWFE benchmark path needs untreated units. Alternatives: sp.event_study_convention, sp.bacon_decomposition. Typical minimum N: 50.",
    "input_schema": {
      "properties": {
        "cluster": {
          "description": "Cluster identifier column for clustered standard errors.",
          "type": "string"
        },
        "estimators": {
          "description": "Registry keys to run; defaults to every estimator with a runner.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "first_treat": {
          "description": "First-treatment period; 0 = never-treated",
          "type": "string"
        },
        "time": {
          "description": "Time period column",
          "type": "string"
        },
        "tolerance": {
          "description": "Threshold for the matches_twfe verdict; defaults to a scale-free 1e-6 * max(1, max|beta_twfe|).",
          "type": "number"
        },
        "unit": {
          "description": "Unit identifier",
          "type": "string"
        },
        "window": {
          "description": "Relative-time window; defaults to the widest the panel supports.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "first_treat",
        "time",
        "unit",
        "y"
      ],
      "type": "object"
    },
    "name": "compare_event_study_conventions"
  },
  {
    "description": "Fit multiple meta-learners and compare their ATE estimates. Assumptions: Unconfoundedness given the covariates; Overlap / positivity across the covariate space; Nuisance functions are estimated consistently; cross-fitting controls overfitting bias. Pre-conditions: Covariates, a treatment indicator, and an outcome for each unit; Enough data to fit flexible nuisance models with sample-splitting / cross-fitting. Failure modes: CATE estimates are unstable or extrapolate beyond the covariate support -> Restrict to the overlap region, increase data, or use a doubly-robust learner (DR-/R-learner). Alternatives: sp.dml, sp.causal_forest, sp.tmle. Typical minimum N: 500.",
    "input_schema": {
      "properties": {
        "covariates": {
          "description": "Covariate / effect modifier variables.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "learners": {
          "description": "Which learners to compare. Default: all five ('s','t','x','r','dr').",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "treat": {
          "description": "Binary treatment variable (0/1).",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable.",
          "type": "string"
        }
      },
      "required": [
        "covariates",
        "treat",
        "y"
      ],
      "type": "object"
    },
    "name": "compare_metalearners"
  },
  {
    "description": "Moreira (2003) CLR confidence set -- re-export of Validation: certified parity evidence.",
    "input_schema": {
      "properties": {
        "add_const": {
          "default": true,
          "description": "add_const parameter (bool).",
          "type": "boolean"
        },
        "beta_grid": {
          "description": "Grid of beta values to evaluate.",
          "type": "string"
        },
        "endog": {
          "description": "endog parameter (Union[np.ndarray, pd.Series, str]).",
          "type": "string"
        },
        "exog": {
          "description": "exog parameter (Optional[Union[np.ndarray, pd.DataFrame, List[str]]]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "instruments": {
          "description": "instruments parameter (Union[np.ndarray, pd.DataFrame, List[str]]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "level": {
          "default": 0.95,
          "description": "Confidence level or reporting level.",
          "type": "number"
        },
        "method": {
          "default": "exact",
          "description": "Estimator or algorithm variant to use.",
          "type": "string"
        },
        "n_grid": {
          "default": 201,
          "description": "Number of grid.",
          "type": "integer"
        },
        "n_sim": {
          "default": 5000,
          "description": "Number of sim.",
          "type": "integer"
        },
        "random_state": {
          "description": "Random seed or RandomState for reproducible stochastic steps.",
          "type": "integer"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "endog",
        "instruments",
        "y"
      ],
      "type": "object"
    },
    "name": "conditional_lr_ci"
  },
  {
    "description": "Unified conformal causal inference dispatcher. kind= selects the estimator: 'cate' / 'counterfactual' / 'ite' (Lei-Candes 2021 base) / 'weighted' (TBCR 2019 primitive) / 'density' / 'multidp' / 'debiased' / 'fair' (2025-2026 frontier) / 'continuous' (dose-response) / 'interference' (cluster-exchangeable). Kwargs pass through to the target function; see sp.conformal_family guide. Assumptions: Exchangeability of calibration and test points (base case); For kind='weighted': known or estimable density ratio between calibration and test; For kind='cate' / 'ite': selection-on-observables with correct propensity / outcome model. Pre-conditions: calibration sample disjoint from training sample (auto-split or user-supplied); exchangeability between calibration and test distributions (weighted variants for covariate shift); for CATE / ITE variants: unconfoundedness + overlap on covariates. Failure modes: Calibration and test distributions differ (covariate shift) -> Use kind='weighted' with estimated density ratios; Calibration set too small -- intervals wide -> Increase calibration sample or raise alpha; coverage gets loose below ~100; Miscalibrated nuisance (propensity / outcome) for CATE...",
    "input_schema": {
      "properties": {
        "kind": {
          "default": "cate",
          "description": "Conformal estimator -- call sp.conformal_available_kinds() for the full list.",
          "type": "string"
        }
      },
      "required": [],
      "type": "object"
    },
    "name": "conformal"
  },
  {
    "description": "Counterfactual-fair conformal prediction for ITE (2025). Wraps standard conformal ITE intervals with a demographic-parity adjustment, giving distribution-free coverage under protected-attribute shifts. Validation: validated evidence tier (known-truth, reference, external-parity, or Monte Carlo artifact). Assumptions: Exchangeability for conformal coverage, applied within protected-group strata; Counterfactual fairness: protected attribute excluded from the outcome regression (used only for stratified calibration); ITE ignorability / overlap so the treated and control nuisances are identified. Pre-conditions: DataFrame with y, treat, predictive covariates, and a categorical protected column; Calibration set per protected stratum for group-wise conformal coverage; Optional test_data to emit fair ITE intervals on. Failure modes: A protected stratum has too few calibration rows for valid group-wise quantiles -> Collapse sparse protected categories or pool strata before calibration; protected column accidentally leaks into covariates, breaking counterfactual fairness -> Remove the protected attribute from covariates; pass it only via the protected argument. Alternatives: sp.conformal,...",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.1,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "covariates": {
          "description": "Covariate matrix, DataFrame, or column names.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "protected": {
          "description": "Protected-attribute column",
          "type": "string"
        },
        "treat": {
          "description": "Treatment indicator or first-treatment-period column.",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "covariates",
        "protected",
        "treat",
        "y"
      ],
      "type": "object"
    },
    "name": "conformal_fair_ite"
  },
  {
    "description": "Conformal ITE -- article alias for :func:`conformal_cate`. Validation: validated evidence tier (known-truth, reference, external-parity, or Monte Carlo artifact).",
    "input_schema": {
      "properties": {
        "X": {
          "description": "Feature matrix or covariate DataFrame.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "d": {
          "description": "d parameter (str).",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "X",
        "d",
        "y"
      ],
      "type": "object"
    },
    "name": "conformal_ite"
  },
  {
    "description": "Conformal inference for synthetic control (Chernozhukov, Wuthrich & Zhu). Validation: certified parity evidence. Assumptions: A convex (or regularized) combination of donor units reproduces the treated unit's pre-treatment outcome path; No interference: the treatment does not affect the donor units (SUTVA); No anticipation before the treatment date. Pre-conditions: Panel of one or more treated units plus an untreated donor pool, observed over time; Pre-treatment window long enough to fit donor weights (rule of thumb: more pre-periods than donors used); Outcome observed for every unit in every period. Failure modes: Large pre-treatment RMSPE -- the synthetic unit fails to track the treated unit before treatment -> Add donors / predictors, lengthen the pre-period, or use a bias-corrected estimator (sdid, augsynth); Placebo / permutation inference shows the estimate is not extreme relative to donors -> Report the placebo distribution honestly; the effect may not be distinguishable from noise. Alternatives: sp.sdid, sp.augsynth, sp.gsynth, sp.callaway_santanna. Typical minimum N: 15.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level (``scinference`` defaults to 0.1).",
          "type": "number"
        },
        "grid_range": {
          "description": "(min, max) of the hypothesis grid. Default: the point estimate +/- max(5 x SD of the pre-period SC residuals, 3 x the largest absolute post-period gap). A confidence set reaching an end of the grid is truncated there (flagged in ``model_info['ci_truncated']``, with a warning). When ``alpha`` is below the smallest attainable p-value (``1/(T0+1)`` pointwise, ``1/(T0+T1)`` joint) nothing can be rejected and the set is ``(-inf, inf)``.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "grid_size": {
          "default": 101,
          "description": "Number of points in the hypothesis grid for CI inversion.",
          "type": "integer"
        },
        "outcome": {
          "description": "Outcome variable name.",
          "type": "string"
        },
        "penalization": {
          "default": 0.0,
          "description": "Ridge penalty on the weights.",
          "type": "number"
        },
        "scm_method": {
          "default": "classic",
          "description": "Label of the counterfactual model. Both fit simplex-constrained least squares of the treated outcome on the donor outcomes (no intercept, ``w >= 0``, ``sum(w) = 1``) plus ``penalization * ||w||^2``; ``'classic'`` with ``penalization = 0`` is ``scinference``'s ``sc`` estimator.",
          "enum": [
            "classic",
            "ridge"
          ],
          "type": "string"
        },
        "time": {
          "description": "Time period column.",
          "type": "string"
        },
        "treated_unit": {
          "description": "Identifier of the treated unit.",
          "type": "string"
        },
        "treatment_time": {
          "description": "First treatment period.",
          "type": "string"
        },
        "unit": {
          "description": "Unit identifier column.",
          "type": "string"
        }
      },
      "required": [
        "outcome",
        "time",
        "treated_unit",
        "treatment_time",
        "unit"
      ],
      "type": "object"
    },
    "name": "conformal_synth"
  },
  {
    "description": "Compute Conley (1999) spatial -- and optionally spatio-temporal -- HAC Validation: certified parity evidence. Cost: Sparse and scale-safe: a scipy cKDTree ball query enumerates only observation pairs within dist_cutoff, so memory is O(n + pairs-within-cutoff) rather than O(n^2). This is the recommended Conley path on large samples -- unlike feols(vce='conley') / hdfe_ols(vce='conley'), which are dense. Cost still grows with dist_cutoff: a cutoff large enough to link most observations recovers the quadratic pair count.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals.",
          "type": "number"
        },
        "dist_cutoff": {
          "description": "Distance cutoff *h* in kilometres. Pairs farther apart than this receive zero weight.",
          "type": "number"
        },
        "distance": {
          "default": "haversine",
          "description": "``\"haversine\"`` uses great-circle distance on a sphere of radius 6371 km, and is symmetric. ``\"planar\"`` reproduces ``acreg``'s convention exactly: 111 km per degree of latitude and ``cos(lat_ref) * 111`` km per degree of longitude, anchored at the *reference* point, which makes the distance asymmetric; the resulting covariance is symmetrised the same way ``acreg`` does (Mata ``_makesymmetric``, which mirrors the lower triangle). Choose ``\"planar\"`` for bit-level ``acreg`` parity, ``\"haversine\"`` for a proper great-circle metric.",
          "type": "string"
        },
        "kernel": {
          "default": "uniform",
          "description": "Spatial kernel: ``\"uniform\"`` (indicator) or ``\"bartlett\"`` (linearly declining weight ``1 - d/h``).",
          "type": "string"
        },
        "lag_cutoff": {
          "description": "Serial-correlation bandwidth in periods, for pairs within the same unit. Requires ``time``.",
          "type": "integer"
        },
        "lag_cutoff_cross": {
          "description": "Time bandwidth for pairs drawn from *different* units -- ``acreg``'s ``lagdist()``. Defaults to ``0`` (``acreg``'s default), i.e. contemporaneous cross-unit correlation only.",
          "type": "integer"
        },
        "lat": {
          "description": "Column name for latitude.",
          "type": "string"
        },
        "lon": {
          "description": "Column name for longitude.",
          "type": "string"
        },
        "result": {
          "description": "Fitted OLS result. Must have ``data_info`` containing ``'X'`` (design matrix), ``'y'`` (response), and ``'residuals'``.",
          "type": "string"
        },
        "time": {
          "description": "Column name holding the time period. Must be integer-valued. Requires ``lag_cutoff`` and ``unit``.",
          "type": "string"
        },
        "time_kernel": {
          "default": "bartlett",
          "description": "Time kernel: ``\"bartlett\"`` (``1 - |dt| / (lag_cutoff + 1)``, the Newey-West convention, matching ``acreg``'s ``hac bartlett``) or ``\"uniform\"`` (indicator, matching ``acreg`` without ``hac``).",
          "type": "string"
        },
        "unit": {
          "description": "Column name identifying the panel unit. Enables panel de-duplication: the spatial neighbour search runs on the distinct unit coordinates rather than on every row, so a unit observed T times costs one point in the KD-tree instead of T.",
          "type": "string"
        }
      },
      "required": [
        "dist_cutoff",
        "lat",
        "lon",
        "result"
      ],
      "type": "object"
    },
    "name": "conley"
  },
  {
    "description": "DiD with continuous treatment intensity. Four modes: (i) 'twfe' TWFE with dosexpost interaction; (ii) 'att_gt' dose-quantile group-time ATT versus the untreated (dose=0) arm with bootstrap SE (heuristic); (iii) 'dose_response' local-linear regression of DeltaY=Y_post-Y_pre on baseline dose; (iv) 'cgs' Callaway-Goodman-Bacon-Sant'Anna (2024) ATT(d|g,t) MVP -- 2-period design, OR only, bootstrap SE, [pending verification] markers on paper formulas. Full CGS parity (cohort aggregation, DR/IPW, analytical IF variance) is on the roadmap -- see docs/rfc/continuous_did_cgs.md. Validation: certified evidence with scoped limitations. Known limitations: method='cgs' is an MVP -- 2-period design, OR only, bootstrap SE; full CGS parity (cohort aggregation, DR/IPW, analytical IF variance) is on the roadmap (see docs/rfc/continuous_did_cgs.md). Other modes (twfe / att_gt / dose_response) are stable. Assumptions: Parallel trends in potential outcomes across dose levels; No anticipation of treatment; Strong parallel trends (CGS 2024) required for ATT(d|g,t) interpretation in att_gt mode. Pre-conditions: panel data with unit x time x outcome x continuous dose; at least one unit with dose == 0 acts...",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "cluster": {
          "description": "Cluster variable for SE (TWFE mode)",
          "type": "string"
        },
        "controls": {
          "description": "Control variables",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "dose": {
          "description": "Continuous treatment / dose variable",
          "type": "string"
        },
        "id": {
          "description": "Unit identifier",
          "type": "string"
        },
        "method": {
          "default": "att_gt",
          "description": "Estimation mode",
          "enum": [
            "att_gt",
            "twfe",
            "dose_response",
            "cgs"
          ],
          "type": "string"
        },
        "n_boot": {
          "default": 500,
          "description": "Bootstrap replications for SE",
          "type": "integer"
        },
        "n_quantiles": {
          "default": 5,
          "description": "Number of dose quantiles for discretisation",
          "type": "integer"
        },
        "post": {
          "description": "Binary post-treatment indicator (inferred from t_pre / t_post if omitted)",
          "type": "string"
        },
        "seed": {
          "description": "Random seed for reproducible stochastic steps.",
          "type": "integer"
        },
        "t_post": {
          "description": "First post-treatment period",
          "type": "integer"
        },
        "t_pre": {
          "description": "Last pre-treatment period",
          "type": "integer"
        },
        "time": {
          "description": "Time period column",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable",
          "type": "string"
        }
      },
      "required": [
        "dose",
        "id",
        "time",
        "y"
      ],
      "type": "object"
    },
    "name": "continuous_did"
  },
  {
    "description": "LATE with a continuous instrument (Xie et al. 2025). Estimates the LATE on the maximal complier class via quantile-bin Wald ratios, weighted by the bin-pair with the largest first-stage response. Validation: validated evidence tier (known-truth, reference, external-parity, or Monte Carlo artifact). Assumptions: Instrument relevance (non-zero first stage); Exclusion restriction: the instrument affects the outcome only through the treatment; Independence/exogeneity of the instrument; for LATE, monotonicity (no defiers). Pre-conditions: An instrument plausibly affecting treatment, an endogenous treatment, and an outcome; A strong first stage (assess instrument strength before interpreting estimates); formula includes the (endog ~ instruments) parenthesised block. Failure modes: Weak first stage -- biased point estimates and unreliable conventional SEs -> Report first-stage F / effective F and use weak-IV-robust inference (Anderson-Rubin); First-stage F < 10 (Stock-Yogo 5% bias) -> Use weak-IV-robust inference (Anderson-Rubin) or LIML; Over-identification test rejects (sp.estat 'overid') -> At least one instrument is invalid; drop instruments or switch to just-identified LIML. Alterna...",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "instrument": {
          "description": "Continuous instrument",
          "type": "string"
        },
        "n_boot": {
          "default": 200,
          "description": "Number of bootstrap replications.",
          "type": "integer"
        },
        "n_quantiles": {
          "default": 4,
          "description": "Number of instrument quantile bins",
          "type": "integer"
        },
        "seed": {
          "default": 0,
          "description": "Random seed for reproducible stochastic steps.",
          "type": "integer"
        },
        "treat": {
          "description": "Treatment indicator or first-treatment-period column.",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "instrument",
        "treat",
        "y"
      ],
      "type": "object"
    },
    "name": "continuous_iv_late"
  },
  {
    "description": "Compute contrasts of predictive margins across levels of a variable. Validation: certified parity evidence.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level.",
          "type": "number"
        },
        "method": {
          "default": "r",
          "description": "Contrast type: - ``'r'`` (reference): each level vs *reference* level. - ``'ar'`` (adjacent): each level vs the previous level. - ``'gw'`` (grand-mean weighted): each level vs the weighted grand mean of all levels.",
          "type": "string"
        },
        "reference": {
          "description": "Reference level when ``method='r'``. Defaults to the smallest observed level.",
          "type": "string"
        },
        "result": {
          "description": "Fitted model result.",
          "type": "string"
        },
        "variable": {
          "description": "Categorical variable whose levels are contrasted.",
          "type": "string"
        }
      },
      "required": [
        "result",
        "variable"
      ],
      "type": "object"
    },
    "name": "contrast"
  },
  {
    "description": "Gaussian-copula sensitivity to unobserved confounding.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "estimate": {
          "description": "estimate parameter (float).",
          "type": "number"
        },
        "rho_grid": {
          "description": "Correlation grid. Defaults to ``np.linspace(-0.5, 0.5, 21)``.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "se": {
          "description": "se parameter (float).",
          "type": "number"
        },
        "sigma_u": {
          "default": 1.0,
          "description": "Standard deviations of the latent confounder and the outcome. With default values the bias coefficient is numerically equal to ``rho``, matching Chernozhukov-Cinelli-Hazlett's \"percentile scaling.\"",
          "type": "number"
        },
        "sigma_y": {
          "default": 1.0,
          "description": "Standard deviations of the latent confounder and the outcome. With default values the bias coefficient is numerically equal to ``rho``, matching Chernozhukov-Cinelli-Hazlett's \"percentile scaling.\"",
          "type": "number"
        }
      },
      "required": [
        "estimate",
        "se"
      ],
      "type": "object"
    },
    "name": "copula_sensitivity"
  },
  {
    "description": "Cox Proportional Hazards model via partial likelihood. Validation: certified parity evidence.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals.",
          "type": "number"
        },
        "cluster": {
          "description": "Column name for cluster-robust SE.",
          "type": "string"
        },
        "duration": {
          "description": "Column name for follow-up time (overrides formula LHS).",
          "type": "string"
        },
        "event": {
          "description": "Column name for event indicator (1 = event, 0 = censored).",
          "type": "string"
        },
        "formula": {
          "description": "Formula of the form ``'duration ~ x1 + x2'``. If given, ``duration`` is inferred from the LHS.",
          "type": "string"
        },
        "hazard_ratio": {
          "default": true,
          "description": "If True, report hazard ratios in the summary alongside coefficients.",
          "type": "boolean"
        },
        "robust": {
          "default": "nonrobust",
          "description": "``'hc0'`` for sandwich SE.",
          "type": "string"
        },
        "strata": {
          "description": "Column name for stratification variable.",
          "type": "string"
        },
        "ties": {
          "default": "efron",
          "description": "Tie-handling method: ``'efron'`` or ``'breslow'``.",
          "type": "string"
        },
        "x": {
          "description": "Covariate column names (overrides formula RHS).",
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "required": [],
      "type": "object"
    },
    "name": "cox"
  },
  {
    "description": "Cox proportional hazards with shared gamma frailty. Validation: certified parity evidence.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level (kept for API symmetry; intervals are built by the result's accessors).",
          "type": "number"
        },
        "cluster": {
          "description": "Column identifying clusters (e.g. hospital, site).",
          "type": "string"
        },
        "formula": {
          "description": "``\"duration + event ~ x1 + x2\"`` (like R's ``Surv(time, event) ~ x``).",
          "type": "string"
        },
        "maxiter": {
          "default": 50,
          "description": "Newton-Raphson controls for the penalised fit at each theta.",
          "type": "integer"
        },
        "theta": {
          "description": "Fix the frailty variance instead of estimating it (R ``frailty(id, theta=)``). ``0`` fits the ordinary Cox model.",
          "type": "number"
        },
        "ties": {
          "default": "efron",
          "description": "Tie handling in the partial likelihood (R's default is Efron, Stata's ``stcox`` default is Breslow; identical without ties).",
          "enum": [
            "efron",
            "breslow"
          ],
          "type": "string"
        },
        "tol": {
          "default": 1e-10,
          "description": "Newton-Raphson controls for the penalised fit at each theta.",
          "type": "number"
        }
      },
      "required": [
        "cluster",
        "formula"
      ],
      "type": "object"
    },
    "name": "cox_frailty"
  },
  {
    "description": "CPS-style wage data with a gender gap.",
    "input_schema": {
      "properties": {
        "n": {
          "default": 3000,
          "description": "n parameter (int).",
          "type": "integer"
        },
        "seed": {
          "default": 42,
          "description": "Random seed for reproducible stochastic steps.",
          "type": "integer"
        }
      },
      "required": [],
      "type": "object"
    },
    "name": "cps_wage"
  },
  {
    "description": "CR2 bias-corrected cluster-robust standard errors (Bell & McCaffrey 2002). Validation: certified parity evidence.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals.",
          "type": "number"
        },
        "cluster": {
          "description": "Name of the cluster variable in ``data``.",
          "type": "string"
        },
        "result": {
          "description": "A fitted regression result from ``sp.regress()``.",
          "type": "string"
        }
      },
      "required": [
        "cluster",
        "result"
      ],
      "type": "object"
    },
    "name": "cr2_se"
  },
  {
    "description": "CR3 cluster-jackknife variance (delete-one-cluster). Validation: certified parity evidence.",
    "input_schema": {
      "properties": {
        "X": {
          "description": "Feature matrix or covariate DataFrame.",
          "type": "string"
        },
        "cluster": {
          "description": "Cluster identifier column for clustered standard errors.",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "X",
        "cluster",
        "y"
      ],
      "type": "object"
    },
    "name": "cr3_jackknife_vcov"
  },
  {
    "description": "Cross-validate ONE estimand across INDEPENDENT engines (StatsPAI, pyfixest, linearmodels, DoubleML, R's fixest, Stata) and report whether they agree (AGREE / PARTIAL / DISAGREE / INSUFFICIENT). Use this to honour the cross-package reproducibility rule: trust a number only when >=2 independent implementations reproduce it. Needs a data_path.",
    "input_schema": {
      "properties": {
        "covariates": {
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "endog": {
          "description": "Endogenous regressors (IV).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "engines": {
          "description": "Engines to run, e.g. ['statspai','R::fixest','pyfixest','Stata']. Omit for 'auto' (all installed + applicable).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "estimand": {
          "description": "Model family to fit in every engine.",
          "enum": [
            "ols",
            "feols",
            "iv",
            "poisson",
            "dml",
            "did"
          ],
          "type": "string"
        },
        "fixed_effects": {
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "formula": {
          "description": "fixest-style 'y ~ x | fe | endog ~ z'.",
          "type": "string"
        },
        "g": {
          "description": "DiD only: cohort / first-treatment period (0 = never treated).",
          "type": "string"
        },
        "i": {
          "description": "DiD only: unit-id column.",
          "type": "string"
        },
        "instruments": {
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "t": {
          "description": "DiD only: time column.",
          "type": "string"
        },
        "treatment": {
          "description": "Focal regressor (reconciled coefficient).",
          "type": "string"
        },
        "vcov": {
          "type": "string"
        },
        "y": {
          "description": "Outcome column.",
          "type": "string"
        }
      },
      "required": [
        "estimand"
      ],
      "type": "object"
    },
    "name": "cross_validate"
  },
  {
    "description": "Delete-one-cluster jackknife (CV3) standard error for a Callaway-Sant'Anna aggregate: drop each cluster, re-estimate every ATT(g,t) and the cohort shares, re-aggregate, and report (R-1)/R * sum (ATT_(-h) - ATT)^2 with t(R-1) inference. The few-cluster / few-treated-cluster alternative to the analytic and multiplier-bootstrap SEs, matching R didjack and Stata csdidjack. Assumptions: Same identifying assumptions as callaway_santanna (parallel trends, no anticipation, SUTVA); Clusters are independent; inference uses t with R-1 degrees of freedom. Pre-conditions: every delete-one-cluster sample still identifies the requested aggregate; cluster membership is constant within unit. Failure modes: Deleting one cluster removes the only never-treated or not-yet-treated comparison -> Report the analytic or bootstrap SE and state that the jackknife is undefined on this design; Inference options (bstrap, cband, se_method) passed through -> Drop them; the jackknife is the inference. Alternatives: sp.aggte, sp.callaway_santanna, sp.wild_cluster_bootstrap. Typical minimum N: 50.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Level of the t(R-1) interval",
          "type": "number"
        },
        "cluster": {
          "description": "Time-invariant cluster variable; defaults to the unit",
          "type": "string"
        },
        "g": {
          "description": "First-treatment period (0 = never treated)",
          "type": "string"
        },
        "id": {
          "description": "Unit column",
          "type": "string"
        },
        "time": {
          "description": "Time column",
          "type": "string"
        },
        "type": {
          "default": "simple",
          "description": "Aggregation whose overall summary is jackknifed",
          "enum": [
            "simple",
            "dynamic",
            "group",
            "calendar"
          ],
          "type": "string"
        },
        "y": {
          "description": "Outcome column",
          "type": "string"
        }
      },
      "required": [
        "g",
        "id",
        "time",
        "y"
      ],
      "type": "object"
    },
    "name": "cs_jackknife"
  },
  {
    "description": "One-call staggered-DID workflow following Baker et al. (2026).",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "anticipation": {
          "default": 0,
          "description": "anticipation parameter (int).",
          "type": "integer"
        },
        "balance": {
          "default": true,
          "description": "balance parameter (bool).",
          "type": "boolean"
        },
        "control_group": {
          "default": "nevertreated",
          "description": "control_group parameter (str).",
          "enum": [
            "nevertreated",
            "notyettreated"
          ],
          "type": "string"
        },
        "data_or_result": {
          "description": "Either a long-format panel (then ``y, g, t, i`` are required and :func:`callaway_santanna` is run first), or an already-fitted :func:`callaway_santanna` result.",
          "type": "string"
        },
        "estimator": {
          "default": "dr",
          "description": "estimator parameter (str).",
          "enum": [
            "dr",
            "ipw",
            "reg"
          ],
          "type": "string"
        },
        "functional_form": {
          "default": true,
          "description": "functional_form parameter (bool).",
          "type": "boolean"
        },
        "g": {
          "description": "Outcome / cohort / time / unit id columns (required when ``data_or_result`` is a DataFrame).",
          "type": "string"
        },
        "i": {
          "description": "Outcome / cohort / time / unit id columns (required when ``data_or_result`` is a DataFrame).",
          "type": "string"
        },
        "max_e": {
          "default": Infinity,
          "description": "Event-time window passed to the dynamic aggregation.",
          "type": "number"
        },
        "min_e": {
          "default": -Infinity,
          "description": "Event-time window passed to the dynamic aggregation.",
          "type": "number"
        },
        "n_boot": {
          "default": 1000,
          "description": "Multiplier-bootstrap replications for :func:`aggte`.",
          "type": "integer"
        },
        "random_state": {
          "default": 0,
          "description": "Seed for the bootstrap (set to ``None`` for non-reproducibility).",
          "type": "integer"
        },
        "rr_method": {
          "default": "smoothness",
          "description": "Sensitivity restriction handed to :func:`breakdown_m`.",
          "enum": [
            "smoothness",
            "relative_magnitude"
          ],
          "type": "string"
        },
        "save_to": {
          "description": "When set, treats the value as a *path prefix* and writes the report in every supported format in one call: - ``<prefix>.txt`` -- fixed-width plain-text report - ``<prefix>.md`` -- GitHub-flavoured Markdown - ``<prefix>.tex`` -- booktabs LaTeX fragment - ``<prefix>.xlsx`` -- multi-sheet workbook - ``<prefix>.png`` -- 2x2 summary figure (only if matplotlib is installed; silently skipped otherwise) Missing parent directories are created on the fly.",
          "type": "string"
        },
        "t": {
          "description": "Outcome / cohort / time / unit id columns (required when ``data_or_result`` is a DataFrame).",
          "type": "string"
        },
        "triangulate": {
          "default": true,
          "description": "Also fit the other covariate strategies (regression adjustment, IPW, doubly robust) and report them side by side, as in the paper's Table 7 / Figure 4. Only meaningful with covariates.",
          "type": "boolean"
        },
        "verbose": {
          "default": true,
          "description": "If ``True``, print the report before returning.",
          "type": "boolean"
        },
        "weights": {
          "description": "Unit weights omega. Changes the target parameter, not just precision -- see :func:`statspai.did.callaway_santanna`.",
          "type": "string"
        },
        "x": {
          "description": "Covariates for conditional parallel trends.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Outcome / cohort / time / unit id columns (required when ``data_or_result`` is a DataFrame).",
          "type": "string"
        }
      },
      "required": [
        "data_or_result"
      ],
      "type": "object"
    },
    "name": "cs_report"
  },
  {
    "description": "Cumulative incidence functions for competing risks (Aalen-Johansen). Validation: certified parity evidence.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for the confidence bands.",
          "type": "number"
        },
        "conf_type": {
          "default": "linear",
          "description": "``\"linear\"`` is ``F +/- z se`` clipped to [0, 1]; ``\"log-log\"`` is ``F^exp(+/- z se / (F log F))``, stcompet's bounds.",
          "enum": [
            "linear",
            "log-log"
          ],
          "type": "string"
        },
        "duration": {
          "description": "Column name for the follow-up time.",
          "type": "string"
        },
        "event": {
          "description": "Column name for the event indicator. ``0`` = censored; ``1, 2, ...`` = competing causes.",
          "type": "string"
        },
        "group": {
          "description": "Column name for a grouping variable. When supplied, CIFs are estimated per group and Gray's K-sample test is reported per cause.",
          "type": "string"
        },
        "rho": {
          "default": 0.0,
          "description": "Power of the ``(1 - F(t-))^rho`` weight in Gray's test (cmprsk's ``rho``).",
          "type": "number"
        },
        "variance": {
          "default": "delta",
          "description": "Pointwise variance of the CIF. ``\"delta\"`` is the Marubini-Valsecchi delta-method estimator (the one Stata's ``stcompet`` reports); ``\"gray\"`` is Gray's asymptotic variance, the ``var`` component of R ``cmprsk::cuminc``.",
          "enum": [
            "delta",
            "gray"
          ],
          "type": "string"
        }
      },
      "required": [
        "duration",
        "event"
      ],
      "type": "object"
    },
    "name": "cuminc"
  },
  {
    "description": "CUSUM test for parameter stability. Validation: certified parity evidence.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "x": {
          "description": "Regressors.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Dependent variable.",
          "type": "string"
        }
      },
      "required": [
        "y"
      ],
      "type": "object"
    },
    "name": "cusum_test"
  },
  {
    "description": "Declare a causal DAG and perform identification analysis: backdoor/frontdoor adjustment sets, d-separation, path enumeration, bad controls detection, variable role classification, do-operator. Validation: validated evidence tier (known-truth, reference, external-parity, or Monte Carlo artifact). Assumptions: The graph is acyclic and contains the relevant common causes; Adjustment-set validity depends on the supplied graph being substantively correct. Pre-conditions: Nodes and directed edges encode a substantive causal model; Treatment and outcome nodes are named consistently. Failure modes: No valid adjustment set or cycle detected -> Inspect graph structure, remove cycles, or use sensitivity analysis for unobserved common causes. Alternatives: sp.identify, sp.dag_recommend_estimator, sp.swig. Typical minimum N: 1.",
    "input_schema": {
      "properties": {
        "spec": {
          "description": "Edge spec: \"Z -> X; Z -> Y; X -> Y\"",
          "type": "string"
        }
      },
      "required": [
        "spec"
      ],
      "type": "object"
    },
    "name": "dag"
  },
  {
    "description": "Load a classic textbook DAG: confounding, collider, mediation, discrimination, movie_star, police, frontdoor, bad_control_earnings, m_bias.",
    "input_schema": {
      "properties": {
        "name": {
          "description": "Example name, e.g. 'discrimination'",
          "type": "string"
        }
      },
      "required": [
        "name"
      ],
      "type": "object"
    },
    "name": "dag_example"
  },
  {
    "description": "Das Gupta (1993) multi-factor decomposition. Validation: certified parity evidence.",
    "input_schema": {
      "properties": {
        "by": {
          "description": "Column(s) identifying the stratum of each row, used to pair the rows of ``data_a`` and ``data_b``. Both frames must contain the same strata. Without ``by`` the rows are paired by position, which requires equal lengths.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "data_a": {
          "description": "One row per stratum (e.g. age group). The aggregate for each population is ``R = sum_i prod_f factor_{f,i}``; with a single row it is simply ``prod_f factor_f``.",
          "type": "string"
        },
        "data_b": {
          "description": "One row per stratum (e.g. age group). The aggregate for each population is ``R = sum_i prod_f factor_{f,i}``; with a single row it is simply ``prod_f factor_f``.",
          "type": "string"
        },
        "factor_names": {
          "description": "factor_names parameter (Sequence[str]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "required": [
        "data_a",
        "data_b",
        "factor_names"
      ],
      "type": "object"
    },
    "name": "das_gupta"
  },
  {
    "description": "Triple Differences (DDD). Extends 2x2 DID with a within-unit subgroup comparison to eliminate additional confounders. method='3wfe' (default) is the triple-interaction regression; covariates there are additive and do not identify the covariate-adjusted ATT, so method='dr'/'reg'/'ipw' with id= routes to the conditional estimators of sp.ddd_heterogeneous. Validation: certified parity evidence. Assumptions: Parallel trends in the DDD differential (weaker than DID PT); No anticipation; SUTVA. Pre-conditions: treat x time x subgroup variation exists; subgroup is binary and meaningful within the treatment group; method != '3wfe' also needs id= and both periods per unit. Failure modes: Staggered adoption with heterogeneous effects -> Textbook DDD can carry negative weights with staggered timing. Use sp.ddd_heterogeneous, the Ortiz-Villavicencio and Sant'Anna (2025) estimators pinned against R triplediff; covariates= passed with the default method='3wfe' -> Additive covariates do not identify the covariate-adjusted ATT. Pass method='dr' with id=. Alternatives: sp.ddd_heterogeneous, sp.did_2x2, sp.callaway_santanna. Typical minimum N: 100.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "cluster": {
          "description": "Cluster variable for standard errors",
          "type": "string"
        },
        "covariates": {
          "description": "Covariates. Under method='3wfe' they enter additively and do NOT identify the covariate-adjusted ATT (the caveat is recorded in model_info['diagnostics']); for conditional-parallel-trends DDD use method='dr' with id=.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "id": {
          "description": "Unit identifier; required by every method other than '3wfe', which pairs each unit's two periods",
          "type": "string"
        },
        "method": {
          "default": "3wfe",
          "description": "'3wfe' is the triple-interaction regression; 'dr' / 'reg' / 'ipw' hand the design to sp.ddd_heterogeneous, whose covariate adjustment identifies the DDD ATT under conditional parallel trends",
          "enum": [
            "3wfe",
            "dr",
            "reg",
            "ipw"
          ],
          "type": "string"
        },
        "robust": {
          "default": true,
          "description": "HC1 heteroskedasticity-robust standard errors.",
          "type": "boolean"
        },
        "subgroup": {
          "description": "Binary affected-subgroup indicator (1=affected, 0=unaffected)",
          "type": "string"
        },
        "time": {
          "description": "Binary time period indicator",
          "type": "string"
        },
        "treat": {
          "description": "Binary treatment group indicator",
          "type": "string"
        },
        "weights": {
          "description": "Analytical weights column, as Stata's [aweight=...].",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable",
          "type": "string"
        }
      },
      "required": [
        "subgroup",
        "time",
        "treat",
        "y"
      ],
      "type": "object"
    },
    "name": "ddd"
  },
  {
    "description": "Heterogeneity-robust triple differences (DDD) for staggered adoption. Decomposes DDD into per-(cohort, time) cells via a Callaway-Sant'Anna-style aggregation, with the unaffected subgroup's DID as a placebo. Avoids the negative-weight issue that textbook TWFE DDD inherits from TWFE DID (Goodman-Bacon 2021 analogue). Validation: certified evidence with scoped limitations. Known limitations: the placebo joint test is only produced on the bootstrap path; se='analytic' reports None for it, because that test needs the joint covariance of the placebo arms rather than of the DDD; control_group='notyettreated' is only partially comparable to triplediff 0.2.4: its per-control-cohort estimates agree exactly, but the reference misindexes the influence functions it combines, so the combined numbers differ by convention on cells where the comparison does not span the whole panel; the aggregation convention differs from triplediff::agg_ddd(type='simple'): the default weights cohorts by treated-eligible units; pass weight_by='cohort' to match the R package. Assumptions: Parallel trends relaxed to: same differential trend across treated vs never-treated, within both affected and unaffected subgro...",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "cohort": {
          "description": "First-treatment period (never_value = never-treated)",
          "type": "string"
        },
        "control_group": {
          "default": "nevertreated",
          "description": "Control units: never-treated, or not-yet-treated cohorts combined by minimum distance (see the docstring warning -- that path deliberately diverges from triplediff 0.2.4, which misindexes its influence functions there)",
          "enum": [
            "nevertreated",
            "notyettreated"
          ],
          "type": "string"
        },
        "est_method": {
          "default": "dr",
          "description": "Nuisance combination: doubly robust, inverse probability weighting, or outcome regression",
          "enum": [
            "dr",
            "ipw",
            "reg"
          ],
          "type": "string"
        },
        "n_boot": {
          "default": 500,
          "description": "Number of bootstrap replications.",
          "type": "integer"
        },
        "never_value": {
          "default": 0,
          "description": "Value in cohort for never-treated units",
          "type": "string"
        },
        "se": {
          "description": "'analytic' influence-function variance (exact, and what triplediff reports) or 'bootstrap' clustered on unit (the only path that fills in placebo_joint_test). Defaults to bootstrap without covariates, analytic with them",
          "enum": [
            "analytic",
            "bootstrap"
          ],
          "type": "string"
        },
        "seed": {
          "description": "Random seed for reproducible stochastic steps.",
          "type": "integer"
        },
        "subgroup": {
          "description": "Binary within-group subgroup indicator (1=affected, 0=placebo)",
          "type": "string"
        },
        "time": {
          "description": "Time period column.",
          "type": "string"
        },
        "unit": {
          "description": "Unit identifier column.",
          "type": "string"
        },
        "weight_by": {
          "default": "eligible",
          "description": "Aggregation weights: 'eligible' (treated units in the affected subgroup) or 'cohort' (whole cohort, both subgroups -- reproduces triplediff::agg_ddd)",
          "enum": [
            "eligible",
            "cohort"
          ],
          "type": "string"
        },
        "x": {
          "description": "Base-period covariates; identification becomes CONDITIONAL DDD parallel trends",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "cohort",
        "subgroup",
        "time",
        "unit",
        "y"
      ],
      "type": "object"
    },
    "name": "ddd_heterogeneous"
  },
  {
    "description": "Unified entry point for all decomposition methods. Validation: certified parity evidence. Assumptions: Overlapping support of covariates across groups (reweighting / RIF methods are invalid outside overlap); Linearity assumption holds for Oaxaca-Blinder-type decompositions; non-linear methods (FFL/DFL/Machado-Mata) relax this; Conditional independence of group membership for causal interpretation (otherwise: descriptive decomposition only). Pre-conditions: Data contains a binary or categorical group indicator with both groups represented; Outcome and covariates are numeric (or properly encoded) and finite; Sample sizes per group are large enough to estimate group-specific moments (rule of thumb: each group >= 100). Failure modes: Trimming warning at common-support boundaries -> Inspect propensity-score support; restrict the analysis sample or use bounds; RIF coefficients explode at distribution tails -> Use higher-bandwidth kernel density, restrict quantile range, or switch to FFL. Alternatives: sp.dfl_decompose, sp.ffl_decompose, sp.oaxaca, sp.rif_decomposition. Typical minimum N: 200.",
    "input_schema": {
      "properties": {
        "method": {
          "description": "One of the methods listed in ``available_methods()``. Aliases are supported (e.g. 'mm' -> 'machado_mata').",
          "type": "string"
        }
      },
      "required": [
        "method"
      ],
      "type": "object"
    },
    "name": "decompose"
  },
  {
    "description": "Return the within-transformed ``x`` and the singleton keep mask. Validation: certified parity evidence.",
    "input_schema": {
      "properties": {
        "drop_singletons": {
          "default": true,
          "description": "drop_singletons parameter (bool).",
          "type": "boolean"
        },
        "fe": {
          "description": "fe parameter (Union[pd.DataFrame, np.ndarray, None]).",
          "type": "string"
        },
        "maxiter": {
          "default": 10000,
          "description": "maxiter parameter (int).",
          "type": "integer"
        },
        "slopes": {
          "description": "slopes parameter (Optional[Sequence[SlopeSpec]]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "solver": {
          "default": "map",
          "description": "solver parameter (str).",
          "type": "string"
        },
        "tol": {
          "default": 1e-08,
          "description": "Numerical convergence tolerance.",
          "type": "number"
        },
        "weights": {
          "description": "Observation weights.",
          "type": "string"
        },
        "x": {
          "description": "Primary running variable, regressor, or feature input for this estimator.",
          "type": "string"
        }
      },
      "required": [
        "fe",
        "x"
      ],
      "type": "object"
    },
    "name": "demean"
  },
  {
    "description": "De-meaned / De-trended Synthetic Control Method. Validation: certified parity evidence. Assumptions: A convex (or regularized) combination of donor units reproduces the treated unit's pre-treatment outcome path; No interference: the treatment does not affect the donor units (SUTVA); No anticipation before the treatment date. Pre-conditions: Panel of one or more treated units plus an untreated donor pool, observed over time; Pre-treatment window long enough to fit donor weights (rule of thumb: more pre-periods than donors used); Outcome observed for every unit in every period. Failure modes: Large pre-treatment RMSPE -- the synthetic unit fails to track the treated unit before treatment -> Add donors / predictors, lengthen the pre-period, or use a bias-corrected estimator (sdid, augsynth); Placebo / permutation inference shows the estimate is not extreme relative to donors -> Report the placebo distribution honestly; the effect may not be distinguishable from noise. Alternatives: sp.sdid, sp.augsynth, sp.gsynth, sp.callaway_santanna. Typical minimum N: 15.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level.",
          "type": "number"
        },
        "covariates": {
          "description": "Not supported: the weights are fitted on (de-meaned / de-trended) pre-treatment outcomes only. Passing covariates raises ``NotImplementedError`` (it used to be silently ignored).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "outcome": {
          "description": "Outcome variable name.",
          "type": "string"
        },
        "penalization": {
          "default": 0.0,
          "description": "Ridge penalty on weights.",
          "type": "number"
        },
        "placebo": {
          "default": true,
          "description": "Run in-space placebo inference.",
          "type": "boolean"
        },
        "time": {
          "description": "Time period column.",
          "type": "string"
        },
        "treated_unit": {
          "description": "Identifier of the treated unit.",
          "type": "string"
        },
        "treatment_time": {
          "description": "First treatment period (inclusive).",
          "type": "string"
        },
        "unit": {
          "description": "Unit identifier column.",
          "type": "string"
        },
        "variant": {
          "default": "demeaned",
          "description": "* ``'demeaned'`` -- subtract unit-level pre-treatment means. * ``'detrended'`` -- subtract unit-level linear time trends.",
          "enum": [
            "demeaned",
            "detrended"
          ],
          "type": "string"
        }
      },
      "required": [
        "outcome",
        "time",
        "treated_unit",
        "treatment_time",
        "unit"
      ],
      "type": "object"
    },
    "name": "demeaned_synth"
  },
  {
    "description": "Route design facts to a method-selection status.",
    "input_schema": {
      "properties": {
        "assignment": {
          "description": "assignment parameter (Optional[str]).",
          "type": "string"
        },
        "controls": {
          "description": "Control-variable column names.",
          "type": "string"
        },
        "data_topology": {
          "description": "data_topology parameter (Optional[str]).",
          "type": "string"
        },
        "estimand": {
          "description": "estimand parameter (Optional[str]).",
          "type": "string"
        },
        "identification_support": {
          "description": "identification_support parameter (Optional[str]).",
          "type": "string"
        },
        "needs": {
          "description": "needs parameter (Optional[str]).",
          "type": "string"
        }
      },
      "required": [],
      "type": "object"
    },
    "name": "design_intake"
  },
  {
    "description": "Design-robust event study with explicit negative-weight diagnostics per cohort x relative-time cell. Reports which event-study coefficients receive negative weights in TWFE and flags the affected horizons. Assumptions: Parallel trends across cohorts; treatment effects may be heterogeneous across cohort and time; No anticipation prior to the event time; Implicit TWFE comparison weights are non-negative (negative-weight contamination is diagnosed, not assumed away). Pre-conditions: Long-format panel with y, treat, time, id (same conventions as callaway_santanna); Staggered/variable treatment timing so the per-(cohort, time) weight diagnostic is meaningful; Event-time window (leads, lags) contained within observed pre/post coverage. Failure modes: model_info weights show large negative TWFE weights flagging forbidden comparisons -> Drop already-treated controls and use a heterogeneity-robust staggered estimator instead of TWFE; Too few treated units per cohort-time cell to identify weights or SEs -> Coarsen the event-time window or pool cohorts to raise per-cell counts. Alternatives: sp.sun_abraham, sp.bacon_decomposition, sp.cohort_anchored_event_study, sp.callaway_santanna. Typical...",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "cluster": {
          "description": "Cluster identifier column for clustered standard errors.",
          "type": "string"
        },
        "id": {
          "description": "Unit, subject, or panel identifier column.",
          "type": "string"
        },
        "lags": {
          "default": 4,
          "description": "lags parameter (int).",
          "type": "integer"
        },
        "leads": {
          "default": 4,
          "description": "leads parameter (int).",
          "type": "integer"
        },
        "time": {
          "description": "Time period column.",
          "type": "string"
        },
        "treat": {
          "description": "Treatment indicator or first-treatment-period column.",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "id",
        "time",
        "treat",
        "y"
      ],
      "type": "object"
    },
    "name": "design_robust_event_study"
  },
  {
    "description": "Auto-detect the study design (panel / cross-section / RD / IV-style) from column shapes and types. Returns the guessed design plus the columns that drove the inference. Call this BEFORE recommend() when the user pastes a CSV with no context.",
    "input_schema": {
      "properties": {
        "id_col_hint": {
          "type": "string"
        },
        "time_col_hint": {
          "type": "string"
        }
      },
      "required": [],
      "type": "object"
    },
    "name": "detect_design"
  },
  {
    "description": "DFL (1996) reweighting decomposition at a chosen distributional statistic. Validation: certified parity evidence. Assumptions: DiNardo-Fortin-Lemieux reweighting: ignorable group assignment given covariates; Propensity-score model is correctly specified for the reweighting kernel; Common support across groups (no extrapolation beyond observed covariate range). Pre-conditions: Binary group indicator with sufficient overlap on covariates; Outcome distribution to decompose is continuous (typically log-wage). Failure modes: Extreme propensity-score weights inflate variance -> Trim or stabilize weights, or restrict to the common-support region. Alternatives: sp.ffl_decompose, sp.oaxaca, sp.machado_mata. Typical minimum N: 500.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "group": {
          "description": "Group or cohort identifier.",
          "type": "string"
        },
        "inference": {
          "default": "analytical",
          "description": "inference parameter (str).",
          "enum": [
            "bootstrap",
            "analytical"
          ],
          "type": "string"
        },
        "n_boot": {
          "default": 299,
          "description": "Number of bootstrap replications.",
          "type": "integer"
        },
        "quantile_grid": {
          "description": "If provided, also compute quantile-process decomposition on this grid.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "reference": {
          "default": 0,
          "description": "- 0: reweight Group B to look like A's X (default). The counterfactual is F_{Y<1|0>} -- A's X distribution with B's outcome structure. - 1: reweight Group A to look like B's X. The counterfactual is F_{Y<0|1>} -- B's X distribution with A's outcome structure. .. warning:: ``reference`` has different economic semantics across method families. In DFL, ``reference=0`` yields cf = *A's X, B's beta* (reweighting approach). In ``machado_mata`` / ``melly`` / ``cfm``, ``reference=0`` yields cf = *A's beta, B's X* (coefficient-substitution approach). These are **opposite** counterfactual constructions. Within each method labels are internally consistent (DFL structure = A - cf; MM composition = A - cf). When comparing estimates across methods, read the per-method docstrings carefully.",
          "enum": [
            "0",
            "1"
          ],
          "type": "integer"
        },
        "seed": {
          "default": 12345,
          "description": "Random seed for reproducible stochastic steps.",
          "type": "integer"
        },
        "stat": {
          "default": "mean",
          "description": "stat parameter (str).",
          "enum": [
            "mean",
            "variance",
            "std",
            "quantile",
            "iqr",
            "gini",
            "log_var"
          ],
          "type": "string"
        },
        "stat_convention": {
          "default": "statspai",
          "description": "Weighted variance / quantile definition for the reweighted counterfactual (see ``_common.statistic_value``). ``'hmisc'`` reproduces ``ddecompose::dfl_decompose``, which uses ``Hmisc::wtd.var`` and ``Hmisc::wtd.quantile``; the reweighting itself is identical under both. ``stat='gini'`` is the exact plug-in Gini either way; ``ddecompose`` integrates the Lorenz curve numerically and differs from it in the fourth significant digit.",
          "enum": [
            "statspai",
            "hmisc"
          ],
          "type": "string"
        },
        "tau": {
          "default": 0.5,
          "description": "Quantile level or target treatment-effect index.",
          "type": "number"
        },
        "trim": {
          "default": 0.001,
          "description": "trim parameter (float).",
          "type": "number"
        },
        "weights": {
          "description": "Observation weights.",
          "type": "string"
        },
        "x": {
          "description": "Primary running variable, regressor, or feature input for this estimator.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "group",
        "x",
        "y"
      ],
      "type": "object"
    },
    "name": "dfl_decompose"
  },
  {
    "description": "Comprehensive regression diagnostics in one call.",
    "input_schema": {
      "properties": {
        "print_results": {
          "default": true,
          "description": "Print formatted output.",
          "type": "boolean"
        },
        "x": {
          "description": "Independent variable names (excluding constant).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Dependent variable name.",
          "type": "string"
        }
      },
      "required": [
        "x",
        "y"
      ],
      "type": "object"
    },
    "name": "diagnose"
  },
  {
    "description": "Method-aware diagnostic battery: auto-selects tests by model type (OLS/DID/RDD/IV/SCM). Assumptions: The result object carries a recognizable method_type so the correct diagnostic battery can be routed; Each sub-check (e.g. parallel-trends, weak-IV, overid, balance) is only valid under that method's own identifying assumptions; Tests use the supplied alpha as the significance threshold; p-values are interpreted, not corrected for multiplicity. Pre-conditions: A fitted EconometricResults or CausalResult from a StatsPAI estimator; The estimator must expose enough fitted internals (residuals, first-stage, design info) for its checks. Failure modes: Passed a raw DataFrame, dict, or estimate float instead of a fitted result object -> Fit an estimator first and pass the returned result object, not the input data; Method type is unrecognized so no diagnostic battery applies and 'checks' comes back empty -> Run the method-appropriate standalone diagnostic directly instead of the router. Alternatives: sp.unified_sensitivity, sp.sensemakr, sp.oster_bounds, sp.spec_curve. Typical minimum N: 30.",
    "input_schema": {
      "properties": {
        "result": {
          "description": "Fitted result from any StatsPAI estimator",
          "type": "string"
        }
      },
      "required": [
        "result"
      ],
      "type": "object"
    },
    "name": "diagnose_result"
  },
  {
    "description": "Fit a classic 2-period 2-group difference-in-differences. Pass treatment / time / post column names. For staggered adoption across many cohorts use callaway_santanna instead. Validation: validated evidence tier (known-truth, reference, external-parity, or Monte Carlo artifact).",
    "input_schema": {
      "properties": {
        "post": {
          "description": "Binary post-treatment period indicator",
          "type": "string"
        },
        "time": {
          "description": "Time column",
          "type": "string"
        },
        "treat": {
          "description": "Binary treatment-group indicator",
          "type": "string"
        },
        "y": {
          "description": "Outcome column",
          "type": "string"
        }
      },
      "required": [
        "y",
        "treat",
        "time"
      ],
      "type": "object"
    },
    "name": "did"
  },
  {
    "description": "Gardner (2022) two-stage DID estimator. Validation: certified parity evidence. Assumptions: Conditional parallel trends between treated and comparison groups absent treatment; No anticipation of treatment before its onset; Treatment effects may be heterogeneous across cohorts and time (no homogeneity required). Pre-conditions: Panel or repeated cross-section with a unit (or group) identifier and a time identifier; At least one never-treated or not-yet-treated comparison group; Pre-treatment periods to assess parallel trends. Failure modes: Pre-treatment event-study coefficients are jointly non-zero (pre-trend violation) -> Use honest DiD bounds to quantify robustness to trend violations, or condition on covariates; Two-way fixed-effects estimate is contaminated by 'forbidden' comparisons / negative weights -> Use a heterogeneity-robust estimator (Callaway-Sant'Anna, Borusyak et al., Gardner two-stage); Pre-trend joint test p < 0.05 (or underpowered at 0.10) -> Use sp.sensitivity_rr (Rambachan & Roth honest CI) or switch to sp.callaway_santanna. Alternatives: sp.callaway_santanna, sp.did, sp.honest_did, sp.sun_abraham. Typical minimum N: 100.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Two-sided CI level.",
          "type": "number"
        },
        "boot_seed": {
          "default": 0,
          "description": "Seed for the cluster bootstrap (deterministic results).",
          "type": "integer"
        },
        "cluster": {
          "description": "Cluster variable for the standard errors. Defaults to ``group``. At least two clusters are required.",
          "type": "string"
        },
        "controls": {
          "description": "Additional covariates included in Stage 1.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "event_study": {
          "default": false,
          "description": "If True, Stage 2 reports coefficients by relative time ``k = t - first_treat_i``.",
          "type": "boolean"
        },
        "first_treat": {
          "description": "First-treatment-period column. Never-treated units should be encoded as ``0``, ``NaN``, or ``+inf``.",
          "type": "string"
        },
        "group": {
          "description": "Unit (panel-id) column.",
          "type": "string"
        },
        "horizon": {
          "description": "Relative-time leads/lags to report when ``event_study=True``; defaults to ``range(-5, 6)`` intersected with available support. Each reported coefficient is the (weighted) mean of the residualised outcome at that relative time; the Stage-2 regression has no intercept, so the set of horizons requested does not change any individual coefficient or its standard error.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "n_boot": {
          "default": 199,
          "description": "Number of cluster-bootstrap replications when ``vce='bootstrap'``.",
          "type": "integer"
        },
        "time": {
          "description": "Time column.",
          "type": "string"
        },
        "vce": {
          "default": "analytic",
          "description": "Standard-error mode. ``'analytic'`` is the two-stage corrected clustered variance of Butts & Gardner (2022): the Stage-2 influence function is adjusted for the estimation of the Stage-1 fixed effects (see the module docstring), which is what R ``did2s`` and Stata ``did2s`` report and what StatsPAI reproduces to ~1e-8 relative. ``'bootstrap'`` resamples whole clusters and re-runs the full two-step procedure. ``'none'`` skips inference (``se``, ``ci`` and ``pvalue`` are NaN); it is what the bootstrap uses internally. Point estimates are identical in every mode.",
          "enum": [
            "analytic",
            "bootstrap"
          ],
          "type": "string"
        },
        "weights": {
          "description": "Column of strictly positive estimation weights applied to both stages (weighted least squares), the counterpart of R ``did2s(weights=)`` and Stata ``did2s [aw=]``. In event-study mode the overall ATT is the weight-share average of the post-treatment coefficients.",
          "type": "string"
        },
        "y": {
          "description": "Outcome column name.",
          "type": "string"
        }
      },
      "required": [
        "first_treat",
        "group",
        "time",
        "y"
      ],
      "type": "object"
    },
    "name": "did_2stage"
  },
  {
    "description": "Canonical 2x2 DID: two groups (treated / control) x two periods (pre / post). Point estimate via either group-means differencing or OLS on the treat x post interaction; optional covariates, robust / cluster SE, and sample weights. Validation: certified parity evidence. Do NOT use when: treatment timing is staggered across units -- the TWFE 2x2 estimate is a negative-weighted mix (Goodman-Bacon 2021); use sp.callaway_santanna, sp.sun_abraham or sp.did_imputation. Assumptions: Parallel trends; No anticipation; SUTVA (no spillovers). Pre-conditions: data has exactly two time periods (pre, post); treat is 0/1 constant within unit (unit-level, not time-varying); at least a handful of treated and control units. Failure modes: Staggered timing (> 2 periods with varying treat start) -> Use sp.callaway_santanna / sp.sun_abraham / sp.did_imputation; Very few clusters at the group level -> Use wild cluster bootstrap via sp.wild_cluster_bootstrap; Pre-trend joint test p < 0.05 (or underpowered at 0.10) -> Use sp.sensitivity_rr (Rambachan & Roth honest CI) or switch to sp.callaway_santanna. Alternatives: sp.drdid, sp.did_analysis, sp.callaway_santanna, sp.sun_abraham. Typical minimum N: 30.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "cluster": {
          "description": "Column for cluster-robust SE (defaults to treat)",
          "type": "string"
        },
        "covariates": {
          "description": "Covariates included additively; for DR use sp.drdid",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "robust": {
          "default": true,
          "description": "Heteroskedasticity-robust SE when no cluster provided",
          "type": "boolean"
        },
        "time": {
          "description": "Time / period indicator",
          "type": "string"
        },
        "treat": {
          "description": "Binary treatment-group indicator (0/1)",
          "type": "string"
        },
        "weights": {
          "description": "Optional column name for sampling weights",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable",
          "type": "string"
        }
      },
      "required": [
        "time",
        "treat",
        "y"
      ],
      "type": "object"
    },
    "name": "did_2x2"
  },
  {
    "description": "Workflow wrapper that runs a full DiD pipeline: auto-detects 2x2 vs. staggered, runs the right estimator (CS by default), optionally runs Bacon decomposition, event study, and Rambachan-Roth sensitivity, and aggregates into a DIDAnalysis report object. Assumptions: Parallel trends between treated and control (2x2) or across cohorts (staggered) after any covariate adjustment; No anticipation: pre-treatment outcomes unaffected by future treatment; SUTVA / no interference across units; correct design auto-detection (2x2 vs staggered). Pre-conditions: Panel or repeated cross-section with y, treat, time; id required for staggered designs; treat is binary 0/1 for 2x2, or first-treatment period (0 = never-treated) for staggered; event_window within observed periods when run_event_study is enabled. Failure modes: Staggered design detected but no id column supplied, so cohorts cannot be formed -> Pass the unit identifier via id= so first-treatment cohorts can be inferred; Event-study pre-trend test rejects parallel trends in the bundled report -> Inspect the included honest_did sensitivity output and report bounds rather than the point ATT; Requested method incompatible with the detected d...",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "cluster": {
          "description": "Cluster variable for the SEs.",
          "type": "string"
        },
        "control_group": {
          "default": "nevertreated",
          "description": "For the CS / SA estimators.",
          "enum": [
            "nevertreated",
            "notyettreated"
          ],
          "type": "string"
        },
        "covariates": {
          "description": "Control variables.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "estimator": {
          "default": "dr",
          "description": "For CS: doubly robust, IPW, or outcome regression.",
          "enum": [
            "dr",
            "ipw",
            "reg"
          ],
          "type": "string"
        },
        "event_window": {
          "description": "Event-study window, e.g. (-5, 5); auto-detected if None.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "id": {
          "description": "Unit, subject, or panel identifier column.",
          "type": "string"
        },
        "method": {
          "default": "auto",
          "description": "Estimator selection",
          "enum": [
            "auto",
            "2x2",
            "cs",
            "sa",
            "sdid"
          ],
          "type": "string"
        },
        "robust": {
          "default": true,
          "description": "HC1 robust standard errors.",
          "type": "boolean"
        },
        "run_bacon": {
          "default": true,
          "description": "run_bacon parameter (bool).",
          "type": "boolean"
        },
        "run_event_study": {
          "default": true,
          "description": "run_event_study parameter (bool).",
          "type": "boolean"
        },
        "run_sensitivity": {
          "default": true,
          "description": "run_sensitivity parameter (bool).",
          "type": "boolean"
        },
        "time": {
          "description": "Time period column.",
          "type": "string"
        },
        "treat": {
          "description": "Binary treatment or first-treat column",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "id",
        "time",
        "treat",
        "y"
      ],
      "type": "object"
    },
    "name": "did_analysis"
  },
  {
    "description": "Covariate balance for a DiD design, in the shape Baker et al. (2026, Table 4) report it: Imbens-Rubin normalized differences computed twice -- once on baseline covariate LEVELS and once on covariate CHANGES across the treatment date -- optionally weighted and unweighted side by side. The changes panel is the informative half: DiD identifies off trends, so a covariate that is balanced in levels can still be moving differentially, and imbalances routinely flip sign between the two panels. Flags |norm. diff| > 0.25. Evidence about whether UNCONDITIONAL parallel trends is plausible; it cannot test parallel trends itself. Validation: validated evidence tier (known-truth, reference, external-parity, or Monte Carlo artifact). Known limitations: pooled multi-cohort balance is not implemented: one table per treated cohort only, because the normalized difference is a two-group statistic; only the reliability-weight variance correction is implemented for the weighted panel; survey-design (replicate-weight) variances are not supported; inference is not implemented: the normalized difference is reported as a descriptive effect size with no standard error or test, by design; the weighted denomi...",
    "input_schema": {
      "properties": {
        "base_period": {
          "description": "Pre-treatment period for the levels panel (default g-1)",
          "type": "string"
        },
        "cohort": {
          "description": "Treated cohort to audit (default: the largest)",
          "type": "string"
        },
        "comparison_period": {
          "description": "Second period for the changes panel (default g)",
          "type": "string"
        },
        "control_group": {
          "default": "nevertreated",
          "description": "Must match the comparison group of the estimator you intend to run",
          "enum": [
            "nevertreated",
            "notyettreated"
          ],
          "type": "string"
        },
        "covariates": {
          "description": "Columns to audit in levels and in changes",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "g": {
          "description": "First-treatment period (0 = never treated)",
          "type": "string"
        },
        "i": {
          "description": "Unit identifier",
          "type": "string"
        },
        "t": {
          "description": "t parameter (str).",
          "type": "string"
        },
        "threshold": {
          "default": 0.25,
          "description": "Imbens & Rubin (2015, p. 277) rule of thumb",
          "type": "number"
        },
        "weights": {
          "description": "Unit weights; when given, weighted and unweighted statistics are reported side by side because they describe different populations",
          "type": "string"
        }
      },
      "required": [
        "covariates",
        "g",
        "i",
        "t"
      ],
      "type": "object"
    },
    "name": "did_balance"
  },
  {
    "description": "DiD with a BCF-style forest on long-differenced outcomes: each cohort is compared with never-treated units over its own pre and post periods, and a prognostic forest plus treatment-effect booster (bootstrap uncertainty, not MCMC) is fitted to the long difference. For heterogeneous effects with group-time inference prefer sp.did_forest. Assumptions: Parallel trends: treated and control groups would have followed the same trajectory absent treatment; No anticipation: outcomes in pre-treatment periods are unaffected by future treatment; SUTVA: no spillovers between units. Pre-conditions: data is panel or repeated cross-section with a time column; treat column is binary (0/1) for 2x2, or first-treatment-period (int) for staggered; at least one pre-treatment period (>= 2 periods for 2x2; >= 3 recommended for event study). Failure modes: Pre-trend joint test p < 0.05 (or underpowered at 0.10) -> Use sp.sensitivity_rr (Rambachan & Roth honest CI) or switch to sp.callaway_santanna; Staggered treatment timing with TWFE method -> TWFE can give negative weights; use Callaway-Sant'Anna, Sun-Abraham, or BJS imputation; Pre-trend test underpowered (Roth 2022) -> Check sp.pretrends_power -- if l...",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "covariates": {
          "description": "Covariate matrix, DataFrame, or column names.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "id": {
          "description": "Unit, subject, or panel identifier column.",
          "type": "string"
        },
        "n_bootstrap": {
          "default": 100,
          "description": "Bootstrap replications of the BCF fit per cohort (covariate path).",
          "type": "integer"
        },
        "n_trees": {
          "default": 50,
          "description": "Number of trees.",
          "type": "integer"
        },
        "seed": {
          "description": "Random seed for reproducible stochastic steps.",
          "type": "integer"
        },
        "time": {
          "description": "Time period column.",
          "type": "string"
        },
        "treat": {
          "description": "Treatment indicator or first-treatment-period column.",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "id",
        "time",
        "treat",
        "y"
      ],
      "type": "object"
    },
    "name": "did_bcf"
  },
  {
    "description": "Calibrated placebo simulation for DiD estimator selection: strip the estimated effect out of the observed panel, redraw the adoption pattern, inject a known effect, and refit every candidate estimator. Returns bias, RMSE, coverage and rejection rate with Monte Carlo standard errors, so the choice of estimator is settled on this panel rather than by citation. Assumptions: The redrawn assignment is random, so parallel trends holds by construction; the table measures estimator behaviour, not the credibility of the design; The calibration removes the horizon-averaged estimated effect, not cell-level deviations from it; The target is the treated-observation average of the injected effect; a heterogeneous effect makes estimand differences show up as bias. Pre-conditions: one row per (unit, period); cohort constant within a unit; at least two never-treated units to redraw an assignment against; a balanced panel for resample='units'. Failure modes: Fewer than two never-treated units, so a redrawn assignment has no comparison group -> Extend the panel with untreated units, or study the design with sp.did_few_treated instead; resample='units' on an unbalanced panel -> Use resample='wild' or...",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "One minus the nominal level",
          "type": "number"
        },
        "assignment": {
          "default": "resample_cohorts",
          "description": "How the adoption pattern is redrawn",
          "enum": [
            "resample_cohorts",
            "random_timing",
            "observed"
          ],
          "type": "string"
        },
        "calibrate": {
          "default": "imputation",
          "description": "Removal of the observed effect before injecting a known one",
          "enum": [
            "imputation",
            "none"
          ],
          "type": "string"
        },
        "cohort": {
          "description": "Period of first treatment; 0/NaN/inf = never",
          "type": "string"
        },
        "control_group": {
          "default": "nevertreated",
          "description": "Comparison group handed to the estimators that take one",
          "enum": [
            "nevertreated",
            "notyettreated"
          ],
          "type": "string"
        },
        "effect": {
          "default": 0.0,
          "description": "Effect injected into treated cells (0 = a size study); a callable of the horizon, or of (cohort, period), gives a heterogeneous one",
          "type": "number"
        },
        "estimators": {
          "description": "Candidates to score",
          "enum": [
            "twfe",
            "callaway_santanna",
            "sun_abraham",
            "did_imputation",
            "gardner_did",
            "etwfe",
            "did_multiplegt_dyn",
            "stacked_did",
            "lp_did"
          ],
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "id": {
          "description": "Unit identifier",
          "type": "string"
        },
        "n_jobs": {
          "default": 1,
          "description": "Replications in parallel (-1 = all CPUs)",
          "type": "integer"
        },
        "n_sims": {
          "default": 200,
          "description": "Replications",
          "type": "integer"
        },
        "resample": {
          "default": "units",
          "description": "Outcome noise: unit residual paths, a unit-level Rademacher multiplier, or none",
          "enum": [
            "units",
            "wild",
            "none"
          ],
          "type": "string"
        },
        "seed": {
          "default": 0,
          "description": "Base seed; draw k uses seed + k",
          "type": "integer"
        },
        "time": {
          "description": "Period column",
          "type": "string"
        },
        "y": {
          "description": "Outcome column",
          "type": "string"
        }
      },
      "required": [
        "cohort",
        "id",
        "time",
        "y"
      ],
      "type": "object"
    },
    "name": "did_calibrated_simulation"
  },
  {
    "description": "Count the clusters treatment is assigned at and grade the count against the simulation grid of Ulloa-Perez et al. (2025), who found that at 30 clusters every modern staggered DiD estimator they evaluated under-covered a nominal 95% interval, with coverage improving as clusters accumulated. Thirty is the smallest cell they ran, so fewer clusters is reported as outside their evidence rather than as merely worse. Also reports clusters per cohort, since a group-time effect rests on the clusters in its own cohort. Assumptions: The grading reports what published simulation evidence exists at this cluster count; it is not a power calculation for this design or estimator. Pre-conditions: panel with unit and cohort columns. Failure modes: Cluster column empty or absent -> Check that the cluster column is populated. Alternatives: sp.wild_cluster_bootstrap, sp.ri_test, sp.conley.",
    "input_schema": {
      "properties": {
        "cluster": {
          "description": "Level treatment is assigned at (state, provider group, district). Defaults to unit with a warning: the two coincide only under independent unit-level assignment, and assuming so is the optimistic error.",
          "type": "string"
        },
        "first_treat": {
          "description": "First-treatment period; 0 = never-treated",
          "type": "string"
        },
        "unit": {
          "description": "Unit identifier",
          "type": "string"
        },
        "warn": {
          "default": true,
          "description": "Warn when the design sits in or below the weakest cell of the reference grid.",
          "type": "boolean"
        }
      },
      "required": [
        "first_treat",
        "unit"
      ],
      "type": "object"
    },
    "name": "did_cluster_diagnostics"
  },
  {
    "description": "Report which of Baker, Callaway, Cunningham, Goodman-Bacon & Sant'Anna's (2026) eight forward-engineering steps a fitted DiD result actually pins down: target parameter, identifying assumption, estimation strategy, inference frame, estimate, sensitivity, heterogeneity. A slot the result cannot determine is reported as undetermined rather than filled with a default, because an unstated choice is still a choice the write-up owes the reader. Assumptions: Reports what the result object records; it cannot verify that a recorded assumption is true of the data. Pre-conditions: a fitted DiD result object. Failure modes: Result carries no model_info -> Every step is reported undetermined, which is the informative answer. Alternatives: sp.audit_result, sp.assumption_audit, sp.cs_report.",
    "input_schema": {
      "properties": {
        "result": {
          "description": "Output of a DiD estimator",
          "type": "string"
        }
      },
      "required": [
        "result"
      ],
      "type": "object"
    },
    "name": "did_design_contract"
  },
  {
    "description": "R-style alias: ``synthdid::did_estimate``. Validation: certified parity evidence. Assumptions: Parallel trends in the absence of treatment, after the synthetic/DiD weighting; No anticipation and no interference between units (SUTVA); The control pool's outcome process is stable around the intervention. Pre-conditions: Panel with treated and control units and a clear treatment date; Pre-treatment periods available to assess comparability of trends. Failure modes: Weighted pre-treatment trends still diverge between treated and synthetic control -> Inspect the unit/time weights and pre-trend fit; consider event-study DiD with honest bounds. Alternatives: sp.synth, sp.augsynth, sp.callaway_santanna, sp.gardner_did. Typical minimum N: 15.",
    "input_schema": {
      "properties": {
        "time": {
          "description": "Time period column.",
          "type": "string"
        },
        "treat_time": {
          "description": "treat_time parameter.",
          "type": "string"
        },
        "treat_unit": {
          "description": "treat_unit parameter.",
          "type": "string"
        },
        "unit": {
          "description": "Unit identifier column.",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "time",
        "treat_time",
        "treat_unit",
        "unit",
        "y"
      ],
      "type": "object"
    },
    "name": "did_estimate"
  },
  {
    "description": "Conley-Taber (2011) and Ferman-Pinto (2019) inference for a DiD with few treated groups: the placebo distribution of the coefficient is read off the control groups, the interval is inverted from it, and Ferman-Pinto rescales the draws for the heteroskedasticity unequal group sizes generate. Use when one or a handful of clusters are treated, where the cluster-robust variance over-rejects badly. Assumptions: Group-level errors are independent across groups; Conley-Taber: identically distributed across groups; Ferman-Pinto relaxes this to Var(W) = A + B / M; The point estimate is not consistent with a fixed number of treated groups. Pre-conditions: one row per (unit, time); collapse individual data first; many control groups (at least ten; the asymptotics are in their number). Failure modes: Fewer than ten control groups -> Use wild cluster bootstrap or a design with more treated clusters; ferman_pinto without group_size -> Pass the cell counts, or use method='conley_taber'. Alternatives: sp.wild_cluster_bootstrap, sp.cs_jackknife, sp.cluster_robust_se. Typical minimum N: 100.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "One minus the confidence level",
          "type": "number"
        },
        "covariates": {
          "description": "Covariates partialled out",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "group_size": {
          "description": "Observations behind each cell (required by ferman_pinto)",
          "type": "string"
        },
        "id": {
          "description": "Group identifier",
          "type": "string"
        },
        "max_draws": {
          "default": 10000,
          "description": "Cap on control-group combinations",
          "type": "integer"
        },
        "method": {
          "default": "conley_taber",
          "description": "Placebo construction",
          "enum": [
            "conley_taber",
            "ferman_pinto"
          ],
          "type": "string"
        },
        "null_value": {
          "default": 0.0,
          "description": "Null the p-value tests",
          "type": "number"
        },
        "seed": {
          "default": 0,
          "description": "Seed for random combinations",
          "type": "integer"
        },
        "time": {
          "description": "Period column",
          "type": "string"
        },
        "treat": {
          "description": "0/1 treatment by group and period",
          "type": "string"
        },
        "y": {
          "description": "Outcome column",
          "type": "string"
        }
      },
      "required": [
        "id",
        "time",
        "treat",
        "y"
      ],
      "type": "object"
    },
    "name": "did_few_treated"
  },
  {
    "description": "Difference-in-differences causal forests for staggered adoption: one honest causal forest per clean Callaway-Sant'Anna group-time comparison on the long-differenced outcome, doubly-robust ATT(g,t), influence-function event study and overall ATT, unit-level CATEs and per-cell heterogeneity tests. Validation: validated evidence tier (known-truth, reference, external-parity, or Monte Carlo artifact). Assumptions: Conditional parallel trends given x for the chosen comparison group; No anticipation beyond `anticipation` periods; Overlap: every covariate profile has comparison units. Pre-conditions: absorbing (staggered) treatment coded by first treatment period; covariates are fixed per unit (baseline values); enough treated and comparison units per group-time cell (min_group_size). Failure modes: pretrend_test p-value small -> Pre-period placebos reject parallel trends; add covariates or bound the violation; AssumptionWarning: group-time cells were dropped -> Inspect result.dropped_cells; lower min_group_size or restrict event_window; Pre-trend joint test p < 0.05 (or underpowered at 0.10) -> Use sp.sensitivity_rr (Rambachan & Roth honest CI) or switch to sp.callaway_santanna. Alterna...",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Level for confidence intervals.",
          "type": "number"
        },
        "anticipation": {
          "default": 0,
          "description": "anticipation parameter (int).",
          "type": "integer"
        },
        "clusters": {
          "description": "Cluster column constant within unit.",
          "type": "string"
        },
        "cohort": {
          "description": "First treatment period (0/NaN = never treated).",
          "type": "string"
        },
        "control_group": {
          "default": "notyettreated",
          "description": "control_group parameter (str).",
          "enum": [
            "notyettreated",
            "nevertreated"
          ],
          "type": "string"
        },
        "event_window": {
          "description": "event_window parameter (tuple).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "forest_kwargs": {
          "description": "Extra sp.CausalForest arguments (min_samples_leaf, mtry, honesty_fraction, ...).",
          "type": "object"
        },
        "id": {
          "description": "Unit id column; unit= is an alias.",
          "type": "string"
        },
        "min_group_size": {
          "default": 20,
          "description": "min_group_size parameter (int).",
          "type": "integer"
        },
        "n_estimators": {
          "default": 1000,
          "description": "Trees per group-time forest.",
          "type": "integer"
        },
        "n_jobs": {
          "default": 1,
          "description": "Threads per forest (-1 = all cores).",
          "type": "integer"
        },
        "propensity_clip": {
          "default": 0.001,
          "description": "Upper clip for the cohort-membership probability in the control weights e/(1-e).",
          "type": "number"
        },
        "random_state": {
          "default": 0,
          "description": "Seed; cell k uses random_state + k.",
          "type": "integer"
        },
        "time": {
          "description": "Numeric period.",
          "type": "string"
        },
        "x": {
          "description": "Time-invariant effect modifiers.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "cohort",
        "id",
        "time",
        "x",
        "y"
      ],
      "type": "object"
    },
    "name": "did_forest"
  },
  {
    "description": "Heterogeneous-adoption DiD using quasi-untreateded groups. Validation: validated evidence tier (known-truth, reference, external-parity, or Monte Carlo artifact).",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "1 - alpha confidence level.",
          "type": "number"
        },
        "bandwidth": {
          "default": "mse-dpi",
          "description": "Bandwidth for the local polynomial fit at dose zero -- one value, or one per reported horizon (placebos first, then effects). **Required for now.** Stata's default ``bw_method('mse-dpi')`` selector is not yet implemented; see Notes.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "dynamic": {
          "default": false,
          "description": "Scale effect l by the average **cumulative** dose from F to ``F-1+l`` instead of the dose at ``F-1+l``. The current-dose normalization is right under a static model, the cumulative one under a dynamic model where past treatment still matters.",
          "type": "boolean"
        },
        "effects": {
          "default": 1,
          "description": "Number of event-study effects. Effect l is the effect at period ``F-1+l``, i.e. l periods after adoption.",
          "type": "integer"
        },
        "group": {
          "description": "Column names. ``treat`` is the dose, not an indicator.",
          "type": "string"
        },
        "kernel": {
          "default": "epanechnikov",
          "description": "Default epanechnikov, matching ``did_had``.",
          "enum": [
            "epanechnikov",
            "triangular",
            "uniform",
            "gaussian"
          ],
          "type": "string"
        },
        "placebo": {
          "default": 0,
          "description": "Number of placebo estimates, built symmetrically: the ``F-1`` to ``F-1+l`` evolution is replaced by ``F-1`` to ``F-1-l``, with the dose taken from the matching post period.",
          "type": "integer"
        },
        "time": {
          "description": "Column names. ``treat`` is the dose, not an indicator.",
          "type": "string"
        },
        "treat": {
          "description": "Column names. ``treat`` is the dose, not an indicator.",
          "type": "string"
        },
        "trends_lin": {
          "default": false,
          "description": "Allow group-specific linear trends, estimated from each group's ``F-2`` to ``F-1`` evolution and subtracted. Costs one placebo, and needs at least three pre-treat periods.",
          "type": "boolean"
        },
        "y": {
          "description": "Column names. ``treat`` is the dose, not an indicator.",
          "type": "string"
        },
        "yatchew": {
          "default": false,
          "description": "Report the Yatchew differencing test alongside each horizon. Effects are tested for **linearity** in the dose (order 1), placebos for **mean independence** of the pre-period evolution from the future dose (order 0). Theorem 5 of the paper: with (quasi-)untreated groups, plain OLS of the evolution on the dose is unbiased for the same estimand *iff* that conditional expectation is linear. Failing to reject therefore licenses the far simpler estimator; rejecting says the nonparametric machinery is doing real work.",
          "type": "boolean"
        }
      },
      "required": [
        "group",
        "time",
        "treat",
        "y"
      ],
      "type": "object"
    },
    "name": "did_had"
  },
  {
    "description": "Borusyak-Jaravel-Spiess (2024) imputation DiD. Fits a TWFE model on untreated observations only, imputes counterfactual Y(0) for treated obs, and averages the imputation residuals. Efficient under no-anticipation + parallel trends; analytical SE via bjs_inference. Validation: certified evidence with scoped limitations. Known limitations: R/Stata parity is for the documented untreated-only TWFE and simple ATT aggregation convention only; event-study and SE rows are backend-specific diagnostics. Do NOT use when: there is no never-treated (or not-yet-treated) group to fit the untreated-potential-outcome model on -- imputation has no estimation sample; the design is a simple 2x2 -- sp.did(method='2x2') is the same estimand and far cheaper; pre-trends are visibly non-parallel -- BJS imputes Y(0) from a two-way model that assumes them away, so violations are absorbed silently rather than surfaced. Cost: Fits the untreated two-way model once, then imputes; cheap in memory. vce='bootstrap' multiplies total runtime by n_boot -- budget accordingly before raising n_boot. Assumptions: Parallel trends in absolute levels; No anticipation (no pre-treatment reaction); SUTVA. Pre-conditions: panel...",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "balanced": {
          "default": false,
          "description": "Keep only eventually-treated units observed at every non-negative requested horizon (Stata: hbalance)",
          "type": "boolean"
        },
        "boot_seed": {
          "default": 0,
          "description": "Seed for the cluster bootstrap (deterministic results).",
          "type": "integer"
        },
        "cluster": {
          "description": "Cluster identifier column for clustered standard errors.",
          "type": "string"
        },
        "controls": {
          "description": "Control-variable column names.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "fe": {
          "description": "Fixed effects in the Y(0) model, replacing the default unit+time. Stata's fe(): entries are column names or 'a#b' interacted cells; [] means no fixed effects.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "first_treat": {
          "description": "First-treatment period; 0 = never-treated",
          "type": "string"
        },
        "group": {
          "description": "Unit identifier",
          "type": "string"
        },
        "hetby": {
          "description": "Report heterogeneous ATTs by a time-invariant unit-level variable (Stata: hetby())",
          "type": "string"
        },
        "horizon": {
          "description": "Relative-time leads / lags (default: all available)",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "min_n": {
          "description": "Drop event-study horizons with fewer treated observations (Stata: minn())",
          "type": "integer"
        },
        "n_boot": {
          "default": 199,
          "description": "Cluster-bootstrap replications when vce='bootstrap'.",
          "type": "integer"
        },
        "pretrend_method": {
          "default": "bjs",
          "description": "Reference convention for the PRE-treatment event-study coefficients. 'bjs' matches Stata did_imputation, 'in-sample' is the fect/did2s residual average (attenuated by N0/N), 'symmetric' is Roth's (2026) TWFE-comparable repair for non-staggered designs. Post-treatment coefficients are identical under all three.",
          "enum": [
            "bjs",
            "in-sample",
            "symmetric"
          ],
          "type": "string"
        },
        "pretrends": {
          "description": "Estimate k placebo pre-trend coefficients (-k..-1) and report their joint Wald test (Stata: pretrends(k))",
          "type": "integer"
        },
        "project": {
          "description": "Regress the imputed treatment effects on these covariates and report constant plus slopes. Stata's project(); mutually exclusive with hetby.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "save_residuals": {
          "default": false,
          "description": "Store untreated-fit residuals in model_info (Stata: saveresid())",
          "type": "boolean"
        },
        "save_weights": {
          "default": false,
          "description": "Store exact estimation weights w with ATT = w'y in model_info (Stata: saveweights())",
          "type": "boolean"
        },
        "se_method": {
          "description": "Shared DiD spelling for vce=: 'analytic', 'bootstrap' or 'auto'. Passing both raises.",
          "enum": [
            "analytic",
            "bootstrap",
            "auto"
          ],
          "type": "string"
        },
        "time": {
          "description": "Time period column",
          "type": "string"
        },
        "time_covariates": {
          "description": "Controls interacted with the period fixed effects (one coefficient per period). Stata's timecontrols().",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "unit_covariates": {
          "description": "Controls interacted with the unit fixed effects (one slope per unit). Stata did_imputation's unitcontrols(); unit_covariates=[time] gives unit-specific trends.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "vce": {
          "default": "analytic",
          "description": "Standard-error mode for the overall ATT. 'analytic' is the exact BJS variance (reproduces Stata did_imputation and R didimputation); 'bootstrap' resamples clusters.",
          "enum": [
            "analytic",
            "bootstrap"
          ],
          "type": "string"
        },
        "weights": {
          "description": "Estimation weights omega (Stata did_imputation [aw=], R didimputation wname=): weighted least squares in the untreated Y(0) model, omega-weighted averages over treated cells, exact variance with the weighted projection. Changes the estimand, not the precision. Not combinable with project.",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "first_treat",
        "group",
        "time",
        "y"
      ],
      "type": "object"
    },
    "name": "did_imputation"
  },
  {
    "description": "Heuristic what-if adjustment of a staggered DiD for a user-supplied timing-misclassification probability pi_misclass and anticipation horizon. A sensitivity check motivated by Augustin-Gutknecht-Liu (2025), not their estimator: it does not identify misclassification or anticipation from the data. Assumptions: Parallel trends: treated and control groups would have followed the same trajectory absent treatment; No anticipation: outcomes in pre-treatment periods are unaffected by future treatment; SUTVA: no spillovers between units. Pre-conditions: pi_misclass is between 0 and 0.5 (else identification flips); Known anticipation horizon; data is panel or repeated cross-section with a time column. Failure modes: Pre-trend joint test p < 0.05 (or underpowered at 0.10) -> Use sp.sensitivity_rr (Rambachan & Roth honest CI) or switch to sp.callaway_santanna; Staggered treatment timing with TWFE method -> TWFE can give negative weights; use Callaway-Sant'Anna, Sun-Abraham, or BJS imputation; Pre-trend test underpowered (Roth 2022) -> Check sp.pretrends_power -- if low, report honest CI via sp.sensitivity_rr. Alternatives: sp.callaway_santanna, sp.sun_abraham, sp.did_imputation, sp.sdid. Typ...",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "anticipation_periods": {
          "default": 0,
          "description": "anticipation_periods parameter (int).",
          "type": "integer"
        },
        "cluster": {
          "description": "Cluster identifier column for clustered standard errors.",
          "type": "string"
        },
        "id": {
          "description": "Unit, subject, or panel identifier column.",
          "type": "string"
        },
        "pi_misclass": {
          "default": 0.0,
          "description": "P(observed treat != true treat) -- between 0 and 1",
          "type": "number"
        },
        "time": {
          "description": "Time period column.",
          "type": "string"
        },
        "treat": {
          "description": "First-treatment period (possibly noisy)",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "id",
        "time",
        "treat",
        "y"
      ],
      "type": "object"
    },
    "name": "did_misclassified"
  },
  {
    "description": "de Chaisemartin & D'Haultfuille (2020) DID_M estimator. Weighted average of consecutive-period DID cells where treatment 'switchers' are compared to 'stayers'. Handles treatments that switch on AND off (unlike Callaway-Sant'Anna which assumes staggered adoption). Supports placebo lags, dynamic horizons, cluster bootstrap SE, joint placebo test and average-cumulative-effect summary from dCDH (2024). The heteroskedastic-weights variant and full dCDH (2024) intertemporal event-study (did_multiplegt_dyn Stata) are on the roadmap -- see docs/rfc/multiplegt_dyn.md. Validation: certified parity evidence. Assumptions: Parallel trends between switchers and stayers; Stable treatment effects across consecutive periods (for the DID_M weighted average interpretation); No anticipation. Pre-conditions: long-format panel with one row per unit x period; treatment is binary (0/1) and may vary over time within a unit; at least two periods observed per unit so a first difference can be computed. Failure modes: No switching cells (nobody changes treatment) -> did_multiplegt identifies effects only from treatment switches. Fall back to callaway_santanna if the design is staggered adoption; Joint placeb...",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "cluster": {
          "description": "Cluster variable for bootstrap (defaults to group)",
          "type": "string"
        },
        "controls": {
          "description": "Controls residualised via first differences",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "dynamic": {
          "default": 0,
          "description": "Number of post-treatment dynamic horizons",
          "type": "integer"
        },
        "group": {
          "description": "Unit identifier",
          "type": "string"
        },
        "n_boot": {
          "default": 100,
          "description": "Cluster-bootstrap replications",
          "type": "integer"
        },
        "placebo": {
          "default": 0,
          "description": "Number of pre-treatment placebo lags",
          "type": "integer"
        },
        "placebo_sign": {
          "default": "stata",
          "description": "Placebo sign convention. dCDH's own Stata and R packages disagree: on did::mpdta both give |placebo_1| = 0.024269 with identical effects, but opposite signs. Default keeps Stata's",
          "enum": [
            "stata",
            "r"
          ],
          "type": "string"
        },
        "seed": {
          "description": "Random seed for reproducible stochastic steps.",
          "type": "integer"
        },
        "time": {
          "description": "Time period column",
          "type": "string"
        },
        "treatment": {
          "description": "Binary current-treatment indicator (may switch on and off)",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable",
          "type": "string"
        }
      },
      "required": [
        "group",
        "time",
        "treatment",
        "y"
      ],
      "type": "object"
    },
    "name": "did_multiplegt"
  },
  {
    "description": "[experimental] dCDH (2024) intertemporal event-study DiD (MVP -- see docs/rfc/multiplegt_dyn.md). At each horizon l in {-placebo, ..., dynamic}, compares Y_{F+l} - Y_{F-1} between units first switching at F and a not-yet-treated or never-treated control set held stable across the horizon. Effects, placebos, switcher counts, the switcher-weighted aggregate and the analytic (se_method='analytic') standard errors are pinned to the authors' DIDmultiplegtDYN / Stata did_multiplegt_dyn; weight= is supported. **MVP caveats**: no controls=, trends or normalized/continuous options; heteroskedastic-weights variant pending. Validation status: experimental. Known limitations: switch-off events are handled, but the heteroskedastic-weights variant (dCDH 2023 EJ survey) is not implemented; se_method='analytic' reproduces DIDmultiplegtDYN's variance only for the options implemented here: controls=, trends and normalized/continuous variants are not implemented, and the joint placebo/overall tests come from the bootstrap only; the headline aggregation convention differs from DIDmultiplegtDYN's Av_tot_eff: the default weights horizons equally; pass aggregation='switchers' to match the R package. Cos...",
    "input_schema": {
      "properties": {
        "aggregation": {
          "default": "simple",
          "description": "Headline weighting over dynamic horizons: 'simple' (equal weight) or 'switchers' (weight by switchers per horizon -- reproduces DIDmultiplegtDYN's Av_tot_eff)",
          "enum": [
            "simple",
            "switchers"
          ],
          "type": "string"
        },
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "cluster": {
          "description": "Cluster column (defaults to group)",
          "type": "string"
        },
        "continuous": {
          "description": "Degree of the per-period polynomial in the period-one treatment that replaces the baseline match when every group's period-one treatment differs; the treatment may then be non-binary",
          "type": "integer"
        },
        "control": {
          "default": "not_yet_treated",
          "description": "Control group",
          "enum": [
            "not_yet_treated",
            "never_treated"
          ],
          "type": "string"
        },
        "controls": {
          "description": "Covariates; the outcome's first difference is replaced by its residual from a regression on the covariates' first differences and time fixed effects, fitted on the not-yet-switched (g, t)s and separately by baseline treatment",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "dynamic": {
          "default": 3,
          "description": "Number of post-treatment dynamic horizons",
          "type": "integer"
        },
        "effects_equal": {
          "default": false,
          "description": "Test H0 that the dynamic effects are all equal. True tests every effect; a (lower, upper) pair tests that horizon range. Stata's effects_equal().",
          "type": "boolean"
        },
        "group": {
          "description": "Unit identifier",
          "type": "string"
        },
        "n_boot": {
          "default": 500,
          "description": "Number of bootstrap replications.",
          "type": "integer"
        },
        "normalized": {
          "default": false,
          "description": "Report the effect per unit of treatment received between the base period and the horizon",
          "type": "boolean"
        },
        "placebo": {
          "default": 0,
          "description": "Number of pre-treatment placebo horizons",
          "type": "integer"
        },
        "same_switchers": {
          "default": false,
          "description": "Restrict the treated arm to switchers observed at every requested horizon, holding the composition fixed across relative time. Stata's same_switchers.",
          "type": "boolean"
        },
        "se_method": {
          "default": "bootstrap",
          "description": "'bootstrap' resamples clusters; 'analytic' uses the influence functions (~100x faster, but NOT pinned to DIDmultiplegtDYN -- about 1% below its reported SEs)",
          "enum": [
            "bootstrap",
            "analytic"
          ],
          "type": "string"
        },
        "seed": {
          "description": "Random seed for reproducible stochastic steps.",
          "type": "integer"
        },
        "switchers": {
          "description": "Estimate on switch-in ('in') or switch-out ('out') events only. Stata's switchers(); default pools both.",
          "enum": [
            "in",
            "out"
          ],
          "type": "string"
        },
        "time": {
          "description": "Time period column.",
          "type": "string"
        },
        "treatment": {
          "description": "Binary treatment (0/1); switch-on and switch-off events both count, see switchers=",
          "type": "string"
        },
        "trends_nonparam": {
          "description": "Time-invariant variables a control must match the switcher on, over and above the baseline treatment",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "weights": {
          "description": "Observation weights (Stata did_multiplegt_dyn weight(), R weight=, both accepted as the alias weight=): read at the row a unit contributes from; switcher means, control means, cohort weights and the switchers aggregation all become weighted.",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "group",
        "time",
        "treatment",
        "y"
      ],
      "type": "object"
    },
    "name": "did_multiplegt_dyn"
  },
  {
    "description": "Classic DID diagram showing treatment effect with counterfactual.",
    "input_schema": {
      "properties": {
        "annotate_effect": {
          "default": true,
          "description": "Annotate the treatment effect arrow on the plot.",
          "type": "boolean"
        },
        "ax": {
          "description": "ax parameter.",
          "type": "string"
        },
        "colors": {
          "description": "(treatment, control, counterfactual) colors.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "figsize": {
          "default": [
            10,
            6
          ],
          "description": "figsize parameter (Tuple[float, float]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "labels": {
          "description": "Custom labels: ``{'treat': ..., 'control': ..., 'counterfactual': ...}``.",
          "type": "object"
        },
        "show_counterfactual": {
          "default": true,
          "description": "Draw the dashed counterfactual line.",
          "type": "boolean"
        },
        "time": {
          "description": "Time period variable.",
          "type": "string"
        },
        "title": {
          "description": "title parameter (Optional[str]).",
          "type": "string"
        },
        "treat": {
          "description": "Binary treatment group indicator (0/1).",
          "type": "string"
        },
        "treat_time": {
          "description": "Treatment onset time. If None, inferred as the midpoint.",
          "type": "integer"
        },
        "y": {
          "description": "Outcome variable.",
          "type": "string"
        }
      },
      "required": [
        "time",
        "treat",
        "y"
      ],
      "type": "object"
    },
    "name": "did_plot"
  },
  {
    "description": "DID report bundle: fits selected methods and writes report artifacts.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Same as :func:`did_summary`.",
          "type": "number"
        },
        "cluster": {
          "description": "Same as :func:`did_summary`.",
          "type": "string"
        },
        "controls": {
          "description": "Same as :func:`did_summary`.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "first_treat": {
          "description": "Same as :func:`did_summary`.",
          "type": "string"
        },
        "group": {
          "description": "Same as :func:`did_summary`.",
          "type": "string"
        },
        "include_sensitivity": {
          "default": true,
          "description": "Whether to run Rambachan-Roth breakdown M*. Defaults to ``True`` in ``did_report`` (vs ``False`` in ``did_summary``) because a report is expected to be comprehensive.",
          "type": "boolean"
        },
        "methods": {
          "default": "auto",
          "description": "Same as :func:`did_summary`.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "plot_sort_by": {
          "default": "estimate",
          "description": "Sort the forest plot by estimate ascending.",
          "type": "string"
        },
        "save_to": {
          "description": "Directory path. Created if it does not exist.",
          "type": "string"
        },
        "time": {
          "description": "Same as :func:`did_summary`.",
          "type": "string"
        },
        "verbose": {
          "default": false,
          "description": "Print progress lines.",
          "type": "boolean"
        },
        "y": {
          "description": "Same as :func:`did_summary`.",
          "type": "string"
        }
      },
      "required": [
        "first_treat",
        "group",
        "save_to",
        "time",
        "y"
      ],
      "type": "object"
    },
    "name": "did_report"
  },
  {
    "description": "One-call method-robustness comparison for staggered DID.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals.",
          "type": "number"
        },
        "cluster": {
          "description": "Cluster variable for SE (defaults to ``group`` in each sub-method).",
          "type": "string"
        },
        "controls": {
          "description": "Time-varying covariates passed to methods that support them.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "first_treat": {
          "description": "First-treatment period per unit; NaN (or 0) for never-treated.",
          "type": "string"
        },
        "group": {
          "description": "Unit identifier.",
          "type": "string"
        },
        "include_sensitivity": {
          "default": false,
          "description": "If ``True`` and ``'cs'`` is among the methods fit, compute the Rambachan-Roth (2023) *breakdown M\\** -- the largest relative violation of parallel trends under which the treatment effect is still significantly different from zero. The value is added to ``model_info['breakdown_m']`` and to the ``breakdown_m`` column of ``detail`` (CS row only; other methods leave ``NaN``).",
          "type": "boolean"
        },
        "methods": {
          "default": "auto",
          "description": "Methods to run. Valid keys: ``'cs'``, ``'sa'``, ``'bjs'``, ``'etwfe'``, ``'stacked'``, or ``'all'`` / ``'auto'`` for all.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "time": {
          "description": "Time / period variable (integer-valued).",
          "type": "string"
        },
        "verbose": {
          "default": false,
          "description": "Print progress for each method.",
          "type": "boolean"
        },
        "y": {
          "description": "Outcome variable.",
          "type": "string"
        }
      },
      "required": [
        "first_treat",
        "group",
        "time",
        "y"
      ],
      "type": "object"
    },
    "name": "did_summary"
  },
  {
    "description": "Forest plot of DID method-robustness summary.",
    "input_schema": {
      "properties": {
        "ax": {
          "description": "Existing axes to draw on. If ``None`` a new figure is created.",
          "type": "string"
        },
        "color": {
          "default": "#2C3E50",
          "description": "Color for point estimates and CIs.",
          "type": "string"
        },
        "figsize": {
          "default": [
            9,
            5
          ],
          "description": "Figure size when creating a new figure.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "highlight_color": {
          "default": "#C0392B",
          "description": "Color for the cross-method mean line.",
          "type": "string"
        },
        "reference": {
          "description": "Horizontal reference value (e.g. 0 for 'no effect'). Defaults to ``0``.",
          "type": "number"
        },
        "result": {
          "description": "Output of :func:`did_summary`. Must have a ``detail`` DataFrame with columns ``estimate``, ``ci_low``, ``ci_high``, and either ``method`` or ``estimator``.",
          "type": "string"
        },
        "sort_by": {
          "description": "If ``'estimate'``, sort methods by point estimate ascending. Otherwise keep the order in ``result.detail``.",
          "type": "string"
        },
        "title": {
          "description": "Plot title. Defaults to ``\"DID Method-Robustness Summary\"``.",
          "type": "string"
        }
      },
      "required": [
        "result"
      ],
      "type": "object"
    },
    "name": "did_summary_plot"
  },
  {
    "description": "Render a :func:`did_summary` result as a LaTeX",
    "input_schema": {
      "properties": {
        "caption": {
          "default": "DID method-robustness summary.",
          "description": "LaTeX caption.",
          "type": "string"
        },
        "digits": {
          "default": 4,
          "description": "Decimal precision.",
          "type": "integer"
        },
        "include_breakdown": {
          "default": true,
          "description": "Include the Rambachan-Roth breakdown M* column when sensitivity was requested.",
          "type": "boolean"
        },
        "include_ci": {
          "default": true,
          "description": "Include the 95 % CI column.",
          "type": "boolean"
        },
        "label": {
          "default": "tab:did_summary",
          "description": "LaTeX label for the table.",
          "type": "string"
        },
        "result": {
          "description": "Output of :func:`did_summary`.",
          "type": "string"
        }
      },
      "required": [
        "result"
      ],
      "type": "object"
    },
    "name": "did_summary_to_latex"
  },
  {
    "description": "Render a :func:`did_summary` result as a GitHub-Flavoured Markdown table.",
    "input_schema": {
      "properties": {
        "digits": {
          "default": 4,
          "description": "Decimal precision for numeric columns.",
          "type": "integer"
        },
        "include_breakdown": {
          "default": true,
          "description": "Include the Rambachan-Roth breakdown M* column (CS row only, blank for others). Ignored if sensitivity was not requested.",
          "type": "boolean"
        },
        "include_ci": {
          "default": true,
          "description": "Include the 95 % CI column.",
          "type": "boolean"
        },
        "result": {
          "description": "Output of :func:`did_summary`.",
          "type": "string"
        }
      },
      "required": [
        "result"
      ],
      "type": "object"
    },
    "name": "did_summary_to_markdown"
  },
  {
    "description": "DiD with time-varying covariates frozen at baseline (Caetano, Callaway, Payne & Sant'Anna 2026, arXiv:2608.03881). Avoids the bad-controls bias that arises when treatment affects the covariates: freezes X at period g + baseline_offset (default g-1) per cohort and uses the frozen values as controls in a per-(g, t) DiD on the long difference. est_method picks the cell estimator (outcome regression, stabilised IPW, or the doubly robust combination), control_group widens the comparison set to the not-yet-treated, and vce switches between the cluster bootstrap, the influence-function plug-in and the multiplier bootstrap. covariate_pretest=True adds the paper's two placebo statistics on the covariate itself. aggregation='group' averages each cohort's post-period cells then weights cohorts by size; 'simple' weights every cell by its treated count. Validation: certified parity evidence. Assumptions: Conditional parallel trends given frozen baseline X; No anticipation; SUTVA. Pre-conditions: staggered adoption with >= 1 never-treated unit; covariates column(s) exist for the baseline period per cohort; integer-valued time column. Failure modes: No observation at baseline period for some uni...",
    "input_schema": {
      "properties": {
        "aggregation": {
          "default": "group",
          "description": "``\"group\"``: average ATT(g, t) over each cohort's post periods, then across cohorts weighted by cohort size -- the overall ATT reported by ``ptetools`` and ``did::aggte(type = \"group\")``. ``\"simple\"``: weight every post cell by its number of treated units, as ``did::aggte(type = \"simple\")`` does on a balanced panel.",
          "enum": [
            "group",
            "simple"
          ],
          "type": "string"
        },
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "baseline_offset": {
          "default": -1,
          "description": "Offset relative to first-treatment period for freezing",
          "type": "integer"
        },
        "cohort": {
          "description": "First-treatment period (never_value = never-treated)",
          "type": "string"
        },
        "control_group": {
          "default": "nevertreated",
          "description": "Comparison units; 'notyettreated' adds cohorts that have not switched by either period of the cell",
          "enum": [
            "nevertreated",
            "notyettreated"
          ],
          "type": "string"
        },
        "covariate_pretest": {
          "default": false,
          "description": "Also run the comparison with each covariate as the outcome, before and after treatment",
          "type": "boolean"
        },
        "covariates": {
          "description": "Time-varying covariates to freeze at baseline",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "est_method": {
          "default": "reg",
          "description": "Cell estimator: outcome regression, stabilised IPW, or the doubly robust combination",
          "enum": [
            "reg",
            "ipw",
            "dr"
          ],
          "type": "string"
        },
        "n_boot": {
          "default": 500,
          "description": "Number of bootstrap replications.",
          "type": "integer"
        },
        "never_value": {
          "default": 0,
          "description": "never_value parameter.",
          "type": "string"
        },
        "seed": {
          "description": "Random seed for reproducible stochastic steps.",
          "type": "integer"
        },
        "time": {
          "description": "Time period column.",
          "type": "string"
        },
        "unit": {
          "description": "Unit identifier column.",
          "type": "string"
        },
        "vce": {
          "default": "bootstrap",
          "description": "Standard error: unit cluster bootstrap, the influence-function plug-in, or the multiplier bootstrap",
          "enum": [
            "bootstrap",
            "analytic",
            "multiplier"
          ],
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "cohort",
        "covariates",
        "time",
        "unit",
        "y"
      ],
      "type": "object"
    },
    "name": "did_timevarying_covariates"
  },
  {
    "description": "Direct outcome regression (plug-in Q-model) OPE. Validation: certified parity evidence. Assumptions: Plug-in outcome regression (Q-model) is correctly specified; No unmeasured confounding in the logged data. Pre-conditions: X (context), A (logged action), R (reward) are available to fit the outcome model. Failure modes: Model misspecification bias -- the Q-model extrapolates outside the logged action support -> Prefer the doubly-robust estimator, which is robust to Q-model misspecification. Alternatives: sp.doubly_robust, sp.ips, sp.snips. Typical minimum N: 500.",
    "input_schema": {
      "properties": {
        "A": {
          "description": "A parameter (np.ndarray).",
          "type": "string"
        },
        "R": {
          "description": "R parameter (np.ndarray).",
          "type": "string"
        },
        "X": {
          "description": "Feature matrix or covariate DataFrame.",
          "type": "string"
        },
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "n_actions": {
          "description": "Number of actions.",
          "type": "integer"
        },
        "pi_target": {
          "description": "pi_target parameter.",
          "type": "string"
        },
        "q_hat": {
          "description": "q_hat parameter (Optional[np.ndarray]).",
          "type": "string"
        }
      },
      "required": [
        "A",
        "R",
        "X",
        "pi_target"
      ],
      "type": "object"
    },
    "name": "direct_method"
  },
  {
    "description": "Distributional Synthetic Controls (Gunsilius 2023). Validation: certified parity evidence. Assumptions: A convex (or regularized) combination of donor units reproduces the treated unit's pre-treatment outcome path; No interference: the treatment does not affect the donor units (SUTVA); No anticipation before the treatment date. Pre-conditions: Panel of one or more treated units plus an untreated donor pool, observed over time; Pre-treatment window long enough to fit donor weights (rule of thumb: more pre-periods than donors used); Outcome observed for every unit in every period. Failure modes: Large pre-treatment RMSPE -- the synthetic unit fails to track the treated unit before treatment -> Add donors / predictors, lengthen the pre-period, or use a bias-corrected estimator (sdid, augsynth); Placebo / permutation inference shows the estimate is not extreme relative to donors -> Report the placebo distribution honestly; the effect may not be distinguishable from noise. Alternatives: sp.sdid, sp.augsynth, sp.gsynth, sp.callaway_santanna. Typical minimum N: 15.",
    "input_schema": {
      "properties": {
        "M": {
          "default": 1000,
          "description": "Individual-level data, ``method='quantile'``: number of quantile nodes in each weight regression (R ``M``). Default nodes are the mid-points ``(m - 0.5) / M``.",
          "type": "integer"
        },
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals.",
          "type": "number"
        },
        "cdf_grid": {
          "description": "``method='mixture'``: outcome grid per period (all periods, in time order) on which the CDFs are compared and inverted (R's ``runif(G)`` grid). Default: ``G`` equispaced points on ``[floor(10 min)/10 - 0.25, ceil(10 max)/10 + 0.25]``.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "method": {
          "description": "Individual-level data: ``'quantile'`` (default; R ``mixture = FALSE``) or ``'mixture'`` (R ``mixture = TRUE``) -- see the module docstring. Aggregate panels: ``'mixture'`` (default) or ``'quantile'`` select the fallback heuristic.",
          "enum": [
            "quantile",
            "mixture"
          ],
          "type": "string"
        },
        "n_quantiles": {
          "description": "Individual-level data: ``G`` -- the counterfactual quantile function is reported on ``0, 1/G, ..., 1`` and the mixture CDF grid has ``G`` points (default 1000, as R). Aggregate panels: number of quantile grid points on (0, 1) (default 100).",
          "type": "integer"
        },
        "outcome": {
          "description": "Outcome variable column name.",
          "type": "string"
        },
        "placebo": {
          "default": true,
          "description": "Run in-space placebo permutation tests for inference.",
          "type": "boolean"
        },
        "q_nodes": {
          "description": "Quantile nodes for the weight regressions: one array reused for every pre-period, or one array per pre-period (e.g. R's ``runif(M)`` draws, to reproduce ``DiSCo`` exactly).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "seed": {
          "description": "Unused (all node sets are deterministic unless supplied).",
          "type": "integer"
        },
        "simplex": {
          "default": false,
          "description": "Individual-level data: restrict weights to be non-negative (R ``simplex``). ``False`` (R default) allows negative weights.",
          "type": "boolean"
        },
        "time": {
          "description": "Time period column name.",
          "type": "string"
        },
        "treated_unit": {
          "description": "Value in *unit* that identifies the treated unit.",
          "type": "string"
        },
        "treatment_time": {
          "description": "First period of treatment (inclusive).",
          "type": "string"
        },
        "unit": {
          "description": "Unit identifier column name.",
          "type": "string"
        }
      },
      "required": [
        "outcome",
        "time",
        "treated_unit",
        "treatment_time",
        "unit"
      ],
      "type": "object"
    },
    "name": "discos"
  },
  {
    "description": "Visualise distributional synthetic control results.",
    "input_schema": {
      "properties": {
        "ax": {
          "description": "Pre-existing axes for the plot.",
          "type": "string"
        },
        "ci_alpha": {
          "default": 0.2,
          "description": "Transparency for CI band.",
          "type": "number"
        },
        "color": {
          "default": "#2C3E50",
          "description": "Primary plot colour.",
          "type": "string"
        },
        "figsize": {
          "default": [
            10,
            6
          ],
          "description": "Figure size.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "result": {
          "description": "Output from ``discos()`` or ``qqsynth()``.",
          "type": "string"
        },
        "title": {
          "description": "Plot title override.",
          "type": "string"
        },
        "type": {
          "default": "quantile_effect",
          "description": "default 'quantile_effect' ``'quantile_effect'``: treatment effect Delta(tau) across quantiles with CIs. ``'quantile_comparison'``: overlay treated vs. counterfactual quantile functions. ``'gap'``: gap plot (treated - synthetic) over time. ``'weights'``: horizontal bar chart of donor weights.",
          "enum": [
            "quantile_effect",
            "quantile_comparison",
            "gap",
            "weights"
          ],
          "type": "string"
        }
      },
      "required": [
        "result"
      ],
      "type": "object"
    },
    "name": "discos_plot"
  },
  {
    "description": "Test for distributional treatment effects. Assumptions: A convex (or regularized) combination of donor units reproduces the treated unit's pre-treatment outcome path; No interference: the treatment does not affect the donor units (SUTVA); No anticipation before the treatment date. Pre-conditions: Panel of one or more treated units plus an untreated donor pool, observed over time; Pre-treatment window long enough to fit donor weights (rule of thumb: more pre-periods than donors used); Outcome observed for every unit in every period. Failure modes: Large pre-treatment RMSPE -- the synthetic unit fails to track the treated unit before treatment -> Add donors / predictors, lengthen the pre-period, or use a bias-corrected estimator (sdid, augsynth); Placebo / permutation inference shows the estimate is not extreme relative to donors -> Report the placebo distribution honestly; the effect may not be distinguishable from noise. Alternatives: sp.sdid, sp.augsynth, sp.gsynth, sp.callaway_santanna. Typical minimum N: 15.",
    "input_schema": {
      "properties": {
        "result": {
          "description": "Output from ``discos()`` or ``qqsynth()``.",
          "type": "string"
        },
        "test": {
          "default": "ks",
          "description": "``'ks'``: two-sample Kolmogorov-Smirnov test comparing treated and counterfactual quantile functions. ``'cvm'``: Cramer-von Mises test statistic (permutation-based). ``'stochastic_dominance'``: first-order stochastic dominance test.",
          "enum": [
            "ks",
            "cvm",
            "stochastic_dominance"
          ],
          "type": "string"
        }
      },
      "required": [
        "result"
      ],
      "type": "object"
    },
    "name": "discos_test"
  },
  {
    "description": "Jackson & VanderWeele (2018) causal disparity decomposition.",
    "input_schema": {
      "properties": {
        "covariates": {
          "description": "Covariate matrix, DataFrame, or column names.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "group": {
          "description": "Group or cohort identifier.",
          "type": "string"
        },
        "mediator": {
          "description": "mediator parameter (str).",
          "type": "string"
        },
        "target_level": {
          "description": "Value at which to fix mediator for the \"initial\" counterfactual.",
          "type": "number"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "group",
        "mediator",
        "y"
      ],
      "type": "object"
    },
    "name": "disparity_decompose"
  },
  {
    "description": "Synthetic disparity panel with treatment, mediator, outcome.",
    "input_schema": {
      "properties": {
        "n": {
          "default": 3000,
          "description": "n parameter (int).",
          "type": "integer"
        },
        "seed": {
          "default": 42,
          "description": "Random seed for reproducible stochastic steps.",
          "type": "integer"
        }
      },
      "required": [],
      "type": "object"
    },
    "name": "disparity_panel"
  },
  {
    "description": "Distributional IV: the complier LATE at each quantile of ``y``. Validation: validated evidence tier (known-truth, reference, external-parity, or Monte Carlo artifact). Assumptions: Selection-on-observables (unconfoundedness + overlap) or, for IV variants, instrument validity; For IV-QTE: rank invariance / rank similarity (monotonicity of the structural quantile function); Relevance: instruments predict the endogenous regressor (first-stage F >= 10 rule of thumb). Pre-conditions: Covariates, treatment, and outcome; for IV-quantile methods, a valid instrument; Enough data to estimate the outcome distribution across quantiles; formula includes the (endog ~ instruments) parenthesised block. Failure modes: Estimated conditional quantiles cross (non-monotone), or tail quantiles are unstable -> Use rearrangement / monotonization and avoid extreme quantiles where data are sparse; First-stage F < 10 (Stock-Yogo 5% bias) -> Use weak-IV-robust inference (Anderson-Rubin) or LIML; Over-identification test rejects (sp.estat 'overid') -> At least one instrument is invalid; drop instruments or switch to just-identified LIML. Alternatives: sp.qte, sp.iv, sp.dml, sp.deepiv. Typical minimum N: 500.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "covariates": {
          "description": "Conditioning set for ``P(Z=1|X)``. Unlike versions <= 1.20.0 (which accepted and silently ignored this argument) it now changes the estimate.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "instrument": {
          "description": "Outcome, binary treatment, binary instrument.",
          "type": "string"
        },
        "n_boot": {
          "default": 200,
          "description": "Bootstrap replications when ``se='bootstrap'``.",
          "type": "integer"
        },
        "quantiles": {
          "description": "Defaults to ``(0.1, 0.25, 0.5, 0.75, 0.9)``.",
          "type": "string"
        },
        "se": {
          "default": "auto",
          "description": "``'auto'`` picks ``'analytic'`` without covariates and ``'bootstrap'`` with them (the analytic influence function treats ``pi(X)`` as known).",
          "enum": [
            "auto",
            "analytic",
            "bootstrap"
          ],
          "type": "string"
        },
        "seed": {
          "default": 0,
          "description": "Random seed for reproducible stochastic steps.",
          "type": "integer"
        },
        "treat": {
          "description": "Outcome, binary treatment, binary instrument.",
          "type": "string"
        },
        "y": {
          "description": "Outcome, binary treatment, binary instrument.",
          "type": "string"
        }
      },
      "required": [
        "instrument",
        "treat",
        "y"
      ],
      "type": "object"
    },
    "name": "dist_iv"
  },
  {
    "description": "Treatment effect on the *distribution* of the outcome, bin by bin. Bins the outcome, runs Callaway-Sant'Anna on each bin indicator, and reports the effect on P(Y in bin). The per-bin effects sum to zero by construction -- treatment redistributes probability mass, it does not create it -- so the content is the SHAPE: which parts of the outcome distribution gained and which lost. A mean ATT of zero is perfectly consistent with large offsetting movements in the tails, and this is what shows them. R didFF::distDD. Validation: certified evidence with scoped limitations. Known limitations: reports point estimates and standard errors only; the reference runs no test here and neither does this; simultaneous (uniform) confidence bands over bins are not implemented; the standard errors are pointwise only, so reading several bins at once overstates joint confidence. Assumptions: parallel trends for each bin indicator; no anticipation. Pre-conditions: panel with at least one treated cohort and a comparison group; outcome takes at least two distinct values. Alternatives: sp.functional_form_test, sp.qdid, sp.cic, sp.panel_qtet. Typical minimum N: 100.",
    "input_schema": {
      "properties": {
        "aggregation": {
          "default": "group",
          "description": "Which aggte aggregation defines the per-bin effect",
          "enum": [
            "group",
            "simple",
            "dynamic",
            "calendar"
          ],
          "type": "string"
        },
        "allow_unbalanced_panel": {
          "default": false,
          "description": "allow_unbalanced_panel parameter (bool).",
          "type": "boolean"
        },
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "anticipation": {
          "default": 0,
          "description": "anticipation parameter (int).",
          "type": "integer"
        },
        "balance_e": {
          "description": "balance_e parameter (int).",
          "type": "integer"
        },
        "binpoints": {
          "description": "Explicit bin edges",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "control_group": {
          "default": "nevertreated",
          "description": "control_group parameter (str).",
          "type": "string"
        },
        "estimator": {
          "default": "dr",
          "description": "estimator parameter (str).",
          "type": "string"
        },
        "g": {
          "description": "First-treatment period (0 = never treated)",
          "type": "string"
        },
        "i": {
          "description": "Unit identifier",
          "type": "string"
        },
        "max_e": {
          "description": "max_e parameter (float).",
          "type": "number"
        },
        "min_e": {
          "description": "min_e parameter (float).",
          "type": "number"
        },
        "n_bins": {
          "default": "auto",
          "description": "Equal-width bins, or 'auto' for the didFF rule. Bins span the WHOLE panel here (unlike functional_form_test, which bins untreated rows only): the estimand is about where treated mass ended up",
          "type": "integer"
        },
        "panel": {
          "default": true,
          "description": "panel parameter (bool).",
          "type": "boolean"
        },
        "t": {
          "description": "t parameter (str).",
          "type": "string"
        },
        "weights": {
          "description": "Sampling-weight column",
          "type": "string"
        },
        "x": {
          "description": "Primary running variable, regressor, or feature input for this estimator.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "g",
        "i",
        "t",
        "y"
      ],
      "type": "object"
    },
    "name": "distributional_did"
  },
  {
    "description": "Estimate distributional treatment effects. Validation: validated evidence tier (known-truth, reference, external-parity, or Monte Carlo artifact). Assumptions: Selection-on-observables (unconfoundedness + overlap) or, for IV variants, instrument validity; For IV-QTE: rank invariance / rank similarity (monotonicity of the structural quantile function). Pre-conditions: Covariates, treatment, and outcome; for IV-quantile methods, a valid instrument; Enough data to estimate the outcome distribution across quantiles. Failure modes: Estimated conditional quantiles cross (non-monotone), or tail quantiles are unstable -> Use rearrangement / monotonization and avoid extreme quantiles where data are sparse. Alternatives: sp.qte, sp.iv, sp.dml. Typical minimum N: 500.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "method": {
          "default": "ipw",
          "description": "Estimator or algorithm variant to use.",
          "enum": [
            "ipw",
            "dr",
            "cic"
          ],
          "type": "string"
        },
        "n_boot": {
          "default": 500,
          "description": "Number of bootstrap replications.",
          "type": "integer"
        },
        "n_grid": {
          "default": 100,
          "description": "Number of grid.",
          "type": "integer"
        },
        "quantiles": {
          "description": "quantiles parameter (Optional[List[float]]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "seed": {
          "description": "Random seed for reproducible stochastic steps.",
          "type": "integer"
        },
        "treatment": {
          "description": "0-3 group encoding for CiC).",
          "type": "string"
        },
        "x": {
          "description": "Primary running variable, regressor, or feature input for this estimator.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "treatment",
        "y"
      ],
      "type": "object"
    },
    "name": "distributional_te"
  },
  {
    "description": "Species-diversity indices from record-level or count data: Shannon entropy, species richness, Pielou evenness, the Simpson family (concentration / Gini-Simpson / inverse) and Hill numbers of any order. Accepts long-format sighting records or a site-by-species matrix and groups straight onto a panel index, so an ecological outcome can be built inside the same pipeline that estimates on it. min_records makes the small-sample filter explicit rather than a footnote.",
    "input_schema": {
      "properties": {
        "base": {
          "description": "Log base for Shannon",
          "type": "number"
        },
        "by": {
          "description": "Grouping keys, typically the panel index",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "count": {
          "description": "Abundance column; omit when one row is one record",
          "type": "string"
        },
        "index": {
          "default": "shannon",
          "description": "Index/indices to compute, or 'all'",
          "enum": [
            "shannon",
            "richness",
            "pielou",
            "simpson",
            "gini_simpson",
            "inv_simpson",
            "hill",
            "all"
          ],
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "min_records": {
          "default": 0,
          "description": "Groups below this many records return NaN",
          "type": "integer"
        },
        "q": {
          "default": 1.0,
          "description": "Hill number order",
          "type": "number"
        },
        "species": {
          "description": "Species column (required for long-format input)",
          "type": "string"
        }
      },
      "required": [],
      "type": "object"
    },
    "name": "diversity_index"
  },
  {
    "description": "Neural-net propensity score estimator (arXiv:2404.04794, 2024). Assumptions: Unconfoundedness / selection on observables: treatment is conditionally independent of potential outcomes given covariates; Overlap (positivity): 0 < e(X) < 1 for all units (scores are clipped to [0.02, 0.98]); Covariates are pre-treatment and the MLP correctly approximates e(X) = P(T=1 | X). Pre-conditions: Cross-sectional or pooled DataFrame with a binary treatment column and numeric covariate columns; No missing values in the covariate matrix passed to the network; Enough observations per treatment arm to fit a small MLP without overfitting. Failure modes: Heavy clipping at 0.02/0.98 indicates near-deterministic treatment and positivity violation -> Trim or restrict to the region of common support before using the scores in a weighted estimator; torch absent and sklearn MLP fails to converge in max_iter with lbfgs -> Increase max_iter, scale covariates, or shrink hidden_sizes for a simpler network. Alternatives: sp.overlap_weighted_did, sp.drdid, sp.did. Typical minimum N: 200.",
    "input_schema": {
      "properties": {
        "covariates": {
          "description": "Covariate matrix, DataFrame, or column names.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "hidden_sizes": {
          "description": "hidden_sizes parameter (list).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "treatment": {
          "description": "Treatment indicator, treatment variable, or treatment array.",
          "type": "string"
        }
      },
      "required": [
        "covariates",
        "treatment"
      ],
      "type": "object"
    },
    "name": "dl_propensity_score"
  },
  {
    "description": "Double/Debiased Machine Learning supports PLR, binary-D IRM, PLIV, and binary-D/binary-Z IIVM. Python-only external OOFPredictions supports unweighted, unnormalised IRM ATE scoring with exact alignment; caller_declared records are declarations rather than proof against leakage. Validation: certified evidence with scoped limitations. Known limitations: external_predictions, store_oof, and observation_ids are Python-only and absent from JSON/MCP inputs; external records accept no dict, JSON string, or file path; store_oof=True retains individual-level records for get_oof() and get_residuals() in memory only; ordinary serialization omits them; Retained IRM needs at least 10 rows per treatment arm in each training fold or raises DataInsufficient instead of fallback; internal explicit fold_indices require n_rep=1, while external repeated IRM records are allowed when the explicit partition is equivalent for every repeat. Assumptions: Unconfoundedness: Y(d) perp D | X (conditional ignorability); Overlap: 0 < P(D=1 | X) < 1 for the estimand support (strong for IRM); Nuisance-function estimators converge at op(n^{-1/4}) -- fast enough that orthogonal moments give sqrtn CATE. Pre-conditions...",
    "input_schema": {
      "properties": {
        "covariates": {
          "description": "List of control variable names",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "instrument": {
          "description": "Instrument (required for pliv/iivm)",
          "type": "string"
        },
        "model": {
          "default": "plr",
          "description": "DML model family",
          "enum": [
            "plr",
            "irm",
            "pliv",
            "iivm"
          ],
          "type": "string"
        },
        "n_folds": {
          "default": 5,
          "description": "Cross-fitting folds",
          "type": "integer"
        },
        "n_rep": {
          "default": 1,
          "description": "Repeated cross-fitting splits (median aggregation)",
          "type": "integer"
        },
        "normalize_ipw": {
          "default": false,
          "description": "Self-normalize the inverse-propensity weights for the IPW models (irm/iivm); matches DoubleML's normalize_ipw. Rejected for plr/pliv.",
          "type": "boolean"
        },
        "score": {
          "description": "Orthogonal score variant (DoubleML-compatible). PLR: 'partialling out' (default) or 'IV-type'. IRM: 'ATE' (default) or 'ATTE'. None selects the model default; defaults reproduce historical output exactly.",
          "enum": [
            "partialling out",
            "IV-type",
            "ATE",
            "ATTE"
          ],
          "type": "string"
        },
        "treat": {
          "description": "Treatment variable",
          "type": "string"
        },
        "trimming_threshold": {
          "default": 0.01,
          "description": "Symmetric propensity clip [t, 1-t] for irm/iivm (DoubleML trimming_rule='truncate'). Default 0.01 = historical clip.",
          "type": "number"
        },
        "y": {
          "description": "Outcome",
          "type": "string"
        }
      },
      "required": [
        "covariates",
        "treat",
        "y"
      ],
      "type": "object"
    },
    "name": "dml"
  },
  {
    "description": "Build a :class:`DMLDiagnostics` report from a DML CausalResult. Validation: validated evidence tier (known-truth, reference, external-parity, or Monte Carlo artifact). Assumptions: Unconfoundedness (IRM/PLR) or instrument validity (IIVM/PLIV) given the covariates; Overlap / positivity; Neyman-orthogonal score plus cross-fitting; nuisance estimators converge fast enough (o(n^-1/4)). Pre-conditions: Covariates, treatment, and outcome with enough data for cross-fitted machine-learning nuisances; For instrumented variants (PLIV / IIVM): an instrument as well. Failure modes: Propensity scores near 0/1 -- overlap failure inflates variance and bias -> Trim extreme scores, restrict the estimand to the overlap region, or report sensitivity (dml_sensitivity). Alternatives: sp.tmle, sp.auto_cate, sp.causal_forest. Typical minimum N: 500.",
    "input_schema": {
      "properties": {
        "clip": {
          "default": 0.02,
          "description": "For IRM-style overlap: count units with propensity within ``[0, clip] [1-clip, 1]`` as overlap-violating.",
          "type": "number"
        },
        "result": {
          "description": "Result returned by :func:`statspai.dml.dml`. Must include the post-fit residuals (``model_info['_y_resid']``, ``model_info['_d_resid']``); for IRM, additionally the propensity ``model_info['diagnostics']['pscore_min']`` etc. are surfaced.",
          "type": "string"
        }
      },
      "required": [
        "result"
      ],
      "type": "object"
    },
    "name": "dml_diagnostics"
  },
  {
    "description": "Model-averaging DML (PLR) per Ahrens et al. (2025, JAE). Fits DML-PLR under multiple candidate nuisance learners and reports a risk-weighted (or equal/single-best) average of their theta estimates with a covariance-adjusted SE. Validation: certified parity evidence. Assumptions: Unconfoundedness (IRM/PLR) or instrument validity (IIVM/PLIV) given the covariates; Overlap / positivity; Neyman-orthogonal score plus cross-fitting; nuisance estimators converge fast enough (o(n^-1/4)). Pre-conditions: Covariates, treatment, and outcome with enough data for cross-fitted machine-learning nuisances; For instrumented variants (PLIV / IIVM): an instrument as well. Failure modes: Propensity scores near 0/1 -- overlap failure inflates variance and bias -> Trim extreme scores, restrict the estimand to the overlap region, or report sensitivity (dml_sensitivity). Alternatives: sp.tmle, sp.auto_cate, sp.causal_forest. Typical minimum N: 500.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "candidates": {
          "description": "List of (ml_g, ml_m, label) sklearn triples; defaults to Lasso/Ridge/RF/GBM",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "covariates": {
          "description": "Covariate columns X",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "fold_indices": {
          "description": "Explicit cross-fitting fold per row (column name or array of length ``len(data)``). Overrides ``n_folds`` / ``seed``; every candidate uses the same partition.",
          "type": "string"
        },
        "n_folds": {
          "default": 5,
          "description": "Number of cross-fitting or cross-validation folds.",
          "type": "integer"
        },
        "seed": {
          "default": 0,
          "description": "Random seed for reproducible stochastic steps.",
          "type": "integer"
        },
        "treat": {
          "description": "Treatment column",
          "type": "string"
        },
        "weight_rule": {
          "default": "short_stacking",
          "description": "Weighting of candidate estimators",
          "enum": [
            "short_stacking",
            "single_best",
            "inverse_risk",
            "equal"
          ],
          "type": "string"
        },
        "y": {
          "description": "Outcome column",
          "type": "string"
        }
      },
      "required": [
        "covariates",
        "treat",
        "y"
      ],
      "type": "object"
    },
    "name": "dml_model_averaging"
  },
  {
    "description": "Long-panel Double/Debiased ML for static panel models with fixed effects (Clarke & Polselli 2025, simplified). Absorbs unit (and optional time) fixed effects via within-transform, cross-fits ML nuisance learners with folds that split units, and reports cluster-robust SE at the unit level. PLR moment (continuous or binary treatment). Validation: certified parity evidence. Assumptions: Conditional unconfoundedness within unit: E[epsilon_it | X_it, alpha_i, lambda_t] = 0; Strict exogeneity conditional on covariates (weaker than standard FE); Nuisance learners converge fast enough (op(n^{-1/4})) after within-transform. Pre-conditions: long panel: at least unit and outcome columns; include_time_fe=True needs time column; enough units (clusters) for cluster-robust SE -- >= 30 ideally; enough periods per unit for within-transform to leave variation in the treatment. Failure modes: Few units (< 30) -- cluster-robust SE under-coverage -> Use wild cluster bootstrap (sp.wild_cluster_bootstrap) or CR3 jackknife; Within-unit variation in treatment is near zero -> Unit FE absorbs almost all treatment variation -- switch to between estimator or cross-section; Nuisance cross-val R2 near zero on d...",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "binary_treatment": {
          "default": false,
          "description": "binary_treatment parameter (bool).",
          "type": "boolean"
        },
        "covariates": {
          "description": "Covariate columns X_it",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "fold_indices": {
          "description": "Explicit cross-fitting fold per row (a column name or a length- ``len(data)`` array), constant within unit. Overrides ``n_folds`` and ``seed``; with a shared partition the estimate is reproducible across implementations (e.g.",
          "type": "string"
        },
        "include_time_fe": {
          "default": false,
          "description": "Whether to include time fe.",
          "type": "boolean"
        },
        "ml_g": {
          "description": "Outcome nuisance learner",
          "type": "string"
        },
        "ml_m": {
          "description": "Treatment nuisance learner",
          "type": "string"
        },
        "n_folds": {
          "default": 5,
          "description": "Number of cross-fitting or cross-validation folds.",
          "type": "integer"
        },
        "seed": {
          "default": 0,
          "description": "Random seed for reproducible stochastic steps.",
          "type": "integer"
        },
        "time": {
          "description": "Time column (required if include_time_fe)",
          "type": "string"
        },
        "treat": {
          "description": "Treatment column",
          "type": "string"
        },
        "unit": {
          "description": "Unit ID column (FE + clustering)",
          "type": "string"
        },
        "y": {
          "description": "Outcome column",
          "type": "string"
        }
      },
      "required": [
        "covariates",
        "treat",
        "unit",
        "y"
      ],
      "type": "object"
    },
    "name": "dml_panel"
  },
  {
    "description": "Compute DML-OVB sensitivity for a fitted DML CausalResult. Validation: certified parity evidence. Assumptions: Unconfoundedness (IRM/PLR) or instrument validity (IIVM/PLIV) given the covariates; Overlap / positivity; Neyman-orthogonal score plus cross-fitting; nuisance estimators converge fast enough (o(n^-1/4)). Pre-conditions: Covariates, treatment, and outcome with enough data for cross-fitted machine-learning nuisances; For instrumented variants (PLIV / IIVM): an instrument as well. Failure modes: Propensity scores near 0/1 -- overlap failure inflates variance and bias -> Trim extreme scores, restrict the estimand to the overlap region, or report sensitivity (dml_sensitivity). Alternatives: sp.tmle, sp.auto_cate, sp.causal_forest. Typical minimum N: 500.",
    "input_schema": {
      "properties": {
        "benchmark_covariates": {
          "description": "Subset of the original covariates to benchmark against. For each ``X_k``, the benchmark sets ``cf_y_bench, cf_d_bench`` to the partial R2 that ``X_k`` itself contributes (multiplied by ``k_y, k_d`` to express \"what if a confounder were kx as strong as ``X_k``?\").",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "cf_d": {
          "description": "Hypothesized partial-R2 of an unobserved confounder with the residualised outcome and treatment. If both are given, the report includes a bias bound and adjusted-estimate range.",
          "type": "number"
        },
        "cf_y": {
          "description": "Hypothesized partial-R2 of an unobserved confounder with the residualised outcome and treatment. If both are given, the report includes a bias bound and adjusted-estimate range.",
          "type": "number"
        },
        "k_d": {
          "default": 1.0,
          "description": "Multipliers for the benchmark strengths.",
          "type": "number"
        },
        "k_y": {
          "default": 1.0,
          "description": "Multipliers for the benchmark strengths.",
          "type": "number"
        },
        "q": {
          "default": 1.0,
          "description": "Bias threshold as a fraction of |theta|. ``q=1`` => confounder needed to shrink estimate to zero; ``q=0.5`` => half the estimate.",
          "type": "number"
        },
        "result": {
          "description": "result parameter.",
          "type": "string"
        }
      },
      "required": [
        "result"
      ],
      "type": "object"
    },
    "name": "dml_sensitivity"
  },
  {
    "description": "Dose-response function for a continuous treatment under unconfoundedness. Uses generalised propensity-score weighting or double ML for the conditional expectation E[Y(d)]. Validation: certified parity evidence. Assumptions: Weak unconfoundedness: Y(d) perp D | X for each d; Generalised overlap: positive conditional density of D at each evaluated dose; Smoothness of dose-response function (for local-polynomial / kernel smoothing). Pre-conditions: treat is continuous (numeric, not binary); covariates comprise the confounding set; n >= 1000 for stable dose-response curves. Failure modes: Sparse data at extreme doses -> Narrow dose_range; CIs at tails will be wide and uninformative; Heavy-tailed generalised propensity weights -> Use stabilised weights or restrict to common-support dose window. Alternatives: sp.dml, sp.metalearner, sp.causal_forest. Typical minimum N: 1000.",
    "input_schema": {
      "properties": {
        "covariates": {
          "description": "Covariate matrix, DataFrame, or column names.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "dose_range": {
          "description": "(lo, hi) over which to evaluate dose-response",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "n_bootstrap": {
          "default": 200,
          "description": "Number of bootstrap replications.",
          "type": "integer"
        },
        "n_dose_points": {
          "default": 20,
          "description": "Number of dose points.",
          "type": "integer"
        },
        "treat": {
          "description": "Continuous treatment / dose",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "covariates",
        "treat",
        "y"
      ],
      "type": "object"
    },
    "name": "dose_response"
  },
  {
    "description": "Double negative control estimator (Miao et al. 2018; Shi et al. 2020). Assumptions: Two valid proxies of the hidden confounder U: a treatment-confounding proxy and an outcome-confounding proxy; Negative-control exposure has no causal effect on Y given (U, X) -- exclusion restriction on the NCE arm; Negative-control outcome is not caused by the treatment D given (U, X). Pre-conditions: data has columns for outcome, treatment, a negative-control exposure (nce) and a negative-control outcome (nco); nce and nco share the unmeasured confounder but satisfy the respective exclusion restrictions. Failure modes: Weak proxy: NCE barely correlated with NCO (weak-instrument analogue) -- unstable ATE -> Use stronger proxies or the doubly-robust regression-based estimator instead of just-identified 2SLS; A proxy violates its exclusion restriction (direct effect on Y or caused by D) -- biased ATE -> Validate the negative controls separately or fall back to a sensitivity analysis. Alternatives: sp.proximal, sp.proximal_regression, sp.frontdoor. Typical minimum N: 200.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "covariates": {
          "description": "Covariate matrix, DataFrame, or column names.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "nce": {
          "description": "nce parameter (str).",
          "type": "string"
        },
        "nco": {
          "description": "nco parameter (str).",
          "type": "string"
        },
        "treat": {
          "description": "Treatment indicator or first-treatment-period column.",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "nce",
        "nco",
        "treat",
        "y"
      ],
      "type": "object"
    },
    "name": "double_negative_control"
  },
  {
    "description": "Doubly-robust OPE (Dudik et al. 2011). Validation: certified parity evidence. Assumptions: Doubly robust: consistent if EITHER the outcome (Q) model OR the logging propensity model is correctly specified; Positivity / common support holds; No unmeasured confounding in the logged data. Pre-conditions: X, A, R, logging propensities and a fitted Q-model (or its predictions) are available. Failure modes: Both nuisance models misspecified -- DR guarantee is lost and the estimate is biased -> Cross-fit the nuisances or validate the Q-model and propensity fit separately. Alternatives: sp.ips, sp.snips, sp.direct_method. Typical minimum N: 500.",
    "input_schema": {
      "properties": {
        "A": {
          "description": "A parameter (np.ndarray).",
          "type": "string"
        },
        "R": {
          "description": "R parameter (np.ndarray).",
          "type": "string"
        },
        "X": {
          "description": "Feature matrix or covariate DataFrame.",
          "type": "string"
        },
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "clip": {
          "default": 50.0,
          "description": "clip parameter (float).",
          "type": "number"
        },
        "n_actions": {
          "description": "Number of actions.",
          "type": "integer"
        },
        "pi_behavior": {
          "description": "pi_behavior parameter (Optional[np.ndarray]).",
          "type": "string"
        },
        "pi_target": {
          "description": "pi_target parameter.",
          "type": "string"
        },
        "q_hat": {
          "description": "q_hat parameter (Optional[np.ndarray]).",
          "type": "string"
        }
      },
      "required": [
        "A",
        "R",
        "X",
        "pi_target"
      ],
      "type": "object"
    },
    "name": "doubly_robust"
  },
  {
    "description": "Doubly-robust DiD (Sant'Anna & Zhao 2020). Combines outcome regression with IPW; consistent if either model is correct. Primary estimator for 2x2 DiD with covariates. Validation: certified parity evidence. Assumptions: Conditional parallel trends given X; Overlap / positivity: 0 < P(D=1|X) < 1; Correct specification of at least one nuisance model. Pre-conditions: panel or repeated cross-section with 2 periods; group is a binary unit-level treatment indicator; covariates have non-zero variance and overlap. Failure modes: Propensity score near 0/1 (overlap violation) -> Trim extreme propensity scores or use sp.ipw_trim. Alternatives: sp.did_2x2, sp.callaway_santanna, sp.wooldridge_did. Typical minimum N: 100.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "covariates": {
          "description": "Covariates X",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "est_method": {
          "default": "dr",
          "description": "Estimator family. With method/normalized/locally_efficient and id=, this reaches all 14 R DRDID 1.2.3 estimators: dr -> drdid_[imp_]panel / drdid_[imp_]rc[1]; ipw -> [std_]ipw_did_panel|rc; reg -> reg_did_panel|rc; twfe -> twfe_did_panel|rc. 'twfe' is for comparison, not recommendation: with covariates it is the specification Sant'Anna-Zhao and Caetano-Callaway warn about.",
          "enum": [
            "dr",
            "ipw",
            "reg",
            "twfe"
          ],
          "type": "string"
        },
        "group": {
          "description": "Unit-level treatment indicator (0/1)",
          "type": "string"
        },
        "id": {
          "description": "Unit identifier for true two-period panel DR-DID",
          "type": "string"
        },
        "locally_efficient": {
          "default": true,
          "description": "est_method='dr' on repeated cross-sections only. False drops the semiparametric-efficiency terms, giving drdid_rc1 / drdid_imp_rc1, which avoid fitting outcome regressions on the treated cells. Both are consistent.",
          "type": "boolean"
        },
        "method": {
          "default": "imp",
          "description": "Nuisance estimators for est_method='dr' (R DRDID::drdid(estMethod=)): 'imp' uses inverse probability tilting + odds-weighted least squares so the DR moment is Neyman-orthogonal by construction; 'trad' uses plain logit + OLS with the estimation effects propagated. Ignored unless est_method='dr'.",
          "enum": [
            "imp",
            "trad"
          ],
          "type": "string"
        },
        "n_boot": {
          "description": "Deprecated / inert: standard errors come from the Sant'Anna-Zhao influence function on every path.",
          "type": "integer"
        },
        "normalized": {
          "default": true,
          "description": "est_method='ipw' only. True = Hajek-normalised (std_ipw_did_*), control arm divided by its own weight mass. False = Abadie (2005) (ipw_did_*), both arms sharing the denominator E[D].",
          "type": "boolean"
        },
        "seed": {
          "description": "Random seed for reproducible stochastic steps.",
          "type": "integer"
        },
        "time": {
          "description": "Time period column",
          "type": "string"
        },
        "trim_level": {
          "default": 0.995,
          "description": "Drop control units whose propensity score reaches this cutoff (DRDID trim.level). 1.0 disables.",
          "type": "number"
        },
        "weights": {
          "description": "Observation weights column (R DRDID i.weights), renormalised to mean one.",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable",
          "type": "string"
        }
      },
      "required": [
        "group",
        "time",
        "y"
      ],
      "type": "object"
    },
    "name": "drdid"
  },
  {
    "description": "Dynamic Double/Debiased ML for a treatment assigned repeatedly over time (Lewis & Syrgkanis 2021). Use it when the treatment moves the state that drives later treatment: controlling for the later state blocks the indirect path, not controlling for it leaves the confounding in. Returns the effect of intervening on each period's treatment on the final outcome, the total sequence effect, and the joint covariance -- so a contrast across periods has an honest standard error instead of a sum of variances. Optional linear heterogeneity in baseline modifiers. Reproduces econml's DynamicDML to 1e-15 given the same folds and learners. Validation: validated evidence tier (known-truth, reference, external-parity, or Monte Carlo artifact). Assumptions: sequential ignorability given the recorded state, which must include the treatment history (lags >= 1); no unmeasured time-varying confounding; linear-in-treatment structural mean model (SNMM). Pre-conditions: long panel with one row per unit-period; at least two periods; units incomplete over the window are dropped; treatment varies within period. Failure modes: DataInsufficient: the moment system is singular -> The state predicts the treatment...",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "baseline": {
          "description": "Time-invariant controls (first-period values).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "covariates": {
          "description": "Time-varying state read at each period.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "fold_ids": {
          "description": "One fold index per complete unit, to reproduce an external split.",
          "type": "string"
        },
        "id": {
          "description": "Unit id column; unit= is accepted as an alias.",
          "type": "string"
        },
        "lags": {
          "default": 1,
          "description": "Lagged treatments added to each period's state. lags=0 warns: omitting the treatment history moved the test design's estimates by -15%, -15% and +37%.",
          "type": "integer"
        },
        "model_t": {
          "description": "Treatment nuisance; default RidgeCV.",
          "type": "string"
        },
        "model_y": {
          "description": "Outcome nuisance; default RidgeCV.",
          "type": "string"
        },
        "modifiers": {
          "description": "Baseline effect modifiers; the final stage becomes linear in them and result.coef holds the projection.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "n_folds": {
          "default": 5,
          "description": "Folds; whole units held out.",
          "type": "integer"
        },
        "periods": {
          "description": "Periods to use, in order; default every sorted value.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "random_state": {
          "default": 0,
          "description": "Random seed or RandomState for reproducible stochastic steps.",
          "type": "integer"
        },
        "time": {
          "description": "Period column",
          "type": "string"
        },
        "treat": {
          "description": "Treatment column",
          "type": "string"
        },
        "y": {
          "description": "Outcome; only its last-period value is used.",
          "type": "string"
        }
      },
      "required": [
        "id",
        "time",
        "treat",
        "y"
      ],
      "type": "object"
    },
    "name": "dynamic_dml"
  },
  {
    "description": "Hainmueller (2012) entropy balancing. Targets the ATT by exactly balancing covariate means across treatment groups. No propensity-score model specification needed. Validation: certified parity evidence.",
    "input_schema": {
      "properties": {
        "covariates": {
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "moments": {
          "default": 1,
          "description": "Max moment balanced (1=means, 2=vars).",
          "type": "integer"
        },
        "treat": {
          "type": "string"
        },
        "y": {
          "type": "string"
        }
      },
      "required": [
        "y",
        "treat",
        "covariates"
      ],
      "type": "object"
    },
    "name": "ebalance"
  },
  {
    "description": "Olea-Pflueger (2013) robust effective F statistic for weak instruments. Validation: certified parity evidence.",
    "input_schema": {
      "properties": {
        "absorb": {
          "description": "High-dimensional fixed effects to partial out of the endogenous regressor, the instruments and the controls before the first stage -- the same residualisation ``sp.iv(absorb=...)`` performs, so the effective F describes the specification actually fitted. The absorbed degrees of freedom are charged to ``df_resid``.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "cluster": {
          "description": "Cluster the first-stage moment variance. ``Omega`` becomes the (multiway, Cameron-Gelbach-Miller) cluster-sum meat with the ``ivreg2`` finite-sample factor ``G_min/(G_min-1) * (n-1)/(n-K)``. This is the right diagnostic whenever the second stage is",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "endog": {
          "description": "Endogenous regressor (single endogenous variable).",
          "type": "string"
        },
        "exog": {
          "description": "Included exogenous controls (a constant is added automatically).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "instruments": {
          "description": "Excluded instruments.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "vcov": {
          "default": "HC1",
          "description": "Variance estimator for the first-stage residuals: - ``'classic'`` -- homoskedastic; F_eff equals first-stage F. - ``'HC0'`` -- White heteroskedasticity-robust. - ``'HC1'`` -- HC0 with small-sample correction ``n/(n-k)``. Ignored when ``cluster`` is given.",
          "enum": [
            "HC0",
            "HC1",
            "classic"
          ],
          "type": "string"
        }
      },
      "required": [
        "endog",
        "instruments"
      ],
      "type": "object"
    },
    "name": "effective_f_test"
  },
  {
    "description": "Engle-Granger (1987) two-step cointegration test. Validation: certified parity evidence.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Level at which ``rank`` (1 = cointegrated) is decided.",
          "enum": [
            "0.01",
            "0.05",
            "0.10"
          ],
          "type": "number"
        },
        "lags": {
          "description": "Number of lagged differences ``L`` in the step-2 regression (Stata ``egranger, lags()``). If None, uses the fixed rule ``int(4 * (n / 100) ** 0.25)``.",
          "type": "integer"
        },
        "trend": {
          "default": "c",
          "description": "Deterministic terms in step 1: constant; constant + linear trend (``egranger, trend``); constant + linear + quadratic trend (``egranger, qtrend``). The trend is ``t = 0, 1, ...``.",
          "enum": [
            "c",
            "ct",
            "ctt"
          ],
          "type": "string"
        },
        "variables": {
          "description": "Variables to test (first is dependent).",
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "required": [],
      "type": "object"
    },
    "name": "engle_granger"
  },
  {
    "description": "Enhanced event study plot with pre/post shading and significance coloring.",
    "input_schema": {
      "properties": {
        "alpha_level": {
          "default": 0.05,
          "description": "alpha_level parameter (float).",
          "type": "number"
        },
        "ax": {
          "description": "ax parameter.",
          "type": "string"
        },
        "ci_alpha": {
          "default": 0.15,
          "description": "Confidence band transparency.",
          "type": "number"
        },
        "color": {
          "default": "#2C3E50",
          "description": "Default color for estimates.",
          "type": "string"
        },
        "figsize": {
          "default": [
            10,
            6
          ],
          "description": "figsize parameter (Tuple[float, float]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "marker": {
          "default": "o",
          "description": "marker parameter (str).",
          "type": "string"
        },
        "markersize": {
          "default": 6,
          "description": "markersize parameter (int).",
          "type": "integer"
        },
        "post_color": {
          "default": "#FDEDEC",
          "description": "Post-treatment shading color.",
          "type": "string"
        },
        "pre_color": {
          "default": "#EBF5FB",
          "description": "Pre-treatment shading color.",
          "type": "string"
        },
        "result": {
          "description": "DID result with event study in ``model_info['event_study']``.",
          "type": "string"
        },
        "shade_post": {
          "default": true,
          "description": "Shade post-treatment region.",
          "type": "boolean"
        },
        "shade_pre": {
          "default": true,
          "description": "Shade pre-treatment region.",
          "type": "boolean"
        },
        "show_zero": {
          "default": true,
          "description": "Show horizontal zero line.",
          "type": "boolean"
        },
        "sig_color": {
          "default": "#E74C3C",
          "description": "Color for significant estimates. None disables coloring.",
          "type": "string"
        },
        "title": {
          "description": "title parameter (Optional[str]).",
          "type": "string"
        },
        "uniform_band": {
          "default": false,
          "description": "Overlay the sup-t *simultaneous* band (:func:`sp.uniform_bands`), computed separately for the leads and for the post-treatment horizons from the estimator's joint event-study covariance. A pointwise interval covers one horizon; a plot invites a statement about the path, which is what this band licenses. Silently skipped -- with a warning -- when the estimator exposes no event-study covariance at all.",
          "type": "boolean"
        }
      },
      "required": [
        "result"
      ],
      "type": "object"
    },
    "name": "enhanced_event_study_plot"
  },
  {
    "description": "Unified post-estimation diagnostics dispatcher.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for interpretation strings.",
          "type": "number"
        },
        "lags": {
          "default": 1,
          "description": "Number of lags for the Breusch-Godfrey test.",
          "type": "integer"
        },
        "powers": {
          "default": 3,
          "description": "Highest power of y-hat for the RESET test.",
          "type": "integer"
        },
        "print_results": {
          "default": true,
          "description": "If True, print a formatted table to stdout.",
          "type": "boolean"
        },
        "result": {
          "description": "A fitted result object with ``params``, ``data_info``, etc.",
          "type": "string"
        },
        "test": {
          "default": "all",
          "description": "Name of the diagnostic test. One of ``'hettest'``, ``'white'``, ``'reset'``, ``'ovtest'``, ``'bgodfrey'``, ``'dwatson'``, ``'vif'``, ``'ic'``, ``'linktest'``, ``'normality'``, ``'leverage'``, ``'endogenous'``, ``'overid'``, ``'firststage'``, ``'all'``.",
          "type": "string"
        }
      },
      "required": [
        "result"
      ],
      "type": "object"
    },
    "name": "estat"
  },
  {
    "description": "Display a pyfixest-style regression table for StatsPAI results.",
    "input_schema": {
      "properties": {
        "kwargs": {
          "description": "Passed to ``pyfixest.etable()``.",
          "type": "string"
        },
        "results": {
          "description": "One or more fitted results.",
          "type": "string"
        }
      },
      "required": [],
      "type": "object"
    },
    "name": "etable"
  },
  {
    "description": "Endogenous treatment effects model. Validation: certified parity evidence.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "cluster": {
          "description": "Cluster column. Uses Stata's ML cluster factor ``g/(g-1)``.",
          "type": "string"
        },
        "maxiter": {
          "default": 200,
          "description": "maxiter parameter (int).",
          "type": "integer"
        },
        "method": {
          "default": "mle",
          "description": "``'mle'`` for full-information maximum likelihood (Stata's default) or ``'twostep'`` for the control-function estimator (Stata's ``twostep`` option). They are different estimators, not two routes to the same numbers.",
          "type": "string"
        },
        "robust": {
          "default": "nonrobust",
          "description": "``'nonrobust'`` (observed information), ``'robust'`` (sandwich, with Stata's ``N/(N-1)`` factor) or ``'cluster'``. Passing ``cluster=`` implies ``'cluster'``. ``vce=`` is accepted as an alias.",
          "type": "string"
        },
        "tol": {
          "default": 1e-08,
          "description": "Numerical convergence tolerance.",
          "type": "number"
        },
        "treatment": {
          "description": "Binary treatment variable (0/1, both values present).",
          "type": "string"
        },
        "x": {
          "description": "Exogenous regressors.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Outcome variable.",
          "type": "string"
        },
        "z": {
          "description": "Instruments for the selection equation.",
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "required": [
        "treatment",
        "x",
        "y",
        "z"
      ],
      "type": "object"
    },
    "name": "etregress"
  },
  {
    "description": "Extended Two-Way Fixed Effects (Wooldridge 2021). Explicit API mirroring the R etwfe package. The headline reports the treated-observation-weighted simple ATT from etwfe::emfx(type='simple') / Stata jwdid, with cgroup selecting not-yet-treated or never-treated controls. family='poisson'/'logit' switches to Wooldridge (2023) nonlinear ETWFE for count / binary outcomes, reporting the average marginal effect on the response scale. Validation: certified evidence with scoped limitations. Known limitations: cgroup='nevertreated' combined with panel=False (repeated cross-sections) is not yet supported; pass either panel=True with cgroup='nevertreated' or panel=False with cgroup='notyet'; family='poisson'/'logit' with xvar, panel=False, or cgroup='nevertreated' is not yet supported; these raise rather than being silently ignored; family='poisson'/'logit' reports an average marginal effect on the response scale (counts / probability) rather than a link-scale coefficient -- the R etwfe::emfx convention; weights= is not yet supported together with xvar or with family='poisson'/'logit'; both combinations raise; cgroup='nevertreated' combined with panel=False (repeated cross-sections) is not y...",
    "input_schema": {
      "properties": {
        "agg_weights": {
          "default": "estimation",
          "description": "How cohort-by-period cells enter the emfx aggregates when weights= is set (the rules coincide otherwise). 'estimation' (Stata jwdid, estat): each cell weighted by the sum of the estimation weights over its treated observations, so the never-treated simple aggregate equals the weighted Callaway-Sant'Anna simple ATT. 'unit' (R etwfe::emfx): one unit weight per treated observation, estimation weights enter the regression only.",
          "enum": [
            "estimation",
            "unit"
          ],
          "type": "string"
        },
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "cgroup": {
          "default": "notyet",
          "description": "Control group: 'notyet' (not-yet-treated) or 'nevertreated'. The latter is only supported when panel=True.",
          "enum": [
            "notyet",
            "nevertreated"
          ],
          "type": "string"
        },
        "cluster": {
          "description": "Cluster identifier column for clustered standard errors.",
          "type": "string"
        },
        "controls": {
          "description": "Control-variable column names.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "family": {
          "description": "Outcome model. None/'gaussian' is the linear ETWFE. 'poisson' (counts) and 'logit' (binary) fit Wooldridge (2023) nonlinear ETWFE by MLE and report the average marginal effect on the response scale, matching R etwfe::emfx. The nonlinear branch requires panel=True, cgroup='notyet', and no xvar.",
          "enum": [
            "gaussian",
            "poisson",
            "logit",
            "binomial"
          ],
          "type": "string"
        },
        "first_treat": {
          "description": "first_treat parameter (str).",
          "type": "string"
        },
        "group": {
          "description": "Group or cohort identifier.",
          "type": "string"
        },
        "panel": {
          "default": true,
          "description": "If False, treat data as repeated cross-section",
          "type": "boolean"
        },
        "time": {
          "description": "Time period column.",
          "type": "string"
        },
        "weights": {
          "description": "Column of non-negative observation weights. The cohort-by-period regression becomes weighted least squares with R fixest weights= / Stata reghdfe [pw=] semantics (zero-weight rows are dropped, NaN or negative weights raise), keeping the unweighted fit's cluster-robust small-sample convention. Not available with xvar or family='poisson'/'logit'.",
          "type": "string"
        },
        "xvar": {
          "description": "R-style alias for controls",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "first_treat",
        "group",
        "time",
        "y"
      ],
      "type": "object"
    },
    "name": "etwfe"
  },
  {
    "description": "R ``etwfe::emfx``-style aggregated marginal effects for an ETWFE fit. Validation: certified parity evidence. Assumptions: Conditional parallel trends between treated and comparison groups absent treatment; No anticipation of treatment before its onset; Treatment effects may be heterogeneous across cohorts and time (no homogeneity required). Pre-conditions: Panel or repeated cross-section with a unit (or group) identifier and a time identifier; At least one never-treated or not-yet-treated comparison group; Pre-treatment periods to assess parallel trends. Failure modes: Pre-treatment event-study coefficients are jointly non-zero (pre-trend violation) -> Use honest DiD bounds to quantify robustness to trend violations, or condition on covariates; Two-way fixed-effects estimate is contaminated by 'forbidden' comparisons / negative weights -> Use a heterogeneity-robust estimator (Callaway-Sant'Anna, Borusyak et al., Gardner two-stage). Alternatives: sp.callaway_santanna, sp.did, sp.honest_did. Typical minimum N: 100.",
    "input_schema": {
      "properties": {
        "agg_weights": {
          "description": "For a fit estimated with ``weights=``: whether each cohort-by-period cell enters an aggregate with the sum of the estimation weights over its treated observations (``'estimation'``, Stata ``jwdid, estat``) or with its observation count (``'unit'``, R ``etwfe::emfx``). ``None`` (default) reuses the rule the fit was called with (``result.model_info['agg_weights']``). Ignored for unweighted fits, where the two rules coincide.",
          "enum": [
            "estimation",
            "unit"
          ],
          "type": "string"
        },
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals.",
          "type": "number"
        },
        "include_leads": {
          "default": false,
          "description": "For ``type='event'`` and ``type='calendar'``, whether to include pre-treatment relative times (``rel_time < 0``) in the output. These coefficients identify pre-trends and are informative for parallel-trends inspection. Default ``False`` for backward compatibility with earlier versions; set ``True`` for full event-study output matching the R ``etwfe::emfx(type='event')`` default. ``rel_time = -1`` is always the reference category and is excluded.",
          "type": "boolean"
        },
        "result": {
          "description": "Output of :func:`etwfe` or :func:`wooldridge_did`.",
          "type": "string"
        },
        "type": {
          "default": "simple",
          "description": "Aggregation type.",
          "enum": [
            "simple",
            "group",
            "event",
            "calendar"
          ],
          "type": "string"
        },
        "weighting": {
          "default": "treated",
          "description": "Aggregation weights for cohort-level marginal effects. ``'treated'`` uses the number of treated post-period observations, matching R ``etwfe::emfx(type='simple')`` and Stata ``jwdid, estat simple``. ``'cohort'`` preserves the historical StatsPAI cohort-share weighting.",
          "enum": [
            "cohort",
            "treated"
          ],
          "type": "string"
        }
      },
      "required": [
        "result"
      ],
      "type": "object"
    },
    "name": "etwfe_emfx"
  },
  {
    "description": "Compute the E-value for sensitivity to unmeasured confounding. Validation: certified parity evidence. Assumptions: VanderWeele-Ding (2017) E-value: quantifies the minimum confounder-exposure and confounder-outcome risk ratios that could explain away an observed association; The estimate is expressed (or convertible) to a risk-ratio scale; OR and HR map to RR via the rare-outcome approximation (rare=True) or the Ding-VanderWeele common-outcome conversion (rare=False, default); MD/SMD/OLS use exp(0.91*d); The E-value bounds joint confounding but assumes no other bias (selection, measurement, model misspecification). Pre-conditions: A point estimate on a supported scale (RR/OR/HR/MD/SMD/OLS); for an exact risk-difference E-value call sp.evalue_rd with the 2x2 cell counts; Either an SE or an explicit CI to obtain the CI E-value (OLS also needs the outcome sd). Failure modes: Ratio estimate <= 0 supplied for measure RR/OR/HR, or risk difference outside [-1, 1] -> Pass a positive ratio for RR/OR/HR or a difference within [-1, 1], matching the measure argument; Reported E-value near 1.0, i.e. trivially weak confounding overturns the result -> Treat the finding as fragile to unmeasured co...",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level used to build a CI from ``se`` when ``ci`` is not supplied (ratio measures).",
          "type": "number"
        },
        "ci": {
          "description": "Confidence interval on the ``measure`` scale. Takes precedence over ``se`` for ratio measures.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "delta": {
          "default": 1.0,
          "description": "Contrast size for ``OLS`` (E-value for a ``delta``-unit change in the exposure).",
          "type": "number"
        },
        "estimate": {
          "description": "Point estimate on the scale given by ``measure``: - ``'RR'`` risk ratio, ``'OR'`` odds ratio, ``'HR'`` hazard ratio (all must be > 0); - ``'MD'`` / ``'SMD'`` standardised mean difference; - ``'OLS'`` raw linear-regression coefficient (supply ``sd``); - ``'DIFF'`` / ``'RD'`` risk difference (approximate scalar path;",
          "type": "number"
        },
        "measure": {
          "default": "RR",
          "description": "One of ``'RR'``, ``'OR'``, ``'HR'``, ``'MD'``, ``'SMD'``, ``'OLS'``, ``'DIFF'``, ``'RD'``.",
          "type": "string"
        },
        "rare": {
          "description": "For ``OR`` / ``HR`` only: whether the rare-outcome approximation applies. ``rare=True`` treats OR/HR ~ RR; ``rare=False`` (the default) uses the exact common-outcome conversion to the RR scale (``sqrt(OR)`` for OR; the Ding-VanderWeele formula for HR).",
          "type": "boolean"
        },
        "rare_outcome": {
          "description": "Deprecated alias for ``rare`` (kept for backwards compatibility).",
          "type": "boolean"
        },
        "sd": {
          "description": "Outcome standard deviation, required for ``measure='OLS'`` to standardise the coefficient.",
          "type": "number"
        },
        "se": {
          "description": "Standard error of ``estimate``. Required for ``MD`` / ``OLS`` to obtain a confidence-interval E-value; for ratio measures it is used (with ``alpha``) to build a CI when ``ci`` is not given.",
          "type": "number"
        },
        "true": {
          "description": "Reference value the confounding would have to move the estimate to. Defaults to the null: 1 for ratio measures, 0 for difference measures. A non-null ``true`` gives a \"non-null\" E-value.",
          "type": "number"
        }
      },
      "required": [
        "estimate"
      ],
      "type": "object"
    },
    "name": "evalue"
  },
  {
    "description": "Compute an E-value from a StatsPAI CausalResult object. Validation: certified parity evidence.",
    "input_schema": {
      "properties": {
        "measure": {
          "default": "SMD",
          "description": "How to interpret ``result.estimate`` (for ATE/ATT on continuous outcomes ``'SMD'`` is appropriate; pass ``'RR'`` / ``'OR'`` / ``'HR'`` for ratio estimates). Passed through to :func:`evalue`.",
          "type": "string"
        },
        "rare": {
          "description": "rare parameter (Optional[bool]).",
          "type": "boolean"
        },
        "rare_outcome": {
          "description": "rare_outcome parameter (Optional[bool]).",
          "type": "boolean"
        },
        "result": {
          "description": "Result from any StatsPAI causal estimator exposing a scalar ``estimate`` (and ideally ``se`` / ``ci``).",
          "type": "string"
        },
        "true": {
          "description": "true parameter (Optional[float]).",
          "type": "number"
        }
      },
      "required": [
        "result"
      ],
      "type": "object"
    },
    "name": "evalue_from_result"
  },
  {
    "description": "Exact E-value for a risk difference from a 2x2 table. Validation: certified parity evidence.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for the confidence-limit E-value.",
          "type": "number"
        },
        "grid": {
          "default": 0.0001,
          "description": "Step size of the bias-factor grid search for the CI E-value.",
          "type": "number"
        },
        "n00": {
          "description": "Unexposed cases and unexposed non-cases.",
          "type": "number"
        },
        "n01": {
          "description": "Unexposed cases and unexposed non-cases.",
          "type": "number"
        },
        "n10": {
          "description": "Exposed cases and exposed non-cases.",
          "type": "number"
        },
        "n11": {
          "description": "Exposed cases and exposed non-cases.",
          "type": "number"
        },
        "true": {
          "default": 0.0,
          "description": "Reference risk difference (must be <= the observed RD).",
          "type": "number"
        }
      },
      "required": [
        "n00",
        "n01",
        "n10",
        "n11"
      ],
      "type": "object"
    },
    "name": "evalue_rd"
  },
  {
    "description": "E-value computed directly from a risk ratio and its CI bounds. Validation: certified parity evidence.",
    "input_schema": {
      "properties": {
        "rare_outcome": {
          "default": false,
          "description": "Passed through to :func:`evalue` for rare-outcome OR->RR correction.",
          "type": "boolean"
        },
        "rr": {
          "description": "Point-estimate risk ratio.",
          "type": "number"
        },
        "rr_lower": {
          "description": "Optional confidence-interval bounds on the risk ratio scale.",
          "type": "number"
        },
        "rr_upper": {
          "description": "Optional confidence-interval bounds on the risk ratio scale.",
          "type": "number"
        }
      },
      "required": [
        "rr"
      ],
      "type": "object"
    },
    "name": "evalue_rr"
  },
  {
    "description": "Traditional OLS event-study with entity and time FEs. Generates relative-time dummies around the treatment date, omits a reference period, and estimates via TWFE + optional clustered SE. Exposed for users who want the classical specification alongside CS / SA / BJS; not robust to staggered-effect heterogeneity -- use sp.sun_abraham for that. Validation: certified parity evidence. Do NOT use when: only one pre-treatment period is available -- there are no leads to test parallel trends with, so the plot cannot support a pre-trend claim; treatment timing is staggered and heterogeneous -- a pooled TWFE event study contaminates leads with other cohorts' treated periods; use sp.sun_abraham or sp.callaway_santanna. Assumptions: Parallel trends across event time; No anticipation beyond window lead; SUTVA. Pre-conditions: panel with unit x time x outcome; treat_time column gives first-treatment period (or 0/NaN). Failure modes: Staggered heterogeneity -- TWFE event-study biased -> Use sp.sun_abraham for contamination-robust event-study coefficients. Alternatives: sp.sun_abraham, sp.callaway_santanna, sp.did_imputation. Typical minimum N: 50.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "bin_width": {
          "description": "Group relative time into bins of this width instead of one coefficient per period. Bins are anchored at the treatment boundary, so post-treatment bins tile [0, k-1], [k, 2k-1], ... and pre-treatment bins tile [-k, -1], [-2k, -k-1], ...",
          "type": "integer"
        },
        "cluster": {
          "description": "Cluster identifier column for clustered standard errors.",
          "type": "string"
        },
        "covariates": {
          "description": "Covariate matrix, DataFrame, or column names.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "expose_pre_vcov": {
          "default": false,
          "description": "Publish the true pre-period covariance into model_info['vcv_pre']. With True, pretrends_test / pretrends_power / sensitivity_rr / honest_did use the full covariance of the pre-treatment coefficients; with False (default) the key is withheld and those tools fall back to a diagonal covariance and warn.",
          "type": "boolean"
        },
        "ref_period": {
          "default": -1,
          "description": "Reference relative-time period to omit",
          "type": "integer"
        },
        "time": {
          "description": "Time period column.",
          "type": "string"
        },
        "treat_time": {
          "description": "First-treatment period column",
          "type": "string"
        },
        "unit": {
          "description": "Unit identifier",
          "type": "string"
        },
        "weights": {
          "description": "Analytical weights column, as Stata's [aweight=...].",
          "type": "string"
        },
        "window": {
          "default": [
            -4,
            4
          ],
          "description": "(lead, lag) horizons",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "time",
        "treat_time",
        "unit",
        "y"
      ],
      "type": "object"
    },
    "name": "event_study"
  },
  {
    "description": "Report how each DiD estimator builds its event-study reference periods. Two implementations can agree on every post-treatment coefficient and still plot different pre-trends, because the leads are a separate construction (Roth 2026). Returns the convention registry: what each half of the path is differenced against, whether the two halves are symmetric, and whether the path coincides with a dynamic TWFE event study in a non-staggered design. Failure modes: Estimator name not in the convention registry -> Call sp.event_study_convention() with no argument to list the recorded estimators. Alternatives: sp.compare_event_study_conventions, sp.event_study.",
    "input_schema": {
      "properties": {
        "estimator": {
          "description": "Registry key such as 'callaway_santanna[base_period=varying]', or a bare estimator name for all of its option-specific rows. None returns the whole registry.",
          "type": "string"
        }
      },
      "required": [],
      "type": "object"
    },
    "name": "event_study_convention"
  },
  {
    "description": "Adapter that turns an event-study fit into a regtable input.",
    "input_schema": {
      "properties": {
        "include_reference": {
          "default": false,
          "description": "Whether to render the reference period row (typically ``t=-1``) where the estimate is identically zero.",
          "type": "boolean"
        },
        "label_fmt": {
          "default": "t={t}",
          "description": "Format string for the row label of each event-time bin. The ``{t}`` placeholder receives the integer relative time.",
          "type": "string"
        },
        "regex": {
          "description": "Pattern with one capture group that matches the relative time in coefficient names. Required when the CausalResult fast path is not applicable. Examples: ``r\"^tau_(-?\\d+)$\"``, ``r\"^lag(\\d+)$\"``, ``r\"::(-?\\d+)$\"``.",
          "type": "string"
        },
        "result": {
          "description": "Fitted model. The CausalResult fast path is used automatically when ``model_info['event_study']`` is present.",
          "type": "string"
        }
      },
      "required": [
        "result"
      ],
      "type": "object"
    },
    "name": "event_study_table"
  },
  {
    "description": "Event-study coefficients and their joint covariance from any DiD event-study fit (callaway_santanna/aggte, event_study, sun_abraham, gardner_did, did_imputation, stacked_did, lp_did, did_multiplegt_dyn, etwfe). Feeds uniform bands and HonestDiD; flags joint=False when only diagonal or block-diagonal covariance exists. Alternatives: sp.uniform_bands, sp.honest_did.",
    "input_schema": {
      "properties": {
        "allow_diagonal": {
          "default": true,
          "description": "Fall back to diag(se^2) with a warning when no joint covariance exists",
          "type": "boolean"
        },
        "result": {
          "description": "Fitted event-study estimator",
          "type": "string"
        }
      },
      "required": [
        "result"
      ],
      "type": "object"
    },
    "name": "event_study_vcov"
  },
  {
    "description": "Return runnable code examples + registry metadata for a function.",
    "input_schema": {
      "properties": {
        "name": {
          "description": "Canonical StatsPAI function name (e.g. ``\"did\"``, ``\"regress\"``, ``\"callaway_santanna\"``). Lower-cased and stripped before lookup.",
          "type": "string"
        }
      },
      "required": [
        "name"
      ],
      "type": "object"
    },
    "name": "examples"
  },
  {
    "description": "Fairlie (2005) nonlinear decomposition for binary outcomes. Validation: certified parity evidence.",
    "input_schema": {
      "properties": {
        "group": {
          "description": "Group or cohort identifier.",
          "type": "string"
        },
        "model": {
          "default": "logit",
          "description": "Model variant or parameterisation to fit.",
          "enum": [
            "logit",
            "probit"
          ],
          "type": "string"
        },
        "n_sim": {
          "default": 500,
          "description": "Number of sim.",
          "type": "integer"
        },
        "reference": {
          "default": 0,
          "description": "``reference()``). The switching always runs from A's covariates to B's, so the contributions share the sign of the explained gap.",
          "enum": [
            "0",
            "1"
          ],
          "type": "integer"
        },
        "seed": {
          "default": 12345,
          "description": "Random seed for reproducible stochastic steps.",
          "type": "integer"
        },
        "x": {
          "description": "Primary running variable, regressor, or feature input for this estimator.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "enum": [
            "0",
            "1"
          ],
          "type": "string"
        }
      },
      "required": [
        "group",
        "x",
        "y"
      ],
      "type": "object"
    },
    "name": "fairlie"
  },
  {
    "description": "Run FCI. Returns a :class:`FCIResult` with the learned PAG. Validation: validated evidence tier (known-truth, reference, external-parity, or Monte Carlo artifact). Assumptions: Causal Markov condition and faithfulness (PC/GES/FCI); Causal sufficiency for PC/GES (no latent confounders); FCI relaxes this; Acyclicity; LiNGAM additionally assumes a linear non-Gaussian model. Pre-conditions: Constraint-/score-based discovery needs i.i.d. observational data with enough samples for reliable conditional-independence tests; Invariance-based discovery (ICP) needs data labelled by environment / intervention. Failure modes: Unstable skeleton / many undirected edges -- faithfulness or sample size is the likely culprit -> Increase the sample, relax the CI-test threshold, or switch to FCI if latent confounders are plausible. Alternatives: sp.pc_algorithm, sp.fci, sp.ges, sp.lingam. Typical minimum N: 500.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for CI tests.",
          "type": "number"
        },
        "ci_test": {
          "default": "fisherz",
          "description": "Only Fisher-Z partial-correlation test is supported; extensions (kernel / chi-square) can be added later.",
          "type": "string"
        },
        "max_cond_size": {
          "description": "Max size of conditioning set.",
          "type": "integer"
        },
        "variables": {
          "description": "Columns to use; defaults to all numeric columns.",
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "required": [],
      "type": "object"
    },
    "name": "fci"
  },
  {
    "description": "Counterfactual estimators for time-series cross-sectional data (Liu, Wang and Xu 2024): impute the untreated potential outcome of every treated unit-period from a model fitted on untreated cells only -- two-way fixed effects ('fe', the imputation estimator), interactive fixed effects with r factors ('ife'), or nuclear-norm matrix completion ('mc') -- and average Y - Y(0) over treated cells, with the ATT path by relative period. Native port of the R package fect; handles staggered adoption, many treated units, unbalanced panels and treatment reversals. Validation: certified evidence with scoped limitations. Known limitations: Inference is resampling-only (unit bootstrap or jackknife on request); the default returns point estimates only; r and lam are user-supplied; fect's cross-validated choice of r / lambda is not yet supported. Assumptions: Y(0) follows the chosen model (two-way FE / low-rank factors / low nuclear norm) on untreated cells; No anticipation and no carryover after treatment ends; Strict exogeneity of treatment status conditional on the fixed effects / factors. Pre-conditions: long panel with unit x time x outcome and a 0/1 treatment status; every retained unit has a...",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "covariates": {
          "description": "Time-varying covariates entering the Y(0) model linearly",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "criterion": {
          "default": "mspe",
          "description": "CV score minimised (fect criterion): pooled MSPE, geometric-mean MSPE, count-weighted mean absolute residual by relative period, or fect's PC information criterion (method='ife' only, no holdout).",
          "enum": [
            "mspe",
            "gmspe",
            "moment",
            "pc"
          ],
          "type": "string"
        },
        "cv": {
          "default": false,
          "description": "Choose r (method='ife') or lam (method='mc') by fect's cross-validation (fect CV = TRUE); the CV table is stored in model_info['cv'].",
          "type": "boolean"
        },
        "cv_buffer": {
          "default": 1,
          "description": "Periods hidden before the anchor in the rolling holdout (fect cv.buffer).",
          "type": "integer"
        },
        "cv_donut": {
          "default": 1,
          "description": "Cells removed at each end of a block before scoring ('block' / 'treated_units'; fect cv.donut).",
          "type": "integer"
        },
        "cv_method": {
          "default": "rolling",
          "description": "Holdout design (fect cv.method): 'rolling' anchors a block of cv_nobs periods per sampled unit and hides everything after it; 'block' hides cv_nobs-period blocks from all units' untreated cells; 'treated_units' draws those blocks from treated units' pre-treatment cells only.",
          "enum": [
            "rolling",
            "block",
            "treated_units"
          ],
          "type": "string"
        },
        "cv_nobs": {
          "default": 3,
          "description": "Periods per holdout block (fect cv.nobs).",
          "type": "integer"
        },
        "cv_prop": {
          "default": 0.1,
          "description": "fect cv.prop: share of eligible units sampled per fold ('rolling') or of untreated cells hidden per fold ('block' / 'treated_units').",
          "type": "number"
        },
        "cv_rule": {
          "default": "1se",
          "description": "Re-pick after the scan (fect cv.rule): smallest r / largest lam within one fold-level SE of the minimum, within 1 %, or the minimum.",
          "enum": [
            "1se",
            "min",
            "1pct"
          ],
          "type": "string"
        },
        "force": {
          "default": "two-way",
          "description": "Additive fixed effects in the Y(0) model.",
          "enum": [
            "none",
            "unit",
            "time",
            "two-way"
          ],
          "type": "string"
        },
        "k": {
          "default": 20,
          "description": "Number of CV folds (fect k).",
          "type": "integer"
        },
        "lam": {
          "description": "Nuclear-norm penalty on fect's raw scale (method='mc'); the result records lambda_norm = lam / largest singular value.",
          "type": "number"
        },
        "lambda_grid": {
          "description": "Explicit penalty grid for method='mc' with cv=True (fect lambda = c(...)); overrides nlambda.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "max_iter": {
          "default": 1000,
          "description": "Maximum EM iterations",
          "type": "integer"
        },
        "method": {
          "default": "fe",
          "description": "Y(0) model: 'fe' two-way fixed effects, 'ife' interactive fixed effects with r factors, 'mc' matrix completion with penalty lam.",
          "enum": [
            "fe",
            "ife",
            "mc"
          ],
          "type": "string"
        },
        "min_t0": {
          "description": "Drop units with fewer untreated periods (fect: 1 for 'fe', 5 otherwise)",
          "type": "integer"
        },
        "n_boot": {
          "default": 200,
          "description": "Bootstrap replications",
          "type": "integer"
        },
        "nlambda": {
          "default": 10,
          "description": "Length of fect's default penalty grid for method='mc' with cv=True: nlambda - 1 values log-spaced over three decades below the largest singular value, plus 0.",
          "type": "integer"
        },
        "r": {
          "default": 0,
          "description": "Number of latent factors (method='ife')",
          "type": "integer"
        },
        "r_range": {
          "description": "(r_min, r_max) factor-number grid for cv=True; default (0, 5), capped by the data as fect does.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "random_state": {
          "description": "Seed for the CV fold draws (R's fold stream is not reproducible from numpy; the selection is compared to R statistically).",
          "type": "integer"
        },
        "seed": {
          "description": "Bootstrap seed",
          "type": "integer"
        },
        "time": {
          "description": "Time period column",
          "type": "string"
        },
        "tol": {
          "default": 0.001,
          "description": "EM relative convergence tolerance (fect default)",
          "type": "number"
        },
        "treat": {
          "description": "0/1 treatment status in each unit-period (1 = treated)",
          "type": "string"
        },
        "unit": {
          "description": "Unit identifier",
          "type": "string"
        },
        "vce": {
          "description": "Resampling standard errors over units; None reports point estimates only.",
          "enum": [
            "bootstrap",
            "jackknife"
          ],
          "type": "string"
        },
        "y": {
          "description": "Outcome column",
          "type": "string"
        }
      },
      "required": [
        "time",
        "treat",
        "unit",
        "y"
      ],
      "type": "object"
    },
    "name": "fect"
  },
  {
    "description": "Estimate GLM (logit, probit, Gaussian) with high-dimensional fixed effects. Validation: certified parity evidence.",
    "input_schema": {
      "properties": {
        "cluster": {
          "description": "Cluster id column for the extended ``vce=`` menu (also a shorthand for one-way ``{\"CRV1\": cluster}``).",
          "type": "string"
        },
        "conley_cutoff": {
          "description": "conley_cutoff parameter (Optional[float]).",
          "type": "number"
        },
        "conley_lat": {
          "description": "conley_lat parameter (Optional[str]).",
          "type": "string"
        },
        "conley_lon": {
          "description": "conley_lon parameter (Optional[str]).",
          "type": "string"
        },
        "family": {
          "default": "gaussian",
          "description": "GLM family: ``\"gaussian\"``, ``\"logit\"``, ``\"probit\"``.",
          "type": "string"
        },
        "fml": {
          "description": "pyfixest formula.",
          "type": "string"
        },
        "seed": {
          "description": "RNG seed for sampled (non-enumerated) ``vce=\"wild\"`` draws.",
          "type": "integer"
        },
        "vcov": {
          "description": "Variance-covariance estimator (``vce=`` is the canonical alias). Also accepts ``vce=\"CR2\"``/``\"CR3\"``/``\"jackknife\"`` (with ``cluster=``) for the clubSandwich bias-reduced cluster-robust SEs, and ``vce=\"wild\"`` (with ``cluster=``) for the restricted score wild cluster bootstrap (Kline-Santos 2012; bit-exact vs Stata ``boottest`` in the enumerated regime).",
          "type": "object"
        },
        "wild_reps": {
          "default": 9999,
          "description": "Replications for ``vce=\"wild\"``. When ``2**G <= wild_reps`` the full Rademacher grid is enumerated (deterministic).",
          "type": "integer"
        },
        "wild_weight_type": {
          "default": "rademacher",
          "description": "Wild weight distribution (``\"rademacher\"`` or ``\"webb\"``).",
          "type": "string"
        }
      },
      "required": [
        "fml"
      ],
      "type": "object"
    },
    "name": "feglm"
  },
  {
    "description": "Estimate OLS / IV with high-dimensional fixed effects via pyfixest. Validation: certified parity evidence. Do NOT use when: vce='conley' on more than ~20,000 rows -- that path is dense O(n^2) (see cost); use sp.conley on the fitted result, which is sparse and scales. Cost: Default (OLS / HC / CRV1) is linear in n. vce='conley' is the exception: it calls conley_vcov_matrix, which materialises several dense n x n float64 arrays (lat/lon differences, distances, the uniform kernel) -- ~0.8 GB at n=10,000, ~80 GB at n=100,000, ~157 GB at n=140,000. Prefer sp.conley (sparse cKDTree) above ~20,000 rows. Assumptions: Strict exogeneity conditional on the absorbed fixed effects; No perfect collinearity after FE absorption (within-transformation rank); Cluster structure for `vcov={'CRV1': '...'}` matches the relevant dependence. Pre-conditions: Data is a long-format DataFrame; FE columns are categorical or convertible; Every absorbed FE level has more than one observation (singleton dropping behaviour controlled by `drop_singletons`); Optional IV stage: instruments are at least as many as endogenous regressors. Failure modes: Singleton groups dropped warning -> Aggregate small categories or...",
    "input_schema": {
      "properties": {
        "cluster": {
          "description": "Cluster id column for the extended ``vce=`` menu; also a shorthand for one-way ``{\"CRV1\": cluster}``.",
          "type": "string"
        },
        "collin_tol": {
          "default": 1e-06,
          "description": "Collinearity tolerance.",
          "type": "number"
        },
        "conley_cutoff": {
          "description": "Conley distance cutoff in km for ``vce=\"conley\"``.",
          "type": "number"
        },
        "conley_lat": {
          "description": "Coordinate columns (decimal degrees) for ``vce=\"conley\"``.",
          "type": "string"
        },
        "conley_lon": {
          "description": "Coordinate columns (decimal degrees) for ``vce=\"conley\"``.",
          "type": "string"
        },
        "fixef_rm": {
          "default": "none",
          "description": "How to handle singleton fixed effects: ``\"none\"`` (keep) or ``\"singleton\"`` (drop).",
          "type": "string"
        },
        "fml": {
          "description": "A pyfixest-style formula. Examples: - ``\"Y ~ X1 + X2\"`` -- plain OLS - ``\"Y ~ X1 | firm + year\"`` -- two-way fixed effects - ``\"Y ~ 1 | firm | X1 ~ Z1\"`` -- IV with fixed effects - ``\"Y ~ X1 | csw0(firm, year)\"`` -- multiple estimations",
          "type": "string"
        },
        "lean": {
          "default": false,
          "description": "If True, drop large intermediate arrays to save memory.",
          "type": "boolean"
        },
        "seed": {
          "description": "RNG seed for ``vce=\"wild\"``.",
          "type": "integer"
        },
        "ssc": {
          "description": "Small-sample correction via ``pyfixest.ssc()``.",
          "type": "string"
        },
        "vcov": {
          "description": "Variance-covariance estimator (``vce=`` is the canonical alias). - ``\"iid\"`` -- classical - ``\"HC1\"``, ``\"HC2\"``, ``\"HC3\"`` -- heteroskedasticity-robust - ``{\"CRV1\": \"firm\"}`` -- cluster-robust - ``{\"CRV1\": \"firm + year\"}`` -- two-way clustering - ``vce=\"CR2\"`` / ``\"CR3\"`` / ``\"jackknife\"`` (with ``cluster=``) -- Pustejovsky-Tipton bias-reduced cluster-robust on the FE-absorbed within design; matches R ``clubSandwich::vcovCR(plm)``. - ``vce=\"wild\"`` (with ``cluster=``) -- WCR wild cluster bootstrap (Cameron-Gelbach-Miller 2008); validated against Stata ``boottest``. - ``vce=\"conley\"`` (with ``conley_lat=/conley_lon=/conley_cutoff=``) -- Conley spatial HAC (Stata ``acreg`` planar-distance convention).",
          "type": "object"
        },
        "weights": {
          "description": "Column name for regression weights.",
          "type": "string"
        },
        "wild_reps": {
          "default": 999,
          "description": "Bootstrap replications for ``vce=\"wild\"``.",
          "type": "integer"
        },
        "wild_weight_type": {
          "default": "rademacher",
          "description": "Wild weight distribution (``\"rademacher\"``, ``\"webb\"``, ``\"mammen\"``).",
          "type": "string"
        }
      },
      "required": [
        "fml"
      ],
      "type": "object"
    },
    "name": "feols"
  },
  {
    "description": "Estimate Poisson regression with high-dimensional fixed effects via pyfixest. Validation: certified parity evidence. Assumptions: Conditional mean exponential link: E[y | x, alpha] = exp(x'beta + alpha); Strict exogeneity conditional on the absorbed fixed effects (PPML consistency). Pre-conditions: Outcome is a non-negative count or non-negative continuous variable; Fixed effects columns are categorical; absorbed groups exist. Failure modes: Convergence failure or extreme exponentiated predictions -> Drop large-magnitude regressors, rescale, or switch to OLS on log(1+y) (with caveats); Separation: some FE level perfectly predicts zero outcomes -> Drop perfectly-predicted groups and rerun; document the restriction. Alternatives: sp.feols, sp.regress, sp.panel. Typical minimum N: 200.",
    "input_schema": {
      "properties": {
        "cluster": {
          "description": "Cluster id column for the extended ``vce=`` menu; also a shorthand for one-way ``{\"CRV1\": cluster}``.",
          "type": "string"
        },
        "collin_tol": {
          "default": 1e-06,
          "description": "Collinearity tolerance.",
          "type": "number"
        },
        "conley_cutoff": {
          "description": "conley_cutoff parameter (Optional[float]).",
          "type": "number"
        },
        "conley_lat": {
          "description": "conley_lat parameter (Optional[str]).",
          "type": "string"
        },
        "conley_lon": {
          "description": "conley_lon parameter (Optional[str]).",
          "type": "string"
        },
        "fixef_rm": {
          "default": "none",
          "description": "Singleton fixed effect handling.",
          "type": "string"
        },
        "fml": {
          "description": "pyfixest formula. E.g. ``\"Y ~ X1 | firm\"``.",
          "type": "string"
        },
        "iwls_maxiter": {
          "default": 25,
          "description": "Max IWLS iterations.",
          "type": "integer"
        },
        "iwls_tol": {
          "default": 1e-08,
          "description": "IWLS convergence tolerance.",
          "type": "number"
        },
        "seed": {
          "description": "RNG seed for sampled (non-enumerated) ``vce=\"wild\"`` draws.",
          "type": "integer"
        },
        "ssc": {
          "description": "Small-sample correction.",
          "type": "string"
        },
        "vcov": {
          "description": "Variance-covariance estimator (``vce=`` is the canonical alias). Besides the pyfixest values (``\"iid\"``, ``\"HC1\"``, ``{\"CRV1\": \"firm\"}``, ...), accepts the extended menu: - ``vce=\"CR2\"`` / ``\"CR3\"`` / ``\"jackknife\"`` (with ``cluster=``) -- clubSandwich glm bias-reduced cluster-robust SEs on the FE-as-dummies design; matches R ``clubSandwich::vcovCR(glm)``. - ``vce=\"wild\"`` (with ``cluster=``) -- restricted score wild cluster bootstrap (Kline-Santos 2012) with Stata ``boottest``'s exact studentization; bit-exact vs ``boottest`` in the enumerated regime.",
          "type": "object"
        },
        "weights": {
          "description": "Column name for regression weights (not supported with the extended ``vce=`` menu).",
          "type": "string"
        },
        "wild_reps": {
          "default": 9999,
          "description": "Replications for ``vce=\"wild\"``. When ``2**G <= wild_reps`` the full Rademacher grid is enumerated (deterministic).",
          "type": "integer"
        },
        "wild_weight_type": {
          "default": "rademacher",
          "description": "Wild weight distribution (``\"rademacher\"`` or ``\"webb\"``).",
          "type": "string"
        }
      },
      "required": [
        "fml"
      ],
      "type": "object"
    },
    "name": "fepois"
  },
  {
    "description": "Firpo-Fortin-Lemieux two-step detailed distributional decomposition. Validation: certified parity evidence. Assumptions: Firpo-Fortin-Lemieux RIF regression: small perturbations to the covariate distribution induce small changes in the distributional statistic; Linear approximation of the recentered influence function is locally valid. Pre-conditions: Outcome is continuous (e.g. log earnings) with adequate distributional support; Covariates explain a non-trivial share of outcome variation across groups. Failure modes: RIF instability at extreme quantiles -> Avoid quantiles below ~0.05 or above ~0.95; widen the kernel bandwidth. Alternatives: sp.dfl_decompose, sp.oaxaca, sp.rif_decomposition. Typical minimum N: 500.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "group": {
          "description": "Group or cohort identifier.",
          "enum": [
            "0",
            "1"
          ],
          "type": "string"
        },
        "inference": {
          "default": "analytical",
          "description": "inference parameter (str).",
          "enum": [
            "analytical",
            "bootstrap"
          ],
          "type": "string"
        },
        "n_boot": {
          "default": 299,
          "description": "Number of bootstrap replications.",
          "type": "integer"
        },
        "quantile_convention": {
          "default": "statspai",
          "description": "Quantile RIF convention (``stat='quantile'`` / ``'iqr'``). ``'rifreg'`` reproduces ``ddecompose`` / ``rifreg``.",
          "enum": [
            "statspai",
            "dineq",
            "rifreg"
          ],
          "type": "string"
        },
        "reference": {
          "default": 0,
          "description": "0: B reweighted to look like A's X (composition = effect of A's X on B's outcomes relative to observed B)",
          "enum": [
            "0",
            "1"
          ],
          "type": "integer"
        },
        "seed": {
          "default": 12345,
          "description": "Random seed for reproducible stochastic steps.",
          "type": "integer"
        },
        "stat": {
          "default": "quantile",
          "description": "'log_var', 'theil_t', 'theil_l', 'atkinson'}",
          "type": "string"
        },
        "tau": {
          "default": 0.5,
          "description": "Quantile level or target treatment-effect index.",
          "type": "number"
        },
        "trim": {
          "default": 0.001,
          "description": "trim parameter (float).",
          "type": "number"
        },
        "weights": {
          "description": "Observation weights.",
          "type": "string"
        },
        "x": {
          "description": "Primary running variable, regressor, or feature input for this estimator.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "group",
        "x",
        "y"
      ],
      "type": "object"
    },
    "name": "ffl_decompose"
  },
  {
    "description": "Fine & Gray (1999) proportional subdistribution hazards model. Validation: certified parity evidence.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals.",
          "type": "number"
        },
        "cause": {
          "default": 1,
          "description": "Cause of interest (default ``1``).",
          "type": "integer"
        },
        "duration": {
          "description": "Follow-up-time column.",
          "type": "string"
        },
        "event": {
          "description": "Event indicator: ``0`` = censored, ``1, 2, ...`` = causes.",
          "type": "string"
        },
        "max_iter": {
          "default": 50,
          "description": "Newton-Raphson controls (``tol`` is on the largest coefficient step).",
          "type": "integer"
        },
        "small_sample": {
          "default": false,
          "description": "Multiply the robust variance by ``n / (n - 1)``, as Stata's ``stcrreg`` does.",
          "type": "boolean"
        },
        "tol": {
          "default": 1e-10,
          "description": "Newton-Raphson controls (``tol`` is on the largest coefficient step).",
          "type": "number"
        },
        "vce": {
          "default": "robust",
          "description": "``\"robust\"`` is Fine & Gray's sandwich variance, whose score residuals include the term for estimating the censoring",
          "enum": [
            "robust",
            "model"
          ],
          "type": "string"
        },
        "x": {
          "description": "Covariate column names.",
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "required": [
        "duration",
        "event",
        "x"
      ],
      "type": "object"
    },
    "name": "finegray"
  },
  {
    "description": "Fisher's exact randomization test with enhanced features. Validation: certified parity evidence.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for the Hodges-Lehmann confidence interval.",
          "type": "number"
        },
        "cluster": {
          "description": "Variable for cluster-level randomization (permute by cluster).",
          "type": "string"
        },
        "controls": {
          "description": "Control variables for covariate-adjusted inference. When provided, the test statistic is computed on residuals from regressing Y on controls.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "n_perm": {
          "default": 10000,
          "description": "Number of random permutations. When the design admits no more than ``n_perm`` distinct assignments (complete, cluster or stratified randomization) they are all enumerated instead, so the p-value is exact (the R ``ri2`` / ``randomizr`` rule), and ``n_perm`` on the result reports the number of assignments.",
          "type": "integer"
        },
        "seed": {
          "description": "Random seed for reproducibility.",
          "type": "integer"
        },
        "statistic": {
          "default": "ate",
          "description": "Test statistic to use: - ``'ate'``: Average treatment effect (difference in means). - ``'ks'``: Kolmogorov-Smirnov statistic. - ``'rank_sum'``: Wilcoxon rank-sum statistic.",
          "type": "string"
        },
        "stratify": {
          "description": "Variable for stratified permutation (permute within strata).",
          "type": "string"
        },
        "treatment": {
          "description": "Binary treatment variable name (0/1).",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable name.",
          "type": "string"
        }
      },
      "required": [
        "treatment",
        "y"
      ],
      "type": "object"
    },
    "name": "fisher_exact"
  },
  {
    "description": "Functional doubly-robust CATE estimator. Validation: validated evidence tier (known-truth, reference, external-parity, or Monte Carlo artifact). Assumptions: Unconfoundedness given the covariates; Overlap / positivity across the covariate space; Nuisance functions are estimated consistently; cross-fitting controls overfitting bias. Pre-conditions: Covariates, a treatment indicator, and an outcome for each unit; Enough data to fit flexible nuisance models with sample-splitting / cross-fitting. Failure modes: CATE estimates are unstable or extrapolate beyond the covariate support -> Restrict to the overlap region, increase data, or use a doubly-robust learner (DR-/R-learner). Alternatives: sp.dml, sp.causal_forest, sp.tmle. Typical minimum N: 500.",
    "input_schema": {
      "properties": {
        "covariates": {
          "description": "Covariate matrix, DataFrame, or column names.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "seed": {
          "default": 0,
          "description": "Random seed for reproducible stochastic steps.",
          "type": "integer"
        },
        "test_data": {
          "description": "Defaults to ``data``.",
          "type": "string"
        },
        "treat": {
          "description": "Treatment indicator or first-treatment-period column.",
          "type": "string"
        },
        "y_columns": {
          "description": "Outcome columns; len = number of function points.",
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "required": [
        "covariates",
        "treat",
        "y_columns"
      ],
      "type": "object"
    },
    "name": "focal_cate"
  },
  {
    "description": "Return overlap and CATE-distribution diagnostics for a fitted forest. Validation: validated evidence tier (known-truth, reference, external-parity, or Monte Carlo artifact). Assumptions: Unconfoundedness given the covariates; Overlap / positivity; Honesty: separate subsamples are used to choose splits and to estimate effects. Pre-conditions: Covariates, treatment, and outcome with enough data to grow an honest forest. Failure modes: Calibration test rejects -- the forest's heterogeneity is not well calibrated -> Increase the sample / number of trees, or fall back to a doubly-robust learner. Alternatives: sp.dml, sp.auto_cate, sp.tmle. Typical minimum N: 1000.",
    "input_schema": {
      "properties": {
        "T": {
          "description": "T parameter (Optional[np.ndarray]).",
          "type": "string"
        },
        "X": {
          "description": "Feature matrix or covariate DataFrame.",
          "type": "string"
        },
        "forest": {
          "description": "forest parameter ('CausalForest').",
          "type": "string"
        },
        "propensity_bounds": {
          "default": [
            0.05,
            0.95
          ],
          "description": "propensity_bounds parameter (Tuple[float, float]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "required": [
        "forest"
      ],
      "type": "object"
    },
    "name": "forest_diagnostics"
  },
  {
    "description": "Average treatment effects by group after a causal forest, with valid standard errors: means of unbiased scores (imputation scores on treated cells for fe= forests, AIPW scores for pooled forests) over groups given by labels, by quantiles of the out-of-bag CATE (GATES), or by membership in dyadic data (every country over all its pairs). Pair-clustered or dyadic-robust variance, equality Wald test. Validation: validated evidence tier (known-truth, reference, external-parity, or Monte Carlo artifact). Assumptions: fe= forests: parallel trends and no anticipation (imputation); pooled forests: unconfoundedness and overlap (AIPW); cluster='dyadic': many members; unreliable with few (warned). Pre-conditions: forest fitted with sp.causal_forest (default GRF engine); fe= forests: binary treatment and untreated periods for treated units; by / members aligned with the rows used to fit the forest. Failure modes: se is NaN with cluster='dyadic' -> The dyadic variance was not positive (few members); report pair-clustered SEs. Alternatives: sp.did_forest, sp.cate_by_group, sp.gate_test.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "by": {
          "description": "None, 'cate_quantile', or one label per training row.",
          "type": "string"
        },
        "cluster": {
          "description": "None (forest clusters), 'dyadic' (needs members) or cluster ids.",
          "type": "string"
        },
        "covariates": {
          "default": "none",
          "description": "fe= forests: covariates in the untreated outcome model ('none' = did_imputation, 'auto' = time-varying x/w, or names). controls= is accepted as an alias.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "forest": {
          "description": "Fitted GRF-engine forest.",
          "type": "string"
        },
        "members": {
          "description": "(n, 2) members of each dyadic row; with by=None one group per member.",
          "type": "string"
        },
        "min_rows": {
          "default": 1,
          "description": "min_rows parameter (int).",
          "type": "integer"
        },
        "n_groups": {
          "default": 4,
          "description": "Quantile groups for by='cate_quantile'.",
          "type": "integer"
        },
        "scale": {
          "default": "level",
          "description": "'percent' adds 100*(exp(x)-1) columns for log outcomes.",
          "enum": [
            "level",
            "percent"
          ],
          "type": "string"
        },
        "variance": {
          "default": "forest",
          "description": "fe= forests: centre treated residuals on the OOB forest ('forest') or on cohort x event-time means only ('bjs').",
          "enum": [
            "forest",
            "bjs"
          ],
          "type": "string"
        }
      },
      "required": [
        "forest"
      ],
      "type": "object"
    },
    "name": "forest_group_effects"
  },
  {
    "description": "Treatment rule for a causal forest with fixed effects, and what it was worth. sp.policy_tree needs a doubly-robust score and so a propensity, which a within-unit design has none of; the imputation scores that give the ATT are used instead, which fixes the population to the treated cells: of the cells that were treated, which should have been. Fits on one half of the units and prices on the other, because a rule maximised on the scores it is then priced against is priced on its own noise -- where the true gain was exactly 0, the same-sample version averaged +0.048 and called it significant 13% of the time, against +0.005 and 3.5% split. Value, treat-all value and the gain between them share one exact, cluster- or dyad-robust covariance. Aggregates n_splits draws by VEIN. Validation: validated evidence tier (known-truth, reference, external-parity, or Monte Carlo artifact). Assumptions: parallel trends and no anticipation (imputation scores); the rule applies to treated cells; effects on untreated cells are not identified (check sp.forest_support first). Pre-conditions: forest fitted with sp.causal_forest(..., fe='twoway'); enough treated cells in each half to fill a leaf. Failure m...",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "cluster": {
          "description": "Cluster identifier column for clustered standard errors.",
          "type": "string"
        },
        "cost": {
          "default": 0.0,
          "description": "Cost of treating one cell, in the outcome's units. The rule treats where the effect exceeds it; with cost=0 and positive effects, treating everyone is optimal.",
          "type": "number"
        },
        "covariates": {
          "default": "none",
          "description": "Covariates in the untreated outcome model; controls= is accepted as an alias.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "depth": {
          "default": 2,
          "description": "Tree depth; <= 2 is searched exactly, deeper is greedy.",
          "type": "integer"
        },
        "forest": {
          "description": "Fitted GRF-engine forest with fe='twoway'.",
          "type": "string"
        },
        "members": {
          "description": "(n, 2) members of each dyadic row; splits by member.",
          "type": "string"
        },
        "min_leaf_size": {
          "description": "min_leaf_size parameter (int).",
          "type": "integer"
        },
        "n_splits": {
          "default": 21,
          "description": "Splits aggregated by Chernozhukov et al. (2025) VEIN. A rule cannot be averaged, so the tree reported is the one from the median-gain split and split_stability says how far the rule itself moved. n_splits=1 warns.",
          "type": "integer"
        },
        "random_state": {
          "default": 0,
          "description": "Seeds the sequence of splits.",
          "type": "integer"
        },
        "train_frac": {
          "default": 0.5,
          "description": "Share of units (members) that fit the rule.",
          "type": "number"
        },
        "variance": {
          "default": "bjs",
          "description": "Centring of treated residuals before the cohort x event-time blocks.",
          "enum": [
            "bjs",
            "forest"
          ],
          "type": "string"
        },
        "x": {
          "description": "Policy covariates; default the forest's effect modifiers.",
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "required": [
        "forest"
      ],
      "type": "object"
    },
    "name": "forest_policy_tree"
  },
  {
    "description": "Support diagnostics for counterfactual CATE predictions: for each new row, the forest CATE with its little-bag interval, effect modifiers outside the range of the rows that inform the effect (switching units for fe= forests), and the k-nearest-neighbour distance relative to the reference sample. Assumptions: Predictions for untreated units assume tau(x) carries over at equal x. Pre-conditions: forest fitted with sp.causal_forest (default GRF engine). Alternatives: sp.overlap_plot, sp.forest_diagnostics.",
    "input_schema": {
      "properties": {
        "X_new": {
          "description": "Effect modifiers to predict.",
          "type": "string"
        },
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "forest": {
          "description": "Fitted GRF-engine forest.",
          "type": "string"
        },
        "k": {
          "default": 10,
          "description": "k parameter (int).",
          "type": "integer"
        },
        "quantile": {
          "default": 0.95,
          "description": "quantile parameter (float).",
          "type": "number"
        }
      },
      "required": [
        "X_new",
        "forest"
      ],
      "type": "object"
    },
    "name": "forest_support"
  },
  {
    "description": "Fortified proximal causal inference (Yu, Shi & Tchetgen Tchetgen 2025). Adds a bridge-function stability constraint that gives robust ATT under mild misspecification of the outcome/treatment bridge. Validation: validated evidence tier (known-truth, reference, external-parity, or Monte Carlo artifact). Assumptions: The proxies are valid negative controls (relevant to the confounder, excluded from the causal channel); A bridge function exists (completeness conditions hold). Pre-conditions: Treatment-inducing and outcome-inducing proxy variables (negative controls) for the unobserved confounder. Failure modes: Proxies are weak or invalid -- the bridge function is poorly identified -> Test proxy relevance, select stronger proxies, or fall back to sensitivity analysis. Alternatives: sp.select_pci_proxies, sp.dml. Typical minimum N: 500.",
    "input_schema": {
      "properties": {
        "covariates": {
          "description": "Covariate matrix, DataFrame, or column names.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "proxy_w": {
          "description": "Outcome-side proxies",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "proxy_z": {
          "description": "Treatment-side proxies",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "treat": {
          "description": "Treatment indicator or first-treatment-period column.",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "proxy_w",
        "proxy_z",
        "treat",
        "y"
      ],
      "type": "object"
    },
    "name": "fortified_pci"
  },
  {
    "description": "Fractional response model (Papke & Wooldridge 1996). Validation: certified parity evidence.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "cluster": {
          "description": "Cluster variable for clustered SE.",
          "type": "string"
        },
        "link": {
          "default": "logit",
          "description": "Link function: 'logit' or 'probit'.",
          "type": "string"
        },
        "maxiter": {
          "default": 100,
          "description": "maxiter parameter (int).",
          "type": "integer"
        },
        "robust": {
          "default": "robust",
          "description": "Always use robust SE (quasi-MLE).",
          "type": "string"
        },
        "tol": {
          "default": 1e-08,
          "description": "Numerical convergence tolerance.",
          "type": "number"
        },
        "x": {
          "description": "Regressors.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Outcome variable in [0, 1].",
          "type": "string"
        }
      },
      "required": [],
      "type": "object"
    },
    "name": "fracreg"
  },
  {
    "description": "Translate a single R / fixest / felm / did expression to a verified StatsPAI tool-call payload. Returns the same shape as from_stata. Supported callables: feols / felm / lm / att_gt / did. Pass ONE expression -- no assignment, no piping.",
    "input_schema": {
      "properties": {
        "expression": {
          "description": "One R expression, e.g. 'feols(y ~ x | id^year, data=df, cluster=\"id\")'.",
          "type": "string"
        }
      },
      "required": [
        "expression"
      ],
      "type": "object"
    },
    "name": "from_r"
  },
  {
    "description": "Translate a single Stata command to a verified StatsPAI tool-call payload. Returns ``python_code`` (string for chat replies) AND ``arguments`` (ready-to-dispatch JSON-RPC for tools/call). Tier-1 commands: regress / xtreg / reghdfe / ivreg2 / csdid / did_imputation / synth / rdrobust; count-panel commands include nbreg / xtnbreg / ppmlhdfe. Unrecognised commands return close-match suggestions instead of guessing.",
    "input_schema": {
      "properties": {
        "command": {
          "description": "One Stata command, e.g. 'reghdfe y x, absorb(id year) cluster(id)'. Multi-command lines must be split by the caller.",
          "type": "string"
        }
      },
      "required": [
        "command"
      ],
      "type": "object"
    },
    "name": "from_stata"
  },
  {
    "description": "Pearl's front-door adjustment: identifies ATE with unmeasured confounding when a mediator fully transmits the effect of D on Y. Supports binary or continuous mediator; integrate_by controls Pearl (marginal) vs Fulcher et al. (conditional) aggregation. Validation: validated evidence tier (known-truth, reference, external-parity, or Monte Carlo artifact). Assumptions: Sequential exchangeability / no unmeasured confounding at each time point; Positivity: every treatment level is possible given the past; Correct specification of the treatment and/or outcome models. Pre-conditions: Sequentially measured covariates, (time-varying) treatment, and outcome; Models for the treatment process and the outcome (or weights). Failure modes: Stabilized weights have extreme values (positivity near-violation) -> Truncate weights, simplify the treatment model, or use a doubly-robust estimator (TMLE). Alternatives: sp.tmle, sp.g_computation, sp.ipw. Typical minimum N: 300.",
    "input_schema": {
      "properties": {
        "covariates": {
          "description": "Pre-treatment covariates",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "integrate_by": {
          "default": "marginal",
          "description": "MC integration formulation (continuous M only)",
          "enum": [
            "marginal",
            "conditional"
          ],
          "type": "string"
        },
        "mediator": {
          "description": "Fully-transmitting mediator",
          "type": "string"
        },
        "mediator_type": {
          "default": "auto",
          "description": "Mediator model",
          "enum": [
            "auto",
            "binary",
            "continuous"
          ],
          "type": "string"
        },
        "treat": {
          "description": "Binary treatment (0/1)",
          "type": "string"
        },
        "y": {
          "description": "Outcome",
          "type": "string"
        }
      },
      "required": [
        "mediator",
        "treat",
        "y"
      ],
      "type": "object"
    },
    "name": "front_door"
  },
  {
    "description": "Front-door adjustment -- article-friendly alias for Validation: validated evidence tier (known-truth, reference, external-parity, or Monte Carlo artifact).",
    "input_schema": {
      "properties": {
        "X": {
          "description": "Feature matrix or covariate DataFrame.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "d": {
          "description": "d parameter (str).",
          "type": "string"
        },
        "m": {
          "description": "m parameter (str).",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "d",
        "m",
        "y"
      ],
      "type": "object"
    },
    "name": "frontdoor"
  },
  {
    "description": "Roth & Sant'Anna (2023) test of whether parallel trends can hold for EVERY strictly monotonic transformation of the outcome. Bins the outcome, recovers the counterfactual probability mass the design implies for the treated group in each bin via Callaway-Sant'Anna, and tests the moment inequalities that mass must satisfy to be a density. Rejection means levels and logs are answering different questions, so the functional form is doing identifying work. Validation: certified evidence with scoped limitations. Known limitations: a large p-value is only a failure to reject, not evidence FOR functional-form insensitivity: the test has little power with few units or coarse bins; standard errors and the critical value are asymptotic; a bootstrap variant is not implemented. Assumptions: Callaway-Sant'Anna identification for each binned indicator; No anticipation before g - anticipation. Pre-conditions: staggered or single-cohort panel with never-treated or not-yet-treated controls; outcome with enough support to bin. Failure modes: Every bin has a degenerate influence function -> The binning is finer than the data support. Lower n_bins or supply binpoints; Test rejects -> Parallel trends c...",
    "input_schema": {
      "properties": {
        "aggregation": {
          "default": "group",
          "description": "Which aggte aggregation defines the implied density",
          "enum": [
            "group",
            "simple",
            "dynamic",
            "calendar"
          ],
          "type": "string"
        },
        "allow_unbalanced_panel": {
          "default": false,
          "description": "allow_unbalanced_panel parameter (bool).",
          "type": "boolean"
        },
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "anticipation": {
          "default": 0,
          "description": "anticipation parameter (int).",
          "type": "integer"
        },
        "balance_e": {
          "description": "Only bites under aggregation='dynamic': keep cohorts observed for this many event times",
          "type": "integer"
        },
        "binpoints": {
          "description": "Explicit bin edges, padded to cover the outcome range if they fall short; cannot be combined with n_bins",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "control_group": {
          "default": "nevertreated",
          "description": "control_group parameter (str).",
          "type": "string"
        },
        "estimator": {
          "default": "dr",
          "description": "estimator parameter (str).",
          "type": "string"
        },
        "g": {
          "description": "First-treatment period (0 = never treated)",
          "type": "string"
        },
        "i": {
          "description": "Unit identifier",
          "type": "string"
        },
        "max_e": {
          "description": "Only bites under aggregation='dynamic': latest event time entering the aggregate",
          "type": "number"
        },
        "min_e": {
          "description": "Only bites under aggregation='dynamic': earliest event time entering the aggregate",
          "type": "number"
        },
        "n_bins": {
          "default": "auto",
          "description": "Equal-width outcome bins; too coarse a grid buys a large p-value for nothing. 'auto' follows didFF: an outcome with fewer than 20 distinct untreated values is treated as discrete (one bin per value), otherwise it is cut into min(20, n_distinct) bins",
          "type": "integer"
        },
        "n_sims": {
          "default": 100000,
          "description": "Draws behind the least-favourable critical value",
          "type": "integer"
        },
        "panel": {
          "default": true,
          "description": "panel parameter (bool).",
          "type": "boolean"
        },
        "random_state": {
          "default": 0,
          "description": "Random seed or RandomState for reproducible stochastic steps.",
          "type": "integer"
        },
        "t": {
          "description": "t parameter (str).",
          "type": "string"
        },
        "weights": {
          "description": "Sampling-weight column; unset weights every unit equally",
          "type": "string"
        },
        "x": {
          "description": "Primary running variable, regressor, or feature input for this estimator.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "g",
        "i",
        "t",
        "y"
      ],
      "type": "object"
    },
    "name": "functional_form_test"
  },
  {
    "description": "Parametric g-formula (standardization) estimator. ATE/ATT for binary D, or dose-response curve for continuous D. Consistent under correctly-specified outcome model; not doubly robust. Validation: certified parity evidence. Assumptions: Sequential exchangeability / no unmeasured confounding at each time point; Positivity: every treatment level is possible given the past; Correct specification of the treatment and/or outcome models. Pre-conditions: Sequentially measured covariates, (time-varying) treatment, and outcome; Models for the treatment process and the outcome (or weights). Failure modes: Stabilized weights have extreme values (positivity near-violation) -> Truncate weights, simplify the treatment model, or use a doubly-robust estimator (TMLE). Alternatives: sp.tmle, sp.g_computation, sp.ipw. Typical minimum N: 300.",
    "input_schema": {
      "properties": {
        "covariates": {
          "description": "Baseline covariates",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "estimand": {
          "default": "ATE",
          "description": "Target estimand",
          "enum": [
            "ATE",
            "ATT",
            "dose_response"
          ],
          "type": "string"
        },
        "n_boot": {
          "default": 500,
          "description": "Bootstrap replications for SE",
          "type": "integer"
        },
        "treat": {
          "description": "Treatment variable",
          "type": "string"
        },
        "treat_values": {
          "description": "Dose grid (required for dose_response)",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Outcome",
          "type": "string"
        }
      },
      "required": [
        "covariates",
        "treat",
        "y"
      ],
      "type": "object"
    },
    "name": "g_computation"
  },
  {
    "description": "G-estimation for a multi-stage dynamic treatment regime. Validation: certified parity evidence. Assumptions: Sequential exchangeability / no unmeasured confounding at each time point; Positivity: every treatment level is possible given the past; Correct specification of the treatment and/or outcome models. Pre-conditions: Sequentially measured covariates, (time-varying) treatment, and outcome; Models for the treatment process and the outcome (or weights). Failure modes: Stabilized weights have extreme values (positivity near-violation) -> Truncate weights, simplify the treatment model, or use a doubly-robust estimator (TMLE). Alternatives: sp.tmle, sp.g_computation, sp.ipw. Typical minimum N: 300.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "covariates_by_stage": {
          "description": "Covariates (tailoring variables) available at each stage. covariates_by_stage[k] are the variables available when deciding treatment k.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "n_bootstrap": {
          "default": 500,
          "description": "Number of bootstrap replications.",
          "type": "integer"
        },
        "propensity_covariates": {
          "description": "Covariates for propensity model at each stage. If None, uses covariates_by_stage.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "propensity_model": {
          "default": "logit",
          "description": "Model for ``E[A_k | propensity covariates]`` in the g-estimating equation ``sum (A_k - A_hat_k) (Y~ - X_k beta - psi_k A_k) = 0`` (solved jointly with ``sum X_k (Y~ - X_k beta - psi_k A_k) = 0``, ``X_k`` = ``[1, covariates_by_stage[k]]``). ``'logit'`` is the logistic MLE that R ``DTRreg(method = \"gest\")`` fits for a binary treatment. ``'linear'`` is a linear-probability OLS fit; with propensity covariates equal to the stage covariates it reduces ``psi_k`` to the OLS coefficient on ``A_k`` in ``Y~ ~ X_k + A_k``.",
          "enum": [
            "logit",
            "linear"
          ],
          "type": "string"
        },
        "random_state": {
          "default": 42,
          "description": "Random seed or RandomState for reproducible stochastic steps.",
          "type": "integer"
        },
        "treatments": {
          "description": "Treatment variables at each stage, in temporal order. E.g., ['A1', 'A2'] for a two-stage DTR.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Final outcome variable.",
          "type": "string"
        }
      },
      "required": [
        "covariates_by_stage",
        "treatments",
        "y"
      ],
      "type": "object"
    },
    "name": "g_estimation"
  },
  {
    "description": "Counterfactual gap after equalising covariate distributions. Validation: certified parity evidence.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "group": {
          "description": "Group or cohort identifier.",
          "type": "string"
        },
        "inference": {
          "default": "analytical",
          "description": "inference parameter (str).",
          "enum": [
            "analytical",
            "bootstrap"
          ],
          "type": "string"
        },
        "method": {
          "default": "aipw",
          "description": "AIPW is doubly robust (recommended).",
          "enum": [
            "regression",
            "ipw",
            "aipw"
          ],
          "type": "string"
        },
        "n_boot": {
          "default": 299,
          "description": "Number of bootstrap replications.",
          "type": "integer"
        },
        "seed": {
          "default": 12345,
          "description": "Random seed for reproducible stochastic steps.",
          "type": "integer"
        },
        "target_dist": {
          "default": 1,
          "description": "- 1: shift Group A's covariate distribution to match Group B's - 0: shift Group B's to match Group A's",
          "enum": [
            "0",
            "1"
          ],
          "type": "integer"
        },
        "trim": {
          "default": 0.001,
          "description": "trim parameter (float).",
          "type": "number"
        },
        "x": {
          "description": "Primary running variable, regressor, or feature input for this estimator.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "group",
        "x",
        "y"
      ],
      "type": "object"
    },
    "name": "gap_closing"
  },
  {
    "description": "Fit GARCH(p,q) by conditional Gaussian MLE. Validation: certified parity evidence.",
    "input_schema": {
      "properties": {
        "mean": {
          "default": true,
          "description": "Estimate a constant mean mu; if False, mu = 0.",
          "type": "boolean"
        },
        "p": {
          "default": 1,
          "description": "Number of GARCH (lagged sigma2) terms (Stata ``garch(p)``).",
          "type": "integer"
        },
        "presample": {
          "default": "stata",
          "description": "How the recursion is started; both use ``m = mean(epsilon2)`` at the current mu. ``'stata'``: pre-sample epsilon2 and sigma2 equal ``m`` and every observation's sigma2 follows the recursion (Stata ``arch``, default ``arch0(xb)``). ``'rugarch'``: sigma2_t = ``m`` for the first ``max(p, q)`` observations (``rugarch::ugarchfit``, sGARCH). The log-likelihood sums over all observations in both cases.",
          "enum": [
            "stata",
            "rugarch"
          ],
          "type": "string"
        },
        "q": {
          "default": 1,
          "description": "Number of ARCH (lagged epsilon2) terms (Stata ``arch(q)``).",
          "type": "integer"
        },
        "vce": {
          "default": "oim",
          "description": "Covariance of the estimates: inverse observed information (Hessian of the log-likelihood, by central differences of the analytic score), outer product of the scores (Stata's ``arch`` default), or the Bollerslev-Wooldridge sandwich ``H^{-1} (S'S) H^{-1}``.",
          "enum": [
            "oim",
            "opg",
            "robust"
          ],
          "type": "string"
        },
        "y": {
          "description": "Return series (or log-return, etc.).",
          "type": "string"
        }
      },
      "required": [
        "y"
      ],
      "type": "object"
    },
    "name": "garch"
  },
  {
    "description": "Gardner (2022) two-stage DID. Stage-1 fits two-way FEs on untreated observations; Stage-2 regresses the residualised outcome on treatment dummies (ATT or event study). Standard errors are the Butts-Gardner two-stage corrected clustered variance (R/Stata did2s), reproduced to ~1e-8 on castle-doctrine; weights= gives did2s(weights=) / [aw=]. Validation: certified parity evidence. Assumptions: Conditional parallel trends between treated and comparison groups absent treatment; No anticipation of treatment before its onset; Treatment effects may be heterogeneous across cohorts and time (no homogeneity required). Pre-conditions: Panel or repeated cross-section with a unit (or group) identifier and a time identifier; At least one never-treated or not-yet-treated comparison group; Pre-treatment periods to assess parallel trends. Failure modes: Pre-treatment event-study coefficients are jointly non-zero (pre-trend violation) -> Use honest DiD bounds to quantify robustness to trend violations, or condition on covariates; Two-way fixed-effects estimate is contaminated by 'forbidden' comparisons / negative weights -> Use a heterogeneity-robust estimator (Callaway-Sant'Anna, Borusyak et al., G...",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "boot_seed": {
          "description": "Seed for the cluster bootstrap",
          "type": "integer"
        },
        "cluster": {
          "description": "Cluster variable for the two-stage clustered SEs (defaults to group)",
          "type": "string"
        },
        "controls": {
          "description": "Additional covariates",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "event_study": {
          "default": false,
          "description": "If True, report coefficients by relative time k = t - first_treat",
          "type": "boolean"
        },
        "first_treat": {
          "description": "First-treatment-period column; 0/NaN/inf = never treated",
          "type": "string"
        },
        "group": {
          "description": "Unit/panel-id column",
          "type": "string"
        },
        "horizon": {
          "description": "Relative-time leads/lags to report (default range(-5, 6))",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "n_boot": {
          "default": 199,
          "description": "Cluster-bootstrap replications when vce='bootstrap'",
          "type": "integer"
        },
        "time": {
          "description": "Time column",
          "type": "string"
        },
        "vce": {
          "default": "analytic",
          "description": "Standard errors: 'analytic' (did2s corrected two-stage clustered variance, stage-1 estimation error propagated; the did2s/R and Stata convention), 'stage2' (pre-1.29 stage-2-only SE), 'bootstrap' (cluster bootstrap of both stages), or 'none'",
          "enum": [
            "analytic",
            "stage2",
            "bootstrap",
            "none"
          ],
          "type": "string"
        },
        "weights": {
          "description": "Strictly positive estimation weights for both stages (R did2s weights= / Stata [aw=])",
          "type": "string"
        },
        "y": {
          "description": "Outcome column",
          "type": "string"
        }
      },
      "required": [
        "first_treat",
        "group",
        "time",
        "y"
      ],
      "type": "object"
    },
    "name": "gardner_did"
  },
  {
    "description": "Sorted group average treatment effects (GATES) and heterogeneity tests. Validation: certified parity evidence. Assumptions: Unconfoundedness given the covariates; Overlap / positivity across the covariate space; Nuisance functions are estimated consistently; cross-fitting controls overfitting bias. Pre-conditions: Covariates, a treatment indicator, and an outcome for each unit; Enough data to fit flexible nuisance models with sample-splitting / cross-fitting. Failure modes: CATE estimates are unstable or extrapolate beyond the covariate support -> Restrict to the overlap region, increase data, or use a doubly-robust learner (DR-/R-learner). Alternatives: sp.dml, sp.causal_forest, sp.tmle. Typical minimum N: 500.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "baseline": {
          "description": "As in :func:`blp_test`.",
          "type": "string"
        },
        "by": {
          "description": "``'cate'`` or a column name.",
          "type": "string"
        },
        "covariates": {
          "description": "Outcome, binary treatment and covariates; required for GATES.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "n_folds": {
          "default": 5,
          "description": "As in :func:`blp_test`.",
          "type": "integer"
        },
        "n_groups": {
          "default": 4,
          "description": "Number of groups.",
          "type": "integer"
        },
        "propensity": {
          "description": "As in :func:`blp_test`.",
          "type": "string"
        },
        "proxy": {
          "default": "cross_fit",
          "description": "As in :func:`blp_test`.",
          "type": "string"
        },
        "result": {
          "description": "result parameter (CausalResult).",
          "type": "string"
        },
        "seed": {
          "default": 42,
          "description": "As in :func:`blp_test`.",
          "type": "integer"
        },
        "treat": {
          "description": "Outcome, binary treatment and covariates; required for GATES.",
          "type": "string"
        },
        "vce": {
          "default": "HC1",
          "description": "As in :func:`blp_test`.",
          "type": "string"
        },
        "y": {
          "description": "Outcome, binary treatment and covariates; required for GATES.",
          "type": "string"
        }
      },
      "required": [
        "by",
        "result"
      ],
      "type": "object"
    },
    "name": "gate_test"
  },
  {
    "description": "Gelbach (2016) decomposition of omitted variable bias. Validation: certified parity evidence.",
    "input_schema": {
      "properties": {
        "added_x": {
          "description": "Variables added to obtain the full (long) specification.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "alpha": {
          "default": 0.05,
          "description": "Significance level.",
          "type": "number"
        },
        "base_x": {
          "description": "Variables in the base (short) specification.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "var_of_interest": {
          "description": "Which base variable's coefficient change to decompose. Defaults to the first element of ``base_x``.",
          "type": "string"
        },
        "vce": {
          "default": "robust",
          "description": "Heteroskedasticity-robust or homoskedastic covariance, as ``b1x2 ..., robust`` and plain ``b1x2``. Standard errors were a two-term delta method that treated the auxiliary and long-regression estimates as independent; ``b1x2`` keeps their covariance. On ``cps_wage`` that moved the SEs by 0.03%, but the old formula has no guarantee of being that close. Point estimates are unchanged.",
          "enum": [
            "robust",
            "nonrobust"
          ],
          "type": "string"
        },
        "y": {
          "description": "Outcome variable name.",
          "type": "string"
        }
      },
      "required": [
        "added_x",
        "base_x",
        "y"
      ],
      "type": "object"
    },
    "name": "gelbach"
  },
  {
    "description": "High-order bunching design with bias correction. Validation: validated evidence tier (known-truth, reference, external-parity, or Monte Carlo artifact). Assumptions: The counterfactual density would be smooth through the threshold absent the policy; Excess mass at the threshold reflects the behavioural elasticity of interest; No other discontinuity coincides with the threshold. Pre-conditions: A behavioural choice variable (earnings, hours, ...) with a known kink or notch in the budget/choice set; A visible empirical density of the running variable around the threshold. Failure modes: Round-number heaping or a coincident policy contaminates the bunching mass -> Exclude heaping points, widen the excluded region, and test the counterfactual polynomial order. Alternatives: sp.rdrobust, sp.rkd. Typical minimum N: 500.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "bandwidth": {
          "default": 1.0,
          "description": "Bandwidth used for local smoothing or kernel weighting.",
          "type": "number"
        },
        "bin_width": {
          "description": "Defaults to bandwidth / 25.",
          "type": "number"
        },
        "cutoff": {
          "default": 0.0,
          "description": "cutoff parameter (float).",
          "type": "number"
        },
        "n_boot": {
          "default": 200,
          "description": "Number of bootstrap replications.",
          "type": "integer"
        },
        "polynomial_order": {
          "default": 4,
          "description": "Order of the counterfactual polynomial fit.",
          "type": "integer"
        },
        "running": {
          "description": "Running variable (e.g. earnings).",
          "type": "string"
        },
        "seed": {
          "default": 0,
          "description": "Random seed for reproducible stochastic steps.",
          "type": "integer"
        }
      },
      "required": [
        "running"
      ],
      "type": "object"
    },
    "name": "general_bunching"
  },
  {
    "description": "Genetic Matching for ATT estimation. Validation: certified parity evidence. Cost: Genetic search: population_size x generations full matching + balance evaluations (default 40 x 20 = 800 matching passes), each of which builds a pairwise distance matrix. Budget it as hundreds of sp.match calls, not one. Assumptions: Unconfoundedness: treatment is as-good-as-random given the measured covariates; Overlap / common support: every unit has a non-degenerate probability of each treatment; The covariate set blocks all back-door paths. Pre-conditions: Pre-treatment covariates measured for treated and control units; A binary (or low-cardinality) treatment indicator; Sufficient covariate overlap between treatment arms. Failure modes: Poor overlap -- extreme propensity scores or few acceptable matches -> Trim or restrict to the common-support region and report the discarded units; Covariate imbalance remains after matching/weighting -> Re-specify the balancing model (CBPS, entropy balancing) and re-check standardized mean differences. Alternatives: sp.propensity_score, sp.cbps, sp.ebalance, sp.dml. Typical minimum N: 200.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "covariates": {
          "description": "Covariate matrix, DataFrame, or column names.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "generations": {
          "default": 20,
          "description": "generations parameter (int).",
          "type": "integer"
        },
        "k": {
          "default": 1,
          "description": "Number of matches per treated unit.",
          "type": "integer"
        },
        "mutation_rate": {
          "default": 0.2,
          "description": "mutation_rate parameter (float).",
          "type": "number"
        },
        "population_size": {
          "default": 40,
          "description": "population_size parameter (int).",
          "type": "integer"
        },
        "random_state": {
          "default": 42,
          "description": "Random seed or RandomState for reproducible stochastic steps.",
          "type": "integer"
        },
        "treat": {
          "description": "Binary treatment indicator.",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "covariates",
        "treat",
        "y"
      ],
      "type": "object"
    },
    "name": "genmatch"
  },
  {
    "description": "User-friendly alias for :func:`sp.rdms` (multi-score RD). Validation: certified parity evidence. Assumptions: Conditional expectations of potential outcomes are continuous at the cutoff; Units cannot precisely manipulate the running variable around the cutoff (no sorting); For fuzzy designs: monotonicity of treatment take-up at the cutoff. Pre-conditions: A continuous running/forcing variable with a known cutoff that (sharply or fuzzily) assigns treatment; Enough observations in a neighbourhood of the cutoff to fit a local polynomial. Failure modes: Density of the running variable jumps at the cutoff (manipulation / sorting) -> Run a McCrary / density test (rdplotdensity); if manipulation is present the design is invalid near the cutoff; Estimate swings with the bandwidth -- results are not robust -> Report a bandwidth-sensitivity curve and use a data-driven MSE-optimal bandwidth. Alternatives: sp.rdrobust, sp.rdrandinf, sp.rdbwselect. Typical minimum N: 500.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "bandwidth": {
          "description": "Bandwidth used for local smoothing or kernel weighting.",
          "type": "number"
        },
        "cutoff1": {
          "default": 0,
          "description": "cutoff1 parameter (float).",
          "type": "number"
        },
        "cutoff2": {
          "default": 0,
          "description": "cutoff2 parameter (float).",
          "type": "number"
        },
        "kernel": {
          "default": "triangular",
          "description": "Kernel function used for weighting or smoothing.",
          "type": "string"
        },
        "treat": {
          "description": "Treatment indicator or first-treatment-period column.",
          "type": "string"
        },
        "x1": {
          "description": "x1 parameter (str).",
          "type": "string"
        },
        "x2": {
          "description": "x2 parameter (str).",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "x1",
        "x2",
        "y"
      ],
      "type": "object"
    },
    "name": "geographic_rd"
  },
  {
    "description": "German reunification dataset (simulated).",
    "input_schema": {
      "properties": {},
      "required": [],
      "type": "object"
    },
    "name": "german_reunification"
  },
  {
    "description": "Greedy Equivalence Search. Validation: validated evidence tier (known-truth, reference, external-parity, or Monte Carlo artifact). Assumptions: Causal Markov condition and faithfulness (PC/GES/FCI); Causal sufficiency for PC/GES (no latent confounders); FCI relaxes this; Acyclicity; LiNGAM additionally assumes a linear non-Gaussian model. Pre-conditions: Constraint-/score-based discovery needs i.i.d. observational data with enough samples for reliable conditional-independence tests; Invariance-based discovery (ICP) needs data labelled by environment / intervention. Failure modes: Unstable skeleton / many undirected edges -- faithfulness or sample size is the likely culprit -> Increase the sample, relax the CI-test threshold, or switch to FCI if latent confounders are plausible. Alternatives: sp.pc_algorithm, sp.fci, sp.ges, sp.lingam. Typical minimum N: 500.",
    "input_schema": {
      "properties": {
        "max_iter": {
          "default": 500,
          "description": "Maximum total edge additions + removals.",
          "type": "integer"
        }
      },
      "required": [],
      "type": "object"
    },
    "name": "ges"
  },
  {
    "description": "Doubly-robust scores behind a forest's average effect (grf::get_scores): AIPW for causal and multi-arm forests, compliance-weighted for instrumental forests, censoring-adjusted for causal survival forests. Their mean is the average effect; use them for custom averages, subgroup contrasts or policy values. Validation: validated evidence tier (known-truth, reference, external-parity, or Monte Carlo artifact). Assumptions: the forest's nuisances are consistent. Pre-conditions: forest with doubly-robust scores. Failure modes: MethodIncompatibility: has no doubly-robust scores -> Prediction forests have no causal scores. Alternatives: sp.best_linear_projection, sp.policy_tree.",
    "input_schema": {
      "properties": {
        "forest": {
          "description": "``sp.causal_forest`` (GRF engine, binary or continuous treatment), ``sp.iv_forest`` (average conditional LATE), ``sp.multi_arm_forest`` (one column per contrast), ``sp.causal_survival_forest``.",
          "type": "string"
        }
      },
      "required": [
        "forest"
      ],
      "type": "object"
    },
    "name": "get_scores"
  },
  {
    "description": "Plot an ``aggte()`` result, mirroring R :func:`did::ggdid`.",
    "input_schema": {
      "properties": {
        "ax": {
          "description": "ax parameter.",
          "type": "string"
        },
        "band_color": {
          "default": "#F18F01",
          "description": "Colours for the pointwise estimate and the uniform band.",
          "type": "string"
        },
        "figsize": {
          "default": [
            10,
            6
          ],
          "description": "figsize parameter (Tuple[float, float]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "point_color": {
          "default": "#2E86AB",
          "description": "Colours for the pointwise estimate and the uniform band.",
          "type": "string"
        },
        "result": {
          "description": "Output of :func:`aggte`.",
          "type": "string"
        },
        "show_pointwise": {
          "default": true,
          "description": "Draw pointwise CI lines.",
          "type": "boolean"
        },
        "show_uniform": {
          "default": true,
          "description": "Draw uniform band (shaded region).",
          "type": "boolean"
        },
        "title": {
          "description": "title parameter (Optional[str]).",
          "type": "string"
        }
      },
      "required": [
        "result"
      ],
      "type": "object"
    },
    "name": "ggdid"
  },
  {
    "description": "Fit a Generalized Linear Model. Validation: certified parity evidence.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals.",
          "type": "number"
        },
        "cluster": {
          "description": "Variable name for clustered standard errors.",
          "type": "string"
        },
        "exposure": {
          "description": "Variable name for exposure (``log(exposure)`` is added as offset).",
          "type": "string"
        },
        "family": {
          "default": "gaussian",
          "description": "Distribution family. One of ``\"gaussian\"``, ``\"binomial\"``, ``\"poisson\"``, ``\"gamma\"``, ``\"inverse_gaussian\"``, ``\"negative_binomial\"``.",
          "type": "string"
        },
        "formula": {
          "description": "Model formula (e.g. ``\"y ~ x1 + x2\"``).",
          "type": "string"
        },
        "information": {
          "default": "observed",
          "description": "Information matrix used as the variance bread. ``'observed'`` is Stata's ``glm`` default ``vce(oim)`` (and the bread of its robust / cluster sandwiches); ``'expected'`` is the IRLS Fisher information that R's ``glm`` reports. They coincide for canonical links (logit binomial, log Poisson, identity Gaussian) and differ otherwise.",
          "enum": [
            "observed",
            "expected"
          ],
          "type": "string"
        },
        "link": {
          "description": "Link function. If ``None`` the canonical link for the chosen family is used. Options: ``\"identity\"``, ``\"log\"``, ``\"logit\"``, ``\"probit\"``, ``\"inverse\"``, ``\"cloglog\"``, ``\"power\"``, ``\"sqrt\"``.",
          "type": "string"
        },
        "maxiter": {
          "default": 100,
          "description": "Maximum number of IRLS iterations.",
          "type": "integer"
        },
        "offset": {
          "description": "Variable name for offset.",
          "type": "string"
        },
        "robust": {
          "default": "nonrobust",
          "description": "Standard-error type (``\"nonrobust\"``, ``\"hc0\"``-``\"hc3\"``, ``\"hac\"``).",
          "type": "string"
        },
        "tol": {
          "default": 1e-08,
          "description": "Convergence tolerance on the relative change in deviance.",
          "type": "number"
        },
        "weights": {
          "description": "Variable name for observation weights.",
          "type": "string"
        },
        "x": {
          "description": "Names of independent variables (alternative to formula).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Name of the dependent variable (alternative to formula).",
          "type": "string"
        }
      },
      "required": [],
      "type": "object"
    },
    "name": "glm"
  },
  {
    "description": "General GMM estimator for arbitrary moment conditions. Validation: certified parity evidence.",
    "input_schema": {
      "properties": {
        "W": {
          "description": "Weighting matrix ``(q, q)`` for the first step. Defaults to the identity. Supplying one and asking for ``se='unadjusted'`` warns unless it happens to be efficient.",
          "type": "string"
        },
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "center": {
          "default": false,
          "description": "Centre the moments before forming ``S``. ``False`` matches Stata, ``True`` matches R's ``gmm``.",
          "type": "boolean"
        },
        "cluster": {
          "description": "Group labels, one per moment row. Required when ``vcov='cluster'``.",
          "type": "string"
        },
        "hac_bandwidth": {
          "description": "Bartlett bandwidth for ``vcov='hac'``; the kernel vanishes at ``lag == hac_bandwidth``.",
          "type": "integer"
        },
        "jacobian": {
          "description": "``D(theta, data) -> ndarray`` of shape ``(q, k)``, the derivative of the *average* moment. Supplying it removes finite-difference error from the standard errors and from the affine test.",
          "type": "string"
        },
        "maxiter": {
          "default": 200,
          "description": "maxiter parameter (int).",
          "type": "integer"
        },
        "method": {
          "default": "twostep",
          "description": "Estimator or algorithm variant to use.",
          "enum": [
            "onestep",
            "twostep",
            "iterative",
            "cue"
          ],
          "type": "string"
        },
        "moment_fn": {
          "description": "``g(theta, data) -> ndarray`` of shape ``(n, q)``: the moment contribution of each observation.",
          "type": "string"
        },
        "param_names": {
          "description": "param_names parameter (Optional[List[str]]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "sandwich_weight": {
          "default": "reestimated",
          "description": "Weight inside the robust variance of the two-step / iterated estimators. ``'reestimated'`` uses ``S-1`` re-evaluated at the final estimate, so the sandwich collapses to ``(D'S-1D)-1/n`` (R ``gmm::gmm``). ``'estimation'`` uses the weight the final round was *minimised* with -- computed from the penultimate estimate -- with ``S`` at the final estimate: ``(D'WD)-1 D'WSWD (D'WD)-1 / n``, as Stata's ``gmm`` documents in its Methods and formulas. The two coincide for the iterated estimator at convergence and for the one-step estimator; for two-step they differ at order 1/n.",
          "enum": [
            "reestimated",
            "estimation"
          ],
          "type": "string"
        },
        "se": {
          "default": "robust",
          "description": "``'robust'`` returns the sandwich, valid for any ``W``. ``'unadjusted'`` returns the efficient-GMM variance ``(D'WD)^{-1}/n``, which describes the estimator *only* at the efficient weight; otherwise it warns.",
          "enum": [
            "robust",
            "unadjusted"
          ],
          "type": "string"
        },
        "theta0": {
          "description": "Starting values. Also the expansion point for the closed form when the moments are affine.",
          "type": "string"
        },
        "tol": {
          "default": 1e-08,
          "description": "Numerical convergence tolerance.",
          "type": "number"
        },
        "vcov": {
          "default": "mds",
          "description": "Estimator for the moment covariance ``S``.",
          "enum": [
            "mds",
            "iid",
            "hac",
            "cluster"
          ],
          "type": "string"
        }
      },
      "required": [
        "moment_fn",
        "theta0"
      ],
      "type": "object"
    },
    "name": "gmm"
  },
  {
    "description": "Granger causality test. Validation: certified parity evidence.",
    "input_schema": {
      "properties": {
        "caused": {
          "description": "Variable being tested for causation.",
          "type": "string"
        },
        "causing": {
          "description": "Variable(s) hypothesised to cause; a list tests all their lags jointly (Stata ``vargranger``'s ``ALL`` row when it lists every other variable).",
          "type": "string"
        },
        "lags": {
          "description": "Number of lags (if fitting new VAR).",
          "type": "integer"
        },
        "var_result": {
          "description": "Pre-estimated VAR model.",
          "type": "string"
        }
      },
      "required": [],
      "type": "object"
    },
    "name": "granger_causality"
  },
  {
    "description": "Plot group-time ATT estimates from Callaway-Sant'Anna.",
    "input_schema": {
      "properties": {
        "alpha_level": {
          "default": 0.05,
          "description": "Significance threshold.",
          "type": "number"
        },
        "ax": {
          "description": "ax parameter.",
          "type": "string"
        },
        "color": {
          "default": "#2C3E50",
          "description": "Default color for dot plot.",
          "type": "string"
        },
        "figsize": {
          "default": [
            12,
            7
          ],
          "description": "figsize parameter (Tuple[float, float]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "insig_color": {
          "default": "#BDC3C7",
          "description": "Color for insignificant estimates.",
          "type": "string"
        },
        "plot_type": {
          "default": "dot",
          "description": "'dot' or 'heatmap'.",
          "type": "string"
        },
        "result": {
          "description": "Result from ``callaway_santanna()`` or ``did(method='cs')``. Must have ``detail`` DataFrame with 'group', 'time', 'att' columns.",
          "type": "string"
        },
        "sig_color": {
          "default": "#E74C3C",
          "description": "Color for significant estimates.",
          "type": "string"
        },
        "title": {
          "description": "title parameter (Optional[str]).",
          "type": "string"
        }
      },
      "required": [
        "result"
      ],
      "type": "object"
    },
    "name": "group_time_plot"
  },
  {
    "description": "Generalized Synthetic Control via interactive fixed effects. Validation: certified parity evidence. Do NOT use when: there is only one treated unit and a short pre-period -- the interactive fixed-effects factors are not identified; use sp.synth; fewer pre-treatment periods than the number of factors being fit -- factor estimation is degenerate. Cost: Cross-validating n_factors refits the factor model cv_folds x max_factors times, and placebo/bootstrap inference refits again per replication -- runtime is multiplicative in those three knobs. Pin n_factors to skip the CV sweep. Assumptions: A convex (or regularized) combination of donor units reproduces the treated unit's pre-treatment outcome path; No interference: the treatment does not affect the donor units (SUTVA); No anticipation before the treatment date. Pre-conditions: Panel of one or more treated units plus an untreated donor pool, observed over time; Pre-treatment window long enough to fit donor weights (rule of thumb: more pre-periods than donors used); Outcome observed for every unit in every period. Failure modes: Large pre-treatment RMSPE -- the synthetic unit fails to track the treated unit before treatment -> Add don...",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level.",
          "type": "number"
        },
        "backend": {
          "default": "native",
          "description": "``'native'`` uses StatsPAI's Python interactive fixed-effects implementation. ``'gsynth'``/``'r'`` delegates to the R ``gsynth`` package through ``Rscript`` using the Track-A reference specification ``force='two-way'``, ``CV=TRUE``, ``r=c(0, max_factors)``, and ``se=FALSE``. The R backend is intended for exact reference-package parity; the native path remains the dependency-light default.",
          "enum": [
            "native",
            "gsynth",
            "r"
          ],
          "type": "string"
        },
        "covariates": {
          "description": "Additional time-varying covariates.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "cv_folds": {
          "default": 5,
          "description": "Cross-validation folds for factor selection.",
          "type": "integer"
        },
        "max_factors": {
          "default": 5,
          "description": "Maximum factors to try during CV.",
          "type": "integer"
        },
        "n_factors": {
          "description": "Number of latent factors. If None, selected by cross-validation.",
          "type": "integer"
        },
        "outcome": {
          "description": "Outcome variable name.",
          "type": "string"
        },
        "placebo": {
          "default": true,
          "description": "Run placebo inference.",
          "type": "boolean"
        },
        "seed": {
          "description": "Random seed.",
          "type": "integer"
        },
        "time": {
          "description": "Time period column.",
          "type": "string"
        },
        "treated_unit": {
          "description": "Identifier of the treated unit.",
          "type": "string"
        },
        "treatment_time": {
          "description": "First treatment period (inclusive).",
          "type": "string"
        },
        "unit": {
          "description": "Unit identifier column.",
          "type": "string"
        }
      },
      "required": [
        "outcome",
        "time",
        "treated_unit",
        "treatment_time",
        "unit"
      ],
      "type": "object"
    },
    "name": "gsynth"
  },
  {
    "description": "TMLE with Highly Adaptive Lasso (HAL) nuisance learners (Qian & van der Laan 2025). The stable 'delta' variant plugs HAL into standard TMLE. The reserved 'projection' variant raises NotImplementedError until the Riesz-projection targeting step has reference parity. Validation: validated evidence tier (known-truth, reference, external-parity, or Monte Carlo artifact). Known limitations: variant='projection' raises NotImplementedError -- the Riesz-projection targeting step from Li-Qiu-Wang-vdL (2025) Section 3.2 is not yet ported (the v1.11.x code path was a no-op on the point estimate; see CHANGELOG). The implementation roadmap and parity-test gates are in docs/rfc/hal_tmle_projection.md. Assumptions: Unconfoundedness (sequential exchangeability for longitudinal/LTMLE); Positivity / overlap of treatment given history; At least one nuisance (outcome or treatment) is estimated consistently; the targeting step gives double robustness. Pre-conditions: Covariates, treatment, and outcome (for survival/longitudinal variants: time-to-event and time-varying covariates); Enough data to fit a Super Learner / HAL nuisance library. Failure modes: Near-positivity violations create extreme clever...",
    "input_schema": {
      "properties": {
        "C_propensity": {
          "default": 1.0,
          "description": "Inverse L1 penalty for HAL propensity classifier",
          "type": "number"
        },
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "covariates": {
          "description": "Covariate matrix, DataFrame, or column names.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "estimand": {
          "default": "ATE",
          "description": "Estimand",
          "enum": [
            "ATE",
            "ATT"
          ],
          "type": "string"
        },
        "lambda_outcome": {
          "description": "Outcome L1 penalty; None -> 5-fold CV",
          "type": "number"
        },
        "max_anchors_per_col": {
          "default": 40,
          "description": "Column name for max anchors per.",
          "type": "integer"
        },
        "n_folds": {
          "default": 5,
          "description": "Number of cross-fitting or cross-validation folds.",
          "type": "integer"
        },
        "random_state": {
          "default": 42,
          "description": "Random seed or RandomState for reproducible stochastic steps.",
          "type": "integer"
        },
        "treat": {
          "description": "Binary treatment",
          "type": "string"
        },
        "variant": {
          "default": "delta",
          "description": "HAL-TMLE variant",
          "enum": [
            "delta",
            "projection"
          ],
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "covariates",
        "treat",
        "y"
      ],
      "type": "object"
    },
    "name": "hal_tmle"
  },
  {
    "description": "Harvest every valid 2x2 DID comparison from a staggered panel and aggregate them via precision-weighted / simple / cohort-weighted averages. Agnostic to cohort structure; useful for robustness comparisons against CS / SA / BJS. Validation: certified parity evidence. Assumptions: Parallel trends hold for every valid 2x2 sub-comparison being harvested and aggregated; No anticipation before each cohort's first treated period (reference horizon -1 is clean); Independence across units (unit-level influence functions; the joint covariance across cells, horizons and estimated weights is propagated). Pre-conditions: Long-format panel with unit, time, outcome columns; Either a binary treat indicator or a precomputed cohort (first-treatment) column with a never_value marker; Multiple cohorts and overlapping periods so valid 2x2 comparisons exist across the requested horizons. Failure modes: No clean (never-treated or not-yet-treated) controls so no valid 2x2 cells can be harvested -> Add never-treated units or restrict horizons to periods with available clean controls; Precision weighting dominated by one tiny high-variance comparison distorts the aggregate -> Switch weighting to 'equal' or...",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "cohort": {
          "description": "First-treat cohort column (for static harvesting)",
          "type": "string"
        },
        "horizons": {
          "description": "horizons parameter (list).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "never_value": {
          "default": 0,
          "description": "never_value parameter.",
          "type": "string"
        },
        "outcome": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        },
        "reference": {
          "default": -1,
          "description": "Pre-treatment reference horizon relative to each cohort",
          "type": "integer"
        },
        "time": {
          "description": "Time period column.",
          "type": "string"
        },
        "treat": {
          "description": "Time-varying treat indicator (for dynamic harvesting)",
          "type": "string"
        },
        "unit": {
          "description": "Unit identifier column.",
          "type": "string"
        },
        "weighting": {
          "default": "precision",
          "description": "How the harvested 2x2 estimates are aggregated: 'precision' inverse-variance (minimum-variance under independence), 'equal' unweighted, 'n_treated' by each comparison's treated-unit count.",
          "enum": [
            "precision",
            "equal",
            "n_treated"
          ],
          "type": "string"
        }
      },
      "required": [
        "outcome",
        "time",
        "unit"
      ],
      "type": "object"
    },
    "name": "harvest_did"
  },
  {
    "description": "Hausman test for FE vs RE in panel data. Validation: validated evidence tier (known-truth, reference, external-parity, or Monte Carlo artifact).",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "id": {
          "description": "Unit identifier.",
          "type": "string"
        },
        "time": {
          "description": "Time period identifier.",
          "type": "string"
        },
        "x": {
          "description": "Independent variables (time-varying).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Dependent variable.",
          "type": "string"
        }
      },
      "required": [
        "id",
        "time",
        "x",
        "y"
      ],
      "type": "object"
    },
    "name": "hausman_test"
  },
  {
    "description": "reghdfe-style OLS with high-dimensional fixed effects. Validation: certified parity evidence. Do NOT use when: vce='conley' on more than ~20,000 rows -- dense O(n^2) memory (see cost); use sp.conley on the fitted result instead. Cost: Absorption is linear in n. vce='conley' is the exception: the within-transformed design goes through conley_vcov_matrix, which builds dense n x n distance and kernel matrices -- ~80 GB at n=100,000. vce='cr2'/'cr3' are per-cluster and cheap by comparison. Assumptions: Linear conditional mean after absorbing high-dimensional fixed effects (Frisch-Waugh-Lovell); Exogeneity of regressors conditional on the absorbed fixed-effect dimensions; Clustered SEs require enough clusters for asymptotics; multiway cluster needs each dimension well-populated. Pre-conditions: Continuous outcome; Fixed-effect factor variable(s) in the '| fe1 + fe2' part of the formula. Failure modes: Singleton groups absorb their own observations and bias clustered SEs -> Keep drop_singletons=True so singletons are removed before estimation; Alternating-projections absorber does not converge -> Raise maxiter / loosen tol, or reduce the number of FE dimensions; Too few clusters make wi...",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "cluster": {
          "description": "One-way or multi-way cluster column(s).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "conley_cutoff": {
          "description": "Conley distance cutoff in km for ``vce=\"conley\"``.",
          "type": "number"
        },
        "conley_lat": {
          "description": "Coordinate columns (decimal degrees) for ``vce=\"conley\"``.",
          "type": "string"
        },
        "conley_lon": {
          "description": "Coordinate columns (decimal degrees) for ``vce=\"conley\"``.",
          "type": "string"
        },
        "drop_singletons": {
          "default": true,
          "description": "drop_singletons parameter (bool).",
          "type": "boolean"
        },
        "formula": {
          "description": "``\"y ~ x1 + x2 | fe1 + fe2 + fe3\"``. The ``| fe...`` part is optional. Both sides accept bare names, ``c.x`` / ``i.f``, ``a:b``, ``a*b``, ``f1^f2`` and the varying-slope forms ``i.f#c.x`` / ``i.f##c.x`` / ``f[[x]]`` / ``f[x]`` -- see the module docstring for the full grammar.",
          "type": "string"
        },
        "maxiter": {
          "default": 10000,
          "description": "maxiter parameter (int).",
          "type": "integer"
        },
        "se_type": {
          "description": "Override automatic inference of SE type. Usually inferred from ``cluster`` / ``wild``.",
          "enum": [
            "iid",
            "cluster",
            "multiway_cluster",
            "wild_cluster"
          ],
          "type": "string"
        },
        "tol": {
          "default": 1e-08,
          "description": "Numerical convergence tolerance.",
          "type": "number"
        },
        "vce": {
          "description": "Canonical SE-menu keyword (matches ``sp.regress`` / ``sp.feols``): - ``\"robust\"`` / ``\"hc1\"`` -- heteroskedasticity-robust on the FE-absorbed design with reghdfe's small-sample factor ``N/(N-k-df_a)``; matches Stata ``reghdfe ..., vce(robust)``. - ``\"hc0\"`` -- no small-sample factor. - ``\"CR2\"`` / ``\"CR3\"`` / ``\"jackknife\"`` -- Pustejovsky-Tipton (2018) bias-reduced cluster-robust on the within design (requires ``cluster=``, one-way); matches R ``clubSandwich::vcovCR(plm)``. - ``\"conley\"`` -- Conley spatial HAC on the within design (requires ``conley_lat=/conley_lon=/conley_cutoff=``; Stata ``acreg`` planar distance convention). - ``\"wild\"`` -- shorthand for ``wild=True`` (requires ``cluster=``).",
          "type": "string"
        },
        "weights": {
          "description": "Observation weights. Column name or raw array.",
          "type": "string"
        },
        "wild": {
          "default": false,
          "description": "If True (and ``cluster`` is given), return wild-cluster-bootstrap p-values / CIs alongside classical cluster SE. Applied variable- by-variable. Only supported with a single cluster column.",
          "type": "boolean"
        },
        "wild_n_boot": {
          "default": 999,
          "description": "Bootstrap replications.",
          "type": "integer"
        },
        "wild_seed": {
          "description": "wild_seed parameter (Optional[int]).",
          "type": "integer"
        },
        "wild_weight_type": {
          "default": "webb",
          "description": "wild_weight_type parameter (str).",
          "enum": [
            "rademacher",
            "webb",
            "mammen"
          ],
          "type": "string"
        }
      },
      "required": [
        "formula"
      ],
      "type": "object"
    },
    "name": "hdfe_ols"
  },
  {
    "description": "Heckman two-step selection model correcting for sample selection bias. Validation: certified parity evidence.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals",
          "type": "number"
        },
        "select": {
          "description": "Binary selection indicator (1 = observed, 0 = not)",
          "type": "string"
        },
        "x": {
          "description": "Regressors in the outcome equation",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Outcome variable (observed only when select=1)",
          "type": "string"
        },
        "z": {
          "description": "Selection-equation variables (include exclusion restrictions in z but not x)",
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "required": [
        "select",
        "x",
        "y",
        "z"
      ],
      "type": "object"
    },
    "name": "heckman"
  },
  {
    "description": "Breusch-Pagan test for heteroskedasticity. Validation: certified parity evidence.",
    "input_schema": {
      "properties": {
        "x": {
          "description": "Primary running variable, regressor, or feature input for this estimator.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "x",
        "y"
      ],
      "type": "object"
    },
    "name": "het_test"
  },
  {
    "description": "Holm (1979) step-down correction. Validation: certified parity evidence.",
    "input_schema": {
      "properties": {
        "pvalues": {
          "description": "Unadjusted p-values.",
          "type": "string"
        }
      },
      "required": [
        "pvalues"
      ],
      "type": "object"
    },
    "name": "holm"
  },
  {
    "description": "Rambachan-Roth (2023) 'honest' DID sensitivity analysis. Operates on a fitted event-study / DID result: pass ``result_id`` from a prior ``sp.event_study`` / ``sp.callaway_santanna`` / ``sp.did_imputation`` / ``sp.sun_abraham`` fit run with ``as_handle=true`` (or use the ``honest_did_from_result`` tool, which auto-extracts the event-study coefficients). Returns honest confidence intervals under varying degrees of parallel-trends violation (smoothness or relative-magnitude restrictions). Call this when a pre-trend test rejects at low power instead of abandoning the design. Validation: certified parity evidence.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for the honest CI.",
          "type": "number"
        },
        "e": {
          "default": 0,
          "description": "Event-time period (post-treatment horizon) to build the honest CI for.",
          "type": "integer"
        },
        "m_grid": {
          "description": "Grid of M bounds on the parallel-trends violation to sweep.",
          "items": {
            "type": "number"
          },
          "type": "array"
        },
        "method": {
          "default": "smoothness",
          "description": "Restriction family: 'smoothness' (second differences) or 'relative_magnitude'.",
          "enum": [
            "smoothness",
            "relative_magnitude"
          ],
          "type": "string"
        }
      },
      "required": [],
      "type": "object"
    },
    "name": "honest_did"
  },
  {
    "description": "Rambachan-Roth (2023) honest CIs on a fitted DID / event-study result. Auto-extracts betas + sigma + pre/post-period counts from the result; the LLM never ferries arrays.",
    "input_schema": {
      "properties": {
        "e": {
          "default": 0,
          "description": "Relative event time to audit.",
          "type": "integer"
        },
        "m_bar": {
          "description": "Bound on deviation magnitude (optional).",
          "type": "number"
        },
        "method": {
          "default": "SD",
          "description": "SD = smoothness deviation (Rambachan-Roth default); RM = relative magnitude.",
          "enum": [
            "SD",
            "RM"
          ],
          "type": "string"
        },
        "result_id": {
          "description": "Handle to a DID / event-study result.",
          "type": "string"
        }
      },
      "required": [
        "result_id"
      ],
      "type": "object"
    },
    "name": "honest_did_from_result"
  },
  {
    "description": "Half-sample spread of the mean CATE prediction (deprecated). Validation: validated evidence tier (known-truth, reference, external-parity, or Monte Carlo artifact). Assumptions: Unconfoundedness given the covariates; Overlap / positivity; Honesty: separate subsamples are used to choose splits and to estimate effects. Pre-conditions: Covariates, treatment, and outcome with enough data to grow an honest forest. Failure modes: Calibration test rejects -- the forest's heterogeneity is not well calibrated -> Increase the sample / number of trees, or fall back to a doubly-robust learner. Alternatives: sp.dml, sp.auto_cate, sp.tmle. Typical minimum N: 1000.",
    "input_schema": {
      "properties": {
        "X": {
          "description": "Feature matrix or covariate DataFrame.",
          "type": "string"
        },
        "forest": {
          "description": "forest parameter ('CausalForest').",
          "type": "string"
        },
        "n_splits": {
          "default": 25,
          "description": "Number of random half-sample draws.",
          "type": "integer"
        },
        "seed": {
          "description": "Random seed for reproducible stochastic steps.",
          "type": "integer"
        }
      },
      "required": [
        "forest"
      ],
      "type": "object"
    },
    "name": "honest_variance"
  },
  {
    "description": "Horowitz-Manski (2000) bounds conditioning on covariates. Validation: certified parity evidence. Assumptions: Only weak (set-identifying) assumptions are imposed; the result is an interval, not a point; Lee bounds add monotonicity of selection; Oster's delta adds proportional selection on observed vs. unobserved. Pre-conditions: The data needed for the point-identifying analysis, plus the weakest credible identifying restriction; For Lee bounds: a binary selection/attrition indicator. Failure modes: Bounds are too wide to be informative -> Add a credible auxiliary restriction (monotone treatment response, instrument) to tighten the bounds. Alternatives: sp.oster_delta, sp.lee_bounds, sp.manski_bounds. Typical minimum N: 100.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "covariates": {
          "description": "Covariates to condition on (discretised via quartiles for continuous variables).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "n_boot": {
          "default": 500,
          "description": "Bootstrap replications.",
          "type": "integer"
        },
        "random_state": {
          "default": 42,
          "description": "Random seed or RandomState for reproducible stochastic steps.",
          "type": "integer"
        },
        "treatment": {
          "description": "Binary treatment variable (0/1).",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable.",
          "type": "string"
        },
        "y_lower": {
          "description": "Known lower bound of Y. Defaults to observed min.",
          "type": "number"
        },
        "y_upper": {
          "description": "Known upper bound of Y. Defaults to observed max.",
          "type": "number"
        }
      },
      "required": [
        "covariates",
        "treatment",
        "y"
      ],
      "type": "object"
    },
    "name": "horowitz_manski"
  },
  {
    "description": "Hurdle (two-part) model for count data. Validation: certified parity evidence.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "cluster": {
          "description": "Cluster identifier column for clustered standard errors.",
          "type": "string"
        },
        "count_model": {
          "default": "poisson",
          "description": "Count distribution: \"poisson\" or \"negbin\".",
          "type": "string"
        },
        "formula": {
          "description": "Patsy-style formula.",
          "type": "string"
        },
        "maxiter": {
          "default": 200,
          "description": "maxiter parameter (int).",
          "type": "integer"
        },
        "robust": {
          "default": "nonrobust",
          "description": "Robust standard-error or covariance estimator option.",
          "type": "string"
        },
        "tol": {
          "default": 1e-08,
          "description": "Numerical convergence tolerance.",
          "type": "number"
        },
        "x": {
          "description": "Regressors (used for both hurdle and count parts).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Dependent variable name.",
          "type": "string"
        }
      },
      "required": [],
      "type": "object"
    },
    "name": "hurdle"
  },
  {
    "description": "Intra-class correlation for a fitted random-intercept model. Validation: certified parity evidence.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for the confidence interval. Default 0.05.",
          "type": "number"
        },
        "component": {
          "default": "_cons",
          "description": "Random-intercept name (``\"_cons\"``); for a three-level fit the grouping column of the level whose ICC is wanted.",
          "type": "string"
        },
        "n_boot": {
          "default": 0,
          "description": "Parametric-bootstrap intervals are not implemented; must be 0.",
          "type": "integer"
        },
        "result": {
          "description": "A ``MixedResult`` from :func:`statspai.mixed` or an ``MEGLMResult`` from ``melogit`` / ``meologit`` / ``meglm(family='gaussian')``, with a random intercept only.",
          "type": "string"
        },
        "seed": {
          "description": "Unused (reserved for the bootstrap).",
          "type": "integer"
        }
      },
      "required": [
        "result"
      ],
      "type": "object"
    },
    "name": "icc"
  },
  {
    "description": "Causal Impact visualization (Google-style 3-panel plot).",
    "input_schema": {
      "properties": {
        "ax": {
          "description": "Only for single-panel types.",
          "type": "string"
        },
        "figsize": {
          "default": [
            12,
            9
          ],
          "description": "figsize parameter (tuple[float, float]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "result": {
          "description": "Result from ``causal_impact()``.",
          "type": "string"
        },
        "title": {
          "description": "title parameter (Optional[str]).",
          "type": "string"
        },
        "type": {
          "default": "all",
          "description": "'all': 3-panel (original + pointwise + cumulative). 'original': actual vs counterfactual. 'pointwise': pointwise causal effect. 'cumulative': cumulative effect.",
          "type": "string"
        }
      },
      "required": [
        "result"
      ],
      "type": "object"
    },
    "name": "impactplot"
  },
  {
    "description": "Compute a single inequality index. Validation: certified parity evidence.",
    "input_schema": {
      "properties": {
        "alpha": {
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "eps": {
          "default": 1.0,
          "description": "eps parameter (float).",
          "type": "number"
        },
        "index": {
          "default": "theil_t",
          "description": "atkinson, gini, cv2",
          "type": "string"
        },
        "weights": {
          "description": "Observation weights.",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "y"
      ],
      "type": "object"
    },
    "name": "inequality_index"
  },
  {
    "description": "Export the per-unit influence functions of a Callaway-Sant'Anna fit as a tidy, self-contained DataFrame (optionally written to disk) -- the StatsPAI equivalent of Stata csdid saverif(). Feed the export to sp.aggte_from_influence for post-hoc custom aggregation without refitting or re-loading the data. Validation: validated evidence tier (known-truth, reference, external-parity, or Monte Carlo artifact). Pre-conditions: result was produced by sp.callaway_santanna. Failure modes: result carries no influence functions -> Fit with sp.callaway_santanna first; other estimators do not store the (g,t) influence-function grid. Alternatives: sp.aggte, sp.aggte_from_influence. Typical minimum N: 50.",
    "input_schema": {
      "properties": {
        "path": {
          "description": "Optional file path -- .parquet via to_parquet, anything else via to_csv",
          "type": "string"
        },
        "result": {
          "description": "Output of sp.callaway_santanna",
          "type": "string"
        }
      },
      "required": [
        "result"
      ],
      "type": "object"
    },
    "name": "influence_functions"
  },
  {
    "description": "Instrumental forest: heterogeneous local average treatment effects.",
    "input_schema": {
      "properties": {
        "W_hat": {
          "description": "Precomputed ``E[Y|X]``, ``E[W|X]``, ``E[Z|X]``; by default each is the out-of-bag prediction of a regression forest with ``max(50, n_estimators // 4)`` trees.",
          "type": "string"
        },
        "Y_hat": {
          "description": "Precomputed ``E[Y|X]``, ``E[W|X]``, ``E[Z|X]``; by default each is the out-of-bag prediction of a regression forest with ``max(50, n_estimators // 4)`` trees.",
          "type": "string"
        },
        "Z_hat": {
          "description": "Precomputed ``E[Y|X]``, ``E[W|X]``, ``E[Z|X]``; by default each is the out-of-bag prediction of a regression forest with ``max(50, n_estimators // 4)`` trees.",
          "type": "string"
        },
        "alpha": {
          "description": "Significance level of the reported interval.",
          "type": "number"
        },
        "ci_group_size": {
          "description": "Trees per little bag; 1 disables variance estimates.",
          "type": "integer"
        },
        "clusters": {
          "description": "Cluster ids: trees sample whole clusters and every standard error is cluster-robust.",
          "type": "string"
        },
        "compliance_score": {
          "description": "``Delta(X_i)`` for the average-effect scores; by default estimated by an auxiliary 500-tree causal forest of ``W`` on ``Z``.",
          "type": "string"
        },
        "covariates": {
          "description": "Effect modifiers ``X``; the nuisances are also regressed on them.",
          "type": "string"
        },
        "equalize_cluster_weights": {
          "description": "Give every cluster the same weight (incompatible with ``weights``).",
          "type": "boolean"
        },
        "honest": {
          "description": "grf honesty options.",
          "type": "string"
        },
        "honesty_fraction": {
          "description": "grf honesty options.",
          "type": "string"
        },
        "honesty_prune_leaves": {
          "description": "grf honesty options.",
          "type": "string"
        },
        "imbalance_penalty": {
          "description": "imbalance_penalty parameter (float).",
          "type": "number"
        },
        "instrument": {
          "description": "Outcome, treatment (binary or continuous) and instrument (binary or continuous).",
          "type": "string"
        },
        "max_depth": {
          "description": "Depth cap (no grf analogue).",
          "type": "integer"
        },
        "max_samples": {
          "description": "Fraction of clusters drawn per tree (grf ``sample.fraction``).",
          "type": "number"
        },
        "min_samples_leaf": {
          "description": "grf ``min.node.size``.",
          "type": "integer"
        },
        "mtry": {
          "description": "Candidate variables per split (grf default ``min(ceil(sqrt(p) + 20), p)``).",
          "type": "integer"
        },
        "n_bootstrap": {
          "description": "Ignored. Standard errors now come from the doubly-robust scores.",
          "type": "string"
        },
        "n_estimators": {
          "description": "Trees (grf ``num.trees``).",
          "type": "integer"
        },
        "n_jobs": {
          "description": "Number of jobs.",
          "type": "integer"
        },
        "random_state": {
          "description": "Random seed or RandomState for reproducible stochastic steps.",
          "type": "integer"
        },
        "reduced_form_weight": {
          "description": "Mix the IV split criterion with the causal-forest criterion that treats ``W`` as exogenous (grf ``reduced.form.weight``).",
          "type": "number"
        },
        "split_alpha": {
          "description": "grf ``alpha``: minimum share of the parent's instrument variation each child of a stabilised split keeps.",
          "type": "number"
        },
        "stabilize_splits": {
          "description": "Use the instrument in the split constraints.",
          "type": "boolean"
        },
        "treat": {
          "description": "Outcome, treatment (binary or continuous) and instrument (binary or continuous).",
          "type": "string"
        },
        "weights": {
          "description": "Sample weights (grf ``sample.weights``).",
          "type": "string"
        },
        "y": {
          "description": "Outcome, treatment (binary or continuous) and instrument (binary or continuous).",
          "type": "string"
        }
      },
      "required": [],
      "type": "object"
    },
    "name": "instrumental_forest"
  },
  {
    "description": "Interactive fixed effects estimator (Bai 2009). Validation: certified parity evidence.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "dof": {
          "default": "exact",
          "description": "Parameters absorbed by the factor structure. ``'exact'`` counts ``r(N + T - r)`` (``F'`` is identified only up to an rxr rotation); ``'regife'`` counts ``r(N + T)``, as Stata ``regife`` does through ``reghdfe`` (which flags that it could not detect the redundant ones). Enters ``sigma2`` and ``K`` in the cluster factor.",
          "enum": [
            "exact",
            "regife"
          ],
          "type": "string"
        },
        "id": {
          "default": "id",
          "description": "Unit identifier.",
          "type": "string"
        },
        "maxiter": {
          "default": 10000,
          "description": "maxiter parameter (int).",
          "type": "integer"
        },
        "method": {
          "default": "iterative",
          "description": "Bai's (2009) iterated least squares: alternate principal components of ``Y - Xbeta`` (factors) and least squares of ``Y M_F`` on ``X M_F`` (slopes) until the slopes move by less than ``tol``. ``'pca'`` is accepted for backward compatibility and runs the same algorithm (it always did); it is deprecated.",
          "type": "string"
        },
        "n_factors": {
          "default": 1,
          "description": "Number of interactive factors (r).",
          "type": "integer"
        },
        "robust": {
          "default": true,
          "description": "``True``: cluster-robust by unit (Bai's ``D0-1 D_Z D0-1`` with the Stata CR1 factor ``G/(G-1)*(NT-1)/(NT-K)``); ``False``: Bai's homoskedastic ``sigma2 D0-1``. In both, the regressors enter through ``Z_j = M_ X_j M_F`` -- projected off *both* the factor space and the loading space.",
          "type": "boolean"
        },
        "time": {
          "default": "time",
          "description": "Time identifier.",
          "type": "string"
        },
        "tol": {
          "default": 1e-10,
          "description": "Convergence criterion on max |Deltabeta|. The iteration converges linearly, so the default is tight (``regife``'s own default is 1e-9).",
          "type": "number"
        },
        "x": {
          "description": "Regressors.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Dependent variable.",
          "type": "string"
        }
      },
      "required": [
        "x",
        "y"
      ],
      "type": "object"
    },
    "name": "interactive_fe"
  },
  {
    "description": "Unified interference / spillover dispatcher. design= selects the estimator: 'partial' (Hudgens-Halloran cluster) / 'network_exposure' (Aronow-Samii HT) / 'peer_effects' (Manski / Bramoulle linear-in-means) / 'network_hte' (Wu & Yuan 2025 orthogonal, arXiv:2509.18484) / 'inward_outward' (directed network; Fang, Airoldi & Forastiere 2025, arXiv:2506.06615) / 'cluster_matched_pair' (Bai 2022) / 'cluster_cross' (Ding et al. 2025) / 'cluster_staggered' (Zhou et al. 2025) / 'dnc_gnn' (Zhao et al. 2026). Kwargs pass through to the target function; see sp.interference_family guide. Validation: validated evidence tier (known-truth, reference, external-parity, or Monte Carlo artifact). Assumptions: Partial interference (within-cluster spillover only) OR an explicit exposure mapping; SUTVA modulo the declared spillover structure; Correctly specified exposure function (e.g. fraction-treated, neighbour-share). Pre-conditions: clustered data OR network / adjacency matrix; treatment varies within cluster (or exposure is well-defined on the network); enough clusters (>= 30) for cluster-robust inference. Failure modes: Few clusters (< 30) with cluster-level inference -> Use wild cluster bootstrap...",
    "input_schema": {
      "properties": {
        "design": {
          "default": "partial",
          "description": "Interference design -- call sp.interference_available_designs() for the full list.",
          "type": "string"
        }
      },
      "required": [],
      "type": "object"
    },
    "name": "interference"
  },
  {
    "description": "Conditional marginal effects of ``d`` across a moderator ``x`` Validation: certified parity evidence.",
    "input_schema": {
      "properties": {
        "adaptive": {
          "default": true,
          "description": "Kernel estimator only. ``True`` scales the bandwidth at each evaluation point by the moderator's density as the R package does (``h(x) = bw * sqrt(geometric-mean density / density(x))``); ``False`` uses the fixed Gaussian kernel ``phi((X - x)/bw)`` of the Stata ``interflex`` command.",
          "type": "boolean"
        },
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "bw": {
          "description": "Kernel bandwidth on the moderator's scale (before the adaptive density scaling). ``estimator='kernel'`` needs either ``bw`` or ``cv=True``, which selects it by interflex's cross-validation (``interflex(bw = NULL)``).",
          "type": "number"
        },
        "bw_grid": {
          "default": 30,
          "description": "interflex ``grid``: an integer gives that many candidates log-spaced from ``range(x)/100`` to ``range(x)``; a sequence is used as the grid.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "cutoffs": {
          "description": "cutoffs parameter (Optional[Sequence[float]]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "cv": {
          "default": false,
          "description": "Kernel estimator only: choose ``bw`` by interflex's k-fold cross-validation. Observations are split into ``kfold`` folds stratified on ``d`` (``interflex:::createFolds``); for every candidate bandwidth the local-linear coefficients are fitted on the training folds over the evaluation grid (with the training sample's moderator density for the adaptive scaling), carried to each test observation by linear interpolation between its two nearest grid points, and the mean squared (``metric='mse'``) or absolute (``'mae'``) prediction error of ``y`` is averaged over folds; the bandwidth minimising it, divided by the number of evaluation points that produced a fit, is chosen exactly as the R package does. Requires ``bw=None``.",
          "type": "boolean"
        },
        "d": {
          "description": "Outcome, treatment (binary 0/1 or continuous) and moderator.",
          "type": "string"
        },
        "estimator": {
          "default": "binning",
          "description": "estimator parameter (str).",
          "enum": [
            "binning",
            "linear",
            "kernel"
          ],
          "type": "string"
        },
        "kfold": {
          "default": 10,
          "description": "Number of folds (interflex ``kfold``).",
          "type": "integer"
        },
        "metric": {
          "default": "mse",
          "description": "CV loss (interflex ``metric``).",
          "enum": [
            "mse",
            "mae"
          ],
          "type": "string"
        },
        "n_boot": {
          "default": 200,
          "description": "Bootstrap replications and seed.",
          "type": "integer"
        },
        "nbins": {
          "default": 3,
          "description": "Number of moderator bins (cut at sample quantiles) for the binning estimator; ``cutoffs`` overrides the quantiles.",
          "type": "integer"
        },
        "neval": {
          "default": 50,
          "description": "Evaluation points, equally spaced over the moderator's range.",
          "type": "integer"
        },
        "random_state": {
          "description": "Seed for the fold assignment. interflex draws its folds with R's stream, so the selection is compared to R statistically (across seeds), not byte for byte.",
          "type": "integer"
        },
        "seed": {
          "description": "Bootstrap replications and seed.",
          "type": "integer"
        },
        "treat_type": {
          "description": "Inferred from ``d`` when omitted (two distinct values = discrete).",
          "enum": [
            "discrete",
            "continuous"
          ],
          "type": "string"
        },
        "vce": {
          "default": "robust",
          "description": "Variance construction. ``'robust'`` is the HC1 sandwich (interflex's default) and ``'homoscedastic'`` the classical covariance for the linear and binning models and the Wald test; ``'bootstrap'`` (kernel estimator only) draws nonparametric bootstrap standard errors, the linear and binning estimators always reporting delta-method SEs.",
          "enum": [
            "robust",
            "homoscedastic",
            "bootstrap"
          ],
          "type": "string"
        },
        "wald": {
          "default": true,
          "description": "Report the Wald and LR tests of the linear interaction against the binning model (``estimator='binning'`` or ``'linear'``).",
          "type": "boolean"
        },
        "wald_full_moderate": {
          "default": true,
          "description": "Whether the fully interacted model behind the Wald / LR tests also interacts the covariates ``z`` with the bins (R interflex). The Stata command leaves the covariates uninteracted; set ``False`` to reproduce its test.",
          "type": "boolean"
        },
        "wald_test": {
          "default": "chisq",
          "description": "Reference distribution of the Wald statistic: chi-square (R ``lmtest::waldtest(test = \"Chisq\")``) or the F distribution with the full model's residual degrees of freedom (Stata interflex).",
          "enum": [
            "chisq",
            "F"
          ],
          "type": "string"
        },
        "weights": {
          "description": "Observation weights column.",
          "type": "string"
        },
        "x": {
          "description": "Outcome, treatment (binary 0/1 or continuous) and moderator.",
          "type": "string"
        },
        "x_eval": {
          "description": "Explicit evaluation points.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Outcome, treatment (binary 0/1 or continuous) and moderator.",
          "type": "string"
        },
        "z": {
          "description": "Additional covariates entering linearly.",
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "required": [
        "d",
        "x",
        "y"
      ],
      "type": "object"
    },
    "name": "interflex"
  },
  {
    "description": "The interflex figure: marginal effect of ``d`` across ``x`` with its",
    "input_schema": {
      "properties": {
        "ax": {
          "description": "Draw into an existing axes; a new figure is created otherwise.",
          "type": "string"
        },
        "result": {
          "description": "Output of :func:`interflex`.",
          "type": "string"
        },
        "show_hist": {
          "default": true,
          "description": "Add the moderator histogram (treated / control) under the curve.",
          "type": "boolean"
        }
      },
      "required": [
        "result"
      ],
      "type": "object"
    },
    "name": "interflex_plot"
  },
  {
    "description": "Natural-language interpretation of a fitted result. When the connected MCP client advertised sampling, this REUSES the agent's own model (no API key) to explain the estimate, its uncertainty, and what the design does / does not identify -- optionally focused by a `question` and tuned for an `audience`. With no sampling available it falls back to a deterministic structured brief: it NEVER fabricates a narrative. Every claim is grounded in the result's own numbers -- the model is told not to invent estimates. Pass the result_id from an earlier as_handle=true call.",
    "input_schema": {
      "properties": {
        "audience": {
          "default": "researcher",
          "description": "Tone / depth: 'researcher' (precise, names identification assumptions), 'policymaker' (plain, decision-focused), 'general' (no jargon).",
          "enum": [
            "researcher",
            "policymaker",
            "general"
          ],
          "type": "string"
        },
        "question": {
          "description": "Optional specific question to focus the interpretation (e.g. 'is the effect economically meaningful?').",
          "type": "string"
        },
        "result_id": {
          "description": "Handle to a previously-fitted result.",
          "type": "string"
        }
      },
      "required": [
        "result_id"
      ],
      "type": "object"
    },
    "name": "interpret_result"
  },
  {
    "description": "Inverse propensity score OPE. Validation: certified parity evidence. Assumptions: Logging (behaviour) policy propensities are known or correctly estimated; Positivity / common support: the logging policy assigns positive probability to every action the target policy takes; No unmeasured confounding in the logged data. Pre-conditions: X (context), A (logged action), R (reward) and logging propensities are available. Failure modes: High-variance estimate from extreme importance weights when the target policy diverges from logging -> Use self-normalised IPS (snips) or the doubly-robust estimator to reduce variance. Alternatives: sp.snips, sp.doubly_robust, sp.direct_method. Typical minimum N: 500.",
    "input_schema": {
      "properties": {
        "A": {
          "description": "A parameter (np.ndarray).",
          "type": "string"
        },
        "R": {
          "description": "R parameter (np.ndarray).",
          "type": "string"
        },
        "X": {
          "description": "Feature matrix or covariate DataFrame.",
          "type": "string"
        },
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "clip": {
          "default": 50.0,
          "description": "clip parameter (float).",
          "type": "number"
        },
        "pi_behavior": {
          "description": "pi_behavior parameter (Optional[np.ndarray]).",
          "type": "string"
        },
        "pi_target": {
          "description": "pi_target parameter.",
          "type": "string"
        }
      },
      "required": [
        "A",
        "R",
        "X",
        "pi_target"
      ],
      "type": "object"
    },
    "name": "ips"
  },
  {
    "description": "Inverse Probability Weighting for ATE/ATT/ATC with propensity score trimming. Validation: certified parity evidence. Assumptions: Sequential exchangeability / no unmeasured confounding at each time point; Positivity: every treatment level is possible given the past; Correct specification of the treatment and/or outcome models. Pre-conditions: Sequentially measured covariates, (time-varying) treatment, and outcome; Models for the treatment process and the outcome (or weights). Failure modes: Stabilized weights have extreme values (positivity near-violation) -> Truncate weights, simplify the treatment model, or use a doubly-robust estimator (TMLE). Alternatives: sp.tmle, sp.g_computation, sp.ipw. Typical minimum N: 300.",
    "input_schema": {
      "properties": {
        "covariates": {
          "description": "Covariate matrix, DataFrame, or column names.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "estimand": {
          "default": "ATE",
          "description": "Target estimand",
          "enum": [
            "ATE",
            "ATT",
            "ATC"
          ],
          "type": "string"
        },
        "treat": {
          "description": "Treatment indicator or first-treatment-period column.",
          "type": "string"
        },
        "trim": {
          "default": 0.0,
          "description": "Propensity score trimming threshold",
          "type": "number"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "covariates",
        "treat",
        "y"
      ],
      "type": "object"
    },
    "name": "ipw"
  },
  {
    "description": "Compute impulse response functions from VAR. Validation: certified parity evidence.",
    "input_schema": {
      "properties": {
        "cumulative": {
          "default": false,
          "description": "Return cumulative responses ``sum_{j<=s}`` (Stata ``cirf`` / ``coirf``; ``vars::irf(cumulative = TRUE)``).",
          "type": "boolean"
        },
        "impulse": {
          "description": "Impulse variable (if None, all).",
          "type": "string"
        },
        "orthogonal": {
          "default": true,
          "description": "Orthogonalized (Cholesky) IRF.",
          "type": "boolean"
        },
        "periods": {
          "default": 20,
          "description": "Number of periods for IRF.",
          "type": "integer"
        },
        "response": {
          "description": "Response variable (if None, all).",
          "type": "string"
        },
        "sigma_df": {
          "description": "Divisor of the residual covariance that the orthogonalisation uses: ``'ml'`` = ``T`` (Stata ``irf create`` after ``var``), ``'unbiased'`` = ``T - m`` with ``m`` regressors per equation (``vars::Psi``, and Stata after ``var, dfk``). Default follows the fitted model's ``se_df`` (``'stata'``/``'ml'`` -> ``'ml'``, ``'r'``/``'unbiased'`` -> ``'unbiased'``). Irrelevant when ``orthogonal=False``.",
          "enum": [
            "ml",
            "unbiased"
          ],
          "type": "string"
        },
        "var_result": {
          "description": "Estimated VAR model.",
          "type": "string"
        }
      },
      "required": [
        "var_result"
      ],
      "type": "object"
    },
    "name": "irf"
  },
  {
    "description": "Segmented regression for interrupted time series. Validation: certified parity evidence.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "hac_lag": {
          "default": 4,
          "description": "Newey-West truncation lag (Bartlett weights ``1 - j/(L+1)``).",
          "type": "integer"
        },
        "hac_small_sample": {
          "default": false,
          "description": "hac_small_sample parameter (bool).",
          "type": "boolean"
        },
        "intervention": {
          "description": "Time index (integer row position) at which the intervention begins. Required.",
          "type": "integer"
        },
        "seasonality_harmonics": {
          "default": 2,
          "description": "seasonality_harmonics parameter (int).",
          "type": "integer"
        },
        "seasonality_period": {
          "description": "Period P of Fourier seasonal terms (e.g. 12 for monthly data with annual cycle). If None, no seasonality is added.",
          "type": "integer"
        },
        "time": {
          "description": "Time column. If None, uses row index 0..n-1.",
          "type": "string"
        },
        "y": {
          "description": "Outcome column.",
          "type": "string"
        }
      },
      "required": [
        "y"
      ],
      "type": "object"
    },
    "name": "its"
  },
  {
    "description": "Unified IV estimation and the entry point for the whole IV family: k-class (2SLS, LIML, Fuller, GMM, JIVE) plus the many-weak-instrument jackknife variants (jive1, ujive, ijive, rjive, jive_mw), rigorous/post-Lasso instrument selection (lasso, rlasso, post_lasso), marginal treatment effects (mte) and their MST sharp bounds (ivmte_bounds), plausibly-exogenous sensitivity (plausibly_exog_ltz / plausibly_exog_uci), nonparametric and ML variants (npiv, kernel, ivdml, deepiv), quantile IV (ivqreg), Bayesian IV (bayes), continuous-instrument LATE (continuous_late), many-weak-IV Anderson-Rubin (many_weak_ar) and shift-share (shift_share). Includes first-stage F, Sargan/Hansen J, Kleibergen-Paap rk, Sanderson-Windmeijer per-endog F and Hausman diagnostics. Validation: certified parity evidence. Assumptions: Relevance: instruments predict the endogenous regressor (first-stage F >= 10 rule of thumb); Exclusion: instruments affect outcome only through the endogenous regressor; Monotonicity (for LATE interpretation under heterogeneous effects). Pre-conditions: formula includes the (endog ~ instruments) parenthesised block; at least as many instruments as endogenous regressors (order condition...",
    "input_schema": {
      "properties": {
        "augmented_diagnostics": {
          "default": true,
          "description": "When the method returns EconometricResults, attach the Kleibergen-Paap rk statistic, Sanderson-Windmeijer per-endogenous F and Olea-Pflueger effective F to result.diagnostics.",
          "type": "boolean"
        },
        "cluster": {
          "description": "Column name for cluster-robust SEs",
          "type": "string"
        },
        "formula": {
          "description": "IV formula: 'y ~ (endog ~ instruments) + exog'",
          "type": "string"
        },
        "fuller_alpha": {
          "default": 1.0,
          "description": "Fuller constant (method='fuller' only)",
          "type": "number"
        },
        "method": {
          "default": "2sls",
          "description": "Estimation method. The k-class methods (2sls, liml, fuller, gmm, jive) take `formula` + `data`; the rest take their own explicit arguments -- see the target function's docstring. Aliases are case-insensitive and -/_ interchangeable.",
          "enum": [
            "2sls",
            "liml",
            "fuller",
            "gmm",
            "jive",
            "jive1",
            "ujive",
            "ijive",
            "rjive",
            "jive_mw",
            "many_weak_ar",
            "lasso",
            "rlasso",
            "post_lasso",
            "mte",
            "ivmte_bounds",
            "plausibly_exog_ltz",
            "plausibly_exog_uci",
            "npiv",
            "kernel",
            "ivdml",
            "deepiv",
            "ivqreg",
            "bayes",
            "continuous_late",
            "shift_share"
          ],
          "type": "string"
        },
        "robust": {
          "default": "nonrobust",
          "description": "Standard error type",
          "enum": [
            "nonrobust",
            "hc0",
            "hc1",
            "hc2",
            "hc3"
          ],
          "type": "string"
        }
      },
      "required": [
        "formula"
      ],
      "type": "object"
    },
    "name": "iv"
  },
  {
    "description": "Nevo-Rosen (2012) bounds for LATE under imperfect instruments. Assumptions: Only weak (set-identifying) assumptions are imposed; the result is an interval, not a point; Lee bounds add monotonicity of selection; Oster's delta adds proportional selection on observed vs. unobserved. Pre-conditions: The data needed for the point-identifying analysis, plus the weakest credible identifying restriction; For Lee bounds: a binary selection/attrition indicator. Failure modes: Bounds are too wide to be informative -> Add a credible auxiliary restriction (monotone treatment response, instrument) to tighten the bounds. Alternatives: sp.oster_delta, sp.lee_bounds, sp.manski_bounds. Typical minimum N: 100.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "assumption": {
          "default": "monotone_iv",
          "description": "- ``'monotone_iv'``: instrument has same-sign direct effect as through the treatment (Nevo-Rosen Proposition 2). - ``'less_than_late'``: direct effect of Z on Y is weakly less than the indirect effect (tighter).",
          "type": "string"
        },
        "controls": {
          "description": "Control variables (residualized out via OLS).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "instrument": {
          "description": "Instrument variable (binary).",
          "type": "string"
        },
        "n_boot": {
          "default": 500,
          "description": "Number of bootstrap replications.",
          "type": "integer"
        },
        "random_state": {
          "default": 42,
          "description": "Random seed or RandomState for reproducible stochastic steps.",
          "type": "integer"
        },
        "treatment": {
          "description": "Endogenous treatment (binary).",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable.",
          "type": "string"
        }
      },
      "required": [
        "instrument",
        "treatment",
        "y"
      ],
      "type": "object"
    },
    "name": "iv_bounds"
  },
  {
    "description": "Run several k-class / JIVE estimators on the same IV specification and return a one-row-per-method comparison DataFrame (estimate, SE, CI, first-stage F). Useful as a sensitivity sanity check before reporting. Assumptions: Instrument relevance (non-zero first stage); Exclusion restriction: the instrument affects the outcome only through the treatment; Independence/exogeneity of the instrument; for LATE, monotonicity (no defiers). Pre-conditions: An instrument plausibly affecting treatment, an endogenous treatment, and an outcome; A strong first stage (assess instrument strength before interpreting estimates). Failure modes: Weak first stage -- biased point estimates and unreliable conventional SEs -> Report first-stage F / effective F and use weak-IV-robust inference (Anderson-Rubin). Alternatives: sp.iv, sp.anderson_rubin_ci, sp.dml. Typical minimum N: 200.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "endog_name": {
          "description": "Override endogenous-coefficient name lookup",
          "type": "string"
        },
        "formula": {
          "description": "Model formula using patsy/R-style syntax.",
          "type": "string"
        },
        "methods": {
          "default": [
            "2sls",
            "liml",
            "fuller",
            "jive"
          ],
          "description": "methods parameter (tuple[str]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "required": [
        "formula"
      ],
      "type": "object"
    },
    "name": "iv_compare"
  },
  {
    "description": "Modern IV reporting bundle (R `ivDiag` analogue). Combines 2SLS point estimate, analytic + pairs/wild bootstrap SEs, Olea-Pflueger effective F, Lee-McCrary-Moreira-Porter (2022) tF-corrected critical value, Anderson-Rubin / CLR / K weak-IV-robust confidence sets, Kleibergen-Paap rk LM, Conley-Hansen-Rossi (2012) plausibly-exogenous LTZ sensitivity, and a Blandhol-Bonney-Mogstad-Torgovitsky (2022/2025) / Soczynski (2024) `TSLS-as-LATE` caveat into a single IVDiagResult. Validation: certified parity evidence. Assumptions: Instrument relevance (non-zero first stage); Exclusion restriction: the instrument affects the outcome only through the treatment; Independence/exogeneity of the instrument; for LATE, monotonicity (no defiers). Pre-conditions: An instrument plausibly affecting treatment, an endogenous treatment, and an outcome; A strong first stage (assess instrument strength before interpreting estimates). Failure modes: Weak first stage -- biased point estimates and unreliable conventional SEs -> Report first-stage F / effective F and use weak-IV-robust inference (Anderson-Rubin). Alternatives: sp.iv, sp.anderson_rubin_ci, sp.dml. Typical minimum N: 200.",
    "input_schema": {
      "properties": {
        "absorb": {
          "description": "High-dimensional fixed effects to partial out before the bundle is computed, so every statistic describes the absorbed specification (ivreghdfe-equivalent)",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "boot_methods": {
          "default": [
            "pairs"
          ],
          "description": "Subset of {'pairs','wild'}",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "cluster": {
          "description": "Cluster column for cluster-robust SE / cluster bootstrap",
          "type": "string"
        },
        "endog": {
          "description": "Single endogenous regressor",
          "type": "string"
        },
        "exog": {
          "description": "Optional included exogenous controls",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "h0": {
          "default": 0.0,
          "description": "Null hypothesis for AR/CLR/K",
          "type": "number"
        },
        "include_clr_ci": {
          "default": false,
          "description": "Whether to include clr ci.",
          "type": "boolean"
        },
        "include_k_ci": {
          "default": false,
          "description": "Whether to include k ci.",
          "type": "boolean"
        },
        "instruments": {
          "description": "instruments parameter (list[str] | str).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "ltz_gamma_sd": {
          "description": "Standard deviation of CHR (2012) LTZ Gaussian prior on gamma",
          "type": "number"
        },
        "n_boot": {
          "default": 1000,
          "description": "Bootstrap replications (0 to skip)",
          "type": "integer"
        },
        "random_state": {
          "description": "Random seed or RandomState for reproducible stochastic steps.",
          "type": "integer"
        },
        "vcov": {
          "default": "HC1",
          "description": "Heteroskedasticity-robust covariance type",
          "enum": [
            "HC0",
            "HC1",
            "classic"
          ],
          "type": "string"
        },
        "y": {
          "description": "Outcome column",
          "type": "string"
        }
      },
      "required": [
        "endog",
        "instruments",
        "y"
      ],
      "type": "object"
    },
    "name": "iv_diag"
  },
  {
    "description": "Instrumental forest (grf::instrumental_forest): heterogeneous local average treatment effects tau(x) = Cov[Y,Z|x]/Cov[W,Z|x] from an honest generalized random forest that splits on the IV gradient and uses the instrument in its split constraints. Reports out-of-bag CATEs with little-bag variances and the doubly-robust average conditional LATE (compliance-weighted scores). Given grf's forest, the local solve, scores, average and BLP match grf to 1e-13 (T2); the forest itself is T3. Validation: validated evidence tier (known-truth, reference, external-parity, or Monte Carlo artifact). Assumptions: instrument relevance, exclusion and independence given X; monotonicity for a complier interpretation of tau(x). Pre-conditions: instrument varies within covariate cells; first stage not near zero anywhere. Failure modes: DataInsufficient: the instrument has no variation -> Use a varying instrument; UserWarning: compliance score near zero -> Locally weak instrument; trim covariate regions or report the ATE with caution. Alternatives: sp.ivreg, sp.dml, sp.causal_forest. Typical minimum N: 1000.",
    "input_schema": {
      "properties": {
        "W_hat": {
          "description": "Precomputed E[W|X]; default out-of-bag regression forest.",
          "type": "string"
        },
        "Y_hat": {
          "description": "Precomputed E[Y|X]; default out-of-bag regression forest.",
          "type": "string"
        },
        "Z_hat": {
          "description": "Precomputed E[Z|X]; default out-of-bag regression forest.",
          "type": "string"
        },
        "alpha": {
          "default": 0.05,
          "description": "Significance level of the reported interval.",
          "type": "number"
        },
        "ci_group_size": {
          "default": 2,
          "description": "Trees per little bag; 1 disables variance estimates.",
          "type": "integer"
        },
        "clusters": {
          "description": "Cluster ids: trees sample whole clusters and every standard error is cluster-robust.",
          "type": "string"
        },
        "compliance_score": {
          "description": "``Delta(X_i)`` for the average-effect scores; by default estimated by an auxiliary 500-tree causal forest of ``W`` on ``Z``.",
          "type": "string"
        },
        "covariates": {
          "description": "Effect modifiers ``X``; the nuisances are also regressed on them.",
          "type": "string"
        },
        "equalize_cluster_weights": {
          "default": false,
          "description": "Give every cluster the same weight (incompatible with ``weights``).",
          "type": "boolean"
        },
        "honest": {
          "default": true,
          "description": "grf honesty: grow on one half of the drawn sample, estimate leaves on the other.",
          "type": "boolean"
        },
        "honesty_fraction": {
          "default": 0.5,
          "description": "Share of the drawn sample used to grow the tree.",
          "type": "number"
        },
        "honesty_prune_leaves": {
          "default": true,
          "description": "Prune leaves left empty by the estimation half.",
          "type": "boolean"
        },
        "imbalance_penalty": {
          "default": 0.0,
          "description": "grf imbalance.penalty on unbalanced splits.",
          "type": "number"
        },
        "instrument": {
          "description": "Instrument (binary or continuous).",
          "type": "string"
        },
        "max_depth": {
          "description": "Depth cap (no grf analogue).",
          "type": "string"
        },
        "max_samples": {
          "default": 0.5,
          "description": "Fraction of clusters drawn per tree (grf ``sample.fraction``).",
          "type": "number"
        },
        "min_samples_leaf": {
          "default": 5,
          "description": "grf ``min.node.size``.",
          "type": "integer"
        },
        "mtry": {
          "description": "Candidate variables per split (grf default ``min(ceil(sqrt(p) + 20), p)``).",
          "type": "string"
        },
        "n_bootstrap": {
          "description": "Ignored. Standard errors now come from the doubly-robust scores.",
          "type": "string"
        },
        "n_estimators": {
          "default": 2000,
          "description": "Trees (grf ``num.trees``).",
          "type": "integer"
        },
        "n_jobs": {
          "default": 1,
          "description": "Threads used to grow trees (-1 = all cores).",
          "type": "integer"
        },
        "random_state": {
          "default": 42,
          "description": "Seed; different seeds give independent forests.",
          "type": "integer"
        },
        "reduced_form_weight": {
          "default": 0.0,
          "description": "Mix the IV split criterion with the causal-forest criterion that treats ``W`` as exogenous (grf ``reduced.form.weight``).",
          "type": "number"
        },
        "split_alpha": {
          "default": 0.05,
          "description": "grf ``alpha``: minimum share of the parent's instrument variation each child of a stabilised split keeps.",
          "type": "number"
        },
        "stabilize_splits": {
          "default": true,
          "description": "Use the instrument in the split constraints.",
          "type": "boolean"
        },
        "treat": {
          "description": "Treatment (binary or continuous).",
          "type": "string"
        },
        "weights": {
          "description": "Sample weights (grf ``sample.weights``).",
          "type": "string"
        },
        "y": {
          "description": "Outcome (column name or array).",
          "type": "string"
        }
      },
      "required": [],
      "type": "object"
    },
    "name": "iv_forest"
  },
  {
    "description": "Instrumental-variable quantile regression (Chernozhukov-Hansen). Validation: validated evidence tier (known-truth, reference, external-parity, or Monte Carlo artifact).",
    "input_schema": {
      "properties": {
        "add_constant": {
          "default": true,
          "description": "add_constant parameter (bool).",
          "type": "boolean"
        },
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals.",
          "type": "number"
        },
        "bootstrap": {
          "default": 0,
          "description": "Number of pairs-bootstrap replications for standard errors. ``0`` disables bootstrap; asymptotic rank-test inversion is not implemented in this MVP.",
          "type": "integer"
        },
        "endog": {
          "description": "Endogenous regressor(s) ``D``.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "exog": {
          "description": "Exogenous controls ``X`` (may be empty).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "instruments": {
          "description": "Instrument(s) ``Z``. Must be at least as many as ``endog``.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "n_grid": {
          "default": 41,
          "description": "Grid resolution for the profile search over ``alpha`` (scalar case) -- ignored when ``endog`` is multi-dimensional.",
          "type": "integer"
        },
        "random_state": {
          "default": 1234,
          "description": "Random seed or RandomState for reproducible stochastic steps.",
          "type": "integer"
        },
        "refine": {
          "default": true,
          "description": "After the grid search, refine ``alpha`` with a local optimizer.",
          "type": "boolean"
        },
        "tau": {
          "default": 0.5,
          "description": "Quantile(s) of interest in ``(0, 1)``.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "verbose": {
          "default": false,
          "description": "verbose parameter (bool).",
          "type": "boolean"
        },
        "y": {
          "description": "Outcome variable.",
          "type": "string"
        }
      },
      "required": [
        "endog",
        "instruments",
        "y"
      ],
      "type": "object"
    },
    "name": "ivqreg"
  },
  {
    "description": "2SLS instrumental-variables regression with robust or clustered SEs and first-stage F diagnostics. Formula syntax: 'y ~ x_exog + (d_endog ~ z_instrument)'. Validation: certified parity evidence.",
    "input_schema": {
      "properties": {
        "formula": {
          "description": "'y ~ x + (d ~ z)' style.",
          "type": "string"
        },
        "robust": {
          "default": "hc1",
          "enum": [
            "hc1",
            "hc2",
            "hc3",
            "nonrobust"
          ],
          "type": "string"
        }
      },
      "required": [
        "formula"
      ],
      "type": "object"
    },
    "name": "ivreg"
  },
  {
    "description": "Leave-one-cluster-out jackknife standard errors. Validation: certified parity evidence.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals.",
          "type": "number"
        },
        "cluster": {
          "description": "Name of the cluster variable in ``data``.",
          "type": "string"
        },
        "result": {
          "description": "A fitted regression result from ``sp.regress()``.",
          "type": "string"
        }
      },
      "required": [
        "cluster",
        "result"
      ],
      "type": "object"
    },
    "name": "jackknife_se"
  },
  {
    "description": "Jackknife Instrumental Variables Estimation (JIVE). Validation: certified parity evidence.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "cluster": {
          "description": "Cluster identifier column for clustered standard errors.",
          "type": "string"
        },
        "robust": {
          "default": "nonrobust",
          "description": "Robust standard-error or covariance estimator option.",
          "type": "string"
        },
        "variant": {
          "default": "jive1",
          "description": "Angrist, Imbens & Krueger (1999) jackknife instrument, used as the instrument in an IV second stage (Stata ``jive``'s ``ujive1`` / ``ujive2``): - ``'jive1'``: ``(Z_i pi - h_i x_i) / (1 - h_i)`` -- the leave-one-out first-stage fitted value. - ``'jive2'``: ``(Z_i pi - h_i x_i) / (1 - 1/n)``. ``h_i`` is the leverage of the first-stage regression on the included exogenous regressors and the excluded instruments.",
          "type": "string"
        },
        "x_endog": {
          "description": "x_endog parameter (List[str]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "x_exog": {
          "description": "x_exog parameter (Optional[List[str]]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        },
        "z": {
          "description": "Instrument, proxy, or auxiliary variable used by this estimator.",
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "required": [
        "x_endog",
        "y"
      ],
      "type": "object"
    },
    "name": "jive"
  },
  {
    "description": "Johansen (1991) cointegration test. Validation: certified parity evidence.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Level of the Osterwald-Lenum critical values (the table Stata's ``vecrank`` uses) that decide ``rank``.",
          "enum": [
            "0.05",
            "0.01"
          ],
          "type": "number"
        },
        "lags": {
          "default": 1,
          "description": "Number of lagged **differences** in the VECM. This is Stata's ``vecrank, lags(p)`` minus one and ``ca.jo(K = lags + 1)``.",
          "type": "integer"
        },
        "test": {
          "default": "trace",
          "description": "test parameter (str).",
          "enum": [
            "trace",
            "maxeig"
          ],
          "type": "string"
        },
        "trend": {
          "default": "c",
          "description": "Deterministic specification (Stata ``vecrank, trend()`` names in parentheses): ``'n'`` (``none``); ``'rc'`` (``rconstant``, constant restricted to the cointegrating space = ``ca.jo(ecdet=\"const\")``); ``'c'`` (``constant``, unrestricted constant = ``ca.jo(ecdet=\"none\")``); ``'rt'`` (``rtrend``, trend restricted to the cointegrating space, unrestricted constant = ``ca.jo(ecdet=\"trend\")``); ``'ct'`` (``trend``, unrestricted constant and trend). Stata's long names are accepted as aliases.",
          "type": "string"
        },
        "variables": {
          "description": "Variables to test.",
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "required": [],
      "type": "object"
    },
    "name": "johansen"
  },
  {
    "description": "Deprecated alias for :func:`dist_iv`. Validation: validated evidence tier (known-truth, reference, external-parity, or Monte Carlo artifact). Assumptions: Selection-on-observables (unconfoundedness + overlap) or, for IV variants, instrument validity; For IV-QTE: rank invariance / rank similarity (monotonicity of the structural quantile function). Pre-conditions: Covariates, treatment, and outcome; for IV-quantile methods, a valid instrument; Enough data to estimate the outcome distribution across quantiles. Failure modes: Estimated conditional quantiles cross (non-monotone), or tail quantiles are unstable -> Use rearrangement / monotonization and avoid extreme quantiles where data are sparse. Alternatives: sp.qte, sp.iv, sp.dml. Typical minimum N: 500.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "covariates": {
          "description": "Covariate matrix, DataFrame, or column names.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "instrument": {
          "description": "instrument parameter (str).",
          "type": "string"
        },
        "n_boot": {
          "default": 200,
          "description": "Number of bootstrap replications.",
          "type": "integer"
        },
        "quantiles": {
          "description": "quantiles parameter (Optional[np.ndarray]).",
          "type": "string"
        },
        "seed": {
          "default": 0,
          "description": "Random seed for reproducible stochastic steps.",
          "type": "integer"
        },
        "treat": {
          "description": "Treatment indicator or first-treatment-period column.",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "instrument",
        "treat",
        "y"
      ],
      "type": "object"
    },
    "name": "kan_dlate"
  },
  {
    "description": "Kaplan-Meier non-parametric survival function estimator. Validation: certified parity evidence.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals (Greenwood formula).",
          "type": "number"
        },
        "duration": {
          "description": "Column name for duration / follow-up time.",
          "type": "string"
        },
        "event": {
          "description": "Column name for event indicator (1 = event, 0 = censored).",
          "type": "string"
        },
        "group": {
          "description": "Column name for group variable (stratification).",
          "type": "string"
        }
      },
      "required": [
        "duration",
        "event"
      ],
      "type": "object"
    },
    "name": "kaplan_meier"
  },
  {
    "description": "Kernel IV regression with uniform confidence bands (Lob et al. 2025). Estimates the structural function h*(d) = E[Y | do(D=d)] via kernel-weighted local averaging under a continuous instrument Z, with wild-bootstrap uniform SEs. Assumptions: Instrument relevance (non-zero first stage); Exclusion restriction: the instrument affects the outcome only through the treatment; Independence/exogeneity of the instrument; for LATE, monotonicity (no defiers). Pre-conditions: An instrument plausibly affecting treatment, an endogenous treatment, and an outcome; A strong first stage (assess instrument strength before interpreting estimates); formula includes the (endog ~ instruments) parenthesised block. Failure modes: Weak first stage -- biased point estimates and unreliable conventional SEs -> Report first-stage F / effective F and use weak-IV-robust inference (Anderson-Rubin); First-stage F < 10 (Stock-Yogo 5% bias) -> Use weak-IV-robust inference (Anderson-Rubin) or LIML; Over-identification test rejects (sp.estat 'overid') -> At least one instrument is invalid; drop instruments or switch to just-identified LIML. Alternatives: sp.iv, sp.anderson_rubin_ci, sp.dml, sp.deepiv. Typical minimum...",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "bandwidth": {
          "description": "Silverman default",
          "type": "number"
        },
        "grid": {
          "description": "Grid of d-values (default 30 quantile-evenly spaced)",
          "type": "string"
        },
        "instrument": {
          "description": "Continuous instrument Z",
          "type": "string"
        },
        "n_boot": {
          "default": 100,
          "description": "Number of bootstrap replications.",
          "type": "integer"
        },
        "ridge": {
          "default": 0.001,
          "description": "Tikhonov regularisation",
          "type": "number"
        },
        "seed": {
          "default": 0,
          "description": "Random seed for reproducible stochastic steps.",
          "type": "integer"
        },
        "treat": {
          "description": "Continuous treatment D",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "instrument",
        "treat",
        "y"
      ],
      "type": "object"
    },
    "name": "kernel_iv"
  },
  {
    "description": "Run RDD + RKD + Bunching on the same data. Assumptions: The counterfactual density would be smooth through the threshold absent the policy; Excess mass at the threshold reflects the behavioural elasticity of interest; No other discontinuity coincides with the threshold. Pre-conditions: A behavioural choice variable (earnings, hours, ...) with a known kink or notch in the budget/choice set; A visible empirical density of the running variable around the threshold. Failure modes: Round-number heaping or a coincident policy contaminates the bunching mass -> Exclude heaping points, widen the excluded region, and test the counterfactual polynomial order. Alternatives: sp.rdrobust, sp.rkd. Typical minimum N: 500.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "bandwidth": {
          "description": "Bandwidth used for local smoothing or kernel weighting.",
          "type": "number"
        },
        "bin_width": {
          "description": "bin_width parameter (Optional[float]).",
          "type": "number"
        },
        "cutoff": {
          "default": 0.0,
          "description": "cutoff parameter (float).",
          "type": "number"
        },
        "polynomial_order": {
          "default": 2,
          "description": "polynomial_order parameter (int).",
          "type": "integer"
        },
        "running": {
          "description": "Running variable (also forms density for Bunching).",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable (used by RDD/RKD).",
          "type": "string"
        }
      },
      "required": [
        "running",
        "y"
      ],
      "type": "object"
    },
    "name": "kink_unified"
  },
  {
    "description": "Kitagawa (1955) two-factor rate decomposition. Validation: certified parity evidence.",
    "input_schema": {
      "properties": {
        "by": {
          "description": "Category variable(s) defining cells.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "group": {
          "description": "Binary group indicator.",
          "type": "string"
        },
        "normalize": {
          "default": "symmetric",
          "description": "- 'a': rate effect evaluated at A's composition - 'b': rate effect evaluated at B's composition - 'symmetric': average (default)",
          "enum": [
            "symmetric",
            "a",
            "b"
          ],
          "type": "string"
        },
        "rate": {
          "description": "Column holding the category-specific rate (or 0/1 outcome at the individual level).",
          "type": "string"
        },
        "weights": {
          "description": "Cell population weights. If None, each row treated as individual-level data (weight = 1).",
          "type": "string"
        }
      },
      "required": [
        "by",
        "group",
        "rate"
      ],
      "type": "object"
    },
    "name": "kitagawa_decompose"
  },
  {
    "description": "Kitagawa (2015) specification test for the validity of LATE.",
    "input_schema": {
      "properties": {
        "instrument": {
          "description": "Binary instrument variable (Z).",
          "type": "string"
        },
        "n_boot": {
          "default": 1000,
          "description": "Number of bootstrap replications for the p-value.",
          "type": "integer"
        },
        "n_grid": {
          "default": 100,
          "description": "Number of grid points for evaluating the CDF conditions.",
          "type": "integer"
        },
        "seed": {
          "description": "Random seed for reproducibility.",
          "type": "integer"
        },
        "treatment": {
          "description": "Endogenous binary treatment variable (D).",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable name.",
          "type": "string"
        }
      },
      "required": [
        "instrument",
        "treatment",
        "y"
      ],
      "type": "object"
    },
    "name": "kitagawa_test"
  },
  {
    "description": "LASSO-selected instrumental variables (information-criterion penalty). Validation: validated evidence tier (known-truth, reference, external-parity, or Monte Carlo artifact). Assumptions: Relevance: instruments predict the endogenous regressor (first-stage F >= 10 rule of thumb); Exclusion: instruments affect outcome only through the endogenous regressor; Monotonicity (for LATE interpretation under heterogeneous effects). Pre-conditions: formula includes the (endog ~ instruments) parenthesised block; at least as many instruments as endogenous regressors (order condition); instruments are not themselves endogenous in the outcome equation. Failure modes: First-stage F < 10 (Stock-Yogo 5% bias) -> Use weak-IV-robust inference (Anderson-Rubin) or LIML; Over-identification test rejects (sp.estat 'overid') -> At least one instrument is invalid; drop instruments or switch to just-identified LIML; Hausman endogeneity test fails to reject -> OLS may be consistent and more efficient; report both. Alternatives: sp.deepiv, sp.bartik, sp.proximal, sp.regress. Typical minimum N: 100.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "cluster": {
          "description": "Cluster identifier column for clustered standard errors.",
          "type": "string"
        },
        "d": {
          "description": "d parameter (Optional[Any]).",
          "type": "string"
        },
        "penalty": {
          "default": "bic",
          "description": "Instrument selection criterion: 'bic', 'aic', 'cv'.",
          "type": "string"
        },
        "robust": {
          "default": "robust",
          "description": "Robust standard-error or covariance estimator option.",
          "type": "string"
        },
        "x_endog": {
          "description": "x_endog parameter (Optional[List[str]]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "x_exog": {
          "description": "x_exog parameter (Optional[List[str]]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        },
        "z": {
          "description": "Full set of candidate instruments.",
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "required": [
        "y"
      ],
      "type": "object"
    },
    "name": "lasso_iv"
  },
  {
    "description": "LASSO-based variable selection. Validation: validated evidence tier (known-truth, reference, external-parity, or Monte Carlo artifact).",
    "input_schema": {
      "properties": {
        "eps": {
          "default": 0.001,
          "description": "Ratio of lambda_min / lambda_max.",
          "type": "number"
        },
        "max_iter": {
          "default": 1000,
          "description": "Maximum coordinate descent iterations per lambda.",
          "type": "integer"
        },
        "method": {
          "default": "cv",
          "description": "How to choose the regularisation parameter lambda.",
          "enum": [
            "cv",
            "bic",
            "aic"
          ],
          "type": "string"
        },
        "n_folds": {
          "default": 10,
          "description": "Number of cross-validation folds (only for ``method=\"cv\"``).",
          "type": "integer"
        },
        "n_lambda": {
          "default": 100,
          "description": "Number of lambda values in the grid.",
          "type": "integer"
        },
        "seed": {
          "default": 42,
          "description": "Random seed for CV fold assignment.",
          "type": "integer"
        },
        "tol": {
          "default": 1e-06,
          "description": "Convergence tolerance.",
          "type": "number"
        },
        "verbose": {
          "default": true,
          "description": "Print progress.",
          "type": "boolean"
        },
        "x": {
          "description": "Candidate independent variables.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Dependent variable column.",
          "type": "string"
        }
      },
      "required": [
        "x",
        "y"
      ],
      "type": "object"
    },
    "name": "lasso_select"
  },
  {
    "description": "Compute Lee (2009) bounds for ATE under sample selection. Validation: certified parity evidence. Assumptions: Only weak (set-identifying) assumptions are imposed; the result is an interval, not a point; Lee bounds add monotonicity of selection; Oster's delta adds proportional selection on observed vs. unobserved. Pre-conditions: The data needed for the point-identifying analysis, plus the weakest credible identifying restriction; For Lee bounds: a binary selection/attrition indicator. Failure modes: Bounds are too wide to be informative -> Add a credible auxiliary restriction (monotone treatment response, instrument) to tighten the bounds. Alternatives: sp.oster_delta, sp.lee_bounds, sp.manski_bounds. Typical minimum N: 100.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level.",
          "type": "number"
        },
        "covariates": {
          "description": "Reserved for covariate-tightened bounds, which are not implemented; a non-empty list is ignored with a ``UserWarning`` (it used to be ignored silently).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "n_bootstrap": {
          "default": 500,
          "description": "Bootstrap iterations for inference.",
          "type": "integer"
        },
        "random_state": {
          "default": 42,
          "description": "Random seed or RandomState for reproducible stochastic steps.",
          "type": "integer"
        },
        "se_method": {
          "default": "bootstrap",
          "description": "Standard errors of the two bounds. ``'analytic'`` is the asymptotic variance of Lee (2009, Proposition 3) exactly as Stata's ``leebounds`` (default ``vce(analytic)``) computes it; ``'bootstrap'`` resamples units ``n_bootstrap`` times.",
          "enum": [
            "bootstrap",
            "analytic"
          ],
          "type": "string"
        },
        "selection": {
          "description": "Binary selection/retention indicator (1 = observed, 0 = missing).",
          "type": "string"
        },
        "treat": {
          "description": "Binary treatment variable (0/1).",
          "type": "string"
        },
        "trimming": {
          "default": "quantile",
          "description": "How the ``p = |p1 - p0| / max(p1, p0)`` share is trimmed from the arm with higher retention. ``'quantile'`` is Lee's (2009) sample",
          "enum": [
            "quantile",
            "exact"
          ],
          "type": "string"
        },
        "y": {
          "description": "Outcome variable (may have NaN for selected-out units).",
          "type": "string"
        }
      },
      "required": [
        "selection",
        "treat",
        "y"
      ],
      "type": "object"
    },
    "name": "lee_bounds"
  },
  {
    "description": "Limited Information Maximum Likelihood (LIML) estimator. Validation: certified parity evidence. Assumptions: Relevance: instruments predict the endogenous regressor (first-stage F >= 10 rule of thumb); Exclusion: instruments affect outcome only through the endogenous regressor; Monotonicity (for LATE interpretation under heterogeneous effects). Pre-conditions: formula includes the (endog ~ instruments) parenthesised block; at least as many instruments as endogenous regressors (order condition); instruments are not themselves endogenous in the outcome equation. Failure modes: First-stage F < 10 (Stock-Yogo 5% bias) -> Use weak-IV-robust inference (Anderson-Rubin) or LIML; Over-identification test rejects (sp.estat 'overid') -> At least one instrument is invalid; drop instruments or switch to just-identified LIML; Hausman endogeneity test fails to reject -> OLS may be consistent and more efficient; report both. Alternatives: sp.deepiv, sp.bartik, sp.proximal, sp.regress. Typical minimum N: 100.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "cluster": {
          "description": "Cluster identifier column for clustered standard errors.",
          "type": "string"
        },
        "formula": {
          "description": "Model formula using patsy/R-style syntax.",
          "type": "string"
        },
        "fuller": {
          "description": "Fuller's constant (typically 1 or 4). If None, pure LIML.",
          "type": "number"
        },
        "robust": {
          "default": "nonrobust",
          "description": "Robust standard-error or covariance estimator option.",
          "type": "string"
        },
        "x_endog": {
          "description": "Endogenous regressors.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "x_exog": {
          "description": "Exogenous regressors (included instruments).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Outcome variable.",
          "type": "string"
        },
        "z": {
          "description": "Excluded instruments.",
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "required": [],
      "type": "object"
    },
    "name": "liml"
  },
  {
    "description": "Estimate a linear combination of coefficients with inference. Validation: certified parity evidence.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level.",
          "type": "number"
        },
        "expression": {
          "description": "Linear combination (Stata ``lincom`` syntax). Examples: - ``\"x1 + x2\"`` -- beta_x1 + beta_x2 - ``\"x1 - x2\"`` -- beta_x1 - beta_x2 - ``\"2*x1 + 3*x2\"`` -- 2*beta_x1 + 3*beta_x2 - ``\"_cons + x1\"`` -- intercept plus slope",
          "type": "string"
        },
        "result": {
          "description": "Fitted model.",
          "type": "string"
        }
      },
      "required": [
        "expression",
        "result"
      ],
      "type": "object"
    },
    "name": "lincom"
  },
  {
    "description": "Fit DirectLiNGAM (Shimizu 2011). Validation: validated evidence tier (known-truth, reference, external-parity, or Monte Carlo artifact). Assumptions: Causal Markov condition and faithfulness (PC/GES/FCI); Causal sufficiency for PC/GES (no latent confounders); FCI relaxes this; Acyclicity; LiNGAM additionally assumes a linear non-Gaussian model. Pre-conditions: Constraint-/score-based discovery needs i.i.d. observational data with enough samples for reliable conditional-independence tests; Invariance-based discovery (ICP) needs data labelled by environment / intervention. Failure modes: Unstable skeleton / many undirected edges -- faithfulness or sample size is the likely culprit -> Increase the sample, relax the CI-test threshold, or switch to FCI if latent confounders are plausible. Alternatives: sp.pc_algorithm, sp.fci, sp.ges, sp.lingam. Typical minimum N: 500.",
    "input_schema": {
      "properties": {
        "standardize": {
          "default": true,
          "description": "Zero-mean / unit-variance each variable before the algorithm.",
          "type": "boolean"
        }
      },
      "required": [],
      "type": "object"
    },
    "name": "lingam"
  },
  {
    "description": "List all available replication datasets and guides.",
    "input_schema": {
      "properties": {},
      "required": [],
      "type": "object"
    },
    "name": "list_replications"
  },
  {
    "description": "Propose a candidate DAG from variable names + domain description. Assumptions: LLM-proposed graphs / priors are hypotheses to validate, not statistical identification; Human review or data-driven falsification is required before any causal claim. Pre-conditions: Domain context and a bounded list of candidate variables; A configured, logged LLM provider for reproducibility. Failure modes: Proposals are unstable across runs or include hallucinated variables/edges -> Fix the model release and seed, add constraints, and cross-check with constraint-based discovery. Alternatives: sp.llm_dag_constrained, sp.causal_discovery, sp.dag. Typical minimum N: 1.",
    "input_schema": {
      "properties": {
        "client": {
          "description": "An LLM client implementing ``.complete(prompt: str) -> str``. If ``None``, use the deterministic heuristic backend.",
          "type": "string"
        },
        "domain": {
          "default": "",
          "description": "Free-text domain description (e.g. \"labor economics, education and earnings\"). Helps the LLM but ignored by the heuristic backend.",
          "type": "string"
        },
        "seed": {
          "default": 0,
          "description": "Random seed for reproducible stochastic steps.",
          "type": "integer"
        },
        "variables": {
          "description": "Names of variables in the dataset.",
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "required": [
        "variables"
      ],
      "type": "object"
    },
    "name": "llm_dag_propose"
  },
  {
    "description": "Propose sensitivity-analysis priors for the substantive setting. Assumptions: LLM-proposed graphs / priors are hypotheses to validate, not statistical identification; Human review or data-driven falsification is required before any causal claim. Pre-conditions: Domain context and a bounded list of candidate variables; A configured, logged LLM provider for reproducibility. Failure modes: Proposals are unstable across runs or include hallucinated variables/edges -> Fix the model release and seed, add constraints, and cross-check with constraint-based discovery. Alternatives: sp.llm_dag_constrained, sp.causal_discovery, sp.dag. Typical minimum N: 1.",
    "input_schema": {
      "properties": {
        "client": {
          "description": "LLM client with ``.complete(prompt: str) -> str``.",
          "type": "string"
        },
        "domain": {
          "default": "health",
          "description": "domain parameter (str).",
          "enum": [
            "health",
            "education",
            "labor",
            "policy",
            "marketing"
          ],
          "type": "string"
        },
        "outcome": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        },
        "treatment": {
          "description": "Treatment indicator, treatment variable, or treatment array.",
          "type": "string"
        }
      },
      "required": [
        "outcome",
        "treatment"
      ],
      "type": "object"
    },
    "name": "llm_sensitivity_priors"
  },
  {
    "description": "Enumerate plausible unobserved confounders for a study. Assumptions: LLM-proposed graphs / priors are hypotheses to validate, not statistical identification; Human review or data-driven falsification is required before any causal claim. Pre-conditions: Domain context and a bounded list of candidate variables; A configured, logged LLM provider for reproducibility. Failure modes: Proposals are unstable across runs or include hallucinated variables/edges -> Fix the model release and seed, add constraints, and cross-check with constraint-based discovery. Alternatives: sp.llm_dag_constrained, sp.causal_discovery, sp.dag. Typical minimum N: 1.",
    "input_schema": {
      "properties": {
        "client": {
          "description": "LLM client with ``.complete(prompt: str) -> str``.",
          "type": "string"
        },
        "domain": {
          "default": "health",
          "description": "domain parameter (str).",
          "enum": [
            "health",
            "education",
            "labor",
            "policy",
            "marketing"
          ],
          "type": "string"
        },
        "outcome": {
          "description": "Free-text descriptions (used by LLM, ignored by heuristic).",
          "type": "string"
        },
        "point_estimate_rr": {
          "default": 1.5,
          "description": "Observed risk ratio; suggested E-values are scaled relative to this so the user can read \"to nullify a RR of X you'd need an unobserved RR of Y\".",
          "type": "number"
        },
        "treatment": {
          "description": "Free-text descriptions (used by LLM, ignored by heuristic).",
          "type": "string"
        }
      },
      "required": [
        "outcome",
        "treatment"
      ],
      "type": "object"
    },
    "name": "llm_unobserved_confounders"
  },
  {
    "description": "Linear-model forest (grf::lm_forest): covariate-varying coefficients h_k(x) in Y = c(x) + sum_k h_k(x) W_k, by forest-weighted local least squares of the centred outcome(s) on the centred regressors. Use for varying-coefficient models and continuous multi-dimensional treatments; for mutually exclusive arms use sp.multi_arm_forest. Validation: validated evidence tier (known-truth, reference, external-parity, or Monte Carlo artifact). Assumptions: the conditional model is linear in W given X. Pre-conditions: regressors vary conditionally on X. Failure modes: DataInsufficient: coefficient forest left rows without an out-of-bag prediction -> Increase n_estimators. Alternatives: sp.multi_arm_forest, sp.causal_forest. Typical minimum N: 500.",
    "input_schema": {
      "properties": {
        "W_hat": {
          "description": "Precomputed E[W|X], (n, K); default out-of-bag (multi-task) regression forest.",
          "type": "string"
        },
        "Y_hat": {
          "description": "Precomputed E[Y|X], (n, q); default out-of-bag (multi-task) regression forest.",
          "type": "string"
        },
        "ci_group_size": {
          "default": 2,
          "description": "Trees per little bag; 1 disables variance estimates.",
          "type": "integer"
        },
        "clusters": {
          "description": "Cluster ids: trees sample whole clusters; SEs are cluster-robust.",
          "type": "string"
        },
        "covariates": {
          "description": "The covariates ``X`` along which the coefficients vary.",
          "type": "string"
        },
        "equalize_cluster_weights": {
          "default": false,
          "description": "Give every cluster equal weight (incompatible with weights).",
          "type": "boolean"
        },
        "honest": {
          "default": true,
          "description": "grf honesty: grow on one half of the drawn sample, estimate leaves on the other.",
          "type": "boolean"
        },
        "honesty_fraction": {
          "default": 0.5,
          "description": "Share of the drawn sample used to grow the tree.",
          "type": "number"
        },
        "honesty_prune_leaves": {
          "default": true,
          "description": "Prune leaves left empty by the estimation half.",
          "type": "boolean"
        },
        "imbalance_penalty": {
          "default": 0.0,
          "description": "grf imbalance.penalty on unbalanced splits.",
          "type": "number"
        },
        "max_depth": {
          "description": "Optional depth cap (no grf analogue).",
          "type": "string"
        },
        "max_samples": {
          "default": 0.5,
          "description": "Fraction of clusters drawn per tree (grf sample.fraction); at most 0.5 when ci_group_size > 1.",
          "type": "number"
        },
        "min_samples_leaf": {
          "default": 5,
          "description": "grf min.node.size: nodes with at most this many growing samples are not split.",
          "type": "integer"
        },
        "mtry": {
          "description": "Candidate variables per split (grf default min(ceil(sqrt(p) + 20), p)).",
          "type": "string"
        },
        "n_estimators": {
          "default": 2000,
          "description": "Trees (grf num.trees).",
          "type": "integer"
        },
        "n_jobs": {
          "default": 1,
          "description": "Threads used to grow trees (-1 = all cores).",
          "type": "integer"
        },
        "random_state": {
          "default": 42,
          "description": "Seed; different seeds give independent forests.",
          "type": "integer"
        },
        "regressors": {
          "description": "The regressors ``W_1..W_K`` whose coefficients vary with ``x``.",
          "type": "string"
        },
        "split_alpha": {
          "default": 0.05,
          "description": "grf alpha: minimum share of the parent each child keeps.",
          "type": "number"
        },
        "stabilize_splits": {
          "default": false,
          "description": "Apply the causal-forest split constraints to every regressor.",
          "type": "boolean"
        },
        "weights": {
          "description": "Sample weights (grf sample.weights).",
          "type": "string"
        },
        "y": {
          "description": "Outcome(s); several outcomes are modelled jointly.",
          "type": "string"
        }
      },
      "required": [],
      "type": "object"
    },
    "name": "lm_forest"
  },
  {
    "description": "Estimate impulse responses via Jorda (2005) local projections. Validation: certified parity evidence. Assumptions: Outcome and shock series are (covariance-)stationary so horizon-h projections are not spurious; Shock is conditionally exogenous at t given controls and the auto-added lags (no contemporaneous feedback from y_t to shock_t); Newey-West truncation lag is adequate for the moving-average serial correlation induced by overlapping horizons. Pre-conditions: Single time-ordered series in a DataFrame with the outcome and shock columns; For identification='lpirfs_cholesky', endog_order contains the outcome and shock columns in the intended Cholesky ordering; Length comfortably exceeds horizons + max lag so the deepest horizon regression retains enough usable rows. Failure modes: Confidence bands explode or flip sign at long horizons after passing already-lagged controls together with auto_lag=True -> Set auto_lag=False for a bare specification, or drop your manual lags so collinear duplicate-lag columns are not formed; The horizon-0 response is zero when the shock is ordered after the response variable -> Use the intended endog_order, or switch back to identification='direct' i...",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for the confidence band.",
          "type": "number"
        },
        "auto_lag": {
          "default": true,
          "description": "If ``True`` (the legacy default), also adds ``y_{t-1}`` and ``shock_{t-1}`` as automatic regressors. Set ``False`` for a bare ``y_{t+h} ~ const + shock_t + controls`` specification. These two auto-controls were silent in the pre-1.16 docstring.",
          "type": "boolean"
        },
        "controls": {
          "description": "Additional regressors taken **verbatim** from ``data``: the column values at time t are used directly, without re-lagging. If you want the lag of a control, lag it yourself before passing it in (e.g. ``df[\"unemp_lag\"] = df[\"unemp\"].shift(1)`` and then ``controls=[\"unemp_lag\"]``). The pre-1.16 behaviour silently re-lagged controls a second time on top of an auto- added ``y_{t-1}``, producing collinear columns and surprising impulse responses; see ``MIGRATION.md`` for context.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "cumulative": {
          "default": false,
          "description": "If ``True``, return the cumulative response ``y_{t+h} - y_{t-1}``. Default (False) returns ``y_{t+h}`` directly.",
          "type": "boolean"
        },
        "endog_order": {
          "description": "Endogenous variable order used only when ``identification='lpirfs_cholesky'``. Defaults to ``[outcome, shock]``.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "horizons": {
          "default": 20,
          "description": "Number of horizons h = 0, 1, ..., H to estimate.",
          "type": "integer"
        },
        "identification": {
          "default": "direct",
          "description": "Shock-identification convention. ``'direct'`` uses the coefficient on the observed ``shock`` variable in each horizon regression. ``'lpirfs_cholesky'`` reproduces ``lpirfs::lp_lin`` with ``lags_endog_lin=1`` and ``shock_type=1``: the variables in ``endog_order`` define the Cholesky ordering, and the reported response is the unit structural shock for ``shock``.",
          "enum": [
            "direct",
            "lpirfs_cholesky"
          ],
          "type": "string"
        },
        "nw_lags": {
          "description": "Newey-West truncation lag. Defaults to ``round(1.5 * horizons)`` per Kilian & Kim (2011) recommendation.",
          "type": "integer"
        },
        "outcome": {
          "description": "Column name of the outcome variable y.",
          "type": "string"
        },
        "shock": {
          "description": "Column name of the shock / treatment variable.",
          "type": "string"
        }
      },
      "required": [
        "outcome",
        "shock"
      ],
      "type": "object"
    },
    "name": "local_projections"
  },
  {
    "description": "Logit (logistic) regression via maximum likelihood. Validation: certified parity evidence.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals.",
          "type": "number"
        },
        "at_values": {
          "description": "Variable values for ``marginal_effects='at'``.",
          "type": "object"
        },
        "cluster": {
          "description": "Column name for clustered standard errors.",
          "type": "string"
        },
        "formula": {
          "description": "Formula like ``\"y ~ x1 + x2\"``.",
          "type": "string"
        },
        "marginal_effects": {
          "description": "``'average'`` (AME), ``'mean'`` (MEM), or ``'at'`` (MER).",
          "type": "string"
        },
        "maxiter": {
          "default": 100,
          "description": "Maximum Newton-Raphson iterations.",
          "type": "integer"
        },
        "odds_ratio": {
          "default": false,
          "description": "Report odds ratios instead of log-odds coefficients.",
          "type": "boolean"
        },
        "robust": {
          "default": "nonrobust",
          "description": "``'nonrobust'`` for MLE SE, ``'hc1'`` / ``'robust'`` for sandwich SE.",
          "type": "string"
        },
        "tol": {
          "default": 1e-08,
          "description": "Convergence tolerance on log-likelihood change.",
          "type": "number"
        },
        "weights": {
          "description": "Column name for frequency/analytic weights.",
          "type": "string"
        },
        "x": {
          "description": "Regressor names (alternative to formula).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Dependent variable name (alternative to formula).",
          "type": "string"
        }
      },
      "required": [],
      "type": "object"
    },
    "name": "logit"
  },
  {
    "description": "Log-rank test for equality of survival distributions across groups. Validation: certified parity evidence.",
    "input_schema": {
      "properties": {
        "duration": {
          "description": "Column names.",
          "type": "string"
        },
        "event": {
          "description": "Column names.",
          "type": "string"
        },
        "group": {
          "description": "Column names.",
          "type": "string"
        }
      },
      "required": [
        "duration",
        "event",
        "group"
      ],
      "type": "object"
    },
    "name": "logrank_test"
  },
  {
    "description": "Love plot: dot plot of standardized mean differences before/after.",
    "input_schema": {
      "properties": {
        "ax": {
          "description": "ax parameter.",
          "type": "string"
        },
        "covariates": {
          "description": "Covariate columns.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "figsize": {
          "default": [
            7
          ],
          "description": "(width, height). Height defaults to 0.4 * n_covariates + 1.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "ps_method": {
          "default": "logit",
          "description": "PS estimation method for balance computation.",
          "type": "string"
        },
        "threshold": {
          "default": 0.1,
          "description": "SMD threshold for the vertical dashed line (default 0.1).",
          "type": "number"
        },
        "title": {
          "default": "Covariate Balance (Love Plot)",
          "description": "Plot title.",
          "type": "string"
        },
        "treatment": {
          "description": "Binary treatment column.",
          "type": "string"
        },
        "weights": {
          "description": "IPW or matching weights. If None, inverse-PS weights are computed.",
          "type": "string"
        }
      },
      "required": [],
      "type": "object"
    },
    "name": "love_plot"
  },
  {
    "description": "Local-Projections DiD (Dube-Girardi-Jorda-Taylor 2023). At each event-time horizon h in {-P, ..., H}, runs a separate OLS of Y_{t+h} - Y_{t-1} on the treatment change Deltad_{t} with time FE and cluster-robust SE, using 'not-yet-treated' or 'never-treated' units as controls. Event-study beta_h paths are returned in ``model_info['event_study']``. Validation: certified parity evidence. Assumptions: Parallel trends across event time (standard DiD); No anticipation within the pre-treatment horizon; SUTVA. Pre-conditions: long-format panel with consecutive integer time; treatment is binary 0/1 and time-varying; horizons feasible: enough periods for Y_{t-1} and Y_{t+H}. Failure modes: Horizon-0 n_obs is tiny because few units switch on in the clean-control window -> Widen clean_controls='never_treated' -> 'not_yet_treated' or shorten horizons; Placebo CIs don't cover zero -- parallel trends suspect -> Apply sp.honest_did to the event-study paths for Rambachan-Roth sensitivity bounds. Alternatives: sp.callaway_santanna, sp.sun_abraham, sp.did_imputation, sp.gardner_did. Typical minimum N: 100.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "clean_controls": {
          "default": "not_yet_treated",
          "description": "Control selection",
          "enum": [
            "not_yet_treated",
            "never_treated"
          ],
          "type": "string"
        },
        "cluster": {
          "description": "Cluster variable (defaults to unit)",
          "type": "string"
        },
        "controls": {
          "description": "Control-variable column names.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "horizons": {
          "default": [
            -3,
            5
          ],
          "description": "(min, max) event-time horizons to estimate",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "time": {
          "description": "Integer period (consecutive)",
          "type": "string"
        },
        "time_fe": {
          "default": true,
          "description": "time_fe parameter (bool).",
          "type": "boolean"
        },
        "treatment": {
          "description": "Binary time-varying treatment (0/1)",
          "type": "string"
        },
        "unit": {
          "description": "Unit identifier",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "time",
        "treatment",
        "unit",
        "y"
      ],
      "type": "object"
    },
    "name": "lp_did"
  },
  {
    "description": "Likelihood-ratio test comparing a *restricted* and a *full* model. Validation: certified parity evidence.",
    "input_schema": {
      "properties": {
        "boundary": {
          "description": "Whether to apply the chi2 boundary correction. When ``None`` (default) we infer it from whether the restriction touches a variance component -- the only parameters that live on the boundary of their support. ``boundary=False`` reproduces Stata's ``lrtest`` and R's ``anova()`` (naive chi2(df) tail; Stata prints a note that the test is conservative when the null is on the boundary). The degrees of freedom are the difference in the number of estimated parameters (Stata ``e(k)``), which counts every variance and covariance parameter.",
          "type": "boolean"
        },
        "full": {
          "description": "Two fitted mixed models. ``full`` should strictly nest ``restricted`` -- i.e. the parameter space of the restricted model is a subset of the full model's.",
          "type": "string"
        },
        "restricted": {
          "description": "Two fitted mixed models. ``full`` should strictly nest ``restricted`` -- i.e. the parameter space of the restricted model is a subset of the full model's.",
          "type": "string"
        }
      },
      "required": [
        "full",
        "restricted"
      ],
      "type": "object"
    },
    "name": "lrtest"
  },
  {
    "description": "Longitudinal TMLE for static regime contrasts. Validation: certified parity evidence. Assumptions: Sequential exchangeability (no unmeasured time-varying confounding) at every treatment time point; Positivity at each time point conditional on the past; Correctly specified outcome (Q) and treatment (g) models, though TMLE is doubly robust to one of them. Pre-conditions: Long-format data with time-varying treatment, confounders and the static regime contrast of interest. Failure modes: Near-positivity violation: estimated treatment probabilities approach 0 or 1, inflating variance -> Bound (truncate) the cumulative weights or use a marginal structural model with stabilised weights. Alternatives: sp.tmle, sp.g_computation, sp.msm. Typical minimum N: 500.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "baseline": {
          "description": "Baseline time-invariant covariates.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "censoring": {
          "description": "Censoring indicator column per time point (``1=observed``, ``0=censored``). If None, no censoring is modeled.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "covariates_time": {
          "description": "``covariates_time[k]`` lists time-k covariate columns (may be empty). Length ``K``.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "outcome_type": {
          "default": "auto",
          "description": "``auto`` detects from unique values of ``y``.",
          "enum": [
            "auto",
            "binary",
            "continuous"
          ],
          "type": "string"
        },
        "propensity_bounds": {
          "default": [
            0.01,
            1.0
          ],
          "description": "Bounds on the *cumulative* probability of following the regime (and remaining uncensored) up to each time point, ``prod_j g_j`` -- the ``gbounds`` of R ``ltmle``, whose default this is.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "regime_control": {
          "description": "Regimes to contrast. Default: all-1 vs all-0. A regime may also be a **callable** ``regime(k, history)`` for *dynamic regimes* that depend on the simulated / observed history of baseline and time-varying covariates. The callable receives ``k`` (int 0..K-1) and ``history`` -- a dict mapping column name to the length-``n`` numpy array observed up to that timepoint -- and must return a length-``n`` numpy array of 0/1 treatment assignments.",
          "enum": [
            "0",
            "1"
          ],
          "type": "string"
        },
        "regime_treated": {
          "description": "Regimes to contrast. Default: all-1 vs all-0. A regime may also be a **callable** ``regime(k, history)`` for *dynamic regimes* that depend on the simulated / observed history of baseline and time-varying covariates. The callable receives ``k`` (int 0..K-1) and ``history`` -- a dict mapping column name to the length-``n`` numpy array observed up to that timepoint -- and must return a length-``n`` numpy array of 0/1 treatment assignments.",
          "enum": [
            "0",
            "1"
          ],
          "type": "string"
        },
        "treatments": {
          "description": "Treatment column per time point, length ``K``.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Final outcome column.",
          "type": "string"
        }
      },
      "required": [
        "covariates_time",
        "treatments",
        "y"
      ],
      "type": "object"
    },
    "name": "ltmle"
  },
  {
    "description": "LTMLE for a discrete-time survival outcome under dynamic regimes. Validation: validated evidence tier (known-truth, reference, external-parity, or Monte Carlo artifact). Assumptions: Unconfoundedness (sequential exchangeability for longitudinal/LTMLE); Positivity / overlap of treatment given history; At least one nuisance (outcome or treatment) is estimated consistently; the targeting step gives double robustness. Pre-conditions: Covariates, treatment, and outcome (for survival/longitudinal variants: time-to-event and time-varying covariates); Enough data to fit a Super Learner / HAL nuisance library. Failure modes: Near-positivity violations create extreme clever-covariate weights -> Truncate weights, restrict the estimand, or report a positivity diagnostic. Alternatives: sp.dml, sp.ipw, sp.g_computation. Typical minimum N: 400.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "baseline": {
          "description": "Time-invariant baseline covariates.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "censoring": {
          "description": "``1`` if the subject is observed *through* interval k, ``0`` if right-censored at or before k. If omitted, no censoring.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "covariates_time": {
          "description": "Time-varying covariates at each interval.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "event_indicators": {
          "description": "Column names for the per-interval event indicator ``T_k`` (``1`` if the event occurs *in* interval k, ``0`` otherwise).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "propensity_bounds": {
          "default": [
            0.01,
            0.99
          ],
          "description": "propensity_bounds parameter (Tuple[float, float]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "regime_control": {
          "description": "Treatment regimes, same semantics as :func:`ltmle`.",
          "type": "string"
        },
        "regime_treated": {
          "description": "Treatment regimes, same semantics as :func:`ltmle`.",
          "type": "string"
        },
        "treatments": {
          "description": "Treatment column per interval.",
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "required": [
        "covariates_time",
        "event_indicators",
        "treatments"
      ],
      "type": "object"
    },
    "name": "ltmle_survival"
  },
  {
    "description": "Machado-Mata (2005) quantile decomposition. Validation: validated evidence tier (known-truth, reference, external-parity, or Monte Carlo artifact).",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "group": {
          "description": "Group or cohort identifier.",
          "type": "string"
        },
        "inference": {
          "default": "none",
          "description": "inference parameter (str).",
          "type": "string"
        },
        "n_boot": {
          "default": 199,
          "description": "Number of bootstrap replications.",
          "type": "integer"
        },
        "n_sim": {
          "default": 500,
          "description": "Number of sim.",
          "type": "integer"
        },
        "n_tau_qr": {
          "default": 100,
          "description": "Number ``J`` of quantile regressions, fitted at the midpoints ``(j - 0.5)/J``; each simulated draw picks one of them uniformly, so the draws sample the uniform on (0, 1) at resolution ``1/J`` (Machado & Mata draw ``u ~ U(0, 1)`` directly; with this grid, as ``n_sim -> inf`` the decomposition converges to :func:`melly_decompose` on the same ``J``). Changed in 1.29.0 from 99 regressions on ``linspace(0.01, 0.99, 99)``.",
          "type": "integer"
        },
        "reference": {
          "default": 0,
          "description": "0: use Group A's coefficients with Group B's X. The counterfactual is F_{Y<0|1>} -- A's beta on B's X. 1: use Group B's coefficients with Group A's X. .. warning:: Opposite convention to ``dfl_decompose``. In DFL, ``reference=0`` means *A's X, B's beta* (reweighting). Here, ``reference=0`` means *A's beta, B's X* (coefficient swap). See ``dfl_decompose`` docstring for the full convention map.",
          "enum": [
            "0",
            "1"
          ],
          "type": "integer"
        },
        "seed": {
          "default": 12345,
          "description": "Random seed for reproducible stochastic steps.",
          "type": "integer"
        },
        "tau_grid": {
          "description": "tau grid for reporting (default: deciles 0.1..0.9)",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "x": {
          "description": "Primary running variable, regressor, or feature input for this estimator.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "group",
        "x",
        "y"
      ],
      "type": "object"
    },
    "name": "machado_mata"
  },
  {
    "description": "Compute Manski (1990) worst-case bounds on ATE. Validation: certified parity evidence. Assumptions: Only weak (set-identifying) assumptions are imposed; the result is an interval, not a point; Lee bounds add monotonicity of selection; Oster's delta adds proportional selection on observed vs. unobserved. Pre-conditions: The data needed for the point-identifying analysis, plus the weakest credible identifying restriction; For Lee bounds: a binary selection/attrition indicator. Failure modes: Bounds are too wide to be informative -> Add a credible auxiliary restriction (monotone treatment response, instrument) to tighten the bounds. Alternatives: sp.oster_delta, sp.lee_bounds, sp.manski_bounds. Typical minimum N: 100.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "assumption": {
          "default": "none",
          "description": "Additional assumption: - 'none': no assumptions (widest bounds) - 'mtr': Monotone Treatment Response (Y(1) >= Y(0) for all): ``[0, worst-case upper]``. - 'mts': Monotone Treatment Selection with positive selection, ``E[Y(d) | D=1] >= E[Y(d) | D=0]`` (Manski & Pepper 2000): ``[worst-case lower, E[Y|D=1] - E[Y|D=0]]``. - 'mts_mtr': MTS and MTR jointly: ``[0, E[Y|D=1] - E[Y|D=0]]``. Raises ``ValueError`` when ``E[Y|D=1] < E[Y|D=0]``: the two assumptions are then jointly refuted and the identified set is empty. Before 1.29, ``'mts'`` returned the ``'mts_mtr'`` interval (i.e. silently imposed MTR as well) and, when the naive difference was negative, swapped its endpoints instead of reporting the empty set. These match the \"worst case\", \"MTS positive selection\" and \"MTS and MTR positive selection\" rows of Stata's ``tebounds`` at zero misclassification.",
          "type": "string"
        },
        "n_bootstrap": {
          "default": 500,
          "description": "Number of bootstrap replications.",
          "type": "integer"
        },
        "random_state": {
          "default": 42,
          "description": "Random seed or RandomState for reproducible stochastic steps.",
          "type": "integer"
        },
        "treat": {
          "description": "Binary treatment variable (0/1).",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable.",
          "type": "string"
        },
        "y_lower": {
          "description": "Known lower bound of the outcome. If None, uses observed min.",
          "type": "number"
        },
        "y_upper": {
          "description": "Known upper bound of the outcome. If None, uses observed max.",
          "type": "number"
        }
      },
      "required": [
        "treat",
        "y"
      ],
      "type": "object"
    },
    "name": "manski_bounds"
  },
  {
    "description": "Compute marginal effects from a fitted model. Validation: certified parity evidence.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level.",
          "type": "number"
        },
        "at": {
          "description": "Fix covariates at specific values for conditional margins. E.g., ``{'age': 30, 'female': 1}``.",
          "type": "object"
        },
        "eps": {
          "default": 1e-05,
          "description": "Step size for numerical differentiation.",
          "type": "number"
        },
        "method": {
          "default": "ame",
          "description": "- 'ame': Average Marginal Effect (average dy/dx across all obs) - 'mem': Marginal Effect at the Mean (dy/dx at mean of X)",
          "type": "string"
        },
        "result": {
          "description": "Fitted model result (must have ``.params`` and associated data).",
          "type": "string"
        },
        "variables": {
          "description": "Variables to compute dy/dx for. Default: all regressors.",
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "required": [
        "result"
      ],
      "type": "object"
    },
    "name": "margins"
  },
  {
    "description": "Compute predictive margins at specific covariate values. Validation: certified parity evidence.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals.",
          "type": "number"
        },
        "at": {
          "description": "Mapping of variable names to lists/arrays of values. If multiple variables are given, the Cartesian product of all value lists is used. Example:: at={\"experience\": [1, 5, 10], \"female\": [0, 1]} produces 6 grid points.",
          "type": "object"
        },
        "result": {
          "description": "Fitted model result (must have ``.params`` and associated data).",
          "type": "string"
        }
      },
      "required": [
        "at",
        "result"
      ],
      "type": "object"
    },
    "name": "margins_at"
  },
  {
    "description": "Plot predictive margins from ``margins_at()`` with confidence bands.",
    "input_schema": {
      "properties": {
        "ax": {
          "description": "ax parameter.",
          "type": "string"
        },
        "by": {
          "description": "Variable to produce separate lines for (legend grouping).",
          "type": "string"
        },
        "figsize": {
          "default": [
            8,
            5
          ],
          "description": "figsize parameter (Tuple[float, float]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "margins_at_df": {
          "description": "Output from ``margins_at()``.",
          "type": "string"
        },
        "palette": {
          "description": "Colours for each ``by`` group.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "title": {
          "description": "title parameter (Optional[str]).",
          "type": "string"
        },
        "x": {
          "description": "Variable to place on the x-axis. If *None*, inferred as the at-variable with the most unique values.",
          "type": "string"
        },
        "xlabel": {
          "description": "xlabel parameter (Optional[str]).",
          "type": "string"
        },
        "ylabel": {
          "default": "Predicted Value",
          "description": "ylabel parameter (str).",
          "type": "string"
        }
      },
      "required": [
        "margins_at_df"
      ],
      "type": "object"
    },
    "name": "margins_at_plot"
  },
  {
    "description": "Marginal-effects result that pipes straight into ``sp.regtable``.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Forwarded verbatim to :func:`margins`.",
          "type": "number"
        },
        "at": {
          "description": "Forwarded verbatim to :func:`margins`.",
          "type": "object"
        },
        "eps": {
          "default": 1e-05,
          "description": "Forwarded verbatim to :func:`margins`.",
          "type": "number"
        },
        "method": {
          "default": "ame",
          "description": "Forwarded verbatim to :func:`margins`.",
          "type": "string"
        },
        "result": {
          "description": "Fitted model -- same input ``sp.margins`` accepts.",
          "type": "string"
        },
        "variables": {
          "description": "Forwarded verbatim to :func:`margins`.",
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "required": [
        "result"
      ],
      "type": "object"
    },
    "name": "margins_table"
  },
  {
    "description": "Plot marginal effects with confidence intervals.",
    "input_schema": {
      "properties": {
        "ax": {
          "description": "ax parameter.",
          "type": "string"
        },
        "color": {
          "default": "#2C3E50",
          "description": "color parameter (str).",
          "type": "string"
        },
        "figsize": {
          "default": [
            8,
            5
          ],
          "description": "figsize parameter (Tuple[float, float]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "margins_df": {
          "description": "Output from ``margins()``.",
          "type": "string"
        },
        "title": {
          "description": "title parameter (Optional[str]).",
          "type": "string"
        }
      },
      "required": [
        "margins_df"
      ],
      "type": "object"
    },
    "name": "marginsplot"
  },
  {
    "description": "Propensity score and covariate matching for treatment effect estimation. Validation: certified evidence with scoped limitations. Known limitations: greedy nearest-neighbour matching without replacement is order-dependent: the m_order convention can differ across packages and materially moves the estimate (>5x spread on MatchIt::lalonde with Mahalanobis distance). m_order='data' and 'closest' reproduce MatchIt exactly; m_order='farthest' is StatsPAI's own dynamic rule and is not MatchIt-equivalent; bias_correction=True follows a different convention from Matching::Match's BiasAdjust: StatsPAI regresses on the full covariate vector with unweighted OLS over all controls, the reference regresses on the matching variables weighted by match counts, so bias-corrected estimates can differ from it by about 0.1%. The uncorrected estimate and its Abadie-Imbens standard error are exact. Cost: Builds the dense n_treated x n_control distance matrix via scipy cdist before selecting neighbours: memory is O(n_treated x n_control). Comfortable into the thousands per arm; use a caliper or coarser blocking beyond that. Assumptions: Unconfoundedness / CIA: Y(d) perp D | X; Overlap / common support: tr...",
    "input_schema": {
      "properties": {
        "bootstrap_reps": {
          "default": 200,
          "description": "Replications for se_method='bootstrap'",
          "type": "integer"
        },
        "bootstrap_seed": {
          "description": "Seed for the bootstrap resampler",
          "type": "integer"
        },
        "caliper_scale": {
          "default": "raw",
          "description": "Units of `caliper`: 'raw' on the distance scale (Stata psmatch2) or 'sd' in standard deviations of the propensity score (MatchIt std.caliper=TRUE).",
          "enum": [
            "raw",
            "sd"
          ],
          "type": "string"
        },
        "covariates": {
          "description": "Covariate matrix, DataFrame, or column names.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "llr_stata_compat": {
          "default": false,
          "description": "method='llr' only: reproduce Stata psmatch2's SUBSTITUTE for LLR (lpoly-smoothed outcome + nearest-neighbour matching) rather than genuine local linear regression.",
          "type": "boolean"
        },
        "m_order": {
          "default": "smallest_min_dist",
          "description": "Order treated units are processed in for greedy matching without replacement. The result depends on it materially (>5x spread on lalonde with Mahalanobis distance). 'data' and 'closest' reproduce the MatchIt rules of the same name.",
          "enum": [
            "smallest_min_dist",
            "data",
            "closest",
            "farthest",
            "smallest",
            "largest"
          ],
          "type": "string"
        },
        "mahalanobis_cov": {
          "default": "pooled",
          "description": "Covariance defining the Mahalanobis metric: 'pooled' within-group (Rubin 1980, MatchIt) or 'total' full-sample (pre-1.21 behaviour).",
          "enum": [
            "pooled",
            "total"
          ],
          "type": "string"
        },
        "method": {
          "default": "nearest",
          "description": "Matching method. 'caliper' is not a method -- pass caliper= to any nearest-neighbour variant.",
          "enum": [
            "nearest",
            "psm",
            "mahalanobis",
            "kernel",
            "radius",
            "llr",
            "stratify",
            "cem",
            "optimal",
            "genmatch",
            "cardinality",
            "ebalance",
            "cbps",
            "sbw",
            "overlap"
          ],
          "type": "string"
        },
        "outcome": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        },
        "se_method": {
          "default": "auto",
          "description": "Standard error. 'auto' resolves to 'abadie_imbens' for nearest-neighbour, 'psmatch2' for kernel / radius, 'bootstrap' for llr. 'abadie_imbens' is the sample-ATT conditional variance (Stata psmatch2 ai()) and the only option measured to be correctly sized (0.95-1.04x the sampling SD, coverage 0.905-0.956 over 36 designs x 1000 reps; see benchmarks/matching_se_coverage.py). 'abadie_imbens_pop' is the population-ATT variance Matching::Match reports; 'psmatch2' the analytic Stata SE (1.50-1.69x, too wide); 'ai' the simple matched-pair SE (0.56-0.91x, never reaches nominal coverage); 'bootstrap' resamples within arm and re-estimates the propensity score each draw. 'abadie_imbens_2016' is the Abadie-Imbens (2016) estimated-propensity-score variance Stata teffects psmatch reports (population ATT; the score-estimation term can be negative).",
          "enum": [
            "auto",
            "ai",
            "psmatch2",
            "abadie_imbens",
            "abadie_imbens_pop",
            "abadie_imbens_2016",
            "bootstrap"
          ],
          "type": "string"
        },
        "tie_tolerance": {
          "default": 0.0,
          "description": "With ties='all', how close squared distances (scaled by the variance of the distance measure) must be to count as tied. 1e-5 reproduces Matching::Match's distance.tolerance default.",
          "type": "number"
        },
        "ties": {
          "default": "first",
          "description": "How equidistant controls are handled under matching with replacement. 'first' keeps the lowest-index one; 'all' pools them and splits the weight (the Matching::Match convention, which removes the row-order dependence).",
          "enum": [
            "first",
            "all"
          ],
          "type": "string"
        },
        "treatment": {
          "description": "Treatment indicator, treatment variable, or treatment array.",
          "type": "string"
        }
      },
      "required": [
        "covariates",
        "outcome",
        "treatment"
      ],
      "type": "object"
    },
    "name": "match"
  },
  {
    "description": "Matrix-completion causal panel estimator (Athey et al., 2021). Validation: validated evidence tier (known-truth, reference, external-parity, or Monte Carlo artifact).",
    "input_schema": {
      "properties": {
        "d": {
          "description": "d parameter (str).",
          "type": "string"
        },
        "time": {
          "description": "Time period column.",
          "type": "string"
        },
        "unit": {
          "description": "Unit identifier column.",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "d",
        "time",
        "unit",
        "y"
      ],
      "type": "object"
    },
    "name": "matrix_completion"
  },
  {
    "description": "Estimate treatment effects using matrix completion. Validation: certified parity evidence. Do NOT use when: the panel is nearly fully treated -- matrix completion needs a substantial observed-control block to recover the low-rank structure. Cost: Iterative soft-impute: one SVD of the N x T outcome matrix per iteration, i.e. O(max_iter x N x T x min(N,T)). n_bootstrap multiplies the whole loop -- this is the dominant cost on wide panels. Assumptions: The relationship between the treated unit and controls is stable absent the intervention (causal_impact); Matrix-completion: the untreated potential outcomes follow a low-rank factor structure with treatment as the missingness pattern; No concurrent intervention affects the controls. Pre-conditions: A treated unit with a pre-period and a set of control series, or a panel with a low-rank structure. Failure modes: Pre-period fit is poor or controls are themselves affected by the intervention -> Re-select controls, lengthen the pre-period, or use synthetic-control / DiD diagnostics. Alternatives: sp.synth, sp.sdid, sp.gsynth. Typical minimum N: 50.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level.",
          "type": "number"
        },
        "fixed_effects": {
          "default": "two-way",
          "description": "Unpenalised additive effects fitted alongside the low-rank component. ``\"two-way\"`` is the Athey et al. (2021) estimator and the default of R ``MCPanel`` (``to_estimate_u = to_estimate_v = TRUE``) and ``fect`` (``force = \"two-way\"``). ``\"none\"`` is pure soft-impute (``MCPanel`` with both switches off; no intercept, so the outcome level is shrunk towards zero); it is the estimator this function computed in StatsPAI <= 1.28.0.",
          "enum": [
            "two-way",
            "unit",
            "time"
          ],
          "type": "string"
        },
        "lambda_reg": {
          "description": "Singular-value threshold ``lambda`` on the objective above (the ``1/2``-scaled squared loss). The same minimiser is obtained from R ``MCPanel::mcnnm_fit(lambda_L = 2 * lambda / |Omega|)`` and from ``fect(method = \"mc\", CV = FALSE, lambda = lambda / (N * T))``; both conversions are reported in ``model_info``. If None, a heuristic (no reference implementation uses it) is applied: ``lambda = sd(Y[Omega]) * sqrt(max(N, T)) / 10``.",
          "type": "number"
        },
        "max_iter": {
          "default": 5000,
          "description": "Maximum soft-impute iterations. A ``RuntimeWarning`` is raised if the point-estimate fit hits the cap before ``tol``.",
          "type": "integer"
        },
        "max_rank": {
          "description": "Hard cap on the rank of the low-rank component (not part of the reference problem). If None, no constraint.",
          "type": "integer"
        },
        "n_bootstrap": {
          "default": 200,
          "description": "Unit-bootstrap replications for the standard error.",
          "type": "integer"
        },
        "random_state": {
          "default": 42,
          "description": "Random seed or RandomState for reproducible stochastic steps.",
          "type": "integer"
        },
        "time": {
          "description": "Time period variable.",
          "type": "string"
        },
        "tol": {
          "default": 1e-10,
          "description": "Convergence tolerance on the relative Frobenius change of the fitted counterfactual matrix.",
          "type": "number"
        },
        "treat": {
          "description": "Binary treatment indicator (0/1). Can be staggered.",
          "type": "string"
        },
        "unit": {
          "description": "Unit identifier variable.",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable.",
          "type": "string"
        }
      },
      "required": [
        "time",
        "treat",
        "unit",
        "y"
      ],
      "type": "object"
    },
    "name": "mc_panel"
  },
  {
    "description": "Matrix Completion Synthetic Control Method. Validation: certified parity evidence. Assumptions: A convex (or regularized) combination of donor units reproduces the treated unit's pre-treatment outcome path; No interference: the treatment does not affect the donor units (SUTVA); No anticipation before the treatment date. Pre-conditions: Panel of one or more treated units plus an untreated donor pool, observed over time; Pre-treatment window long enough to fit donor weights (rule of thumb: more pre-periods than donors used); Outcome observed for every unit in every period. Failure modes: Large pre-treatment RMSPE -- the synthetic unit fails to track the treated unit before treatment -> Add donors / predictors, lengthen the pre-period, or use a bias-corrected estimator (sdid, augsynth); Placebo / permutation inference shows the estimate is not extreme relative to donors -> Report the placebo distribution honestly; the effect may not be distinguishable from noise. Alternatives: sp.sdid, sp.augsynth, sp.gsynth, sp.callaway_santanna. Typical minimum N: 15.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals.",
          "type": "number"
        },
        "covariates": {
          "description": "Time-varying covariates to partial out before matrix completion.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "cv_folds": {
          "default": 5,
          "description": "Number of CV folds for automatic lambda selection.",
          "type": "integer"
        },
        "fixed_effects": {
          "default": "two-way",
          "description": "Unpenalised additive effects fitted with the low-rank part. ``\"two-way\"`` is the Athey et al. (2021) estimator and the default of R ``MCPanel`` and ``fect``; ``\"none\"`` is pure soft-impute (the estimator of StatsPAI <= 1.28.0), which shrinks the outcome level towards zero.",
          "enum": [
            "two-way",
            "unit",
            "time"
          ],
          "type": "string"
        },
        "lambda_reg": {
          "description": "Singular-value threshold on the ``1/2``-scaled squared loss. The same minimiser is R ``MCPanel::mcnnm_fit(lambda_L = 2 * lambda / n_observed)`` and ``fect(method = \"mc\", CV = FALSE, lambda = lambda / (N * T))`` (both reported in ``model_info``). If ``None`` (default), selected by this function's own random-entry K-fold cross-validation (not the ``MCPanel`` / ``fect`` CV rule).",
          "type": "number"
        },
        "max_iter": {
          "default": 5000,
          "description": "Maximum Soft-Impute iterations (``RuntimeWarning`` if hit).",
          "type": "integer"
        },
        "outcome": {
          "description": "Outcome variable name.",
          "type": "string"
        },
        "placebo": {
          "default": true,
          "description": "Run placebo (permutation) inference by treating each control unit as if it were treated.",
          "type": "boolean"
        },
        "seed": {
          "description": "Random seed for the CV fold assignment. With ``seed=None`` and ``lambda_reg=None`` the selected lambda (hence the estimate) can differ between calls.",
          "type": "integer"
        },
        "time": {
          "description": "Time period column.",
          "type": "string"
        },
        "tol": {
          "default": 1e-10,
          "description": "Convergence tolerance (relative change in Frobenius norm of the fitted matrix).",
          "type": "number"
        },
        "treated_unit": {
          "description": "Identifier of the treated unit.",
          "type": "string"
        },
        "treatment_time": {
          "description": "First treatment period (inclusive).",
          "type": "string"
        },
        "unit": {
          "description": "Unit identifier column.",
          "type": "string"
        }
      },
      "required": [
        "outcome",
        "time",
        "treated_unit",
        "treatment_time",
        "unit"
      ],
      "type": "object"
    },
    "name": "mc_synth"
  },
  {
    "description": "McCrary (2008) density test for manipulation of the running variable at the cutoff in regression-discontinuity designs. A significant discontinuity in the density of x at c is direct evidence that units are sorting around the cutoff (e.g. test-taking strategy, income manipulation), invalidating local randomisation. Validation: certified parity evidence. Assumptions: Smooth density of x at c under the null of no manipulation; Local-linear density estimator captures the shape near c. Pre-conditions: x is continuous with mass on both sides of c; no extreme heaping at c (rounded data invalidates the local-linear density estimate). Failure modes: Test rejects (p < alpha) -- manipulation evidence -> Switch to donut-hole RD (sp.rdrobust(donut=delta)) or partial-identification bounds (sp.rdrbounds); Heaped data near c (e.g. integer-rounded scores) -> The density-test statistic is unreliable on heaped data; consider Frandsen (2017) integer-RD adjustment. Alternatives: sp.rddensity, sp.rdrbounds. Typical minimum N: 200.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "bin_width": {
          "description": "Histogram bin width (``DCdensity``'s ``bin``). Default: ``2 sd(x) n^(-1/2)``.",
          "type": "string"
        },
        "bw": {
          "description": "Bandwidth; auto if None",
          "type": "number"
        },
        "c": {
          "default": 0.0,
          "description": "Cutoff value",
          "type": "number"
        },
        "n_bins": {
          "description": "Histogram bins; auto if None",
          "type": "integer"
        },
        "x": {
          "description": "Running variable",
          "type": "string"
        }
      },
      "required": [
        "x"
      ],
      "type": "object"
    },
    "name": "mccrary_test"
  },
  {
    "description": "Mediation analysis (Imai-Keele-Tingley 2010). Decomposes the total effect into natural direct effect (NDE) and natural indirect effect (NIE) via an interventional or sequential-ignorability identification strategy. Validation: certified parity evidence. Assumptions: Sequential ignorability: (Y(t,m), M(t)) perp T | X; Y(t,m) perp M | T, X; No post-treatment confounder of the mediator-outcome relationship (classical Imai-Keele-Tingley); SUTVA on both mediator and outcome. Pre-conditions: binary treatment 0/1; mediator is a post-treatment variable causally between treat and y; pre-treatment covariates capture confounding for T-Y, M-Y, T-M. Failure modes: NDE + NIE do not sum to total effect (difference vs product decomposition) -> Nonlinear / interactive mediator model -- use sp.mediate_interventional or four-way decomposition; Sensitivity to unobserved T-M / M-Y confounder unknown -> Always report sp.mediate_sensitivity (Imai-Keele-Yamamoto rho bound); Post-treatment confounder L suspected -> Use sp.four_way_decomposition (VanderWeele 2014) which handles L. Alternatives: sp.mediate_sensitivity, sp.mediate_interventional, sp.four_way_decomposition, sp.proximal. Typical minimum N: 500.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "covariates": {
          "description": "Pre-treatment confounders",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "mediator": {
          "description": "Mediator variable",
          "type": "string"
        },
        "n_boot": {
          "default": 1000,
          "description": "Bootstrap reps for NDE/NIE CIs",
          "type": "integer"
        },
        "treat": {
          "description": "Binary treatment",
          "type": "string"
        },
        "y": {
          "description": "Outcome",
          "type": "string"
        }
      },
      "required": [
        "mediator",
        "treat",
        "y"
      ],
      "type": "object"
    },
    "name": "mediate"
  },
  {
    "description": "Interventional (in)direct effects (VanderWeele, Vansteelandt, Robins 2014). Identifies mediation effects in the presence of treatment-induced mediator-outcome confounders where natural (in)direct effects are not identified. Validation: certified parity evidence. Assumptions: Sequential exchangeability / no unmeasured confounding at each time point; Positivity: every treatment level is possible given the past; Correct specification of the treatment and/or outcome models. Pre-conditions: Sequentially measured covariates, (time-varying) treatment, and outcome; Models for the treatment process and the outcome (or weights). Failure modes: Stabilized weights have extreme values (positivity near-violation) -> Truncate weights, simplify the treatment model, or use a doubly-robust estimator (TMLE). Alternatives: sp.tmle, sp.g_computation, sp.ipw. Typical minimum N: 300.",
    "input_schema": {
      "properties": {
        "covariates": {
          "description": "Baseline covariates",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "mediator": {
          "description": "Mediator variable",
          "type": "string"
        },
        "treat": {
          "description": "Binary treatment",
          "type": "string"
        },
        "tv_confounders": {
          "description": "Treatment-induced M-Y confounders",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Outcome",
          "type": "string"
        }
      },
      "required": [
        "mediator",
        "treat",
        "y"
      ],
      "type": "object"
    },
    "name": "mediate_interventional"
  },
  {
    "description": "Causal-mediation analysis -- article-facing alias for Validation: certified parity evidence.",
    "input_schema": {
      "properties": {
        "X": {
          "description": "Feature matrix or covariate DataFrame.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "d": {
          "description": "d parameter (str).",
          "type": "string"
        },
        "m": {
          "description": "m parameter (str).",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "d",
        "m",
        "y"
      ],
      "type": "object"
    },
    "name": "mediation"
  },
  {
    "description": "Linear nested-models mediation decomposition (VanderWeele 2014 Validation: certified parity evidence.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "covariates": {
          "description": "Covariate matrix, DataFrame, or column names.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "inference": {
          "default": "analytical",
          "description": "``'none'``: point estimates only. ``'analytical'`` (default): delta-method standard errors from the two regressions' classical OLS covariances (``s^2 (X'X)^{-1}``, ``s^2`` on ``n - p``), block-diagonal across the two models -- Stata ``paramed`` / R ``CMAverse`` ``inference = \"delta\"``. Up to 1.28.0 this option computed nothing and ``se`` was ``None``.",
          "enum": [
            "analytical",
            "bootstrap"
          ],
          "type": "string"
        },
        "mediator": {
          "description": "mediator parameter (str).",
          "type": "string"
        },
        "n_boot": {
          "default": 299,
          "description": "Number of bootstrap replications.",
          "type": "integer"
        },
        "seed": {
          "default": 12345,
          "description": "Random seed for reproducible stochastic steps.",
          "type": "integer"
        },
        "treatment": {
          "description": "Treatment indicator, treatment variable, or treatment array.",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "mediator",
        "treatment",
        "y"
      ],
      "type": "object"
    },
    "name": "mediation_decompose"
  },
  {
    "description": "Random-effects Gamma GLMM with log link (Stata ``meglm`` ``family(gamma)``). Validation: certified parity evidence.",
    "input_schema": {
      "properties": {
        "group": {
          "description": "Group or cohort identifier.",
          "type": "string"
        },
        "nAGQ": {
          "default": 1,
          "description": "nAGQ parameter (int).",
          "type": "integer"
        },
        "offset": {
          "description": "Offset term or offset column.",
          "type": "string"
        },
        "x_fixed": {
          "description": "x_fixed parameter (Sequence[str]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "x_random": {
          "description": "x_random parameter (Optional[Sequence[str]]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "group",
        "x_fixed",
        "y"
      ],
      "type": "object"
    },
    "name": "megamma"
  },
  {
    "description": "Fit a generalised linear mixed model. Validation: certified parity evidence.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Optimisation controls / CI width. ``maxiter`` / ``tol`` drive the L-BFGS-B stage; for AGHQ (``nAGQ > 1``) the default budget is internally tightened to ``maxiter=5000`` and ``tol=1e-12`` (explicit user-supplied controls are respected). Either way the optimum is then finished with full Newton steps on the numerical gradient and Hessian, so the reported estimates sit at the optimum to ~1e-9 rather than at L-BFGS-B's relative-function-change stopping point (which on these likelihoods is ~1e-4 away).",
          "type": "number"
        },
        "cov_type": {
          "default": "unstructured",
          "description": "Random-effect covariance: ``'unstructured'`` (default), ``'diagonal'``, ``'identity'``.",
          "type": "string"
        },
        "curvature": {
          "default": "observed",
          "description": "Curvature of the log integrand used by the Laplace approximation and to scale the AGHQ nodes. ``'observed'`` (default) is the second derivative of the conditional log-likelihood at the mode -- the Laplace approximation proper, and what Stata ``me*`` (``intmethod(laplace)`` / ``mcaghermite``), ``glmmTMB`` and ``ordinal::clmm`` compute. ``'expected'`` substitutes the Fisher information (the PIRLS weights of ``lme4::glmer`` / ``glmer.nb``). The two coincide for canonical links (Gaussian, binomial-logit, Poisson-log) and differ for gamma-log and NB-2-log, where they are two different approximations of the same integral.",
          "type": "string"
        },
        "family": {
          "default": "gaussian",
          "description": "``'gaussian'``, ``'binomial'``, ``'poisson'``, ``'gamma'``, or ``'nbinomial'`` (alias ``'negbin'``).",
          "type": "string"
        },
        "group": {
          "description": "Grouping variable for random effects.",
          "type": "string"
        },
        "maxiter": {
          "default": 300,
          "description": "Optimisation controls / CI width. ``maxiter`` / ``tol`` drive the L-BFGS-B stage; for AGHQ (``nAGQ > 1``) the default budget is internally tightened to ``maxiter=5000`` and ``tol=1e-12`` (explicit user-supplied controls are respected). Either way the optimum is then finished with full Newton steps on the numerical gradient and Hessian, so the reported estimates sit at the optimum to ~1e-9 rather than at L-BFGS-B's relative-function-change stopping point (which on these likelihoods is ~1e-4 away).",
          "type": "integer"
        },
        "nAGQ": {
          "default": 1,
          "description": "Number of adaptive Gauss-Hermite quadrature points per scalar random effect. ``1`` (default) == Laplace approximation. Use ``nAGQ=7`` to match Stata ``meglm intpoints(7)``; values ``> 1`` require a single scalar random effect (no random slopes).",
          "type": "integer"
        },
        "offset": {
          "description": "Column of fixed offsets added to the linear predictor (e.g. ``log(exposure)`` for Poisson rate models).",
          "type": "string"
        },
        "tol": {
          "default": 1e-08,
          "description": "Optimisation controls / CI width. ``maxiter`` / ``tol`` drive the L-BFGS-B stage; for AGHQ (``nAGQ > 1``) the default budget is internally tightened to ``maxiter=5000`` and ``tol=1e-12`` (explicit user-supplied controls are respected). Either way the optimum is then finished with full Newton steps on the numerical gradient and Hessian, so the reported estimates sit at the optimum to ~1e-9 rather than at L-BFGS-B's relative-function-change stopping point (which on these likelihoods is ~1e-4 away).",
          "type": "number"
        },
        "trials": {
          "description": "Column of trial counts for binomial responses. Defaults to 1 (Bernoulli).",
          "type": "string"
        },
        "x_fixed": {
          "description": "Fixed-effect regressors (intercept added automatically).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "x_random": {
          "description": "Random-slope variables; defaults to random intercept only.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Outcome column. For binomial models this is the number of successes; pair it with ``trials=`` to model proportions.",
          "type": "string"
        }
      },
      "required": [
        "group",
        "x_fixed",
        "y"
      ],
      "type": "object"
    },
    "name": "meglm"
  },
  {
    "description": "Melly (2005) quantile decomposition. Validation: certified parity evidence.",
    "input_schema": {
      "properties": {
        "group": {
          "description": "Group or cohort identifier.",
          "type": "string"
        },
        "n_tau_qr": {
          "default": 100,
          "description": "Number ``J`` of quantile regressions in the process, fitted at the midpoints ``(j - 0.5)/J`` (the grid of Stata ``cdeco``). Changed in 1.29.0 from 99 regressions on ``linspace(0.01, 0.99, 99)``, which dropped the outer 0.5% of each conditional tail.",
          "type": "integer"
        },
        "reference": {
          "default": 0,
          "description": "Same convention as ``machado_mata``: ``reference=0`` uses A's beta on B's X (coefficient-swap counterfactual F_{Y<0|1>}), opposite to ``dfl_decompose`` whose ``reference=0`` uses A's X with B's beta.",
          "enum": [
            "0",
            "1"
          ],
          "type": "integer"
        },
        "tau_grid": {
          "description": "Grid of tau values to evaluate.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "x": {
          "description": "Primary running variable, regressor, or feature input for this estimator.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "group",
        "x",
        "y"
      ],
      "type": "object"
    },
    "name": "melly_decompose"
  },
  {
    "description": "Random-effects logistic regression (Stata ``melogit``). Validation: certified parity evidence. Assumptions: Binary outcome with logit link conditional on random effects; Cluster-level random intercepts (and slopes) are normally distributed; Random effects independent of covariates (no correlated-effects endogeneity). Pre-conditions: Binary (0/1) outcome; Grouping variable for the random effects. Failure modes: Adaptive quadrature likelihood does not converge -> Increase quadrature points or simplify the random-effects structure to a single intercept; Estimated random-effect variance near zero (no clustering) -> Drop the random effect and fit ordinary logit; Perfect separation in a sparse cluster -> Collapse sparse categories or add a weak penalty/prior. Alternatives: sp.mixed, sp.regress. Typical minimum N: 300.",
    "input_schema": {
      "properties": {
        "group": {
          "description": "Group or cohort identifier.",
          "type": "string"
        },
        "nAGQ": {
          "default": 1,
          "description": "nAGQ parameter (int).",
          "type": "integer"
        },
        "trials": {
          "description": "trials parameter (Optional[str]).",
          "type": "string"
        },
        "x_fixed": {
          "description": "x_fixed parameter (Sequence[str]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "x_random": {
          "description": "x_random parameter (Optional[Sequence[str]]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "group",
        "x_fixed",
        "y"
      ],
      "type": "object"
    },
    "name": "melogit"
  },
  {
    "description": "Random-effects negative-binomial regression (Stata ``menbreg``). Validation: certified parity evidence.",
    "input_schema": {
      "properties": {
        "group": {
          "description": "Group or cohort identifier.",
          "type": "string"
        },
        "nAGQ": {
          "default": 1,
          "description": "nAGQ parameter (int).",
          "type": "integer"
        },
        "offset": {
          "description": "Offset term or offset column.",
          "type": "string"
        },
        "x_fixed": {
          "description": "x_fixed parameter (Sequence[str]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "x_random": {
          "description": "x_random parameter (Optional[Sequence[str]]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "group",
        "x_fixed",
        "y"
      ],
      "type": "object"
    },
    "name": "menbreg"
  },
  {
    "description": "Random-effects ordinal logit (Stata ``meologit``, R ``ordinal::clmm``). Validation: certified parity evidence.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "cov_type": {
          "default": "unstructured",
          "description": "Covariance estimator type.",
          "type": "string"
        },
        "curvature": {
          "default": "observed",
          "description": "curvature parameter (str).",
          "type": "string"
        },
        "group": {
          "description": "Group or cohort identifier.",
          "type": "string"
        },
        "maxiter": {
          "default": 300,
          "description": "maxiter parameter (int).",
          "type": "integer"
        },
        "nAGQ": {
          "default": 1,
          "description": "nAGQ parameter (int).",
          "type": "integer"
        },
        "offset": {
          "description": "Offset term or offset column.",
          "type": "string"
        },
        "tol": {
          "default": 1e-06,
          "description": "Numerical convergence tolerance.",
          "type": "number"
        },
        "x_fixed": {
          "description": "x_fixed parameter (Sequence[str]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "x_random": {
          "description": "x_random parameter (Optional[Sequence[str]]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "group",
        "x_fixed",
        "y"
      ],
      "type": "object"
    },
    "name": "meologit"
  },
  {
    "description": "Random-effects Poisson regression (Stata ``mepoisson``). Validation: certified parity evidence.",
    "input_schema": {
      "properties": {
        "group": {
          "description": "Group or cohort identifier.",
          "type": "string"
        },
        "nAGQ": {
          "default": 1,
          "description": "nAGQ parameter (int).",
          "type": "integer"
        },
        "offset": {
          "description": "Offset term or offset column.",
          "type": "string"
        },
        "x_fixed": {
          "description": "x_fixed parameter (Sequence[str]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "x_random": {
          "description": "x_random parameter (Optional[Sequence[str]]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "group",
        "x_fixed",
        "y"
      ],
      "type": "object"
    },
    "name": "mepoisson"
  },
  {
    "description": "Summary-data meta-analysis with fixed- and random-effects pooling. Validation: certified parity evidence.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence/prediction intervals.",
          "type": "number"
        },
        "effects": {
          "description": "Per-study effect sizes (e.g. log odds ratios, mean differences).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "labels": {
          "description": "Study labels for the forest plot.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "method": {
          "default": "DL",
          "description": "Which model the headline ``estimate`` reports: DerSimonian-Laird random effects (default) or fixed-effect inverse-variance. Both are always computed and available on the result.",
          "enum": [
            "DL",
            "fixed"
          ],
          "type": "string"
        },
        "se": {
          "description": "Per-study standard errors (must be positive).",
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "required": [
        "effects",
        "se"
      ],
      "type": "object"
    },
    "name": "meta_analysis"
  },
  {
    "description": "Meta-learner framework for CATE: S-, T-, X-, R-, DR-Learner. Validation: certified parity evidence. Assumptions: Unconfoundedness: Y(d) perp D | X; Overlap: 0 < P(D=1 | X) < 1; For R-Learner / DR-Learner: orthogonality between treatment residual and outcome residual. Pre-conditions: binary treatment (0/1); covariates numeric; categoricals encoded; enough treated AND control to train separate outcome models (T/X/DR-Learner). Failure modes: Large divergence across learner types -> Use sp.compare_metalearners to identify which learner is biased; DR-Learner is safest under model misspecification; S-Learner estimates near zero regardless of true effect -> S-Learner regularization smooths treatment coefficient toward zero; use T/X/DR instead; X-Learner fails when treated group is very small -> X-Learner needs well-identified control-outcome model; fall back to T-Learner or weighted T-Learner. Alternatives: sp.causal_forest, sp.dml, sp.tmle, sp.bcf. Typical minimum N: 500.",
    "input_schema": {
      "properties": {
        "covariates": {
          "description": "Covariate matrix, DataFrame, or column names.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "fold_indices": {
          "description": "Explicit cross-fitting partition: one integer label 0..n_folds-1 per row of data. Replaces every internal split (R/DR nuisances and the AIPW cross-fit behind estimate/se). Default None keeps KFold(n_folds, shuffle=True, random_state=42); passing that split's labels reproduces the default exactly.",
          "type": "string"
        },
        "learner": {
          "default": "dr",
          "description": "Learner type",
          "enum": [
            "s",
            "t",
            "x",
            "r",
            "dr"
          ],
          "type": "string"
        },
        "treat": {
          "description": "Binary treatment column (0/1)",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "covariates",
        "treat",
        "y"
      ],
      "type": "object"
    },
    "name": "metalearner"
  },
  {
    "description": "Generate a referee-grade *Methods and Formulas* appendix for results.",
    "input_schema": {
      "properties": {
        "format": {
          "default": "latex",
          "description": "Output format. ``\"latex\"`` emits ``\\subsection*`` blocks with display math; ``\"markdown\"`` emits ``###`` headings with ``$$`` math; ``\"text\"`` emits a plain-text rendering.",
          "enum": [
            "latex",
            "markdown",
            "text"
          ],
          "type": "string"
        },
        "include_assumptions": {
          "default": true,
          "description": "Include the identifying-assumptions list.",
          "type": "boolean"
        },
        "include_citation": {
          "default": true,
          "description": "Append the APA-style reference from ``result.cite()``.",
          "type": "boolean"
        },
        "include_diagnostics": {
          "default": true,
          "description": "Include the inference block (SE method, clustering, bandwidth, F, CI).",
          "type": "boolean"
        },
        "include_provenance": {
          "default": true,
          "description": "Append a one-line provenance trace (StatsPAI version + estimator identity + methods-spec key) -- the \"exact code path\" leg of the formula / citation / code-path traceability triple.",
          "type": "boolean"
        },
        "results": {
          "description": "One or more fitted result objects exposing ``method`` / ``model_info`` / ``cite``.",
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "required": [
        "results"
      ],
      "type": "object"
    },
    "name": "methods_appendix"
  },
  {
    "description": "Two-period Mincer wage distribution with a structural shift.",
    "input_schema": {
      "properties": {
        "n": {
          "default": 5000,
          "description": "n parameter (int).",
          "type": "integer"
        },
        "seed": {
          "default": 42,
          "description": "Random seed for reproducible stochastic steps.",
          "type": "integer"
        }
      },
      "required": [],
      "type": "object"
    },
    "name": "mincer_wage_panel"
  },
  {
    "description": "Fit a linear mixed-effects model. Validation: certified parity evidence. Assumptions: Continuous outcome, linear in fixed effects; Random effects (intercepts/slopes) normally distributed with the chosen covariance structure; Residuals normal and homoscedastic conditional on random effects; random effects independent of covariates. Pre-conditions: Continuous outcome; Grouping variable (or nested list of grouping levels) for random effects. Failure modes: REML/ML optimizer fails to converge with rich random-slope covariance -> Switch cov_type to 'diagonal' or 'identity', or drop random slopes; Singular covariance (boundary variance estimate) -> Simplify the random-effects covariance or remove the offending random term; Binary or count outcome passed to a linear model -> Use a generalized mixed model for the appropriate family. Alternatives: sp.regress, sp.melogit, sp.feols. Typical minimum N: 200.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Optimiser controls and inference significance level. The defaults use a tight likelihood tolerance so REML variance components and ICC agree with R ``lme4`` / Stata ``mixed`` on parity fixtures.",
          "type": "number"
        },
        "cov_type": {
          "default": "unstructured",
          "description": "Parameterisation of the random-effect covariance matrix *G*: ``'unstructured'`` (default), ``'diagonal'``, or ``'identity'``.",
          "type": "string"
        },
        "group": {
          "description": "Grouping variable. Pass a list like ``[\"school\", \"class\"]`` to estimate a three-level nested model -- the innermost level is used as the cluster for the random slopes/intercept; the outer levels enter as additional random-intercept blocks.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "maxiter": {
          "default": 1000,
          "description": "Optimiser controls and inference significance level. The defaults use a tight likelihood tolerance so REML variance components and ICC agree with R ``lme4`` / Stata ``mixed`` on parity fixtures.",
          "type": "integer"
        },
        "method": {
          "default": "reml",
          "description": "``'reml'`` (default) or ``'ml'``.",
          "type": "string"
        },
        "tol": {
          "default": 1e-09,
          "description": "Optimiser controls and inference significance level. The defaults use a tight likelihood tolerance so REML variance components and ICC agree with R ``lme4`` / Stata ``mixed`` on parity fixtures.",
          "type": "number"
        },
        "x_fixed": {
          "description": "Fixed-effect regressors (intercept is added automatically).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "x_random": {
          "description": "Random-slope variables. ``None`` => random intercept only.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Dependent variable column.",
          "type": "string"
        }
      },
      "required": [
        "group",
        "x_fixed",
        "y"
      ],
      "type": "object"
    },
    "name": "mixed"
  },
  {
    "description": "Mixed Logit (random-coefficient MNL) via simulated maximum likelihood. Validation: certified parity evidence.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level.",
          "type": "number"
        },
        "alt": {
          "description": "Alternative identifier -- accepted for API compatibility with ``statsmodels.MNLogit`` / Stata conventions, but the ordering of alternatives is taken directly from the DataFrame's row order within each ``chid`` group.",
          "type": "string"
        },
        "chid": {
          "description": "Choice-situation identifier. All rows with the same ``chid`` form one choice set.",
          "type": "string"
        },
        "correlated": {
          "default": false,
          "description": "If True, estimate a full Cholesky factor of ``cov(beta_random)``; otherwise only diagonal standard deviations.",
          "type": "boolean"
        },
        "halton_burn": {
          "default": 10,
          "description": "Leading Halton points discarded per dimension.",
          "type": "integer"
        },
        "halton_seed": {
          "default": 1234,
          "description": "halton_seed parameter (int).",
          "type": "integer"
        },
        "halton_shift": {
          "default": true,
          "description": "Randomised-start Halton (one uniform shift per dimension, seeded by ``halton_seed``). ``halton_burn=15, halton_shift=False`` with ``n_draws=50`` reproduces the draws of Stata ``mixlogit``'s defaults (``nrep(50) burn(15)``), so the simulated likelihood is the same function and the estimates agree to optimiser precision.",
          "type": "boolean"
        },
        "maxiter": {
          "default": 200,
          "description": "maxiter parameter (int).",
          "type": "integer"
        },
        "n_draws": {
          "default": 500,
          "description": "Number of Halton draws per individual. Rule-of-thumb: use ``>= 1000`` for correlated models or precise inference.",
          "type": "integer"
        },
        "panel_id": {
          "description": "Individual identifier. When provided, the SAME draws of the random coefficients are used for every choice of the individual (Train 2009 Section 6.5). Omit for cross-sectional data.",
          "type": "string"
        },
        "random_dist": {
          "description": "Per-random-variable distribution -- one of ``'normal'`` (default), ``'lognormal'``, ``'triangular'``.",
          "type": "object"
        },
        "robust": {
          "default": true,
          "description": "Huber sandwich ``H-1 B H-1`` with ``B`` the outer product of the per-individual scores (Stata ``mixlogit, robust``). ``False`` -> inverse observed Hessian (Stata ``mixlogit``'s default ``oim``).",
          "type": "boolean"
        },
        "small_sample": {
          "default": true,
          "description": "Multiply the robust variance by ``N/(N - 1)``, N = number of individuals, as Stata's ``robust`` does. Ignored when ``robust=False``.",
          "type": "boolean"
        },
        "tol": {
          "default": 1e-06,
          "description": "Numerical convergence tolerance.",
          "type": "number"
        },
        "verbose": {
          "default": false,
          "description": "verbose parameter (bool).",
          "type": "boolean"
        },
        "x_fixed": {
          "description": "Columns entering with fixed (non-random) coefficients.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "x_random": {
          "description": "Columns entering with random coefficients (at least one required).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Column name with the 0/1 chosen indicator.",
          "type": "string"
        }
      },
      "required": [
        "chid",
        "y"
      ],
      "type": "object"
    },
    "name": "mixlogit"
  },
  {
    "description": "ML-enhanced partial-identification bounds on the ATE. Assumptions: Only weak (set-identifying) assumptions are imposed; the result is an interval, not a point; Lee bounds add monotonicity of selection; Oster's delta adds proportional selection on observed vs. unobserved. Pre-conditions: The data needed for the point-identifying analysis, plus the weakest credible identifying restriction; For Lee bounds: a binary selection/attrition indicator. Failure modes: Bounds are too wide to be informative -> Add a credible auxiliary restriction (monotone treatment response, instrument) to tighten the bounds. Alternatives: sp.oster_delta, sp.lee_bounds, sp.manski_bounds. Typical minimum N: 100.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level of the band.",
          "type": "number"
        },
        "covariates": {
          "description": "Covariates X used for the outcome / propensity regressions.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "custom_learner": {
          "description": "Any ``.fit()`` / ``.predict()``-compatible regressor.",
          "type": "string"
        },
        "learner": {
          "default": "random_forest",
          "description": "\"random_forest\" Outcome-regression learner. Ignored if ``custom_learner`` is set.",
          "enum": [
            "random_forest",
            "gradient_boosting"
          ],
          "type": "string"
        },
        "n_bootstrap": {
          "default": 200,
          "description": "Non-parametric bootstrap replicates for the 2-sided frequentist band. Set to 0 to return the raw plug-in bounds only.",
          "type": "integer"
        },
        "n_splits": {
          "default": 5,
          "description": "Number of cross-fitting folds.",
          "type": "integer"
        },
        "random_state": {
          "description": "Random seed or RandomState for reproducible stochastic steps.",
          "type": "integer"
        },
        "treat": {
          "description": "Outcome and 0/1 treatment column names.",
          "type": "string"
        },
        "y": {
          "description": "Outcome and 0/1 treatment column names.",
          "type": "string"
        },
        "y_max": {
          "description": "A priori bounds on Y. Defaults to the empirical min/max. **Tighter** external bounds (e.g. if Y is a probability, use ``[0, 1]``) give tighter ML bounds.",
          "type": "number"
        },
        "y_min": {
          "description": "A priori bounds on Y. Defaults to the empirical min/max. **Tighter** external bounds (e.g. if Y is a probability, use ``[0, 1]``) give tighter ML bounds.",
          "type": "number"
        }
      },
      "required": [
        "covariates",
        "treat",
        "y"
      ],
      "type": "object"
    },
    "name": "ml_bounds"
  },
  {
    "description": "Multinomial logit for J > 2 unordered categories via MLE. Validation: certified parity evidence.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "base": {
          "default": 0,
          "description": "Base / reference category (index into sorted unique values).",
          "type": "integer"
        },
        "cluster": {
          "description": "Cluster variable for clustered SE.",
          "type": "string"
        },
        "formula": {
          "description": "Formula ``\"y ~ x1 + x2\"``.",
          "type": "string"
        },
        "maxiter": {
          "default": 100,
          "description": "maxiter parameter (int).",
          "type": "integer"
        },
        "robust": {
          "default": "nonrobust",
          "description": "``\"robust\"`` / ``\"HC1\"`` for Huber-White sandwich SE.",
          "type": "string"
        },
        "rrr": {
          "default": false,
          "description": "Report Relative Risk Ratios (exp(beta)) instead of coefficients.",
          "type": "boolean"
        },
        "tol": {
          "default": 1e-08,
          "description": "Numerical convergence tolerance.",
          "type": "number"
        },
        "x": {
          "description": "Regressors.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Dependent variable (categorical, integer-coded).",
          "type": "string"
        }
      },
      "required": [],
      "type": "object"
    },
    "name": "mlogit"
  },
  {
    "description": "Model-averaging / stacking DML-PLR estimator. Validation: certified parity evidence.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Two-sided CI level.",
          "type": "number"
        },
        "candidates": {
          "description": "Candidate nuisance learners. ``ml_g`` regresses ``y`` on ``X``; ``ml_m`` regresses ``treat`` on ``X``. Defaults to a Lasso/Ridge/ RandomForest/GradientBoosting roster.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "covariates": {
          "description": "Covariate columns ``X``.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "fold_indices": {
          "description": "Explicit cross-fitting fold per row (column name or array of length ``len(data)``). Overrides ``n_folds`` / ``seed``; every candidate uses the same partition.",
          "type": "string"
        },
        "n_folds": {
          "default": 5,
          "description": "Cross-fitting folds per candidate.",
          "type": "integer"
        },
        "sample_weight": {
          "description": "Per-observation weights. If supplied, every nuisance fit uses ``sample_weight=`` (with a graceful fallback warning if the learner does not accept it), the CLS stacking objective becomes weighted least squares, and the PLR moment + sandwich variance use weighted sums. The MSE used for ``inverse_risk`` / ``single_best`` weighting is also the weighted MSE.",
          "type": "string"
        },
        "seed": {
          "default": 0,
          "description": "Random seed for reproducible stochastic steps.",
          "type": "integer"
        },
        "treat": {
          "description": "Continuous-or-binary treatment column.",
          "type": "string"
        },
        "weight_rule": {
          "default": "short_stacking",
          "description": "How to combine candidate nuisance predictions or estimates. * ``\"short_stacking\"`` *(default; Ahrens et al. 2025 eq. 7)* -- solve constrained least squares on cross-fitted predictions for each nuisance separately (``y`` and ``D``), produce stacked nuisances, plug into the PLR moment equation. * ``\"single_best\"`` -- Ahrens et al. (2025, fn. 8): pick the candidate with lowest joint nuisance MSE. * ``\"inverse_risk\"`` -- :math:`w_k \\propto 1/(\\text{MSE}_g + \\text{MSE}_m)`. Convenience baseline; **not** in the paper. * ``\"equal\"`` -- :math:`w_k = 1/K`. Convenience baseline; **not** in the paper. For the non-stacking rules (``inverse_risk`` / ``equal`` / ``single_best``) the function computes per-candidate :math:`\\hat\\theta_k` and reports the weighted average with a between-candidate-covariance-corrected SE; for ``\"short_stacking\"`` it reports the standard PLR sandwich SE on the stacked-nuisance score (Neyman orthogonality is preserved).",
          "enum": [
            "short_stacking",
            "single_best",
            "inverse_risk",
            "equal"
          ],
          "type": "string"
        },
        "y": {
          "description": "Outcome column.",
          "type": "string"
        }
      },
      "required": [
        "covariates",
        "treat",
        "y"
      ],
      "type": "object"
    },
    "name": "model_averaging_dml"
  },
  {
    "description": "Unified Mendelian Randomization dispatcher. method= selects the estimator: 'ivw' / 'egger' / 'median' / 'penalized_median' / 'mode' / 'all' (runs IVW+Egger+Median together) / 'mvmr' / 'mediation' / 'bma' (multi-exposure) / 'presso' / 'radial' / 'leave_one_out' / 'steiger' / 'heterogeneity' / 'pleiotropy_egger' / 'f_statistic' (diagnostics). Kwargs are passed through to the target function unchanged; see sp.mendelian_family guide. Validation: certified parity evidence. Assumptions: Relevance: SNPs predict exposure (F-statistic >= 10 per SNP or set-F); Independence: SNPs perp confounders of exposure-outcome; Exclusion restriction: SNPs affect outcome only through exposure (InSIDE for Egger; >= 50% valid for median; modal for mode-based). Pre-conditions: SNP-summary statistics for exposure and outcome aligned by SNP; beta_exposure / beta_outcome / se_exposure / se_outcome arrays of equal length; >= 10 genetic instruments for reliable IVW/median/mode; >= 20 for robust Egger intercept. Failure modes: Egger intercept p < 0.05 -- directional pleiotropy -> Use weighted-median or mode-based estimator; report Egger intercept + I2 as pleiotropy diagnostic; Q-statistic rejects homogeneity (Co...",
    "input_schema": {
      "properties": {
        "method": {
          "default": "ivw",
          "description": "MR estimator / diagnostic -- call sp.mr_available_methods() for the full list.",
          "type": "string"
        }
      },
      "required": [],
      "type": "object"
    },
    "name": "mr"
  },
  {
    "description": "Marginal Structural Models for time-varying treatments with time-varying confounders. Uses stabilized IPTW and cluster-robust inference. Handles binary or continuous treatment; exposure summary can be current, cumulative, or ever. Validation: certified parity evidence. Assumptions: Sequential exchangeability / no unmeasured confounding at each time point; Positivity: every treatment level is possible given the past; Correct specification of the treatment and/or outcome models. Pre-conditions: Sequentially measured covariates, (time-varying) treatment, and outcome; Models for the treatment process and the outcome (or weights). Failure modes: Stabilized weights have extreme values (positivity near-violation) -> Truncate weights, simplify the treatment model, or use a doubly-robust estimator (TMLE). Alternatives: sp.tmle, sp.g_computation, sp.ipw. Typical minimum N: 300.",
    "input_schema": {
      "properties": {
        "baseline": {
          "description": "Baseline covariates",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "density_sd": {
          "default": "unbiased",
          "description": "Residual-SD convention for continuous-treatment density weights; see :func:`stabilized_weights`.",
          "enum": [
            "unbiased",
            "ml"
          ],
          "type": "string"
        },
        "exposure": {
          "default": "cumulative",
          "description": "Exposure summary",
          "enum": [
            "cumulative",
            "current",
            "ever"
          ],
          "type": "string"
        },
        "family": {
          "default": "gaussian",
          "description": "Outcome family",
          "enum": [
            "gaussian",
            "binomial"
          ],
          "type": "string"
        },
        "id": {
          "description": "Unit identifier",
          "type": "string"
        },
        "time": {
          "description": "Period identifier",
          "type": "string"
        },
        "time_varying": {
          "description": "Time-varying confounders (pre-treatment)",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "treat": {
          "description": "Time-varying treatment",
          "type": "string"
        },
        "trim": {
          "default": 0.01,
          "description": "Weight truncation quantile",
          "type": "number"
        },
        "y": {
          "description": "Outcome",
          "type": "string"
        }
      },
      "required": [
        "id",
        "time",
        "time_varying",
        "treat",
        "y"
      ],
      "type": "object"
    },
    "name": "msm"
  },
  {
    "description": "Multi-arm causal forest (grf::multi_arm_causal_forest): CATEs of K-1 arms against a reference, estimated jointly by one forest whose trees split on the gradient of all contrasts (multi-arm R-learner with GRF weights). Propensities from a probability forest, doubly-robust per-arm ATEs, BLPs per contrast. Operators match grf to 1e-13 (T2); forest statistics match grf within Monte Carlo error (T3). Validation: validated evidence tier (known-truth, reference, external-parity, or Monte Carlo artifact). Assumptions: unconfoundedness given X; overlap for every arm. Pre-conditions: every arm observed; propensities bounded away from zero. Failure modes: DataInsufficient: need at least two treatment arms -> Pass a treatment with two or more values; UserWarning: smallest estimated arm propensity -> Poor overlap: pass propensity_bounds= or trim. Alternatives: sp.causal_forest, sp.multi_treatment, sp.metalearner. Typical minimum N: 1000.",
    "input_schema": {
      "properties": {
        "W_hat": {
          "description": "``(n, K)`` arm propensities in the order of ``.arms``; default out-of-bag probability forest.",
          "type": "string"
        },
        "Y_hat": {
          "description": "``E[Y | X]``; default out-of-bag regression forest.",
          "type": "string"
        },
        "alpha": {
          "default": 0.05,
          "description": "Significance level for the reported intervals.",
          "type": "number"
        },
        "ci_group_size": {
          "default": 2,
          "description": "Trees per little bag; 1 disables variance estimates.",
          "type": "integer"
        },
        "clusters": {
          "description": "Cluster ids: trees sample whole clusters; SEs are cluster-robust.",
          "type": "string"
        },
        "covariates": {
          "description": "Covariates ``X``.",
          "type": "string"
        },
        "equalize_cluster_weights": {
          "default": false,
          "description": "Give every cluster equal weight (incompatible with weights).",
          "type": "boolean"
        },
        "honest": {
          "default": true,
          "description": "grf honesty: grow on one half of the drawn sample, estimate leaves on the other.",
          "type": "boolean"
        },
        "honesty_fraction": {
          "default": 0.5,
          "description": "Share of the drawn sample used to grow the tree.",
          "type": "number"
        },
        "honesty_prune_leaves": {
          "default": true,
          "description": "Prune leaves left empty by the estimation half.",
          "type": "boolean"
        },
        "imbalance_penalty": {
          "default": 0.0,
          "description": "grf imbalance.penalty on unbalanced splits.",
          "type": "number"
        },
        "max_depth": {
          "description": "Optional depth cap (no grf analogue).",
          "type": "string"
        },
        "max_samples": {
          "default": 0.5,
          "description": "Fraction of clusters drawn per tree (grf sample.fraction); at most 0.5 when ci_group_size > 1.",
          "type": "number"
        },
        "min_samples_leaf": {
          "default": 5,
          "description": "grf min.node.size: nodes with at most this many growing samples are not split.",
          "type": "integer"
        },
        "mtry": {
          "description": "Candidate variables per split (grf default min(ceil(sqrt(p) + 20), p)).",
          "type": "string"
        },
        "n_estimators": {
          "default": 2000,
          "description": "Trees (grf num.trees).",
          "type": "integer"
        },
        "n_jobs": {
          "default": 1,
          "description": "Threads used to grow trees (-1 = all cores).",
          "type": "integer"
        },
        "propensity_bounds": {
          "description": "Clip the estimated propensities to these bounds in the average- effect scores (not done by default, as in grf).",
          "type": "string"
        },
        "random_state": {
          "default": 42,
          "description": "Seed; different seeds give independent forests.",
          "type": "integer"
        },
        "reference": {
          "description": "Reference arm; defaults to the smallest label (``0`` for integer arms, matching the previous API).",
          "type": "string"
        },
        "split_alpha": {
          "default": 0.05,
          "description": "grf alpha: minimum share of the parent each child keeps.",
          "type": "number"
        },
        "stabilize_splits": {
          "default": true,
          "description": "Apply the treatment/instrument split constraints (grf stabilize.splits).",
          "type": "boolean"
        },
        "treat": {
          "description": "Arm labels (any values; at least two arms).",
          "type": "string"
        },
        "weights": {
          "description": "Sample weights (grf sample.weights).",
          "type": "string"
        },
        "y": {
          "description": "Outcome.",
          "type": "string"
        }
      },
      "required": [],
      "type": "object"
    },
    "name": "multi_arm_forest"
  },
  {
    "description": "User-friendly alias for :func:`sp.rdmc` (multi-cutoff RD). Validation: certified parity evidence. Assumptions: Conditional expectations of potential outcomes are continuous at the cutoff; Units cannot precisely manipulate the running variable around the cutoff (no sorting); For fuzzy designs: monotonicity of treatment take-up at the cutoff. Pre-conditions: A continuous running/forcing variable with a known cutoff that (sharply or fuzzily) assigns treatment; Enough observations in a neighbourhood of the cutoff to fit a local polynomial. Failure modes: Density of the running variable jumps at the cutoff (manipulation / sorting) -> Run a McCrary / density test (rdplotdensity); if manipulation is present the design is invalid near the cutoff; Estimate swings with the bandwidth -- results are not robust -> Report a bandwidth-sensitivity curve and use a data-driven MSE-optimal bandwidth. Alternatives: sp.rdrobust, sp.rdrandinf, sp.rdbwselect. Typical minimum N: 500.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "bandwidth": {
          "description": "Bandwidth used for local smoothing or kernel weighting.",
          "type": "number"
        },
        "cutoff_var": {
          "description": "cutoff_var parameter (Optional[str]).",
          "type": "string"
        },
        "cutoffs": {
          "description": "cutoffs parameter (Optional[List[float]]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "kernel": {
          "default": "triangular",
          "description": "Kernel function used for weighting or smoothing.",
          "type": "string"
        },
        "pooling": {
          "default": "ivw",
          "description": "pooling parameter (str).",
          "type": "string"
        },
        "x": {
          "description": "Primary running variable, regressor, or feature input for this estimator.",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "x",
        "y"
      ],
      "type": "object"
    },
    "name": "multi_cutoff_rd"
  },
  {
    "description": "Multiple Outcomes Synthetic Control Method (Sun, Ben-Michael & Feller). Validation: certified parity evidence. Assumptions: A convex (or regularized) combination of donor units reproduces the treated unit's pre-treatment outcome path; No interference: the treatment does not affect the donor units (SUTVA); No anticipation before the treatment date. Pre-conditions: Panel of one or more treated units plus an untreated donor pool, observed over time; Pre-treatment window long enough to fit donor weights (rule of thumb: more pre-periods than donors used); Outcome observed for every unit in every period. Failure modes: Large pre-treatment RMSPE -- the synthetic unit fails to track the treated unit before treatment -> Add donors / predictors, lengthen the pre-period, or use a bias-corrected estimator (sdid, augsynth); Placebo / permutation inference shows the estimate is not extreme relative to donors -> Report the placebo distribution honestly; the effect may not be distinguishable from noise. Alternatives: sp.sdid, sp.augsynth, sp.gsynth, sp.callaway_santanna. Typical minimum N: 15.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and joint test.",
          "type": "number"
        },
        "method": {
          "default": "concatenated",
          "description": "Weight-estimation strategy. * ``'concatenated'`` -- stack all K standardised outcome panels vertically and solve one quadratic programme. * ``'averaged'`` -- standardise each outcome, average across K, then solve SCM on the mean series.",
          "enum": [
            "concatenated",
            "averaged"
          ],
          "type": "string"
        },
        "outcomes": {
          "description": "Column names for the K outcome variables.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "penalization": {
          "default": 0.0,
          "description": "Ridge-type penalty added to the diagonal of the donor cross-product matrix (``penalization * I``). Helps when donors are collinear.",
          "type": "number"
        },
        "placebo": {
          "default": true,
          "description": "Run in-space placebo permutations for inference (each donor is pretended to be treated in turn).",
          "type": "boolean"
        },
        "standardize": {
          "default": true,
          "description": "Standardise each outcome to zero mean / unit variance before stacking or averaging (strongly recommended when outcome scales differ).",
          "type": "boolean"
        },
        "time": {
          "description": "Time period column.",
          "type": "string"
        },
        "treated_unit": {
          "description": "Value identifying the treated unit.",
          "type": "string"
        },
        "treatment_time": {
          "description": "First treatment period (inclusive).",
          "type": "string"
        },
        "unit": {
          "description": "Unit identifier column.",
          "type": "string"
        }
      },
      "required": [
        "outcomes",
        "time",
        "treated_unit",
        "treatment_time",
        "unit"
      ],
      "type": "object"
    },
    "name": "multi_outcome_synth"
  },
  {
    "description": "Multi-task regression forest for ``E[Y_1, ..., Y_q | X]``",
    "input_schema": {
      "properties": {
        "ci_group_size": {
          "default": 1,
          "description": "Trees per little bag (variance estimates need >= 2).",
          "type": "integer"
        },
        "clusters": {
          "description": "Cluster ids (trees sample whole clusters).",
          "type": "string"
        },
        "covariates": {
          "description": "Covariates ``X``.",
          "type": "string"
        },
        "equalize_cluster_weights": {
          "default": false,
          "description": "equalize_cluster_weights parameter (bool).",
          "type": "boolean"
        },
        "honest": {
          "default": true,
          "description": "grf honesty options.",
          "type": "boolean"
        },
        "honesty_fraction": {
          "default": 0.5,
          "description": "grf honesty options.",
          "type": "number"
        },
        "honesty_prune_leaves": {
          "default": true,
          "description": "grf honesty options.",
          "type": "boolean"
        },
        "imbalance_penalty": {
          "default": 0.0,
          "description": "imbalance_penalty parameter (float).",
          "type": "number"
        },
        "max_depth": {
          "description": "max_depth parameter (Optional[int]).",
          "type": "integer"
        },
        "max_samples": {
          "default": 0.5,
          "description": "grf ``sample.fraction``.",
          "type": "number"
        },
        "min_samples_leaf": {
          "default": 5,
          "description": "grf ``min.node.size``.",
          "type": "integer"
        },
        "mtry": {
          "description": "mtry parameter (Optional[int]).",
          "type": "integer"
        },
        "n_estimators": {
          "default": 2000,
          "description": "Trees (grf ``num.trees``).",
          "type": "integer"
        },
        "n_jobs": {
          "default": 1,
          "description": "Number of jobs.",
          "type": "integer"
        },
        "random_state": {
          "default": 42,
          "description": "Random seed or RandomState for reproducible stochastic steps.",
          "type": "integer"
        },
        "split_alpha": {
          "default": 0.05,
          "description": "grf ``alpha`` (minimum child share).",
          "type": "number"
        },
        "weights": {
          "description": "Sample weights (grf ``sample.weights``).",
          "type": "string"
        },
        "y": {
          "description": "Outcomes.",
          "type": "string"
        }
      },
      "required": [],
      "type": "object"
    },
    "name": "multi_regression_forest"
  },
  {
    "description": "User-friendly alias for :func:`sp.rd_multi_score`. Assumptions: Conditional expectations of potential outcomes are continuous at the cutoff; Units cannot precisely manipulate the running variable around the cutoff (no sorting); For fuzzy designs: monotonicity of treatment take-up at the cutoff. Pre-conditions: A continuous running/forcing variable with a known cutoff that (sharply or fuzzily) assigns treatment; Enough observations in a neighbourhood of the cutoff to fit a local polynomial. Failure modes: Density of the running variable jumps at the cutoff (manipulation / sorting) -> Run a McCrary / density test (rdplotdensity); if manipulation is present the design is invalid near the cutoff; Estimate swings with the bandwidth -- results are not robust -> Report a bandwidth-sensitivity curve and use a data-driven MSE-optimal bandwidth. Alternatives: sp.rdrobust, sp.rdrandinf, sp.rdbwselect. Typical minimum N: 500.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "bandwidth": {
          "description": "Bandwidth used for local smoothing or kernel weighting.",
          "type": "number"
        },
        "cutoffs": {
          "description": "cutoffs parameter (List[float]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "kernel": {
          "default": "triangular",
          "description": "Kernel function used for weighting or smoothing.",
          "type": "string"
        },
        "running_vars": {
          "description": "running_vars parameter (List[str]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "cutoffs",
        "running_vars",
        "y"
      ],
      "type": "object"
    },
    "name": "multi_score_rd"
  },
  {
    "description": "Effects of multi-valued (3+ level) treatments via AIPW. Returns pairwise contrasts versus a reference level. Validation: certified parity evidence. Assumptions: Generalised unconfoundedness: Y(a) perp T | X for all a; Generalised overlap: 0 < P(T=a | X) < 1 for each arm a; SUTVA across arms. Pre-conditions: treat is integer-valued with >= 2 distinct levels; covariates comprise the confounding set; enough units per treatment arm (>= 50 per arm). Failure modes: Some arm has near-zero propensity in the data -> Violates overlap -- drop that arm or use bounds; Tiny treatment cells (< 30) -> Collapse sparse arms or use regularised multinomial propensity. Alternatives: sp.multi_arm_forest, sp.dml, sp.metalearner. Typical minimum N: 300.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "covariates": {
          "description": "Covariate matrix, DataFrame, or column names.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "n_bootstrap": {
          "default": 500,
          "description": "Number of bootstrap replications.",
          "type": "integer"
        },
        "outcome_model": {
          "default": "gbm",
          "description": "Per-arm outcome regression ``mu_k(X)``. ``'gbm'`` is a gradient boosting regressor (100 trees, depth 3); ``'linear'`` is OLS with an intercept fitted separately in each arm, which is the outcome model of Stata's ``teffects aipw`` (``linear by ML``).",
          "enum": [
            "gbm",
            "linear"
          ],
          "type": "string"
        },
        "reference": {
          "description": "Reference treatment level (defaults to 0 / smallest)",
          "type": "integer"
        },
        "se_method": {
          "default": "bootstrap",
          "description": "``'bootstrap'`` re-fits the whole estimator (same outcome model) on ``n_bootstrap`` resamples. ``'influence'`` reports ``sd(phi_k - phi_ref) / sqrt(n)`` from the AIPW influence function with the nuisance fits treated as known.",
          "enum": [
            "bootstrap",
            "influence",
            "sandwich"
          ],
          "type": "string"
        },
        "treat": {
          "description": "Multi-valued treatment (int)",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "covariates",
        "treat",
        "y"
      ],
      "type": "object"
    },
    "name": "multi_treatment"
  },
  {
    "description": "Compute N-way cluster-robust variance of an OLS coefficient vector. Validation: certified parity evidence.",
    "input_schema": {
      "properties": {
        "X": {
          "description": "Design matrix used in the regression.",
          "type": "string"
        },
        "clusters": {
          "description": "One or more cluster variables, one per dimension. Non-numeric labels are supported.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "df_adjust": {
          "default": true,
          "description": "If True, apply the G/(G-1) * (n-1)/(n-k) CR1 finite-sample correction per component variance. If False, uses raw sandwich (useful when the caller has already degreed-freedom adjusted).",
          "type": "boolean"
        },
        "n_params": {
          "description": "Override for the ``k`` used in DOF adjustment; useful when FEs have been absorbed (pass total absorbed DOF here).",
          "type": "integer"
        },
        "psd_correct": {
          "default": true,
          "description": "Project V onto PSD cone by zeroing negative eigenvalues.",
          "type": "boolean"
        },
        "resid": {
          "description": "OLS residuals.",
          "type": "string"
        }
      },
      "required": [
        "X",
        "clusters",
        "resid"
      ],
      "type": "object"
    },
    "name": "multiway_cluster_vcov"
  },
  {
    "description": "Fit a negative-binomial count model. Use this for overdispersed non-negative count outcomes; formulas may include explicit fixed effects with 'y ~ x | id' for moderate panels. Validation: certified parity evidence.",
    "input_schema": {
      "properties": {
        "cluster": {
          "description": "Column name for cluster-robust SEs.",
          "type": "string"
        },
        "dispersion": {
          "default": "mean",
          "description": "NB2 mean dispersion or NB1 constant dispersion.",
          "enum": [
            "mean",
            "constant"
          ],
          "type": "string"
        },
        "exposure": {
          "description": "Positive exposure column; log(exposure) is used as offset.",
          "type": "string"
        },
        "formula": {
          "description": "R-style formula, e.g. 'count ~ x1 + x2 | id'",
          "type": "string"
        },
        "irr": {
          "default": false,
          "description": "Report incidence-rate ratios instead of log coefficients.",
          "type": "boolean"
        },
        "offset": {
          "description": "Column containing a log offset.",
          "type": "string"
        },
        "robust": {
          "default": "nonrobust",
          "enum": [
            "nonrobust",
            "robust",
            "hc0",
            "hc1"
          ],
          "type": "string"
        }
      },
      "required": [
        "formula"
      ],
      "type": "object"
    },
    "name": "nbreg"
  },
  {
    "description": "Regress outcome on a *negative-control exposure*. Assumptions: The negative-control exposure has no causal effect on the outcome by design (Lipsitch et al. 2010); It shares the same confounding structure as the real exposure. Pre-conditions: data has the outcome and a negative-control exposure column. Failure modes: Significant coefficient on the negative-control exposure signals residual confounding, not an effect -> Treat a non-zero coefficient as a confounding alarm; add covariates or run a sensitivity analysis. Alternatives: sp.negative_control_outcome, sp.sensemakr, sp.evalue. Typical minimum N: 100.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "covariates": {
          "description": "Covariate matrix, DataFrame, or column names.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "nce": {
          "description": "nce parameter (str).",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "nce",
        "y"
      ],
      "type": "object"
    },
    "name": "negative_control_exposure"
  },
  {
    "description": "Lipsitch-style NCO calibration. Assumptions: The negative-control outcome is not caused by the treatment (Lipsitch-style calibration); It shares confounders with the real outcome. Pre-conditions: data has a negative-control outcome and a treatment column. Failure modes: Coefficient on treatment differs significantly from zero -- residual confounding detected -> Condition on more covariates or quantify the implied bias with a sensitivity analysis. Alternatives: sp.negative_control_exposure, sp.sensemakr, sp.evalue. Typical minimum N: 100.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "covariates": {
          "description": "Measured confounders to condition on.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "nco": {
          "description": "Negative-control outcome -- a variable plausibly unaffected by the true treatment but sharing confounders with the real Y.",
          "type": "string"
        },
        "treat": {
          "description": "Treatment indicator or exposure variable.",
          "type": "string"
        }
      },
      "required": [
        "nco",
        "treat"
      ],
      "type": "object"
    },
    "name": "negative_control_outcome"
  },
  {
    "description": "Aronow-Samii Horvitz-Thompson estimator for arbitrary interference via a user-supplied exposure mapping. Handles Bernoulli randomisation designs with simulated conservative variance. Validation: validated evidence tier (known-truth, reference, external-parity, or Monte Carlo artifact). Known limitations: design='complete' is reserved but not implemented; passing it raises NotImplementedError. Use design='bernoulli' with p_treat=K/N as an approximation only if that matches the assignment mechanism you are willing to assume. Assumptions: Exposure mapping is correctly specified (as4 / as3 / as2 -- Aronow-Samii hierarchy); Positivity: every exposure level has positive probability under the design; Network adjacency is fixed / known (measurement error in ties introduces bias). Pre-conditions: adjacency is a binary n x n matrix encoding network ties; Y, Z have same length n; randomisation design is known (bernoulli with p_treat, or complete). Failure modes: Some exposure level has < 5 observed units -> Switch to a coarser mapping (as4 -> as3) or increase sample size; Variance estimate extremely conservative (wide CI) -> HT-style variance is conservative by design -- use sp.spillover for...",
    "input_schema": {
      "properties": {
        "Y": {
          "description": "Outcome vector",
          "type": "string"
        },
        "Z": {
          "description": "Treatment vector (0/1)",
          "type": "string"
        },
        "adjacency": {
          "description": "Adjacency matrix (n x n) or sparse",
          "type": "string"
        },
        "design": {
          "default": "bernoulli",
          "description": "Randomisation design",
          "enum": [
            "bernoulli",
            "complete"
          ],
          "type": "string"
        },
        "mapping": {
          "default": "as4",
          "description": "Exposure mapping",
          "enum": [
            "as4",
            "as3",
            "as2",
            "custom"
          ],
          "type": "string"
        },
        "n_sim": {
          "default": 2000,
          "description": "Number of sim.",
          "type": "integer"
        },
        "p_treat": {
          "description": "Marginal treatment probability",
          "type": "number"
        }
      },
      "required": [
        "Y",
        "Z",
        "adjacency"
      ],
      "type": "object"
    },
    "name": "network_exposure"
  },
  {
    "description": "Alias for ``icp(..., method='nonlinear')`` -- Heinze-Deml et al. 2018. Assumptions: Causal Markov condition and faithfulness (PC/GES/FCI); Causal sufficiency for PC/GES (no latent confounders); FCI relaxes this; Acyclicity; LiNGAM additionally assumes a linear non-Gaussian model. Pre-conditions: Constraint-/score-based discovery needs i.i.d. observational data with enough samples for reliable conditional-independence tests; Invariance-based discovery (ICP) needs data labelled by environment / intervention. Failure modes: Unstable skeleton / many undirected edges -- faithfulness or sample size is the likely culprit -> Increase the sample, relax the CI-test threshold, or switch to FCI if latent confounders are plausible. Alternatives: sp.pc_algorithm, sp.fci, sp.ges, sp.lingam. Typical minimum N: 500.",
    "input_schema": {
      "properties": {
        "X": {
          "description": "Feature matrix or covariate DataFrame.",
          "type": "string"
        },
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "environment": {
          "description": "environment parameter (np.ndarray).",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "X",
        "environment",
        "y"
      ],
      "type": "object"
    },
    "name": "nonlinear_icp"
  },
  {
    "description": "Bunching at Notches estimator (Kleven & Waseem 2013). Validation: validated evidence tier (known-truth, reference, external-parity, or Monte Carlo artifact). Assumptions: The counterfactual density would be smooth through the threshold absent the policy; Excess mass at the threshold reflects the behavioural elasticity of interest; No other discontinuity coincides with the threshold. Pre-conditions: A behavioural choice variable (earnings, hours, ...) with a known kink or notch in the budget/choice set; A visible empirical density of the running variable around the threshold. Failure modes: Round-number heaping or a coincident policy contaminates the bunching mass -> Exclude heaping points, widen the excluded region, and test the counterfactual polynomial order. Alternatives: sp.rdrobust, sp.rkd. Typical minimum N: 500.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level.",
          "type": "number"
        },
        "bin_width": {
          "default": 500,
          "description": "Bin width for the histogram.",
          "type": "number"
        },
        "exclude_range": {
          "description": "Range around the notch to exclude from the counterfactual fit. If None, defaults to (notch_point - 3*bin_width, notch_point + 5*bin_width).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "n_boot": {
          "default": 500,
          "description": "Number of bootstrap replications for standard errors.",
          "type": "integer"
        },
        "notch_point": {
          "description": "Location of the notch in the running variable.",
          "type": "number"
        },
        "notch_size": {
          "description": "Size of the discontinuous jump (delta-tau). If provided, a structural elasticity is estimated.",
          "type": "number"
        },
        "poly_order": {
          "default": 7,
          "description": "Polynomial order for the counterfactual distribution.",
          "type": "integer"
        },
        "seed": {
          "default": 42,
          "description": "Random seed for reproducibility.",
          "type": "integer"
        },
        "x": {
          "description": "Running variable name (e.g., 'income').",
          "type": "string"
        }
      },
      "required": [
        "notch_point",
        "x"
      ],
      "type": "object"
    },
    "name": "notch"
  },
  {
    "description": "Learn a DAG from data using NOTEARS (Zheng et al. 2018). Validation: validated evidence tier (known-truth, reference, external-parity, or Monte Carlo artifact). Assumptions: Causal Markov condition and faithfulness (PC/GES/FCI); Causal sufficiency for PC/GES (no latent confounders); FCI relaxes this; Acyclicity; LiNGAM additionally assumes a linear non-Gaussian model. Pre-conditions: Constraint-/score-based discovery needs i.i.d. observational data with enough samples for reliable conditional-independence tests; Invariance-based discovery (ICP) needs data labelled by environment / intervention. Failure modes: Unstable skeleton / many undirected edges -- faithfulness or sample size is the likely culprit -> Increase the sample, relax the CI-test threshold, or switch to FCI if latent confounders are plausible. Alternatives: sp.pc_algorithm, sp.fci, sp.ges, sp.lingam. Typical minimum N: 500.",
    "input_schema": {
      "properties": {
        "h_tol": {
          "default": 1e-08,
          "description": "Convergence threshold for acyclicity constraint h(W).",
          "type": "number"
        },
        "lambda1": {
          "default": 0.1,
          "description": "L1 penalty weight for sparsity. Higher = sparser graph.",
          "type": "number"
        },
        "max_iter": {
          "default": 100,
          "description": "Maximum augmented Lagrangian iterations.",
          "type": "integer"
        },
        "random_state": {
          "default": 42,
          "description": "Random seed.",
          "type": "integer"
        },
        "rho_max": {
          "default": 1e+16,
          "description": "Maximum penalty parameter rho.",
          "type": "number"
        },
        "variables": {
          "description": "Column names to use. If None, uses all numeric columns.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "w_threshold": {
          "default": 0.3,
          "description": "Threshold for pruning small edge weights. Edges with |W_ij| < w_threshold are removed.",
          "type": "number"
        }
      },
      "required": [],
      "type": "object"
    },
    "name": "notears"
  },
  {
    "description": "Oaxaca-Blinder decomposition of mean outcome gaps. Validation: certified parity evidence. Assumptions: Linearity of conditional mean within each group; Constant returns to covariates within group (no interactions ignored); Reference-group choice does not change interpretive sign of explained vs. unexplained gaps. Pre-conditions: Binary group indicator with both groups represented; Linear specification of outcome on covariates within each group. Failure modes: Detailed decomposition signs flip when reference group changes -> Report aggregated decomposition only, or use pooled reference (Neumark / Cotton). Alternatives: sp.ffl_decompose, sp.dfl_decompose, sp.rif_decomposition. Typical minimum N: 200.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for p-values.",
          "type": "number"
        },
        "detail": {
          "default": true,
          "description": "If True, compute variable-level contributions to the explained component.",
          "type": "boolean"
        },
        "group": {
          "description": "Binary group indicator (0 = Group A, 1 = Group B).",
          "type": "string"
        },
        "reference": {
          "default": 0,
          "description": "Reference coefficient vector beta*: - ``0`` -- Group A coefficients (beta_A). The \"explained\" part uses Group A's returns as the benchmark. - ``1`` -- Group B coefficients (beta_B). - ``'pooled'`` -- Pooled OLS (Neumark 1988). - ``'cotton'`` -- Sample-size weighted average (Cotton 1988). - ``'reimers'`` -- Equal-weighted average (Reimers 1983).",
          "enum": [
            "pooled",
            "cotton",
            "reimers"
          ],
          "type": "integer"
        },
        "x": {
          "description": "Covariate names.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Outcome variable name.",
          "type": "string"
        }
      },
      "required": [
        "group",
        "x",
        "y"
      ],
      "type": "object"
    },
    "name": "oaxaca"
  },
  {
    "description": "Safe offline policy learning with a cost-constraint. Assumptions: Sequential ignorability: no unobserved confounders of actions and outcomes; Positivity: the behaviour policy explores all evaluated actions; The environment satisfies the assumed (Markov) dynamics. Pre-conditions: Logged trajectories (states, actions, rewards) from a known or estimable behaviour policy. Failure modes: Poor behaviour-policy coverage -- the target policy queries unseen state-action regions -> Use offline-safe / pessimistic methods and report effective sample size of the importance weights. Alternatives: sp.offline_safe_policy, sp.policy_value. Typical minimum N: 1000.",
    "input_schema": {
      "properties": {
        "action": {
          "description": "Column names. state and action must be discrete.",
          "type": "string"
        },
        "cost": {
          "description": "Column names. state and action must be discrete.",
          "type": "string"
        },
        "cost_threshold": {
          "default": 0.5,
          "description": "Max allowed expected cost per step.",
          "type": "number"
        },
        "discount": {
          "default": 0.95,
          "description": "discount parameter (float).",
          "type": "number"
        },
        "n_iter": {
          "default": 100,
          "description": "Number of iter.",
          "type": "integer"
        },
        "reward": {
          "description": "Column names. state and action must be discrete.",
          "type": "string"
        },
        "seed": {
          "default": 0,
          "description": "Random seed for reproducible stochastic steps.",
          "type": "integer"
        },
        "state": {
          "description": "Column names. state and action must be discrete.",
          "type": "string"
        }
      },
      "required": [
        "action",
        "cost",
        "reward",
        "state"
      ],
      "type": "object"
    },
    "name": "offline_safe_policy"
  },
  {
    "description": "Ordered logit (proportional odds) model via MLE. Validation: certified parity evidence.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "cluster": {
          "description": "Cluster identifier column for clustered standard errors.",
          "type": "string"
        },
        "formula": {
          "description": "Formula ``\"y ~ x1 + x2\"``.",
          "type": "string"
        },
        "maxiter": {
          "default": 100,
          "description": "maxiter parameter (int).",
          "type": "integer"
        },
        "robust": {
          "default": "nonrobust",
          "description": "Robust standard-error or covariance estimator option.",
          "type": "string"
        },
        "tol": {
          "default": 1e-08,
          "description": "Numerical convergence tolerance.",
          "type": "number"
        },
        "x": {
          "description": "Primary running variable, regressor, or feature input for this estimator.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Ordered categorical dependent variable.",
          "type": "string"
        }
      },
      "required": [],
      "type": "object"
    },
    "name": "ologit"
  },
  {
    "description": "Ordered probit model via MLE. Validation: certified parity evidence.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "cluster": {
          "description": "Cluster identifier column for clustered standard errors.",
          "type": "string"
        },
        "formula": {
          "description": "Formula ``\"y ~ x1 + x2\"``.",
          "type": "string"
        },
        "maxiter": {
          "default": 100,
          "description": "maxiter parameter (int).",
          "type": "integer"
        },
        "robust": {
          "default": "nonrobust",
          "description": "Robust standard-error or covariance estimator option.",
          "type": "string"
        },
        "tol": {
          "default": 1e-08,
          "description": "Numerical convergence tolerance.",
          "type": "number"
        },
        "x": {
          "description": "Primary running variable, regressor, or feature input for this estimator.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Ordered categorical dependent variable.",
          "type": "string"
        }
      },
      "required": [],
      "type": "object"
    },
    "name": "oprobit"
  },
  {
    "description": "Optimal 1:1 matching via the Hungarian algorithm. Validation: certified parity evidence. Do NOT use when: either arm has more than ~10,000 units -- the assignment problem is superquadratic (see cost); use sp.psm or sp.match (greedy nearest-neighbour) at that scale. Cost: Materialises the dense n_treated x n_control distance matrix, then solves a linear sum assignment (Hungarian, ~O(n^3) worst case). Both memory and time degrade sharply past a few thousand units per arm. Assumptions: Unconfoundedness: treatment is as-good-as-random given the measured covariates; Overlap / common support: every unit has a non-degenerate probability of each treatment; The covariate set blocks all back-door paths. Pre-conditions: Pre-treatment covariates measured for treated and control units; A binary (or low-cardinality) treatment indicator; Sufficient covariate overlap between treatment arms. Failure modes: Poor overlap -- extreme propensity scores or few acceptable matches -> Trim or restrict to the common-support region and report the discarded units; Covariate imbalance remains after matching/weighting -> Re-specify the balancing model (CBPS, entropy balancing) and re-check standardized mean dif...",
    "input_schema": {
      "properties": {
        "caliper": {
          "description": "Drop any pair with distance greater than ``caliper``.",
          "type": "number"
        },
        "covariates": {
          "description": "Covariate matrix, DataFrame, or column names.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "metric": {
          "default": "mahalanobis",
          "description": "metric parameter (str).",
          "type": "string"
        },
        "outcome": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        },
        "treatment": {
          "description": "Treatment indicator, treatment variable, or treatment array.",
          "type": "string"
        }
      },
      "required": [
        "covariates",
        "outcome",
        "treatment"
      ],
      "type": "object"
    },
    "name": "optimal_match"
  },
  {
    "description": "Oster (2019) sensitivity to selection on unobservables -- computes the bounding coefficient under the assumption that selection on unobservables (proportional to delta x selection on observables) brings the explained variance to r_max. The breakdown delta tells you how strong unobserved selection has to be to overturn your result. Validation: certified parity evidence. Assumptions: Selection on unobservables is proportional (by factor delta) to selection on observables; r_max upper-bounds the explained variance achievable with all confounders included; Linear functional form for y on (treat, controls). Pre-conditions: you have fitted both a short (treatment-only) and long (treatment + controls) regression of y; long-regression R^2 is meaningfully larger than short-regression R^2. Failure modes: breakdown delta < 1.0 (weak unobservables overturn the result) -> The result is fragile; report the breakdown delta alongside the point estimate; r2_long ~ r2_short (controls add no explanatory power) -> Oster's identified set degenerates when long and short R^2 are nearly equal; use sp.evalue or sp.sensemakr instead. Alternatives: sp.evalue, sp.sensemakr, sp.rosenbaum_bounds. Typical minim...",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "beta_long": {
          "description": "beta_long parameter (float).",
          "type": "number"
        },
        "beta_short": {
          "description": "Short-regression coefficient; if None, fit from data",
          "type": "number"
        },
        "controls": {
          "description": "Control-variable column names.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "delta": {
          "default": 1.0,
          "description": "Ratio of unobserved-to-observed selection (1.0 = equally strong)",
          "type": "number"
        },
        "r2_long": {
          "description": "r2_long parameter (float).",
          "type": "number"
        },
        "r2_short": {
          "description": "r2_short parameter (float).",
          "type": "number"
        },
        "r_max": {
          "description": "Hypothetical R^2 from a regression that includes all unobserved confounders; default 1.3*R^2_long",
          "type": "number"
        },
        "treat": {
          "description": "Treatment indicator or first-treatment-period column.",
          "type": "string"
        },
        "y": {
          "description": "Outcome (alternative to passing beta_short/long directly)",
          "type": "string"
        }
      },
      "required": [],
      "type": "object"
    },
    "name": "oster_bounds"
  },
  {
    "description": "Oster (2019) coefficient stability bounds and delta* computation. Validation: certified parity evidence. Assumptions: Only weak (set-identifying) assumptions are imposed; the result is an interval, not a point; Lee bounds add monotonicity of selection; Oster's delta adds proportional selection on observed vs. unobserved. Pre-conditions: The data needed for the point-identifying analysis, plus the weakest credible identifying restriction; For Lee bounds: a binary selection/attrition indicator. Failure modes: Bounds are too wide to be informative -> Add a credible auxiliary restriction (monotone treatment response, instrument) to tighten the bounds. Alternatives: sp.oster_delta, sp.lee_bounds, sp.manski_bounds. Typical minimum N: 100.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "delta_range": {
          "default": [
            -2.0,
            2.0
          ],
          "description": "Range of proportional selection parameter delta.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "n_boot": {
          "default": 500,
          "description": "Number of bootstrap replications.",
          "type": "integer"
        },
        "n_grid": {
          "default": 200,
          "description": "Grid points for delta in the identified set computation.",
          "type": "integer"
        },
        "r_max": {
          "default": 1.3,
          "description": "Maximum R-squared assumption. A value in ``(R_full, 1]`` is used as R_max itself. A value above 1 cannot be an R-squared and is read as a multiplier of the controlled R-squared, so the default 1.3 gives Oster's recommended ``R_max = min(1, 1.3 * R_full)``; ``<= 0`` means the same. A value in ``(0, R_full]`` falls back to that recommendation with a warning.",
          "type": "number"
        },
        "random_state": {
          "default": 42,
          "description": "Random seed or RandomState for reproducible stochastic steps.",
          "type": "integer"
        },
        "x_base": {
          "description": "Key treatment/variable(s) of interest.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "x_controls": {
          "description": "Additional controls whose inclusion tightens identification.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Outcome variable.",
          "type": "string"
        }
      },
      "required": [
        "x_base",
        "x_controls",
        "y"
      ],
      "type": "object"
    },
    "name": "oster_delta"
  },
  {
    "description": "Mirrored density plot of propensity scores by treatment group.",
    "input_schema": {
      "properties": {
        "ax": {
          "description": "Axes to plot on. If None, a new figure is created.",
          "type": "string"
        },
        "covariates": {
          "description": "Covariates for PS estimation (ignored if *ps* supplied).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "figsize": {
          "default": [
            8,
            4
          ],
          "description": "Figure size (width, height).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "method": {
          "default": "logit",
          "description": "PS estimation method if *ps* is None.",
          "type": "string"
        },
        "ps": {
          "description": "Pre-estimated propensity scores.",
          "type": "string"
        },
        "title": {
          "default": "Propensity Score Overlap",
          "description": "Plot title.",
          "type": "string"
        },
        "treatment": {
          "description": "Binary treatment column.",
          "type": "string"
        }
      },
      "required": [
        "covariates",
        "treatment"
      ],
      "type": "object"
    },
    "name": "overlap_plot"
  },
  {
    "description": "Overlap-weighted 2x2 DiD. Weights observations by e(X)(1-e(X)), where e(X) is the estimated propensity score, placing highest weight on units with the most overlap between treated and control covariate distributions. Useful when overlap is poor at the tails. Assumptions: Overlap weights target the sub-population with positive overlap; Correct PS model OR outcome model for DR variant; Conditional parallel trends given covariates X after overlap weighting. Pre-conditions: 2 periods, binary treat; covariates with variation; Two-period panel with a binary treat indicator and a binary pre/post time indicator. Failure modes: Propensity scores pile up near 0/1 giving near-zero overlap weights and unstable ATT -> Trim extreme-PS units or simplify the covariate set to restore common support; ps_model='dl' requested but the deep-learning propensity backend fails to converge -> Fall back to ps_model='logit' or 'gbm' for a more stable propensity estimate; More than two time periods passed to this 2x2-only estimator -> Collapse to a single pre/post contrast or use a staggered/multi-period estimator. Alternatives: sp.drdid, sp.did_2x2, sp.did, sp.callaway_santanna. Typical minimum N: 200.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "covariates": {
          "description": "Covariates X",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "ps_model": {
          "default": "logit",
          "description": "Propensity score model",
          "enum": [
            "logit",
            "rf",
            "gbm",
            "dl"
          ],
          "type": "string"
        },
        "time": {
          "description": "Time period column.",
          "type": "string"
        },
        "treat": {
          "description": "Binary treatment indicator",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "covariates",
        "time",
        "treat",
        "y"
      ],
      "type": "object"
    },
    "name": "overlap_weighted_did"
  },
  {
    "description": "Overlap-weight (ATO) treatment effect estimator. Validation: certified parity evidence. Assumptions: Unconfoundedness: treatment is as-good-as-random given the measured covariates; Overlap / common support: every unit has a non-degenerate probability of each treatment; The covariate set blocks all back-door paths. Pre-conditions: Pre-treatment covariates measured for treated and control units; A binary (or low-cardinality) treatment indicator; Sufficient covariate overlap between treatment arms. Failure modes: Poor overlap -- extreme propensity scores or few acceptable matches -> Trim or restrict to the common-support region and report the discarded units; Covariate imbalance remains after matching/weighting -> Re-specify the balancing model (CBPS, entropy balancing) and re-check standardized mean differences. Alternatives: sp.propensity_score, sp.cbps, sp.ebalance, sp.dml. Typical minimum N: 200.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "covariates": {
          "description": "Covariates for the logistic propensity-score model.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "estimand": {
          "default": "ATO",
          "description": "Which generalized-weight scheme to use. All follow Li-Li-Li (2019) Table 1; 'ATO' uses the overlap weights; 'matching' uses the ``min(e, 1-e)`` weight; 'entropy' uses ``-e*log(e) - (1-e)*log(1-e)``; 'ATE/ATT/ATC' reduce to standard IPW for comparison.",
          "enum": [
            "ATO",
            "ATE",
            "ATT",
            "ATC",
            "matching",
            "entropy"
          ],
          "type": "string"
        },
        "n_bootstrap": {
          "default": 500,
          "description": "Paired-sample bootstrap replications for SE.",
          "type": "integer"
        },
        "seed": {
          "description": "Random seed for reproducible stochastic steps.",
          "type": "integer"
        },
        "treat": {
          "description": "Binary 0/1 treatment column.",
          "type": "string"
        },
        "trim": {
          "default": 0.0,
          "description": "Optional clip of pscore to ``[trim, 1-trim]``. For overlap weights this is rarely needed -- set to 0 by default.",
          "type": "number"
        },
        "y": {
          "description": "Outcome column.",
          "type": "string"
        }
      },
      "required": [
        "covariates",
        "treat",
        "y"
      ],
      "type": "object"
    },
    "name": "overlap_weights"
  },
  {
    "description": "Unified panel regression: FE, RE, between, FD, pooled OLS, two-way FE, Mundlak/Chamberlain CRE, Arellano-Bond, Blundell-Bond system GMM. Results include built-in diagnostics: .hausman_test(), .bp_lm_test(), .f_test_effects(), .pesaran_cd_test(), .compare(method). Validation: certified parity evidence. Assumptions: Static FE: strict exogeneity of regressors conditional on unit fixed effects (E[u_it | x_i, alpha_i] = 0); Random effects: unit effect uncorrelated with regressors; relax with Mundlak / Chamberlain; Dynamic GMM: weak exogeneity and no second-order serial correlation in differenced errors. Pre-conditions: Data is a long-format panel keyed by (entity, time) with at least 2 time periods per entity; Outcome and regressors are numeric or properly encoded; Method-specific structure satisfied (e.g. dynamic GMM needs T moderate, system GMM needs initial-condition validity). Failure modes: Hausman test rejects RE -> Switch to fixed effects (method='fe') or correlated random effects (method='mundlak'); Few clusters (< 30) inflate Type I error with cluster-robust SEs -> Use wild-cluster bootstrap or CR2/CR3 small-sample corrections; High-dimensional fixed effects make the design si...",
    "input_schema": {
      "properties": {
        "cluster": {
          "description": "Cluster variable: entity, time, or twoway",
          "type": "string"
        },
        "entity": {
          "description": "Unit identifier column",
          "type": "string"
        },
        "formula": {
          "description": "Regression formula: 'y ~ x1 + x2'",
          "type": "string"
        },
        "gmm_lags": {
          "default": "(2, 5)",
          "description": "GMM instrument lag range",
          "type": "string"
        },
        "lags": {
          "default": 1,
          "description": "AR lags for dynamic panel (ab/system)",
          "type": "integer"
        },
        "method": {
          "default": "fe",
          "description": "Estimation method",
          "enum": [
            "fe",
            "re",
            "be",
            "fd",
            "pooled",
            "twoway",
            "mundlak",
            "cre",
            "chamberlain",
            "ab",
            "system"
          ],
          "type": "string"
        },
        "robust": {
          "default": "nonrobust",
          "description": "Standard errors: nonrobust, robust, kernel, driscoll-kraay",
          "type": "string"
        },
        "time": {
          "description": "Time column",
          "type": "string"
        },
        "twostep": {
          "default": false,
          "description": "Two-step GMM",
          "type": "boolean"
        }
      },
      "required": [
        "entity",
        "formula",
        "time"
      ],
      "type": "object"
    },
    "name": "panel"
  },
  {
    "description": "Estimate the same model with multiple panel methods and return a side-by-side comparison table.",
    "input_schema": {
      "properties": {
        "entity": {
          "description": "Panel entity identifier column.",
          "type": "string"
        },
        "formula": {
          "description": "Model formula using patsy/R-style syntax.",
          "type": "string"
        },
        "methods": {
          "description": "List of methods to compare, default: pooled/fe/re/twoway/mundlak",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "time": {
          "description": "Time period column.",
          "type": "string"
        }
      },
      "required": [
        "entity",
        "formula",
        "time"
      ],
      "type": "object"
    },
    "name": "panel_compare"
  },
  {
    "description": "Panel FGLS (Feasible Generalized Least Squares). Validation: certified parity evidence.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "corr": {
          "default": "independent",
          "description": "Within-panel correlation: 'independent', 'ar1' (panel-specific AR(1)), 'psar1' (common AR(1)).",
          "type": "string"
        },
        "id": {
          "default": "id",
          "description": "Panel identifier.",
          "type": "string"
        },
        "igls": {
          "default": false,
          "description": "Iterate the variance estimates to convergence (Stata's ``igls``). The default is the two-step estimator Stata's ``xtgls`` reports without that option. .. versionchanged:: 1.27.0 This function iterated unconditionally, so its default was Stata's ``igls`` while the docstring claimed equivalence to the plain command. On a balanced N=60, T=12 panel under ``panels(hetero)`` the two differ by 2.8% on the slope. Pass ``igls=True`` for the previous behaviour.",
          "type": "boolean"
        },
        "maxiter": {
          "default": 100,
          "description": "maxiter parameter (int).",
          "type": "integer"
        },
        "panels": {
          "default": "heteroskedastic",
          "description": "Error structure across panels: 'homoskedastic', 'heteroskedastic', 'correlated' (cross-sectional).",
          "type": "string"
        },
        "time": {
          "default": "time",
          "description": "Time identifier.",
          "type": "string"
        },
        "tol": {
          "default": 1e-06,
          "description": "Numerical convergence tolerance.",
          "type": "number"
        },
        "x": {
          "description": "Regressors.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Dependent variable.",
          "type": "string"
        }
      },
      "required": [
        "x",
        "y"
      ],
      "type": "object"
    },
    "name": "panel_fgls"
  },
  {
    "description": "Panel logit model. Validation: certified parity evidence.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals.",
          "type": "number"
        },
        "cluster": {
          "description": "Column for cluster-robust SEs.",
          "type": "string"
        },
        "id": {
          "default": "id",
          "description": "Unit and time identifier columns.",
          "type": "string"
        },
        "maxiter": {
          "default": 200,
          "description": "Maximum optimizer iterations.",
          "type": "integer"
        },
        "method": {
          "default": "fe",
          "description": "'fe' (conditional FE logit), 're' (random effects), 'cre' (Mundlak).",
          "type": "string"
        },
        "n_quadrature": {
          "default": 12,
          "description": "Gauss-Hermite quadrature points (RE/CRE only).",
          "type": "integer"
        },
        "robust": {
          "default": "nonrobust",
          "description": "'nonrobust' or 'robust'.",
          "type": "string"
        },
        "time": {
          "default": "time",
          "description": "Unit and time identifier columns.",
          "type": "string"
        },
        "tol": {
          "default": 1e-08,
          "description": "Gradient tolerance.",
          "type": "number"
        },
        "x": {
          "description": "Regressors.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Binary dependent variable (0/1).",
          "type": "string"
        }
      },
      "required": [
        "x",
        "y"
      ],
      "type": "object"
    },
    "name": "panel_logit"
  },
  {
    "description": "Panel probit model. Validation: certified parity evidence.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals.",
          "type": "number"
        },
        "cluster": {
          "description": "Column for cluster-robust SEs.",
          "type": "string"
        },
        "id": {
          "default": "id",
          "description": "Unit and time identifier columns.",
          "type": "string"
        },
        "maxiter": {
          "default": 200,
          "description": "Maximum optimizer iterations.",
          "type": "integer"
        },
        "method": {
          "default": "re",
          "description": "'re' (random effects) or 'cre' (Mundlak). FE probit not supported (incidental parameters problem).",
          "type": "string"
        },
        "n_quadrature": {
          "default": 12,
          "description": "Gauss-Hermite quadrature points.",
          "type": "integer"
        },
        "robust": {
          "default": "nonrobust",
          "description": "'nonrobust' or 'robust'.",
          "type": "string"
        },
        "time": {
          "default": "time",
          "description": "Unit and time identifier columns.",
          "type": "string"
        },
        "tol": {
          "default": 1e-08,
          "description": "Gradient tolerance.",
          "type": "number"
        },
        "x": {
          "description": "Regressors.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Binary dependent variable (0/1).",
          "type": "string"
        }
      },
      "required": [
        "x",
        "y"
      ],
      "type": "object"
    },
    "name": "panel_probit"
  },
  {
    "description": "Callaway & Li (2019) quantile treatment effect on the treated for panel data. Recovers the counterfactual DISTRIBUTION of untreated outcomes for the treated group via distributional DiD plus a copula-stability assumption. Needs a balanced THREE-period panel (the third period identifies the copula). Exact parity with R qte::panel.qtet (6.8e-12). Validation: certified parity evidence. Assumptions: Distributional DiD; Copula stability: the dependence between the period-t change and the period-(t-1) level equals that between the period-(t-1) change and the period-(t-2) level, for the treated. Untestable at t, but checked on the untreated group and reported in model_info['copula_check']; Continuous outcome: with mass points the rank map is not measure-preserving; model_info['coherence_check'] flags it. Pre-conditions: balanced panel over three periods; binary treatment, read at period t; continuous outcome (mass points distort the rank map). Failure modes: Outcome has mass points (e.g. many zero earnings) -> The rank map collapses tied units onto one value and the QTT curve is distorted; the reported ATT (a mean DiD) is unaffected. Use sp.cic bounds for discrete outcomes; Only two peri...",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "n_boot": {
          "default": 200,
          "description": "Number of bootstrap replications.",
          "type": "integer"
        },
        "quantiles": {
          "description": "quantiles parameter (list).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "se": {
          "default": "bootstrap",
          "description": "SE method",
          "enum": [
            "bootstrap",
            "none"
          ],
          "type": "string"
        },
        "seed": {
          "default": 0,
          "description": "Random seed for reproducible stochastic steps.",
          "type": "integer"
        },
        "t": {
          "description": "Post-period VALUE of time",
          "type": "string"
        },
        "time": {
          "description": "Period",
          "type": "string"
        },
        "tmin1": {
          "description": "Pre-period VALUE of time",
          "type": "string"
        },
        "tmin2": {
          "description": "Pre-pre-period VALUE; identifies the copula",
          "type": "string"
        },
        "treat": {
          "description": "Binary treatment",
          "type": "string"
        },
        "unit": {
          "description": "Unit id",
          "type": "string"
        },
        "y": {
          "description": "Outcome",
          "type": "string"
        }
      },
      "required": [
        "t",
        "time",
        "tmin1",
        "tmin2",
        "treat",
        "unit",
        "y"
      ],
      "type": "object"
    },
    "name": "panel_qtet"
  },
  {
    "description": "Panel unit root test. Validation: certified parity evidence.",
    "input_schema": {
      "properties": {
        "convention": {
          "default": "stata",
          "description": "Where Stata ``xtunitroot`` and ``plm::purtest`` differ: * IPS: the IPS Table-3 moments are looked up at T = observations per panel (Stata) or T - lags - 1 (plm); * LLC: the LLC mean/std adjustments at T~ = T - lags - 1 (Stata) or T (plm); long-run-variance bandwidth ``int(3.21 T^(1/3))`` (Stata) or ``round(3.21 T^(1/3))`` (plm); Stata normalises by the short-run variance rss/n and uses the se with divisor N (plm with ``dfcor = FALSE`` is identical on those two).",
          "enum": [
            "stata",
            "plm"
          ],
          "type": "string"
        },
        "dfcor": {
          "description": "Residual variance of the per-unit ADF regressions (IPS, Fisher, plm-LLC) and of the Hadri residuals: rss/(n - K) if True, rss/n if False. Default True for ``convention='stata'`` (OLS, as Stata's ``regress``) and False for ``'plm'`` (plm's default).",
          "type": "boolean"
        },
        "id": {
          "default": "id",
          "description": "Unit identifier.",
          "type": "string"
        },
        "lags": {
          "description": "Number of lagged differences in every ADF regression (Stata ``lags()``, plm ``lags = <int>``). If None, uses ``int(4 * (T / 100) ** 0.25)`` per unit (not an information criterion). Ignored by 'hadri'.",
          "type": "integer"
        },
        "robust": {
          "default": true,
          "description": "Hadri only: heteroskedasticity-consistent version (unit-specific variances; Stata ``hadri, robust``, plm ``Hcons = TRUE``). False pools the variance (Stata default, plm ``Hcons = FALSE``).",
          "type": "boolean"
        },
        "test": {
          "default": "ips",
          "description": "Test type: 'llc' (Levin-Lin-Chu), 'ips' (Im-Pesaran-Shin W-t-bar), 'fisher' (Maddala-Wu inverse chi-squared of the per-unit ADF p-values; the inverse-normal, inverse-logit and modified statistics are in ``details``), 'hadri' (stationarity LM test).",
          "type": "string"
        },
        "time": {
          "default": "time",
          "description": "Time identifier.",
          "type": "string"
        },
        "trend": {
          "default": "c",
          "description": "'n' (none), 'c' (constant), 'ct' (constant + trend). IPS and Hadri require 'c' or 'ct'.",
          "type": "string"
        },
        "variable": {
          "description": "Variable to test.",
          "type": "string"
        }
      },
      "required": [
        "variable"
      ],
      "type": "object"
    },
    "name": "panel_unitroot"
  },
  {
    "description": "panelView-style display of a panel's treatment status and outcomes.",
    "input_schema": {
      "properties": {
        "ax": {
          "description": "ax parameter.",
          "type": "string"
        },
        "figsize": {
          "default": [
            10,
            6
          ],
          "description": "figsize parameter (Tuple[float, float]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "time": {
          "description": "Unit and time identifiers.",
          "type": "string"
        },
        "treat": {
          "description": "0/1 treatment status in each unit-period.",
          "type": "string"
        },
        "type": {
          "default": "treat",
          "description": "``'treat'`` draws the unit-by-period treatment-status tiles (``sp.treatment_rollout_plot``); ``'outcome'`` draws every unit's outcome path, treated periods highlighted, with the mean path of the never-treated units.",
          "enum": [
            "treat",
            "outcome"
          ],
          "type": "string"
        },
        "unit": {
          "description": "Unit and time identifiers.",
          "type": "string"
        },
        "y": {
          "description": "Outcome column, required for ``type=\"outcome\"``.",
          "type": "string"
        }
      },
      "required": [
        "time",
        "treat",
        "unit"
      ],
      "type": "object"
    },
    "name": "panel_view"
  },
  {
    "description": "Plot raw outcome means over time for treatment and control groups.",
    "input_schema": {
      "properties": {
        "agg": {
          "default": "mean",
          "description": "Aggregation function: 'mean' or 'median'.",
          "type": "string"
        },
        "ax": {
          "description": "Existing axes to plot on.",
          "type": "string"
        },
        "ci": {
          "default": true,
          "description": "Show 95% confidence intervals (+/-1.96 SE of mean).",
          "type": "boolean"
        },
        "colors": {
          "description": "Colors for (treatment, control). Default: ('#E74C3C', '#2C3E50').",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "figsize": {
          "default": [
            10,
            6
          ],
          "description": "Figure size.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "id": {
          "description": "Unit identifier (for panel data).",
          "type": "string"
        },
        "labels": {
          "description": "Custom labels, e.g. ``{'treat': 'New Jersey', 'control': 'Pennsylvania'}``.",
          "type": "object"
        },
        "time": {
          "description": "Time period variable.",
          "type": "string"
        },
        "title": {
          "description": "Plot title.",
          "type": "string"
        },
        "treat": {
          "description": "Treatment group indicator. Binary (0/1) for 2x2, or first-treatment-period for staggered (0 = never treated).",
          "type": "string"
        },
        "treat_time": {
          "description": "Treatment onset time. Draws a vertical line if provided.",
          "type": "integer"
        },
        "y": {
          "description": "Outcome variable.",
          "type": "string"
        }
      },
      "required": [
        "time",
        "treat",
        "y"
      ],
      "type": "object"
    },
    "name": "parallel_trends_plot"
  },
  {
    "description": "Run the full parallel-trends robustness pipeline on a DiD result. Validation: validated evidence tier (known-truth, reference, external-parity, or Monte Carlo artifact).",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level.",
          "type": "number"
        },
        "delta": {
          "description": "Hypothesised pre-trend violation passed to ``pretrends_power``.",
          "type": "string"
        },
        "e": {
          "default": 0,
          "description": "Relative time whose effect the sensitivity analysis targets.",
          "type": "integer"
        },
        "families": {
          "default": [
            "SD",
            "RM"
          ],
          "description": "Restriction families. ``\"SD\"`` maps to ``honest_did``'s ``method='smoothness'`` (bounded second differences); ``\"RM\"`` maps to ``method='relative_magnitude'``.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "m_grid": {
          "description": "Grid of violation magnitudes. Default: the ``honest_did`` default, multiples of the standard error at ``e``.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "result": {
          "description": "A fitted DiD/event-study result carrying event-study estimates in ``result.model_info['event_study']`` (e.g. from ``sp.event_study``, ``sp.callaway_santanna``, ``sp.sun_abraham``). .. note:: If the result does not carry a pre-period covariance matrix in ``model_info['vcv_pre']``, the pre-trend test and power fall back to assuming the pre-period coefficients are mutually independent and warn loudly. ``sp.event_study`` computes the full cluster-robust covariance; pass ``expose_pre_vcov=True`` to it to have this pipeline use the correct covariance instead of the diagonal fallback.",
          "type": "string"
        }
      },
      "required": [
        "result"
      ],
      "type": "object"
    },
    "name": "parallel_trends_robustness"
  },
  {
    "description": "Partial-correlation p-value for H0: ``X Y | Z``.",
    "input_schema": {
      "properties": {
        "Z": {
          "description": "Instrument matrix or auxiliary covariate matrix.",
          "type": "string"
        },
        "x": {
          "description": "Primary running variable, regressor, or feature input for this estimator.",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "x",
        "y"
      ],
      "type": "object"
    },
    "name": "partial_corr_pvalue"
  },
  {
    "description": "Partial identification of ATE -- article alias for the ``bounds`` module. Assumptions: Only weak (set-identifying) assumptions are imposed; the result is an interval, not a point; Lee bounds add monotonicity of selection; Oster's delta adds proportional selection on observed vs. unobserved. Pre-conditions: The data needed for the point-identifying analysis, plus the weakest credible identifying restriction; For Lee bounds: a binary selection/attrition indicator. Failure modes: Bounds are too wide to be informative -> Add a credible auxiliary restriction (monotone treatment response, instrument) to tighten the bounds. Alternatives: sp.oster_delta, sp.lee_bounds, sp.manski_bounds. Typical minimum N: 100.",
    "input_schema": {
      "properties": {
        "X": {
          "description": "Feature matrix or covariate DataFrame.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "assumptions": {
          "description": "assumptions parameter (Optional[List[str]]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "d": {
          "description": "d parameter (str).",
          "type": "string"
        },
        "instrument": {
          "description": "instrument parameter (Optional[str]).",
          "type": "string"
        },
        "method": {
          "default": "manski",
          "description": "Estimator or algorithm variant to use.",
          "type": "string"
        },
        "selection": {
          "description": "selection parameter (Optional[str]).",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "d",
        "y"
      ],
      "type": "object"
    },
    "name": "partial_identification"
  },
  {
    "description": "Estimate the Population Average Treatment Effect (PATE). Validation: validated evidence tier (known-truth, reference, external-parity, or Monte Carlo artifact).",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for the confidence interval.",
          "type": "number"
        },
        "covariates": {
          "description": "Shared covariates present in both datasets.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "data_experiment": {
          "description": "Experimental/study sample. Must contain *y*, *treatment*, and all *covariates*.",
          "type": "string"
        },
        "data_target": {
          "description": "Target population sample. Must contain all *covariates*. Need not contain *y* or *treatment*.",
          "type": "string"
        },
        "method": {
          "default": "ipw",
          "description": "Estimation strategy: * ``'ipw'`` -- Inverse probability of sampling weights. * ``'aipw'`` -- Augmented IPW (doubly robust). * ``'calibration'`` -- Entropy balancing on covariate moments.",
          "enum": [
            "ipw",
            "aipw",
            "calibration"
          ],
          "type": "string"
        },
        "n_boot": {
          "default": 500,
          "description": "Number of bootstrap replications for standard-error estimation.",
          "type": "integer"
        },
        "seed": {
          "description": "Random seed for reproducibility.",
          "type": "integer"
        },
        "treatment": {
          "description": "Binary treatment indicator (only in data_experiment).",
          "type": "string"
        },
        "trim": {
          "default": 0.01,
          "description": "Trimming threshold for participation propensities (values below *trim* or above 1 - *trim* are clipped).",
          "type": "number"
        },
        "y": {
          "description": "Outcome variable (only used from data_experiment).",
          "type": "string"
        }
      },
      "required": [
        "covariates",
        "data_experiment",
        "data_target",
        "treatment",
        "y"
      ],
      "type": "object"
    },
    "name": "pate"
  },
  {
    "description": "Learn causal structure using the PC algorithm. Validation: validated evidence tier (known-truth, reference, external-parity, or Monte Carlo artifact). Assumptions: Causal Markov condition and faithfulness (PC/GES/FCI); Causal sufficiency for PC/GES (no latent confounders); FCI relaxes this; Acyclicity; LiNGAM additionally assumes a linear non-Gaussian model. Pre-conditions: Constraint-/score-based discovery needs i.i.d. observational data with enough samples for reliable conditional-independence tests; Invariance-based discovery (ICP) needs data labelled by environment / intervention. Failure modes: Unstable skeleton / many undirected edges -- faithfulness or sample size is the likely culprit -> Increase the sample, relax the CI-test threshold, or switch to FCI if latent confounders are plausible. Alternatives: sp.pc_algorithm, sp.fci, sp.ges, sp.lingam. Typical minimum N: 500.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for conditional independence tests. Lower alpha = sparser graph (fewer edges).",
          "type": "number"
        },
        "ci_test": {
          "default": "fisherz",
          "description": "Conditional independence test: 'fisherz' (partial correlation) or 'hsic' (kernel-based, non-linear).",
          "type": "string"
        },
        "forbidden": {
          "description": "Background knowledge: edges that must NOT appear in the final graph (treated as undirected -- both ``(a, b)`` and ``(b, a)`` are forbidden when either is given). The skeleton phase keeps these absent regardless of CI test outcomes.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "max_cond_size": {
          "description": "Maximum conditioning set size. If None, goes up to d-2.",
          "type": "integer"
        },
        "required": {
          "description": "Background knowledge: directed edges ``a -> b`` that must appear in the CPDAG. The skeleton phase preserves them regardless of CI rejection, and the orientation phase pins their direction.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "variables": {
          "description": "Column names to use. If None, uses all numeric columns.",
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "required": [],
      "type": "object"
    },
    "name": "pc_algorithm"
  },
  {
    "description": "Proximal causal inference for modified treatment policies (Park & Ying 2025). Estimates the effect of a policy that shifts the treatment distribution (e.g., raises the dose by 10%) under unobserved confounding identified by PCI. Assumptions: The proxies are valid negative controls (relevant to the confounder, excluded from the causal channel); A bridge function exists (completeness conditions hold). Pre-conditions: Treatment-inducing and outcome-inducing proxy variables (negative controls) for the unobserved confounder. Failure modes: Proxies are weak or invalid -- the bridge function is poorly identified -> Test proxy relevance, select stronger proxies, or fall back to sensitivity analysis. Alternatives: sp.select_pci_proxies, sp.dml. Typical minimum N: 500.",
    "input_schema": {
      "properties": {
        "delta": {
          "description": "Additive shift applied to the treatment under the modified policy",
          "type": "number"
        },
        "proxy_w": {
          "description": "proxy_w parameter (list).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "proxy_z": {
          "description": "proxy_z parameter (list).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "treat": {
          "description": "Treatment indicator or first-treatment-period column.",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "delta",
        "proxy_w",
        "proxy_z",
        "treat",
        "y"
      ],
      "type": "object"
    },
    "name": "pci_mtp"
  },
  {
    "description": "PCMCI causal discovery for stationary time-series. Assumptions: Causal Markov condition and faithfulness (PC/GES/FCI); Causal sufficiency for PC/GES (no latent confounders); FCI relaxes this; Acyclicity; LiNGAM additionally assumes a linear non-Gaussian model. Pre-conditions: Constraint-/score-based discovery needs i.i.d. observational data with enough samples for reliable conditional-independence tests; Invariance-based discovery (ICP) needs data labelled by environment / intervention. Failure modes: Unstable skeleton / many undirected edges -- faithfulness or sample size is the likely culprit -> Increase the sample, relax the CI-test threshold, or switch to FCI if latent confounders are plausible. Alternatives: sp.pc_algorithm, sp.fci, sp.ges, sp.lingam. Typical minimum N: 500.",
    "input_schema": {
      "properties": {
        "ci_test": {
          "description": "Custom CI test ``(x, y, Z) -> p_value``. Defaults to :func:`partial_corr_pvalue`.",
          "type": "number"
        },
        "max_conds_dim": {
          "description": "Hard cap on the conditioning-set size during PC1. ``None`` means no cap (the algorithm stops automatically when no predictors remain).",
          "type": "integer"
        },
        "mci_alpha": {
          "description": "Significance threshold for the final MCI adjacency. Defaults to ``pc_alpha``.",
          "type": "number"
        },
        "pc_alpha": {
          "default": 0.05,
          "description": "Significance threshold used during the PC1 selection stage.",
          "type": "number"
        },
        "tau_max": {
          "default": 3,
          "description": "Maximum lag to consider for parent candidates.",
          "type": "integer"
        },
        "variables": {
          "description": "Columns to use. Defaults to all columns.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "verbose": {
          "default": false,
          "description": "verbose parameter (bool).",
          "type": "boolean"
        }
      },
      "required": [],
      "type": "object"
    },
    "name": "pcmci"
  },
  {
    "description": "End-to-end DID workflow: preflight -> did/CS estimator -> audit -> honest-DID sensitivity -> bacon decomposition -> brief. Returns one markdown report + the primary result_id. Use this when the user pastes a DID dataset and asks 'is the effect real?' -- the pipeline runs every diagnostic the literature expects.",
    "input_schema": {
      "properties": {
        "cohort": {
          "description": "First-treatment cohort column. When supplied, dispatches callaway_santanna instead of classic 2x2 did.",
          "type": "string"
        },
        "covariates": {
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "id": {
          "description": "Unit id (panel) -- required for staggered-DID.",
          "type": "string"
        },
        "time": {
          "description": "Time column.",
          "type": "string"
        },
        "treat": {
          "description": "Binary treatment indicator.",
          "type": "string"
        },
        "y": {
          "description": "Outcome column.",
          "type": "string"
        }
      },
      "required": [
        "y",
        "treat",
        "time"
      ],
      "type": "object"
    },
    "name": "pipeline_did"
  },
  {
    "description": "End-to-end IV workflow: ivreg -> first-stage F (effective + Olea-Pflueger) -> Anderson-Rubin CI -> e-value. Returns one markdown report + result_id.",
    "input_schema": {
      "properties": {
        "formula": {
          "description": "'y ~ x_exog + (d_endog ~ z_instrument)' style.",
          "type": "string"
        }
      },
      "required": [
        "formula"
      ],
      "type": "object"
    },
    "name": "pipeline_iv"
  },
  {
    "description": "End-to-end RD workflow: rdrobust -> rdplot (PNG image) -> rddensity (McCrary) -> rdsensitivity (bandwidth). Returns one markdown report + result_id + an image content block.",
    "input_schema": {
      "properties": {
        "c": {
          "default": 0.0,
          "description": "Cutoff value.",
          "type": "number"
        },
        "fuzzy": {
          "description": "Treatment column for fuzzy RD (optional).",
          "type": "string"
        },
        "x": {
          "description": "Running variable column.",
          "type": "string"
        },
        "y": {
          "type": "string"
        }
      },
      "required": [
        "y",
        "x"
      ],
      "type": "object"
    },
    "name": "pipeline_rd"
  },
  {
    "description": "Render the canonical diagnostic plot for a fitted result and return it as an inline PNG image content block. MCP clients with vision (Claude Desktop, vision-capable agents) get the plot for free; clients that don't support image content see only the JSON metadata. Plot kind is auto-selected from the result type: event-study for DID, rdplot for RD, gap plot for synth, balance plot for matching, ROC for classification, etc.",
    "input_schema": {
      "properties": {
        "figsize": {
          "description": "Width, height in inches (default [8,5]).",
          "items": {
            "type": "number"
          },
          "type": "array"
        },
        "kind": {
          "description": "Override the auto-detected plot kind. Common values: 'event_study', 'rdplot', 'synth_gap', 'love_plot', 'coef_plot'.",
          "type": "string"
        },
        "result_id": {
          "description": "Handle to a fitted result.",
          "type": "string"
        }
      },
      "required": [
        "result_id"
      ],
      "type": "object"
    },
    "name": "plot_from_result"
  },
  {
    "description": "Poisson regression via MLE (IRLS). Validation: certified parity evidence.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals.",
          "type": "number"
        },
        "cluster": {
          "description": "Variable name for clustered standard errors.",
          "type": "string"
        },
        "exposure": {
          "description": "Exposure variable (will be logged and used as offset).",
          "type": "string"
        },
        "formula": {
          "description": "Model formula, e.g. \"y ~ x1 + x2\".",
          "type": "string"
        },
        "irr": {
          "default": false,
          "description": "If True, report Incidence Rate Ratios (exp(beta)) instead of raw coefficients.",
          "type": "boolean"
        },
        "maxiter": {
          "default": 100,
          "description": "Maximum IRLS iterations.",
          "type": "integer"
        },
        "offset": {
          "description": "Offset variable (log of exposure already computed).",
          "type": "string"
        },
        "robust": {
          "default": "nonrobust",
          "description": "Standard error type: \"nonrobust\", \"robust\"/\"hc0\", \"hc1\".",
          "type": "string"
        },
        "tol": {
          "default": 1e-08,
          "description": "Convergence tolerance.",
          "type": "number"
        },
        "weights": {
          "description": "Frequency/analytic weight variable.",
          "type": "string"
        },
        "x": {
          "description": "Independent variable names (alternative to formula).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Dependent variable name (alternative to formula).",
          "type": "string"
        }
      },
      "required": [],
      "type": "object"
    },
    "name": "poisson"
  },
  {
    "description": "Rank-and-treat policy under a budget constraint.",
    "input_schema": {
      "properties": {
        "budget": {
          "description": "Maximum number of units that can be treated. Mutually exclusive with ``frac``.",
          "type": "integer"
        },
        "cate": {
          "description": "Per-unit effect estimates -- a raw array, a ``metalearner()`` / ``tarnet()`` result, or a fitted ``causal_forest()`` model (training-sample effects are used).",
          "type": "string"
        },
        "frac": {
          "description": "Maximum *fraction* of units that can be treated (in ``(0, 1]``).",
          "type": "number"
        },
        "min_effect": {
          "default": 0.0,
          "description": "Never treat a unit whose predicted effect is at or below this threshold, even with budget left over -- treating predicted non-responders wastes budget and can do harm.",
          "type": "number"
        }
      },
      "required": [
        "cate"
      ],
      "type": "object"
    },
    "name": "policy_targeting"
  },
  {
    "description": "Doubly-robust policy-tree -- article-facing alias. Validation: certified parity evidence.",
    "input_schema": {
      "properties": {
        "X": {
          "description": "Feature matrix or covariate DataFrame.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "covariates": {
          "description": "Covariate matrix, DataFrame, or column names.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "d": {
          "description": "d parameter (Optional[str]).",
          "type": "string"
        },
        "depth": {
          "description": "depth parameter (Optional[int]).",
          "type": "integer"
        },
        "max_depth": {
          "description": "max_depth parameter (Optional[int]).",
          "type": "integer"
        },
        "treat": {
          "description": "Treatment indicator or first-treatment-period column.",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "y"
      ],
      "type": "object"
    },
    "name": "policy_tree"
  },
  {
    "description": "Evaluate the value of a treatment policy relative to treating no one. Validation: certified parity evidence. Assumptions: The relationship between the treated unit and controls is stable absent the intervention (causal_impact); Matrix-completion: the untreated potential outcomes follow a low-rank factor structure with treatment as the missingness pattern; No concurrent intervention affects the controls. Pre-conditions: A treated unit with a pre-period and a set of control series, or a panel with a low-rank structure. Failure modes: Pre-period fit is poor or controls are themselves affected by the intervention -> Re-select controls, lengthen the pre-period, or use synthetic-control / DiD diagnostics. Alternatives: sp.synth, sp.sdid, sp.gsynth. Typical minimum N: 50.",
    "input_schema": {
      "properties": {
        "policy": {
          "description": "Policy recommendations (0 or 1; a probability of treatment gives the value of the stochastic policy). Same length as ``scores``, or a scalar for a constant policy.",
          "type": "string"
        },
        "scores": {
          "description": "Doubly robust scores (AIPW pseudo-outcomes for treatment). Positive scores indicate the individual benefits from treatment. Must be one-dimensional.",
          "type": "string"
        }
      },
      "required": [
        "policy",
        "scores"
      ],
      "type": "object"
    },
    "name": "policy_value"
  },
  {
    "description": "Return the post-estimation actions supported by *result*.",
    "input_schema": {
      "properties": {
        "include_diagnostics": {
          "default": true,
          "description": "Include scalar diagnostics from ``model_info`` / ``diagnostics``.",
          "type": "boolean"
        },
        "result": {
          "description": "Fitted StatsPAI result, estimator, or compatible object.",
          "type": "string"
        }
      },
      "required": [
        "result"
      ],
      "type": "object"
    },
    "name": "postestimation_contract"
  },
  {
    "description": "Return the post-estimation actions supported by *result*.",
    "input_schema": {
      "properties": {
        "include_diagnostics": {
          "default": true,
          "description": "Include scalar diagnostics from ``model_info`` / ``diagnostics``.",
          "type": "boolean"
        },
        "result": {
          "description": "Fitted StatsPAI result, estimator, or compatible object.",
          "type": "string"
        }
      },
      "required": [
        "result"
      ],
      "type": "object"
    },
    "name": "postestimation_report"
  },
  {
    "description": "Prediction-powered estimate of a population mean.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "CI level (1 - alpha confidence).",
          "type": "number"
        },
        "tune": {
          "default": true,
          "description": "Use the PPI++ power-tuning weight ``lambda in [0, 1]``. ``False`` fixes ``lambda = 1`` (the original PPI estimator).",
          "type": "boolean"
        },
        "y": {
          "description": "Gold-standard (human) outcomes on the labeled sample.",
          "type": "string"
        },
        "yhat": {
          "description": "Model predictions on the *same* labeled rows.",
          "type": "string"
        },
        "yhat_unlabeled": {
          "description": "Model predictions on the unlabeled rows.",
          "type": "string"
        }
      },
      "required": [
        "y",
        "yhat",
        "yhat_unlabeled"
      ],
      "type": "object"
    },
    "name": "ppi_mean"
  },
  {
    "description": "Prediction-powered OLS with a labeled audit sample.",
    "input_schema": {
      "properties": {
        "X": {
          "description": "Covariates for the labeled rows.",
          "type": "string"
        },
        "X_unlabeled": {
          "description": "Covariates for the unlabeled rows (same columns as ``X``).",
          "type": "string"
        },
        "add_intercept": {
          "default": true,
          "description": "Prepend a constant column to both design matrices.",
          "type": "boolean"
        },
        "alpha": {
          "default": 0.05,
          "description": "CI level (1 - alpha confidence).",
          "type": "number"
        },
        "target": {
          "description": "Which coefficient is the headline ``estimate``. Defaults to the first non-intercept term.",
          "type": "integer"
        },
        "tune": {
          "default": true,
          "description": "Per-coefficient PPI++ power tuning. ``False`` fixes ``lambda = 1``.",
          "type": "boolean"
        },
        "y": {
          "description": "Gold-standard outcomes for the labeled rows.",
          "type": "string"
        },
        "yhat": {
          "description": "Model-predicted outcomes for the same labeled rows.",
          "type": "string"
        },
        "yhat_unlabeled": {
          "description": "Model-predicted outcomes for the unlabeled rows.",
          "type": "string"
        }
      },
      "required": [
        "X",
        "X_unlabeled",
        "y",
        "yhat",
        "yhat_unlabeled"
      ],
      "type": "object"
    },
    "name": "ppi_ols"
  },
  {
    "description": "Pseudo-Poisson Maximum Likelihood with high-dimensional fixed effects. Validation: certified parity evidence. Do NOT use when: vce='conley' on more than ~20,000 rows -- dense O(n^2) memory (see cost); vce='conley' with high-dimensional fixed effects -- the conleyreg-matching construction is dummy-based and raises MethodIncompatibility past 1,000 dummy columns; use cluster= (CRV1) there. Cost: IRLS is linear in n. vce='conley' builds the FE-as-dummies design plus dense n x n great-circle distance and kernel matrices (glm_conley_vcov) -- ~0.8 GB at n=10,000 and ~80 GB at n=100,000 -- and it refuses designs with >= n or > 1,000 dummy columns. Use cluster= (CRV1) instead at that scale.",
    "input_schema": {
      "properties": {
        "absorb": {
          "description": "Fixed effects to absorb, e.g. ``\"origin + destination + year\"``. Overrides any FE specification in the formula.",
          "type": "string"
        },
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals.",
          "type": "number"
        },
        "cluster": {
          "description": "Variable name for clustered standard errors (recommended for gravity models, e.g. cluster on country-pair). A pair ``cluster=[\"a\", \"b\"]`` requests two-way clustering (Cameron-Gelbach-Miller 2011 inclusion-exclusion with the single ``G_min/(G_min-1)`` small-sample factor -- byte-identical to Stata ``ppmlhdfe ..., cluster(a b)``).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "conley_cutoff": {
          "description": "conley_cutoff parameter (Optional[float]).",
          "type": "number"
        },
        "conley_lat": {
          "description": "conley_lat parameter (Optional[str]).",
          "type": "string"
        },
        "conley_lon": {
          "description": "conley_lon parameter (Optional[str]).",
          "type": "string"
        },
        "formula": {
          "description": "Model formula. Fixed effects can be specified via ``|``: ``\"trade ~ dist + contig | origin + destination + year\"``",
          "type": "string"
        },
        "maxiter": {
          "default": 1000,
          "description": "Maximum IRLS iterations.",
          "type": "integer"
        },
        "robust": {
          "default": "robust",
          "description": "Default is robust SE (as in Stata's ppmlhdfe). Options: \"robust\"/\"hc1\" (sandwich with the ``ssc`` small-sample factor), \"hc0\" (sandwich, no factor), \"nonrobust\".",
          "type": "string"
        },
        "seed": {
          "description": "RNG seed for sampled (non-enumerated) wild draws.",
          "type": "integer"
        },
        "separation": {
          "default": true,
          "description": "If True, check for separation (perfect prediction of zeros) and warn. Observations causing separation are not dropped automatically.",
          "type": "boolean"
        },
        "ssc": {
          "default": "stata",
          "description": "Small-sample factor applied to the heteroskedasticity-robust sandwich. ``\"stata\"`` multiplies by ``N/(N-1)``, the Stata ``glm``/``ppmlhdfe vce(robust)`` convention (matches ``ppmlhdfe`` at machine precision). ``\"fixest\"`` multiplies by ``(N-1)/(N-K)`` with ``K`` counting the slopes plus the absorbed fixed-effect levels (minus one per additional fixed-effect dimension for collinearity), the ``fixest::fepois`` default ``ssc(adj = TRUE, fixef.K = \"full\")``. ``\"none\"`` applies no factor. The three differ by less than ``sqrt(N/(N-K))`` and are documented, not competing, conventions; point estimates are unaffected. Clustered variances keep the ``G/(G-1)`` factor.",
          "enum": [
            "stata",
            "fixest"
          ],
          "type": "string"
        },
        "tol": {
          "default": 1e-08,
          "description": "Convergence tolerance.",
          "type": "number"
        },
        "vce": {
          "description": "Canonical SE-menu keyword. ``\"robust\"``/``\"hc1\"``/``\"hc0\"`` alias the ``robust=`` parameter. ``\"wild\"`` (with ``cluster=``) runs the boottest-convention score wild cluster bootstrap on the FE-absorbed design -- exact at any FE dimensionality (the weighted-FWL reduction of the score numerator is exact) and byte-identical to ``sp.fepois(vce=\"wild\")`` on low-dimensional FE. ``\"CR2\"``/``\"CR3\"``/``\"jackknife\"`` (with ``cluster=``) compute the clubSandwich glm bias-reduced SEs on the FE-as-dummies design (guarded against high-dimensional FE).",
          "type": "string"
        },
        "weights": {
          "description": "Weight variable name.",
          "type": "string"
        },
        "wild_reps": {
          "default": 9999,
          "description": "Replications for ``vce=\"wild\"`` (enumerates the 2^G grid when ``2**G <= wild_reps``).",
          "type": "integer"
        },
        "wild_weight_type": {
          "default": "rademacher",
          "description": "Wild weight distribution.",
          "type": "string"
        },
        "x": {
          "description": "Independent variable names (alternative to formula).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Dependent variable name (alternative to formula).",
          "type": "string"
        }
      },
      "required": [],
      "type": "object"
    },
    "name": "ppmlhdfe"
  },
  {
    "description": "Predict CATE on new (out-of-sample) data. Assumptions: Unconfoundedness given the covariates; Overlap / positivity across the covariate space; Nuisance functions are estimated consistently; cross-fitting controls overfitting bias. Pre-conditions: Covariates, a treatment indicator, and an outcome for each unit; Enough data to fit flexible nuisance models with sample-splitting / cross-fitting. Failure modes: CATE estimates are unstable or extrapolate beyond the covariate support -> Restrict to the overlap region, increase data, or use a doubly-robust learner (DR-/R-learner). Alternatives: sp.dml, sp.causal_forest, sp.tmle. Typical minimum N: 500.",
    "input_schema": {
      "properties": {
        "new_data": {
          "description": "New data with the same covariate columns used in estimation.",
          "type": "string"
        },
        "result": {
          "description": "Result from ``metalearner()`` containing a fitted estimator.",
          "type": "string"
        }
      },
      "required": [
        "new_data",
        "result"
      ],
      "type": "object"
    },
    "name": "predict_cate"
  },
  {
    "description": "Run pre-fit identification checks for a chosen method on a DataFrame. Verdict in {PASS, WARN, FAIL}. ALWAYS call this before fitting on an unfamiliar dataset to surface design problems (overlap, cohort sizes, IV first-stage F, running-variable density at the cutoff).",
    "input_schema": {
      "properties": {
        "cohort": {
          "type": "string"
        },
        "covariates": {
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "id": {
          "description": "Unit id column.",
          "type": "string"
        },
        "instrument": {
          "type": "string"
        },
        "method": {
          "description": "Estimator name: 'did', 'rd', 'iv', 'synth', 'matching', 'dml', ...",
          "type": "string"
        },
        "running_var": {
          "type": "string"
        },
        "time": {
          "type": "string"
        },
        "treatment": {
          "type": "string"
        },
        "y": {
          "description": "Outcome column.",
          "type": "string"
        }
      },
      "required": [
        "method"
      ],
      "type": "object"
    },
    "name": "preflight"
  },
  {
    "description": "Pre-trend equivalence tests (Liu, Wang & Xu 2024, the fect diagnostic panel). Reverses the usual null: instead of testing whether pre-period effects are zero, tests whether they are demonstrably *small*. Failing to reject 'no pre-trend' is often just low power (Roth 2022), so the conventional test alone overstates the evidence for parallel trends. Reports the joint F test alongside its non-central-F and TOST equivalence counterparts, where a *small* p-value is the reassuring outcome. Validation: validated evidence tier (known-truth, reference, external-parity, or Monte Carlo artifact). Known limitations: the TOST is computed only when tost_threshold is supplied; there is no universal outcome-scale default, so it is not invented. Pre-conditions: result carries influence functions so the joint pre-period covariance can be recovered; at least two pre-treatment periods (one is absorbed as the normalisation reference); more treated units than pre-periods. Alternatives: sp.pretrends_test, sp.pretrends_power, sp.honest_did.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "f_threshold": {
          "default": 0.6,
          "description": "Dimensionless effect-size bound for the F equivalence test (fect's default)",
          "type": "number"
        },
        "result": {
          "description": "DiD result carrying an event study and influence functions (e.g. sp.callaway_santanna)",
          "type": "string"
        },
        "tost_threshold": {
          "description": "Equivalence bound in outcome units. Omitted by default because there is no defensible universal scale for 'negligible pre-trend'; the TOST is skipped when it is not supplied. fect uses 0.36 * residual SD.",
          "type": "number"
        }
      },
      "required": [
        "result"
      ],
      "type": "object"
    },
    "name": "pretrends_equivalence"
  },
  {
    "description": "Power of the pre-trend test against a hypothesised violation. Validation: certified parity evidence.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level of the pre-trend test.",
          "type": "number"
        },
        "delta": {
          "description": "Hypothesised trend violation in the pre-period (length = number of pre-periods). Default: linear trend ``delta[k] = (k+1) * min(|SE|)`` -- a violation equal to one SE at the furthest lag, declining linearly to near-zero.",
          "type": "string"
        },
        "result": {
          "description": "Event-study result with pre-treatment estimates and SEs.",
          "type": "string"
        },
        "test": {
          "default": "individual",
          "description": "Which pre-test the power refers to. ``\"individual\"`` is the practice Roth (2022) analyses and the one his ``pretrends`` R package implements: the analyst eyeballs the event-study plot and calls the pre-trends into question if *any* pre-period coefficient is individually significant. Power is then one minus the probability that every pre-period coefficient falls inside its own ``+/- z_{1-alpha/2} * SE`` band, integrated over the joint normal with mean ``delta`` -- a multivariate-normal rectangle probability. ``\"joint\"`` is the power of the joint Wald test that all pre-period coefficients are zero, ``chi2(K)`` with non-centrality ``delta' Sigma^-1 delta``. Reported by :func:`pretrends_test`, and a strictly different quantity -- not a tighter or looser version of the same one. The two are not even comparable at face value: the joint test has size exactly ``alpha``, while the coefficient-by-coefficient test rejects with probability above ``alpha`` under the null because each of the K coefficients gets its own ``alpha``-level look. Which comes out more powerful against a given trend depends on the design. ``power_joint`` is always reported alongside, so both are available from one call. .. versionchanged:: 1.21.0 The default moved from ``\"joint\"`` to ``\"individual\"`` so the number matches Roth's ``pretrends`` package. This changes the returned ``power`` for existing calls -- see MIGRATION.md. Pass ``test=\"joint\"`` to recover the previous behaviour.",
          "enum": [
            "individual",
            "joint"
          ],
          "type": "string"
        }
      },
      "required": [
        "result"
      ],
      "type": "object"
    },
    "name": "pretrends_power"
  },
  {
    "description": "Slope of a linear pre-trend the pre-test would detect ``target_power`` Validation: certified parity evidence.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level of the pre-test.",
          "type": "number"
        },
        "result": {
          "description": "Event-study result with pre-treatment estimates and SEs. As with :func:`pretrends_power`, supply the full pre-period covariance via ``model_info['vcv_pre']`` -- the diagonal fallback overstates the detectable slope.",
          "type": "string"
        },
        "target_power": {
          "default": 0.5,
          "description": "Power the returned slope achieves. 0.5 is the ``pretrends``",
          "type": "number"
        },
        "test": {
          "default": "individual",
          "description": "Which pre-test to solve against; see :func:`pretrends_power`.",
          "enum": [
            "individual",
            "joint"
          ],
          "type": "string"
        }
      },
      "required": [
        "result"
      ],
      "type": "object"
    },
    "name": "pretrends_slope_for_power"
  },
  {
    "description": "Print a combined pre-trends diagnostic report.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level.",
          "type": "number"
        },
        "delta": {
          "description": "Passed to ``pretrends_power``.",
          "type": "string"
        },
        "result": {
          "description": "Event-study result.",
          "type": "string"
        }
      },
      "required": [
        "result"
      ],
      "type": "object"
    },
    "name": "pretrends_summary"
  },
  {
    "description": "Joint Wald test of pre-treatment ATTs (or event-study leads) against zero -- the canonical sanity check for the parallel-trends assumption in DiD designs. Failing to reject is necessary but not sufficient evidence for parallel trends; always pair with sp.honest_did / sp.sensitivity_rr for design-robust inference. Assumptions: The test asks whether the pre-period ATTs *jointly* differ from zero; Failing to reject is consistent with parallel trends but does NOT prove it (low power problem -- Roth 2022). Pre-conditions: result has at least one pre-treatment period coefficient and its variance; covariance between pre-period coefficients is available (cluster-robust SE recommended). Failure modes: Single pre-period (no pretrends to test) -> Pretrends test needs >= 2 pre-treatment periods; widen the panel or drop the test; High-power study rejects but visual pretrends look flat -> Use sp.honest_did + sp.sensitivity_rr to bound the bias; reporting *both* is standard practice. Alternatives: sp.sensitivity_rr, sp.honest_did, sp.event_study. Typical minimum N: 50.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "result": {
          "description": "DiD or event-study result with pre-period coefficients",
          "type": "string"
        },
        "type": {
          "default": "wald",
          "description": "Test statistic",
          "enum": [
            "wald"
          ],
          "type": "string"
        }
      },
      "required": [
        "result"
      ],
      "type": "object"
    },
    "name": "pretrends_test"
  },
  {
    "description": "Principal Stratification (Frangakis & Rubin 2002). 'monotonicity' method identifies the complier PCE (= LATE) and reports Zhang-Rubin sharp bounds on the always-survivor SACE. 'principal_score' uses Ding-Lu covariate weighting to point-identify stratum-specific effects under principal ignorability. Validation: certified evidence with scoped limitations. Known limitations: Always-survivor SACE under encouragement design (Mealli & Pacini 2013, partial identification) is not yet implemented; only AIR / Wald LATE point estimates (tau_Y on outcome, tau_S on the post-treatment stratum) are reported when an instrument is supplied. Assumptions: Monotonicity (no defiers) for method='monotonicity'; Principal ignorability for method='principal_score' (strata perp Y(d) | X); SUTVA and exclusion restriction for the never-takers / always-takers interpretation. Pre-conditions: binary treatment; binary post-treatment stratum variable (compliance, survival, employment, ...); covariates required when method='principal_score' (for Ding-Lu weighting). Failure modes: Zhang-Rubin bounds include 0 and both signs -> Strata partition too weak for point identification -- add covariates and use method='prin...",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "CI level (e.g. 0.05 for 95% CIs)",
          "type": "number"
        },
        "covariates": {
          "description": "Baseline covariates (required for principal_score)",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "instrument": {
          "description": "Binary instrument column. When supplied, switches to the AIR / Wald LATE estimator: under random Z, monotonicity, and exclusion, reports two LATEs among Z-compliers -- tau_Y for the effect of the treatment on the outcome, and tau_S for the effect on the post-treatment stratum variable. method= is ignored on this path.",
          "type": "string"
        },
        "method": {
          "default": "monotonicity",
          "description": "Identification strategy",
          "enum": [
            "monotonicity",
            "principal_score"
          ],
          "type": "string"
        },
        "n_boot": {
          "default": 500,
          "description": "Bootstrap replications",
          "type": "integer"
        },
        "seed": {
          "description": "Random seed for reproducible bootstrap draws",
          "type": "integer"
        },
        "strata": {
          "description": "Binary post-treatment variable",
          "type": "string"
        },
        "treat": {
          "description": "Binary treatment",
          "type": "string"
        },
        "trimming": {
          "default": "quantile",
          "description": "Trimming rule for the Zhang-Rubin SACE bounds (monotonicity method); same meaning as in :func:`sp.lee_bounds`, with which the bounds coincide.",
          "enum": [
            "quantile",
            "exact"
          ],
          "type": "string"
        },
        "y": {
          "description": "Outcome",
          "type": "string"
        }
      },
      "required": [
        "strata",
        "treat",
        "y"
      ],
      "type": "object"
    },
    "name": "principal_strat"
  },
  {
    "description": "Probability forest for ``P[Y = k | X]`` (``grf::probability_forest``).",
    "input_schema": {
      "properties": {
        "ci_group_size": {
          "default": 2,
          "description": "Trees per little bag (variance estimates need >= 2).",
          "type": "integer"
        },
        "clusters": {
          "description": "Cluster ids (trees sample whole clusters).",
          "type": "string"
        },
        "covariates": {
          "description": "Covariates ``X``.",
          "type": "string"
        },
        "equalize_cluster_weights": {
          "default": false,
          "description": "equalize_cluster_weights parameter (bool).",
          "type": "boolean"
        },
        "honest": {
          "default": true,
          "description": "grf honesty options.",
          "type": "boolean"
        },
        "honesty_fraction": {
          "default": 0.5,
          "description": "grf honesty options.",
          "type": "number"
        },
        "honesty_prune_leaves": {
          "default": true,
          "description": "grf honesty options.",
          "type": "boolean"
        },
        "imbalance_penalty": {
          "default": 0.0,
          "description": "imbalance_penalty parameter (float).",
          "type": "number"
        },
        "max_depth": {
          "description": "max_depth parameter (Optional[int]).",
          "type": "integer"
        },
        "max_samples": {
          "default": 0.5,
          "description": "grf ``sample.fraction``.",
          "type": "number"
        },
        "min_samples_leaf": {
          "default": 5,
          "description": "grf ``min.node.size``.",
          "type": "integer"
        },
        "mtry": {
          "description": "mtry parameter (Optional[int]).",
          "type": "integer"
        },
        "n_estimators": {
          "default": 2000,
          "description": "Trees (grf ``num.trees``).",
          "type": "integer"
        },
        "n_jobs": {
          "default": 1,
          "description": "Number of jobs.",
          "type": "integer"
        },
        "random_state": {
          "default": 42,
          "description": "Random seed or RandomState for reproducible stochastic steps.",
          "type": "integer"
        },
        "split_alpha": {
          "default": 0.05,
          "description": "grf ``alpha`` (minimum child share).",
          "type": "number"
        },
        "weights": {
          "description": "Sample weights (grf ``sample.weights``).",
          "type": "string"
        },
        "y": {
          "description": "Class labels (any hashable values; classes are sorted).",
          "type": "string"
        }
      },
      "required": [],
      "type": "object"
    },
    "name": "probability_forest"
  },
  {
    "description": "Probit regression via maximum likelihood. Validation: certified parity evidence.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals.",
          "type": "number"
        },
        "at_values": {
          "description": "Variable values for ``marginal_effects='at'``.",
          "type": "object"
        },
        "cluster": {
          "description": "Column name for clustered standard errors.",
          "type": "string"
        },
        "formula": {
          "description": "Formula like ``\"y ~ x1 + x2\"``.",
          "type": "string"
        },
        "marginal_effects": {
          "description": "``'average'`` (AME), ``'mean'`` (MEM), or ``'at'`` (MER).",
          "type": "string"
        },
        "maxiter": {
          "default": 100,
          "description": "Maximum Newton-Raphson iterations.",
          "type": "integer"
        },
        "robust": {
          "default": "nonrobust",
          "description": "``'nonrobust'`` for MLE SE, ``'hc1'`` / ``'robust'`` for sandwich SE.",
          "type": "string"
        },
        "tol": {
          "default": 1e-08,
          "description": "Convergence tolerance on log-likelihood change.",
          "type": "number"
        },
        "weights": {
          "description": "Column name for frequency/analytic weights.",
          "type": "string"
        },
        "x": {
          "description": "Regressor names (alternative to formula).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Dependent variable name (alternative to formula).",
          "type": "string"
        }
      },
      "required": [],
      "type": "object"
    },
    "name": "probit"
  },
  {
    "description": "Estimate propensity scores P(D=1|X). Assumptions: Unconfoundedness: treatment is as-good-as-random given the measured covariates; Overlap / common support: every unit has a non-degenerate probability of each treatment; The covariate set blocks all back-door paths. Pre-conditions: Pre-treatment covariates measured for treated and control units; A binary (or low-cardinality) treatment indicator; Sufficient covariate overlap between treatment arms. Failure modes: Poor overlap -- extreme propensity scores or few acceptable matches -> Trim or restrict to the common-support region and report the discarded units; Covariate imbalance remains after matching/weighting -> Re-specify the balancing model (CBPS, entropy balancing) and re-check standardized mean differences. Alternatives: sp.propensity_score, sp.cbps, sp.ebalance, sp.dml. Typical minimum N: 200.",
    "input_schema": {
      "properties": {
        "covariates": {
          "description": "Covariate column names.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "method": {
          "default": "logit",
          "description": "Estimation method. ``'logit'`` uses IRLS (no sklearn needed). ``'probit'`` uses scipy.optimize. ``'gbm'`` tries sklearn GradientBoostingClassifier, falling back to logit with interactions.",
          "enum": [
            "logit",
            "probit",
            "gbm"
          ],
          "type": "string"
        },
        "treatment": {
          "description": "Name of binary treatment column (0/1).",
          "type": "string"
        },
        "trimming": {
          "description": "If ``'crump'``, apply Crump et al. (2009) trimming after estimation. Trimmed observations receive ``NaN`` scores.",
          "type": "string"
        }
      },
      "required": [
        "covariates",
        "treatment"
      ],
      "type": "object"
    },
    "name": "propensity_score"
  },
  {
    "description": "Proximal Causal Inference via linear 2SLS on the outcome bridge. Identifies ATE with unmeasured confounding using two proxy variables: a treatment-side Z (instrument for W) and an outcome-side W (endogenous bridge regressor). Validation: validated evidence tier (known-truth, reference, external-parity, or Monte Carlo artifact). Assumptions: Existence of an outcome bridge function h(w, a, x) that recovers E[Y(a) | U, X]; Z and W are conditionally independent given U and (A, X); Z perp Y | U, A, X (exclusion on Z). Pre-conditions: at least one treatment-side proxy Z (independent of outcome given U, X); at least one outcome-side proxy W (independent of treatment given U, X); proxy_z and proxy_w measure the same unmeasured confounder U from different angles. Failure modes: First-stage (Z -> W) too weak -> Try richer Z or more proxies; without first-stage strength the bridge is underidentified; Proxies collapse to nearly-constant -> Proxy variation insufficient -- redesign measurement or fall back to sensitivity (sp.sensemakr); Estimate highly sensitive to bridge specification -> Report multiple bridge families; compare with sp.negative_control_outcome / _exposure. Alternatives: sp.neg...",
    "input_schema": {
      "properties": {
        "bridge": {
          "default": "linear",
          "description": "Bridge function family",
          "enum": [
            "linear"
          ],
          "type": "string"
        },
        "covariates": {
          "description": "Baseline covariates",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "n_boot": {
          "default": 0,
          "description": "Bootstrap SE replications",
          "type": "integer"
        },
        "proxy_w": {
          "description": "Outcome-side proxies (endogenous)",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "proxy_z": {
          "description": "Treatment-side proxies (instruments for W)",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "treat": {
          "description": "Treatment",
          "type": "string"
        },
        "y": {
          "description": "Outcome",
          "type": "string"
        }
      },
      "required": [
        "proxy_w",
        "proxy_z",
        "treat",
        "y"
      ],
      "type": "object"
    },
    "name": "proximal"
  },
  {
    "description": "Doubly-robust regression-based PCI estimator for the ATE. Assumptions: A treatment-inducing confounding proxy Z and an outcome-inducing confounding proxy W are observed; Outcome and treatment confounding bridge functions exist (proximal-g and proximal-h completeness conditions); Latent confounding is fully captured by (Z, W, X) -- no residual unmeasured confounding outside the proxy span. Pre-conditions: data has outcome, binary treatment, z_proxy and w_proxy columns; treatment is binary for the bridge logistic step. Failure modes: Treatment-bridge logistic regression fails to converge; propensity collapses to the marginal P(D=1) -> Check detail['propensity_fallback']; the DR correction is neutralised -- improve proxies or covariates. Alternatives: sp.proximal, sp.double_negative_control, sp.frontdoor. Typical minimum N: 200.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "covariates": {
          "description": "Measured covariates X.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "propensity_bounds": {
          "default": [
            0.02,
            0.98
          ],
          "description": "propensity_bounds parameter (tuple).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "treat": {
          "description": "Binary treatment column.",
          "type": "string"
        },
        "w_proxy": {
          "description": "Outcome-inducing confounding proxy W.",
          "type": "string"
        },
        "y": {
          "description": "Outcome column.",
          "type": "string"
        },
        "z_proxy": {
          "description": "Treatment-inducing confounding proxy Z.",
          "type": "string"
        }
      },
      "required": [
        "treat",
        "w_proxy",
        "y",
        "z_proxy"
      ],
      "type": "object"
    },
    "name": "proximal_regression"
  },
  {
    "description": "Compute comprehensive propensity score balance table.",
    "input_schema": {
      "properties": {
        "covariates": {
          "description": "Covariate columns to assess balance for.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "method": {
          "default": "logit",
          "description": "PS estimation method ('logit', 'probit', 'gbm').",
          "type": "string"
        },
        "treatment": {
          "description": "Binary treatment column.",
          "type": "string"
        },
        "weights": {
          "description": "IPW or matching weights. If None, inverse-PS weights are computed automatically from estimated propensity scores.",
          "type": "string"
        }
      },
      "required": [
        "covariates",
        "treatment"
      ],
      "type": "object"
    },
    "name": "ps_balance"
  },
  {
    "description": "Propensity-score matching -- article alias for :func:`match` Validation: certified parity evidence. Assumptions: Unconfoundedness: treatment is as-good-as-random given the measured covariates; Overlap / common support: every unit has a non-degenerate probability of each treatment; The covariate set blocks all back-door paths. Pre-conditions: Pre-treatment covariates measured for treated and control units; A binary (or low-cardinality) treatment indicator; Sufficient covariate overlap between treatment arms. Failure modes: Poor overlap -- extreme propensity scores or few acceptable matches -> Trim or restrict to the common-support region and report the discarded units; Covariate imbalance remains after matching/weighting -> Re-specify the balancing model (CBPS, entropy balancing) and re-check standardized mean differences. Alternatives: sp.propensity_score, sp.cbps, sp.ebalance, sp.dml. Typical minimum N: 200.",
    "input_schema": {
      "properties": {
        "X": {
          "description": "Feature matrix or covariate DataFrame.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "d": {
          "description": "d parameter (str).",
          "type": "string"
        },
        "method": {
          "default": "nn",
          "description": "Estimator or algorithm variant to use.",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "X",
        "d",
        "y"
      ],
      "type": "object"
    },
    "name": "psm"
  },
  {
    "description": "Stata psmatch2-faithful supported propensity-score matching paths (nearest-neighbour, kernel, radius, local linear regression, Mahalanobis): returns matched-sample variables (_pscore _treated _support _weight _y; plus _n1 through _nn _pdif for nearest-neighbour), the psmatch2 analytic ATT standard error, plus post-matching balance (.pstest() reproduces Stata pstest exactly), common-support plotting, and weighted PSM-DID. Validation: certified parity evidence. Assumptions: Unconfoundedness / CIA: Y(d) perp D | X; Overlap / common support on the propensity score; SUTVA: no interference between matched units. Pre-conditions: binary treatment 0/1; covariates are pre-treatment (temporally prior to D); enough control units for each treated unit under k:1 matching. Failure modes: Residual imbalance after matching (max |SMD| > 0.1) -> Tighten caliper, add covariate interactions (ps_poly), or switch to sp.ebalance; Treated units off common support -> Pass common_support='minmax' (Stata `common`) or sp.trimming. Alternatives: sp.match, sp.psm, sp.ebalance, sp.cbps. Typical minimum N: 200.",
    "input_schema": {
      "properties": {
        "ai": {
          "default": 0,
          "description": "Abadie-Imbens (2006) robust SE with J within-arm matches (Stata ai(J))",
          "type": "integer"
        },
        "bootstrap_reps": {
          "default": 200,
          "description": "Bootstrap replications when se='bootstrap'",
          "type": "integer"
        },
        "bootstrap_seed": {
          "description": "Seed for the bootstrap resampler (pass for reproducibility)",
          "type": "integer"
        },
        "bwidth": {
          "default": 0.06,
          "description": "Kernel bandwidth (method='kernel')",
          "type": "number"
        },
        "caliper": {
          "description": "Max PS distance / radius bandwidth",
          "type": "number"
        },
        "common_support": {
          "default": "none",
          "description": "Common-support trimming",
          "enum": [
            "none",
            "minmax"
          ],
          "type": "string"
        },
        "covariates": {
          "description": "Covariate matrix, DataFrame, or column names.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "kernel": {
          "default": "epan",
          "description": "Kernel type (method='kernel' or 'llr'). NOTE: Stata's psmatch2 does not run LLR with kerneltype(epan) -- it substitutes lpoly-smoothed nearest-neighbour matching. Use 'tricube' to reproduce psmatch2's own LLR routine.",
          "enum": [
            "epan",
            "normal",
            "biweight",
            "uniform",
            "tricube"
          ],
          "type": "string"
        },
        "llr_stata_compat": {
          "default": false,
          "description": "method='llr' only: reproduce Stata psmatch2's SUBSTITUTE for LLR (lpoly-smoothed outcome + nearest-neighbour matching) instead of genuine local linear regression. Set only to reconcile a published psmatch2 number.",
          "type": "boolean"
        },
        "method": {
          "default": "neighbor",
          "description": "Matching algorithm",
          "enum": [
            "neighbor",
            "kernel",
            "radius",
            "llr",
            "mahalanobis"
          ],
          "type": "string"
        },
        "neighbor": {
          "default": 1,
          "description": "Number of nearest neighbours k",
          "type": "integer"
        },
        "outcome": {
          "description": "Outcome variable (Stata outcome(); optional)",
          "type": "string"
        },
        "se": {
          "default": "psmatch2",
          "description": "Standard-error estimator. 'bootstrap' re-estimates the propensity score each replication and is the only valid choice for method='llr' (Stata reports seatt = . there).",
          "enum": [
            "psmatch2",
            "ai",
            "abadie_imbens",
            "bootstrap"
          ],
          "type": "string"
        },
        "treat": {
          "description": "Binary treatment column (0/1)",
          "type": "string"
        }
      },
      "required": [
        "covariates",
        "treat"
      ],
      "type": "object"
    },
    "name": "psmatch2"
  },
  {
    "description": "Propensity score distribution plot (common support diagnostic).",
    "input_schema": {
      "properties": {
        "ax": {
          "description": "ax parameter.",
          "type": "string"
        },
        "colors": {
          "default": [
            "#3498DB",
            "#E74C3C"
          ],
          "description": "Colors for (control, treated).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "covariates": {
          "description": "Covariates used to estimate the propensity score.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "figsize": {
          "default": [
            8,
            5
          ],
          "description": "figsize parameter (tuple).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "labels": {
          "default": [
            "Control",
            "Treated"
          ],
          "description": "Labels for (control, treated).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "n_bins": {
          "default": 40,
          "description": "Number of histogram bins.",
          "type": "integer"
        },
        "title": {
          "description": "title parameter (Optional[str]).",
          "type": "string"
        },
        "treat": {
          "description": "Binary treatment column.",
          "type": "string"
        },
        "trim": {
          "description": "If set, draw vertical lines at (trim, 1-trim) to show the recommended trimming region.",
          "type": "number"
        }
      },
      "required": [
        "covariates",
        "treat"
      ],
      "type": "object"
    },
    "name": "psplot"
  },
  {
    "description": "Publication readiness checklist.",
    "input_schema": {
      "properties": {
        "design": {
          "description": "Research design: 'rct', 'did', 'rd', 'iv', 'observational'.",
          "type": "string"
        },
        "has_balance": {
          "default": false,
          "description": "Already have balance table.",
          "type": "boolean"
        },
        "has_heterogeneity": {
          "default": false,
          "description": "Already have subgroup analysis.",
          "type": "boolean"
        },
        "has_mht": {
          "default": false,
          "description": "Already have MHT correction.",
          "type": "boolean"
        },
        "has_placebo": {
          "default": false,
          "description": "Already have placebo tests.",
          "type": "boolean"
        },
        "has_pretrends": {
          "default": false,
          "description": "Already have pre-trend tests.",
          "type": "boolean"
        },
        "has_robustness": {
          "default": false,
          "description": "Already have robustness checks.",
          "type": "boolean"
        },
        "has_sensitivity": {
          "default": false,
          "description": "Already have sensitivity analysis.",
          "type": "boolean"
        },
        "results": {
          "description": "List of estimated result objects.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "venue": {
          "default": "top5_econ",
          "description": "Target venue: 'top5_econ', 'aej_applied', 'rct'.",
          "type": "string"
        }
      },
      "required": [],
      "type": "object"
    },
    "name": "pub_ready"
  },
  {
    "description": "Pairwise comparisons of predictive margins across all levels. Validation: certified parity evidence.",
    "input_schema": {
      "properties": {
        "adjust": {
          "default": "none",
          "description": "P-value adjustment method: - ``'none'``: unadjusted. - ``'bonferroni'``: Bonferroni correction. - ``'sidak'``: Sidak correction. - ``'holm'``: Holm step-down procedure.",
          "type": "string"
        },
        "alpha": {
          "default": 0.05,
          "description": "Significance level for (adjusted) confidence intervals.",
          "type": "number"
        },
        "result": {
          "description": "Fitted model result.",
          "type": "string"
        },
        "variable": {
          "description": "Categorical variable whose levels are compared pairwise.",
          "type": "string"
        }
      },
      "required": [
        "result",
        "variable"
      ],
      "type": "object"
    },
    "name": "pwcompare"
  },
  {
    "description": "Quantile Difference-in-Differences (QDiD): applies the DiD contrast to quantiles, [Q11(t)-Q10(t)] - [Q01(t)-Q00(t)], on a 2x2 design with bootstrap SE. This is NOT changes-in-changes -- Athey & Imbens (2006) propose CiC and explicitly criticise QDiD; use sp.cic for CiC. Validation: certified parity evidence. Assumptions: CIC rank invariance: the quantile rank in the untreated distribution is stable across groups; Continuous outcome support covering both groups in both periods; SUTVA (no cross-group spillovers). Pre-conditions: panel or repeated cross-section; group is binary 0/1; time is binary 0/1 (pre / post). Failure modes: Outcome heavily discrete / zero-inflated -> CIC rank-matching is unstable on discrete supports -- use QTE regression (sp.qte) or Firpo-RIF; Bootstrap CI across quantiles varies wildly -> Thin tails at extreme quantiles -- restrict to [0.2, 0.8] or raise n_boot to 2000. Alternatives: sp.qte, sp.did, sp.rifreg. Typical minimum N: 500.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "group": {
          "description": "Binary treated / control group",
          "type": "string"
        },
        "n_boot": {
          "default": 500,
          "description": "Number of bootstrap replications.",
          "type": "integer"
        },
        "quantiles": {
          "description": "Quantiles to estimate, defaults to [0.1, ..., 0.9]",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "time": {
          "description": "Binary pre / post indicator",
          "type": "string"
        },
        "y": {
          "description": "Outcome",
          "type": "string"
        }
      },
      "required": [
        "group",
        "time",
        "y"
      ],
      "type": "object"
    },
    "name": "qdid"
  },
  {
    "description": "Quantile Synthetic Control: exactly ``sp.discos(..., method='quantile')``. Assumptions: A convex (or regularized) combination of donor units reproduces the treated unit's pre-treatment outcome path; No interference: the treatment does not affect the donor units (SUTVA); No anticipation before the treatment date. Pre-conditions: Panel of one or more treated units plus an untreated donor pool, observed over time; Pre-treatment window long enough to fit donor weights (rule of thumb: more pre-periods than donors used); Outcome observed for every unit in every period. Failure modes: Large pre-treatment RMSPE -- the synthetic unit fails to track the treated unit before treatment -> Add donors / predictors, lengthen the pre-period, or use a bias-corrected estimator (sdid, augsynth); Placebo / permutation inference shows the estimate is not extreme relative to donors -> Report the placebo distribution honestly; the effect may not be distinguishable from noise. Alternatives: sp.sdid, sp.augsynth, sp.gsynth, sp.callaway_santanna. Typical minimum N: 15.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level.",
          "type": "number"
        },
        "n_quantiles": {
          "default": 100,
          "description": "Number of quantile grid points.",
          "type": "integer"
        },
        "outcome": {
          "description": "Outcome variable column.",
          "type": "string"
        },
        "placebo": {
          "default": true,
          "description": "Run placebo permutation inference.",
          "type": "boolean"
        },
        "seed": {
          "description": "Random seed.",
          "type": "integer"
        },
        "time": {
          "description": "Time period column.",
          "type": "string"
        },
        "treated_unit": {
          "description": "Identifier of the treated unit.",
          "type": "string"
        },
        "treatment_time": {
          "description": "First treatment period (inclusive).",
          "type": "string"
        },
        "unit": {
          "description": "Unit identifier column.",
          "type": "string"
        }
      },
      "required": [
        "outcome",
        "time",
        "treated_unit",
        "treatment_time",
        "unit"
      ],
      "type": "object"
    },
    "name": "qqsynth"
  },
  {
    "description": "Quantile regression at specified quantile(s). Validation: certified parity evidence. Assumptions: Conditional quantile is linear in covariates; Outcome continuous (or finely discretized) so the check-function minimization is well-posed; Powell (1991) sandwich SEs require a consistent kernel density of the conditional density at zero. Pre-conditions: Continuous outcome variable; Quantile tau strictly in (0,1). Failure modes: Sparse data in tail quantiles gives unstable density estimate and wide SEs -> Estimate a more central quantile or pool more data near the tail; Crossing/degenerate fit when regressors are collinear -> Drop or combine collinear regressors before refitting. Alternatives: sp.regress, sp.feols. Typical minimum N: 200.",
    "input_schema": {
      "properties": {
        "formula": {
          "description": "'y ~ x1 + x2' (alternative to y/x)",
          "type": "string"
        },
        "quantile": {
          "default": 0.5,
          "description": "Quantile (0-1)",
          "type": "number"
        },
        "x": {
          "description": "Regressor columns (alternative to formula)",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Outcome column (alternative to formula)",
          "type": "string"
        }
      },
      "required": [],
      "type": "object"
    },
    "name": "qreg"
  },
  {
    "description": "Quantile treatment effects. 'firpo_qte' / 'firpo_qtt' give Firpo (2007) efficient UNCONDITIONAL QTE / QTT by propensity reweighting (analytic influence-function SE); 'conditional_qr' gives the CONDITIONAL QTE (coefficient on D in a quantile regression, Koenker & Bassett 1978); 'distribution' gives the QTT via an IPW counterfactual distribution. Validation: certified parity evidence. Assumptions: For 'firpo_qte' / 'firpo_qtt' / 'distribution': unconfoundedness + overlap; For 'conditional_qr': unconfoundedness conditional on controls; note this is a CONDITIONAL estimand with no causal reading absent rank invariance; Correct parametric quantile model (sensitivity tested via multiple quantiles). Pre-conditions: binary treatment (all methods); continuous outcome; controls cover the confounding set. Failure modes: Large IPW weights (method='ipw') -> Extreme propensities -- trim (sp.trimming) or switch to doubly-robust DR-QTE; Quantile crossing -> Use rearrangement (Chernozhukov-Fernandez-Val-Galichon) or monotone constraints. Alternatives: sp.qdid, sp.rifreg, sp.cic, sp.metalearner. Typical minimum N: 500.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "controls": {
          "description": "Control-variable column names.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "method": {
          "default": "firpo_qte",
          "description": "Estimand / estimator",
          "enum": [
            "firpo_qte",
            "firpo_qtt",
            "conditional_qr",
            "distribution"
          ],
          "type": "string"
        },
        "n_boot": {
          "default": 500,
          "description": "Number of bootstrap replications.",
          "type": "integer"
        },
        "quantiles": {
          "description": "quantiles parameter (list).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "treatment": {
          "description": "Treatment indicator, treatment variable, or treatment array.",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "treatment",
        "y"
      ],
      "type": "object"
    },
    "name": "qte"
  },
  {
    "description": "Panel quantile treatment effects with high-dimensional controls. Validation: validated evidence tier (known-truth, reference, external-parity, or Monte Carlo artifact). Assumptions: Selection-on-observables (unconfoundedness + overlap) or, for IV variants, instrument validity; For IV-QTE: rank invariance / rank similarity (monotonicity of the structural quantile function). Pre-conditions: Covariates, treatment, and outcome; for IV-quantile methods, a valid instrument; Enough data to estimate the outcome distribution across quantiles. Failure modes: Estimated conditional quantiles cross (non-monotone), or tail quantiles are unstable -> Use rearrangement / monotonization and avoid extreme quantiles where data are sparse. Alternatives: sp.qte, sp.iv, sp.dml. Typical minimum N: 500.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "covariates": {
          "description": "Candidate control set; narrowed by double-selection LASSO.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "lasso_alpha": {
          "description": "Penalty on standardised covariates. ``None`` uses the Belloni-Chernozhukov-Hansen plug-in penalty.",
          "type": "number"
        },
        "method": {
          "default": "canay",
          "description": "See the module docstring. ``'canay'`` assumes the individual effect is a pure location shift and needs a reasonably long panel.",
          "enum": [
            "canay",
            "dummy_fe",
            "pooled"
          ],
          "type": "string"
        },
        "n_boot": {
          "default": 200,
          "description": "Number of bootstrap replications.",
          "type": "integer"
        },
        "quantiles": {
          "description": "Defaults to ``(0.1, 0.25, 0.5, 0.75, 0.9)``.",
          "type": "string"
        },
        "se": {
          "default": "bootstrap",
          "description": "``'bootstrap'`` resamples **units**, preserving within-unit dependence. There is no analytic option: Canay's two-step variance depends on the first step, and a naive quantile-regression SE would understate it.",
          "type": "string"
        },
        "seed": {
          "default": 0,
          "description": "Random seed for reproducible stochastic steps.",
          "type": "integer"
        },
        "time": {
          "description": "Time period column.",
          "type": "string"
        },
        "treat": {
          "description": "Treatment indicator or first-treatment-period column.",
          "type": "string"
        },
        "unit": {
          "description": "Unit identifier column.",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "covariates",
        "time",
        "treat",
        "unit",
        "y"
      ],
      "type": "object"
    },
    "name": "qte_hd_panel"
  },
  {
    "description": "Quantile forest for conditional quantiles of ``Y`` (``grf::quantile_forest``). Validation: validated evidence tier (known-truth, reference, external-parity, or Monte Carlo artifact).",
    "input_schema": {
      "properties": {
        "clusters": {
          "description": "Cluster ids (trees sample whole clusters).",
          "type": "string"
        },
        "covariates": {
          "description": "Covariates ``X``.",
          "type": "string"
        },
        "equalize_cluster_weights": {
          "default": false,
          "description": "equalize_cluster_weights parameter (bool).",
          "type": "boolean"
        },
        "honest": {
          "default": true,
          "description": "grf honesty options.",
          "type": "boolean"
        },
        "honesty_fraction": {
          "default": 0.5,
          "description": "grf honesty options.",
          "type": "number"
        },
        "honesty_prune_leaves": {
          "default": true,
          "description": "grf honesty options.",
          "type": "boolean"
        },
        "imbalance_penalty": {
          "default": 0.0,
          "description": "imbalance_penalty parameter (float).",
          "type": "number"
        },
        "max_depth": {
          "description": "max_depth parameter (Optional[int]).",
          "type": "integer"
        },
        "max_samples": {
          "default": 0.5,
          "description": "grf ``sample.fraction``.",
          "type": "number"
        },
        "min_samples_leaf": {
          "default": 5,
          "description": "grf ``min.node.size``.",
          "type": "integer"
        },
        "mtry": {
          "description": "mtry parameter (Optional[int]).",
          "type": "integer"
        },
        "n_estimators": {
          "default": 2000,
          "description": "Trees (grf ``num.trees``).",
          "type": "integer"
        },
        "n_jobs": {
          "default": 1,
          "description": "Number of jobs.",
          "type": "integer"
        },
        "quantiles": {
          "default": [
            0.1,
            0.5,
            0.9
          ],
          "description": "Quantiles that calibrate the splits and are predicted by default.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "random_state": {
          "default": 42,
          "description": "Random seed or RandomState for reproducible stochastic steps.",
          "type": "integer"
        },
        "regression_splitting": {
          "default": false,
          "description": "Use ordinary regression splits instead (grf ``regression.splitting``; the quantile regression forest of Meinshausen 2006).",
          "type": "boolean"
        },
        "split_alpha": {
          "default": 0.05,
          "description": "grf ``alpha`` (minimum child share).",
          "type": "number"
        },
        "weights": {
          "description": "Sample weights (grf ``sample.weights``).",
          "type": "string"
        },
        "y": {
          "description": "Outcome.",
          "type": "string"
        }
      },
      "required": [],
      "type": "object"
    },
    "name": "quantile_forest"
  },
  {
    "description": "Test that quasi-untreateded groups exist (dCDH et al., Section 3.3). Validation: validated evidence tier (known-truth, reference, external-parity, or Monte Carlo artifact).",
    "input_schema": {
      "properties": {
        "dose": {
          "description": "Doses at the horizon being tested. Non-positive values are",
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "required": [
        "dose"
      ],
      "type": "object"
    },
    "name": "quasi_untreated_test"
  },
  {
    "description": "Rank-weighted average treatment effect (RATE) [@yadlowsky2025evaluating]. Validation: certified parity evidence. Assumptions: Unconfoundedness given the covariates; Overlap / positivity; Honesty: separate subsamples are used to choose splits and to estimate effects. Pre-conditions: Covariates, treatment, and outcome with enough data to grow an honest forest. Failure modes: Calibration test rejects -- the forest's heterogeneity is not well calibrated -> Increase the sample / number of trees, or fall back to a doubly-robust learner. Alternatives: sp.dml, sp.auto_cate, sp.tmle. Typical minimum N: 1000.",
    "input_schema": {
      "properties": {
        "T": {
          "description": "The training sample (defaults to the stored arrays).",
          "type": "string"
        },
        "X": {
          "description": "The training sample (defaults to the stored arrays).",
          "type": "string"
        },
        "Y": {
          "description": "The training sample (defaults to the stored arrays).",
          "type": "string"
        },
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "cluster": {
          "description": "Fixed-effects forests only; ``'dyadic'`` needs ``members=``.",
          "type": "string"
        },
        "covariates": {
          "default": "none",
          "description": "Fixed-effects forests only: time-varying covariates for the untreated model behind the imputation scores. Returns dict with keys ``estimate``, ``se``, ``ci_low``, ``ci_high``, ``target``, ``toc_curve`` (``(q_grid, 2)``), ``n``, ``method``, ``priority_source``, ``n_clusters``. References [@yadlowsky2025evaluating] Examples >>> import numpy as np >>> import pandas as pd >>> import statspai as sp >>> rng = np.random.default_rng(42) >>> n = 400 >>> X = rng.normal(size=(n, 3)) >>> T = rng.binomial(1, 0.5, size=n) >>> tau = 1.0 + X[:, 0] # heterogeneous effect >>> Y = X[:, 1] + tau * T + rng.normal(scale=0.5, size=n) >>> df = pd.DataFrame({ ... \"y\": Y, \"d\": T, ... \"x0\": X[:, 0], \"x1\": X[:, 1], \"x2\": X[:, 2], ... }) >>> cf = sp.causal_forest( ... data=df, formula=\"y ~ d | x0 + x1 + x2\", ... n_estimators=50, random_state=0, ... ) >>> res = sp.rate(cf, target=\"AUTOC\") >>> sorted(res.keys()) ['ci_high', 'ci_low', 'estimate', 'method', 'n', 'n_clusters', \\ >>> res[\"toc_curve\"].shape (100, 2)",
          "type": "string"
        },
        "forest": {
          "description": "forest parameter ('CausalForest').",
          "type": "string"
        },
        "members": {
          "description": "Fixed-effects forests only: the two members of each dyadic row.",
          "type": "string"
        },
        "n_bootstrap": {
          "default": 200,
          "description": "Half-sample draws for ``se_method='half_sample'``.",
          "type": "integer"
        },
        "priorities": {
          "description": "Priority score per training unit (higher = treat first). Defaults to the forest's CATE predictions.",
          "type": "string"
        },
        "q_grid": {
          "default": 100,
          "description": "Number of equally spaced points in ``(0, 1]`` at which the TOC curve is reported. Does not affect the estimate.",
          "type": "integer"
        },
        "se_method": {
          "default": "auto",
          "description": "``'auto'`` is ``'influence'``, or ``'imputation'`` for a forest with fixed effects. ``'imputation'`` is defined only for those.",
          "enum": [
            "auto",
            "influence",
            "half_sample",
            "imputation"
          ],
          "type": "string"
        },
        "seed": {
          "description": "Seed for ``se_method='half_sample'``.",
          "type": "integer"
        },
        "target": {
          "default": "AUTOC",
          "description": "target parameter (str).",
          "enum": [
            "AUTOC",
            "QINI"
          ],
          "type": "string"
        },
        "variance": {
          "default": "bjs",
          "description": "Fixed-effects forests only: how treated residuals are centred before the cohort x event-time blocks. ``'bjs'`` (default here) is conservative for the population RATE, ``'forest'`` exact for the sample's -- see above.",
          "enum": [
            "bjs",
            "forest"
          ],
          "type": "string"
        }
      },
      "required": [
        "forest"
      ],
      "type": "object"
    },
    "name": "rate"
  },
  {
    "description": "Rank-weighted average treatment effect (AUTOC / QINI) of a causal forest's targeting rule, fitted and evaluated on disjoint units. sp.rate ranks the rows it also scores, which for a fe= forest is not a valid test -- every imputation score carries -gamma_hat_t from the periods the forest saw, so a nominal 5% test rejected 17.5% of the time under no heterogeneity at all (AUTOC averaged -0.025, not 0). Splitting units (or dyadic members) brings that to 7.5% and +0.0008, at 99.5% power. Validation: validated evidence tier (known-truth, reference, external-parity, or Monte Carlo artifact). Assumptions: fe= forests: parallel trends and no anticipation (imputation); pooled forests: unconfoundedness and overlap (AIPW); the two halves are independent: split units, never rows. Pre-conditions: forest fitted with sp.causal_forest (default GRF engine); enough units that both halves keep treated and untreated cells. Failure modes: DataInsufficient: the evaluation half has no variation -> The panel is too small to split; run sp.rate with priorities= from a rule fitted elsewhere. Alternatives: sp.rate, sp.forest_group_effects, sp.cate_eval.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "cluster": {
          "description": "None (forest clusters), 'dyadic' or cluster ids.",
          "type": "string"
        },
        "covariates": {
          "default": "none",
          "description": "fe= forests: covariates in the untreated outcome model.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "forest": {
          "description": "Fitted GRF-engine forest; supplies the data and the hyper-parameters, and is left untouched.",
          "type": "string"
        },
        "members": {
          "description": "(n, 2) members of each dyadic row; splits by member and drops rows straddling the halves.",
          "type": "string"
        },
        "n_splits": {
          "default": 21,
          "description": "Splits aggregated by Chernozhukov et al. (2025) VEIN: median estimate, median of conditional intervals built at 1 - alpha/2, p doubled. n_splits=1 is one draw and warns.",
          "type": "integer"
        },
        "q_grid": {
          "default": 100,
          "description": "Points on the TOC curve.",
          "type": "integer"
        },
        "random_state": {
          "default": 0,
          "description": "Seeds the sequence of splits.",
          "type": "integer"
        },
        "se_method": {
          "default": "auto",
          "description": "'auto' picks 'imputation' for fe= forests, 'influence' otherwise.",
          "enum": [
            "auto",
            "imputation",
            "influence",
            "half_sample"
          ],
          "type": "string"
        },
        "target": {
          "default": "AUTOC",
          "description": "RATE weighting.",
          "enum": [
            "AUTOC",
            "QINI"
          ],
          "type": "string"
        },
        "train_frac": {
          "default": 0.5,
          "description": "Share of units (members) that fit the rule.",
          "type": "number"
        },
        "variance": {
          "default": "bjs",
          "description": "fe= forests: 'bjs' is conservative for the population RATE, 'forest' exact for the realised sample's.",
          "enum": [
            "bjs",
            "forest"
          ],
          "type": "string"
        }
      },
      "required": [
        "forest"
      ],
      "type": "object"
    },
    "name": "rate_split"
  },
  {
    "description": "Boundary discontinuity design: pointwise effects along a 2-D boundary. Validation: certified parity evidence. Assumptions: Conditional expectations of potential outcomes are continuous at the cutoff; Units cannot precisely manipulate the running variable around the cutoff (no sorting); For fuzzy designs: monotonicity of treatment take-up at the cutoff. Pre-conditions: A continuous running/forcing variable with a known cutoff that (sharply or fuzzily) assigns treatment; Enough observations in a neighbourhood of the cutoff to fit a local polynomial. Failure modes: Density of the running variable jumps at the cutoff (manipulation / sorting) -> Run a McCrary / density test (rdplotdensity); if manipulation is present the design is invalid near the cutoff; Estimate swings with the bandwidth -- results are not robust -> Report a bandwidth-sensitivity curve and use a data-driven MSE-optimal bandwidth. Alternatives: sp.rdrobust, sp.rdrandinf, sp.rdbwselect. Typical minimum N: 500.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "``1 - alpha`` is R's ``level / 100``.",
          "type": "number"
        },
        "approach": {
          "default": "location",
          "description": "See the module docstring.",
          "enum": [
            "location",
            "distance",
            "pooled"
          ],
          "type": "string"
        },
        "boundary": {
          "description": "``f(x1) -> x2`` describing the boundary; used only to place default evaluation points (``n_eval`` of them) and by ``approach=\"pooled\"``. ``None`` means the line ``x1 = 0``.",
          "type": "string"
        },
        "bwcheck": {
          "default": "auto",
          "description": "Minimum number of observations (or mass points) inside every bandwidth; ``'auto'`` is R's default ``50 + p + 1``.",
          "type": "integer"
        },
        "bwparam": {
          "default": "main",
          "description": "Fuzzy designs: select the bandwidth for the ratio or the ITT.",
          "enum": [
            "main",
            "itt"
          ],
          "type": "string"
        },
        "bwselect": {
          "default": "mserd",
          "description": "``mserd``, ``cerrd``, ``imserd``, ``icerrd``, ``msetwo``, ``certwo``, ``imsetwo`` or ``icertwo``.",
          "type": "string"
        },
        "cluster": {
          "description": "Cluster identifier (vce must then be hc0 or hc1).",
          "type": "string"
        },
        "cqt": {
          "default": 0.5,
          "description": "Distance approach: quantile of the distance below which the pilot polynomial for the bias constant is fitted.",
          "type": "number"
        },
        "deriv": {
          "default": [
            0,
            0
          ],
          "description": "Location approach: partial derivative of the effect to estimate.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "distance": {
          "description": "Distance approach: signed distances (non-negative on the treated side) to each evaluation point. Default: Euclidean distance to ``eval_points`` signed by ``treatment``.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "eval_points": {
          "description": "Boundary points (R's ``b``). Default: ``n_eval`` points along ``boundary`` between the 10th and 90th percentiles of the data.",
          "type": "string"
        },
        "fitmethod": {
          "default": "joint",
          "description": "``joint`` applies the degrees-of-freedom correction to both sides together (R's default).",
          "enum": [
            "joint",
            "separate"
          ],
          "type": "string"
        },
        "fuzzy": {
          "description": "Treatment take-up for a fuzzy design; the effect is the ratio of the outcome and take-up jumps.",
          "type": "string"
        },
        "h": {
          "description": "Bandwidth. ``location``: scalar or ``(k, 4)`` array ``(h01, h02, h11, h12)``; ``distance``: scalar or ``(k, 2)`` array ``(h0, h1)``. ``None`` selects it (R ``rdbw2d`` / ``rdbw2d.distance``).",
          "type": "number"
        },
        "kernel": {
          "default": "triangular",
          "description": "Kernel function used for weighting or smoothing.",
          "enum": [
            "triangular",
            "epanechnikov",
            "uniform",
            "gaussian"
          ],
          "type": "string"
        },
        "kernel_type": {
          "default": "prod",
          "description": "Location approach: product or radial kernel.",
          "enum": [
            "prod",
            "rad"
          ],
          "type": "string"
        },
        "kink_position": {
          "description": "Distance approach: 0-based indices (or a boolean mask) of evaluation points that are known kinks of the boundary.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "kink_unknown": {
          "default": false,
          "description": "Distance approach: boundary may have kinks at unknown places; undersmooth the bandwidth (first) and the bias-correction bandwidth (second).",
          "type": "boolean"
        },
        "masspoints": {
          "default": "check",
          "description": "masspoints parameter (str).",
          "enum": [
            "check",
            "adjust",
            "off"
          ],
          "type": "string"
        },
        "method": {
          "default": "dpi",
          "description": "Location approach: pilot bandwidth rule for the bias constants.",
          "enum": [
            "dpi",
            "rot"
          ],
          "type": "string"
        },
        "n_eval": {
          "default": 1,
          "description": "Number of eval.",
          "type": "integer"
        },
        "p": {
          "default": 1,
          "description": "Polynomial order of the point estimator.",
          "type": "integer"
        },
        "q": {
          "description": "Order of the bias-correction fit; default ``p + 1`` (``p`` for the distance approach with ``kink_unknown``).",
          "type": "integer"
        },
        "scalebiascrct": {
          "default": 1.0,
          "description": "Location approach: weight of the higher-order bias correction in the bandwidth selector.",
          "type": "number"
        },
        "scaleregul": {
          "description": "Regularisation weight of the bandwidth selector; default is R's (3 for location, 1 for distance).",
          "type": "number"
        },
        "side": {
          "default": "two",
          "description": "Two-sided or one-sided intervals.",
          "enum": [
            "two",
            "left",
            "right"
          ],
          "type": "string"
        },
        "stdvars": {
          "default": true,
          "description": "Location approach: standardise x1, x2 before selecting bandwidths.",
          "type": "boolean"
        },
        "tangvec": {
          "description": "Location approach: tangential direction for a directional derivative (overrides ``deriv``).",
          "type": "string"
        },
        "treatment": {
          "description": "0/1 assignment (R's ``assignment``); defines the two sides of the boundary.",
          "type": "string"
        },
        "vce": {
          "default": "hc1",
          "description": "vce parameter (str).",
          "enum": [
            "hc1",
            "hc0",
            "hc2",
            "hc3"
          ],
          "type": "string"
        },
        "weights": {
          "description": "WBATE weights over the evaluation points (R ``summary(..., WBATE=)``); normalised to sum to one. Default: equal weights.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "x1": {
          "description": "The two running variables.",
          "type": "string"
        },
        "x2": {
          "description": "The two running variables.",
          "type": "string"
        },
        "y": {
          "description": "Outcome.",
          "type": "string"
        }
      },
      "required": [
        "treatment",
        "x1",
        "x2",
        "y"
      ],
      "type": "object"
    },
    "name": "rd2d"
  },
  {
    "description": "Bandwidth selection for boundary discontinuity designs. Validation: certified parity evidence.",
    "input_schema": {
      "properties": {
        "approach": {
          "default": "location",
          "description": "Bivariate fit at boundary points, or a univariate fit on the signed distance. ``sp.rd2d``'s ``'pooled'`` has no bandwidth selector of its own -- call :func:`sp.rdbwselect` on the signed distance instead.",
          "enum": [
            "location",
            "distance"
          ],
          "type": "string"
        },
        "boundary": {
          "description": "``f(x1) -> x2`` tracing the boundary; ``None`` uses ``x1 = 0``.",
          "type": "string"
        },
        "bwcheck": {
          "default": "auto",
          "description": "bwcheck parameter (Union[int, str, None]).",
          "type": "integer"
        },
        "bwparam": {
          "default": "main",
          "description": "bwparam parameter (str).",
          "type": "string"
        },
        "bwselect": {
          "default": "mserd",
          "description": "bwselect parameter (str).",
          "type": "string"
        },
        "cluster": {
          "description": "Cluster identifier column for clustered standard errors.",
          "type": "string"
        },
        "cqt": {
          "default": 0.5,
          "description": "cqt parameter (float).",
          "type": "number"
        },
        "deriv": {
          "default": [
            0,
            0
          ],
          "description": "deriv parameter (Tuple[int, int]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "distance": {
          "description": "distance parameter (Optional[Union[np.ndarray, List[str]]]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "eval_points": {
          "description": "eval_points parameter (Optional[np.ndarray]).",
          "type": "string"
        },
        "fitmethod": {
          "default": "joint",
          "description": "fitmethod parameter (str).",
          "type": "string"
        },
        "fuzzy": {
          "description": "fuzzy parameter (Optional[str]).",
          "type": "string"
        },
        "kernel": {
          "default": "triangular",
          "description": "``'triangular'``, ``'uniform'`` or ``'epanechnikov'``.",
          "type": "string"
        },
        "kernel_type": {
          "default": "prod",
          "description": "kernel_type parameter (str).",
          "type": "string"
        },
        "kink_position": {
          "description": "kink_position parameter (Optional[Sequence]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "kink_unknown": {
          "default": false,
          "description": "kink_unknown parameter (Union[bool, Tuple[bool, bool]]).",
          "type": "boolean"
        },
        "masspoints": {
          "default": "check",
          "description": "masspoints parameter (str).",
          "type": "string"
        },
        "method": {
          "default": "dpi",
          "description": "Estimator or algorithm variant to use.",
          "type": "string"
        },
        "n_eval": {
          "default": 1,
          "description": "Number of eval.",
          "type": "integer"
        },
        "p": {
          "default": 1,
          "description": "Polynomial order of the point estimator.",
          "type": "integer"
        },
        "scalebiascrct": {
          "default": 1.0,
          "description": "scalebiascrct parameter (float).",
          "type": "number"
        },
        "scaleregul": {
          "default": 1.0,
          "description": "scaleregul parameter (float).",
          "type": "number"
        },
        "stdvars": {
          "default": true,
          "description": "stdvars parameter (bool).",
          "type": "boolean"
        },
        "tangvec": {
          "description": "tangvec parameter (Optional[np.ndarray]).",
          "type": "string"
        },
        "treatment": {
          "description": "Column names, as in :func:`sp.rd2d`.",
          "type": "string"
        },
        "vce": {
          "default": "hc1",
          "description": "vce parameter (str).",
          "type": "string"
        },
        "x1": {
          "description": "Column names, as in :func:`sp.rd2d`.",
          "type": "string"
        },
        "x2": {
          "description": "Column names, as in :func:`sp.rd2d`.",
          "type": "string"
        },
        "y": {
          "description": "Column names, as in :func:`sp.rd2d`.",
          "type": "string"
        }
      },
      "required": [
        "treatment",
        "x1",
        "x2",
        "y"
      ],
      "type": "object"
    },
    "name": "rd2d_bw"
  },
  {
    "description": "2D boundary RD visualization.",
    "input_schema": {
      "properties": {
        "ax": {
          "description": "ax parameter (Optional[Any]).",
          "type": "string"
        },
        "boundary": {
          "description": "Boundary function ``f(x1) -> x2``; ``None`` draws the line ``x1 = 0``.",
          "type": "string"
        },
        "figsize": {
          "default": [
            10,
            8
          ],
          "description": "figsize parameter (Tuple[float, float]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "plot_type": {
          "default": "scatter",
          "description": "plot_type parameter (str).",
          "enum": [
            "scatter",
            "heatmap",
            "boundary_effects"
          ],
          "type": "string"
        },
        "result": {
          "description": "Result of :func:`sp.rd2d`; its evaluation points are marked on the scatter, and ``plot_type='boundary_effects'`` plots its pointwise bias-corrected effects with robust intervals.",
          "type": "string"
        },
        "treatment": {
          "description": "Binary assignment indicator.",
          "type": "string"
        },
        "x1": {
          "description": "Running variable names.",
          "type": "string"
        },
        "x2": {
          "description": "Running variable names.",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable name.",
          "type": "string"
        }
      },
      "required": [
        "treatment",
        "x1",
        "x2",
        "y"
      ],
      "type": "object"
    },
    "name": "rd2d_plot"
  },
  {
    "description": "Bayesian RDD allowing CATE to depend on covariates. Assumptions: Conditional expectations of potential outcomes are continuous at the cutoff; Units cannot precisely manipulate the running variable around the cutoff (no sorting); For fuzzy designs: monotonicity of treatment take-up at the cutoff. Pre-conditions: A continuous running/forcing variable with a known cutoff that (sharply or fuzzily) assigns treatment; Enough observations in a neighbourhood of the cutoff to fit a local polynomial. Failure modes: Density of the running variable jumps at the cutoff (manipulation / sorting) -> Run a McCrary / density test (rdplotdensity); if manipulation is present the design is invalid near the cutoff; Estimate swings with the bandwidth -- results are not robust -> Report a bandwidth-sensitivity curve and use a data-driven MSE-optimal bandwidth. Alternatives: sp.rdrobust, sp.rdrandinf, sp.rdbwselect. Typical minimum N: 500.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "bandwidth": {
          "description": "Bandwidth used for local smoothing or kernel weighting.",
          "type": "number"
        },
        "covariates": {
          "description": "Covariate matrix, DataFrame, or column names.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "cutoff": {
          "default": 0.0,
          "description": "cutoff parameter (float).",
          "type": "number"
        },
        "kernel": {
          "default": "triangular",
          "description": "Kernel function used for weighting or smoothing.",
          "type": "string"
        },
        "n_draws": {
          "default": 2000,
          "description": "Number of draws.",
          "type": "integer"
        },
        "running": {
          "description": "running parameter (str).",
          "type": "string"
        },
        "seed": {
          "default": 0,
          "description": "Random seed for reproducible stochastic steps.",
          "type": "integer"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "covariates",
        "running",
        "y"
      ],
      "type": "object"
    },
    "name": "rd_bayes_hte"
  },
  {
    "description": "Bias-aware confidence interval for fuzzy RD via Anderson-Rubin test inversion (Noack-Rothe 2024 Econometrica). Robust to weak first stages and avoids the power asymmetry of conventional 2SLS-style fuzzy RD CIs (Kaliski-Keane-Neal 2025). Validation: certified parity evidence. Assumptions: Continuity of potential outcomes in x at c (Hahn, Todd, van der Klaauw 2001); No manipulation of x at c (McCrary density test); Local randomization only in a neighborhood of c -- extrapolation away from c is not identified. Pre-conditions: running variable x is continuous with support on both sides of c; treatment assignment is determined by the cutoff c (sharp) or probabilistically at c (fuzzy); sufficient mass of observations within the optimal bandwidth. Failure modes: McCrary density test p < 0.05 -> Use donut-hole RD (donut=<delta>) or partial-identification bounds; Covariate imbalance at cutoff (sp.rdbalance rejects) -> Include covariates as controls, narrow bandwidth, or report as caveat; Effect unstable across bandwidth halvings -> Report sp.rdbwsensitivity and sp.rd_honest (Armstrong-Kolesar honest CI). Alternatives: sp.rdrobust, sp.rd_honest, sp.rdrbounds, sp.bounds. Typical minimum N: 5...",
    "input_schema": {
      "properties": {
        "M_d": {
          "description": "Bound on |g_D''|; auto if None",
          "type": "number"
        },
        "M_y": {
          "description": "Bound on |g_Y''|; auto if None",
          "type": "number"
        },
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "c": {
          "default": 0.0,
          "description": "c parameter (float).",
          "type": "number"
        },
        "fuzzy": {
          "description": "Treatment indicator column",
          "type": "string"
        },
        "h": {
          "description": "Bandwidth used for local smoothing or kernel weighting.",
          "type": "number"
        },
        "kernel": {
          "default": "triangular",
          "description": "Kernel function used for weighting or smoothing.",
          "enum": [
            "triangular",
            "epanechnikov",
            "uniform"
          ],
          "type": "string"
        },
        "x": {
          "description": "Primary running variable, regressor, or feature input for this estimator.",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "fuzzy",
        "x",
        "y"
      ],
      "type": "object"
    },
    "name": "rd_bias_aware_fuzzy"
  },
  {
    "description": "Gradient Boosting for RD -- flexible CATE estimation.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level.",
          "type": "number"
        },
        "c": {
          "default": 0,
          "description": "RD cutoff.",
          "type": "number"
        },
        "covs": {
          "description": "Covariate names for heterogeneity.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "h": {
          "description": "Bandwidth (auto-selected if None).",
          "type": "number"
        },
        "learning_rate": {
          "default": 0.1,
          "description": "Shrinkage factor.",
          "type": "number"
        },
        "max_depth": {
          "default": 3,
          "description": "Maximum tree depth per round.",
          "type": "integer"
        },
        "n_estimators": {
          "default": 200,
          "description": "Number of boosting rounds.",
          "type": "integer"
        },
        "seed": {
          "default": 42,
          "description": "Random seed.",
          "type": "integer"
        },
        "x": {
          "description": "Running variable.",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable.",
          "type": "string"
        }
      },
      "required": [
        "x",
        "y"
      ],
      "type": "object"
    },
    "name": "rd_boost"
  },
  {
    "description": "Run multiple ML-RD methods and compare CATE estimates.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level.",
          "type": "number"
        },
        "c": {
          "default": 0,
          "description": "RD cutoff.",
          "type": "number"
        },
        "covs": {
          "description": "Covariates for heterogeneity / selection.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "h": {
          "description": "Bandwidth (shared across methods).",
          "type": "number"
        },
        "methods": {
          "description": "Subset of ``['forest', 'boost', 'lasso']``. Default: all three.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "seed": {
          "default": 42,
          "description": "Random seed.",
          "type": "integer"
        },
        "x": {
          "description": "Running variable.",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable.",
          "type": "string"
        }
      },
      "required": [
        "x",
        "y"
      ],
      "type": "object"
    },
    "name": "rd_cate_summary"
  },
  {
    "description": "Compare multiple RD estimators on the same data.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Confidence level forwarded to each estimator that accepts ``alpha``.",
          "type": "number"
        },
        "c": {
          "default": 0.0,
          "description": "c parameter (float).",
          "type": "number"
        },
        "fuzzy": {
          "description": "Fuzzy treatment column passed through to all methods that accept it.",
          "type": "string"
        },
        "method_kwargs": {
          "description": "Per-method extra kwargs, e.g. ``{'rdrobust': {'kernel': 'uniform'}}``.",
          "type": "object"
        },
        "methods": {
          "default": [
            "rdrobust",
            "honest",
            "randinf"
          ],
          "description": "Method aliases recognised by the :data:`sp.rd._RD_METHOD_ALIASES` dispatcher. Default: ``('rdrobust', 'honest', 'randinf')``.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "x": {
          "description": "Primary running variable, regressor, or feature input for this estimator.",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "x",
        "y"
      ],
      "type": "object"
    },
    "name": "rd_compare"
  },
  {
    "description": "Four-panel RD diagnostic dashboard.",
    "input_schema": {
      "properties": {
        "bw_grid": {
          "description": "Bandwidths to evaluate in the sensitivity panel. If None, uses ``[0.5, 0.75, 1.0, 1.25, 1.5, 2.0] x h_mse``.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "c": {
          "default": 0.0,
          "description": "Cutoff.",
          "type": "number"
        },
        "covs": {
          "description": "Covariates for the balance panel. If None, the panel shows the density's binomial near-cutoff test instead.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "figsize": {
          "default": [
            12,
            9
          ],
          "description": "figsize parameter (Tuple[float, float]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "fuzzy": {
          "description": "Fuzzy treatment column (passed through to RD plot/sensitivity).",
          "type": "string"
        },
        "h": {
          "description": "Reference bandwidth used for plotting and as the basis for ``bw_grid``. If None, MSE-optimal bandwidth from rdrobust.",
          "type": "number"
        },
        "save": {
          "description": "If a path is given, also save the figure (extension determines format).",
          "type": "string"
        },
        "title": {
          "description": "Suptitle.",
          "type": "string"
        },
        "x": {
          "description": "Primary running variable, regressor, or feature input for this estimator.",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "x",
        "y"
      ],
      "type": "object"
    },
    "name": "rd_dashboard"
  },
  {
    "description": "Honest CI for RD when the running variable takes only a moderate number of distinct values (Kolesar-Rothe 2018 AER). Uses bounded second derivative or bounded misspecification smoothness classes; robust to the loss of asymptotics that affects rdrobust under sparse mass points. Validation: certified parity evidence. Assumptions: Continuity of potential outcomes in x at c (Hahn, Todd, van der Klaauw 2001); No manipulation of x at c (McCrary density test); Local randomization only in a neighborhood of c -- extrapolation away from c is not identified. Pre-conditions: running variable x is continuous with support on both sides of c; treatment assignment is determined by the cutoff c (sharp) or probabilistically at c (fuzzy); sufficient mass of observations within the optimal bandwidth. Failure modes: McCrary density test p < 0.05 -> Use donut-hole RD (donut=<delta>) or partial-identification bounds; Covariate imbalance at cutoff (sp.rdbalance rejects) -> Include covariates as controls, narrow bandwidth, or report as caveat; Effect unstable across bandwidth halvings -> Report sp.rdbwsensitivity and sp.rd_honest (Armstrong-Kolesar honest CI). Alternatives: sp.rdrobust, sp.rd_honest, sp.r...",
    "input_schema": {
      "properties": {
        "K": {
          "description": "Bound on per-side linear-approximation bias; auto if None (BM method)",
          "type": "number"
        },
        "M": {
          "description": "Bound on |g''|; auto if None (BSD method)",
          "type": "number"
        },
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "c": {
          "default": 0.0,
          "description": "c parameter (float).",
          "type": "number"
        },
        "h": {
          "description": "Bandwidth used for local smoothing or kernel weighting.",
          "type": "number"
        },
        "kernel": {
          "default": "triangular",
          "description": "``'bsd'`` kernel.",
          "enum": [
            "triangular",
            "uniform",
            "epanechnikov"
          ],
          "type": "string"
        },
        "method": {
          "default": "bsd",
          "description": "Smoothness class",
          "enum": [
            "bsd",
            "bm"
          ],
          "type": "string"
        },
        "opt_criterion": {
          "default": "MSE",
          "description": "``'bsd'`` bandwidth criterion when ``h`` is None.",
          "enum": [
            "MSE",
            "FLCI",
            "OCI"
          ],
          "type": "string"
        },
        "order": {
          "default": 0,
          "description": "``'bme'``: order of the polynomial on each side (RDHonestBME's ``order``; 0 compares side means).",
          "type": "integer"
        },
        "x": {
          "description": "Discrete running variable",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "x",
        "y"
      ],
      "type": "object"
    },
    "name": "rd_discrete"
  },
  {
    "description": "Distribution-valued sharp RDD. Assumptions: Conditional expectations of potential outcomes are continuous at the cutoff; Units cannot precisely manipulate the running variable around the cutoff (no sorting); For fuzzy designs: monotonicity of treatment take-up at the cutoff. Pre-conditions: A continuous running/forcing variable with a known cutoff that (sharply or fuzzily) assigns treatment; Enough observations in a neighbourhood of the cutoff to fit a local polynomial; running variable x is continuous with support on both sides of c. Failure modes: Density of the running variable jumps at the cutoff (manipulation / sorting) -> Run a McCrary / density test (rdplotdensity); if manipulation is present the design is invalid near the cutoff; Estimate swings with the bandwidth -- results are not robust -> Report a bandwidth-sensitivity curve and use a data-driven MSE-optimal bandwidth; McCrary density test p < 0.05 -> Use donut-hole RD (donut=<delta>) or partial-identification bounds. Alternatives: sp.rdrobust, sp.rdrandinf, sp.rdbwselect, sp.rd_honest. Typical minimum N: 500.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "bandwidth": {
          "description": "Bandwidth used for local smoothing or kernel weighting.",
          "type": "number"
        },
        "cutoff": {
          "default": 0.0,
          "description": "cutoff parameter (float).",
          "type": "number"
        },
        "kernel": {
          "default": "triangular",
          "description": "Kernel function used for weighting or smoothing.",
          "type": "string"
        },
        "quantiles": {
          "description": "Defaults to (0.1, 0.25, 0.5, 0.75, 0.9).",
          "type": "string"
        },
        "running": {
          "description": "running parameter (str).",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "running",
        "y"
      ],
      "type": "object"
    },
    "name": "rd_distribution"
  },
  {
    "description": "Joint RDD + RKD on the conditional distribution of Y. Assumptions: Conditional expectations of potential outcomes are continuous at the cutoff; Units cannot precisely manipulate the running variable around the cutoff (no sorting); For fuzzy designs: monotonicity of treatment take-up at the cutoff. Pre-conditions: A continuous running/forcing variable with a known cutoff that (sharply or fuzzily) assigns treatment; Enough observations in a neighbourhood of the cutoff to fit a local polynomial. Failure modes: Density of the running variable jumps at the cutoff (manipulation / sorting) -> Run a McCrary / density test (rdplotdensity); if manipulation is present the design is invalid near the cutoff; Estimate swings with the bandwidth -- results are not robust -> Report a bandwidth-sensitivity curve and use a data-driven MSE-optimal bandwidth. Alternatives: sp.rdrobust, sp.rdrandinf, sp.rdbwselect. Typical minimum N: 500.",
    "input_schema": {
      "properties": {
        "bandwidth": {
          "description": "Bandwidth used for local smoothing or kernel weighting.",
          "type": "number"
        },
        "cutoff": {
          "default": 0.0,
          "description": "cutoff parameter (float).",
          "type": "number"
        },
        "kernel": {
          "default": "triangular",
          "description": "Kernel function used for weighting or smoothing.",
          "type": "string"
        },
        "quantiles": {
          "description": "quantiles parameter (Optional[np.ndarray]).",
          "type": "string"
        },
        "running": {
          "description": "running parameter (str).",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "running",
        "y"
      ],
      "type": "object"
    },
    "name": "rd_distributional_design"
  },
  {
    "description": "Diagnostic assessment of RD external validity. Assumptions: Conditional expectations of potential outcomes are continuous at the cutoff; Units cannot precisely manipulate the running variable around the cutoff (no sorting); For fuzzy designs: monotonicity of treatment take-up at the cutoff. Pre-conditions: A continuous running/forcing variable with a known cutoff that (sharply or fuzzily) assigns treatment; Enough observations in a neighbourhood of the cutoff to fit a local polynomial. Failure modes: Density of the running variable jumps at the cutoff (manipulation / sorting) -> Run a McCrary / density test (rdplotdensity); if manipulation is present the design is invalid near the cutoff; Estimate swings with the bandwidth -- results are not robust -> Report a bandwidth-sensitivity curve and use a data-driven MSE-optimal bandwidth. Alternatives: sp.rdrobust, sp.rdrandinf, sp.rdbwselect. Typical minimum N: 500.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level.",
          "type": "number"
        },
        "c": {
          "default": 0,
          "description": "RD cutoff value.",
          "type": "number"
        },
        "covs": {
          "description": "Covariate names for overlap and CI testing.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "target_x_range": {
          "description": "Running variable range ``(x_low, x_high)`` defining the target population. Defaults to the full data range.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "x": {
          "description": "Running variable name.",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable name.",
          "type": "string"
        }
      },
      "required": [
        "x",
        "y"
      ],
      "type": "object"
    },
    "name": "rd_external_validity"
  },
  {
    "description": "Angrist-Rokkanen (2015) extrapolation of RD effects away from the cutoff. Assumptions: Conditional expectations of potential outcomes are continuous at the cutoff; Units cannot precisely manipulate the running variable around the cutoff (no sorting); For fuzzy designs: monotonicity of treatment take-up at the cutoff. Pre-conditions: A continuous running/forcing variable with a known cutoff that (sharply or fuzzily) assigns treatment; Enough observations in a neighbourhood of the cutoff to fit a local polynomial; running variable x is continuous with support on both sides of c. Failure modes: Density of the running variable jumps at the cutoff (manipulation / sorting) -> Run a McCrary / density test (rdplotdensity); if manipulation is present the design is invalid near the cutoff; Estimate swings with the bandwidth -- results are not robust -> Report a bandwidth-sensitivity curve and use a data-driven MSE-optimal bandwidth; McCrary density test p < 0.05 -> Use donut-hole RD (donut=<delta>) or partial-identification bounds. Alternatives: sp.rdrobust, sp.rdrandinf, sp.rdbwselect, sp.rd_honest. Typical minimum N: 500.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level.",
          "type": "number"
        },
        "c": {
          "default": 0,
          "description": "RD cutoff value.",
          "type": "number"
        },
        "covs": {
          "description": "Covariate names for conditional independence. Required.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "eval_points": {
          "description": "Running variable values at which to extrapolate the CATE. If None, ``n_eval`` equally spaced points spanning the data range.",
          "type": "string"
        },
        "h_local": {
          "description": "Bandwidth for local RD estimate at the cutoff (for comparison). If None, the MSE-optimal bandwidth from ``rdrobust`` is used.",
          "type": "number"
        },
        "method": {
          "default": "ols",
          "description": "Estimation method: ``'ols'``, ``'ipw'``, or ``'doubly_robust'``.",
          "type": "string"
        },
        "n_eval": {
          "default": 20,
          "description": "Number of evaluation points when ``eval_points`` is None.",
          "type": "integer"
        },
        "treatment": {
          "description": "Treatment variable for fuzzy RD. If None, sharp design assumed (D = 1{X >= c}).",
          "type": "string"
        },
        "x": {
          "description": "Running variable name.",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable name.",
          "type": "string"
        }
      },
      "required": [
        "x",
        "y"
      ],
      "type": "object"
    },
    "name": "rd_extrapolate"
  },
  {
    "description": "RD with flexible covariate adjustment via cross-fit ML residualisation (Noack-Olma-Rothe 2025). Reduces variance of tau at the cutoff by subtracting an ML estimate of E[Y|W] before running rdrobust; consistent under free-of-cutoff continuity of eta, asymptotically efficient when eta converges to E[Y|X=c, W]. Assumptions: Continuity-based RD identification at the cutoff: potential outcomes are continuous in the running variable except for the treatment jump; Cross-fit ML residualisation of the outcome (and treatment, when fuzzy) on covariates only removes outcome variance and does not bias the cutoff estimate, requiring honest K-fold cross-fitting; Covariates predict the outcome well enough to shorten CIs relative to plain rdrobust; covariates are pre-determined (not affected by treatment). Pre-conditions: data has continuous running variable with adequate mass on both sides of the cutoff; Covariates list valid pre-treatment columns (or is empty/None to fall back to rdrobust); n_folds>=2 for genuine cross-fitting. Failure modes: Sparse data near the cutoff makes the local fit and learner unstable -> Widen the bandwidth via bwselect or collect more mass around the cutoff; Covariates...",
    "input_schema": {
      "properties": {
        "W": {
          "description": "Covariates used by the flexible adjustment",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "c": {
          "default": 0.0,
          "description": "c parameter (float).",
          "type": "number"
        },
        "fuzzy": {
          "description": "fuzzy parameter (str).",
          "type": "string"
        },
        "kernel": {
          "default": "triangular",
          "description": "Kernel function used for weighting or smoothing.",
          "enum": [
            "triangular",
            "epanechnikov",
            "uniform"
          ],
          "type": "string"
        },
        "learner": {
          "default": "boost",
          "description": "Built-in learner",
          "enum": [
            "boost",
            "forest",
            "ridge",
            "lasso"
          ],
          "type": "string"
        },
        "n_folds": {
          "default": 5,
          "description": "Cross-fit folds (1 disables CV)",
          "type": "integer"
        },
        "x": {
          "description": "Primary running variable, regressor, or feature input for this estimator.",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "x",
        "y"
      ],
      "type": "object"
    },
    "name": "rd_flex"
  },
  {
    "description": "Causal Forest for RD -- heterogeneous treatment effect estimation. Assumptions: Conditional expectations of potential outcomes are continuous at the cutoff; Units cannot precisely manipulate the running variable around the cutoff (no sorting); For fuzzy designs: monotonicity of treatment take-up at the cutoff. Pre-conditions: A continuous running/forcing variable with a known cutoff that (sharply or fuzzily) assigns treatment; Enough observations in a neighbourhood of the cutoff to fit a local polynomial. Failure modes: Density of the running variable jumps at the cutoff (manipulation / sorting) -> Run a McCrary / density test (rdplotdensity); if manipulation is present the design is invalid near the cutoff; Estimate swings with the bandwidth -- results are not robust -> Report a bandwidth-sensitivity curve and use a data-driven MSE-optimal bandwidth. Alternatives: sp.rdrobust, sp.rdrandinf, sp.rdbwselect. Typical minimum N: 500.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals.",
          "type": "number"
        },
        "c": {
          "default": 0,
          "description": "RD cutoff.",
          "type": "number"
        },
        "covs": {
          "description": "Covariate names used as features for heterogeneity detection. Must not include the running variable *x*.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "h": {
          "description": "Bandwidth (uses IK-style automatic selection if None).",
          "type": "number"
        },
        "honesty": {
          "default": true,
          "description": "Split-sample (honest) estimation: half the data for tree construction, the other half for leaf predictions.",
          "type": "boolean"
        },
        "min_leaf": {
          "default": 20,
          "description": "Minimum leaf size (larger -> more regularisation).",
          "type": "integer"
        },
        "n_trees": {
          "default": 500,
          "description": "Number of trees in each forest.",
          "type": "integer"
        },
        "seed": {
          "default": 42,
          "description": "Random seed.",
          "type": "integer"
        },
        "x": {
          "description": "Running variable.",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable.",
          "type": "string"
        }
      },
      "required": [
        "x",
        "y"
      ],
      "type": "object"
    },
    "name": "rd_forest"
  },
  {
    "description": "Armstrong-Kolesar (2018) honest confidence intervals for sharp regression discontinuity -- the only RD inference procedure with provable finite-sample coverage without bandwidth-selection bias. M is the upper bound on the second derivative of E[Y|X] near the cutoff; smaller M means tighter CIs but riskier coverage if the true curvature is larger. Validation: certified parity evidence. Assumptions: E[Y|X] has bounded second derivative |E[Y|X]''| <= M near c; Continuity of potential outcomes at c (Hahn-Todd-van der Klaauw 2001); No manipulation of x at c (run sp.mccrary_test alongside). Pre-conditions: x is continuous with support on both sides of c; Sample mass within the optimal bandwidth on each side; User-supplied M (or willingness to estimate it from data). Failure modes: M estimated from data and effective sample tiny -> Pass an explicit M based on theory or sensitivity analysis (M_grid in Armstrong-Kolesar 2018 Section 4); Honest CI much wider than rdrobust CI -> rd_honest is *honest* by construction (covers under any |f''| <= M); rdrobust trades coverage for precision. Reporting both is recommended; McCrary density test p < 0.05 -> Use donut-hole RD (donut=<delta>) or partia...",
    "input_schema": {
      "properties": {
        "M": {
          "description": "Upper bound on |E[Y|X]''| near c; if None, estimated from data",
          "type": "number"
        },
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "c": {
          "default": 0.0,
          "description": "Cutoff value",
          "type": "number"
        },
        "h": {
          "description": "Bandwidth; auto-selected by opt_criterion if None",
          "type": "number"
        },
        "kernel": {
          "default": "triangular",
          "description": "Local-linear kernel",
          "enum": [
            "triangular",
            "uniform",
            "epanechnikov"
          ],
          "type": "string"
        },
        "opt_criterion": {
          "default": "mse",
          "description": "Bandwidth optimization criterion",
          "enum": [
            "mse",
            "flci",
            "oci"
          ],
          "type": "string"
        },
        "sclass": {
          "default": "H",
          "description": "Smoothness class for the bound M: 'H' (Holder, RDHonest's default -- f' is M-Lipschitz) or 'T' (Taylor)",
          "enum": [
            "H",
            "T"
          ],
          "type": "string"
        },
        "x": {
          "description": "Running variable",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable",
          "type": "string"
        }
      },
      "required": [
        "x",
        "y"
      ],
      "type": "object"
    },
    "name": "rd_honest"
  },
  {
    "description": "Sharp RDD with network interference (Cabrelli-Marconi 2024). Assumptions: Conditional expectations of potential outcomes are continuous at the cutoff; Units cannot precisely manipulate the running variable around the cutoff (no sorting); For fuzzy designs: monotonicity of treatment take-up at the cutoff. Pre-conditions: A continuous running/forcing variable with a known cutoff that (sharply or fuzzily) assigns treatment; Enough observations in a neighbourhood of the cutoff to fit a local polynomial. Failure modes: Density of the running variable jumps at the cutoff (manipulation / sorting) -> Run a McCrary / density test (rdplotdensity); if manipulation is present the design is invalid near the cutoff; Estimate swings with the bandwidth -- results are not robust -> Report a bandwidth-sensitivity curve and use a data-driven MSE-optimal bandwidth. Alternatives: sp.rdrobust, sp.rdrandinf, sp.rdbwselect. Typical minimum N: 500.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "bandwidth": {
          "description": "Defaults to IQR of own running variable.",
          "type": "number"
        },
        "cutoff": {
          "default": 0.0,
          "description": "cutoff parameter (float).",
          "type": "number"
        },
        "kernel": {
          "default": "triangular",
          "description": "Kernel function used for weighting or smoothing.",
          "type": "string"
        },
        "neighbour_running": {
          "description": "Average running variable across neighbours (precomputed).",
          "type": "string"
        },
        "running": {
          "description": "Own running variable.",
          "type": "string"
        },
        "y": {
          "description": "Outcome.",
          "type": "string"
        }
      },
      "required": [
        "neighbour_running",
        "running",
        "y"
      ],
      "type": "object"
    },
    "name": "rd_interference"
  },
  {
    "description": "LASSO-assisted RD via post-double-selection. Assumptions: Conditional expectations of potential outcomes are continuous at the cutoff; Units cannot precisely manipulate the running variable around the cutoff (no sorting); For fuzzy designs: monotonicity of treatment take-up at the cutoff. Pre-conditions: A continuous running/forcing variable with a known cutoff that (sharply or fuzzily) assigns treatment; Enough observations in a neighbourhood of the cutoff to fit a local polynomial. Failure modes: Density of the running variable jumps at the cutoff (manipulation / sorting) -> Run a McCrary / density test (rdplotdensity); if manipulation is present the design is invalid near the cutoff; Estimate swings with the bandwidth -- results are not robust -> Report a bandwidth-sensitivity curve and use a data-driven MSE-optimal bandwidth. Alternatives: sp.rdrobust, sp.rdrandinf, sp.rdbwselect. Typical minimum N: 500.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level.",
          "type": "number"
        },
        "c": {
          "default": 0,
          "description": "RD cutoff.",
          "type": "number"
        },
        "covs": {
          "description": "Candidate covariates (can be large set).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "cv_folds": {
          "default": 5,
          "description": "Cross-validation folds for LASSO penalty selection.",
          "type": "integer"
        },
        "h": {
          "description": "Bandwidth (auto-selected if None).",
          "type": "number"
        },
        "kernel": {
          "default": "triangular",
          "description": "Kernel for local linear regression ('triangular', 'uniform', 'epanechnikov').",
          "type": "string"
        },
        "x": {
          "description": "Running variable.",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable.",
          "type": "string"
        }
      },
      "required": [
        "x",
        "y"
      ],
      "type": "object"
    },
    "name": "rd_lasso"
  },
  {
    "description": "Multi-cutoff RD extrapolation (Cattaneo, Keele, Titiunik, Vazquez-Bare 2021). Assumptions: Conditional expectations of potential outcomes are continuous at the cutoff; Units cannot precisely manipulate the running variable around the cutoff (no sorting); For fuzzy designs: monotonicity of treatment take-up at the cutoff. Pre-conditions: A continuous running/forcing variable with a known cutoff that (sharply or fuzzily) assigns treatment; Enough observations in a neighbourhood of the cutoff to fit a local polynomial. Failure modes: Density of the running variable jumps at the cutoff (manipulation / sorting) -> Run a McCrary / density test (rdplotdensity); if manipulation is present the design is invalid near the cutoff; Estimate swings with the bandwidth -- results are not robust -> Report a bandwidth-sensitivity curve and use a data-driven MSE-optimal bandwidth. Alternatives: sp.rdrobust, sp.rdrandinf, sp.rdbwselect. Typical minimum N: 500.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level.",
          "type": "number"
        },
        "cutoffs": {
          "description": "Cutoff values. Must contain at least 2 cutoffs.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "eval_points": {
          "description": "Running variable values at which to predict tau(x). Defaults to 30 equally spaced points spanning the data range.",
          "type": "string"
        },
        "method": {
          "default": "linear",
          "description": "Interpolation method: - ``'linear'``: tau(x) = a + b*x - ``'polynomial'``: polynomial of degree min(len(cutoffs)-1, 3) - ``'weighted'``: inverse-variance weighted local linear",
          "type": "string"
        },
        "x": {
          "description": "Running variable name.",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable name.",
          "type": "string"
        }
      },
      "required": [
        "cutoffs",
        "x",
        "y"
      ],
      "type": "object"
    },
    "name": "rd_multi_extrapolate"
  },
  {
    "description": "Multi-score RDD: treatment if all running variables exceed cutoffs.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "bandwidth": {
          "description": "Defaults to median IQR across running vars.",
          "type": "number"
        },
        "cutoffs": {
          "description": "One cutoff per running variable (same length).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "kernel": {
          "default": "triangular",
          "description": "Kernel function used for weighting or smoothing.",
          "type": "string"
        },
        "running_vars": {
          "description": "Multiple running variables.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Outcome.",
          "type": "string"
        }
      },
      "required": [
        "cutoffs",
        "running_vars",
        "y"
      ],
      "type": "object"
    },
    "name": "rd_multi_score"
  },
  {
    "description": "Sweep over (kernel, bwselect, polynomial, donut) and return a",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "bwselects": {
          "default": [
            "mserd",
            "cerrd",
            "msetwo"
          ],
          "description": "Specification grid.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "c": {
          "default": 0.0,
          "description": "c parameter (float).",
          "type": "number"
        },
        "cluster": {
          "description": "Cluster identifier column for clustered standard errors.",
          "type": "string"
        },
        "covs": {
          "description": "covs parameter (Optional[List[str]]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "donuts": {
          "default": [
            0.0
          ],
          "description": "Specification grid.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "fuzzy": {
          "description": "fuzzy parameter (Optional[str]).",
          "type": "string"
        },
        "kernels": {
          "default": [
            "triangular",
            "epanechnikov",
            "uniform"
          ],
          "description": "Specification grid.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "polynomials": {
          "default": [
            1,
            2
          ],
          "description": "Specification grid.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "x": {
          "description": "Primary running variable, regressor, or feature input for this estimator.",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "x",
        "y"
      ],
      "type": "object"
    },
    "name": "rd_robustness_table"
  },
  {
    "description": "Covariate balance test at the RD cutoff.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "c": {
          "default": 0,
          "description": "Cutoff.",
          "type": "number"
        },
        "covs": {
          "description": "Covariate names to test. If None, tests all numeric columns except x.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "h": {
          "description": "Manual bandwidth. If None, uses MSE-optimal per covariate.",
          "type": "number"
        },
        "kernel": {
          "default": "triangular",
          "description": "Kernel function used for weighting or smoothing.",
          "type": "string"
        },
        "p": {
          "default": 1,
          "description": "p parameter (int).",
          "type": "integer"
        },
        "x": {
          "description": "Running variable name.",
          "type": "string"
        }
      },
      "required": [
        "x"
      ],
      "type": "object"
    },
    "name": "rdbalance"
  },
  {
    "description": "Bandwidth selection for :func:`rdhte`, as R ``rdhte::rdbwhte``. Validation: certified parity evidence.",
    "input_schema": {
      "properties": {
        "bwselect": {
          "default": "mserd",
          "description": "bwselect parameter (str).",
          "type": "string"
        },
        "c": {
          "default": 0,
          "description": "c parameter (float).",
          "type": "number"
        },
        "cluster": {
          "description": "Cluster identifier column for clustered standard errors.",
          "type": "string"
        },
        "kernel": {
          "default": "triangular",
          "description": "Kernel function used for weighting or smoothing.",
          "type": "string"
        },
        "p": {
          "default": 1,
          "description": "p parameter (int).",
          "type": "integer"
        },
        "q": {
          "description": "Quantile level.",
          "type": "integer"
        },
        "vce": {
          "description": "vce parameter (Optional[str]).",
          "type": "string"
        },
        "x": {
          "description": "Primary running variable, regressor, or feature input for this estimator.",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        },
        "z": {
          "description": "Instrument, proxy, or auxiliary variable used by this estimator.",
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "required": [
        "x",
        "y",
        "z"
      ],
      "type": "object"
    },
    "name": "rdbwhte"
  },
  {
    "description": "Bandwidth selection for local polynomial RD estimation. Validation: certified parity evidence.",
    "input_schema": {
      "properties": {
        "all": {
          "default": false,
          "description": "If True, compute and return all ten bandwidth types.",
          "type": "boolean"
        },
        "bwselect": {
          "default": "mserd",
          "description": "Bandwidth selection method. One of: - ``'mserd'`` : MSE-optimal common bandwidth (default) - ``'msetwo'`` : MSE-optimal separate left/right bandwidths - ``'msesum'`` : MSE-optimal for the sum of the two intercepts - ``'msecomb1'`` : ``min(mserd, msesum)``, per side - ``'msecomb2'`` : ``median(msetwo, mserd, msesum)``, per side - ``'cerrd'``, ``'certwo'``, ``'cersum'``, ``'cercomb1'``, ``'cercomb2'`` : the CER-optimal counterparts of the above The combination rules are applied to the finished ``h`` and ``b`` of each cascade, element-wise per side -- not stage by stage.",
          "type": "string"
        },
        "c": {
          "default": 0,
          "description": "RD cutoff value.",
          "type": "number"
        },
        "cluster": {
          "description": "Cluster variable name for cluster-robust variance estimation.",
          "type": "string"
        },
        "covs": {
          "description": "Covariate column names. When provided, the variance estimates used in bandwidth selection account for covariate adjustment, typically yielding narrower bandwidths.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "deriv": {
          "default": 0,
          "description": "Derivative order. 0 = standard RD (jump in level), 1 = regression kink design (change in slope).",
          "type": "integer"
        },
        "fuzzy": {
          "description": "Treatment variable name for fuzzy RD. The MSE being minimised is then the one for the Wald ratio rather than for the reduced form, so every stage of the cascade changes. .. versionchanged:: 1.27.0 This argument was parsed and then discarded: it was never passed to the bandwidth cascade, so ``fuzzy=`` returned the sharp bandwidth while the docstring claimed otherwise. On a two-sided noncompliance replica of the Lee 2008 senate data that is a 9% to 16% error in ``h``. Designs with **one-sided** noncompliance were unaffected, because ``rdbwselect`` itself falls back to the sharp bandwidth there (R's ``perf_comp``) -- which is also why the defect survived: the fixture in the repository was one-sided.",
          "type": "string"
        },
        "kernel": {
          "default": "triangular",
          "description": "Kernel function: 'triangular', 'uniform', or 'epanechnikov'.",
          "type": "string"
        },
        "p": {
          "default": 1,
          "description": "Polynomial order for point estimation (1 = local linear).",
          "type": "integer"
        },
        "q": {
          "description": "Polynomial order for bias correction. Default is p + 1.",
          "type": "integer"
        },
        "x": {
          "description": "Running variable column name.",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name.",
          "type": "string"
        }
      },
      "required": [
        "x",
        "y"
      ],
      "type": "object"
    },
    "name": "rdbwselect"
  },
  {
    "description": "Bandwidth sensitivity analysis for RD estimates.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "ax": {
          "description": "ax parameter (Optional[Any]).",
          "type": "string"
        },
        "bw_grid": {
          "description": "Explicit bandwidth values to evaluate. If None, auto-generates a grid as multiples of the MSE-optimal bandwidth.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "bw_range": {
          "default": [
            0.5,
            2.0
          ],
          "description": "Range of multipliers for the optimal bandwidth.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "c": {
          "default": 0,
          "description": "Cutoff.",
          "type": "number"
        },
        "figsize": {
          "default": [
            10,
            6
          ],
          "description": "figsize parameter (Tuple[float, float]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "fuzzy": {
          "description": "Treatment variable for fuzzy RD.",
          "type": "string"
        },
        "kernel": {
          "default": "triangular",
          "description": "Kernel function used for weighting or smoothing.",
          "type": "string"
        },
        "n_grid": {
          "default": 15,
          "description": "Number of grid points if bw_grid is None.",
          "type": "integer"
        },
        "p": {
          "default": 1,
          "description": "Polynomial order.",
          "type": "integer"
        },
        "x": {
          "description": "Outcome and running variable names.",
          "type": "string"
        },
        "y": {
          "description": "Outcome and running variable names.",
          "type": "string"
        }
      },
      "required": [
        "x",
        "y"
      ],
      "type": "object"
    },
    "name": "rdbwsensitivity"
  },
  {
    "description": "Sharp / fuzzy RD -- article-friendly alias for :func:`rdrobust`. Validation: certified parity evidence.",
    "input_schema": {
      "properties": {
        "cutoff": {
          "default": 0.0,
          "description": "cutoff parameter (float).",
          "type": "number"
        },
        "fuzzy": {
          "description": "fuzzy parameter (Optional[str]).",
          "type": "string"
        },
        "running": {
          "description": "running parameter (str).",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "running",
        "y"
      ],
      "type": "object"
    },
    "name": "rdd"
  },
  {
    "description": "CJM (2020) density discontinuity test for RD manipulation. Validation: certified evidence with scoped limitations. Known limitations: Certified native reference-parity evidence covers the default rddensity::rddensity unrestricted triangular-kernel selector and test path on the JSS Lee/RD Senate fixture. Manual side-specific bandwidths follow an explicit user-control convention, not a reference-parity guarantee; backend='r' remains available when direct R package execution is required. Assumptions: Cattaneo-Jansson-Ma (2020) local-polynomial density test: under no manipulation the running-variable density is continuous at the cutoff; Density is estimated from the empirical CDF via local polynomial regression with a data-driven CJM bandwidth and bias-corrected inference (no binning); Sufficient mass on both sides of the cutoff within the bandwidth for the polynomial fit of order p. Pre-conditions: Running variable column + cutoff c for the RD density test; Adequate observations on each side of the cutoff within the chosen bandwidth. Failure modes: Too few points on one side of the cutoff for the local polynomial density fit -> Widen the bandwidth h or lower the polynomial order p to...",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "backend": {
          "default": "native",
          "description": "``\"native\"`` uses StatsPAI's Python port of the default ``rddensity`` unrestricted triangular-kernel selector/test path. ``\"r\"`` delegates to ``rddensity::rddensity`` through ``Rscript`` when the R package is installed, matching the reference package's selector and test statistic.",
          "enum": [
            "native",
            "r"
          ],
          "type": "string"
        },
        "c": {
          "default": 0,
          "description": "RD cutoff.",
          "type": "number"
        },
        "h": {
          "description": "Bandwidth. A scalar applies the same bandwidth on both sides; a length-2 sequence is interpreted as ``(h_left, h_right)``.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "p": {
          "default": 2,
          "description": "Polynomial order for density estimation.",
          "type": "integer"
        },
        "x": {
          "description": "Running variable.",
          "type": "string"
        }
      },
      "required": [
        "x"
      ],
      "type": "object"
    },
    "name": "rddensity"
  },
  {
    "description": "Conditional average treatment effects (CATE) in sharp RD designs. Validation: certified parity evidence. Assumptions: Conditional expectations of potential outcomes are continuous at the cutoff; Units cannot precisely manipulate the running variable around the cutoff (no sorting); For fuzzy designs: monotonicity of treatment take-up at the cutoff. Pre-conditions: A continuous running/forcing variable with a known cutoff that (sharply or fuzzily) assigns treatment; Enough observations in a neighbourhood of the cutoff to fit a local polynomial. Failure modes: Density of the running variable jumps at the cutoff (manipulation / sorting) -> Run a McCrary / density test (rdplotdensity); if manipulation is present the design is invalid near the cutoff; Estimate swings with the bandwidth -- results are not robust -> Report a bandwidth-sensitivity curve and use a data-driven MSE-optimal bandwidth. Alternatives: sp.rdrobust, sp.rdrandinf, sp.rdbwselect. Typical minimum N: 500.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "b": {
          "description": "Bandwidth of the bias-correction fit. Defaults to ``h`` (R has no separate ``b``). Through 1.28.0 this argument was accepted and ignored.",
          "type": "number"
        },
        "bwselect": {
          "default": "mserd",
          "description": "Any ``rdbwselect`` rule.",
          "type": "string"
        },
        "c": {
          "default": 0,
          "description": "Cutoff.",
          "type": "number"
        },
        "cluster": {
          "description": "Cluster variable (CR1).",
          "type": "string"
        },
        "eval_points": {
          "description": "``z`` values at which to report ``CATE(z)`` (continuous ``z``).",
          "type": "string"
        },
        "h": {
          "description": "Bandwidth, or ``(h_left, h_right)``. Default: ``rdrobust``'s ``rdbwselect`` (``bwselect``) on the running variable -- separately in each subgroup for a binary ``z``, as R does.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "kernel": {
          "default": "triangular",
          "description": "Kernel function used for weighting or smoothing.",
          "enum": [
            "triangular",
            "epanechnikov",
            "uniform"
          ],
          "type": "string"
        },
        "n_eval": {
          "default": 20,
          "description": "Number of eval.",
          "type": "integer"
        },
        "p": {
          "default": 1,
          "description": "Polynomial order of the point estimate.",
          "type": "integer"
        },
        "q": {
          "description": "Order of the bias-correction fit, default ``p + 1``.",
          "type": "integer"
        },
        "vce": {
          "description": "Default ``'hc3'``, or ``'cr1'`` with ``cluster`` (R's defaults).",
          "enum": [
            "hc0",
            "hc1",
            "hc2",
            "hc3",
            "cr1"
          ],
          "type": "string"
        },
        "x": {
          "description": "Outcome and running variable.",
          "type": "string"
        },
        "y": {
          "description": "Outcome and running variable.",
          "type": "string"
        },
        "z": {
          "description": "Covariate(s) the effect varies with. A single 0/1 column is treated as two subgroups (R's factor rule).",
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "required": [
        "x",
        "y",
        "z"
      ],
      "type": "object"
    },
    "name": "rdhte"
  },
  {
    "description": "Linear combinations of :func:`rdhte` effects, as R ``rdhte_lincom``. Validation: certified parity evidence.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "linfct": {
          "description": "linfct parameter (Optional[numpy.ndarray]).",
          "type": "string"
        },
        "result": {
          "description": "result parameter (CausalResult).",
          "type": "string"
        },
        "weights": {
          "description": "Observation weights.",
          "type": "string"
        }
      },
      "required": [
        "result"
      ],
      "type": "object"
    },
    "name": "rdhte_lincom"
  },
  {
    "description": "Regression Discontinuity in Time (Hausman & Rapson, 2018). Assumptions: Conditional expectations of potential outcomes are continuous at the cutoff; Units cannot precisely manipulate the running variable around the cutoff (no sorting); For fuzzy designs: monotonicity of treatment take-up at the cutoff. Pre-conditions: A continuous running/forcing variable with a known cutoff that (sharply or fuzzily) assigns treatment; Enough observations in a neighbourhood of the cutoff to fit a local polynomial. Failure modes: Density of the running variable jumps at the cutoff (manipulation / sorting) -> Run a McCrary / density test (rdplotdensity); if manipulation is present the design is invalid near the cutoff; Estimate swings with the bandwidth -- results are not robust -> Report a bandwidth-sensitivity curve and use a data-driven MSE-optimal bandwidth. Alternatives: sp.rdrobust, sp.rdrandinf, sp.rdbwselect. Typical minimum N: 500.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals.",
          "type": "number"
        },
        "cluster": {
          "description": "Cluster variable for clustered standard errors. If provided, cluster-robust SEs are used instead of HAC.",
          "type": "string"
        },
        "cutoff": {
          "description": "The policy change date / time cutoff.",
          "type": "integer"
        },
        "donut": {
          "default": 0,
          "description": "Donut hole: exclude observations within +/- donut units of the cutoff (in the same numeric time units).",
          "type": "integer"
        },
        "h": {
          "description": "Bandwidth in the same units as the numeric time axis (days if datetime). If None, an MSE-optimal bandwidth is selected automatically.",
          "type": "number"
        },
        "kernel": {
          "default": "triangular",
          "description": "Kernel function: 'triangular', 'epanechnikov', 'uniform', or 'gaussian'.",
          "type": "string"
        },
        "p": {
          "default": 1,
          "description": "Local polynomial order (1 = local linear).",
          "type": "integer"
        },
        "seasonality": {
          "description": "Deseasonalise before estimation. One of 'month', 'quarter', 'dow' (day-of-week). Regresses Y on seasonal dummies and uses residuals.",
          "type": "string"
        },
        "time": {
          "description": "Time variable name (datetime or numeric).",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable name.",
          "type": "string"
        }
      },
      "required": [
        "cutoff",
        "time",
        "y"
      ],
      "type": "object"
    },
    "name": "rdit"
  },
  {
    "description": "Multi-cutoff RD design. Validation: certified parity evidence. Assumptions: Conditional expectations of potential outcomes are continuous at the cutoff; Units cannot precisely manipulate the running variable around the cutoff (no sorting); For fuzzy designs: monotonicity of treatment take-up at the cutoff. Pre-conditions: A continuous running/forcing variable with a known cutoff that (sharply or fuzzily) assigns treatment; Enough observations in a neighbourhood of the cutoff to fit a local polynomial. Failure modes: Density of the running variable jumps at the cutoff (manipulation / sorting) -> Run a McCrary / density test (rdplotdensity); if manipulation is present the design is invalid near the cutoff; Estimate swings with the bandwidth -- results are not robust -> Report a bandwidth-sensitivity curve and use a data-driven MSE-optimal bandwidth. Alternatives: sp.rdrobust, sp.rdrandinf, sp.rdbwselect. Typical minimum N: 500.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "bandwidth": {
          "description": "Bandwidth for local polynomial. If None, uses Silverman rule.",
          "type": "number"
        },
        "cutoff_var": {
          "description": "cutoff_var parameter (Optional[str]).",
          "type": "string"
        },
        "cutoffs": {
          "description": "Cutoff values.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "kernel": {
          "default": "triangular",
          "description": "Kernel function used for weighting or smoothing.",
          "type": "string"
        },
        "pooling": {
          "default": "ivw",
          "description": "Pooling method: 'ivw' (inverse-variance weighted) or 'equal'.",
          "type": "string"
        },
        "x": {
          "description": "Running variable.",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable.",
          "type": "string"
        }
      },
      "required": [
        "x",
        "y"
      ],
      "type": "object"
    },
    "name": "rdmc"
  },
  {
    "description": "Multi-score / Geographic RD design at a single boundary point. Validation: certified parity evidence. Assumptions: Conditional expectations of potential outcomes are continuous at the cutoff; Units cannot precisely manipulate the running variable around the cutoff (no sorting); For fuzzy designs: monotonicity of treatment take-up at the cutoff. Pre-conditions: A continuous running/forcing variable with a known cutoff that (sharply or fuzzily) assigns treatment; Enough observations in a neighbourhood of the cutoff to fit a local polynomial. Failure modes: Density of the running variable jumps at the cutoff (manipulation / sorting) -> Run a McCrary / density test (rdplotdensity); if manipulation is present the design is invalid near the cutoff; Estimate swings with the bandwidth -- results are not robust -> Report a bandwidth-sensitivity curve and use a data-driven MSE-optimal bandwidth. Alternatives: sp.rdrobust, sp.rdrandinf, sp.rdbwselect. Typical minimum N: 500.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "bandwidth": {
          "description": "Fixed bandwidth on the signed-distance score. When omitted the CCT MSE-optimal cascade selects it, as the reference does.",
          "type": "number"
        },
        "cutoff1": {
          "default": 0,
          "description": "Cutoff for x1.",
          "type": "number"
        },
        "cutoff2": {
          "default": 0,
          "description": "Cutoff for x2.",
          "type": "number"
        },
        "kernel": {
          "default": "triangular",
          "description": "Kernel function used for weighting or smoothing.",
          "type": "string"
        },
        "treat": {
          "description": "Column holding the 0/1 treatment indicator (R's ``zvar``). Strongly",
          "type": "string"
        },
        "x1": {
          "description": "First running variable (e.g., latitude distance to boundary).",
          "type": "string"
        },
        "x2": {
          "description": "Second running variable (e.g., longitude distance to boundary).",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable.",
          "type": "string"
        }
      },
      "required": [
        "x1",
        "x2",
        "y"
      ],
      "type": "object"
    },
    "name": "rdms"
  },
  {
    "description": "Placebo cutoff test for RD validity.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "ax": {
          "description": "ax parameter (Optional[Any]).",
          "type": "string"
        },
        "c": {
          "default": 0,
          "description": "True cutoff.",
          "type": "number"
        },
        "figsize": {
          "default": [
            10,
            6
          ],
          "description": "figsize parameter (Tuple[float, float]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "fuzzy": {
          "description": "Treatment variable for fuzzy RD.",
          "type": "string"
        },
        "kernel": {
          "default": "triangular",
          "description": "Kernel function used for weighting or smoothing.",
          "type": "string"
        },
        "n_placebo": {
          "default": 10,
          "description": "Number of placebo cutoffs if auto-generating.",
          "type": "integer"
        },
        "p": {
          "default": 1,
          "description": "p parameter (int).",
          "type": "integer"
        },
        "placebo_cutoffs": {
          "description": "Explicit placebo cutoff values. If None, auto-generates from the data distribution.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "side": {
          "default": "both",
          "description": "Which side of the true cutoff to place placebos: 'left', 'right', or 'both'.",
          "type": "string"
        },
        "x": {
          "description": "Outcome and running variable names.",
          "type": "string"
        },
        "y": {
          "description": "Outcome and running variable names.",
          "type": "string"
        }
      },
      "required": [
        "x",
        "y"
      ],
      "type": "object"
    },
    "name": "rdplacebo"
  },
  {
    "description": "RD plot: binned scatter with polynomial fit on each side of the cutoff. Validation: certified parity evidence.",
    "input_schema": {
      "properties": {
        "ax": {
          "description": "ax parameter (Optional[Any]).",
          "type": "string"
        },
        "binselect": {
          "default": "esmv",
          "description": "Bin selection rule, as in R ``rdplot``: - 'es' : IMSE-optimal evenly spaced, spacings estimators - 'espr' : IMSE-optimal evenly spaced, polynomial regression - 'esmv' : mimicking-variance evenly spaced, spacings (default) - 'esmvpr' : mimicking-variance evenly spaced, polynomial regression - 'qs', 'qspr', 'qsmv', 'qsmvpr' : the quantile-spaced analogues With 20% or more mass points on either side the spacings variants switch to their polynomial-regression versions (R's ``masspoints='adjust'``).",
          "type": "string"
        },
        "c": {
          "default": 0,
          "description": "Cutoff.",
          "type": "number"
        },
        "ci_level": {
          "default": 0.95,
          "description": "Level of the per-bin t intervals and the polynomial CI band.",
          "type": "number"
        },
        "covs": {
          "description": "Covariates, adjusted for as R ``rdplot(covs=, covs_eval='mean')`` does.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "donut": {
          "default": 0,
          "description": "If > 0, shades the donut region |x - c| <= donut.",
          "type": "number"
        },
        "figsize": {
          "default": [
            10,
            7
          ],
          "description": "figsize parameter (Tuple[float, float]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "h": {
          "description": "Support of the global polynomial fit on each side (R's ``h``); defaults to the full range of ``x`` on that side.",
          "type": "number"
        },
        "hide_ci": {
          "default": false,
          "description": "If True, suppress all interval displays.",
          "type": "boolean"
        },
        "kernel": {
          "default": "uniform",
          "description": "Kernel weighting the global polynomial over ``[c - h, c + h]``. R's default. (Through 1.28.0 the default read 'triangular' but the argument was ignored and the fit was unweighted.)",
          "enum": [
            "uniform",
            "triangular",
            "epanechnikov"
          ],
          "type": "string"
        },
        "nbins": {
          "description": "Bins per side. If None, chosen by ``binselect``.",
          "type": "integer"
        },
        "p": {
          "default": 4,
          "description": "Order of the global polynomial fitted on each side.",
          "type": "integer"
        },
        "scatter": {
          "default": true,
          "description": "Show binned means.",
          "type": "boolean"
        },
        "shade_ci": {
          "default": true,
          "description": "Shade a pointwise CI band around the polynomial (a StatsPAI addition; not shown when ``covs`` is given).",
          "type": "boolean"
        },
        "show_bw": {
          "default": false,
          "description": "If True, shades the ``sp.rdrobust`` bandwidth window.",
          "type": "boolean"
        },
        "title": {
          "description": "title parameter (Optional[str]).",
          "type": "string"
        },
        "weights": {
          "description": "Observation weights for the polynomial fit.",
          "type": "string"
        },
        "x": {
          "description": "Outcome and running variable names.",
          "type": "string"
        },
        "x_label": {
          "description": "x_label parameter (Optional[str]).",
          "type": "string"
        },
        "y": {
          "description": "Outcome and running variable names.",
          "type": "string"
        },
        "y_label": {
          "description": "y_label parameter (Optional[str]).",
          "type": "string"
        }
      },
      "required": [
        "x",
        "y"
      ],
      "type": "object"
    },
    "name": "rdplot"
  },
  {
    "description": "Density of the running variable on each side of the cutoff. Validation: certified parity evidence.",
    "input_schema": {
      "properties": {
        "ax": {
          "description": "ax parameter (Optional[Any]).",
          "type": "string"
        },
        "c": {
          "default": 0,
          "description": "Cutoff.",
          "type": "number"
        },
        "ci_level": {
          "default": 0.95,
          "description": "Level of the robust bias-corrected band ``f_q +/- z se_q``.",
          "type": "number"
        },
        "figsize": {
          "default": [
            10,
            7
          ],
          "description": "figsize parameter (Tuple[float, float]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "h": {
          "description": "Bandwidths ``(h_left, h_right)``; by default those of :func:`sp.rddensity` at the same ``c`` and ``p``.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "hist": {
          "default": true,
          "description": "Overlay a density-scaled histogram.",
          "type": "boolean"
        },
        "n_grid": {
          "default": 50,
          "description": "Grid points per side (R's ``plotN``, whose default is 10).",
          "type": "integer"
        },
        "nbins": {
          "default": 30,
          "description": "Histogram bins per side.",
          "type": "integer"
        },
        "p": {
          "default": 2,
          "description": "Local polynomial order (``rddensity``'s ``p``).",
          "type": "integer"
        },
        "title": {
          "description": "title parameter (Optional[str]).",
          "type": "string"
        },
        "x": {
          "description": "Running variable.",
          "type": "string"
        }
      },
      "required": [
        "x"
      ],
      "type": "object"
    },
    "name": "rdplotdensity"
  },
  {
    "description": "Power of an RD design given sample size and effect size. Validation: certified parity evidence.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level.",
          "type": "number"
        },
        "c": {
          "default": 0.0,
          "description": "Cutoff, for data mode.",
          "type": "number"
        },
        "h_left": {
          "default": 0.5,
          "description": "Bandwidth fractions (proportion of running-variable support used). Design mode only.",
          "type": "number"
        },
        "h_right": {
          "default": 0.5,
          "description": "Bandwidth fractions (proportion of running-variable support used). Design mode only.",
          "type": "number"
        },
        "n_left": {
          "default": 500,
          "description": "Available sample size on each side. Design mode only.",
          "type": "integer"
        },
        "n_right": {
          "default": 500,
          "description": "Available sample size on each side. Design mode only.",
          "type": "integer"
        },
        "target_power": {
          "description": "If set, compute MDE for this target power instead.",
          "type": "number"
        },
        "tau": {
          "description": "Hypothesised treatment effect at the cutoff.",
          "type": "number"
        },
        "var_left": {
          "default": 1.0,
          "description": "Outcome variance on each side of the cutoff. Design mode only.",
          "type": "number"
        },
        "var_right": {
          "default": 1.0,
          "description": "Outcome variance on each side of the cutoff. Design mode only.",
          "type": "number"
        },
        "x": {
          "description": "Outcome and running-variable column names, for data mode.",
          "type": "string"
        },
        "y": {
          "description": "Outcome and running-variable column names, for data mode.",
          "type": "string"
        }
      },
      "required": [
        "tau"
      ],
      "type": "object"
    },
    "name": "rdpower"
  },
  {
    "description": "Randomization inference for regression discontinuity designs. Validation: certified parity evidence. Assumptions: Conditional expectations of potential outcomes are continuous at the cutoff; Units cannot precisely manipulate the running variable around the cutoff (no sorting); For fuzzy designs: monotonicity of treatment take-up at the cutoff. Pre-conditions: A continuous running/forcing variable with a known cutoff that (sharply or fuzzily) assigns treatment; Enough observations in a neighbourhood of the cutoff to fit a local polynomial. Failure modes: Density of the running variable jumps at the cutoff (manipulation / sorting) -> Run a McCrary / density test (rdplotdensity); if manipulation is present the design is invalid near the cutoff; Estimate swings with the bandwidth -- results are not robust -> Report a bandwidth-sensitivity curve and use a data-driven MSE-optimal bandwidth. Alternatives: sp.rdrobust, sp.rdrandinf, sp.rdbwselect. Typical minimum N: 500.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level.",
          "type": "number"
        },
        "c": {
          "default": 0,
          "description": "RD cutoff value.",
          "type": "number"
        },
        "covs": {
          "description": "Covariate names to partial out before testing.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "fuzzy": {
          "description": "Actual treatment variable for fuzzy RD. The Wald (IV) estimator is computed within the window.",
          "type": "string"
        },
        "kernel": {
          "default": "uniform",
          "description": "Kernel weighting (only 'uniform' currently supported for local randomization).",
          "type": "string"
        },
        "n_perms": {
          "default": 1000,
          "description": "Number of permutations for Fisher randomization test.",
          "type": "integer"
        },
        "p": {
          "default": 0,
          "description": "Polynomial order for adjustment (0 = unadjusted).",
          "type": "integer"
        },
        "seed": {
          "default": 42,
          "description": "Random seed for reproducibility.",
          "type": "integer"
        },
        "statistic": {
          "default": "diffmeans",
          "description": "Test statistic: 'diffmeans', 'ksmirnov', 'ranksum', or 'all'. ``'ttest'`` is accepted as an alias for ``'diffmeans'``, matching rdlocrand, where both names select the same statistic.",
          "type": "string"
        },
        "wl": {
          "description": "Window left bound offset from cutoff (typically negative). The left edge of the window is ``c + wl``.",
          "type": "number"
        },
        "wr": {
          "description": "Window right bound offset from cutoff (typically positive). The right edge of the window is ``c + wr``.",
          "type": "number"
        },
        "x": {
          "description": "Running variable name.",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable name.",
          "type": "string"
        }
      },
      "required": [
        "x",
        "y"
      ],
      "type": "object"
    },
    "name": "rdrandinf"
  },
  {
    "description": "Rosenbaum sensitivity bounds for RD under local randomization. Validation: certified parity evidence.",
    "input_schema": {
      "properties": {
        "c": {
          "default": 0,
          "description": "RD cutoff value.",
          "type": "number"
        },
        "gamma_list": {
          "description": "Odds ratios. Defaults to ``[1, 1.5, 2, 2.5, 3, 4, 5]``.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "n_perms": {
          "default": 1000,
          "description": "Bernoulli assignment draws per p-value. The same draws are reused across thresholds and ``Gamma`` values (common random numbers, as R reseeds before each p-value).",
          "type": "integer"
        },
        "seed": {
          "default": 42,
          "description": "Random seed.",
          "type": "integer"
        },
        "statistic": {
          "default": "ranksum",
          "description": "``'ranksum'`` is R's standardised rank sum (control-rank sum minus its mean over ``sqrt(n0 n1 var(ranks) / n)``); ``'diffmeans'`` the difference in means.",
          "enum": [
            "ranksum",
            "diffmeans"
          ],
          "type": "string"
        },
        "wl": {
          "description": "Window ``[c + wl, c + wr]`` (``wl`` typically negative).",
          "type": "number"
        },
        "wr": {
          "description": "Window ``[c + wl, c + wr]`` (``wl`` typically negative).",
          "type": "number"
        },
        "x": {
          "description": "Running variable name.",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable name.",
          "type": "string"
        }
      },
      "required": [
        "x",
        "y"
      ],
      "type": "object"
    },
    "name": "rdrbounds"
  },
  {
    "description": "Sharp or fuzzy regression-discontinuity with robust bias-corrected CIs (Calonico-Cattaneo-Titiunik 2014). Use fuzzy= for IV-style fuzzy RD. Validation: certified evidence with scoped limitations. Known limitations: observation-level weights are not yet supported -- passing a weight column raises NotImplementedError; R-parity certification applies to bwselect='cct' or manually matched h/b bandwidths; the dependency-light default bwselect='mserd' uses StatsPAI's calibrated selector and can differ from rdrobust::rdrobust defaults.",
    "input_schema": {
      "properties": {
        "c": {
          "default": 0.0,
          "description": "Cutoff value.",
          "type": "number"
        },
        "fuzzy": {
          "description": "Treatment column for fuzzy RD (optional).",
          "type": "string"
        },
        "kernel": {
          "default": "triangular",
          "enum": [
            "triangular",
            "uniform",
            "epanechnikov"
          ],
          "type": "string"
        },
        "x": {
          "description": "Running variable column.",
          "type": "string"
        },
        "y": {
          "type": "string"
        }
      },
      "required": [
        "y",
        "x"
      ],
      "type": "object"
    },
    "name": "rdrobust"
  },
  {
    "description": "Minimum sample size for a given power in an RD design. Validation: certified parity evidence.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "c": {
          "default": 0.0,
          "description": "c parameter (float).",
          "type": "number"
        },
        "h_left": {
          "default": 0.5,
          "description": "h_left parameter (float).",
          "type": "number"
        },
        "h_right": {
          "default": 0.5,
          "description": "h_right parameter (float).",
          "type": "number"
        },
        "ratio": {
          "default": 1.0,
          "description": "``n_right / n_left``. Default 1.0 assumes equal allocation.",
          "type": "number"
        },
        "target_power": {
          "default": 0.8,
          "description": "target_power parameter (float).",
          "type": "number"
        },
        "tau": {
          "description": "Quantile level or target treatment-effect index.",
          "type": "number"
        },
        "var_left": {
          "default": 1.0,
          "description": "var_left parameter (float).",
          "type": "number"
        },
        "var_right": {
          "default": 1.0,
          "description": "var_right parameter (float).",
          "type": "number"
        },
        "x": {
          "description": "Primary running variable, regressor, or feature input for this estimator.",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "tau"
      ],
      "type": "object"
    },
    "name": "rdsampsi"
  },
  {
    "description": "Sensitivity of RD estimates across different window widths. Validation: certified parity evidence.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level.",
          "type": "number"
        },
        "c": {
          "default": 0,
          "description": "RD cutoff value.",
          "type": "number"
        },
        "n_perms": {
          "default": 500,
          "description": "Number of permutations per window.",
          "type": "integer"
        },
        "nwindows": {
          "default": 20,
          "description": "Number of windows when ``wlist`` is None.",
          "type": "integer"
        },
        "p": {
          "default": 0,
          "description": "Polynomial order for adjustment.",
          "type": "integer"
        },
        "plot": {
          "default": false,
          "description": "plot parameter (bool).",
          "type": "boolean"
        },
        "seed": {
          "default": 42,
          "description": "Random seed.",
          "type": "integer"
        },
        "statistic": {
          "default": "diffmeans",
          "description": "Test statistic for inference.",
          "type": "string"
        },
        "wlist": {
          "description": "Symmetric half-window widths to evaluate. If None, an evenly-spaced grid is generated automatically.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "x": {
          "description": "Running variable name.",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable name.",
          "type": "string"
        }
      },
      "required": [
        "x",
        "y"
      ],
      "type": "object"
    },
    "name": "rdsensitivity"
  },
  {
    "description": "One-stop RD diagnostic battery.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "c": {
          "default": 0,
          "description": "Cutoff.",
          "type": "number"
        },
        "covs": {
          "description": "Pre-treatment covariates for balance test.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "full": {
          "default": false,
          "description": "Run extended diagnostics (honest CI, power, placebos).",
          "type": "boolean"
        },
        "fuzzy": {
          "description": "Treatment variable for fuzzy RD.",
          "type": "string"
        },
        "kernel": {
          "default": "triangular",
          "description": "Kernel function used for weighting or smoothing.",
          "type": "string"
        },
        "p": {
          "default": 1,
          "description": "p parameter (int).",
          "type": "integer"
        },
        "plot": {
          "default": false,
          "description": "Generate a multi-panel diagnostic plot.",
          "type": "boolean"
        },
        "verbose": {
          "default": true,
          "description": "Print formatted summary to console.",
          "type": "boolean"
        },
        "x": {
          "description": "Outcome and running variable.",
          "type": "string"
        },
        "y": {
          "description": "Outcome and running variable.",
          "type": "string"
        }
      },
      "required": [
        "x",
        "y"
      ],
      "type": "object"
    },
    "name": "rdsummary"
  },
  {
    "description": "Data-driven window selection for local randomization RD. Validation: certified parity evidence.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.15,
          "description": "Significance level for balance (lenient by default to be conservative about window selection).",
          "type": "number"
        },
        "c": {
          "default": 0,
          "description": "RD cutoff value.",
          "type": "number"
        },
        "covs": {
          "description": "Covariate names to test balance for. If None, uses quantiles of the running variable as pseudo-covariates.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "nwindows": {
          "default": 10,
          "description": "Number of windows to evaluate.",
          "type": "integer"
        },
        "p": {
          "default": 0,
          "description": "Polynomial order for adjustment.",
          "type": "integer"
        },
        "seed": {
          "default": 42,
          "description": "Random seed.",
          "type": "integer"
        },
        "statistic": {
          "default": "diffmeans",
          "description": "Test statistic for balance testing.",
          "type": "string"
        },
        "wmin": {
          "description": "Minimum half-window width. Defaults to the smallest gap between adjacent observations near the cutoff.",
          "type": "number"
        },
        "wstep": {
          "description": "Window increment. Defaults to ``(max_range - wmin) / nwindows``.",
          "type": "number"
        },
        "x": {
          "description": "Running variable name.",
          "type": "string"
        }
      },
      "required": [
        "x"
      ],
      "type": "object"
    },
    "name": "rdwinselect"
  },
  {
    "description": "Method advisor: given a dataset + research question, recommends a ranked list of estimators with reasoning, precondition checks, and a full suggested workflow. This is the first call an agent should make if it doesn't know which estimator to run. Supports DAG input, mediator / proxy / principal-strata variables, and optional resampling-stability verification.",
    "input_schema": {
      "properties": {
        "covariates": {
          "description": "Covariate columns.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "cutoff": {
          "description": "RD cutoff value.",
          "type": "number"
        },
        "design": {
          "description": "Override auto-detected design.",
          "enum": [
            "rct",
            "did",
            "rd",
            "iv",
            "observational",
            "panel",
            "cross-section"
          ],
          "type": "string"
        },
        "id": {
          "description": "Unit identifier (panel).",
          "type": "string"
        },
        "instrument": {
          "description": "Instrumental variable.",
          "type": "string"
        },
        "running_var": {
          "description": "Running variable (RD).",
          "type": "string"
        },
        "time": {
          "description": "Time column (panel / DID).",
          "type": "string"
        },
        "treatment": {
          "description": "Treatment / exposure column.",
          "type": "string"
        },
        "verify": {
          "default": false,
          "description": "If True, run resampling-stability checks on top recommendations.",
          "type": "boolean"
        },
        "y": {
          "description": "Outcome column.",
          "type": "string"
        }
      },
      "required": [
        "y"
      ],
      "type": "object"
    },
    "name": "recommend"
  },
  {
    "description": "Score ``sp.recommend`` / ``sp.audit`` against the ground-truth corpus.",
    "input_schema": {
      "properties": {
        "corpus_path": {
          "description": "Path to an alternative corpus YAML. Defaults to the bundled corpus.",
          "type": "string"
        },
        "fit": {
          "default": true,
          "description": "Also run the dynamic audit pass (fit each top-1 estimator and run ``sp.audit`` on the result). Set ``False`` for a faster recommend-only run.",
          "type": "boolean"
        }
      },
      "required": [],
      "type": "object"
    },
    "name": "recommend_benchmark"
  },
  {
    "description": "Fit an OLS regression with robust (HC1) or clustered SEs. Input is a Wilkinson-style formula like 'y ~ x1 + x2'. Use this for baseline specifications or covariate-adjusted RCT analyses. Validation: certified parity evidence.",
    "input_schema": {
      "properties": {
        "cluster": {
          "description": "Column name for cluster-robust SEs.",
          "type": "string"
        },
        "formula": {
          "description": "R-style formula, e.g. 'y ~ x1 + x2'",
          "type": "string"
        },
        "robust": {
          "default": "hc1",
          "enum": [
            "hc1",
            "hc2",
            "hc3",
            "nonrobust"
          ],
          "type": "string"
        }
      },
      "required": [
        "formula"
      ],
      "type": "object"
    },
    "name": "regress"
  },
  {
    "description": "Honest regression forest for ``E[Y | X]`` (``grf::regression_forest``).",
    "input_schema": {
      "properties": {
        "ci_group_size": {
          "default": 2,
          "description": "Trees per little bag (variance estimates need >= 2).",
          "type": "integer"
        },
        "clusters": {
          "description": "Cluster ids (trees sample whole clusters).",
          "type": "string"
        },
        "covariates": {
          "description": "Covariates ``X``.",
          "type": "string"
        },
        "equalize_cluster_weights": {
          "default": false,
          "description": "equalize_cluster_weights parameter (bool).",
          "type": "boolean"
        },
        "honest": {
          "default": true,
          "description": "grf honesty options.",
          "type": "boolean"
        },
        "honesty_fraction": {
          "default": 0.5,
          "description": "grf honesty options.",
          "type": "number"
        },
        "honesty_prune_leaves": {
          "default": true,
          "description": "grf honesty options.",
          "type": "boolean"
        },
        "imbalance_penalty": {
          "default": 0.0,
          "description": "imbalance_penalty parameter (float).",
          "type": "number"
        },
        "max_depth": {
          "description": "max_depth parameter (Optional[int]).",
          "type": "integer"
        },
        "max_samples": {
          "default": 0.5,
          "description": "grf ``sample.fraction``.",
          "type": "number"
        },
        "min_samples_leaf": {
          "default": 5,
          "description": "grf ``min.node.size``.",
          "type": "integer"
        },
        "mtry": {
          "description": "mtry parameter (Optional[int]).",
          "type": "integer"
        },
        "n_estimators": {
          "default": 2000,
          "description": "Trees (grf ``num.trees``).",
          "type": "integer"
        },
        "n_jobs": {
          "default": 1,
          "description": "Number of jobs.",
          "type": "integer"
        },
        "random_state": {
          "default": 42,
          "description": "Random seed or RandomState for reproducible stochastic steps.",
          "type": "integer"
        },
        "split_alpha": {
          "default": 0.05,
          "description": "grf ``alpha`` (minimum child share).",
          "type": "number"
        },
        "weights": {
          "description": "Sample weights (grf ``sample.weights``).",
          "type": "string"
        },
        "y": {
          "description": "Outcome.",
          "type": "string"
        }
      },
      "required": [],
      "type": "object"
    },
    "name": "regression_forest"
  },
  {
    "description": "Load a famous dataset and a step-by-step replication guide.",
    "input_schema": {
      "properties": {
        "key": {
          "description": "Replication key (see ``sp.list_replications()``).",
          "type": "string"
        },
        "simulated": {
          "description": "Override the entry's default data source. ``True`` forces a simulated replica; ``False`` forces the bundled real CSV (only valid for entries where ``has_real_data`` is True). Default ``None`` uses whatever the entry declares (currently real for ``card_1995`` and ``abadie_2010``).",
          "type": "boolean"
        }
      },
      "required": [
        "key"
      ],
      "type": "object"
    },
    "name": "replicate"
  },
  {
    "description": "Ramsey RESET test for functional form misspecification. Validation: certified parity evidence.",
    "input_schema": {
      "properties": {
        "powers": {
          "default": 3,
          "description": "Include y2, y3, ..., y^powers in the auxiliary regression.",
          "type": "integer"
        },
        "x": {
          "description": "Primary running variable, regressor, or feature input for this estimator.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "x",
        "y"
      ],
      "type": "object"
    },
    "name": "reset_test"
  },
  {
    "description": "Randomization inference p-value. Validation: certified parity evidence.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "cluster": {
          "description": "Cluster-level permutation (permute treatment at cluster level).",
          "type": "string"
        },
        "n_perms": {
          "default": 1000,
          "description": "Number of random permutations. Use 10000+ for publications. When the design admits no more than ``n_perms`` distinct assignments (``C(n, n_1)``, or ``C(G, G_1)`` under ``cluster``) every assignment is enumerated instead and the p-value is exact -- the rule R ``ri2`` / ``randomizr::obtain_permutation_matrix`` apply.",
          "type": "integer"
        },
        "seed": {
          "description": "Random seed.",
          "type": "integer"
        },
        "stat": {
          "default": "diff_means",
          "description": "Test statistic: - ``'diff_means'``: difference in means (Y_bar_1 - Y_bar_0) - ``'ks'``: Kolmogorov-Smirnov statistic - ``'t'``: t-statistic - A callable ``f(Y, D) -> float`` for custom statistics.",
          "type": "string"
        },
        "treat": {
          "description": "Binary treatment indicator (0/1).",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable.",
          "type": "string"
        }
      },
      "required": [
        "treat",
        "y"
      ],
      "type": "object"
    },
    "name": "ri_test"
  },
  {
    "description": "RIF Oaxaca-Blinder decomposition (FFL 2009, Section 5). Validation: certified parity evidence. Assumptions: The recentered influence function for the chosen distributional statistic (quantile, variance, Gini, etc.) is a valid first-order approximation, so its expectation recovers the statistic (Firpo-Fortin-Lemieux 2009); The aggregate Oaxaca-Blinder split into explained (endowments) vs unexplained (coefficients) requires no omitted covariates correlated with group and a correctly specified RIF regression; Detailed (per-covariate) decompositions assume path/normalization invariance and, for the unexplained part, an ignorable reference-group choice. Pre-conditions: group is a binary 0/1 indicator and reference in {0,1}; Covariates and the target distributional statistic are well-defined in both groups; Both groups have enough observations to fit the RIF regression at the chosen statistic. Failure modes: RIF for a tail quantile is noisy where the density is near zero, giving unstable shares -> Avoid extreme quantiles or smooth/bootstrap the density estimate underlying the RIF; Limited covariate overlap between groups makes the explained component unreliable (specification error) ->...",
    "input_schema": {
      "properties": {
        "formula": {
          "description": "Model formula using patsy/R-style syntax.",
          "type": "string"
        },
        "group": {
          "description": "Binary (0/1) group indicator column.",
          "type": "string"
        },
        "quantile_convention": {
          "default": "statspai",
          "description": "Quantile RIF convention for ``statistic=\"quantile\"``. Use ``\"dineq\"`` for R ``dineq::rif`` parity.",
          "enum": [
            "statspai",
            "dineq",
            "rifreg"
          ],
          "type": "string"
        },
        "reference": {
          "default": 0,
          "description": "Which group's coefficients to use as the reference (0 or 1).",
          "type": "integer"
        },
        "statistic": {
          "default": "quantile",
          "description": "statistic parameter (StatisticKind).",
          "type": "string"
        },
        "tau": {
          "default": 0.5,
          "description": "Quantile level or target treatment-effect index.",
          "type": "number"
        }
      },
      "required": [
        "formula",
        "group"
      ],
      "type": "object"
    },
    "name": "rif_decomposition"
  },
  {
    "description": "RIF regression (Firpo, Fortin & Lemieux 2009). Validation: certified parity evidence.",
    "input_schema": {
      "properties": {
        "formula": {
          "description": "``\"y ~ x1 + x2\"`` style.",
          "type": "string"
        },
        "quantile_convention": {
          "default": "statspai",
          "description": "Quantile RIF convention for ``statistic=\"quantile\"``.",
          "enum": [
            "statspai",
            "dineq",
            "rifreg"
          ],
          "type": "string"
        },
        "statistic": {
          "default": "quantile",
          "description": "statistic parameter (StatisticKind).",
          "enum": [
            "quantile",
            "variance",
            "gini"
          ],
          "type": "string"
        },
        "tau": {
          "default": 0.5,
          "description": "Quantile level (default 0.5 = median UQPE).",
          "type": "number"
        }
      },
      "required": [
        "formula"
      ],
      "type": "object"
    },
    "name": "rifreg"
  },
  {
    "description": "Regression Kink Design estimator (Card et al., 2015). Validation: certified parity evidence. Assumptions: Conditional expectations of potential outcomes are continuous at the cutoff; Units cannot precisely manipulate the running variable around the cutoff (no sorting); For fuzzy designs: monotonicity of treatment take-up at the cutoff. Pre-conditions: A continuous running/forcing variable with a known cutoff that (sharply or fuzzily) assigns treatment; Enough observations in a neighbourhood of the cutoff to fit a local polynomial. Failure modes: Density of the running variable jumps at the cutoff (manipulation / sorting) -> Run a McCrary / density test (rdplotdensity); if manipulation is present the design is invalid near the cutoff; Estimate swings with the bandwidth -- results are not robust -> Report a bandwidth-sensitivity curve and use a data-driven MSE-optimal bandwidth. Alternatives: sp.rdrobust, sp.rdrandinf, sp.rdbwselect. Typical minimum N: 500.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals.",
          "type": "number"
        },
        "c": {
          "default": 0,
          "description": "Kink point (cutoff).",
          "type": "number"
        },
        "cluster": {
          "description": "Cluster variable name for clustered standard errors.",
          "type": "string"
        },
        "h": {
          "description": "Bandwidth. If None, the CCT MSE-optimal bandwidth for the first derivative (``rdrobust(deriv=1, bwselect='mserd', vce='hc1')``) is selected.",
          "type": "number"
        },
        "kernel": {
          "default": "triangular",
          "description": "Kernel function: 'triangular', 'epanechnikov', or 'uniform'.",
          "type": "string"
        },
        "p": {
          "default": 1,
          "description": "Local polynomial order (1 = local linear, the default and most common choice for RKD).",
          "type": "integer"
        },
        "treatment": {
          "description": "Treatment variable for fuzzy RKD. If None, estimate the reduced-form kink in E[Y|X] (sharp / reduced-form RKD).",
          "type": "string"
        },
        "x": {
          "description": "Running variable name.",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable name.",
          "type": "string"
        }
      },
      "required": [
        "x",
        "y"
      ],
      "type": "object"
    },
    "name": "rkd"
  },
  {
    "description": "Rigorous Lasso / post-Lasso -- a faithful port of ``hdm::rlasso``. Validation: certified parity evidence.",
    "input_schema": {
      "properties": {
        "X": {
          "description": "Design matrix of candidate covariates (``p`` may exceed ``n``).",
          "type": "string"
        },
        "colnames": {
          "description": "Names for the columns of ``X`` (default ``V1..Vp``).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "control": {
          "description": "Overrides for ``numIter`` (default 15), ``tol`` (default 1e-5) and ``threshold`` (default ``None``).",
          "type": "object"
        },
        "intercept": {
          "default": true,
          "description": "Center ``X`` and ``y`` and report an intercept on the original scale.",
          "type": "boolean"
        },
        "penalty": {
          "description": "Overrides for ``homoscedastic`` (``True`` / ``False`` / ``\"none\"``), ``X.dependent.lambda`` (bool), ``c`` (slack, default 1.1), ``gamma`` (default ``0.1/log(n)``), ``lambda.start`` and ``numSim``. Defaults reproduce hdm exactly.",
          "type": "object"
        },
        "post": {
          "default": true,
          "description": "If ``True``, re-estimate the selected support by OLS (post-Lasso).",
          "type": "boolean"
        },
        "rng": {
          "description": "Only used when ``X.dependent.lambda`` simulation is requested.",
          "type": "string"
        },
        "y": {
          "description": "Response.",
          "type": "string"
        }
      },
      "required": [
        "X",
        "y"
      ],
      "type": "object"
    },
    "name": "rlasso"
  },
  {
    "description": "Effect of ``d`` on ``y`` after Lasso-selecting controls ``x``. Validation: certified parity evidence.",
    "input_schema": {
      "properties": {
        "I3": {
          "description": "Amelioration set forced into the control set (double-selection only) -- hdm's ``I3`` argument.",
          "type": "string"
        },
        "control": {
          "description": "Forwarded to :func:`statspai.rlasso.rlasso`.",
          "type": "object"
        },
        "d": {
          "description": "d parameter (Union[np.ndarray, pd.Series, str]).",
          "type": "string"
        },
        "method": {
          "default": "partialling out",
          "description": "See the module docstring.",
          "type": "string"
        },
        "penalty": {
          "description": "Forwarded to :func:`statspai.rlasso.rlasso`.",
          "type": "object"
        },
        "post": {
          "default": true,
          "description": "Post-Lasso inside the selection steps.",
          "type": "boolean"
        },
        "x": {
          "description": "Primary running variable, regressor, or feature input for this estimator.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "d",
        "x",
        "y"
      ],
      "type": "object"
    },
    "name": "rlasso_effect"
  },
  {
    "description": "Estimate the effect of each targeted column of ``X`` on ``y``. Validation: certified parity evidence.",
    "input_schema": {
      "properties": {
        "X": {
          "description": "Feature matrix or covariate DataFrame.",
          "type": "string"
        },
        "control": {
          "description": "control parameter (Optional[Dict[str, Any]]).",
          "type": "object"
        },
        "index": {
          "description": "index parameter (Optional[Sequence[int]]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "method": {
          "default": "partialling out",
          "description": "Estimator or algorithm variant to use.",
          "type": "string"
        },
        "penalty": {
          "description": "penalty parameter (Optional[Dict[str, Any]]).",
          "type": "object"
        },
        "post": {
          "default": true,
          "description": "post parameter (bool).",
          "type": "boolean"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "X",
        "y"
      ],
      "type": "object"
    },
    "name": "rlasso_effects"
  },
  {
    "description": "Instrumental-variables estimation with rigorous-Lasso selection. Validation: certified parity evidence.",
    "input_schema": {
      "properties": {
        "control": {
          "description": "Forwarded to :func:`statspai.rlasso.rlasso` (penalty level, loadings, iteration controls).",
          "type": "object"
        },
        "d": {
          "description": "name).",
          "type": "string"
        },
        "intercept": {
          "default": true,
          "description": "Passed to the underlying ``rlasso`` first stages.",
          "type": "boolean"
        },
        "penalty": {
          "description": "Forwarded to :func:`statspai.rlasso.rlasso` (penalty level, loadings, iteration controls).",
          "type": "object"
        },
        "post": {
          "default": true,
          "description": "Post-Lasso (OLS refit) inside every selection step.",
          "type": "boolean"
        },
        "select_X": {
          "default": true,
          "description": "Lasso-select among the controls (partialling-out).",
          "type": "boolean"
        },
        "select_Z": {
          "default": true,
          "description": "Lasso-select among the instruments.",
          "type": "boolean"
        },
        "x": {
          "description": "Primary running variable, regressor, or feature input for this estimator.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "name).",
          "type": "string"
        },
        "z": {
          "description": "Instrument, proxy, or auxiliary variable used by this estimator.",
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "required": [
        "d",
        "y",
        "z"
      ],
      "type": "object"
    },
    "name": "rlasso_iv"
  },
  {
    "description": "Logistic rigorous (post-)Lasso -- a faithful port of ``hdm::rlassologit``. Validation: certified parity evidence.",
    "input_schema": {
      "properties": {
        "X": {
          "description": "Feature matrix or covariate DataFrame.",
          "type": "string"
        },
        "colnames": {
          "description": "Column names (default ``V1..Vp``).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "control": {
          "description": "``threshold`` -- coefficients below it are zeroed (default None).",
          "type": "object"
        },
        "intercept": {
          "default": true,
          "description": "Include an intercept.",
          "type": "boolean"
        },
        "penalty": {
          "description": "Overrides for ``c`` (slack; default 1.1 for ``post=True``, else 0.5), ``gamma`` (default ``0.1/log n``) and ``lambda`` (raw penalty; bypasses the data-driven level).",
          "type": "object"
        },
        "post": {
          "default": true,
          "description": "If ``True``, refit the selected support by *unpenalized* logistic regression (post-Lasso); else keep the glmnet-penalized fit.",
          "type": "boolean"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "enum": [
            "0",
            "1"
          ],
          "type": "string"
        }
      },
      "required": [
        "X",
        "y"
      ],
      "type": "object"
    },
    "name": "rlassologit"
  },
  {
    "description": "Effect of ``d`` on a binary ``y`` after Lasso-selecting controls ``x``. Validation: certified parity evidence.",
    "input_schema": {
      "properties": {
        "I3": {
          "description": "Amelioration set forced into the control set (hdm's ``I3``).",
          "type": "string"
        },
        "d": {
          "description": "d parameter (Union[np.ndarray, pd.Series, str]).",
          "type": "string"
        },
        "post": {
          "default": true,
          "description": "Post-Lasso inside the two selection steps.",
          "type": "boolean"
        },
        "x": {
          "description": "Primary running variable, regressor, or feature input for this estimator.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "d",
        "x",
        "y"
      ],
      "type": "object"
    },
    "name": "rlassologit_effect"
  },
  {
    "description": "Logistic high-dimensional effect of each targeted column of ``X``. Validation: certified parity evidence.",
    "input_schema": {
      "properties": {
        "I3": {
          "description": "I3 parameter (Optional[np.ndarray]).",
          "type": "string"
        },
        "X": {
          "description": "Feature matrix or covariate DataFrame.",
          "type": "string"
        },
        "index": {
          "description": "index parameter (Optional[Sequence[int]]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "post": {
          "default": true,
          "description": "post parameter (bool).",
          "type": "boolean"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "X",
        "y"
      ],
      "type": "object"
    },
    "name": "rlassologit_effects"
  },
  {
    "description": "Robust / unconstrained Synthetic Control. Validation: certified parity evidence. Assumptions: A convex (or regularized) combination of donor units reproduces the treated unit's pre-treatment outcome path; No interference: the treatment does not affect the donor units (SUTVA); No anticipation before the treatment date. Pre-conditions: Panel of one or more treated units plus an untreated donor pool, observed over time; Pre-treatment window long enough to fit donor weights (rule of thumb: more pre-periods than donors used); Outcome observed for every unit in every period. Failure modes: Large pre-treatment RMSPE -- the synthetic unit fails to track the treated unit before treatment -> Add donors / predictors, lengthen the pre-period, or use a bias-corrected estimator (sdid, augsynth); Placebo / permutation inference shows the estimate is not extreme relative to donors -> Report the placebo distribution honestly; the effect may not be distinguishable from noise. Alternatives: sp.sdid, sp.augsynth, sp.gsynth, sp.callaway_santanna. Typical minimum N: 15.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level.",
          "type": "number"
        },
        "covariates": {
          "description": "Not supported: the weights are fitted on pre-treatment outcomes only. Passing covariates raises ``NotImplementedError`` (it used to be silently ignored).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "intercept": {
          "default": true,
          "description": "Fit an unpenalised intercept (level shift). Only for unconstrained / elastic_net.",
          "type": "boolean"
        },
        "l1_penalty": {
          "default": 0.0,
          "description": "L1 penalty ``l1_penalty * ||w||_1`` in the objective ``||y - mu - X w||^2 + l2 ||w||^2 + l1 ||w||_1``. glmnet's ``(lambda, alpha)`` on the same data maps to ``l1 = 2 n lambda alpha`` (``n`` = pre-periods) once ``y`` is scaled to unit (1/n) standard deviation.",
          "type": "number"
        },
        "l2_penalty": {
          "default": 0.01,
          "description": "Ridge penalty ``l2_penalty * ||w||^2`` (glmnet: ``l2 = n lambda (1 - alpha)``). ``0`` with ``l1_penalty=0`` is ordinary least squares with an intercept -- Doudchenko & Imbens' unconstrained estimator, identical to ``scpi::scest(w.constr = list(name = \"ols\"))`` on ``scdata(constant = TRUE)``.",
          "type": "number"
        },
        "outcome": {
          "description": "Outcome variable name.",
          "type": "string"
        },
        "placebo": {
          "default": true,
          "description": "Run in-space placebo inference.",
          "type": "boolean"
        },
        "time": {
          "description": "Time period column.",
          "type": "string"
        },
        "treated_unit": {
          "description": "Identifier of the treated unit.",
          "type": "string"
        },
        "treatment_time": {
          "description": "First treatment period.",
          "type": "string"
        },
        "unit": {
          "description": "Unit identifier column.",
          "type": "string"
        },
        "variant": {
          "default": "unconstrained",
          "description": "* ``'unconstrained'`` -- no sign / sum constraints; optional intercept. * ``'elastic_net'`` -- L1 + L2 penalty, no sign constraints. * ``'penalized'`` -- simplex constraints + ridge penalty (see the module docstring; not Abadie & L'Hour 2021).",
          "enum": [
            "unconstrained",
            "elastic_net",
            "penalized"
          ],
          "type": "string"
        }
      },
      "required": [
        "outcome",
        "time",
        "treated_unit",
        "treatment_time",
        "unit"
      ],
      "type": "object"
    },
    "name": "robust_synth"
  },
  {
    "description": "Run an automated battery of robustness checks.",
    "input_schema": {
      "properties": {
        "cluster_var": {
          "description": "Column for clustered SE check.",
          "type": "string"
        },
        "drop_controls": {
          "description": "Baseline controls to drop (one-by-one) for sensitivity.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "extra_controls": {
          "description": "Additional controls to add (one-by-one) beyond baseline.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "formula": {
          "description": "Baseline regression formula, e.g. ``\"y ~ x1 + x2 + x3\"``.",
          "type": "string"
        },
        "subsets": {
          "description": "Named boolean masks for subsample checks.",
          "type": "object"
        },
        "trim_pct": {
          "description": "Drop observations beyond this percentile from both tails. E.g. ``0.01`` trims top and bottom 1 %.",
          "type": "number"
        },
        "winsor_levels": {
          "description": "Winsorization percentiles, e.g. ``[0.01, 0.05]``.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "x": {
          "description": "Key explanatory variable whose estimate stability is assessed.",
          "type": "string"
        }
      },
      "required": [
        "formula",
        "x"
      ],
      "type": "object"
    },
    "name": "robustness_report"
  },
  {
    "description": "Romano-Wolf stepdown adjusted p-values for multiple outcomes. Validation: validated evidence tier (known-truth, reference, external-parity, or Monte Carlo artifact).",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Nominal significance level (used only for display).",
          "type": "number"
        },
        "cluster": {
          "description": "Column name for cluster-robust standard errors and cluster bootstrap resampling.",
          "type": "string"
        },
        "controls": {
          "description": "Additional control variables included in every regression.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "n_boot": {
          "default": 1000,
          "description": "Number of bootstrap replications.",
          "type": "integer"
        },
        "seed": {
          "description": "Random seed for reproducibility (uses ``np.random.default_rng``).",
          "type": "integer"
        },
        "x": {
          "description": "Treatment / regressor(s) of interest. The adjusted p-value corresponds to the *first* element of ``x``.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Outcome variable names (one regression per outcome).",
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "required": [
        "x",
        "y"
      ],
      "type": "object"
    },
    "name": "romano_wolf"
  },
  {
    "description": "Compute Rosenbaum bounds on a paired observational study. Validation: certified parity evidence.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level used to report ``gamma_critical``.",
          "type": "number"
        },
        "alternative": {
          "default": "greater",
          "description": "Direction of the alternative hypothesis for the treatment effect.",
          "enum": [
            "greater",
            "less",
            "two-sided"
          ],
          "type": "string"
        },
        "control": {
          "description": "Outcome in the treated / control unit of each matched pair (same length). Ignored if ``data`` is provided.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "gamma_grid": {
          "description": "Gamma values (>= 1) over which to compute bounding p-values.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "method": {
          "default": "wilcoxon",
          "description": "Wilcoxon signed-rank bound (continuous) or binomial sign test (robust / binary).",
          "enum": [
            "wilcoxon",
            "sign"
          ],
          "type": "string"
        },
        "pair_id": {
          "description": "pair_id parameter (Optional[str]).",
          "type": "string"
        },
        "treat": {
          "description": "Treatment indicator or first-treatment-period column.",
          "type": "string"
        },
        "treated": {
          "description": "Outcome in the treated / control unit of each matched pair (same length). Ignored if ``data`` is provided.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        },
        "zero_method": {
          "default": "pratt",
          "description": "Treatment of pairs with a zero difference in the Wilcoxon bound. ``\"pratt\"`` ranks them with the other pairs and gives them no weight (Rosenbaum's ``DOS2::senWilcox``, Stata ``rbounds``); ``\"wilcox\"`` drops them before ranking (``rbounds::psens``). Identical when no difference is zero. Ignored by the sign test, which always drops zeros.",
          "enum": [
            "pratt",
            "wilcox"
          ],
          "type": "string"
        }
      },
      "required": [],
      "type": "object"
    },
    "name": "rosenbaum_bounds"
  },
  {
    "description": "Compute Rosenbaum bounds on a paired observational study. Validation: certified parity evidence.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level used to report ``gamma_critical``.",
          "type": "number"
        },
        "alternative": {
          "default": "greater",
          "description": "Direction of the alternative hypothesis for the treatment effect.",
          "enum": [
            "greater",
            "less",
            "two-sided"
          ],
          "type": "string"
        },
        "control": {
          "description": "Outcome in the treated / control unit of each matched pair (same length). Ignored if ``data`` is provided.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "gamma_grid": {
          "description": "Gamma values (>= 1) over which to compute bounding p-values.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "method": {
          "default": "wilcoxon",
          "description": "Wilcoxon signed-rank bound (continuous) or binomial sign test (robust / binary).",
          "enum": [
            "wilcoxon",
            "sign"
          ],
          "type": "string"
        },
        "pair_id": {
          "description": "pair_id parameter (Optional[str]).",
          "type": "string"
        },
        "treat": {
          "description": "Treatment indicator or first-treatment-period column.",
          "type": "string"
        },
        "treated": {
          "description": "Outcome in the treated / control unit of each matched pair (same length). Ignored if ``data`` is provided.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        },
        "zero_method": {
          "default": "pratt",
          "description": "Treatment of pairs with a zero difference in the Wilcoxon bound. ``\"pratt\"`` ranks them with the other pairs and gives them no weight (Rosenbaum's ``DOS2::senWilcox``, Stata ``rbounds``); ``\"wilcox\"`` drops them before ranking (``rbounds::psens``). Identical when no difference is zero. Ignored by the sign test, which always drops zeros.",
          "enum": [
            "pratt",
            "wilcox"
          ],
          "type": "string"
        }
      },
      "required": [],
      "type": "object"
    },
    "name": "rosenbaum_gamma"
  },
  {
    "description": "Stable Balancing Weights (Zubizarreta 2015) with optional ATT/ATE Validation: certified parity evidence. Assumptions: Unconfoundedness: treatment is as-good-as-random given the measured covariates; Overlap / common support: every unit has a non-degenerate probability of each treatment; The covariate set blocks all back-door paths. Pre-conditions: Pre-treatment covariates measured for treated and control units; A binary (or low-cardinality) treatment indicator; Sufficient covariate overlap between treatment arms. Failure modes: Poor overlap -- extreme propensity scores or few acceptable matches -> Trim or restrict to the common-support region and report the discarded units; Covariate imbalance remains after matching/weighting -> Re-specify the balancing model (CBPS, entropy balancing) and re-check standardized mean differences. Alternatives: sp.propensity_score, sp.cbps, sp.ebalance, sp.dml. Typical minimum N: 200.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for inference on the outcome.",
          "type": "number"
        },
        "covariates": {
          "description": "Columns whose means must be balanced.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "delta": {
          "default": 0.02,
          "description": "Balance tolerance. With ``tolerance_scale='sd'`` the constraint is ``|mean_T(X_j) - weighted mean_C(X_j)| <= delta_j * sd(X_j)``.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "estimand": {
          "default": "att",
          "description": "``'att'`` reweights controls to match treated means (standard); ``'atc'`` reweights treated to match control means; ``'ate'`` reweights each group to match the pooled means.",
          "enum": [
            "att",
            "ate",
            "atc"
          ],
          "type": "string"
        },
        "include_squares": {
          "default": false,
          "description": "Also balance second-moments (w_j2 columns).",
          "type": "boolean"
        },
        "objective": {
          "default": "variance",
          "description": "Dispersion objective. ``'variance'`` minimises Sigma w_i2; ``'entropy'`` minimises Sigma w_i log(n * w_i) (KL from uniform).",
          "enum": [
            "variance",
            "entropy"
          ],
          "type": "string"
        },
        "solver_options": {
          "description": "Passed to ``scipy.optimize.minimize``.",
          "type": "object"
        },
        "tolerance_scale": {
          "default": "sd",
          "description": "Standard deviation ``delta`` is quoted in. ``'sd'`` is the full-sample sd; ``'target'`` the sd of the group being matched to and ``'group'`` the sd of the group being reweighted, matching ``sbw::sbw``'s ``bal_std=\"target\"`` / ``\"group\"``; ``'raw'`` is unstandardised.",
          "enum": [
            "sd",
            "raw",
            "target",
            "group"
          ],
          "type": "string"
        },
        "treat": {
          "description": "Binary 0/1 treatment indicator column.",
          "type": "string"
        },
        "y": {
          "description": "Outcome column. If provided, a weighted ATT/ATE estimate with HC-robust SE is attached to the returned :class:`SBWResult`.",
          "type": "string"
        }
      },
      "required": [
        "covariates",
        "treat"
      ],
      "type": "object"
    },
    "name": "sbw"
  },
  {
    "description": "R-style alias: ``synthdid::sc_estimate``. Validation: certified parity evidence. Assumptions: A convex (or regularized) combination of donor units reproduces the treated unit's pre-treatment outcome path; No interference: the treatment does not affect the donor units (SUTVA); No anticipation before the treatment date. Pre-conditions: Panel of one or more treated units plus an untreated donor pool, observed over time; Pre-treatment window long enough to fit donor weights (rule of thumb: more pre-periods than donors used); Outcome observed for every unit in every period. Failure modes: Large pre-treatment RMSPE -- the synthetic unit fails to track the treated unit before treatment -> Add donors / predictors, lengthen the pre-period, or use a bias-corrected estimator (sdid, augsynth); Placebo / permutation inference shows the estimate is not extreme relative to donors -> Report the placebo distribution honestly; the effect may not be distinguishable from noise. Alternatives: sp.sdid, sp.augsynth, sp.gsynth, sp.callaway_santanna. Typical minimum N: 15.",
    "input_schema": {
      "properties": {
        "time": {
          "description": "Time period column.",
          "type": "string"
        },
        "treat_time": {
          "description": "treat_time parameter.",
          "type": "string"
        },
        "treat_unit": {
          "description": "treat_unit parameter.",
          "type": "string"
        },
        "unit": {
          "description": "Unit identifier column.",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "time",
        "treat_time",
        "treat_unit",
        "unit",
        "y"
      ],
      "type": "object"
    },
    "name": "sc_estimate"
  },
  {
    "description": "Prepare data matrices for synthetic control estimation. Validation: certified parity evidence.",
    "input_schema": {
      "properties": {
        "outcome": {
          "description": "Outcome variable column name.",
          "type": "string"
        },
        "time": {
          "description": "Time period column name.",
          "type": "string"
        },
        "treated_unit": {
          "description": "Identifier of the treated unit.",
          "type": "string"
        },
        "treatment_time": {
          "description": "First treatment period (periods ``< treatment_time`` are pre-treatment).",
          "type": "string"
        },
        "unit": {
          "description": "Unit identifier column name.",
          "type": "string"
        }
      },
      "required": [
        "outcome",
        "time",
        "treated_unit",
        "treatment_time",
        "unit"
      ],
      "type": "object"
    },
    "name": "scdata"
  },
  {
    "description": "Estimate synthetic control weights (R ``scpi::scest``). Validation: certified parity evidence. Assumptions: A convex (or regularized) combination of donor units reproduces the treated unit's pre-treatment outcome path; No interference: the treatment does not affect the donor units (SUTVA); No anticipation before the treatment date. Pre-conditions: Panel of one or more treated units plus an untreated donor pool, observed over time; Pre-treatment window long enough to fit donor weights (rule of thumb: more pre-periods than donors used); Outcome observed for every unit in every period. Failure modes: Large pre-treatment RMSPE -- the synthetic unit fails to track the treated unit before treatment -> Add donors / predictors, lengthen the pre-period, or use a bias-corrected estimator (sdid, augsynth); Placebo / permutation inference shows the estimate is not extreme relative to donors -> Report the placebo distribution honestly; the effect may not be distinguishable from noise. Alternatives: sp.sdid, sp.augsynth, sp.gsynth, sp.callaway_santanna. Typical minimum N: 15.",
    "input_schema": {
      "properties": {
        "Q": {
          "description": "Norm bound of the constraint (sum of weights for ``simplex``).",
          "type": "number"
        },
        "Q2": {
          "description": "L2 bound for ``'L1-L2'``. Default: R's ridge rule.",
          "type": "number"
        },
        "lasso_lambda": {
          "description": "Deprecated and ignored (emit ``DeprecationWarning``). Before 1.28.x these set L1 / L2 *penalties* of a different estimator; R ``scpi`` constrains the norm of ``w`` instead -- use ``Q`` / ``Q2``.",
          "type": "number"
        },
        "outcome": {
          "description": "Outcome variable column name.",
          "type": "string"
        },
        "ridge_lambda": {
          "description": "Deprecated and ignored (emit ``DeprecationWarning``). Before 1.28.x these set L1 / L2 *penalties* of a different estimator; R ``scpi`` constrains the norm of ``w`` instead -- use ``Q`` / ``Q2``.",
          "type": "number"
        },
        "time": {
          "description": "Time period column name.",
          "type": "string"
        },
        "treated_unit": {
          "description": "Identifier of the treated unit.",
          "type": "string"
        },
        "treatment_time": {
          "description": "First treatment period.",
          "type": "string"
        },
        "unit": {
          "description": "Unit identifier column name.",
          "type": "string"
        },
        "w_constr": {
          "default": "simplex",
          "description": "Weight constraint, as in R ``w.constr = list(name = ...)``: - ``'simplex'`` : w >= 0, sum(w) = Q (Q = 1) - ``'lasso'`` : ||w||_1 <= Q (Q = 1), weights may be negative - ``'ridge'`` : ||w||_2 <= Q, Q from R's ``shrinkage.EST`` rule (``max(Q_hat, 0.5)`` with Q_hat = ||b_ols|| / (1 + lambda), lambda = sigma^2 J / ||b_ols||^2) - ``'L1-L2'`` : w >= 0, sum(w) = 1 and ||w||_2 <= Q2 (Q2 as ridge Q) - ``'ols'`` : unconstrained least squares (``'ls'`` is an alias)",
          "type": "string"
        }
      },
      "required": [
        "outcome",
        "time",
        "treated_unit",
        "treatment_time",
        "unit"
      ],
      "type": "object"
    },
    "name": "scest"
  },
  {
    "description": "Adversarial dose-response estimator (Bica et al. 2020). Assumptions: Unconfoundedness given covariates X for the continuous treatment; Positivity over the dose support; The adversarial generator recovers the counterfactual dose distribution (Bica et al. 2020). Pre-conditions: data with a continuous treatment (dose), outcome and covariates; torch is installed (neural extra) -- imported lazily. Failure modes: Unstable adversarial training -- dose-response estimates vary across seeds -> Average across seeds or use the smoother varying-coefficient estimator. Alternatives: sp.vcnet, sp.dose_response. Typical minimum N: 500.",
    "input_schema": {
      "properties": {
        "covariates": {
          "description": "Covariate matrix, DataFrame, or column names.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "propensity_weights": {
          "description": "propensity_weights parameter (Optional[np.ndarray]).",
          "type": "string"
        },
        "t_grid": {
          "description": "Grid of t values to evaluate.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "treatment": {
          "description": "Treatment indicator, treatment variable, or treatment array.",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "covariates",
        "treatment",
        "y"
      ],
      "type": "object"
    },
    "name": "scigan"
  },
  {
    "description": "Prediction intervals for synthetic control methods (R ``scpi::scpi``). Validation: certified parity evidence. Cost: Prediction intervals come from a simulation step on top of the point fit, so runtime is dominated by the number of simulations rather than n. cores= is accepted for API compatibility with R scpi but the simulation currently runs serially. Assumptions: A convex (or regularized) combination of donor units reproduces the treated unit's pre-treatment outcome path; No interference: the treatment does not affect the donor units (SUTVA); No anticipation before the treatment date. Pre-conditions: Panel of one or more treated units plus an untreated donor pool, observed over time; Pre-treatment window long enough to fit donor weights (rule of thumb: more pre-periods than donors used); Outcome observed for every unit in every period. Failure modes: Large pre-treatment RMSPE -- the synthetic unit fails to track the treated unit before treatment -> Add donors / predictors, lengthen the pre-period, or use a bias-corrected estimator (sdid, augsynth); Placebo / permutation inference shows the estimate is not extreme relative to donors -> Report the placebo distribution honestly; th...",
    "input_schema": {
      "properties": {
        "Q": {
          "description": "Constraint bounds; see :func:`scest`.",
          "type": "number"
        },
        "Q2": {
          "description": "Constraint bounds; see :func:`scest`.",
          "type": "number"
        },
        "alpha": {
          "default": 0.05,
          "description": "Default for both ``u_alpha`` and ``e_alpha``. As in R, the combined interval has nominal coverage at least ``1 - (u_alpha + e_alpha)`` (0.90 with the defaults).",
          "type": "number"
        },
        "cores": {
          "default": 1,
          "description": "Accepted for API compatibility; the simulation runs serially.",
          "type": "integer"
        },
        "draws": {
          "description": "``(J, sims)`` matrix of standard-normal draws used for the in-sample simulation instead of generating them; passing the matrix R's ``rnorm`` produced reproduces R's simulated bounds.",
          "type": "string"
        },
        "e_alpha": {
          "description": "Levels of the in-sample / out-of-sample bounds (default ``alpha``).",
          "type": "number"
        },
        "e_method": {
          "default": "gaussian",
          "description": "Out-of-sample bound: sub-Gaussian, location-scale or quantile regression. All three are always computed (``model_info``).",
          "enum": [
            "gaussian",
            "ls",
            "qreg"
          ],
          "type": "string"
        },
        "e_order": {
          "default": 1,
          "description": "Order of the out-of-sample moment models (R ``e.order``; ``e.lags`` is fixed at 0).",
          "enum": [
            "0",
            "1"
          ],
          "type": "integer"
        },
        "lasso_lambda": {
          "description": "Deprecated and ignored; see :func:`scest`.",
          "type": "number"
        },
        "outcome": {
          "description": "Column names.",
          "type": "string"
        },
        "pi_type": {
          "default": "both",
          "description": "Which interval is reported in ``period_results`` / ``ci``: ``'both'`` = R ``CI.all.<e_method>`` (in-sample + out-of-sample bounds), ``'in_sample'`` = R ``CI.in.sample``, ``'out_of_sample'`` = synthetic fit + out-of-sample bounds only (not an R table).",
          "enum": [
            "both",
            "in_sample",
            "out_of_sample"
          ],
          "type": "string"
        },
        "rho": {
          "description": "Regularisation of the local geometry (default ``'type-2'``).",
          "enum": [
            "type-1",
            "type-2"
          ],
          "type": "number"
        },
        "rho_max": {
          "default": 0.2,
          "description": "Upper bound on ``rho``.",
          "type": "number"
        },
        "ridge_lambda": {
          "description": "Deprecated and ignored; see :func:`scest`.",
          "type": "number"
        },
        "seed": {
          "description": "Seed of the ``numpy`` generator for the in-sample draws (R uses its own ``rnorm`` stream, so simulated bounds agree with R only up to Monte Carlo error unless ``draws`` is supplied).",
          "type": "integer"
        },
        "sims": {
          "default": 200,
          "description": "Number of in-sample simulation draws (R ``sims``; >= 10).",
          "type": "integer"
        },
        "time": {
          "description": "Column names.",
          "type": "string"
        },
        "treated_unit": {
          "description": "Identifier of the treated unit.",
          "type": "string"
        },
        "treatment_time": {
          "description": "First treatment period.",
          "type": "string"
        },
        "u_alpha": {
          "description": "Levels of the in-sample / out-of-sample bounds (default ``alpha``).",
          "type": "number"
        },
        "u_missp": {
          "default": true,
          "description": "Allow a misspecified conditional mean of the pseudo-residuals (R ``u.missp``).",
          "type": "boolean"
        },
        "u_order": {
          "default": 1,
          "description": "Order of the pseudo-residual mean model (R ``u.order``; ``u.lags`` is fixed at 0).",
          "enum": [
            "0",
            "1"
          ],
          "type": "integer"
        },
        "u_sigma": {
          "default": "HC1",
          "description": "Variance estimator of the pseudo-residuals.",
          "enum": [
            "HC0",
            "HC1",
            "HC2",
            "HC3",
            "HC4"
          ],
          "type": "string"
        },
        "unit": {
          "description": "Column names.",
          "type": "string"
        },
        "w_constr": {
          "default": "simplex",
          "description": "Weight constraint; see :func:`scest`.",
          "type": "string"
        }
      },
      "required": [
        "outcome",
        "time",
        "treated_unit",
        "treatment_time",
        "unit"
      ],
      "type": "object"
    },
    "name": "scpi"
  },
  {
    "description": "Synthetic Difference-in-Differences estimator (and SC / DID variants). Validation: certified parity evidence. Do NOT use when: there is no clean pre-treatment block for every unit -- the unit and time weights are fit on the pre-period grid. Cost: Placebo / bootstrap standard errors refit the full weighting problem n_reps times; the point estimate alone is cheap. Lower n_reps while iterating. Assumptions: Parallel trends in the absence of treatment, after the synthetic/DiD weighting; No anticipation and no interference between units (SUTVA); The control pool's outcome process is stable around the intervention. Pre-conditions: Panel with treated and control units and a clear treatment date; Pre-treatment periods available to assess comparability of trends. Failure modes: Weighted pre-treatment trends still diverge between treated and synthetic control -> Inspect the unit/time weights and pre-trend fit; consider event-study DiD with honest bounds. Alternatives: sp.synth, sp.augsynth, sp.callaway_santanna, sp.gardner_did. Typical minimum N: 15.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals.",
          "type": "number"
        },
        "backend": {
          "default": "native",
          "description": "``'native'`` uses StatsPAI's Python implementation. ``'synthdid'``/``'r'`` delegates to the R ``synthdid`` package through ``Rscript`` and returns the reference package's point estimate and ``synthdid_se`` standard error. The R backend is mainly for exact cross-language parity claims; the dependency- light native implementation remains the default.",
          "enum": [
            "native",
            "synthdid",
            "r"
          ],
          "type": "string"
        },
        "covariates": {
          "description": "Not supported by the native solver yet; passing any raises :class:`~statspai.exceptions.MethodIncompatibility` rather than silently estimating the unadjusted model (through 1.28.0 the argument was accepted and ignored).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "method": {
          "default": "sdid",
          "description": "* ``'sdid'`` -- Synthetic DID (unit + time weights) * ``'sc'`` -- Synthetic Control (unit weights only) * ``'did'`` -- DID (uniform weights)",
          "enum": [
            "sdid",
            "sc",
            "did"
          ],
          "type": "string"
        },
        "n_reps": {
          "default": 200,
          "description": "Replications for placebo / bootstrap SE.",
          "type": "integer"
        },
        "outcome": {
          "description": "Outcome variable column. Alias ``y=`` accepted for R-style calls.",
          "type": "string"
        },
        "se_method": {
          "default": "placebo",
          "description": "Standard-error method (see Notes).",
          "enum": [
            "placebo",
            "bootstrap",
            "jackknife"
          ],
          "type": "string"
        },
        "seed": {
          "description": "Random seed for reproducibility.",
          "type": "integer"
        },
        "time": {
          "description": "Time period column.",
          "type": "string"
        },
        "treat_time": {
          "description": "treat_time parameter.",
          "type": "string"
        },
        "treat_unit": {
          "description": "treat_unit parameter.",
          "type": "string"
        },
        "treated_unit": {
          "description": "Treated unit(s). Alias ``treat_unit=`` accepted.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "treatment_time": {
          "description": "First treatment period (inclusive). Alias ``treat_time=`` accepted.",
          "type": "string"
        },
        "unit": {
          "description": "Unit identifier column.",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [],
      "type": "object"
    },
    "name": "sdid"
  },
  {
    "description": "Score and rank candidate proxies for PCI. Assumptions: The proxies are valid negative controls (relevant to the confounder, excluded from the causal channel); A bridge function exists (completeness conditions hold). Pre-conditions: Treatment-inducing and outcome-inducing proxy variables (negative controls) for the unobserved confounder. Failure modes: Proxies are weak or invalid -- the bridge function is poorly identified -> Test proxy relevance, select stronger proxies, or fall back to sensitivity analysis. Alternatives: sp.select_pci_proxies, sp.dml. Typical minimum N: 500.",
    "input_schema": {
      "properties": {
        "candidates": {
          "description": "All variables that could plausibly serve as proxies.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "covariates": {
          "description": "Covariate matrix, DataFrame, or column names.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "top_k": {
          "default": 2,
          "description": "Number of top candidates to recommend per side.",
          "type": "integer"
        },
        "treat": {
          "description": "Treatment indicator or first-treatment-period column.",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "candidates",
        "treat",
        "y"
      ],
      "type": "object"
    },
    "name": "select_pci_proxies"
  },
  {
    "description": "Lee (2009) bounds for ATE under sample selection, optionally Validation: validated evidence tier (known-truth, reference, external-parity, or Monte Carlo artifact). Assumptions: Only weak (set-identifying) assumptions are imposed; the result is an interval, not a point; Lee bounds add monotonicity of selection; Oster's delta adds proportional selection on observed vs. unobserved. Pre-conditions: The data needed for the point-identifying analysis, plus the weakest credible identifying restriction; For Lee bounds: a binary selection/attrition indicator. Failure modes: Bounds are too wide to be informative -> Add a credible auxiliary restriction (monotone treatment response, instrument) to tighten the bounds. Alternatives: sp.oster_delta, sp.lee_bounds, sp.manski_bounds. Typical minimum N: 100.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "covariates": {
          "description": "Covariates to condition on for tighter (conditional) bounds.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "method": {
          "default": "conditional",
          "description": "- ``'conditional'``: compute Lee bounds within covariate strata and average (tighter). - ``'unconditional'``: standard Lee bounds ignoring covariates.",
          "type": "string"
        },
        "n_boot": {
          "default": 500,
          "description": "Number of bootstrap replications.",
          "type": "integer"
        },
        "random_state": {
          "default": 42,
          "description": "Random seed or RandomState for reproducible stochastic steps.",
          "type": "integer"
        },
        "selection": {
          "description": "Binary indicator: 1 = outcome observed, 0 = missing.",
          "type": "string"
        },
        "treatment": {
          "description": "Binary treatment (0/1).",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable (may have NaN when selection=0).",
          "type": "string"
        }
      },
      "required": [
        "selection",
        "treatment",
        "y"
      ],
      "type": "object"
    },
    "name": "selection_bounds"
  },
  {
    "description": "Sensitivity analysis for omitted variable bias (Cinelli & Hazlett 2020). Validation: certified parity evidence. Assumptions: Cinelli-Hazlett (2020) omitted-variable sensitivity in the partial-R-squared framework; the Robustness Value is the minimum partial R-squared a confounder needs with both treatment and outcome to nullify the estimate; Outcome is modeled by OLS of y on treat plus observed controls; benchmarking calibrates confounder strength relative to named observed controls; rv_q is the RV to change the point-estimate sign; rv_qa is the RV to lose significance at alpha. Pre-conditions: A DataFrame with outcome, treatment, and observed control columns; At least one control to anchor the benchmark comparison. Failure modes: Benchmark names a control not present in the controls list or data -> Pass benchmark names that are a subset of the controls actually included in the regression; Perfectly collinear controls make the partial-R-squared decomposition unstable -> Drop redundant collinear controls before computing the robustness value. Alternatives: sp.oster_bounds, sp.evalue, sp.unified_sensitivity. Typical minimum N: 50.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "benchmark": {
          "description": "Covariates to benchmark confounding strength against",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "controls": {
          "description": "Observed control variables",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "treat": {
          "description": "Treatment column of interest",
          "type": "string"
        },
        "y": {
          "description": "Outcome column",
          "type": "string"
        }
      },
      "required": [
        "controls",
        "treat",
        "y"
      ],
      "type": "object"
    },
    "name": "sensemakr"
  },
  {
    "description": "Unified sensitivity analysis for observational causal estimates -- supports Oster (2019) delta/R-max, Cinelli-Hazlett (2020) omitted-variable bias bounds, and E-values (VanderWeele-Ding 2017). Tells the agent how strong an unobserved confounder would have to be to overturn the result. Validation: certified parity evidence.",
    "input_schema": {
      "properties": {
        "controls": {
          "description": "Observed control columns.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "include_oster": {
          "default": true,
          "type": "boolean"
        },
        "include_rosenbaum": {
          "default": true,
          "type": "boolean"
        },
        "include_sensemakr": {
          "default": true,
          "type": "boolean"
        },
        "result": {
          "description": "Fitted regression / causal result handle (result_id from a prior fit run with as_handle=true). Required -- the bounds are computed relative to this estimate.",
          "type": "string"
        },
        "rho_max": {
          "default": 1.0,
          "description": "Max correlation between the omitted confounder and treatment, for the Oster bound.",
          "type": "number"
        },
        "treat": {
          "description": "Treatment column.",
          "type": "string"
        },
        "y": {
          "description": "Outcome column (lets the bound recompute covariate R^2 from data alongside the result).",
          "type": "string"
        }
      },
      "required": [],
      "type": "object"
    },
    "name": "sensitivity"
  },
  {
    "description": "Comprehensive multi-dimensional sensitivity analysis.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "dimensions": {
          "description": "Which dimensions to test. Default: all applicable.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "result": {
          "description": "Baseline estimated result.",
          "type": "string"
        },
        "verbose": {
          "default": true,
          "description": "verbose parameter (bool).",
          "type": "boolean"
        }
      },
      "required": [
        "result"
      ],
      "type": "object"
    },
    "name": "sensitivity_dashboard"
  },
  {
    "description": "Run sp.sensitivity / sp.evalue / sp.oster_bounds / sp.sensemakr on a cached result. Pass method='evalue' (default) for the omitted-confounder-strength bound, 'oster' for delta/R-max, 'cinelli_hazlett' for OVB bounds.",
    "input_schema": {
      "properties": {
        "benchmark_covariate": {
          "description": "Cinelli-Hazlett benchmark column (optional).",
          "type": "string"
        },
        "method": {
          "default": "evalue",
          "enum": [
            "evalue",
            "oster",
            "cinelli_hazlett",
            "auto"
          ],
          "type": "string"
        },
        "result_id": {
          "description": "Handle to a fitted causal result.",
          "type": "string"
        }
      },
      "required": [
        "result_id"
      ],
      "type": "object"
    },
    "name": "sensitivity_from_result"
  },
  {
    "description": "Plot Rambachan & Roth (2023) sensitivity analysis.",
    "input_schema": {
      "properties": {
        "ax": {
          "description": "ax parameter.",
          "type": "string"
        },
        "breakdown_color": {
          "default": "#E74C3C",
          "description": "Color for the breakdown point marker.",
          "type": "string"
        },
        "color": {
          "default": "#2C3E50",
          "description": "CI band color.",
          "type": "string"
        },
        "figsize": {
          "default": [
            10,
            6
          ],
          "description": "figsize parameter (Tuple[float, float]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "original_ci": {
          "description": "Original CI (at M=0) for comparison.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "original_color": {
          "default": "#27AE60",
          "description": "Color for original estimate marker.",
          "type": "string"
        },
        "original_estimate": {
          "description": "Original point estimate.",
          "type": "number"
        },
        "sensitivity": {
          "description": "Output from ``honest_did()``.",
          "type": "string"
        },
        "title": {
          "description": "title parameter (Optional[str]).",
          "type": "string"
        }
      },
      "required": [
        "sensitivity"
      ],
      "type": "object"
    },
    "name": "sensitivity_plot"
  },
  {
    "description": "Rambachan-Roth (2023) honest-DiD sensitivity analysis: computes the largest violation of parallel trends (parametrised by Mbar -- relative magnitude of the post-period violation versus the worst observed pre-period one) under which the post-treatment ATT is still different from zero at level alpha. Reports both the robust confidence sets and the breakdown Mbar. Assumptions: Pre-period violations bound the magnitude of post-period violations (relative-magnitude family); Post-treatment effects are constant across event time (relax via alternative parameter families in Rambachan-Roth 2023 Section 3). Pre-conditions: result has at least one pre-period and one post-period coefficient; result carries the variance-covariance matrix of those coefficients. Failure modes: Breakdown Mbar < 1.0 (small parallel-trends violation overturns the sign) -> The result is fragile to plausible pretrends violations; report the breakdown alongside the point estimate; Confidence set is the entire real line (Mbar grid too coarse) -> Re-run with a finer grid (n_grid=50+) or restrict Mbar to a tighter interval. Alternatives: sp.honest_did, sp.pretrends_test, sp.breakdown_m. Typical minimum N: 50.",
    "input_schema": {
      "properties": {
        "Mbar": {
          "description": "Grid of relative-magnitude bounds; default is np.linspace(0, 2, n_grid)",
          "type": "string"
        },
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "method": {
          "default": "C-LF",
          "description": "Identification method",
          "enum": [
            "C-LF"
          ],
          "type": "string"
        },
        "n_grid": {
          "default": 20,
          "description": "Mbar grid size when Mbar=None",
          "type": "integer"
        },
        "result": {
          "description": "Event-study or DiD result with full pre/post coefficients",
          "type": "string"
        }
      },
      "required": [
        "result"
      ],
      "type": "object"
    },
    "name": "sensitivity_rr"
  },
  {
    "description": "Cohort-by-cohort synthetic DID for staggered-adoption panels: runs sp.sdid on each adoption cohort's sub-panel with not-yet-treated donors (each ATT(g) equals synthdid::synthdid_estimate) and aggregates across cohorts. Not the Arkhangelsky-Samkov sequential SDID estimator. Validation: validated evidence tier (known-truth, reference, external-parity, or Monte Carlo artifact). Assumptions: Parallel trends in the absence of treatment, after the synthetic/DiD weighting; No anticipation and no interference between units (SUTVA); The control pool's outcome process is stable around the intervention. Pre-conditions: Panel with treated and control units and a clear treatment date; Pre-treatment periods available to assess comparability of trends. Failure modes: Weighted pre-treatment trends still diverge between treated and synthetic control -> Inspect the unit/time weights and pre-trend fit; consider event-study DiD with honest bounds. Alternatives: sp.synth, sp.augsynth, sp.callaway_santanna, sp.gardner_did. Typical minimum N: 15.",
    "input_schema": {
      "properties": {
        "cohort": {
          "description": "First-treated period column; never-treated = 0",
          "type": "string"
        },
        "cohort_weights": {
          "default": "size",
          "description": "cohort_weights parameter (str).",
          "enum": [
            "size",
            "equal"
          ],
          "type": "string"
        },
        "n_reps": {
          "default": 200,
          "description": "Number of reps.",
          "type": "integer"
        },
        "never_treated_value": {
          "default": 0,
          "description": "never_treated_value parameter.",
          "type": "string"
        },
        "outcome": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        },
        "se_method": {
          "default": "placebo",
          "description": "se_method parameter (str).",
          "enum": [
            "placebo",
            "bootstrap",
            "jackknife"
          ],
          "type": "string"
        },
        "time": {
          "description": "Time period column.",
          "type": "string"
        },
        "unit": {
          "description": "Unit identifier column.",
          "type": "string"
        }
      },
      "required": [
        "cohort",
        "outcome",
        "time",
        "unit"
      ],
      "type": "object"
    },
    "name": "sequential_sdid"
  },
  {
    "description": "Set every reachable RNG to a known seed for the duration of the",
    "input_schema": {
      "properties": {
        "jax": {
          "default": true,
          "description": "Yield a fresh JAX ``PRNGKey(seed)`` to the caller via the ``jax_key`` attribute on the yielded session object, when JAX is already imported. Never imports jax on its own. (JAX has no global state so we can't seed it -- agents must thread the key explicitly.)",
          "type": "boolean"
        },
        "pythonhashseed": {
          "default": false,
          "description": "Set ``PYTHONHASHSEED`` for the duration of the block. Most causal-inference numerics don't depend on dict iteration order, but spec-curve enumerators and graph-based DAG search sometimes do. Off by default to avoid surprising downstream callers.",
          "type": "boolean"
        },
        "seed": {
          "description": "Seed value. ``None`` (the default) means \"snapshot current state but don't reseed\" -- useful for opportunistic save / restore around code that you don't want to leak RNG drift.",
          "type": "integer"
        },
        "torch": {
          "default": true,
          "description": "Seed PyTorch (CPU + CUDA) when the library is already imported. Never imports torch on its own.",
          "type": "boolean"
        }
      },
      "required": [],
      "type": "object"
    },
    "name": "session"
  },
  {
    "description": "Shorrocks-Shapley decomposition of an inequality index across Validation: certified parity evidence.",
    "input_schema": {
      "properties": {
        "index": {
          "default": "theil_t",
          "description": "index parameter (str).",
          "type": "string"
        },
        "weights": {
          "description": "Observation weights.",
          "type": "string"
        },
        "x": {
          "description": "Primary running variable, regressor, or feature input for this estimator.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "x",
        "y"
      ],
      "type": "object"
    },
    "name": "shapley_inequality"
  },
  {
    "description": "Self-normalised IPS (bias-reduction for large IS weights). Validation: certified parity evidence. Assumptions: Same identification conditions as IPS (known propensities, positivity, no unmeasured confounding); Self-normalisation trades a small bias for large variance reduction under heavy importance weights. Pre-conditions: X (context), A (logged action), R (reward) and logging propensities are available. Failure modes: Residual bias when effective sample size is tiny (few logged actions overlap the target policy) -> Collect more on-support logged data or switch to the doubly-robust estimator. Alternatives: sp.ips, sp.doubly_robust, sp.direct_method. Typical minimum N: 500.",
    "input_schema": {
      "properties": {
        "A": {
          "description": "A parameter (np.ndarray).",
          "type": "string"
        },
        "R": {
          "description": "R parameter (np.ndarray).",
          "type": "string"
        },
        "X": {
          "description": "Feature matrix or covariate DataFrame.",
          "type": "string"
        },
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "clip": {
          "default": 50.0,
          "description": "clip parameter (float).",
          "type": "number"
        },
        "pi_behavior": {
          "description": "pi_behavior parameter (Optional[np.ndarray]).",
          "type": "string"
        },
        "pi_target": {
          "description": "pi_target parameter.",
          "type": "string"
        }
      },
      "required": [
        "A",
        "R",
        "X",
        "pi_target"
      ],
      "type": "object"
    },
    "name": "snips"
  },
  {
    "description": "Lerman-Yitzhaki (1985) Gini source decomposition. Validation: certified parity evidence.",
    "input_schema": {
      "properties": {
        "gini": {
          "default": "corrected",
          "description": "``\"corrected\"`` uses the ``n/(n-1)`` bias-corrected Gini, the default of :func:`inequality_index`. ``\"population\"`` uses the plug-in Gini, as Stata's ``descogini`` and Lerman & Yitzhaki's covariance formula do; shares ``S_k``, Gini correlations ``R_k`` and each source's percentage of the total are identical under both, since the factor cancels.",
          "enum": [
            "corrected",
            "population"
          ],
          "type": "string"
        },
        "sources": {
          "description": "Income-source columns; total income is their row sum.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "weights": {
          "description": "Observation weights.",
          "type": "string"
        }
      },
      "required": [
        "sources"
      ],
      "type": "object"
    },
    "name": "source_decompose"
  },
  {
    "description": "Specification-curve analysis (Simonsohn et al. 2020): enumerates every combination of model choices the user declares defensible, runs them all, and returns the sign/magnitude distribution. Use when an agent needs to report robustness across a researcher-degree-of-freedom multiverse.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "type": "number"
        },
        "cluster_var": {
          "description": "Cluster column for clustered SEs.",
          "type": "string"
        },
        "controls": {
          "description": "The multiverse of control sets: a list of control-column lists, one per specification.",
          "items": {
            "items": {
              "type": "string"
            },
            "type": "array"
          },
          "type": "array"
        },
        "se_types": {
          "description": "Standard-error flavours to sweep (e.g. 'classical', 'hc1', 'cluster').",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "x": {
          "description": "Focal regressor (treatment) column.",
          "type": "string"
        },
        "y": {
          "description": "Outcome column.",
          "type": "string"
        }
      },
      "required": [
        "y",
        "x"
      ],
      "type": "object"
    },
    "name": "spec_curve"
  },
  {
    "description": "Direct + spillover treatment effect estimation under partial interference (within-cluster). Uses the Hudgens-Halloran decomposition with chosen exposure function. Validation: validated evidence tier (known-truth, reference, external-parity, or Monte Carlo artifact). Assumptions: Partial interference: spillover only within cluster, not across; Correct exposure function (fraction / any / count -- sensitivity tested); Overlap: every (treatment x exposure) cell has positive probability. Pre-conditions: data has a cluster column defining the interference boundary; treatment varies within clusters; >= 30 clusters for cluster-robust inference. Failure modes: No within-cluster variation in treatment -> Assignments are cluster-level -- use sp.cluster_matched_pair or cluster-level ATE; Exposure function misspecified -> Compare estimates under exposure_fn in {fraction, any, count}. Alternatives: sp.network_exposure, sp.cluster_matched_pair, sp.peer_effects. Typical minimum N: 500.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "cluster": {
          "description": "Cluster column (interference boundary)",
          "type": "string"
        },
        "covariates": {
          "description": "Covariate matrix, DataFrame, or column names.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "exposure_fn": {
          "default": "fraction",
          "description": "Exposure function",
          "enum": [
            "fraction",
            "any",
            "count"
          ],
          "type": "string"
        },
        "n_bootstrap": {
          "default": 500,
          "description": "Number of bootstrap replications.",
          "type": "integer"
        },
        "treat": {
          "description": "Treatment indicator or first-treatment-period column.",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "cluster",
        "treat",
        "y"
      ],
      "type": "object"
    },
    "name": "spillover"
  },
  {
    "description": "Butts spillover-ring DiD. The usual fix -- a spatial lag of treatment in a TWFE regression -- measures the direct effect against controls the spillover already reached. This sorts untreated units by distance to the nearest treated unit into spillover rings plus CLEAN controls beyond every ring, and estimates the direct effect and each ring's effect against the clean controls only. Validation: certified evidence with scoped limitations. Known limitations: there is no reference implementation, so this carries design-recovery evidence only and no cross-language parity; ring boundaries are the analyst's choice; there is no selector, and a too-wide outer ring silently contaminates the clean controls; covariate adjustment is not implemented. Assumptions: Parallel trends between each group and the clean controls; Spillovers vanish beyond the outermost ring; No anticipation. Pre-conditions: unit positions or a distance matrix; some untreated units beyond the outermost ring. Failure modes: No clean controls -> Every untreated unit is inside a ring. Narrow ring_edges or widen the study area; Ring effects do not decay with distance -> The outermost ring is probably not clean either. Extend r...",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "cohort": {
          "description": "First-treatment period (never_value = never)",
          "type": "string"
        },
        "coords": {
          "description": "Two columns giving each unit's position (Euclidean)",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "distances": {
          "description": "Pre-computed distance matrix, for great-circle or network distances",
          "type": "string"
        },
        "never_value": {
          "default": 0,
          "description": "never_value parameter.",
          "type": "string"
        },
        "ring_edges": {
          "default": [
            0.0,
            1.0
          ],
          "description": "Ring boundaries; untreated units beyond the last edge are the clean controls",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "time": {
          "description": "Time period column.",
          "type": "string"
        },
        "unit": {
          "description": "Unit identifier column.",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "cohort",
        "time",
        "unit",
        "y"
      ],
      "type": "object"
    },
    "name": "spillover_did"
  },
  {
    "description": "Simultaneous quantile regression at multiple quantiles. Validation: certified parity evidence.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "quantiles": {
          "description": "quantiles parameter (Optional[List[float]]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "x": {
          "description": "Primary running variable, regressor, or feature input for this estimator.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "x",
        "y"
      ],
      "type": "object"
    },
    "name": "sqreg"
  },
  {
    "description": "Compute stabilized IPTW weights for time-varying treatments. Validation: certified parity evidence. Assumptions: Sequential exchangeability / no unmeasured confounding at each time point; Positivity: every treatment level is possible given the past; Correct specification of the treatment and/or outcome models. Pre-conditions: Sequentially measured covariates, (time-varying) treatment, and outcome; Models for the treatment process and the outcome (or weights). Failure modes: Stabilized weights have extreme values (positivity near-violation) -> Truncate weights, simplify the treatment model, or use a doubly-robust estimator (TMLE). Alternatives: sp.tmle, sp.g_computation, sp.ipw. Typical minimum N: 300.",
    "input_schema": {
      "properties": {
        "baseline": {
          "description": "Baseline covariates.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "density_sd": {
          "default": "unbiased",
          "description": "Residual standard deviation used in the Gaussian densities of a continuous treatment: ``'unbiased'`` divides the residual sum of squares by ``N - k`` (what ``lm``'s ``sigma`` reports); ``'ml'`` divides by ``N``, the maximum-likelihood estimate, which is the dispersion ``ipw::ipwtm(family = \"gaussian\")`` takes from ``geepack::geeglm``. Ignored for binary treatments.",
          "enum": [
            "unbiased",
            "ml"
          ],
          "type": "string"
        },
        "id": {
          "description": "Unit and time identifiers.",
          "type": "string"
        },
        "time": {
          "description": "Unit and time identifiers.",
          "type": "string"
        },
        "time_varying": {
          "description": "Time-varying confounders (already lagged to pre-treatment).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "treat": {
          "description": "Treatment column (binary 0/1 or continuous).",
          "type": "string"
        },
        "treat_type": {
          "default": "auto",
          "description": "Overrides auto-detection.",
          "enum": [
            "auto",
            "binary",
            "continuous"
          ],
          "type": "string"
        },
        "trim_per_period": {
          "default": 0.0,
          "description": "If > 0, trim each period's density ratio at the symmetric quantile ``[trim_per_period, 1 - trim_per_period]`` *before* taking the cumulative product. A common value is 0.01. Set to 0 to disable per-period trimming (the default).",
          "type": "number"
        }
      },
      "required": [
        "id",
        "time",
        "time_varying",
        "treat"
      ],
      "type": "object"
    },
    "name": "stabilized_weights"
  },
  {
    "description": "Stacked DiD (Cengiz, Dube, Lindner, Zipperer 2019). For each treatment cohort, constructs a sub-experiment with only that cohort + clean (never-treated or not-yet-treated) controls, then TWFE on the stacked panel. Robust to staggered-adoption contamination at the cost of dropping late-treated units in early sub-experiments. Validation: certified parity evidence. Do NOT use when: there is a single adoption cohort -- stacking produces one sub-experiment and is equivalent to sp.did(method='2x2'). Cost: Duplicates rows into one sub-experiment per treated cohort: the stacked dataset is roughly O(n x number of cohorts) before the event window trims it. Narrow window= to bound memory. Assumptions: Parallel trends within each sub-experiment; No anticipation within window; SUTVA. Pre-conditions: staggered adoption with >= 2 cohorts; window horizon available per cohort (else dropped). Failure modes: No clean controls for the latest cohort -> Late cohort's sub-experiment is dropped; check coverage in model_info. Consider sp.callaway_santanna. Alternatives: sp.callaway_santanna, sp.sun_abraham, sp.did_imputation. Typical minimum N: 100.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "cluster": {
          "description": "Cluster identifier column for clustered standard errors.",
          "type": "string"
        },
        "controls": {
          "description": "Control-variable column names.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "first_treat": {
          "description": "first_treat parameter (str).",
          "type": "string"
        },
        "group": {
          "description": "Unit identifier",
          "type": "string"
        },
        "never_treated_only": {
          "default": true,
          "description": "Use only never-treated as controls (drops late-treated)",
          "type": "boolean"
        },
        "time": {
          "description": "Time period column.",
          "type": "string"
        },
        "window": {
          "default": [
            -5,
            5
          ],
          "description": "Event-time (lead, lag) window per sub-experiment",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "first_treat",
        "group",
        "time",
        "y"
      ],
      "type": "object"
    },
    "name": "stacked_did"
  },
  {
    "description": "Callaway-Sant'Anna's estimand with **design-based** inference (Roth & Sant'Anna 2023). Same weights as sp.callaway_santanna -- every not-yet-treated cohort is a control -- but the standard error comes from random adoption timing rather than parallel trends. Use when timing was randomised and you want the familiar CS estimand; use sp.callaway_santanna when it was not. Units already treated in the first period are dropped, since ATT(g,t) is not identified for them. Validation: certified parity evidence. Do NOT use when: adoption timing was not randomised -- use sp.callaway_santanna, whose inference rests on parallel trends instead. Assumptions: treatment timing is randomly assigned; balanced panel. Pre-conditions: balanced panel with at least two cohorts. Alternatives: sp.staggered_rollout, sp.staggered_sa, sp.callaway_santanna. Typical minimum N: 50.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "estimand": {
          "default": "simple",
          "description": "Weighting scheme, as in sp.staggered_rollout",
          "enum": [
            "simple",
            "cohort",
            "calendar",
            "eventstudy"
          ],
          "type": "string"
        },
        "event_time": {
          "default": 0,
          "description": "event_time parameter (float or list).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "fisher": {
          "default": false,
          "description": "fisher parameter (bool).",
          "type": "boolean"
        },
        "g": {
          "description": "First-treatment period; never-treated may be 0, NaN or inf",
          "type": "string"
        },
        "i": {
          "description": "Unit identifier",
          "type": "string"
        },
        "n_fisher": {
          "default": 500,
          "description": "Number of fisher.",
          "type": "integer"
        },
        "random_state": {
          "description": "Random seed or RandomState for reproducible stochastic steps.",
          "type": "integer"
        },
        "se_type": {
          "default": "neyman",
          "description": "Conservative bound, or the adjusted SE R staggered prints",
          "enum": [
            "neyman",
            "adjusted"
          ],
          "type": "string"
        },
        "t": {
          "description": "Time period",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "g",
        "i",
        "t",
        "y"
      ],
      "type": "object"
    },
    "name": "staggered_cs"
  },
  {
    "description": "Efficient DiD for a randomised staggered rollout (Roth & Sant'Anna 2023). Identifies off random adoption *timing*, not parallel trends, so it is the right estimator for policy lotteries, phased launches and wave-randomised RCTs -- and the wrong one for observational rollouts. Uses the cohort's pre-treatment moments as optimal controls; efficient=False gives the plug-in. Validation: certified parity evidence. Do NOT use when: adoption timing was not randomised -- use a parallel-trends estimator such as sp.callaway_santanna instead. Assumptions: treatment timing is randomly assigned (this is the identifying assumption; parallel trends is neither assumed nor sufficient); balanced panel. Pre-conditions: balanced panel with at least two cohorts; single-unit cohorts are dropped with a warning, matching R staggered: their within-cohort covariance is not estimable. Alternatives: sp.callaway_santanna, sp.did_imputation, sp.sun_abraham. Typical minimum N: 50.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "efficient": {
          "default": true,
          "description": "Use the optimal pre-period control weights; False gives the plug-in estimator (R's beta=1)",
          "type": "boolean"
        },
        "estimand": {
          "default": "simple",
          "description": "Weighting: 'simple' (per treated cell), 'cohort' (within-cohort average first), 'calendar' (within-period average first), 'eventstudy' (ATT event_time periods after adoption)",
          "enum": [
            "simple",
            "cohort",
            "calendar",
            "eventstudy"
          ],
          "type": "string"
        },
        "event_time": {
          "default": 0,
          "description": "Only read when estimand='eventstudy'. A list returns one row per event time in .detail with the joint covariance in model_info['vcov']",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "fisher": {
          "default": false,
          "description": "Run a Fisher randomisation test by permuting adoption dates across units; p-value in model_info['fisher_pvalue']",
          "type": "boolean"
        },
        "g": {
          "description": "First-treatment period; never-treated may be 0, NaN or inf",
          "type": "string"
        },
        "i": {
          "description": "Unit identifier",
          "type": "string"
        },
        "n_fisher": {
          "default": 500,
          "description": "Permutation draws for the randomisation test",
          "type": "integer"
        },
        "random_state": {
          "description": "Seed for the permutation draws",
          "type": "integer"
        },
        "se_type": {
          "default": "neyman",
          "description": "Which SE lands in .se: 'neyman' is the conservative bound; 'adjusted' subtracts the variance the randomisation identifies and is what R staggered prints. Both are always in model_info",
          "enum": [
            "neyman",
            "adjusted"
          ],
          "type": "string"
        },
        "t": {
          "description": "Time period",
          "type": "string"
        },
        "use_did_a0": {
          "default": true,
          "description": "Which controls the efficient weights are chosen over. True uses the single DiD contrast at g-1; False uses every pre-period as a separate control (the general form, weakly more efficient). False requires efficient=True",
          "type": "boolean"
        },
        "use_last_treated_only": {
          "default": false,
          "description": "Restrict controls to the last-treated cohort (the Sun-Abraham comparison group) instead of every not-yet-treated cohort",
          "type": "boolean"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "g",
        "i",
        "t",
        "y"
      ],
      "type": "object"
    },
    "name": "staggered_rollout"
  },
  {
    "description": "Sun-Abraham's estimand with **design-based** inference (Roth & Sant'Anna 2023). Identical to sp.staggered_cs except that only the last-treated cohort serves as control, which is what Sun & Abraham's interaction-weighted estimator does. Inference identifies off random adoption timing, not parallel trends. Validation: certified parity evidence. Do NOT use when: adoption timing was not randomised -- use sp.sun_abraham, whose inference rests on parallel trends instead. Assumptions: treatment timing is randomly assigned; balanced panel. Pre-conditions: balanced panel with at least two cohorts. Alternatives: sp.staggered_rollout, sp.staggered_cs, sp.sun_abraham. Typical minimum N: 50.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "estimand": {
          "default": "simple",
          "description": "Weighting scheme, as in sp.staggered_rollout",
          "enum": [
            "simple",
            "cohort",
            "calendar",
            "eventstudy"
          ],
          "type": "string"
        },
        "event_time": {
          "default": 0,
          "description": "event_time parameter (float or list).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "fisher": {
          "default": false,
          "description": "fisher parameter (bool).",
          "type": "boolean"
        },
        "g": {
          "description": "First-treatment period; never-treated may be 0, NaN or inf",
          "type": "string"
        },
        "i": {
          "description": "Unit identifier",
          "type": "string"
        },
        "n_fisher": {
          "default": 500,
          "description": "Number of fisher.",
          "type": "integer"
        },
        "random_state": {
          "description": "Random seed or RandomState for reproducible stochastic steps.",
          "type": "integer"
        },
        "se_type": {
          "default": "neyman",
          "description": "Conservative bound, or the adjusted SE R staggered prints",
          "enum": [
            "neyman",
            "adjusted"
          ],
          "type": "string"
        },
        "t": {
          "description": "Time period",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "g",
        "i",
        "t",
        "y"
      ],
      "type": "object"
    },
    "name": "staggered_sa"
  },
  {
    "description": "Staggered Adoption Synthetic Control (Ben-Michael, Feller & Rothstein). Validation: certified parity evidence. Assumptions: A convex (or regularized) combination of donor units reproduces the treated unit's pre-treatment outcome path; No interference: the treatment does not affect the donor units (SUTVA); No anticipation before the treatment date. Pre-conditions: Panel of one or more treated units plus an untreated donor pool, observed over time; Pre-treatment window long enough to fit donor weights (rule of thumb: more pre-periods than donors used); Outcome observed for every unit in every period. Failure modes: Large pre-treatment RMSPE -- the synthetic unit fails to track the treated unit before treatment -> Add donors / predictors, lengthen the pre-period, or use a bias-corrected estimator (sdid, augsynth); Placebo / permutation inference shows the estimate is not extreme relative to donors -> Report the placebo distribution honestly; the effect may not be distinguishable from noise. Alternatives: sp.sdid, sp.augsynth, sp.gsynth, sp.callaway_santanna. Typical minimum N: 15.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level.",
          "type": "number"
        },
        "fixedeff": {
          "default": false,
          "description": "De-mean outcomes by unit before fitting (multisynth ``fixedeff``; note multisynth's own default is ``TRUE``).",
          "type": "boolean"
        },
        "method": {
          "default": "separate",
          "description": "* ``'separate'`` -- one set of weights per treated unit (multisynth ``time_cohort = FALSE``). * ``'pooled'`` -- one set of weights per adoption cohort, fitted to the cohort's summed outcomes (multisynth ``time_cohort = TRUE``).",
          "enum": [
            "separate",
            "pooled"
          ],
          "type": "string"
        },
        "n_lags": {
          "description": "Number of pre-treatment periods to balance (``None`` = all periods before the last adoption date, as in multisynth).",
          "type": "integer"
        },
        "n_leads": {
          "description": "Number of post-treatment event times in each effect window. ``None`` uses every period through the end of the panel (the longest window), which restricts donors to never-treated units.",
          "type": "integer"
        },
        "nu": {
          "default": 0.0,
          "description": "Pooling parameter. ``0`` fits each unit (cohort) separately; ``'auto'`` uses multisynth's default heuristic.",
          "type": "number"
        },
        "outcome": {
          "description": "Outcome variable name.",
          "type": "string"
        },
        "penalization": {
          "default": 0.0,
          "description": "Ridge penalty ``lambda`` on the weights (multisynth ``lambda``), in the scale of the objective in the module docstring.",
          "type": "number"
        },
        "placebo": {
          "default": true,
          "description": "Compute inference (see ``se_method``). ``False`` returns a point estimate with NaN standard error / p-value.",
          "type": "boolean"
        },
        "se_method": {
          "default": "placebo",
          "description": "* ``'placebo'`` -- in-space placebos: every never-treated unit is assigned each cohort's adoption date and fitted from the other never-treated units; SE is the standard deviation of the placebo ATTs and the p-value is the placebo rank of ``|ATT|``. * ``'jackknife'`` -- leave-one-unit-out jackknife over all units with ``nu`` held at its full-sample value, exactly as ``summary(multisynth, inf_type = \"jackknife\")``; normal-theory p-value and CI.",
          "enum": [
            "placebo",
            "jackknife"
          ],
          "type": "string"
        },
        "time": {
          "description": "Time period column.",
          "type": "string"
        },
        "treatment": {
          "description": "Binary, absorbing treatment indicator (0/1). A unit's adoption period is the first period with ``treatment == 1``.",
          "type": "string"
        },
        "unit": {
          "description": "Unit identifier column.",
          "type": "string"
        }
      },
      "required": [
        "outcome",
        "time",
        "treatment",
        "unit"
      ],
      "type": "object"
    },
    "name": "staggered_synth"
  },
  {
    "description": "Stepwise variable selection for OLS regression. Validation: validated evidence tier (known-truth, reference, external-parity, or Monte Carlo artifact).",
    "input_schema": {
      "properties": {
        "alpha_in": {
          "default": 0.05,
          "description": "p-value threshold for variable entry (when ``criterion=\"pvalue\"``).",
          "type": "number"
        },
        "alpha_out": {
          "default": 0.1,
          "description": "p-value threshold for variable removal (when ``criterion=\"pvalue\"``).",
          "type": "number"
        },
        "criterion": {
          "default": "bic",
          "description": "Optimisation criterion. Default ``\"bic\"``.",
          "enum": [
            "aic",
            "bic",
            "adjr2",
            "pvalue"
          ],
          "type": "string"
        },
        "method": {
          "default": "both",
          "description": "Selection strategy. Default ``\"both\"`` (bidirectional).",
          "enum": [
            "forward",
            "backward",
            "both"
          ],
          "type": "string"
        },
        "verbose": {
          "default": true,
          "description": "Print step-by-step progress.",
          "type": "boolean"
        },
        "x": {
          "description": "Candidate independent variable column names.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Name of the dependent variable column.",
          "type": "string"
        }
      },
      "required": [
        "x",
        "y"
      ],
      "type": "object"
    },
    "name": "stepwise"
  },
  {
    "description": "Test for stochastic dominance of the treated distribution over the Validation: validated evidence tier (known-truth, reference, external-parity, or Monte Carlo artifact).",
    "input_schema": {
      "properties": {
        "order": {
          "default": 1,
          "description": "Order of stochastic dominance. 1 = first-order (CDF dominance). 2 = second-order (integrated CDF dominance).",
          "enum": [
            "1",
            "2"
          ],
          "type": "integer"
        },
        "result": {
          "description": "Output from ``discos()`` or ``qqsynth()``.",
          "type": "string"
        }
      },
      "required": [
        "result"
      ],
      "type": "object"
    },
    "name": "stochastic_dominance"
  },
  {
    "description": "Structural break detection. Validation: certified parity evidence.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level.",
          "type": "number"
        },
        "max_breaks": {
          "default": 5,
          "description": "Maximum number of breaks to test.",
          "type": "integer"
        },
        "method": {
          "default": "bai-perron",
          "description": "Estimator or algorithm variant to use.",
          "type": "string"
        },
        "min_segment": {
          "default": 0.15,
          "description": "Minimum segment length as fraction of sample.",
          "type": "number"
        },
        "pvalue_method": {
          "default": "hansen",
          "description": "p-value of the sup-F statistic ('sup-f' / 'chow'). ``'hansen'`` is Hansen's (1997) response-surface approximation of the Andrews (1993) limit law, the one ``strucchange::sctest(type = \"supF\")`` and Stata's ``estat sbsingle`` report, evaluated at ``lambda = ((n - from) to) / (from (n - to))`` of the candidate range (a single candidate date is an ordinary Chow F test, as in strucchange). ``'simulate'`` is the earlier Monte-Carlo draw from the same limit law (5,000 fixed-seed Brownian-bridge replications on a grid tied to ``n``).",
          "enum": [
            "hansen",
            "simulate"
          ],
          "type": "string"
        },
        "x": {
          "description": "Regressors. If None, uses constant only (mean shift).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Dependent variable.",
          "type": "string"
        }
      },
      "required": [],
      "type": "object"
    },
    "name": "structural_break"
  },
  {
    "description": "Subcluster wild cluster bootstrap for few-treated-clusters. Validation: certified parity evidence.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "cluster": {
          "description": "Primary cluster column (for SE computation).",
          "type": "string"
        },
        "h0": {
          "default": 0.0,
          "description": "Null value.",
          "type": "number"
        },
        "n_boot": {
          "default": 999,
          "description": "Bootstrap replications.",
          "type": "integer"
        },
        "seed": {
          "description": "Random seed for reproducible stochastic steps.",
          "type": "integer"
        },
        "subcluster": {
          "description": "Finer grouping at which sign-flips occur. If ``None``, every observation is its own subcluster (pure Rademacher at obs level).",
          "type": "string"
        },
        "test_var": {
          "description": "Parameter to test; default last element of ``x``.",
          "type": "string"
        },
        "weight_type": {
          "default": "webb",
          "description": "Distribution of sign flips. ``'webb'`` (6-point) recommended when treatment has <= 5 treated clusters.",
          "enum": [
            "rademacher",
            "webb",
            "mammen"
          ],
          "type": "string"
        },
        "x": {
          "description": "Primary running variable, regressor, or feature input for this estimator.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "cluster",
        "x",
        "y"
      ],
      "type": "object"
    },
    "name": "subcluster_wild_bootstrap"
  },
  {
    "description": "Run subgroup heterogeneity analysis with forest plot. Validation: certified parity evidence.",
    "input_schema": {
      "properties": {
        "by": {
          "description": "Mapping of *display name* -> *column name* for grouping.",
          "type": "object"
        },
        "formula": {
          "description": "Regression formula, e.g. ``\"wage ~ education + experience\"``.",
          "type": "string"
        },
        "robust": {
          "default": "hc1",
          "description": "Standard error type for subgroup regressions.",
          "type": "string"
        },
        "x": {
          "description": "Key explanatory variable.",
          "type": "string"
        }
      },
      "required": [
        "by",
        "formula",
        "x"
      ],
      "type": "object"
    },
    "name": "subgroup_analysis"
  },
  {
    "description": "Subgroup decomposition (between / within) of an inequality index. Validation: certified parity evidence.",
    "input_schema": {
      "properties": {
        "alpha": {
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "by": {
          "description": "by parameter (str).",
          "type": "string"
        },
        "eps": {
          "default": 1.0,
          "description": "eps parameter (float).",
          "type": "number"
        },
        "index": {
          "default": "theil_t",
          "description": "index parameter (str).",
          "type": "string"
        },
        "weights": {
          "description": "Observation weights.",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "by",
        "y"
      ],
      "type": "object"
    },
    "name": "subgroup_decompose"
  },
  {
    "description": "Sun-Abraham (2021) interaction-weighted event-study. Fixes the contamination in dynamic event-study TWFE coefficients from other relative-time bins by using cohort-specific interaction weights. Canonical companion to Callaway-Sant'Anna for event studies. Validation: certified parity evidence. Do NOT use when: all units adopt treatment simultaneously -- the cohort x relative-time interactions collapse; use sp.did(method='2x2'); cohorts are very small (a handful of units each) -- interaction-weighted estimates become noisy and the cohort-share weights unstable. Cost: Builds a saturated cohort x relative-time interaction design: columns grow as (cohorts x event-time window), so a wide window on a many-cohort panel produces a large dense design matrix. Trim via event_window=. Assumptions: Parallel trends across cohorts; No anticipation within event_window lead horizon; SUTVA. Pre-conditions: panel with unit x time x outcome; g is the first-treatment period (int), 0 / NaN for never-treated; >= 2 pre-periods per cohort for event-study leads. Failure modes: No never-treated cohort when control_group='nevertreated' -> Pass control_group='notyettreated' or add never-treated units; Pre-tren...",
    "input_schema": {
      "properties": {
        "aggregation": {
          "default": "event_time",
          "description": "Headline summary convention: 'event_time' (equal weight per relative time) or 'fixest_att' (cohort-size weighted, matching fixest agg='att').",
          "enum": [
            "event_time",
            "fixest_att"
          ],
          "type": "string"
        },
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "cluster": {
          "description": "Cluster variable (defaults to i)",
          "type": "string"
        },
        "control_cohort": {
          "description": "Nominate the reference cohort explicitly: a 0/1 indicator column (Stata eventstudyinteract's control_cohort()) or a cohort value from g.",
          "type": "string"
        },
        "control_group": {
          "default": "nevertreated",
          "description": "Control arm",
          "enum": [
            "nevertreated",
            "notyettreated"
          ],
          "type": "string"
        },
        "covariates": {
          "description": "Covariate matrix, DataFrame, or column names.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "event_window": {
          "description": "(lead, lag) window for event-study coefficients",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "g": {
          "description": "First-treatment period (0 = never-treated)",
          "type": "string"
        },
        "i": {
          "description": "Unit identifier",
          "type": "string"
        },
        "pretest": {
          "default": "joint",
          "description": "Report the joint pre-trend Wald test ('joint') or skip it ('none').",
          "enum": [
            "joint",
            "none"
          ],
          "type": "string"
        },
        "pretest_periods": {
          "description": "Restrict the pre-trend test to the k estimated leads closest to treatment.",
          "type": "integer"
        },
        "share_variance": {
          "default": true,
          "description": "Carry the cohort-share estimation term of Sun & Abraham (2021, Prop. 3) in the event-study variance (True: Stata eventstudyinteract convention) or treat the interaction weights as fixed (False: fixest::sunab convention). The two coincide at single-cohort relative times; point estimates are unaffected.",
          "type": "boolean"
        },
        "t": {
          "description": "Time period column",
          "type": "string"
        },
        "weights": {
          "description": "Unit-level sampling/population weights omega. Enters the fixed-effect projection, the least-squares solve, the cluster-robust variance, and the interaction weights (which become shares of omega-mass). Changes the target parameter, not the precision.",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "g",
        "i",
        "t",
        "y"
      ],
      "type": "object"
    },
    "name": "sun_abraham"
  },
  {
    "description": "Fit a Super Learner ensemble. Validation: validated evidence tier (known-truth, reference, external-parity, or Monte Carlo artifact). Assumptions: Unconfoundedness (sequential exchangeability for longitudinal/LTMLE); Positivity / overlap of treatment given history; At least one nuisance (outcome or treatment) is estimated consistently; the targeting step gives double robustness. Pre-conditions: Covariates, treatment, and outcome (for survival/longitudinal variants: time-to-event and time-varying covariates); Enough data to fit a Super Learner / HAL nuisance library. Failure modes: Near-positivity violations create extreme clever-covariate weights -> Truncate weights, restrict the estimand, or report a positivity diagnostic. Alternatives: sp.dml, sp.ipw, sp.g_computation. Typical minimum N: 400.",
    "input_schema": {
      "properties": {
        "X": {
          "description": "Feature matrix or covariate DataFrame.",
          "type": "string"
        },
        "library": {
          "description": "Candidate learners. If None, uses a default library.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "n_folds": {
          "default": 5,
          "description": "Cross-validation folds.",
          "type": "integer"
        },
        "random_state": {
          "default": 42,
          "description": "Random seed or RandomState for reproducible stochastic steps.",
          "type": "integer"
        },
        "task": {
          "default": "regression",
          "description": "'regression' or 'classification'.",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "X",
        "y"
      ],
      "type": "object"
    },
    "name": "super_learner"
  },
  {
    "description": "Seemingly Unrelated Regression (SUR). Validation: certified parity evidence.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "equations": {
          "description": "Mapping from equation name to (dep_var, list_of_regressors).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "maxiter": {
          "default": 100,
          "description": "maxiter parameter (int).",
          "type": "integer"
        },
        "method": {
          "default": "fgls",
          "description": "'ols' (equation-by-equation), 'fgls' (feasible GLS / SUR), 'iterative' (iterated SUR).",
          "type": "string"
        },
        "tol": {
          "default": 1e-08,
          "description": "Numerical convergence tolerance.",
          "type": "number"
        }
      },
      "required": [
        "equations"
      ],
      "type": "object"
    },
    "name": "sureg"
  },
  {
    "description": "Survival forest: conditional survival curves ``S(t | x)`` from Validation: validated evidence tier (known-truth, reference, external-parity, or Monte Carlo artifact).",
    "input_schema": {
      "properties": {
        "clusters": {
          "description": "Cluster-sampled trees; sample weights enter the curve estimates (not the log-rank splits, as in grf).",
          "type": "string"
        },
        "covariates": {
          "description": "Covariates ``X``.",
          "type": "string"
        },
        "equalize_cluster_weights": {
          "default": false,
          "description": "Cluster-sampled trees; sample weights enter the curve estimates (not the log-rank splits, as in grf).",
          "type": "boolean"
        },
        "event": {
          "description": "1 = event observed, 0 = censored.",
          "type": "string"
        },
        "failure_times": {
          "description": "Increasing grid of times for the curves; default all distinct event times.",
          "type": "string"
        },
        "honest": {
          "default": true,
          "description": "Forest options (grf names ``sample.fraction``, ...).",
          "type": "boolean"
        },
        "honesty_fraction": {
          "default": 0.5,
          "description": "Forest options (grf names ``sample.fraction``, ...).",
          "type": "number"
        },
        "honesty_prune_leaves": {
          "default": true,
          "description": "Forest options (grf names ``sample.fraction``, ...).",
          "type": "boolean"
        },
        "max_depth": {
          "description": "max_depth parameter (Optional[int]).",
          "type": "integer"
        },
        "max_samples": {
          "default": 0.5,
          "description": "Forest options (grf names ``sample.fraction``, ...).",
          "type": "number"
        },
        "min_samples_leaf": {
          "default": 15,
          "description": "min_samples_leaf parameter (int).",
          "type": "integer"
        },
        "mtry": {
          "description": "Forest options (grf names ``sample.fraction``, ...).",
          "type": "integer"
        },
        "n_estimators": {
          "default": 1000,
          "description": "Number of estimators.",
          "type": "integer"
        },
        "n_jobs": {
          "default": 1,
          "description": "Number of jobs.",
          "type": "integer"
        },
        "prediction_type": {
          "default": "Kaplan-Meier",
          "description": "prediction_type parameter (str).",
          "enum": [
            "Kaplan-Meier",
            "Nelson-Aalen"
          ],
          "type": "string"
        },
        "random_state": {
          "default": 42,
          "description": "Random seed or RandomState for reproducible stochastic steps.",
          "type": "integer"
        },
        "split_alpha": {
          "default": 0.05,
          "description": "Each child must hold at least ``max(1, split_alpha * n_parent)`` failures (grf ``alpha``).",
          "type": "number"
        },
        "time": {
          "description": "Observed time (event or censoring), non-negative.",
          "type": "string"
        },
        "weights": {
          "description": "Cluster-sampled trees; sample weights enter the curve estimates (not the log-rank splits, as in grf).",
          "type": "string"
        }
      },
      "required": [],
      "type": "object"
    },
    "name": "survival_forest"
  },
  {
    "description": "Nonparametric sensitivity for survival / hazard-ratio outcomes. Validation: validated evidence tier (known-truth, reference, external-parity, or Monte Carlo artifact).",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "baseline_survival_t": {
          "default": 0.5,
          "description": "Baseline S_0(t) used to report Delta survival at time t.",
          "type": "number"
        },
        "gamma_grid": {
          "description": "Gamma (>= 1) values. Defaults to ``np.linspace(1.0, 3.0, 21)``.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "log_hr": {
          "description": "log_hr parameter (float).",
          "type": "number"
        },
        "se_log_hr": {
          "description": "se_log_hr parameter (float).",
          "type": "number"
        }
      },
      "required": [
        "log_hr",
        "se_log_hr"
      ],
      "type": "object"
    },
    "name": "survival_sensitivity"
  },
  {
    "description": "Zhang-Rubin (2003) sharp bounds on the Survivor Average Causal Effect. Validation: certified parity evidence. Assumptions: Sequential exchangeability / no unmeasured confounding at each time point; Positivity: every treatment level is possible given the past; Correct specification of the treatment and/or outcome models. Pre-conditions: Sequentially measured covariates, (time-varying) treatment, and outcome; Models for the treatment process and the outcome (or weights). Failure modes: Stabilized weights have extreme values (positivity near-violation) -> Truncate weights, simplify the treatment model, or use a doubly-robust estimator (TMLE). Alternatives: sp.tmle, sp.g_computation, sp.ipw. Typical minimum N: 300.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "n_boot": {
          "default": 500,
          "description": "Bootstrap replications for the endpoint CIs.",
          "type": "integer"
        },
        "seed": {
          "description": "Random seed for reproducible stochastic steps.",
          "type": "integer"
        },
        "survival": {
          "description": "Binary survival indicator (0/1), assumed monotone in ``treat``.",
          "type": "string"
        },
        "treat": {
          "description": "Binary randomised treatment (0/1).",
          "type": "string"
        },
        "trimming": {
          "default": "quantile",
          "description": "Trimming rule; see :func:`sp.lee_bounds`. Under monotonicity the Zhang-Rubin SACE bounds are Lee (2009) bounds, and the two functions return the same endpoints.",
          "enum": [
            "quantile",
            "exact"
          ],
          "type": "string"
        },
        "y": {
          "description": "Outcome; may be missing (NaN) for non-survivors, as it is under truncation by death, but must be observed wherever ``survival == 1``.",
          "type": "string"
        }
      },
      "required": [
        "survival",
        "treat",
        "y"
      ],
      "type": "object"
    },
    "name": "survivor_average_causal_effect"
  },
  {
    "description": "Parametric survival model (AFT parameterization). Validation: certified parity evidence.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "cluster": {
          "description": "Cluster identifier column for clustered standard errors.",
          "type": "string"
        },
        "dist": {
          "default": "weibull",
          "description": "dist parameter (str).",
          "type": "string"
        },
        "duration": {
          "description": "Follow-up time column (or formula LHS).",
          "type": "string"
        },
        "event": {
          "description": "Event indicator column.",
          "type": "string"
        },
        "formula": {
          "description": "Formula ``'duration ~ x1 + x2'``.",
          "type": "string"
        },
        "robust": {
          "default": "nonrobust",
          "description": "Robust standard-error or covariance estimator option.",
          "type": "string"
        },
        "x": {
          "description": "Covariate columns (or formula RHS).",
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "required": [],
      "type": "object"
    },
    "name": "survreg"
  },
  {
    "description": "Unified synthetic control estimator. method= selects variant: 'classic', 'demeaned', 'detrended', 'unconstrained', 'elastic_net', 'augmented', 'sdid', 'gsynth', 'staggered'. inference= selects: 'placebo', 'conformal', 'bootstrap', 'jackknife'. Validation: certified evidence with scoped limitations. Known limitations: Classical SCM certification is specification-specific: ADH/Synth parity requires passing the same special_predictors recipe; the default outcome-only V=I path is a documented Kaul-style convention; Default native classical SCM can differ from Synth on Basque-style panels by a documented local-optimum convention (the outer V optimisation has multiple near-equivalent minima); use backend='synth' or canonical special_predictors when exact R parity is required. Do NOT use when: many units are treated at once -- classic SCM is built for one (or few) treated units; use sp.gsynth, sp.sdid or sp.callaway_santanna; the pre-treatment window is short (fewer than ~10 periods) -- the donor weights overfit noise and pre-period fit stops being evidence; the treated unit's pre-period outcome lies outside the convex hull of the donors -- no non-negative weighting can match it; check t...",
    "input_schema": {
      "properties": {
        "backend": {
          "default": "native",
          "description": "Optional reference backend for exact R parity: synth for classic SCM",
          "type": "string"
        },
        "inference": {
          "description": "Inference method: placebo/conformal/bootstrap/jackknife",
          "type": "string"
        },
        "method": {
          "default": "classic",
          "description": "SCM variant: classic/demeaned/detrended/unconstrained/elastic_net/augmented/sdid/gsynth/staggered",
          "type": "string"
        },
        "outcome": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        },
        "time": {
          "description": "Time period column.",
          "type": "string"
        },
        "treated_unit": {
          "description": "Treated unit (not needed for staggered)",
          "type": "string"
        },
        "treatment": {
          "description": "Binary treatment column (staggered only)",
          "type": "string"
        },
        "treatment_time": {
          "description": "First treatment period",
          "type": "integer"
        },
        "unit": {
          "description": "Unit identifier column.",
          "type": "string"
        }
      },
      "required": [
        "outcome",
        "time",
        "unit"
      ],
      "type": "object"
    },
    "name": "synth"
  },
  {
    "description": "Run multiple SCM variants and compare them side by side. Cost: Runs every estimator in methods= end to end, so cost is the sum of the individual fits -- and each placebo-enabled member internally re-runs once per donor. Expect it to be the slowest call in a synthetic-control workflow; narrow methods= once you have shortlisted.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals.",
          "type": "number"
        },
        "methods": {
          "description": "SCM variants to compare. If ``None`` (default), all 20 registered methods are attempted, in ascending complexity order: ``classic, penalized, demeaned, detrended, unconstrained, elastic_net, augmented, sdid, gsynth, mc, discos, scpi, penscm, fdid, sparse, cluster, kernel, kernel_ridge, bayesian, bsts``. Pass an explicit subset to reduce runtime.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "outcome": {
          "description": "Outcome variable column name.",
          "type": "string"
        },
        "placebo": {
          "default": true,
          "description": "Whether to run placebo inference for each method.",
          "type": "boolean"
        },
        "time": {
          "description": "Time period column.",
          "type": "string"
        },
        "treated_unit": {
          "description": "Identifier of the treated unit.",
          "type": "string"
        },
        "treatment_time": {
          "description": "First treatment period (inclusive).",
          "type": "string"
        },
        "unit": {
          "description": "Unit identifier column.",
          "type": "string"
        }
      },
      "required": [
        "outcome",
        "time",
        "unit"
      ],
      "type": "object"
    },
    "name": "synth_compare"
  },
  {
    "description": "Donor-pool bootstrap sensitivity for Synthetic Control. Validation: certified parity evidence.",
    "input_schema": {
      "properties": {
        "k": {
          "description": "Donor subset size. Default is ``floor(J * 0.75)`` where *J* is the total number of donors.",
          "type": "integer"
        },
        "n_samples": {
          "default": 100,
          "description": "Number of random donor subsets to draw.",
          "type": "integer"
        },
        "outcome": {
          "description": "Outcome variable.",
          "type": "string"
        },
        "penalization": {
          "default": 0.0,
          "description": "Ridge penalty forwarded to SCM.",
          "type": "number"
        },
        "seed": {
          "description": "Random seed for reproducibility.",
          "type": "integer"
        },
        "time": {
          "description": "Time column.",
          "type": "string"
        },
        "treated_unit": {
          "description": "Identifier of the treated unit.",
          "type": "string"
        },
        "treatment_time": {
          "description": "First treatment period.",
          "type": "string"
        },
        "unit": {
          "description": "Unit identifier column.",
          "type": "string"
        }
      },
      "required": [
        "outcome",
        "time",
        "treated_unit",
        "treatment_time",
        "unit"
      ],
      "type": "object"
    },
    "name": "synth_donor_sensitivity"
  },
  {
    "description": "Leave-one-out donor sensitivity for Synthetic Control. Validation: certified parity evidence.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Accepted for API symmetry with :func:`synth_sensitivity`; it does not affect any returned column.",
          "type": "number"
        },
        "outcome": {
          "description": "Outcome variable.",
          "type": "string"
        },
        "penalization": {
          "default": 0.0,
          "description": "Ridge penalty forwarded to SCM.",
          "type": "number"
        },
        "time": {
          "description": "Time column.",
          "type": "string"
        },
        "treated_unit": {
          "description": "Identifier of the treated unit.",
          "type": "string"
        },
        "treatment_time": {
          "description": "First treatment period.",
          "type": "string"
        },
        "unit": {
          "description": "Unit identifier column.",
          "type": "string"
        }
      },
      "required": [
        "outcome",
        "time",
        "treated_unit",
        "treatment_time",
        "unit"
      ],
      "type": "object"
    },
    "name": "synth_loo"
  },
  {
    "description": "Minimum Detectable Effect for a Synthetic Control design.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for the placebo test.",
          "type": "number"
        },
        "n_simulations": {
          "default": 200,
          "description": "Number of simulations per effect size.",
          "type": "integer"
        },
        "outcome": {
          "description": "Outcome variable name.",
          "type": "string"
        },
        "power_target": {
          "default": 0.8,
          "description": "Desired power level.",
          "type": "number"
        },
        "seed": {
          "description": "Random seed for reproducibility.",
          "type": "integer"
        },
        "time": {
          "description": "Time period column.",
          "type": "string"
        },
        "treated_unit": {
          "description": "Identifier of the treated unit.",
          "type": "string"
        },
        "treatment_time": {
          "description": "First treatment period.",
          "type": "string"
        },
        "unit": {
          "description": "Unit identifier column.",
          "type": "string"
        }
      },
      "required": [
        "outcome",
        "time",
        "treated_unit",
        "treatment_time",
        "unit"
      ],
      "type": "object"
    },
    "name": "synth_mde"
  },
  {
    "description": "Power analysis for Synthetic Control designs. Validation: validated evidence tier (known-truth, reference, external-parity, or Monte Carlo artifact).",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for the placebo test.",
          "type": "number"
        },
        "effect_sizes": {
          "description": "Grid of hypothetical additive effect sizes to evaluate. If ``None``, auto-generates 10 steps from 0 to 3 * pre-treatment SD of the outcome.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "n_simulations": {
          "default": 200,
          "description": "Number of Monte-Carlo simulations per effect size.",
          "type": "integer"
        },
        "outcome": {
          "description": "Outcome variable name.",
          "type": "string"
        },
        "seed": {
          "description": "Random seed for reproducibility.",
          "type": "integer"
        },
        "time": {
          "description": "Time period column.",
          "type": "string"
        },
        "treated_unit": {
          "description": "Identifier of the treated unit.",
          "type": "string"
        },
        "treatment_time": {
          "description": "First treatment period (inclusive).",
          "type": "string"
        },
        "unit": {
          "description": "Unit identifier column.",
          "type": "string"
        }
      },
      "required": [
        "outcome",
        "time",
        "treated_unit",
        "treatment_time",
        "unit"
      ],
      "type": "object"
    },
    "name": "synth_power"
  },
  {
    "description": "Plot the power curve from :func:`synth_power`.",
    "input_schema": {
      "properties": {
        "ax": {
          "description": "Axes to plot on. If ``None``, a new figure is created.",
          "type": "string"
        },
        "figsize": {
          "default": [
            9,
            6
          ],
          "description": "Figure size (width, height) in inches.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "power_result": {
          "description": "Output of :func:`synth_power`. Must contain columns ``effect_size``, ``power``, and ``mde_flag``.",
          "type": "string"
        },
        "title": {
          "description": "Custom plot title. Defaults to ``\"SCM Power Curve -- Minimum Detectable Effect\"``.",
          "type": "string"
        }
      },
      "required": [
        "power_result"
      ],
      "type": "object"
    },
    "name": "synth_power_plot"
  },
  {
    "description": "Quickly recommend the best SCM method for the given data.",
    "input_schema": {
      "properties": {
        "outcome": {
          "description": "Outcome variable column name.",
          "type": "string"
        },
        "time": {
          "description": "Time period column.",
          "type": "string"
        },
        "treated_unit": {
          "description": "Identifier of the treated unit.",
          "type": "string"
        },
        "treatment_time": {
          "description": "First treatment period (inclusive).",
          "type": "string"
        },
        "unit": {
          "description": "Unit identifier column.",
          "type": "string"
        }
      },
      "required": [
        "outcome",
        "time",
        "unit"
      ],
      "type": "object"
    },
    "name": "synth_recommend"
  },
  {
    "description": "Generate a comprehensive Synthetic Control analysis report.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for CIs and hypothesis tests.",
          "type": "number"
        },
        "method": {
          "default": "classic",
          "description": "SCM variant passed to ``synth()``.",
          "type": "string"
        },
        "outcome": {
          "description": "Outcome variable name.",
          "type": "string"
        },
        "output": {
          "default": "text",
          "description": "Output format: ``'text'``, ``'markdown'``, or ``'latex'``.",
          "type": "string"
        },
        "sensitivity": {
          "default": true,
          "description": "Whether to include the sensitivity analysis section.",
          "type": "boolean"
        },
        "time": {
          "description": "Time period column.",
          "type": "string"
        },
        "treated_unit": {
          "description": "Identifier of the treated unit.",
          "type": "string"
        },
        "treatment_time": {
          "description": "First treatment period (inclusive).",
          "type": "string"
        },
        "unit": {
          "description": "Unit identifier column.",
          "type": "string"
        }
      },
      "required": [
        "outcome",
        "time",
        "unit"
      ],
      "type": "object"
    },
    "name": "synth_report"
  },
  {
    "description": "Generate an SCM report and write it directly to a file.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level.",
          "type": "number"
        },
        "filename": {
          "default": "report.md",
          "description": "Output file path.",
          "type": "string"
        },
        "method": {
          "default": "classic",
          "description": "SCM variant passed to ``synth()``.",
          "type": "string"
        },
        "outcome": {
          "description": "Outcome variable name.",
          "type": "string"
        },
        "output": {
          "default": "markdown",
          "description": "Output format: ``'text'``, ``'markdown'``, or ``'latex'``.",
          "type": "string"
        },
        "sensitivity": {
          "default": true,
          "description": "Whether to include the sensitivity analysis section.",
          "type": "boolean"
        },
        "time": {
          "description": "Time period column.",
          "type": "string"
        },
        "treated_unit": {
          "description": "Identifier of the treated unit.",
          "type": "string"
        },
        "treatment_time": {
          "description": "First treatment period (inclusive).",
          "type": "string"
        },
        "unit": {
          "description": "Unit identifier column.",
          "type": "string"
        }
      },
      "required": [
        "outcome",
        "time",
        "unit"
      ],
      "type": "object"
    },
    "name": "synth_report_to_file"
  },
  {
    "description": "Pre-fit-filtered placebo p-values (Abadie et al. 2010). Validation: certified parity evidence.",
    "input_schema": {
      "properties": {
        "metric": {
          "default": "rmspe",
          "description": "Scale on which ``thresholds`` act. ``'rmspe'`` (default) compares pre-period RMSPEs, the convention of Stata ``synth_runner``'s ``pre_limit_mult()``. ``'mspe'`` compares pre-period MSPEs, the convention of Abadie et al. (2010), who drop placebos with a pre-period MSPE above 20 / 5 / 2 times California's, and of R ``SCtools::mspe.test(discard.extreme = TRUE, mspe.limit = ...)``. A threshold ``c`` on the MSPE scale equals ``sqrt(c)`` on the RMSPE scale.",
          "enum": [
            "rmspe",
            "mspe"
          ],
          "type": "string"
        },
        "outcome": {
          "description": "Outcome variable.",
          "type": "string"
        },
        "penalization": {
          "default": 0.0,
          "description": "Ridge penalty.",
          "type": "number"
        },
        "placebo_pool": {
          "default": "include_treated",
          "description": "Donor pool of each placebo fit. ``'include_treated'`` (default) uses every other unit, including the actually-treated one, which is the exact permutation design under the sharp null of no effect and matches ``sp.synth``'s own placebo loop. ``'exclude_treated'`` drops the treated unit from every placebo's donor pool, as R ``SCtools::generate.placebos`` and Stata ``synth_runner`` do.",
          "enum": [
            "include_treated",
            "exclude_treated"
          ],
          "type": "string"
        },
        "thresholds": {
          "description": "Cut-off multiples of the treated unit's pre-treatment fit; a placebo is kept when its fit is ``<= threshold x`` the treated unit's. Default ``[1, 2, 5, 10, 20, np.inf]``.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "time": {
          "description": "Time column.",
          "type": "string"
        },
        "treated_unit": {
          "description": "Identifier of the treated unit.",
          "type": "string"
        },
        "treatment_time": {
          "description": "First treatment period.",
          "type": "string"
        },
        "unit": {
          "description": "Unit identifier column.",
          "type": "string"
        }
      },
      "required": [
        "outcome",
        "time",
        "treated_unit",
        "treatment_time",
        "unit"
      ],
      "type": "object"
    },
    "name": "synth_rmspe_filter"
  },
  {
    "description": "Run all SCM sensitivity diagnostics in a single call.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level.",
          "type": "number"
        },
        "n_donor_samples": {
          "default": 100,
          "description": "Number of random donor subsets for donor sensitivity.",
          "type": "integer"
        },
        "outcome": {
          "description": "Outcome variable.",
          "type": "string"
        },
        "penalization": {
          "default": 0.0,
          "description": "Ridge penalty.",
          "type": "number"
        },
        "seed": {
          "description": "Random seed.",
          "type": "integer"
        },
        "time": {
          "description": "Time column.",
          "type": "string"
        },
        "treated_unit": {
          "description": "Identifier of the treated unit.",
          "type": "string"
        },
        "treatment_time": {
          "description": "First treatment period.",
          "type": "string"
        },
        "unit": {
          "description": "Unit identifier column.",
          "type": "string"
        }
      },
      "required": [
        "outcome",
        "time",
        "treated_unit",
        "treatment_time",
        "unit"
      ],
      "type": "object"
    },
    "name": "synth_sensitivity"
  },
  {
    "description": "Multi-panel sensitivity diagnostic plot.",
    "input_schema": {
      "properties": {
        "figsize": {
          "default": [
            14,
            10
          ],
          "description": "Figure size in inches.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "sensitivity_result": {
          "description": "Output from :func:`synth_sensitivity`.",
          "type": "object"
        },
        "title": {
          "description": "Super-title for the figure.",
          "type": "string"
        }
      },
      "required": [
        "sensitivity_result"
      ],
      "type": "object"
    },
    "name": "synth_sensitivity_plot"
  },
  {
    "description": "Synthetic control on survival curves (not the Han & Shah 2025 SSC estimator). Fits a convex combination of donor Kaplan-Meier curves on the complementary log-log scale to match the treated arm's pre-treatment survival, then reports the post-treatment survival gap with placebo UCBs. Validation: validated evidence tier (known-truth, reference, external-parity, or Monte Carlo artifact). Assumptions: A convex (or regularized) combination of donor units reproduces the treated unit's pre-treatment outcome path; No interference: the treatment does not affect the donor units (SUTVA); No anticipation before the treatment date. Pre-conditions: Panel of one or more treated units plus an untreated donor pool, observed over time; Pre-treatment window long enough to fit donor weights (rule of thumb: more pre-periods than donors used); Outcome observed for every unit in every period. Failure modes: Large pre-treatment RMSPE -- the synthetic unit fails to track the treated unit before treatment -> Add donors / predictors, lengthen the pre-period, or use a bias-corrected estimator (sdid, augsynth); Placebo / permutation inference shows the estimate is not extreme relative to donors -> Report the p...",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "n_placebos": {
          "default": 100,
          "description": "Number of placebos.",
          "type": "integer"
        },
        "seed": {
          "default": 0,
          "description": "Random seed for reproducible stochastic steps.",
          "type": "integer"
        },
        "survival": {
          "description": "Column with survival probability S_i(t)",
          "type": "string"
        },
        "time": {
          "description": "Time period column.",
          "type": "string"
        },
        "treat_time": {
          "description": "treat_time parameter (float).",
          "type": "number"
        },
        "treated": {
          "description": "Boolean column or name of the single treated unit",
          "type": "string"
        },
        "unit": {
          "description": "Unit/panel-id column",
          "type": "string"
        }
      },
      "required": [
        "survival",
        "time",
        "treat_time",
        "treated",
        "unit"
      ],
      "type": "object"
    },
    "name": "synth_survival"
  },
  {
    "description": "Time-placebo (\"backdating\") test for Synthetic Control. Validation: certified parity evidence.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Accepted for API symmetry with :func:`synth_sensitivity`; it does not affect any returned column.",
          "type": "number"
        },
        "n_placebo_times": {
          "description": "Max number of placebo treatment times to try (a subset drawn with a fixed seed). Default is all feasible pre-treatment times: every pre-period from the third on, so each placebo fit keeps >= 2 pre-periods. Real post-treatment periods are discarded before the placebo fits.",
          "type": "integer"
        },
        "outcome": {
          "description": "Outcome variable.",
          "type": "string"
        },
        "penalization": {
          "default": 0.0,
          "description": "Ridge penalty forwarded to SCM.",
          "type": "number"
        },
        "time": {
          "description": "Time column.",
          "type": "string"
        },
        "treated_unit": {
          "description": "Identifier of the treated unit.",
          "type": "string"
        },
        "treatment_time": {
          "description": "Real first treatment period.",
          "type": "string"
        },
        "unit": {
          "description": "Unit identifier column.",
          "type": "string"
        }
      },
      "required": [
        "outcome",
        "time",
        "treated_unit",
        "treatment_time",
        "unit"
      ],
      "type": "object"
    },
    "name": "synth_time_placebo"
  },
  {
    "description": "Multi-sheet Excel workbook for synthetic-control results.",
    "input_schema": {
      "properties": {
        "digits": {
          "default": 6,
          "description": "Rounding for floating-point values.",
          "type": "integer"
        },
        "method_names": {
          "description": "Override sheet / column labels.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "obj": {
          "description": "Object to export.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "path": {
          "description": "Destination ``.xlsx`` file path.",
          "type": "string"
        }
      },
      "required": [
        "obj",
        "path"
      ],
      "type": "object"
    },
    "name": "synth_to_excel"
  },
  {
    "description": "Formatted LaTeX table for synthetic-control results.",
    "input_schema": {
      "properties": {
        "booktabs": {
          "default": true,
          "description": "If True, use ``\\toprule`` / ``\\midrule`` / ``\\bottomrule`` (requires ``\\usepackage{booktabs}``). Falls back to ``\\hline`` if False.",
          "type": "boolean"
        },
        "caption": {
          "description": "Table caption. Defaults to a sensible auto-generated string.",
          "type": "string"
        },
        "digits": {
          "description": "Number of decimal places.",
          "type": "integer"
        },
        "label": {
          "description": "LaTeX label for cross-referencing. Defaults to ``\"tab:synth\"`` (single) or ``\"tab:synth_compare\"`` (multi).",
          "type": "string"
        },
        "method_names": {
          "description": "Override column labels in comparison mode.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "obj": {
          "description": "Object to render. ``SynthComparison`` and lists trigger the side-by-side multi-method layout.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "show_ci": {
          "default": true,
          "description": "Include the confidence-interval row.",
          "type": "boolean"
        },
        "show_weights": {
          "default": false,
          "description": "Append a panel listing the top-N donor weights.",
          "type": "boolean"
        },
        "top_n_weights": {
          "default": 5,
          "description": "How many donors to show per method when ``show_weights=True``.",
          "type": "integer"
        }
      },
      "required": [
        "obj"
      ],
      "type": "object"
    },
    "name": "synth_to_latex"
  },
  {
    "description": "GitHub-flavoured Markdown table for synthetic-control results.",
    "input_schema": {
      "properties": {
        "digits": {
          "description": "digits parameter (Optional[int]).",
          "type": "integer"
        },
        "method_names": {
          "description": "method_names parameter (Optional[Sequence[str]]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "obj": {
          "description": "obj parameter (Union[CausalResult, 'SynthComparison', List[CausalResult]]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "show_ci": {
          "default": true,
          "description": "show_ci parameter (bool).",
          "type": "boolean"
        },
        "show_weights": {
          "default": false,
          "description": "show_weights parameter (bool).",
          "type": "boolean"
        },
        "title": {
          "description": "title parameter (Optional[str]).",
          "type": "string"
        },
        "top_n_weights": {
          "default": 5,
          "description": "top_n_weights parameter (int).",
          "type": "integer"
        }
      },
      "required": [
        "obj"
      ],
      "type": "object"
    },
    "name": "synth_to_markdown"
  },
  {
    "description": "R-style alias: ``synthdid::synthdid_estimate``. Validation: certified parity evidence. Assumptions: Parallel trends in the absence of treatment, after the synthetic/DiD weighting; No anticipation and no interference between units (SUTVA); The control pool's outcome process is stable around the intervention. Pre-conditions: Panel with treated and control units and a clear treatment date; Pre-treatment periods available to assess comparability of trends; panel data in long form (unit x time x outcome). Failure modes: Weighted pre-treatment trends still diverge between treated and synthetic control -> Inspect the unit/time weights and pre-trend fit; consider event-study DiD with honest bounds; Pre-treatment RMSPE > post-treatment effect -> Poor pre-fit -- switch to method='demeaned'/'augmented' or enlarge donor pool; Placebo p-value >= 0.1 despite visible gap -> Use inference='conformal' (valid under weak assumptions) or report ranked placebo statistic. Alternatives: sp.synth, sp.augsynth, sp.callaway_santanna, sp.gardner_did. Typical minimum N: 15.",
    "input_schema": {
      "properties": {
        "time": {
          "description": "Time period column.",
          "type": "string"
        },
        "treat_time": {
          "description": "treat_time parameter.",
          "type": "string"
        },
        "treat_unit": {
          "description": "treat_unit parameter.",
          "type": "string"
        },
        "unit": {
          "description": "Unit identifier column.",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "time",
        "treat_time",
        "treat_unit",
        "unit",
        "y"
      ],
      "type": "object"
    },
    "name": "synthdid_estimate"
  },
  {
    "description": "Run placebo estimates assigning treatment to each control unit.",
    "input_schema": {
      "properties": {
        "method": {
          "default": "sdid",
          "description": "Estimator or algorithm variant to use.",
          "type": "string"
        },
        "time": {
          "description": "Time period column.",
          "type": "string"
        },
        "treat_time": {
          "description": "treat_time parameter.",
          "type": "string"
        },
        "treat_unit": {
          "description": "treat_unit parameter.",
          "type": "string"
        },
        "unit": {
          "description": "Unit identifier column.",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "time",
        "treat_time",
        "treat_unit",
        "unit",
        "y"
      ],
      "type": "object"
    },
    "name": "synthdid_placebo"
  },
  {
    "description": "Plot observed vs synthetic trajectory.",
    "input_schema": {
      "properties": {
        "ax": {
          "description": "ax parameter.",
          "type": "string"
        },
        "ci_alpha": {
          "default": 0.15,
          "description": "ci_alpha parameter (float).",
          "type": "number"
        },
        "figsize": {
          "default": [
            10,
            6
          ],
          "description": "figsize parameter (Tuple[float, float]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "result": {
          "description": "Output of :func:`sdid`.",
          "type": "string"
        },
        "synth_color": {
          "default": "#E74C3C",
          "description": "synth_color parameter (str).",
          "type": "string"
        },
        "title": {
          "description": "title parameter (Optional[str]).",
          "type": "string"
        },
        "treated_color": {
          "default": "#2C3E50",
          "description": "treated_color parameter (str).",
          "type": "string"
        }
      },
      "required": [
        "result"
      ],
      "type": "object"
    },
    "name": "synthdid_plot"
  },
  {
    "description": "Pre-treatment RMSE of treated vs synthetic trajectory.",
    "input_schema": {
      "properties": {
        "ax": {
          "description": "ax parameter.",
          "type": "string"
        },
        "figsize": {
          "default": [
            8,
            5
          ],
          "description": "figsize parameter (Tuple[float, float]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "result": {
          "description": "result parameter (CausalResult).",
          "type": "string"
        }
      },
      "required": [
        "result"
      ],
      "type": "object"
    },
    "name": "synthdid_rmse_plot"
  },
  {
    "description": "Horizontal bar chart of unit weight contributions.",
    "input_schema": {
      "properties": {
        "ax": {
          "description": "ax parameter.",
          "type": "string"
        },
        "figsize": {
          "default": [
            8,
            5
          ],
          "description": "figsize parameter (Tuple[float, float]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "result": {
          "description": "result parameter (CausalResult).",
          "type": "string"
        },
        "top_n": {
          "default": 10,
          "description": "Show the top-N donors by weight.",
          "type": "integer"
        }
      },
      "required": [
        "result"
      ],
      "type": "object"
    },
    "name": "synthdid_units_plot"
  },
  {
    "description": "Unified plot function for all Synthetic Control variants.",
    "input_schema": {
      "properties": {
        "ax": {
          "description": "Pre-existing axes for single-panel plots.",
          "type": "string"
        },
        "figsize": {
          "description": "Figure size. Auto-selected if None.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "labels": {
          "description": "Labels for ``type='compare'``.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "result": {
          "description": "Output of any ``synth()`` variant. Pass a list for ``type='compare'``.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "title": {
          "description": "Override the auto-generated title.",
          "type": "string"
        },
        "top_n": {
          "default": 15,
          "description": "Number of donors to show in weight plots.",
          "type": "integer"
        },
        "type": {
          "default": "trajectory",
          "description": "Plot type: * ``'trajectory'`` -- treated vs synthetic over time. * ``'gap'`` -- effect (gap) over time. * ``'both'`` -- two-panel: trajectory + gap. * ``'weights'`` -- donor weight bar chart. * ``'placebo'`` -- placebo ATT distribution. * ``'placebo_gap'`` -- placebo gap spaghetti plot (Abadie et al. 2010). * ``'rmspe'`` -- post/pre RMSPE ratio histogram (Abadie et al. 2010). * ``'conformal'`` -- period-level effects + conformal CIs. * ``'staggered'`` -- cohort-level ATT comparison. * ``'factors'`` -- latent factor loadings (gsynth only). * ``'compare'`` -- overlay multiple results.",
          "type": "string"
        }
      },
      "required": [
        "result"
      ],
      "type": "object"
    },
    "name": "synthplot"
  },
  {
    "description": "tF adjusted critical value (Lee, McCrary, Moreira & Porter 2022, AER). Validation: certified parity evidence.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "first_stage_F": {
          "description": "first_stage_F parameter (float).",
          "type": "number"
        }
      },
      "required": [
        "first_stage_F"
      ],
      "type": "object"
    },
    "name": "tF_adjustment"
  },
  {
    "description": "Lee-McCrary-Moreira-Porter (2022, AER) tF adjusted critical value. Validation: certified parity evidence.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level. Only ``0.05`` is implemented (the level LMMP tabulate).",
          "type": "number"
        },
        "first_stage_F": {
          "description": "First-stage F statistic of the single excluded instrument, computed with the *same* variance estimator as the t-ratio it adjusts (the robust / cluster Wald F -- equal to the Olea-Pflueger effective F with one instrument). This is what ``ivDiag`` passes.",
          "type": "number"
        }
      },
      "required": [
        "first_stage_F"
      ],
      "type": "object"
    },
    "name": "tF_critical_value"
  },
  {
    "description": "Wald test for linear restrictions on coefficients. Validation: certified parity evidence.",
    "input_schema": {
      "properties": {
        "hypothesis": {
          "description": "Hypothesis specification (Stata ``test`` syntax). Examples: - ``\"x1 = 0\"`` -- beta_x1 = 0 - ``\"x1 = x2\"`` -- beta_x1 = beta_x2 - ``\"x1 = x2 = 0\"`` -- joint: beta_x1 = beta_x2 and beta_x2 = 0 - ``\"x1 x2\"`` -- joint: beta_x1 = 0 and beta_x2 = 0 - ``\"(x1 = 0) (x2 + x3 = 1)\"`` -- grouped restrictions - ``\"x1 - 2*x2 = 0\"`` -- linear restriction; ``_cons`` names the intercept whatever the estimator calls it.",
          "type": "string"
        },
        "result": {
          "description": "Fitted model with ``.params`` and ``.std_errors``; restrictions that involve several coefficients also need its covariance matrix.",
          "type": "string"
        }
      },
      "required": [
        "hypothesis",
        "result"
      ],
      "type": "object"
    },
    "name": "test"
  },
  {
    "description": "Best-linear-predictor calibration test of CATEs [@chernozhukov2025generic]. Validation: certified parity evidence. Assumptions: Unconfoundedness given the covariates; Overlap / positivity; Honesty: separate subsamples are used to choose splits and to estimate effects. Pre-conditions: Covariates, treatment, and outcome with enough data to grow an honest forest. Failure modes: Calibration test rejects -- the forest's heterogeneity is not well calibrated -> Increase the sample / number of trees, or fall back to a doubly-robust learner. Alternatives: sp.dml, sp.auto_cate, sp.tmle. Typical minimum N: 1000.",
    "input_schema": {
      "properties": {
        "T": {
          "description": "The training sample (defaults to the stored arrays).",
          "type": "string"
        },
        "X": {
          "description": "The training sample (defaults to the stored arrays).",
          "type": "string"
        },
        "Y": {
          "description": "The training sample (defaults to the stored arrays).",
          "type": "string"
        },
        "alpha": {
          "default": 0.05,
          "description": "Level for ``ci_low`` / ``ci_high``.",
          "type": "number"
        },
        "covariates": {
          "default": "none",
          "description": "Covariates in the untreated outcome model of the imputation regression (see :func:`average_treatment_effect`).",
          "type": "string"
        },
        "forest": {
          "description": "forest parameter ('CausalForest').",
          "type": "string"
        },
        "method": {
          "default": "auto",
          "description": "Forests with fixed effects only: ``'auto'`` is ``'imputation'`` for ``fe='twoway'`` with a binary treatment and ``'within'`` otherwise. Other forests accept only ``'auto'``.",
          "enum": [
            "auto",
            "imputation",
            "within"
          ],
          "type": "string"
        },
        "vce": {
          "default": "HC3",
          "description": "Heteroskedasticity-robust covariance (``grf``'s ``vcov.type``).",
          "enum": [
            "HC3",
            "HC2",
            "HC1",
            "HC0"
          ],
          "type": "string"
        }
      },
      "required": [
        "forest"
      ],
      "type": "object"
    },
    "name": "test_calibration"
  },
  {
    "description": "Three-Stage Least Squares (3SLS). Validation: certified parity evidence.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "equations": {
          "description": "equations parameter (Dict[str, Tuple[str, List[str], List[str]]]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "instruments": {
          "description": "Full set of instruments (all exogenous variables in the system).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "maxiter": {
          "default": 100,
          "description": "maxiter parameter (int).",
          "type": "integer"
        }
      },
      "required": [
        "equations"
      ],
      "type": "object"
    },
    "name": "three_sls"
  },
  {
    "description": "Targeted Maximum Likelihood Estimation for ATE/ATT with double-robustness. Validation: certified parity evidence. Assumptions: Unconfoundedness: Y(d) perp D | X; Overlap: 0 < P(D=1 | X) < 1 on the estimand support; Consistent estimation of at least one of Q(a, x) = E[Y|A, X] or g(x) = P(A=1|X) (double robustness). Pre-conditions: binary treatment 0/1; covariates comprise the confounding set; n >= 500 for asymptotic efficiency. Failure modes: Extreme propensity scores (ATE IF denominator ~ 0) -> Bound propensity scores away from 0/1 (e.g. 0.025 / 0.975) or trim; Super-learner cross-validated risk not improving over baseline -> Nuisances not learnable; widen the candidate library or use stronger base learners. Alternatives: sp.dml, sp.aipw, sp.metalearner, sp.ltmle. Typical minimum N: 500.",
    "input_schema": {
      "properties": {
        "covariates": {
          "description": "Covariate matrix, DataFrame, or column names.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "estimand": {
          "default": "ATE",
          "description": "Target estimand",
          "type": "string"
        },
        "fold_indices": {
          "description": "Opt-in CV-TMLE: one integer label 0..K-1 (K >= 2) per row of data. Q and g are then Super Learners fitted outside each fold and predicted inside it; the fluctuation is fitted on the pooled out-of-fold predictions and the SE is the EIF at the targeted fits. The default (None) fits both nuisances on the full sample (not cross-fitted). Not combinable with Q / g1W.",
          "type": "string"
        },
        "q_bound": {
          "default": 1e-05,
          "description": "The initial outcome predictions, on the [0, 1] scale the logistic fluctuation works on (continuous outcomes are min-max rescaled), are truncated to ``[q_bound, 1 - q_bound]`` before targeting. ``tmle::tmle`` truncates at ``1 - alpha = 5e-4`` (its default ``alpha = 0.9995``); pass ``q_bound=5e-4`` to reproduce it.",
          "type": "number"
        },
        "treat": {
          "description": "Binary treatment column (0/1)",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "covariates",
        "treat",
        "y"
      ],
      "type": "object"
    },
    "name": "tmle"
  },
  {
    "description": "Tobit model for censored dependent variables. Validation: certified parity evidence. Assumptions: Latent outcome is linear in covariates with normally distributed errors; Censoring threshold is known and exogenous. Pre-conditions: Outcome censoring point and censoring direction are known; Covariates are numeric or properly encoded. Failure modes: MLE fails to converge or sigma is near zero -> Rescale covariates, simplify the model, or compare with censored quantile alternatives. Alternatives: sp.qreg, sp.regress. Typical minimum N: 100.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals",
          "type": "number"
        },
        "ll": {
          "default": 0.0,
          "description": "Lower censoring limit (set -inf for none)",
          "type": "number"
        },
        "ul": {
          "description": "Upper censoring limit (default: none)",
          "type": "number"
        },
        "x": {
          "description": "Regressors",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Censored outcome variable",
          "type": "string"
        }
      },
      "required": [
        "x",
        "y"
      ],
      "type": "object"
    },
    "name": "tobit"
  },
  {
    "description": "Visualise staggered treatment adoption timing.",
    "input_schema": {
      "properties": {
        "ax": {
          "description": "ax parameter.",
          "type": "string"
        },
        "figsize": {
          "default": [
            12,
            7
          ],
          "description": "figsize parameter (Tuple[float, float]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "id": {
          "description": "Unit identifier.",
          "type": "string"
        },
        "never_color": {
          "default": "#BDC3C7",
          "description": "Color for never-treated units.",
          "type": "string"
        },
        "show_cohort_labels": {
          "default": true,
          "description": "Annotate cohort boundaries on the y-axis.",
          "type": "boolean"
        },
        "sort_by": {
          "default": "treat_time",
          "description": "Sort units by: 'treat_time' (earliest first), 'id', or 'random'.",
          "type": "string"
        },
        "time": {
          "description": "Time period variable.",
          "type": "string"
        },
        "title": {
          "description": "title parameter (Optional[str]).",
          "type": "string"
        },
        "treat": {
          "description": "First-treatment-period column (0 = never treated), or binary treatment indicator.",
          "type": "string"
        },
        "treated_color": {
          "default": "#E74C3C",
          "description": "Color for treated unit-periods.",
          "type": "string"
        },
        "untreated_color": {
          "default": "#ECF0F1",
          "description": "Color for untreated unit-periods.",
          "type": "string"
        }
      },
      "required": [
        "id",
        "time",
        "treat"
      ],
      "type": "object"
    },
    "name": "treatment_rollout_plot"
  },
  {
    "description": "Trim sample to optimal overlap region. Assumptions: Unconfoundedness: treatment is as-good-as-random given the measured covariates; Overlap / common support: every unit has a non-degenerate probability of each treatment; The covariate set blocks all back-door paths. Pre-conditions: Pre-treatment covariates measured for treated and control units; A binary (or low-cardinality) treatment indicator; Sufficient covariate overlap between treatment arms. Failure modes: Poor overlap -- extreme propensity scores or few acceptable matches -> Trim or restrict to the common-support region and report the discarded units; Covariate imbalance remains after matching/weighting -> Re-specify the balancing model (CBPS, entropy balancing) and re-check standardized mean differences. Alternatives: sp.propensity_score, sp.cbps, sp.ebalance, sp.dml. Typical minimum N: 200.",
    "input_schema": {
      "properties": {
        "covariates": {
          "description": "Covariates for PS estimation (if *ps* not supplied).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "method": {
          "default": "crump",
          "description": "``'crump'`` uses Crump et al. (2009) optimal rule. ``'sturmer'`` trims at the fixed [0.1, 0.9] interval.",
          "enum": [
            "crump",
            "sturmer"
          ],
          "type": "string"
        },
        "ps": {
          "description": "Pre-estimated propensity scores. If None, estimated via *ps_method*.",
          "type": "string"
        },
        "ps_method": {
          "default": "logit",
          "description": "Method for PS estimation if *ps* is None.",
          "type": "string"
        },
        "treatment": {
          "description": "Binary treatment column.",
          "type": "string"
        }
      },
      "required": [
        "covariates",
        "treatment"
      ],
      "type": "object"
    },
    "name": "trimming"
  },
  {
    "description": "Truncated regression (MLE). Validation: certified parity evidence.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "cluster": {
          "description": "Cluster identifier column for clustered standard errors.",
          "type": "string"
        },
        "ll": {
          "description": "Lower truncation point. None = no lower truncation.",
          "type": "number"
        },
        "maxiter": {
          "default": 200,
          "description": "maxiter parameter (int).",
          "type": "integer"
        },
        "robust": {
          "default": "nonrobust",
          "description": "Robust standard-error or covariance estimator option.",
          "type": "string"
        },
        "tol": {
          "default": 1e-08,
          "description": "Numerical convergence tolerance.",
          "type": "number"
        },
        "ul": {
          "description": "Upper truncation point. None = no upper truncation.",
          "type": "number"
        },
        "x": {
          "description": "Regressors.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Outcome variable.",
          "type": "string"
        }
      },
      "required": [],
      "type": "object"
    },
    "name": "truncreg"
  },
  {
    "description": "TWFE decomposition: Goodman-Bacon (2021) + de Chaisemartin-D'Haultfoeuille weights. Validation: validated evidence tier (known-truth, reference, external-parity, or Monte Carlo artifact). Assumptions: Conditional parallel trends between treated and comparison groups absent treatment; No anticipation of treatment before its onset; Treatment effects may be heterogeneous across cohorts and time (no homogeneity required). Pre-conditions: Panel or repeated cross-section with a unit (or group) identifier and a time identifier; At least one never-treated or not-yet-treated comparison group; Pre-treatment periods to assess parallel trends. Failure modes: Pre-treatment event-study coefficients are jointly non-zero (pre-trend violation) -> Use honest DiD bounds to quantify robustness to trend violations, or condition on covariates; Two-way fixed-effects estimate is contaminated by 'forbidden' comparisons / negative weights -> Use a heterogeneity-robust estimator (Callaway-Sant'Anna, Borusyak et al., Gardner two-stage). Alternatives: sp.callaway_santanna, sp.did, sp.honest_did. Typical minimum N: 100.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level.",
          "type": "number"
        },
        "first_treat": {
          "description": "Treatment timing column (NaN or 0 for never-treated).",
          "type": "string"
        },
        "group": {
          "description": "Unit identifier.",
          "type": "string"
        },
        "time": {
          "description": "Time period variable.",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable.",
          "type": "string"
        }
      },
      "required": [
        "first_treat",
        "group",
        "time",
        "y"
      ],
      "type": "object"
    },
    "name": "twfe_decomposition"
  },
  {
    "description": "Compute two-way cluster-robust standard errors. Validation: certified parity evidence.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals.",
          "type": "number"
        },
        "cluster1": {
          "description": "Column name for the first clustering dimension.",
          "type": "string"
        },
        "cluster2": {
          "description": "Column name for the second clustering dimension.",
          "type": "string"
        },
        "result": {
          "description": "Fitted OLS result. Must have ``data_info`` containing ``'X'`` (design matrix), ``'y'`` (response), and ``'residuals'``.",
          "type": "string"
        }
      },
      "required": [
        "cluster1",
        "cluster2",
        "result"
      ],
      "type": "object"
    },
    "name": "twoway_cluster"
  },
  {
    "description": "Run every applicable sensitivity analysis in one shot: E-value, Oster delta (when R^2 inputs given), Rosenbaum Gamma (when matched_pairs outcomes exposed), Sensemakr (when raw data supplied via data/y/treat/controls), and a breakdown-frontier bias estimate. Also available as result.sensitivity(). Validation: certified parity evidence. Assumptions: Dashboard that dispatches every applicable sensitivity method (E-value, Cinelli-Hazlett RV, Oster's delta) to one fitted result; Oster's delta requires the short- and long-regression R-squared (r2_treated, r2_controlled) plus beta_uncontrolled; rho_max is Oster's R_max bound (default 1.0); The Sensemakr component needs the raw estimation data via data/y/treat/controls; result objects do not carry it, so it is skipped (with a note) when those are omitted. Pre-conditions: A result with point estimate, SE, and CI attributes; r2_treated and r2_controlled for the Oster's-delta component. Failure modes: Result lacks estimate/se/ci, so no sensitivity component can be computed -> Pass a fitted result exposing estimate, se, and ci rather than a raw scalar or DataFrame; Oster's delta omitted because r2_treated / r2_controlled were not provided ->...",
    "input_schema": {
      "properties": {
        "beta_uncontrolled": {
          "description": "beta_uncontrolled parameter (float).",
          "type": "number"
        },
        "controls": {
          "description": "Control-variable column names.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "include_oster": {
          "default": true,
          "description": "Whether to include oster.",
          "type": "boolean"
        },
        "include_rosenbaum": {
          "default": true,
          "description": "Whether to include rosenbaum.",
          "type": "boolean"
        },
        "include_sensemakr": {
          "default": true,
          "description": "Whether to include sensemakr.",
          "type": "boolean"
        },
        "r2_controlled": {
          "description": "r2_controlled parameter (float).",
          "type": "number"
        },
        "r2_treated": {
          "description": "r2_treated parameter (float).",
          "type": "number"
        },
        "result": {
          "description": "result parameter (CausalResult | EconometricResults).",
          "type": "string"
        },
        "rho_max": {
          "description": "Oster R_max; None -> min(1, 1.3 R^2_long), the sp.oster_delta default.",
          "type": "number"
        },
        "treat": {
          "description": "Treatment indicator or first-treatment-period column.",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "result"
      ],
      "type": "object"
    },
    "name": "unified_sensitivity"
  },
  {
    "description": "Sup-t simultaneous confidence band for an event study from any DiD estimator: critical value = 1-alpha quantile of max|Z| with Z ~ N(0, R), R the correlation of the covered coefficients. Falls back to the conservative Sidak value when the estimator exposes no joint covariance. Alternatives: sp.aggte, sp.event_study_vcov.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "One minus simultaneous coverage",
          "type": "number"
        },
        "n_draws": {
          "default": 100000,
          "description": "Gaussian draws for the critical value",
          "type": "integer"
        },
        "result": {
          "description": "Fitted event-study estimator",
          "type": "string"
        },
        "seed": {
          "default": 0,
          "description": "Seed for the draws",
          "type": "integer"
        },
        "which": {
          "default": "all",
          "description": "Event times covered jointly",
          "enum": [
            "all",
            "post",
            "pre"
          ],
          "type": "string"
        },
        "window": {
          "description": "Restrict covered event times to lo <= k <= hi",
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "required": [
        "result"
      ],
      "type": "object"
    },
    "name": "uniform_bands"
  },
  {
    "description": "Estimate a Vector Autoregression (VAR) model. Validation: certified parity evidence. Assumptions: All system variables are jointly covariance-stationary (no unit roots / cointegration left unmodeled); Lag order p is adequate so residuals are white noise; under-fitting biases IRFs and Granger tests; Reduced-form errors are serially uncorrelated; structural IRF identification relies on the chosen ordering/recursive scheme. Pre-conditions: Multivariate (>=2 column) time-ordered DataFrame of comparable-frequency series; Sample length large relative to k*p+trend parameters to estimate each equation. Failure modes: Coefficient covariance is singular or IRFs diverge because k^2*p parameters exceed available observations -> Reduce lags, drop variables, or extend the sample so n is much larger than k*p; Explosive IRFs / non-decaying responses from a non-stationary (unit-root or trending) system -> Difference or detrend the series first, or use trend='ct', and confirm stationarity before fitting. Alternatives: sp.local_projections, sp.arima, sp.iv. Typical minimum N: 100.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level.",
          "type": "number"
        },
        "lags": {
          "default": 1,
          "description": "Number of lags.",
          "type": "integer"
        },
        "se_df": {
          "default": "stata",
          "description": "Residual-variance denominator for coefficient standard errors. ``'stata'``/``'ml'`` uses ``T`` and matches Stata ``var`` default conditional-MLE standard errors. ``'r'``/``'unbiased'`` uses ``T - k_params`` and matches the equation-by-equation ``lm()`` standard errors returned inside R ``vars::VAR()``.",
          "enum": [
            "stata",
            "ml",
            "r",
            "unbiased"
          ],
          "type": "string"
        },
        "trend": {
          "default": "c",
          "description": "trend parameter (str).",
          "type": "string"
        },
        "variables": {
          "description": "Variable names. If None, uses all numeric columns.",
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "required": [],
      "type": "object"
    },
    "name": "var"
  },
  {
    "description": "Split-frequency variable importance of any GRF-engine forest (grf::variable_importance): depth-weighted shares of splits per covariate, summing to one. Describes how the forest uses covariates; it is not a test of heterogeneity -- use sp.calibration_test or sp.best_linear_projection for that. Matches grf to 1e-15 given the same split counts. Validation: validated evidence tier (known-truth, reference, external-parity, or Monte Carlo artifact). Pre-conditions: forest fitted by a StatsPAI GRF-family function. Failure modes: MethodIncompatibility: unsupported object -> Pass a GRF-family forest. Alternatives: sp.best_linear_projection, sp.calibration_test.",
    "input_schema": {
      "properties": {
        "decay_exponent": {
          "default": 2.0,
          "description": "How quickly deeper splits lose weight.",
          "type": "number"
        },
        "forest": {
          "description": "Any of ``sp.causal_forest`` (GRF engine), ``sp.iv_forest``, ``sp.multi_arm_forest``, ``sp.lm_forest``, ``sp.regression_forest``, ``sp.multi_regression_forest``, ``sp.probability_forest``, ``sp.quantile_forest``, ``sp.survival_forest``, ``sp",
          "type": "string"
        },
        "max_depth": {
          "default": 4,
          "description": "Deepest level counted.",
          "type": "integer"
        }
      },
      "required": [
        "forest"
      ],
      "type": "object"
    },
    "name": "variable_importance"
  },
  {
    "description": "Varying-coefficient dose-response estimator. Assumptions: Unconfoundedness given covariates X (no hidden confounding of the continuous treatment); Positivity over the dose: every dose has support across X; The dose-response curve is smooth (varying-coefficient prior). Pre-conditions: data with a continuous treatment (dose), outcome and covariates; torch is installed (neural extra) -- imported lazily. Failure modes: Sparse support at extreme doses yields an unreliable dose-response curve there -> Restrict the reported dose range to the supported region or compare against scigan. Alternatives: sp.scigan, sp.dose_response. Typical minimum N: 500.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "covariates": {
          "description": "Covariate matrix, DataFrame, or column names.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "n_basis": {
          "default": 6,
          "description": "Number of B-spline basis functions for the t-axis.",
          "type": "integer"
        },
        "n_bootstrap": {
          "default": 200,
          "description": "Number of bootstrap replications.",
          "type": "integer"
        },
        "random_state": {
          "default": 42,
          "description": "Random seed or RandomState for reproducible stochastic steps.",
          "type": "integer"
        },
        "ridge": {
          "default": 0.01,
          "description": "Tikhonov regularisation on the coefficient matrix.",
          "type": "number"
        },
        "spline_degree": {
          "default": 3,
          "description": "spline_degree parameter (int).",
          "type": "integer"
        },
        "t_grid": {
          "description": "Treatment values at which to evaluate the dose-response curve. Defaults to 40 equally-spaced points between the observed min/max.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "treatment": {
          "description": "Continuous treatment / dose column.",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "covariates",
        "treatment",
        "y"
      ],
      "type": "object"
    },
    "name": "vcnet"
  },
  {
    "description": "Empirically verify a single recommendation.",
    "input_schema": {
      "properties": {
        "B": {
          "default": 50,
          "description": "Number of bootstrap replications (auto-reduced if over budget).",
          "type": "integer"
        },
        "K_subsample": {
          "default": 5,
          "description": "Number of 50% subsample splits.",
          "type": "integer"
        },
        "budget_s": {
          "default": 30.0,
          "description": "Wall-clock budget per recommendation (seconds).",
          "type": "number"
        },
        "n_placebo": {
          "default": 5,
          "description": "Number of permutation placebo runs.",
          "type": "integer"
        },
        "rec": {
          "description": "A single entry from ``RecommendationResult.recommendations``.",
          "type": "object"
        },
        "seed": {
          "default": 42,
          "description": "RNG seed for reproducibility.",
          "type": "integer"
        }
      },
      "required": [
        "rec"
      ],
      "type": "object"
    },
    "name": "verify"
  },
  {
    "description": "Run verify against built-in DGPs with known true effects.",
    "input_schema": {
      "properties": {
        "n_reps": {
          "default": 3,
          "description": "Number of independent DGP draws per scenario. Each uses a different seed to average out Monte Carlo noise.",
          "type": "integer"
        },
        "scenarios": {
          "description": "Subset of {'rct', 'did', 'staggered_did', 'rd', 'iv', 'observational'}. Defaults to all six.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "seed": {
          "default": 0,
          "description": "Base seed. Per-rep seeds are ``seed + offset``.",
          "type": "integer"
        },
        "verbose": {
          "default": true,
          "description": "Print per-scenario progress.",
          "type": "boolean"
        },
        "verify_B": {
          "default": 30,
          "description": "Bootstrap replications per verification run.",
          "type": "integer"
        },
        "verify_budget_s": {
          "default": 15.0,
          "description": "Wall-clock budget per verification run.",
          "type": "number"
        }
      },
      "required": [],
      "type": "object"
    },
    "name": "verify_benchmark"
  },
  {
    "description": "Empirically verify a single recommendation.",
    "input_schema": {
      "properties": {
        "B": {
          "default": 50,
          "description": "Number of bootstrap replications (auto-reduced if over budget).",
          "type": "integer"
        },
        "K_subsample": {
          "default": 5,
          "description": "Number of 50% subsample splits.",
          "type": "integer"
        },
        "budget_s": {
          "default": 30.0,
          "description": "Wall-clock budget per recommendation (seconds).",
          "type": "number"
        },
        "n_placebo": {
          "default": 5,
          "description": "Number of permutation placebo runs.",
          "type": "integer"
        },
        "rec": {
          "description": "A single entry from ``RecommendationResult.recommendations``.",
          "type": "object"
        },
        "seed": {
          "default": 42,
          "description": "RNG seed for reproducibility.",
          "type": "integer"
        }
      },
      "required": [
        "rec"
      ],
      "type": "object"
    },
    "name": "verify_recommendation"
  },
  {
    "description": "Variance Inflation Factors for multicollinearity detection. Validation: certified parity evidence.",
    "input_schema": {
      "properties": {
        "x": {
          "description": "Independent variables.",
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "required": [
        "x"
      ],
      "type": "object"
    },
    "name": "vif"
  },
  {
    "description": "Stata-style unified weak-instrument-robust diagnostic panel. Validation: certified parity evidence.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for the robust confidence sets.",
          "type": "number"
        },
        "clr_method": {
          "default": "exact",
          "description": "How the CLR conditional null distribution is evaluated. ``'exact'`` integrates it numerically (the closed form R ``ivmodel::CLR`` and Stata ``weakiv`` use), making the CLR p-value and set deterministic. ``'simulate'`` is the pre-1.29 Monte-Carlo version.",
          "enum": [
            "exact",
            "simulate"
          ],
          "type": "string"
        },
        "clr_simulations": {
          "default": 20000,
          "description": "Monte-Carlo draws for the CLR null distribution; used only with ``clr_method='simulate'``.",
          "type": "integer"
        },
        "endog": {
          "description": "Outcome and single endogenous regressor (column names in ``data``).",
          "type": "string"
        },
        "exog": {
          "description": "Included exogenous controls. An intercept is always added.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "grid_size": {
          "default": 401,
          "description": "Grid resolution used by AR/CLR/K confidence-set inversion.",
          "type": "integer"
        },
        "h0": {
          "default": 0.0,
          "description": "Null value for the endogenous coefficient. All under-H0 tests (AR, CLR, K) are evaluated at ``beta = h0``.",
          "type": "number"
        },
        "include_clr": {
          "default": true,
          "description": "Also run the CLR test and invert it for a CLR confidence set.",
          "type": "boolean"
        },
        "include_k": {
          "default": true,
          "description": "Also run the Kleibergen K score test and K confidence set.",
          "type": "boolean"
        },
        "instruments": {
          "description": "Excluded instruments.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "random_state": {
          "description": "Random seed or RandomState for reproducible stochastic steps.",
          "type": "integer"
        },
        "vcov": {
          "default": "HC1",
          "description": "Used by the Olea-Pflueger effective F.",
          "enum": [
            "HC0",
            "HC1",
            "classic"
          ],
          "type": "string"
        },
        "y": {
          "description": "Outcome and single endogenous regressor (column names in ``data``).",
          "type": "string"
        }
      },
      "required": [
        "endog",
        "instruments",
        "y"
      ],
      "type": "object"
    },
    "name": "weakrobust"
  },
  {
    "description": "Wild cluster bootstrap t-test for a single coefficient. Validation: certified parity evidence.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence interval.",
          "type": "number"
        },
        "cluster": {
          "description": "Name of the cluster variable.",
          "type": "string"
        },
        "n_boot": {
          "default": 999,
          "description": "Number of bootstrap replications (use odd number).",
          "type": "integer"
        },
        "result": {
          "description": "A fitted regression result from ``sp.regress()``.",
          "type": "string"
        },
        "seed": {
          "description": "Random seed for reproducibility.",
          "type": "integer"
        },
        "variable": {
          "description": "Name of the coefficient to test (H0: beta = 0).",
          "type": "string"
        },
        "weight_type": {
          "default": "rademacher",
          "description": "Bootstrap weight distribution: - ``'rademacher'``: +/-1 with equal probability. - ``'webb'``: Webb (2014) 6-point distribution for G < 12. - ``'mammen'``: Mammen (1993) 2-point distribution.",
          "type": "string"
        }
      },
      "required": [
        "cluster",
        "result",
        "variable"
      ],
      "type": "object"
    },
    "name": "wild_cluster_boot"
  },
  {
    "description": "Cameron-Gelbach-Miller (2008) wild cluster bootstrap -- the canonical fix for cluster-robust inference with few clusters (G < 30). Re-samples cluster-level Rademacher weights to construct a percentile-t reference distribution that has correct size when the standard cluster-robust z-test rejects too often. Validation: certified parity evidence. Do NOT use when: the number of clusters is large (say > 50) -- ordinary CRV1 standard errors are already reliable and much cheaper. Cost: Runtime is roughly n_boot x (one restricted refit). This is the intended trade for few-cluster validity -- do not raise n_boot inside an outer search loop. Assumptions: Errors are exchangeable within clusters (Rademacher weights are robust to most departures); Number of clusters G >= 5 for finite-sample validity. Pre-conditions: long-format dataset with a cluster identifier present; treatment / test variable varies within at least some clusters; test_var (or first column of x) is the coefficient under test. Failure modes: Multi-way clustering requested -> Multi-way wild cluster bootstrap is not yet supported; see sp.subcluster_wild_bootstrap or use cr2_se for two-way; G < 5 clusters -> Wild cluster bootstr...",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "cluster": {
          "description": "Cluster identifier",
          "type": "string"
        },
        "h0": {
          "default": 0.0,
          "description": "Null value of the coefficient",
          "type": "number"
        },
        "n_boot": {
          "default": 999,
          "description": "Number of bootstrap replications.",
          "type": "integer"
        },
        "seed": {
          "description": "Random seed for reproducible stochastic steps.",
          "type": "integer"
        },
        "test_var": {
          "description": "Variable being tested; defaults to first in x",
          "type": "string"
        },
        "weight_type": {
          "default": "rademacher",
          "description": "Bootstrap weight distribution",
          "enum": [
            "rademacher",
            "mammen",
            "webb",
            "normal"
          ],
          "type": "string"
        },
        "x": {
          "description": "Right-hand-side variables",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Outcome variable",
          "type": "string"
        }
      },
      "required": [
        "cluster",
        "x",
        "y"
      ],
      "type": "object"
    },
    "name": "wild_cluster_bootstrap"
  },
  {
    "description": "Confidence interval via bootstrap p-value inversion. Validation: certified parity evidence.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "cluster": {
          "description": "Cluster identifier column for clustered standard errors.",
          "type": "string"
        },
        "grid_size": {
          "default": 41,
          "description": "Number of null-value grid points to evaluate (odd preferred).",
          "type": "integer"
        },
        "grid_span": {
          "default": 6.0,
          "description": "Half-width of the search grid in units of cluster-robust SE.",
          "type": "number"
        },
        "n_boot": {
          "default": 999,
          "description": "Number of bootstrap replications.",
          "type": "integer"
        },
        "seed": {
          "description": "Random seed for reproducible stochastic steps.",
          "type": "integer"
        },
        "test_var": {
          "description": "test_var parameter (Optional[str]).",
          "type": "string"
        },
        "weight_type": {
          "default": "webb",
          "description": "weight_type parameter (str).",
          "type": "string"
        },
        "x": {
          "description": "Primary running variable, regressor, or feature input for this estimator.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "cluster",
        "x",
        "y"
      ],
      "type": "object"
    },
    "name": "wild_cluster_ci_inv"
  },
  {
    "description": "Wooldridge (2021) extended TWFE (ETWFE). Saturated TWFE regression with cohort x post interactions; recovers cohort-specific ATTs. Numerically equivalent to CS / SA / BJS under the saturated specification. Validation: certified parity evidence. Do NOT use when: treatment timing is not staggered -- the extended TWFE saturation buys nothing over sp.did(method='2x2'). Cost: Like sp.etwfe: regressor count grows as O(cohorts x periods) from the saturated interactions, so the design matrix -- not the sample size -- is the binding cost. Assumptions: Parallel trends per cohort; No anticipation; SUTVA. Pre-conditions: panel with unit x time x outcome; first_treat cohort column (first period treated, 0 = never). Failure modes: Singleton cohorts with one unit -> Aggregate small cohorts or drop them. Alternatives: sp.callaway_santanna, sp.sun_abraham, sp.did_imputation, sp.etwfe. Typical minimum N: 50.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "cluster": {
          "description": "Cluster identifier column for clustered standard errors.",
          "type": "string"
        },
        "controls": {
          "description": "Control-variable column names.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "first_treat": {
          "description": "First-treatment period; 0 = never-treated",
          "type": "string"
        },
        "group": {
          "description": "Unit identifier",
          "type": "string"
        },
        "time": {
          "description": "Time period column.",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "first_treat",
        "group",
        "time",
        "y"
      ],
      "type": "object"
    },
    "name": "wooldridge_did"
  },
  {
    "description": "X-Learner CATE -- article alias for :func:`metalearner(learner='x')`. Validation: validated evidence tier (known-truth, reference, external-parity, or Monte Carlo artifact).",
    "input_schema": {
      "properties": {
        "X": {
          "description": "Feature matrix or covariate DataFrame.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "d": {
          "description": "d parameter (str).",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "X",
        "d",
        "y"
      ],
      "type": "object"
    },
    "name": "xlearner"
  },
  {
    "description": "Arellano-Bond / Blundell-Bond GMM for dynamic panels (standalone). Validation: certified parity evidence. Assumptions: No second-order serial correlation in differenced errors; Internal instruments are valid and not too numerous. Pre-conditions: Panel data include unit, time, outcome, and lagged dependent variable structure; Number of time periods is moderate relative to units. Failure modes: Instrument proliferation or AR(2) test rejects -> Collapse instruments, reduce lag depth, or compare with fixed-effects estimates. Alternatives: sp.panel, sp.feols. Typical minimum N: 100.",
    "input_schema": {
      "properties": {
        "ah_instrument": {
          "default": "levels",
          "description": "Anderson-Hsiao instrument for method='ah'",
          "enum": [
            "levels",
            "differences"
          ],
          "type": "string"
        },
        "cluster": {
          "description": "Cluster SEs on a coarser unit than the panel id (must be constant within unit)",
          "type": "string"
        },
        "collapse": {
          "default": false,
          "description": "Collapse instruments (Roodman 2009) to curb proliferation",
          "type": "boolean"
        },
        "endogenous": {
          "description": "Endogenous regressors (own lags 2+ as instruments)",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "h": {
          "default": 3,
          "description": "xtabond2 h(): one-step error covariance; 3 is xtabond2's default, 2 zeroes the system cross quadrants (Stata xtdpdsys), 1 the identity.",
          "type": "integer"
        },
        "id": {
          "default": "id",
          "description": "Unit identifier",
          "type": "string"
        },
        "iv_equation": {
          "description": "System GMM: equation(s) the exogenous regressors instrument; None means 'both' (xtabond2), 'diff' is Stata xtdpdsys.",
          "enum": [
            "both",
            "diff",
            "level"
          ],
          "type": "string"
        },
        "lags": {
          "default": 1,
          "description": "lags parameter (int).",
          "type": "integer"
        },
        "method": {
          "default": "difference",
          "description": "difference (Arellano-Bond), system (Blundell-Bond) or ah (Anderson-Hsiao IV)",
          "enum": [
            "difference",
            "system",
            "ah"
          ],
          "type": "string"
        },
        "orthogonal": {
          "default": false,
          "description": "Forward orthogonal deviations instead of first differences",
          "type": "boolean"
        },
        "predetermined": {
          "description": "Predetermined regressors (own lags 1+ as instruments)",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "steps": {
          "description": "Number of GMM steps, or 'iterated' / 'cue'",
          "type": "string"
        },
        "time": {
          "default": "time",
          "description": "Time column",
          "type": "string"
        },
        "time_dummies": {
          "default": false,
          "description": "Add period dummies as regressors and instruments",
          "type": "boolean"
        },
        "twostep": {
          "default": false,
          "description": "twostep parameter (bool).",
          "type": "boolean"
        },
        "x": {
          "description": "Exogenous regressors",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Dependent variable",
          "type": "string"
        }
      },
      "required": [
        "y"
      ],
      "type": "object"
    },
    "name": "xtabond"
  },
  {
    "description": "Blundell-Bond system GMM for dynamic panels (alias for xtabond with method='system'). Validation: certified parity evidence.",
    "input_schema": {
      "properties": {
        "collapse": {
          "default": false,
          "description": "Collapse instruments (Roodman 2009) to curb proliferation",
          "type": "boolean"
        },
        "h": {
          "default": 2,
          "description": "xtabond2 h(): one-step error covariance; 2 is Stata xtdpdsys, 3 xtabond2's default, 1 the identity.",
          "type": "integer"
        },
        "id": {
          "default": "id",
          "description": "Unit identifier",
          "type": "string"
        },
        "iv_equation": {
          "default": "diff",
          "description": "Equation(s) the exogenous regressors instrument; 'diff' is Stata xtdpdsys, 'both' is xtabond2's iv() default.",
          "enum": [
            "diff",
            "level",
            "both"
          ],
          "type": "string"
        },
        "lags": {
          "default": 1,
          "description": "lags parameter (int).",
          "type": "integer"
        },
        "time": {
          "default": "time",
          "description": "Time column",
          "type": "string"
        },
        "twostep": {
          "default": false,
          "description": "twostep parameter (bool).",
          "type": "boolean"
        },
        "x": {
          "description": "Exogenous regressors",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Dependent variable",
          "type": "string"
        }
      },
      "required": [
        "y"
      ],
      "type": "object"
    },
    "name": "xtdpdsys"
  },
  {
    "description": "Bias-corrected LSDV (LSDVC) estimator for dynamic panels. Validation: certified parity evidence.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level.",
          "type": "number"
        },
        "bias_order": {
          "default": 2,
          "description": "How many terms of the Bun-Kiviet expansion to subtract: ``O(1/T)``, ``O(1/NT)``, or ``O(1/NT^2)``. Bun & Kiviet (2003) find diminishing returns beyond the second.",
          "enum": [
            "1",
            "2",
            "3"
          ],
          "type": "integer"
        },
        "bootstrap": {
          "default": 0,
          "description": "Number of parametric-bootstrap replications for the standard errors. **The reported analytic standard errors are the LSDV ones and do not account for the bias correction**, exactly as in Stata's ``xtlsdvc``; set this to get honest ones. 0 skips the bootstrap and warns.",
          "type": "integer"
        },
        "id": {
          "default": "id",
          "description": "Unit and period identifiers.",
          "type": "string"
        },
        "initial": {
          "default": "ab",
          "description": "Consistent estimator used to evaluate the bias expression: Arellano-Bond, Anderson-Hsiao, or Blundell-Bond system GMM. The correction is only as good as this input, and the three can disagree materially on a persistent series -- ``'bb'`` is the safer choice there, for the same reason system GMM is.",
          "enum": [
            "ab",
            "ah",
            "bb"
          ],
          "type": "string"
        },
        "seed": {
          "description": "Seed for the bootstrap.",
          "type": "integer"
        },
        "time": {
          "default": "time",
          "description": "Unit and period identifiers.",
          "type": "string"
        },
        "x": {
          "description": "Strictly exogenous regressors. Accepts the same lag-operator syntax",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Dependent variable. Its first lag is added automatically -- do **not** pass a hand-built lag in ``x``.",
          "type": "string"
        }
      },
      "required": [
        "y"
      ],
      "type": "object"
    },
    "name": "xtlsdvc"
  },
  {
    "description": "Fit panel negative-binomial regression. Use model='fe' for explicit entity fixed effects via nbreg, or model='re' for a random-intercept NB-2 GLMM via menbreg. Do not use feols for negative-binomial outcomes. Validation: certified parity evidence.",
    "input_schema": {
      "properties": {
        "cluster": {
          "description": "Cluster variable; defaults to entity for model=\"fe\".",
          "type": "string"
        },
        "entity": {
          "description": "Panel/unit identifier column.",
          "type": "string"
        },
        "exposure": {
          "type": "string"
        },
        "formula": {
          "description": "Formula such as 'count ~ x1 + x2'.",
          "type": "string"
        },
        "irr": {
          "default": false,
          "type": "boolean"
        },
        "model": {
          "default": "fe",
          "enum": [
            "fe",
            "re",
            "pooled"
          ],
          "type": "string"
        },
        "offset": {
          "type": "string"
        },
        "time": {
          "description": "Optional time column.",
          "type": "string"
        },
        "time_effects": {
          "default": false,
          "description": "Include time dummies in the fixed-effects model.",
          "type": "boolean"
        }
      },
      "required": [
        "formula",
        "entity"
      ],
      "type": "object"
    },
    "name": "xtnbreg"
  },
  {
    "description": "Yatchew differencing test that E[y | x] is a polynomial of ``order``. Validation: validated evidence tier (known-truth, reference, external-parity, or Monte Carlo artifact).",
    "input_schema": {
      "properties": {
        "het_robust": {
          "default": true,
          "description": "Use the heteroskedasticity-robust statistic of the paper's Appendix E. ``did_had`` always does.",
          "type": "boolean"
        },
        "order": {
          "default": 1,
          "description": "Polynomial order under the null. 1 = linear, 0 = constant.",
          "type": "integer"
        },
        "x": {
          "description": "Running variable and outcome, same length.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Running variable and outcome, same length.",
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "required": [
        "x",
        "y"
      ],
      "type": "object"
    },
    "name": "yatchew_linearity_test"
  },
  {
    "description": "Nonparametric causal decomposition of a group disparity. Validation: certified parity evidence.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Two-sided coverage level.",
          "type": "number"
        },
        "cluster": {
          "description": "Column name to use for cluster bootstrap.",
          "type": "string"
        },
        "group": {
          "description": "Binary group indicator (0/1) -- ``1`` = advantaged / index group.",
          "type": "string"
        },
        "inference": {
          "default": "bootstrap",
          "description": "``\"bootstrap\"`` returns SEs and percentile CIs from the non-parametric (cluster-aware) bootstrap. ``\"analytic\"`` (only with ``method=\"efficient\"``) returns the efficient-influence-function SEs and Wald CIs of ``cdgd``. ``\"none\"`` skips inference.",
          "enum": [
            "bootstrap",
            "analytic"
          ],
          "type": "string"
        },
        "method": {
          "default": "plugin",
          "description": "``\"plugin\"`` uses within-cell OLS for outcomes and within-group logit for the propensity and computes plug-in expectations. ``\"efficient\"`` is the efficient-influence-function estimator of the authors' ``cdgd`` package (``cdgd0_manual``) on those same nuisance fits: Hajek-normalised doubly robust potential outcomes, with selection the residual so the components add up exactly -- recommended when nuisance functions might be misspecified.",
          "enum": [
            "plugin",
            "efficient"
          ],
          "type": "string"
        },
        "n_boot": {
          "default": 499,
          "description": "Number of bootstrap replications.",
          "type": "integer"
        },
        "seed": {
          "default": 12345,
          "description": "Random seed for reproducible stochastic steps.",
          "type": "integer"
        },
        "treatment": {
          "description": "Binary treatment indicator (0/1).",
          "type": "string"
        },
        "trim": {
          "default": 0.005,
          "description": "Lower/upper clip for fitted propensities (only used in ``method=\"efficient\"``).",
          "type": "number"
        },
        "x": {
          "description": "Adjustment covariates (used to identify within-group CATEs).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Name of the (continuous) outcome column.",
          "type": "string"
        }
      },
      "required": [
        "group",
        "treatment",
        "x",
        "y"
      ],
      "type": "object"
    },
    "name": "yu_elwert_decompose"
  },
  {
    "description": "Bauer-Sinning (2008) nonlinear Oaxaca-Blinder decomposition with",
    "input_schema": {
      "properties": {
        "group": {
          "description": "Group or cohort identifier.",
          "type": "string"
        },
        "model": {
          "default": "logit",
          "description": "Model variant or parameterisation to fit.",
          "enum": [
            "logit",
            "probit"
          ],
          "type": "string"
        },
        "reference": {
          "default": 0,
          "description": "reference parameter (int).",
          "enum": [
            "0",
            "1"
          ],
          "type": "integer"
        },
        "variant": {
          "default": "yun",
          "description": "variant parameter (str).",
          "type": "string"
        },
        "x": {
          "description": "Primary running variable, regressor, or feature input for this estimator.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "group",
        "x",
        "y"
      ],
      "type": "object"
    },
    "name": "yun_nonlinear"
  },
  {
    "description": "Zero-first-stage (ZFS) test of the exclusion restriction, plus van Kippersluis-Rietveld (2018) pleiotropy-robust correction. Estimates the instrument's direct effect on the outcome in a subsample where it has no first stage, reports the implied bias in the main-sample IV estimate, and returns the corrected point estimate with a cluster-bootstrap interval. Accepts the same exog / absorb / cluster spec as sp.iv, so the test runs on the specification actually fitted.",
    "input_schema": {
      "properties": {
        "absorb": {
          "description": "Fixed effects absorbed in every component regression",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "cluster": {
          "description": "Cluster identifier column for clustered standard errors.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "endog": {
          "description": "Endogenous regressor",
          "type": "string"
        },
        "exog": {
          "description": "exog parameter (list[str] | str).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "instrument": {
          "description": "The single excluded instrument being tested",
          "type": "string"
        },
        "n_boot": {
          "default": 999,
          "description": "Bootstrap reps for the corrected estimate (0 to skip)",
          "type": "integer"
        },
        "random_state": {
          "description": "Random seed or RandomState for reproducible stochastic steps.",
          "type": "integer"
        },
        "y": {
          "description": "Outcome column",
          "type": "string"
        },
        "zfs": {
          "description": "Boolean column/mask marking the zero-first-stage subsample where the instrument is believed inert",
          "type": "string"
        }
      },
      "required": [
        "endog",
        "instrument",
        "y",
        "zfs"
      ],
      "type": "object"
    },
    "name": "zero_first_stage"
  },
  {
    "description": "Zero-Inflated Negative Binomial (ZINB) regression via MLE. Validation: certified parity evidence.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "cluster": {
          "description": "Cluster variable name.",
          "type": "string"
        },
        "formula": {
          "description": "Patsy-style formula for the count equation.",
          "type": "string"
        },
        "inflate": {
          "description": "Inflation-equation regressors. Default: same as count regressors.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "maxiter": {
          "default": 200,
          "description": "maxiter parameter (int).",
          "type": "integer"
        },
        "robust": {
          "default": "nonrobust",
          "description": "Standard error type.",
          "type": "string"
        },
        "tol": {
          "default": 1e-08,
          "description": "Numerical convergence tolerance.",
          "type": "number"
        },
        "x": {
          "description": "Count-equation regressors.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Dependent variable name.",
          "type": "string"
        }
      },
      "required": [],
      "type": "object"
    },
    "name": "zinb"
  },
  {
    "description": "Zero-Inflated Poisson (ZIP) regression via MLE. Validation: certified parity evidence.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals.",
          "type": "number"
        },
        "cluster": {
          "description": "Cluster variable name for clustered standard errors.",
          "type": "string"
        },
        "formula": {
          "description": "Patsy-style formula for the count equation, e.g. \"y ~ x1 + x2\".",
          "type": "string"
        },
        "inflate": {
          "description": "Inflation-equation regressors. Default: same as count regressors.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "maxiter": {
          "default": 200,
          "description": "Maximum iterations for optimizer.",
          "type": "integer"
        },
        "robust": {
          "default": "nonrobust",
          "description": "\"nonrobust\", \"HC0\", \"HC1\", etc.",
          "type": "string"
        },
        "tol": {
          "default": 1e-08,
          "description": "Convergence tolerance.",
          "type": "number"
        },
        "x": {
          "description": "Count-equation regressors (alternative to formula).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Dependent variable name (alternative to formula).",
          "type": "string"
        }
      },
      "required": [],
      "type": "object"
    },
    "name": "zip_model"
  }
]
